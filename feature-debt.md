# Feature Debt & Plan of Attack

Consolidated from planning documents, verified against the codebase as of 2026-05-10. Completed specs live in `planning-archive/`. Documents left in the project root describe work that is still outstanding — each is referenced in the relevant section below.

Last audited: 2026-05-15. Items marked DONE were verified against the codebase and docs in that audit.

## Situation report (2026-05-10)

**Posture:** Strong shipped state (~v5.36.0 baseline). All critical/security bugs resolved across three audit passes (100+ items in FIX-PROGRAMME). Design system fully standardised. Core product loop (write → publish → pay → read) functional end-to-end. Universal feed (RSS, Nostr, Bluesky, Mastodon, email newsletter ingest + outbound cross-posting) operational. Trust graph Phases 1–2 + 4 live. Relay outbox programme complete (all 6 phases shipped, §60 closed 2026-04-23).

**Workspace experiment:** Merged to `master` on 2026-05-29 (fast-forward). All slices (1–34), refactoring pass, hardening pass, card behaviour Phases 1–3, and full codebase review (REVIEW-PLAN.md, 24 sessions, 300+ diagnoses) are now on `master`.

**Biggest product gaps (launch-blocking or near):**

1. Subscription auto-renewal — the single most critical missing feature per gap analysis
2. ~~CI pipeline~~ — DONE (`.github/workflows/ci.yml`: build, ESLint, Knip, Vitest, `next lint`, `npm audit`)
3. Email-on-publish — trivial implementation, high writer-facing value
4. Owner dashboard — operator visibility before taking real money
5. Landing page — no social proof, no screenshots, no feature explanation

**Audit status:**
| Area | Status |
|------|--------|
| Admin/design system (13 items) | All resolved |
| Universal feed (34+ items, 3 passes) | All resolved |
| Consolidated fix programme (100+ items) | All P0/P1/P2 resolved |
| Frontend audit (12 items) | 6/12 resolved |
| Subscriptions gap analysis | Phase 1 complete, Phase 2 not started |

**Pending architectural decisions:**

- Workspace experiment: merge to master or retire?
- Trust Phase B (dual-graph anonymity): gated on >1k attestors

---

Last worked: 2026-06-14. **Merged Messages inbox (notifications + DMs), global type-size control, topbar retirement on `/about` + `/admin`, ForallMenu Log out.** Four threads from one session. **(1) Merged Messages surface:** notifications and DMs now share one Glasshouse — `MessagesOverlay` → new `MessagesInbox` (`web/src/components/messages/MessagesInbox.tsx`), three columns (notifications log left / conversation list middle / reading pane right, the narrower column), resizable Glasshouse, responsive (side-by-side desktop, horizontal snap-pager mobile). A `new_message` notification selects the conversation in place via `NotificationsPanel`'s new `onMessageActivate` prop (no route-away). The two ForallMenu rows collapse into a single **Messages** row, badge = `dmCount + notificationCount`. `?overlay=notifications` + `/notifications` shim redirect into it. **Retired:** `NotificationsOverlay`, `stores/notificationsOverlay.ts`, and the two-pane `MessagesPanel` (deleted, unreferenced). **(2) Global type size:** per-device control in Settings — `stores/typeScale.ts` (4 steps, localStorage `ah:type-scale`) scales the root `html` font-size; headless `TypeScaleHydrator` applies on boot (mounted in `LayoutShell`, mirrors `PaletteHydrator`); UI `account/TypeSizeControl`. Scales all rem-based tokens. **(3) Topbar retirement:** `/about` + `/admin/*` added to `LayoutShell`'s `chromeless` (`chromelessRoute`); the black `<Nav>` topbar is now reserved for the logged-out marketing/auth register (`/`, `/auth`). `/admin/reports` gained a `← Workspace` link (its only nav was the topbar). **(4) ForallMenu Log out:** the workspace has no topbar, so the ∀ menu now carries the sole in-workspace sign-out row. **Validated:** web `tsc --noEmit` clean (full), root promise-safety lint **0 errors** on touched files, `scripts/check-hairlines.sh` clean. **NOT browser-verified** (`web` is a baked prod image; needs `docker compose build web && up -d web`). **New debt:** the type-size control does **not** scale feed-card byline/dialogue text — the card chassis pins those to `text-[11px]`/`text-[14.5px]` (px), so they need migrating to rem tokens (which also means amending the chassis spec in CLAUDE.md). Docs: this entry + CLAUDE.md (merged-inbox invariant, topbar-reserved note, global-type-size note).

Previously: 2026-06-14. **External subscriptions made feed-derived; standalone Subscriptions page retired.** An `external_subscriptions` row is now a projection of feed membership (exists iff the source is in ≥1 of the owner's feeds), maintained only by `addSource`/`removeSource` in `gateway/src/routes/feeds.ts` — `addSource` upserts the sub (+ atproto backfill job + `markFollowListDirty` for nostr) in-txn; `removeSource` tears it down (+ orphans the source, + kind-3 dirty) when the source leaves the owner's *last* feed, owner-scoped `pg_advisory_xact_lock` guarding the read-then-write. **Retired:** the external-subscription CRUD (`POST /feeds/subscribe`, `GET /feeds`, `DELETE/PATCH /feeds/:id`, `POST /feeds/:id/refresh`) and the dead `post-feed.ts` `GET /feed/:feedId` projector are deleted (`external-feeds.ts` keeps only the admin instance-health route); the web `SubscriptionsPanel`/`SubscribeInput`/`SubscriptionsOverlay`/`subscriptionsOverlay` store + the ForallMenu row + the `feeds` external-CRUD client are gone; `/subscriptions` is a redirect shim to `/workspace`. **Migration 116** drops per-sub `is_muted`/`daily_cap` (per-feed mute is `feed_sources.muted_at`; no per-feed daily cap), deletes legacy floating subscriptions, and re-marks affected owners' kind-3 dirty. **UI:** the hover-card Follow on an external byline adds the source to the *current* feed (`feedId` threaded `CardContext`→`PostByline`→`AuthorModal`→`FollowButton`; membership derived from `listSources`, matched on `followTarget.sourceId` now returned by author-card); omitted where there's no feed context. **Validated:** gateway+web `tsc` clean, schema-drift guard 3/3 green (116 round-trips clean + seeded). **NOT browser-verified** (needs `docker compose build web && up -d web`). **Deferred:** `email` external sources have no Follow affordance (addSource can't take email yet); adding a source to a feed via Follow doesn't force an immediate feed-content refresh (appears on next feed load). Spec: UNIVERSAL-FEED-ADR addendum (2026-06-14); invariant in CLAUDE.md.

Previously: 2026-06-13. **Deep audit of the last week's commits (~75 commits, 280 files) + three fixes.** Five parallel review agents swept the five subsystems shipped this week (feed-retirement Slices 0–7, network-concierge Phases 0–3, workspace/Glasshouse overlays, mobile layout, Nostr outbound interop). **No CRITICAL/HIGH bugs** — the risky invariants all held under inspection (OAuth CSRF fix, token-refresh custody branching, discovery double-gating, SSRF via `safeFetch`/`pinnedWebSocketOptions`, schema-drift discipline, feed-retirement deletions with zero dangling imports, "one post per card"). Three fixes applied: **(1) MEDIUM — atproto cross-account session clobber:** `network_presences` uniqueness was reduced to `(account_id, protocol)` in migration 109, but `atproto_oauth_sessions` is keyed solely by DID (`ON CONFLICT (did)`), so a second account linking the same Bluesky DID would overwrite the first's shared OAuth session and let the outbound worker post under it. **Migration 115** adds `unique (protocol, external_id) where external_id is not null`; both OAuth callbacks (`linked-accounts.ts`) now catch the `23505` and redirect `?linked=already-linked` (banner added in `SettingsPanel`) instead of clobbering. **(2) LOW — `/account/export` rate limit was per-IP:** behind nginx every request shares the proxy IP, so the 5/hour bucket was global (one exporter DoSes everyone) and a stolen cookie from a fresh IP got its own bucket; added a `keyGenerator` (re-derives session via `verifySession`, since the limiter runs at `onRequest` before `requireAuth`) keying on `export:acct:<sub>`. **(3) LOW — palette `hydrate` trusted stored hex blindly:** a corrupt/hand-edited localStorage value collapsed to `0 0 0` (silent black) via `hexToTriple(NaN)`; `hydrate` now gates each value through `normalizeHex`. **Validated:** schema drift guard 3/3 green (migration 115 round-trips clean + seeded), gateway+web `tsc` clean, gateway `boot.test.ts` passes, hairline tripwire clean on touched web files. **NOT browser-verified.** **Deploy note for 115:** build fails if two prod rows already share `(protocol, external_id)` — run `SELECT protocol, external_id, count(*) FROM network_presences WHERE external_id IS NOT NULL GROUP BY 1,2 HAVING count(*)>1` before applying. Lower-priority audit findings left as tracked nits (native note-replies hard-dropped regardless of per-source `exclude_replies` — pre-existing; profile self-delete dropped — documented scope cut; stale `header`-slot comment in `PostCard`; dead `reply_group` type in `ndk.ts`; `TagBrowser` not on `PageShell`).

Previously: 2026-06-12. **FEED-RETIREMENT-PLAN Slice 6 item 4 shipped — the plan is COMPLETE.** The workspace items path converged onto the unified `Post[]`, ranking preserved. The original "converge onto the post-feed projector" framing conflated two axes; the operator decision was to converge the **duplication** (candidate SQL + row mapper) and **keep the ranking** (`effective_score` = volume weight × sampling mode is a shipped feature, not debt — adopting §5 hotness would have silently dropped the FeedComposer volume bar + chronological/scored/random modes). So §5 hotness is **not** applied to composed vessels. **Gateway:** `sourceFilteredItems`/`placeholderExploreItems`/the (caller-less) `/feeds/:id/saves` listing now `SELECT ${FEED_SELECT}${POST_SELECT}` via the shared `feed-sql.ts`+`post-mapper.ts` and map through `feedItemToPost` → `Post[]`; the inline `FEED_SELECT`/`FEED_JOINS` copies + bespoke `rowToItem`/`computeBiddabilityTier` in `feeds.ts` are deleted; the `effective_score` CASE + format-tagged cursor stay workspace-specific. `feedItemToPost` grew `dTag`/`pricePence`/`externalSourceId` (promoted from the browser's client-transitional set → every Post[] surface carries them — this also fixed a latent bug: `WorkTab`'s native-article reader-open read `post.dTag`, which the gateway never set). **Web:** `WorkspaceView` consumes `Post[]` directly (`VesselState.items: Post[]`; `mapApiItem`/`mapExternalApiItem` + the dead `new_user`/`reply_group` branches removed); the client adapter `lib/post/map-feed-item.ts` (+test) and the orphaned `NewUserVesselCard`/`ReplyGroupCard` are deleted; `WorkspaceFeedApi*` item types gone (`items`/`saves` responses are `Post[]`); client `Post` gained `externalSourceId?`. No DDL. **Validated:** gateway+web `tsc` clean, root lint **0 errors**, gateway tests 117/117 (boot is the cold-start 5s flake — passes at 30s), hairlines clean on touched files; both converged queries exercised against the dev DB (columns resolve, valid Posts). **NOT browser-verified** (needs `docker compose build web && up -d web`): a composed vessel renders/threads/quotes, article cards open the reader, the volume bar still reorders. Granular log: `docs/adr/FEED-RETIREMENT-PLAN.md` §0.

Previously: 2026-06-12. **FEED-RETIREMENT-PLAN Slice 7 shipped — legacy card stack deleted; the plan is effectively complete.** `git rm`'d the `components/feed/` card stack (`FeedView`/`ExternalCard`/`ArticleCard`/`NoteCard`/`QuoteCard`/`NeighbourhoodCard`/`ActionSheet`) + the now-orphaned `hooks/useNeighbourhood.ts` (its only importers were `ExternalCard`/`NeighbourhoodCard`). **`AuthorModal.tsx` stays** in `components/feed/` — the one surviving member, shared by `post/PostByline` + `workspace/FeedComposer`, exactly per the acceptance grep. **`SubscribeInput` relocated, not deleted** (Slice 5 carry-forward — it's the omnivorous subscribe field, now `SubscriptionsPanel`'s sole importer): `git mv` → `components/subscriptions/SubscribeInput.tsx` (same `../../` depth, only import was `../../lib/api` — stayed valid); cleared its 3 pre-existing hairlines while moving it. **`FeedDial` deleted, not just orphaned** — a dead control whose only effect was writing the now-inert `feedReach` localStorage key (its sole reader `FeedView` was gone) + firing a `feedReachChanged` event with no listeners; removed its `NetworkPanel` import + render (reach lives as composable sources now). **`lib/api/feed.ts` kept** (it also holds the live `replies` API) — stripped only the `feed` export + `FeedReach` type, both used solely by `FeedView`/`FeedDial`. **Validated:** `grep -r "components/feed/" web/src` → only `AuthorModal`; web `tsc` clean, root lint **0 errors**, hairlines clean on touched files; gateway untouched. **NOT browser-verified** (needs `docker compose build web && up -d web`). **Only remaining in the plan:** Slice 6 item 4 (converge `GET /workspace/feeds/:id/items` onto the post-feed projector — `effective_score`-vs-§5-hotness ranking implications) as the tracked non-blocker. Granular log: `docs/adr/FEED-RETIREMENT-PLAN.md` §0.

Previously: 2026-06-12. **FEED-RETIREMENT-PLAN Slice 6 shipped — backend untangle (items 1–3).** Extracted the shared feed SQL (`FEED_SELECT`/`FEED_JOINS`/`parseCursor`/`CursorParts`) out of `timeline.ts` into a new `gateway/src/lib/feed-sql.ts`, repointing **five** importers (`post-feed`, `post-thread`, `author`, `sources`, `tags` — the plan said four; `tags.ts` joined in Slice 4). With the SQL moved and the `/feed` shim already live, the legacy `GET /feed` reach handler reduced to nothing, so `timeline.ts` was **deleted whole** (route + `followingFeed`/`exploreFeed`/`timelineRoutes`); `index.ts` register + `boot.test.ts` import updated, and every stale `timeline.ts` comment reference across the gateway refreshed to point at `feed-sql.ts`. Also deleted `GET /conversation/:eventId` + its `ConversationNode` interface from `replies.ts` (no frontend caller). **Correction to the plan:** `feedItemToResponse`/`computeBiddabilityTier`/`UNBOUNDED_SCORE` were **not** extracted — they were the legacy handler's row→response mapper and died with it (the Post-model callers map via `lib/post-mapper.ts`); extracting them would have preserved dead code. **Item 4 deferred** (converging `GET /workspace/feeds/:id/items` onto the post-feed projector — real `effective_score`-vs-§5-hotness ranking implications) as the plan's tracked non-blocker. **Validated:** gateway `tsc` clean, root lint 0 errors, **117/117** gateway tests, hairlines clean on all touched files (net −706 lines). **Remaining:** Slice 7 (delete the `components/feed/*` card stack — keep `AuthorModal`/`useAuthorCard`; **relocate `SubscribeInput`, don't delete**; remove `lib/api/feed.ts`'s `feed`/`FeedReach`). Granular log: `docs/adr/FEED-RETIREMENT-PLAN.md` §0.

Previously: 2026-06-12. **FEED-RETIREMENT-PLAN Slices 1–5 shipped** (`8aecbda`, `76c2f2d`, `034536b`, `dddad86`, `a3416d8`). The legacy `/feed` model is retired down to its remaining backend-untangle + deletion passes: `/feed` is now a redirect shim and the logged-in front door lands on `/workspace`. Slice 1 ported `SourceSurface`, Slice 2 the native profile tabs (`WorkTab`/`SocialTab`, replies via a dedicated `GET /author/:id/replies`), Slice 4 the tag surface (`GET /tags/:name/posts`) + `/search`→shim — all rendering the one Post-model card path. **Slice 3** (commit `dddad86`) closed the front door and landed the Slice-0 product decision: migration 114 adds a `reach` `feed_sources` kind (`reach_kind` following|explore) — global reach is now a composable source, wired through `feeds.ts` + FeedComposer quick-add chips, with membership computed in `sourceFilteredItems`' `matched` CTE (not the post-feed projector — different shape). New accounts seed a clone of any operator-flagged `is_starter_template` feed via an idempotent zero-feeds guard in `GET /workspace/feeds`. **Operator prereq: `UPDATE feeds SET is_starter_template=true WHERE id='<uuid>'` — seeding is a no-op until ≥1 feed is flagged.** **Slice 5** (commit `a3416d8`) retired the four Bucket-B standalone routes into workspace Glasshouse overlays on the standard retired-route pattern: `/library` (+ `/bookmarks`/`/history`/`/reading-history`) → LibraryOverlay (article rows open the reader in place), `/network` (+ `/followers`) → NetworkOverlay (`?tab=` preserved), `/subscriptions` → a thin SubscriptionsOverlay (reuses `SubscribeInput`), `/profile` → folded into Settings as `ProfileSection`; all four old routes are now redirect shims and the Nav Profile/Library links repoint to the deep-links (−788 lines). Validated: schema drift guard green, gateway+web `tsc` clean, root lint 0 errors, 117 gateway tests, hairlines clean on touched files; reach + clone SQL exercised against the dev DB. **NOT browser-verified** (needs `docker compose build web && up -d web`). **Remaining:** Slice 6 (extract `timeline.ts` SQL → `feed-sql.ts`, delete legacy `GET /feed` + `GET /conversation/:eventId`), Slice 7 (delete the `components/feed/*` card stack — keep `AuthorModal`/`useAuthorCard`; **relocate `SubscribeInput`, don't delete** — it's now SubscriptionsPanel's sole importer once `FeedView` goes). Granular per-slice log lives in `docs/adr/FEED-RETIREMENT-PLAN.md` §0.

Previously: 2026-06-12. **Two-day-review bug fixes (`8410b57`) + FEED-RETIREMENT-PLAN authored (`fbccb1b`).** All §1 review findings substantiated against the codebase and fixed: mobile resume now survives hiding the active feed (persist only when the displayed feed is the chosen one); palette devtool `hydrate` filters persisted overrides to live registry slugs (registry filter, not `sanitizeThemeOverrides` — the devtool legitimately holds locked slugs); `NetworkReachPanel` pane conventions (`bg-white` fields, `grey-600` secondary); `PostMedia` scrim → `var(--ah-true-black-rgb)`; gateway `FEED_SCHEME_IDS` reordered to mirror `tokens.ts`. **Validated:** web+gateway `tsc` clean, root lint 0 errors on touched files, hairline tripwire clean, 117 gateway tests green. **NOT browser-verified** (needs `docker compose build web && up -d web`): mobile hide-active-feed → un-hide resume; Palette panel preset match after a stale-slug blob; NetworkReachPanel fields. Then fleshed the §4 retirement assessment into **`docs/adr/FEED-RETIREMENT-PLAN.md`** (slices 0–7: reach decision, SourceSurface + profile-tab ports, front-door repoint, tag/search, four Bucket-B overlays, backend untangle, deletion manifest). Re-verification corrected the assessment on four points (recorded in the §4 entry): the profile overlay is a *second* live workspace dependency on legacy cards; post-login still lands on `/feed`; `GET /feed/:feedId` already serves following/explore as Post[] with zero callers; `timeline.ts` has four importers. **Open:** Slice 0 product decision (reach as composable source / seeded default feeds / drop) — operator's call; plan recommends seeded feeds now, composable source later.

---

Last worked: 2026-06-11 (seventh pass). **Mobile workspace — single-feed paging + rank/hide as data (MOBILE-LAYOUT-ADR, all five §VIII slices).** The canvas's pointer-spatial paradigm gets its touch counterpart: at ≤767px (`useIsMobile`) the workspace renders `MobileWorkspace.tsx` — thin bar (wordmark · numeral indicator strip · ∀ docked via new `ForallMenu anchor="bar"`) over one full-bleed feed, paged horizontally in rank order. **Data spine (slice 1):** migration 113 adds `feeds.sort_rank` (backfilled per-owner in `created_at` order) + `feeds.hidden`; `schema.sql` regenerated mechanically (drift guard 3/3 green; dev `_migrations` ledger reconciled first — 9 already-applied rows 098–106/111 were never recorded). Numeral derivation moved from `createdAt` index to `sort_rank` over **visible feeds only** (§V unification: desktop badges renumber for users with hidden feeds — intended). Hide rewired from per-device localStorage to the feeds PATCH, with a one-time bootstrap push of legacy local flags (cleared only after the PATCH lands, so a stale flag can never re-hide a feed unhidden elsewhere); `stores/workspace.ts` `setVesselHidden` → `clearLegacyHidden`. **Reorder (slice 2):** `PUT /workspace/feeds/order` rewrites ranks in full (full-set required; 409 → client refetch); FeedComposer drag-to-rank list (pointer events, not HTML5 DnD, so it works on touch; arrow keys for a11y; hidden feeds listed unnumbered). **Shell + pager (slices 3–4):** Glasshouse gains a full-screen-sheet mode at the breakpoint (placement/drag/resize don't render; modal semantics untouched) so every overlay got its mobile form for free; compose rides the existing `useCompose` → workspace `Composer` bridge. Pager: all visible pages mounted in a translated track, axis-locked claim (≥12px decisively horizontal, mostly-vertical rejects for good, never claims from inside a horizontally-scrollable element or the pip — `PipTrigger` now carries `data-pip-trigger`), rubber-band at ends, fling commit, tap-to-jump strip, resume by feed `id` (`ah:mobile-feed:<userId>`). Desktop and mobile render cards through one extracted `WorkspaceView.renderFeedContents` path. **Appearance (slice 5):** density persists as a second `feeds.appearance` JSONB key (no DDL; `FEED_DENSITIES` mirrors `tokens.ts` `Density`; same server-plus-local-mirror precedence as the scheme), FeedComposer gains a hide toggle, tapping the **active** strip numeral opens the feed's settings sheet, orientation suppressed on mobile. **Validated:** gateway `tsc` + web `tsc`/`next build` green, root lint 0 errors, hairline tripwire clean, reorder/hide SQL smoke-tested in a rolled-back transaction against dev, migration applied to dev DB, images built. **NOT browser-verified** — needs `docker compose up -d gateway web`, then: device-emulation swipe/axis-lock/pip precedence, drag-to-rank in FeedComposer, hide → renumber on both surfaces, density cross-device, resume-by-id. Docs rolled forward: ADR status (Implemented + build notes), CLAUDE.md (scheme rule extended to density/hidden/sort_rank + new Mobile workspace section), §3 residue line, this entry.

---

Last worked: 2026-06-11 (sixth pass). **Per-feed colour schemes + settings preset themes (§3 "palette productisation" shipped, both tiers v1).** Per-feed: FeedComposer's Light/Dark toggle replaced by a curated swatch row (`SchemePicker` — walls strip + interior ground + card chip mini per scheme, 2px selection outline) over six schemes: the hand-tuned `primary`/`dark` reference palettes + `blush`/`sage`/`sand`/`slate`, each defined as **surfaces only** (12 new registry entries + `:root` pairs, 3 per scheme; bar shares walls). `tokens.ts`: `Brightness` widened to `FeedScheme` (legacy alias + persisted field name kept so old localStorage layouts read forward), `deriveVesselPalette` derives every text colour by luminance (PalettePanel-matched weighting) between the two reference ramps, flips crimson→crimson-soft on dark cards, derives bar wells as `color-mix` washes of the walls var (so devtool live-tuning keeps tracking); `VesselPalette.isDark` added (`isDarkPalette` reads it); `nextBrightness` removed. **Storage is server-side feed character**: migration 112 `feeds.appearance` JSONB (+ `schema.sql` regenerated mechanically from a throwaway migrated DB, drift guard 3/3 green; applied to the live dev DB), PATCH `/workspace/feeds/:id` now takes `{ name?, appearance? }` with JSONB **merge** and enum validation (`FEED_SCHEME_IDS` mirrors tokens.ts), `appearance` on every feed response. WorkspaceView bootstrap reconciles the server scheme into the layout store (server wins; the legacy per-device `brightness` stays as cache + fallback for never-picked feeds — no forced migration, a pick writes both). Global tier: Settings **Theme** section (`ThemeSection.tsx` + `web/src/lib/palette/themes.ts`) — 4 curated presets (Standard/Warm/Cool/High contrast) applied through the devtool override store (new `setOverrides`; persists in `ah:palette-overrides`, hydrates on boot via the globally-mounted PalettePanel); `THEME_LOCKED_SLUGS` (glasshouse, ink-925, trust pips) filtered on apply so the way home never re-themes; hand-tuned devtool state reads as "Custom". **Validated:** web `tsc` + Next build green, gateway `tsc` clean, root lint 0 errors, hairline tripwire clean for authored lines (sole hit = pre-existing globals.css comment), schema drift guard green. **NOT browser-verified** — needs `docker compose build web gateway && up -d web gateway`, then: pick schemes on two feeds (vessel repaints live; survives reload), slate (dark) feed text legibility incl. bar wells, settings presets repaint the ground + Standard reverts, ∀/Glasshouse unaffected by any theme.

---

Last worked: 2026-06-11 (fifth pass). **Canonical colour registry + temporary Palette live-tuning kit.** Every colour the frontend renders now routes through `:root` CSS-variable pairs (`--ah-<slug>` / `--ah-<slug>-rgb`), generated from the ordered registry `web/src/lib/palette/registry.ts` (38 entries, one per distinct hex) — Tailwind tokens consume the triples (alpha modifiers intact), workspace `PALETTES`, `globals.css`, and ~40 component files' inline hexes swept to `var()` refs. Contexts that can't resolve `var()` handled individually: ForallMenu ∀ glyph + PostMedia play icon moved from SVG presentation attributes to `style`; `ProvenanceBar` canvas reads computed triples at draw time; Stripe iframe / Google logo / print CSS exempt (see CLAUDE.md › Colour values). **TEMPORARY KIT — REMOVE once the colour scheme is finalised:** ForallMenu "Palette" row opens a non-Glasshouse floating panel (`web/src/components/devtools/PalettePanel.tsx` + `web/src/stores/paletteDevtool.ts`): swatch per registry entry, 2D HSV spectrum picker, copyable positional identifier list, live var rewriting with `localStorage` persistence (`ah:palette-overrides`). Removal = delete those two files + the ForallMenu row + the LayoutShell mount; the registry/var indirection stays. **Validated:** Next build green, root lint 0 errors, hairline tripwire clean for authored lines (flagged PipPanel/NewFeedPrompt 1px borders are pre-existing debt). Needs `docker compose build web && up -d web`.

---

Last worked: 2026-06-11 (fourth pass). **Workspace Composer: per-send cross-post pills restored + ⌘K/Ctrl+K hotkey.** (1) The Composer had lost its per-send network toggles — any valid linked account cross-posted unconditionally. Restored as `.toggle-chip` pills (BLUESKY / MASTODON) in the footer left of Publish, **seeded from `network_presences.cross_post_default`** (the "Default on" checkbox in Reach other networks — DB default TRUE, so out-of-box behaviour is unchanged and that setting now actually governs the composer); pills render for plain notes only (replies/quotes publish through their own paths, no cross-post in this composer). `PROTOCOL_LABELS.activitypub` renamed ACTIVITYPUB→MASTODON to match `ComposeOverlay`. (2) Nav's global ⌘K/Ctrl+K compose hotkey never fired in the workspace (Nav is unmounted when chromeless) — `WorkspaceView` now registers the same listener opening its local Composer, no-oping while `useEditorOverlay.isOpen` (the Glasshouse supersede rule would close the article editor under the writer). **Validated:** web `tsc --noEmit` clean, 135/136 web tests green — the 1 failure (`tests/collision.test.ts` "pushes upward when overlap is smaller from bottom") is **pre-existing on a clean tree** (verified via stash), unrelated vessel-collision math; flagged as debt, not absorbed. Hairlines + root lint clean. Needs `docker compose build web && up -d web`.

---

Last worked: 2026-06-11 (third pass). **Composable feeds no longer hide the reader's own posts (`sourceFilteredItems` self-exclusion removed).** Found via the Phase-3 live test: a native note that crossposted fine never appeared in the author's own workspace feed despite an explicit full-volume self-source. Cause: `gateway/src/routes/feeds.ts` `sourceFilteredItems` carried `AND (fi.author_id IS NULL OR fi.author_id != $1)` — copy-inherited from the explore query at slice 16 (git: `20fee7f`), already patched once (`5054815`, the `IS NULL OR` rescue for external items, which is why an ingested *external echo* of your own post would have shown while the native original stayed hidden). Never a product decision; it silently overrode explicit feed configuration. Fix: clause deleted (SQL comment records the reasoning) — membership in a composable feed is explicit (`matched` CTE requires a `feed_sources` hit), so own items now appear **iff** a source the reader added admits them (self as account source, their publication, a tag they post under); users with no self-source see no change. Explore keeps its self-exclusion in both query paths (`placeholderExploreItems`, `post-feed.ts::explorePostFeed`) — correct discovery semantics. **Validated:** gateway `tsc` clean, 117 tests green (one `boot.test` flake on first run, green in isolation + on rerun). Needs prod `docker compose build gateway && up -d gateway`.

---

Last worked: 2026-06-11 (same day, second pass). **Phase 3 ASSISTED Mastodon LIVE on prod — signup round-trip verified.** Flag flipped on prod and the full loop proven with a real account: authorize hand-off → mastodon.social → account created → email confirm (same browser) → login resumed the stored authorize URL → consent → `provenance='assisted'` presence linked. **UX finding:** the hand-off lands on `/auth/sign_in` whose signup affordance is a small form-footer text link the user couldn't find; the run completed via the direct `/auth/sign_up` URL, proving the stored-location resume survives the detour. `assistedMastodonConsent` copy updated to say you'll land on a login page and to name both the footer link and `${instance}/auth/sign_up`. **Cross-post also live-verified** (same day): note published with the MASTODON pill → `outbound_posts.status='sent'` + status URI on `mastodon.social` — the first live end-to-end exercise of the Mastodon outbound adapter (`postMastodonStatus`), closing the "never exercised live" caveat from the crosspost-parity assessment. **Nothing remaining on Phase 3.** Docs flipped to live/verified: ADR status + §9, CLAUDE.md, DEPLOYMENT.md, this entry.

---

Last worked: 2026-06-11. **NETWORK-CONCIERGE-ADR Phase 3 (ASSISTED activitypub) built dark behind `MASTODON_ASSISTED_ENABLED`.** The §9 spike resolved both Phase-3 unknowns: (1) `mastodon.social` has open, approval-free registration (live `/api/v2/instance`, v4.6-nightly) — so a curated-allowlist default exists (`MASTODON_ASSISTED_INSTANCES`, comma-separated, first = default); (2) the signup round-trip needs no instance cooperation — Mastodon's `/oauth/authorize` does `store_location_for(:user, request.url)` before bouncing to sign-in, email confirmation does *not* auto-sign-in, but `after_sign_in_path_for` resumes the stored authorize URL (verified against `mastodon/mastodon` main), so authorize → sign up → confirm email → log in → consent → shared callback, **same browser only** (session-bound; consent copy says so; a strayed user just retries "Set one up" and completes immediately). Build mirrors the Phase-2 S1–S6 seams: **gateway** `POST /linked-accounts/mastodon/assisted` (allowlist check → live registration guard, 409 closed / 502 unverifiable → app registration → `oauth_state_mastodon` cookie `provenance:'assisted'` + `userId`, **60-min TTL** — the cookie is the only flow state, no PKCE store); shared Mastodon callback threads `provenance` into the upsert (was hardcoded `'linked'`) + verifies optional `userId`; LINKED entry route gains `userId`/`provenance:'linked'` for parity; capabilities → `assistedMastodon` + `assistedMastodonInstances`; shared `buildMastodonAuthorizeUrl` helper. **Web:** `assistedMastodonConsent(instance)` (sets the email-confirm/same-browser expectation), `linkedAccounts.assistedMastodon(instance?)`, `NetworkReachPanel` Mastodon "Set one up" enabled with consent gate + toggle-chip instance picker (only when >1 configured), `InlineReplyBox` dual-path prompt extended to `activitypub` cards. **No dispatcher change** (custody: token in `credentials_enc`, identical to LINKED); **no migration** (the 111 provenance CHECK is protocol-agnostic). docker-compose gateway env + DEPLOYMENT.md rows added. **Validated:** gateway `tsc` clean + 117 tests green; web `tsc --noEmit` clean; root lint 0 errors; hairlines clean on touched files. **NOT live-verified** — needs `docker compose build gateway web && up -d`, `MASTODON_ASSISTED_ENABLED=1`, then one real signup round-trip (the S0-equivalent was source-verified + live instance probes, not a full account creation) before the prod flag flips. Docs rolled forward: ADR status + §6.1.1 S6 + §9 (spike findings + as-built) + §10, CLAUDE.md invariant line, this entry.

---

Last worked: 2026-06-10. **NETWORK-CONCIERGE-ADR §10 UI reframe built (frontend-only; concierge backend still gated).** Built the "now" slice of the concierge journey *shape* — Nostr working, atproto/AP honestly stubbed. **New** `web/src/components/account/NetworkReachPanel.tsx` ("Reach other networks") replaces `LinkedAccountsPanel`; **retired** `LinkedAccountsPanel.tsx` + `PrivacyPreferences.tsx` (both only imported by `SettingsPanel`). Per-network reach model: **Nostr** is a first-class root row with a `Public / Private` discovery toggle (the degenerate concierge, §7 — the two Nostr toggles relocated wholesale from `PrivacyPreferences`, follow-list sub-toggle nested under it and only shown when public); **Bluesky / Mastodon** keep `Link yours` (existing OAuth, verbatim) and add a **disabled "Set one up · soon"** concierge affordance carrying the custody/export honesty line (§10). Contextual entry point `InlineReplyBox` no-presence prompt is now **dual-path** ("Connect your {network} → Settings" + "Don't have one? all.haus can set one up for you — coming soon"). `SettingsPanel` swaps the panel in and drops the now-empty Privacy section. No backend/API/migration change — same `linkedAccounts` + `privacyPreferences` endpoints. **Validated:** web `tsc --noEmit` clean (lone pre-existing `globals.css` side-effect-import error only), root promise-safety lint **0 errors** on touched files (5 carried-over `no-explicit-any` warnings), `scripts/check-hairlines.sh` clean. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: Settings → "Reach other networks" (Nostr Public/Private toggle live, satellite "Set one up · soon" disabled); reply on a Bluesky/Mastodon card with no presence → dual-path prompt. **Remaining:** when Phase 2/3 clear §8.1, wire the two satellite concierge buttons to real provisioning (the only UI change left). Docs rolled forward: ADR status + §10 ("As built"), `NOSTR-OUTBOUND-INTEROP-ADR` §8.4 + `UNIVERSAL-FEED-ADR`/`WORKSPACE-FULL-VIEW-SPEC` component-rename pointers, this entry.

---

Last worked: 2026-06-10. **NETWORK-CONCIERGE-ADR Phases 0 + 1 built (ships dark; Phase 2/3 gated).** Implemented `docs/adr/NETWORK-CONCIERGE-ADR.md` — generalises all.haus from "custodial Nostr + link other networks" to "custodial **Nostr root** + optional satellite presences." **Phase 0 (foundation):** **migration `109_network_presences.sql`** renames+extends `linked_accounts` → `network_presences` (rename `external_handle`→`handle`, `instance_url`→`service_url`; add `provenance` `linked|concierge` + `lifecycle_state` `provisioning|active|suspended|deprovisioned` with CHECKs; swap unique `(account_id,protocol,external_id)` → `(account_id,protocol)` — one presence per network, v1; rename carried-over pkey/fk/indexes/trigger). Chose **rename-in-place** over new-table-and-drop so the `outbound_posts.linked_account_id` FK + indexes auto-follow. Refactored all 6 touchpoints (`gateway/src/routes/{linked-accounts,external-items}.ts`, `gateway/src/lib/outbound-enqueue.ts`, `feed-ingest/src/tasks/{outbound-cross-post,outbound-token-refresh}.ts`, `shared/src/lib/crypto.ts` comment); enqueue+dispatch now gate on `lifecycle_state='active' AND is_valid`, and the atproto token-refresh path is scoped to `provenance='linked'` (concierge has no OAuth session). **Export-mandatory (invariant 4):** new gated `POST /api/v1/keypairs/export` in key-custody (`exportSecretKey` → hex + nsec, buffers zeroed, key never logged) + client wrapper; `/account/export` now ships the nsec — closing the standing contradiction where `ExportModal` advertised "Keys… everything you need to migrate" but the bundle never carried the private key. **Phase 1 (Nostr public presence):** **migration `110_accounts_discovery_enabled.sql`** adds `accounts.discovery_enabled` (opt-in, default false); it now composes with the operator master switch `DISCOVERY_PUBLISH_ENABLED` — `republish*`/`runDiscoverySweep`/`markFollowListDirty` all gate on it (`retractFollowList` exempt, as opt-out cleanup), so new signups no longer auto-publish to the mesh. `privacy-preferences` route handles both prefs (retract on opt-out, arm the backfill sweep on opt-in); `PrivacyPreferences.tsx` gains a "Discoverable on Nostr" toggle above the now-subordinate follow-list toggle. **Validated:** schema drift guard **green (0/1/2)** — `schema.sql` regenerated via throwaway DB (committed schema + 109 + 110), seed re-appended; shared/gateway/key-custody/feed-ingest `tsc` clean; web `tsc --noEmit` clean; root promise-safety lint **0 errors**; `scripts/check-hairlines.sh` clean on `PrivacyPreferences.tsx`. **NOT runtime-verified** — dev DB still has `linked_accounts`; needs migrations 109+110 applied (`psql < migrations/109…; psql < migrations/110…`) then `docker compose build gateway key-custody feed-ingest web && up -d` (the running services + baked `web` image still hold the old code). Phase 1 publishing also needs `DISCOVERY_PUBLISH_ENABLED=1` to leave dark mode. **Deferred (gated by ADR §8.1, decisions+infra not laptop code): Phase 2 atproto concierge** (run `@atproto/pds` sibling, `did:plc` mint, `username.all.haus` wildcard handle resolution, Relay `requestCrawl`, k256 signer in key-custody) — prerequisites: rotation-key custody posture, deprovision/tombstone path, PDS durability SLA; **Phase 3 ActivityPub concierge** (stub, follow-up ADR). The schema (`provenance`/`lifecycle_state`, empty concierge dispatch branch) is already shaped to receive them. Docs rolled forward: CLAUDE.md (new "Network presences" invariant + discovery-gate note), ADR status → Accepted/Phase 0+1 shipped, `docs/audits/SCHEMA-REFERENCE-2026-05.md` (table+index rename), `docs/adr/UNIVERSAL-FEED-ADR.md` §IV.6 (superseded-by pointer), this entry.

Previously: 2026-06-09. **Closed the last two listed workspace escapes — `ExternalCard` source byline fixed, `PubFollowButton` `/auth` determined a non-escape.** (1) **`ExternalCard` source-attribution `/source/:id` byline** (`web/src/components/feed/ExternalCard.tsx`): this legacy feed card is reused inside `SourceSurface`, which renders *inside* the surface overlay, so its byline `<Link href="/source/:id">` did a full-page navigation that escaped the chromeless workspace to the black topbar. Fixed on the `ProfileLink`/`FeedComposer` precedent — keep the real `<Link>` (SSR / cmd-click new-tab / copy-link all still work) but intercept a plain left-click with `openSurfaceHref(\`/source/${id}\`)` (+ `isModifiedClick` guard so modified clicks fall through to the link), re-rooting the source surface *in place* in the overlay. The card's other links (`viewOriginalUri`/`quoteWebUri`/`linkEmbed.url`) are intentional out-to-origin links (the "one route out"), left as-is. (2) **`PubFollowButton`'s logged-out `→ /auth` redirect** (`web/src/components/publication/PubFollowButton.tsx`): audited and determined **not** to be a live escape, so deliberately left unchanged. The workspace is login-gated (`WorkspaceView.tsx:677` bounces logged-out users to `/auth?mode=login`), so a logged-out user never reaches the surface overlay / `PublicationPanel` from within the workspace; the `/auth` branch only fires on the *standalone* full-page `/pub/[slug]` homepage, where a full-page redirect is correct, not an escape. Logged-in users only ever hit the follow/unfollow branch. And there is **no in-app auth overlay** — every logged-out CTA across the app (`VoteControls`/`ReplySection`/`WriterActivity`/`FeedView`/`Nav`) redirects to `/auth` full-page by design, so singling out `PubFollowButton` would be inconsistent for zero behavioural gain. A true auth overlay is a separate, larger initiative (not opened here). **Net: no known workspace escapes remain.** **Validated:** web `tsc --noEmit` clean, root promise-safety lint clean (0 errors/warnings on `ExternalCard`), `scripts/check-hairlines.sh` clean on the touched file. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: open a source surface in the overlay (FeedComposer external source row), click an external card's source byline → the source surface re-roots in place (no topbar behind, Back closes), cmd-click → new tab. Docs: this entry + CLAUDE.md workspace-escape invariant.

Last worked: 2026-06-08. **Publication sub-routes render in-overlay (closes the `/pub/:slug/{about,masthead,archive,:article}` overlay-debt residual from the surface-overlay entry below).** The surface overlay only rendered the publication *homepage* — its About/Masthead/Archive sub-pages and any article link still navigated full-page, escaping the chromeless workspace to the black topbar. Extended the existing surface overlay (no new store/mount) to carry the sub-views: **`web/src/stores/surfaceOverlay.ts`** — the `publication` target gains a `view: PubView` (`home|about|masthead|archive`); `surfaceUrl` maps each to its real `/pub/:slug[/view]` URL; `openPublication(slug, view='home')`; `openSurfaceHref` now also classifies `/pub/:slug/{about,masthead,archive}` (a deeper `/pub/:slug/:article` is left to the caller's reader path — it's an article d-tag, not a surface). Added **`surfacePathMatches(target, pathname)`** keyed on the surface's *base* path so SurfaceOverlay's dismiss-on-nav effect treats an in-overlay sub-view switch (which `replaceState`s a new URL) as "stay open" and only a real navigation away as dismiss — the prior exact-URL check would have self-dismissed on the transient `replaceState`→`usePathname` lag. **`web/src/components/publication/PublicationPanel.tsx`** rewritten from homepage-only to all four views behind a shared masthead header + an in-overlay sub-nav (mono-caps Home·About·Masthead·Archive, active = `text-black`, switches view via `openPublication` in place): **home** (rich serif article list, limit 20) and **archive** (dense list, limit 100) both open the **reader overlay** on row click — never `/pub/:slug/:article`; **masthead** (`getMasthead`, member names via `ProfileLink` → profile overlay, which supersedes per one-Glasshouse); **about** (`pub.about` markdown rendered client-side via the same sanitised `lib/markdown` pipeline the full-page route uses server-side). `SurfaceOverlay.tsx` passes `target.view` through. The full-page `/pub/[slug]/**` routes are untouched — direct visits still render server-side. **Remaining sanctioned escapes** (out of scope here): the `ExternalCard` source-attribution `/source/:id` link + `PubFollowButton`'s logged-out `/auth` redirect. **Validated:** web `tsc --noEmit` clean, root promise-safety lint **0 errors** (5 pre-existing-style `any` warnings on the article-row maps, matching the old panel), `scripts/check-hairlines.sh` clean on all 3 touched files. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: open a publication surface (FeedComposer source row or `openSurfaceHref`), click About/Masthead/Archive (view swaps in place, URL → `/pub/:slug/<view>`, Back closes the whole overlay, no topbar behind), click an article row (reader overlay opens), a masthead name (profile overlay supersedes); deep-link `/pub/:slug/about` direct-visits full-page. Docs: this entry + CLAUDE.md workspace-escape invariant.

Last worked: 2026-06-08. **Settings → ForallMenu item + Glasshouse overlay (resolves the deferred "settings overlay" debt from the escape-audit entry below — the last of the three deferred overlays).** The settings page was the final workspace-reachable surface still escaping to the black topbar (via `InlineReplyBox`'s "Connect your account to reply → /settings"). Retired it into an overlay on the established Ledger precedent. **New:** `web/src/stores/settingsOverlay.ts` (`useSettingsOverlay`, in-memory like `useLedgerOverlay`/`useNotificationsOverlay`; carries an optional `linked` seed); `web/src/components/account/SettingsPanel.tsx` (the settings body extracted from the old page, with the standard `inOverlay` gate — page-capable mode kept for parity like `LedgerPanel`); `web/src/components/workspace/SettingsOverlay.tsx` (wraps `SettingsPanel` in `<Glasshouse maxWidth={720}>`, mounted in `WorkspaceView` alongside the other overlays). **Wiring:** ForallMenu gains a **Settings** row after Ledger (opens the store); `lib/workspace/overlays.ts` registers `case "settings"` + adds `linked` to `OVERLAY_PARAM_KEYS`; Nav's two topbar Settings links repoint to `/workspace?overlay=settings` (matching the Ledger convention); `InlineReplyBox`'s "Settings →" affordance now opens the overlay in place instead of a raw `<a href="/settings">`. **`/settings` route → redirect shim** (`app/settings/page.tsx`, now a server component) → `/workspace?overlay=settings`, **forwarding the gateway's OAuth `?linked=mastodon|bluesky|error` flag** (`gateway/src/routes/linked-accounts.ts` returns to `/settings?linked=…`) so the social-connect banner still surfaces inside the overlay. **Contrast pass on the `#DCDAD3` pane:** the three section labels that render directly on the pane (Email / Payment & payouts / Connected accounts in `EmailChange`/`PaymentSection`/`LinkedAccountsPanel`, all settings-only components) moved `grey-400 → grey-600`; cleared the pre-existing hairlines met en route (`PaymentSection` `divide-y`, three `border-t/border-b` in `LinkedAccountsPanel`, `DangerZone`'s divider → whitespace). **Validated:** web `tsc --noEmit` clean, root promise-safety lint **0 errors** on touched files, `scripts/check-hairlines.sh` clean on all touched/new files. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: ForallMenu → Settings (720px overlay, ✕/Esc/Back close, no topbar behind); connect a Mastodon/Bluesky account → callback lands in the overlay with the banner; `InlineReplyBox` "Settings →" opens the overlay in place. Docs: this entry + CLAUDE.md workspace-escape invariant. **All three deferred workspace overlays (editor, publication, settings) are now shipped.**

Last worked: 2026-06-08. **Palette-aware `VoteControls` + `PostThread` (closes the palette-awareness debt from the entry below).** The last two lost-grey stragglers lived in components shared between the fixed-light Glasshouse pane and the palette-driven (light/dark) workspace vessel, so they couldn't be flat-darkened to `grey-600` without regressing dark mode. Fixed both to derive colour from the surface: **`VoteControls`** (`components/ui/VoteControls.tsx`) gains an optional `palette?: VesselPalette` (replacing the retired dead `dark` prop) — arrows/score now render `style={{ color: palette?.cardMeta ?? '#666666' }}`, active votes `palette?.crimson ?? '#B5242A'`, hover via mode-agnostic `hover:opacity-70` (dropped the old `hover:text-black`/`hover:bg-grey-100` that assumed a light ground). `PostActions` passes the vessel `palette`; the legacy fixed-light callers (`ArticleCard`/`NoteCard`/`WorkTab`/`PlayscriptReply`) pass nothing and fall back to `grey-600`. **`PostThread`** (`components/post/PostThread.tsx`) nav greys (thread loading, "↑ Full conversation", "Show more replies", the two gutter overflow arrows) now read `ctx.palette.cardMeta` instead of hardcoded `text-grey-400`. **Validated:** web `tsc --noEmit` clean, `scripts/check-hairlines.sh` clean on the three touched files. **NOT browser-verified** — needs `docker compose build web && up -d web`, then check vote arrows/score + thread nav read correctly on a light feed, the fixed-light profile/source overlays, AND a dark-mode feed (the whole point — muted text must invert, not vanish). Docs: this entry + CLAUDE.md (the palette-vs-fixed-pane boundary rule now cites these as the reference implementations).

Last worked: 2026-06-08. **Glasshouse panes are a warm mid-light surface (`bg-glasshouse` `#DCDAD3`) + sitewide field/text-contrast sweep.** Frosted overlay panes were fixed white, which vanished against the light workspace ground (`#F0EFEB`) while popping in dark mode; because the dark ground sits far below any modal tone, there was headroom to deepen the pane into a real mid-tone that separates from *both* grounds. Added a `glasshouse` Tailwind token (`#DCDAD3`, `web/tailwind.config.js`) and pointed `Glasshouse.tsx`'s pane at it; **removed the 6px black slab top** from every pane (`Glasshouse.tsx` + the standalone `/reader/[postId]` pane) — the elevation shadow alone now lifts it. Established two fixed conventions on the mid-light interior (codified in CLAUDE.md › Glasshouse exempt-surface rule): **(1) text fields are solid white `bg-white` wells** — one treatment everywhere (article editor title/standfirst/tags, composers, messages); the standalone `/write` carries its own `bg-glasshouse` surface so the same white fields read there too; container panels that merely group controls stay soft `bg-white/40` washes. **(2) secondary text is `grey-600` or darker** — `grey-300/400` are tuned for white and lose contrast on `#DCDAD3`. **Swept the lost greys across every fixed-light overlay:** article editor + compose surfaces, messages (preserving the light-on-black bubble timestamp + white-field placeholders), dashboard context-switcher/empty-states, notifications, profile (`NativeProfilePanel` + Work/Social/Followers/Following tabs + `TrustProfile` + `AuthorProfileView`), surface overlays (`SourceSurface`/`TagBrowser`/`PublicationPanel`/`PublicationFooter`), reader chrome (`ExternalArticleReader`/`ReaderOverlay`/`EditorOverlay` — fixed two undefined `grey-500`s), the Glasshouse ✕, and the legacy `components/feed` cards (`ArticleCard`/`NoteCard`/`NeighbourhoodCard`/`QuoteCard` + `BookmarkButton`/`ShareButton`). Two pane uses of `.btn-text-muted` (`#999`, too light on the pane) swapped for an explicit pane-safe `grey-600`; the **global class is unchanged** so white-surface buttons keep their subtler muted tone. Cleared several pre-existing 1px borders met en route (TagInput + ShareButton dropdowns → shadow-only enclosure; ExternalCard embed boxes → `bg-grey-100` wells; PublicationFooter rule). **Outstanding — palette-awareness debt (deliberately NOT flat-darkened; would regress dark mode):** `VoteControls` (`components/ui/VoteControls.tsx`, shared with the palette-driven `components/post/PostActions` which renders in dark mode; its `dark` prop was retired so it's surface-agnostic and under-contrasted on *both* the pane and white cards) and `components/post/PostThread`'s hardcoded nav greys (loading / "Full conversation" / "Show more replies" / overflow arrows — render on the light/dark vessel interior, not the fixed pane) both need a **palette-aware** fix (`palette.cardMeta`), not a flat `grey-600`. **Validated:** `scripts/check-hairlines.sh` clean on every touched file each round; no dead hovers / undefined greys left. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then eyeball a modal against a light-mode feed (warm card lifts off the ground; white field wells; readable secondary text) and a dark-mode feed (still separates). Docs: this entry + CLAUDE.md (Glasshouse exempt-surface conventions incl. the palette-vs-fixed-pane boundary, design-system Glasshouse + no-hairlines sections).

Last worked: 2026-06-08. **One Glasshouse at a time (sitewide invariant).** Frosted overlay panes were able to stack — opening a second over-workspace surface left the first mounted behind it. Enforced single-open **in the `<Glasshouse>` primitive itself** (`web/src/components/workspace/Glasshouse.tsx`): a module-level `activeGlasshouse` registry (per-instance `token` + `supersede` fn); a newly-mounted pane claims the slot and calls the prior one's `supersede`, and the unmount cleanup clears the slot only when it still owns it (never clobbers a successor). Chosen at the primitive level (not a store-subscription manager) because two consumers — the workspace-local `Composer` + `FeedComposer` — are driven by local React state, not global stores, so only the shared primitive catches all 11 Glasshouse surfaces uniformly. **`supersede` is state-only (never `history.back`):** added an optional `onSupersede` prop defaulting to `onClose`; the three URL-synced overlays (`ReaderOverlay`/`ProfileOverlay`/`SurfaceOverlay`) pass `onSupersede={dismiss}` so superseding doesn't pop the newcomer's freshly-pushed history entry (added a `dismiss` to `stores/reader.ts`, matching the one `profileOverlay`/`surfaceOverlay` already had). Leftover URL-synced history entries (when superseded by an ephemeral overlay) self-heal on the next Back — a minor cosmetic quirk, never a corruption. **Validated:** web `tsc --noEmit` clean, root promise-safety lint 0 errors, `scripts/check-hairlines.sh` clean on all touched files. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: open reader → open a profile byline (reader vanishes), open FeedComposer → open a source overlay (composer vanishes), ForallMenu Dashboard while a reader is open (reader vanishes). Docs: this entry + CLAUDE.md "one Glasshouse at a time" invariant.

Last worked: 2026-06-08. **Source/tag/publication surface overlays (resolves the deferred "publication overlay" debt + the FeedComposer source-row escapes from the escape-audit entry below).** The FeedComposer source rows (`web/src/components/workspace/FeedComposer.tsx::SourceRow`) rendered each source name as a plain `<Link href>` doing a full-page navigation — account → `/:username`, publication → `/pub/:slug`, external → `/source/:id`, tag → `/tag/:name` — every one escaping the chromeless workspace to the black topbar. **(a)** Account rows now open the **profile overlay** in place via `openProfileHref` (gated on `sourceType === 'account'`, since `openProfileHref` reads the href alone and would mis-read `/pub`·`/source`·`/tag` as a native username). **(b)** Publication / external-source / tag rows open a new **unified surface overlay**, mirroring `useProfile`'s one-store-many-kinds shape: `web/src/stores/surfaceOverlay.ts` (`useSurfaceOverlay`, kinds `source|tag|publication`, real-URL push/pop/dismiss copied from `profileOverlay.ts`; + exported `openSurfaceHref(href)` classifier — the byline counterpart to `openProfileHref`) and `web/src/components/workspace/SurfaceOverlay.tsx` (renders `SourceSurface` / `TagBrowser` / `PublicationPanel` in a `<Glasshouse maxWidth={780}>`, mounted **unconditionally in `LayoutShell`** like `ProfileOverlay`, with `surfaceOpen` added to the `chromeless` set). **New body** `web/src/components/publication/PublicationPanel.tsx` (client-fetched masthead + recent articles via `publications.getPublic`/`getPublicArticles`; article rows open the **reader overlay** `useReader.openNative` instead of `/pub/:slug/:article`). **`TagBrowser` gained an `inOverlay` prop** routing its article rows through the reader overlay too (standalone `/tag/[tag]` keeps full-page links); `SourceSurface` is reused as-is (its `ExternalCard` expands inline, no navigation). Direct visits to `/source/:id`·`/tag/:name`·`/pub/:slug` still render full-page (the profile model — no route shims). **Residual:** non-homepage `/pub/:slug/:article|masthead|about` and the source-attribution `/source/:id` link inside `ExternalCard` still navigate full-page; `PubFollowButton`'s logged-out `/auth` redirect stays. **Validated:** web `tsc --noEmit` clean, root promise-safety lint **0 errors** (4 pre-existing `any` warnings), `scripts/check-hairlines.sh` clean on all 6 touched/new files. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then: in the FeedComposer, click a publication/external/tag source → 780px Glasshouse overlay (✕/Esc/Back close, no topbar behind), hover an account username → bio modal (pre-existing), click an account username → profile overlay. Docs: this entry + CLAUDE.md workspace-escape invariant.

Last worked: 2026-06-08. **Consolidated three TipTap article editors into one Glasshouse `EditorOverlay` (resolves the deferred "editor overlay" debt from the escape-audit entry below).** Found article-writing was forked across *three* parallel `useEditor` implementations with drifted feature sets: the full `ArticleEditor` (`/write`, 777 lines — dek+tags+cover+comments+schedule+edit), `ArticleComposePanel` (the `ComposeOverlay` article mode, 601 lines — no dek/tags/cover/comments), and the workspace `Composer`'s article mode (no tags/comments/schedule/draft-resume). `ArticleEditor` was a true superset, so an article written from either lightweight surface silently lost metadata. **Collapsed onto `ArticleEditor` as the single editor**, given a `chrome?: 'page' | 'overlay'` prop (overlay drops the full-page frame + topbar-offset sticky bar; only L333/L335 differ) + an unmount `autoSaver.cancel()`. **New surface:** `web/src/components/workspace/EditorOverlay.tsx` (the editor in a `<Glasshouse maxWidth={780}>`), mounted **unconditionally in `LayoutShell`** like `ProfileOverlay`, with `editorOpen` added to the `chromeless` set so the topbar hides while writing. Opened via new `useEditorOverlay` store (`web/src/stores/editorOverlay.ts`, ephemeral; + exported `seedFromNote` helper carrying the heading→title promotion lifted from the old `switchToArticle`). **Shared `web/src/hooks/useArticleEditorInit.ts`** (extracted verbatim from `app/write/page.tsx` lines 50-218 — membership/draft/edit load + `handlePublish`/`handleSchedule`, parameterised by an `onComplete` callback) so `/write` and the overlay can't drift; both now consume it. **Registered** `case "editor"` in `lib/workspace/overlays.ts` (`?overlay=editor[&draft|&edit|&pub]` deep-links + `OVERLAY_PARAM_KEYS`). **Repointed every entry point** to the overlay (`inOverlay`-gated, standalone pages keep their `<Link href="/write…">`): `DashboardPanel` New-article/draft-title/draft-Edit/published-Edit/empty-state + pub New-article, `PublicationArticlesTab` empty-state, `WorkspaceView` ForallMenu `new-article`, `ComposeOverlay` + `Composer` "Write an article →" (both seed the note body via `seedFromNote`). **Removed the dead paths:** deleted `ArticleComposePanel.tsx` (601 lines); cut `Composer.tsx` 1132→~470 lines (note/reply/quote only — dropped its `useEditor`, `ArticleModePanel`/`ArticleToolbar`, all article state, `switchToArticle`, `handlePublishArticle`, `initialMode` prop); stripped `article` from `stores/compose.ts` (`ComposeMode` → `note|reply`; removed `openArticle`/`setMode`/`articleFlush`/`articleDraftId`/`articlePublicationSlug`/`initialArticleContent`); simplified `WorkspaceView` compose bridge to note-only. `/write` stays as the addressable full-page editor (refactored onto the shared hook, `chrome='page'`). **Validated:** web `tsc --noEmit` clean, root promise-safety lint **0 errors** (221 warnings, all pre-existing `any`/unused debt), `scripts/check-hairlines.sh` clean on all 13 touched/new files (also cleared a pre-existing 1px paywall-toggle border in `ArticleEditor` → `border-2`), grep for `openArticle|articleFlush|initialArticleContent|article*Id|mode === 'article'` zero hits in app code. **NOT browser-verified** — `web` is a baked prod image; needs `docker compose build web && up -d web`, then exercise: ForallMenu → "Write an article" (780px overlay, ✕/Esc close, no topbar behind); note compose → "Write an article →" seeds body + promotes a heading title; dashboard New-article / draft-Edit / published-Edit seed correctly; publish lands back on the dashboard overlay (articles tab); deep-link `?overlay=editor&draft=X`; standalone `/write`, `/write?draft=`, `/write?edit=` still render full-page with chrome. Docs: this entry, CLAUDE.md ("Article writing — one surface" invariant), plan `~/.claude/plans/distributed-noodling-engelbart.md`.

Previously: 2026-06-08. **Workspace "black-topbar" escape audit + quick swaps #1–4 (no surface reachable from the chromeless workspace should mount `<Nav>`).** Audited every workspace-reachable surface (`web/src/components/{workspace,dashboard,messages,post,compose}`) for `Link`/`router.push`/`window.location` navigations that leave the overlay world for a platform-prefix or canvas route (which mounts the black topbar — `LayoutShell` `chromeless = mode==='workspace' || readerOpen || profileOpen`). The primary nav (`ForallMenu`) was already clean (Messages/Notifications/Dashboard/Ledger are overlays; "Write an article" opens the in-workspace `Composer`; `PipPanel` bylines use `ProfileLink`). **Found 4 escape clusters; landed the clean "quick swaps" this pass:** (1) Search→writer now opens the **profile overlay** (`useProfile.openNative`); (2) Search→article opens the **reader overlay** (`useReader.openNative`) — both in `SearchPanel.tsx`, replacing the old `navigate()`→`router.push`; (3) Dashboard blank "New article" + empty-state "Write your first article" open the **workspace Composer** in article mode; (4) Dashboard + `PublicationArticlesTab` article-title open the **reader overlay** — all `inOverlay`-gated (the reader/compose overlays mount in `WorkspaceView`, not globally; the standalone `/dashboard` page keeps its `Link`s). **Key new wiring — the compose bridge:** the global `ComposeOverlay` is *not* mounted in the chromeless workspace, so the Dashboard overlay (a sibling of `Composer` under `WorkspaceView`) couldn't reach a composer. Added a small bridge in `WorkspaceView` that mirrors `useCompose.open('note'|'article')` into its local `<Composer>` state (and clears the store on close); nothing in the workspace used `useCompose` before, so it's a clean new channel any in-workspace surface can use to request a compose. Touched `WorkspaceView.tsx`, `SearchPanel.tsx`, `DashboardPanel.tsx`, `PublicationArticlesTab.tsx`. Validated: web `tsc --noEmit` clean, `scripts/check-hairlines.sh` clean on all four touched files. **Not browser-verified** — `web` is a baked prod image, so seeing it needs `docker compose build web && up -d web`. **Deferred (agreed direction: everything reachable from the workspace becomes an overlay):** an **editor overlay** wrapping the full TipTap `/write` editor, then repoint draft/edit links + pub-seeded "New article" + the composer's "Open in full" button; **publication overlay** for Search→publication (`/pub`) + pub-seeded targets; **settings overlay** for `InlineReplyBox`'s "Connect your account to reply" → `/settings`. Stripe-connect (external redirect) stays as-is. Docs: this entry + CLAUDE.md workspace-escape invariant.

Previously: 2026-06-08. **Universal resolver discovery fallback — branches 2 + 3 shipped (UNIVERSAL-FEED-ADR §V.5.8).** Following branch 1 (Bluesky `searchActors`, shipped earlier 2026-06-08), added the two remaining cheap-and-precise discovery branches so a bare name (`"Guardian"`, `"@jack"`) the deterministic chains can't resolve surfaces pickable external candidates. **Branch 2 (Nostr name search):** `resolver.ts::searchNostrProfiles` runs a NIP-50 `{kinds:[0], search}` query over a temporary SSRF-pinned WebSocket (reusing `pinnedWebSocketOptions`, mirroring `fetchKind0FromRelay`'s lifecycle) against a search relay (`NOSTR_SEARCH_RELAY`, default `wss://relay.nostr.band`); newest kind-0 per pubkey wins; emits `speculative` `nostr_external` matches deduped by hex pubkey. Kind-0 content parsing extracted to shared `parseNostrProfileContent` (now used by both single-profile enrichment and search). **Branch 3 (curated catalog):** new `gateway/src/lib/discovery-catalog.ts` — pure, zero-I/O seed table (~11 well-known publications: Guardian, BBC, NYT, NPR, Al Jazeera, Atlantic, Verge, Ars, Wired, TechCrunch, HN) mapping lowercase name aliases → canonical feed URL, matched by bidirectional substring with a ≥2-char guard; emits `speculative` `rss_feed` matches deduped by feedUrl. **Wiring (`resolveAsync`):** catalog (instant) resolves synchronously and persists first, then Bluesky + Nostr run concurrently via `Promise.all` with incremental `storeAsyncResult` partials — the same partial-result pattern `dotted_host` uses; each branch dedupes within its own protocol's key-space (DID / pubkey / feedUrl). Phase A now registers all three chains (`catalog_discovery`/`bluesky_discovery`/`nostr_discovery`), gated identically (submit-only via `discover`; invite/dm never discover). **No schema change** — reuses `resolver_async_results` (migration 061) + existing `speculative` confidence. Branch 4 (web-search → URL bridge, feature-flagged) remains the only unimplemented branch. **Tests:** new `gateway/tests/{discovery-catalog,nostr-profile-parse}.test.ts` (14 tests); the WebSocket transport mirrors the already-trusted `fetchKind0FromRelay` (untested at that layer, consistent with repo rigor). **Validated:** gateway `tsc --noEmit` clean, root promise-safety lint **0 errors**, full gateway Vitest suite green (117 tests). Docs rolled forward: `docs/adr/UNIVERSAL-FEED-ADR.md` §V.5.8 (branch table, Phasing note, implementation-status block). **Not runtime-verified** — gateway is a baked image; needs rebuild + `docker compose up -d gateway` to exercise live (catalog branch is pure and covered by tests; the Nostr relay branch needs a live relay round-trip).

Previously: 2026-06-06. **Nostr Outbound Interop — Phase A built (ships dark).** Implemented `docs/adr/NOSTR-OUTBOUND-INTEROP-ADR.md` Phase A: makes all.haus users + their public content discoverable on the wider Nostr mesh via a NIP-05 endpoint + three replaceable discovery events (kind 0 profile, kind 3 follow list, kind 10002 relay list) fanned out through the existing `relay_outbox`. **Migration `108_nostr_outbound_discovery.sql`**: adds `profile`/`follow_list`/`relay_list` to `relay_outbox_entity_type_check`; adds `accounts.publish_follow_graph` (default true — the kind-3 opt-out), `follow_list_dirty` (coalescing marker), `discovery_synced_at` (backfill/self-heal cursor) + two partial indexes. `schema.sql` regenerated from a throwaway DB (committed schema.sql + 108), seed re-appended; **drift guard green (0/1/2)**, diff is exactly 108's effect. **New code:** `gateway/src/lib/nostr-events.ts` (event-template builders + `discoveryRelayTargets`/`relayForAccount`/`discoveryEnabled`), `gateway/src/lib/discovery-publish.ts` (`republishProfile`/`republishRelayList`/`republishFollowList`/`retractFollowList` sign-then-enqueue, `markFollowListDirty`, `runDiscoverySweep`), NIP-05 `GET /.well-known/nostr.json` in `gateway/src/index.ts` (ACAO `*`, `no-store`, rate-limited) + nginx `location`, `gateway/src/routes/privacy-preferences.ts` + `web/src/components/account/PrivacyPreferences.tsx` (settings → Privacy toggle, **default-on with world-readable disclosure** in the copy). **Triggers:** kind-0/10002 republish on profile edit + username change (`routes/auth.ts`); kind-3 `follow_list_dirty` marker on follow/unfollow (`routes/follows.ts`) + external-nostr sub/unsub (`routes/external-feeds.ts`). **Key deviation from the ADR (documented in ADR §8):** the kind-3 graphile-job coalescing + the feed-ingest backfill task were both moved into the **gateway's existing 60s scheduler loop** (`runDiscoverySweep`, advisory lock `DISCOVERY`) because **feed-ingest can't sign** (only the gateway reaches key-custody). Coalescing = a `follow_list_dirty` flag drained per cycle (rebuild-from-state → sign → enqueue → clear); one sweep over `discovery_synced_at` does both initial backfill and 7-day self-heal — satisfying the agreed **"self-heal first"** sequencing. New accounts are picked up by the sweep (NULL synced_at, sorted first), so signup/Google-signup were not separately wired. **Ships dark:** all publishing gated by `DISCOVERY_PUBLISH_ENABLED` (default 0); `PUBLIC_FANOUT_RELAY_URLS` (default empty = in-house relay only) controls public-mesh reach; NIP-05 endpoint is read-only and always on. **Deferred fast-follows (agreed):** per-relay ACK accounting on discovery outbox rows (§3.3 item 1 / Risk #1 — self-heal covers convergence meanwhile); Phase B content fan-out; deactivation tombstones (Risk #6); mention `p`-tags. **Validated:** shared+gateway `tsc` clean, web `tsc --noEmit` clean, root promise-safety lint **0 errors**, hairline tripwire clean on touched frontend files, schema drift green, and all new SQL (follow-list union, both sweep selects, the unsubscribe CTE, NIP-05 lookup) validated read-only against the live dev DB (union returns real data; Phase B shows 1001 accounts queued for backfill). Docs rolled forward: CLAUDE.md (Nostr integration line), DEPLOYMENT.md (env table), docker-compose (gateway env), ADR §8 as-built. **NOT yet runtime-verified** — the gateway image was rebuilt but the running container is root-owned and the daemon refuses my kill signal (permission denied, even sandbox-off), so the new code isn't live; needs `docker compose up -d --force-recreate gateway`, then smoke `curl "…/.well-known/nostr.json?name=<user>"`. Migration 108 is applied to the dev DB; **prod applies it on next deploy** via `migrate.ts`.

Previously: 2026-06-05. **ForallMenu ∀ glyph rendered through the circle as an aperture.** The bottom-right workspace control showed the ∀ (U+2200) small and fully contained — `fontSize: 26` centred in the 56px disc, ~46% of the diameter, floating with margin all round. Reworked so the circle reads as an *aperture*: the glyph is enlarged past the disc and the circumference clips it, so only as much of the letter as fits the frame is seen — its feet and tip just cut off by the edge. The clip can't live on the button itself (the absolutely-positioned unread badge sits *outside* the disc at `top/right: -2` and would be clipped too), so the glyph moved into an inner aperture `<span>` (`position: absolute; inset: 0; border-radius: 50%; overflow: hidden`, flex-centred, `fontSize: 84`, `lineHeight: 1`); the badge stays an unclipped sibling on the button (which keeps the disc fill, shadow, and `overflow` visible). Two tuning knobs for later if the crop looks off: the span's `fontSize` (↑ crops harder, ↓ reveals more letter) and an optional `transform: translateY(±2px)` if Literata seats the ∀ ink high/low in its em box and the top/bottom cuts read uneven. Touched only `web/src/components/workspace/ForallMenu.tsx`; `scripts/check-hairlines.sh` clean on it. **Not yet browser-verified** — `web` is a baked prod image, so seeing it needs `docker compose build web && up -d web`. Docs: this entry + `docs/adr/WORKSPACE-EXPERIMENT-ADR.md` build log.

Previously: 2026-06-03. **Subscription renewal audit + hardening (auto-renewal already shipped; the situation-report "biggest gap" was stale).** The 2026-05-10 situation report still lists auto-renewal as the top launch-blocking gap, but it shipped 2026-05-15 (`gateway/src/workers/subscription-expiry.ts`, migration `023_subscription_auto_renew.sql`) — monthly+annual, offers, comps, Nostr re-attestation, renewal/expiry-warning emails, hourly under an advisory lock. Audited the renewal worker against `docs/audits/SUBSCRIPTIONS-GAP-ANALYSIS.md` Step 1 and fixed four divergences in one pass: **(#7, the big one) publication subscriptions never renewed** — the renewal SELECT inner-joined `accounts ON writer_id`, so every `publication_id`-targeted sub was dropped from renewal; combined with Phase-2 expiry only touching `auto_renew = FALSE`, pub subs sat active past period end forever with no further charge (free perpetual access). Fixed with `LEFT JOIN accounts w` + `LEFT JOIN publications p`, `COALESCE(w.nostr_pubkey, p.nostr_pubkey)`, and routing the earning to `writer_id ?? publication_id` (mirrors the subscribe-path quirk: `subscription_events.writer_id` carries the publication id for pub subs). **(#1) reading-tab clamp** — renewal used `GREATEST(free_allowance_remaining_pence - price, 0)`, silently undercharging readers whose tab was below the price while still crediting the writer in full; the tab is designed to go negative (settled via Stripe later), so it now deducts without a floor, matching both subscribe paths. **(#3) annual-discount hardcode** — offer-expiry revert used `* 12 * 0.85` instead of the writer's configurable `accounts.annual_discount_pct`. **(#2) retry-once** — the worker now retries the atomic renewal transaction once before expiring (a transient DB/signing blip no longer permanently kills a paid sub, per Step 1's "retry once, then expire"). New `gateway/tests/subscription-expiry.test.ts` (5 cases: no-floor deduction, pub-sub earning→publication, annual offer-revert uses configured discount, retry-once-then-succeed, expire-after-both-fail). Validated: gateway `tsc --noEmit` clean; 95 gateway tests green (90 + 5 new); root promise-safety lint clean on the worker. **Carried (no behaviour change, needs a product call):** catch-up multi-charge — when the worker resumes after downtime it charges once per missed period per hourly tick rather than advancing to the next future boundary once. Docs: gap-analysis reconciliation note + this entry. **Live-verified 2026-06-04** against the real dev DB (rebuilt gateway image, ran the compiled worker one-off via `docker compose run`, seeded subs cleaned up after): #1 clamp confirmed (reader tab `200 → -300`, no floor; old `GREATEST` would have left 0; charge 500 / earning 460 at the 8% fee); #3 annual-discount confirmed (offer-expiring annual writer sub renewed at `8741 = round(847×12×0.86)` using the writer's configured 14% discount, not the old hardcoded `0.85` = 8639); #2 retry-once + atomic-expire confirmed (the publication sub failed, retried once, then expired with the reader's tab untouched and zero `subscription_events` — proving a failed renewal leaves no partial charge). **#7 surfaced a deeper pre-existing bug — now also fixed:** the LEFT-JOIN fix correctly *picks up* publication subs (previously invisible to renewal), but completing one failed a real FK — `subscription_events.writer_id` was `NOT NULL` and FK→`accounts`, so logging a charge with the `publication_id` in that slot violated it. The *subscribe* path (`publication.ts`) called `logSubscriptionCharge` the same way, so publication-sub charge logging was broken on **both** subscribe and renew; it had never fired only because there are 0 publication subs in the DB. Fixed with **migration `103_subscription_events_publication.sql`**: add a nullable `publication_id` (FK→`publications`) to `subscription_events`, relax `writer_id` to nullable, add `subscription_events_target_check CHECK (writer_id IS NOT NULL OR publication_id IS NOT NULL)`, + partial index. `logSubscriptionCharge` (`routes/subscriptions/shared.ts`) gained a trailing `publicationId` param and now writes both columns; both `publication.ts` subscribe calls pass `(…, null, …, publicationId)`; the worker passes `sub.writer_id` + `sub.publication_id`; the Phase-3 expiry-warning SELECT/INSERT carry `publication_id` too (and skip the account-only warning email for pub subs). `schema.sql` regenerated via `pg_dump` of a throwaway DB (schema.sql + 103) with the seed re-appended; **drift guard green (Checks 0/1/2)**, diff is exactly migration 103's effect. **Live-verified 2026-06-04 (post-fix):** seeded an overdue publication sub, rebuilt the gateway image, ran the worker — it **renewed** (status stays active, period +30d, charge 600 / earning 552 at 8% fee logged with `writer_id NULL` + `publication_id` set, reader tab `100 → -500`), no FK error; seed cleaned up. Validated overall: gateway `tsc` clean; 95 gateway tests green; promise-safety lint 0 errors on touched files. **Carried (unchanged):** catch-up multi-charge after downtime (a product call). Note migration 103 is applied to the dev DB; **prod applies it on next deploy** via `migrate.ts`.

Previously: 2026-06-02. **Workspace dock consolidation — collapsed the three bottom-right floating buttons (∀ · bell · search) into one ∀.** Per the user's call, the chrome-less workspace floor now carries a single control: the ∀ (`ForallMenu`). Search and Notifications fold into its drop-up menu (selecting either opens the existing rich panel **in place**, anchored above the ∀); a new **Messages** row routes to `/messages`; the create actions (New feed / New note / Write an article) and hidden-feed restores are unchanged. The ∀ now carries a **combined unread badge** (`dmCount + notificationCount`) sourced from the shared `useUnreadCounts` store (already globally polled every 15s by `AuthProvider`), so the bell's indicator moves onto the ∀ plus DMs; the Messages/Notifications menu rows also show their individual unread chips. **Refactor:** `NotificationsAnchor.tsx`/`SearchAnchor.tsx` → renamed to controlled panel bodies `NotificationsPanel.tsx`/`SearchPanel.tsx` (no own button/positioning; the dock owns open/close via one shared outside-click+Esc handler). `NotificationsPanel` dropped its own 30s poll loop (the shared store already polls) and now refreshes that store after mark-read/read-all so the ∀ badge stays in sync. `ForallMenu` became the single dock: `view` state machine (`closed`/`menu`/`search`/`notifications`), arrow-key nav over all rows, `MenuRow` via `forwardRef` (React 18). `WorkspaceView` drops the two extra mounts + imports. **Hairlines:** the panels' pre-existing 1px borders/dividers came along in the move; since the new files now own those lines, made them compliant — structural enclosures bumped to 2px, internal divider lines replaced with whitespace. Validated: `tsc --noEmit` clean (web); `npm run lint` 0 errors; `scripts/check-hairlines.sh` clean on all three touched files. **Browser-verified 2026-06-03** (headless Chromium, logged in as `billyisland`): single ∀ disc with no standalone bell/search; drop-up menu rows in order `New feed · New note · Write an article — Search · Messages(54) · Notifications(42)`; Search + Notifications open their rich panels in place above the ∀; Messages routes to `/messages`; combined ∀ badge = `96 = 54 dmCount + 42 notificationCount`. Probed the fragile badge-sync leg — MARK ALL READ dropped the ∀ badge by exactly the notification count (93 → 51, DMs untouched), and outside-click closes the panel. (Note: the running `web` service is a baked production image, not a dev mount, so this required `docker compose build web && up -d web` first — a clean tree alone doesn't put HEAD in the browser.) Scoped to the workspace dock only — the topbar `Nav.tsx` (its own search input + avatar menu) is a separate surface, left untouched. Docs rolled forward: `docs/adr/WORKSPACE-EXPERIMENT-ADR.md` (build-log + slice 21/22 anchor descriptions), `docs/adr/WORKSPACE-MIGRATION-MAP.md` (component list).

Previously: 2026-06-01. **Closed the Phase-5 KNOWN GAP — external like/repost/reply on the focal thread card.** The interactive focal card's `Post` comes from the `/thread` projector (`gateway/src/lib/post-mapper.ts::feedItemToPost`), which never emitted `externalItemId`, so web's `usePostInteractions` left `active = !!externalItemId && interactBack` false → like/repost/reply buttons rendered inert even with a valid linked account. Fixed by mirroring the `postId` id-bridge: added `externalItemId: string | null` to the gateway `Post` interface and emitted it in both projector paths — `feedItemToPost` (`externalItemId: isExternal ? row.external_item_id ?? null : null`; `FEED_SELECT` already carried `fi.external_item_id`, so no SQL change) and the native-comment `commentToPost` (null — native engagement is the all.haus scoresheet). The client `Post` type (`web/src/lib/post/types.ts`) and consumer (`usePostInteractions` line 122) already read the field; the gateway was the sole missing leg. The collapsed *feed* card keeps static `inline-numerals` counters by design (interaction is on the expanded focal). Added 2 regression cases to `gateway/tests/post-mapper.test.ts` (external → id surfaced, native article/note → null). Validated: gateway `tsc` clean, web `tsc` clean, 90 gateway tests green (88 + 2 new). Recorded in CLAUDE.md (gap CLOSED) + `docs/adr/UNIVERSAL-POST-ADR.md` §10. **Not yet browser-verified** (needs the seeded atproto `linked_account` fixture + an external focal in a thread) — `tsc` + the `active` gate confirm the leg is now wired, but a live click-through is the remaining manual step.

Previously: 2026-06-01. **First live in-browser smoke of the flagless workspace (the never-run Phase 5 + Phase R manual step) — found + fixed a P0 that broke all threads, and surfaced one new gap.** Drove headless Chromium against the running stack (logged in as `billyisland` via `/auth/dev-login`). **P0 (fixed) — thread expansion was broken for every card in the cutover workspace** ("COULDN'T LOAD THIS THREAD" on every click). The Phase-3 "id bridge" was wired on the gateway (the `/workspace/feeds/:id/items` JSON carries the real 64-hex `post_id`) but **the client dropped it**: `WorkspaceView.mapApiItem`/`mapExternalApiItem` never copied `postId`, and the `WorkspaceFeedApi{Article,Note,External}` interfaces in `lib/api/feeds.ts` didn't even declare it — so `mapFeedItemToPost`'s `item.postId ?? item.id` always fell back to the origin event-id/UUID, and `GET /thread/:id` 400'd (UUID) / 404'd (event id). Confirmed against the backend: `/thread/<real post_id>` → 200 (full ancestors+focal), `/thread/<UUID>` → 400. Fix = declare `postId` on the three interfaces + propagate it in the three mapper branches (web-only, 2 files, 6 lines); `tsc` clean, web rebuilt, re-verified in-browser: thread expand + re-root (`↑ Full conversation`) now 200, reader overlay from an article card pushes `/reader/<postId>` + Esc restores, external article reader renders `/extract`'d + sanitised HTML (overlay + direct URL), `/author/:id` profile + native `/article/<dTag>` reader all good. **NEW GAP (documented, NOT fixed): external like/repost/reply is non-functional in the workspace.** The collapsed feed card uses static `inline-numerals` counters (no buttons); the interactive focal card's Post comes from the `/thread` projector (`gateway/src/lib/post-mapper.ts`), which **never sets `externalItemId`** → `active = !!externalItemId && interactBack` is false → buttons render inert/disabled (verified: a valid seeded atproto `linked_account` is returned by `/linked-accounts` yet the buttons stay disabled). Fix mirrors the `postId` bridge: have the projector emit `externalItemId` for external thread nodes. **Not exercisable in dev (data gaps, not failures):** native vote/reply (the only workspace feed "Verify Cards" has zero native nostr content), poll voting (no poll), reply pagination/deep re-root (1–2 replies, depth 2). **Dev DB now carries two verification fixtures** (left in place): a seeded RSS article in "Verify Cards" (`feed_items eeeeeeee-0000-…`) and a fake atproto `linked_account` for `billyisland` (`dddddddd-…`, no real creds). Recorded in CLAUDE.md (id-bridge note + Phase 5 KNOWN GAP) and `docs/adr/UNIVERSAL-POST-ADR.md` §10.

Previously: 2026-06-01. **Cleared 4 of the 5 carried audit-debt items from the post-cutover audit (below).** All four are mediums/lows the audit deliberately deferred; none needed a migration or new endpoint. (1) **`/author/:id/posts` final-page cursor** (`gateway/src/routes/author.ts`) — `nextCursor` was emitted whenever the page had any rows, so the last page handed out a cursor that fetched nothing; now emitted only when the page is full (`result.rows.length === limit`). (2) **Reader history-entry leak on stacked opens** (`web/src/stores/reader.ts`) — `openExternal`/`openNative` always `pushState`d, so opening a second article while the overlay was open leaked a history entry `close()` never pops (and desynced the URL); `pushReaderUrl` now takes an `alreadyPushed` flag and `replaceState`s when we already own a pushed entry, keeping exactly one poppable entry. (3) **Unbounded gateway caches** (`gateway/src/routes/external-items.ts`) — the engagement/parent/quote caches had a TTL but never swept, growing one permanent entry per external item ever viewed (only the thread cache was bounded, from H2); generalised the thread cache's bound into one `setCapped<V>` helper (sweep-expired-then-evict-oldest-insertion, cap 1000) and applied it to all four. (4) **`useNeighbourhood` cache + cancellation** (`web/src/hooks/useNeighbourhood.ts`) — the module cache grew without bound and the parent/thread settle could `setState` after the card collapsed/unmounted; bounded the cache (FIFO, cap 200, new `cacheSet`) and added a `mounted` ref gating every async `setState` (main fetch + `loadParent`). **Deliberately NOT fixed: the `/thread` external-thread N+1 walk** (`assembleExternalThread` in `post-thread.ts` — one sequential query per ancestor hop + per BFS descendant). Perf concern (not correctness), bites only deep/wide Bluesky/Mastodon threads, and a recursive-CTE rewrite carries real pagination/ordering-semantics risk that **dev has no thread structure to validate against** (3 external items, 1 reply, max ancestor depth 2). Needs real thread fixtures or a parity harness before a rewrite is safe — left as documented debt. Validated: `tsc --noEmit` clean (gateway + web); 88 gateway + 136 web tests green; `scripts/check-hairlines.sh` clean on all four touched files.

Previously: 2026-06-01. **Post-cutover deep audit of the three-day UNIVERSAL-POST arc (46 commits) + fixes.** Reviewed every commit from the security pass (`29c22d2`) through the Phase 5 cutover (`998bf74`) across five parallel review tracks; found 1 critical + 4 high + several medium issues and fixed the critical/highs in one pass. **C1 (critical) — nostr boosts never joined their THING, and cross-source THING dedup was silently broken.** Root cause: external-nostr `post_id` (and the `(protocol, source_item_uri)` upsert key, and reply-threading) all derive from `source_item_uri`, which the adapter encoded as a `nevent`/`naddr` **with the source's relay hints baked in** — so the same event from two relays minted two `post_id`s, and a boost (which never knows the THING's relay hints) could never reconstruct its key. Fix is adapter-side, no trigger change: all nostr `nevent`/`naddr` encoding is now **relay-free** via shared `nostrEventUri`/`nostrAddrUri` helpers used by *both* the THING path and `detectNostrRepost` (so they can't drift — the original C1 hazard); boost detection prefers the addressable `a` coordinate over `e`. **Migration `101_nostr_relay_free_identity.sql`** retires the stale relay-bearing nostr cache (soft-delete + drop stale edges) so the poller rebuilds it relay-free — SQL can't decode bech32 to re-key in place, and leaving the rows would surface duplicates; idempotent, no-op on dev (0 nostr rows). **H1 — activitypub boosts were detected but dropped** (`fetchOutbox` returned `reposts[]`; the task ignored it) → now recorded. **H2 — paying readers were wrongly paywalled deep-linking to a comment** in a gated thread (`/thread` access check gated on `focalFeedItem`, null for comments) → now keys on the conversation root. **H3 — `/extract` reader HTML was unsanitised** and Phase R newly exposed it on the public `/reader/[postId]` route → new `sanitizeArticleContent` long-form allowlist applied server-side. **H4/M6 — new hairlines** in `QuotedPostTile`/`FeedComposer` removed (→ tint fills), and `check-hairlines.sh` fixed to catch variant-prefixed borders (`hover:`/`group-hover:border-b`) it was missing. Validated: `tsc` clean (shared/gateway/feed-ingest/web); feed-ingest 144 + gateway 75 tests green; migration 101 runs clean on dev; hairline gate clean on touched files. Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 (post-cutover audit-hardening entry); CLAUDE.md updated (relay-free nostr identity + `/extract` sanitisation). **Carried as debt (audit medium/low):** external-thread N+1 walk in `/thread`; unbounded gateway `engagement/parent/quote` caches + `useNeighbourhood` cache (no eviction/cancellation); reader history-entry leak on stacked opens; `/author/:id/posts` returns `nextCursor` on the final page. **(Update 2026-06-01: all but the N+1 walk fixed — see the Last-worked entry above.)**

Previously: 2026-06-01. UNIVERSAL-POST-ADR **Phase 5 — Cut over + delete** shipped — the final phase (Phase 4 hover-modal/profile shipped just before it; see git `46bf1bd`). The unified Post model is now the workspace's only card path. Done as two stages under the resolved decision *close the deferred Phase-2/3 scope cuts first, then cut over*, and **scoped to the workspace** (the standalone `/feed` page + `/source/[id]` + the `components/feed/` card family were deliberately left intact — a separate surface, out of this phase). **Stage 1 (parity, web-only, no migration, no new gateway endpoint):** the 5 affordances the flag-on path deferred — external like/repost + inline reply (origin **interact-back** via a linked account), poll voting, fresh-on-expand counters, parent-context tile — wired via a new `usePostInteractions` hook (consolidating VesselCard's state machine behind a pure unit-tested `interactionCaps()` gate) + a `PostCardInteractive` container (keeps the §4 leaves dumb), reusing existing endpoints (`externalItems.like/repost/reply/pollVote`, `useLiveEngagement`) + kept components (`InlineReplyBox`, `ParentContextTile`, `PollDisplay`). The greenfield `POST /post/:postId/react` scoresheet (§9) was **not** needed for parity (VesselCard's external like/repost are origin interact-back, not the all.haus scoresheet — which neither path has) and stays deferred. **Stage 2 (cutover):** `NewUserVesselCard` extracted to its own module; `usePostCardFlag` gate removed; the two VesselCard-era thread-expand state vars collapsed into one `threadRefreshTick` threaded to `PostThread` via a new optional `usePostThread` `refreshKey` (so an overlay-published reply still surfaces in an open thread). **Deleted** (flag-off-exclusive, zero live importers): `VesselCard`, `useConversation`, `useExternalThread`, `ConversationView`, `ExternalAncestorRail`, `ExternalPlayscriptThread`, the `ConversationNode`/`ConversationResponse` types + `replies.conversation`, `lib/post/flags.ts`, `/dev/postcard`. `tsc` + 136 web tests + hairline check green; 9 commits (C1–C9). **Carried as debt (deliberately deferred):** (1) the greenfield `POST /post/:postId/react` external scoresheet (external repost/save stay quiet placeholders — ADR §9); (2) a later `/feed`+`/source`-scoped pass to retire the legacy global-feed card family (`feed/ExternalCard.tsx`, `FeedView.tsx`, `useNeighbourhood`, `ExternalThreadEntry`/`ParentItem`, `quoted*` fields) — kept because those surfaces still consume them; (3) gateway `GET /conversation/:eventId` is now orphaned by the web client (the `/thread` projector replaced it) but left live this phase, and `GET /external-items/:id/thread` is still used by `useNeighbourhood` on `/feed`+`/source`. **Remaining manual step:** live in-browser smoke of the now-flagless workspace (interactions parity, native vote/reply, reader pane, thread expand/re-root/pagination, `/author`). Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 5; CLAUDE.md updated.

Previously: 2026-06-01. UNIVERSAL-POST-ADR **Phase R — Unified addressable reader pane** shipped (`web/src/components/workspace/ReaderOverlay.tsx` + `web/src/components/article/ExternalArticleReader.tsx` + `web/src/app/reader/[postId]/page.tsx`). One reading environment for native + external articles, two ways in (overlay from the feed + a real shareable URL), via the **store + URL-sync** mechanism — chosen over Next.js intercepting/parallel routes (the codebase had never used those; the workspace is a heavy client surface whose state is preserved for free by an overlay that never unmounts; §0.3's "shallow/intercepting route" was a suggestion, not a hard requirement). The `useReader` store (`web/src/stores/reader.ts`) was generalised from "external url only" to a discriminated target: `openNative(dTag,{postId})` pushes `/article/<dTag>`, `openExternal(url,{postId,…})` pushes `/reader/<postId>`; `close()` pops that history entry and a `popstate` listener (registered by the overlay) makes browser **Back** close it too — one path for Back and the close button. A back-compat `open(url,title,site)` alias keeps the legacy flag-off `VesselCard` RSS path working untouched. **One reader, two entries:** `ReaderOverlay` (mounted once in `WorkspaceView`, replacing the deleted `ReaderPane`) renders over the workspace; the direct-URL full pages render the same inner readers. **Native** → the existing `ArticleReader` (gate-pass unlock + vault decrypt + markdown all run client-side, so the overlay needs no SSR `preRenderedFreeHtml`), client-fetched by d-tag via `articles.getByDTag`. **External** → `ExternalArticleReader` (lifted out of the old `ReaderPane` body, de-thin-lined), fetching reader-mode HTML via `GET /extract?url=`. **URL scheme:** native keeps its canonical SEO-rich `/article/[dTag]` page (unchanged); external gets a new `web/src/app/reader/[postId]/page.tsx` keyed on the universal `post_id` — **resolved with zero new gateway endpoints** by calling the Phase-1 `GET /thread/:postId` projector, reading the focal Post's `origin.uri`/`title`/`sourceName`, and rendering `ExternalArticleReader` full-page; a native/note/missing `post_id` → `notFound()`. **Wiring:** `mapFeedItemToPost` now surfaces `dTag` on the native article `Post` (client-transitional field on the client `Post`); `WorkspaceView`'s flag-on `openReaderFromPost(p)` routes native (`p.author.pubkey` + `p.dTag`) → `openNative`, external → `openExternal`, and is passed as `onOpenReader` to the collapsed `PostCard` **and** through `PostThread` to its `PostCard`s (so an article rendered as a `thread-parent` opens the reader instead of re-rooting). `PostCard`'s `reader-pane` click + `onOpenReader` prop already existed (Phase 2) — no card change. **Deviation from the plan's "defer `ReaderPane` deletion to Phase 5":** `ReaderPane.tsx` was deleted now (its body lifted into `ExternalArticleReader`) because leaving it would reference the removed store fields and break `tsc`; its only importer was the mount we swapped. **Validated:** `tsc --noEmit` clean (web); 32 web unit tests green (incl. a new `map-feed-item` assertion that native article Posts carry `dTag`); `scripts/check-hairlines.sh` clean on all new/touched paths. (ESLint is not configured in the repo — `next lint` prompts for first-time setup — so it is not a runnable gate; `tsc` is the typecheck gate.) Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase R; CLAUDE.md updated. **Remaining manual step:** live in-browser (flag-on workspace: `localStorage.allhaus.devflags.postcard='1'`) — native article card → overlay + `/article/<dTag>` URL + gate-pass unlock; external article card → overlay + `/reader/<postId>` URL; copy each URL into a new tab → same reader full-page; Back/Esc/scrim close + restore the feed; thread-parent article → reader (not re-root). Remaining phases: 4 (hover modal + profile), 5 (cutover + delete legacy).

Previously: 2026-06-01. UNIVERSAL-POST-ADR **Phase 3 — Thread on `PostCard`** shipped (`web/src/hooks/usePostThread.ts` + `web/src/components/post/PostThread.tsx` + pure `web/src/lib/post/thread.ts::deriveThreadView`). One thread engine over the Phase-1 `GET /thread/:postId` projector, replacing `useConversation` (native) + `useExternalThread`/`useNeighbourhood` (external) for the flag-on workspace; native + external render through the *same* `PostCard` levels (`thread-parent` / `focal` / `thread-reply`), so they are visually indistinguishable by construction. Legacy hooks/components stay live on the flag-off path until the Phase 5 cutover. **The load-bearing integration fix — the id bridge:** the workspace flows through `/workspace/feeds/:id/items` (ndk `FeedItem`), and `map-feed-item.ts` was minting `Post.id` from the *origin handle* (nostr event id / external source id), **not** the deterministic `post_id` `/thread` resolves. A nostr event id is also 64-hex, so `/thread/:eventId` passed the route regex but resolved nothing → 404. Fixed by surfacing the real `feed_items.post_id` in the workspace payload (`fi.post_id` → `feeds.ts` `FEED_SELECT`, mapped on all three `rowToItem` branches → ndk `FeedItem.postId` → adapter `id: item.postId ?? item.id`) rather than re-hashing `sha256(protocol␟handle)` in JS (which would have to reconstruct article naddr coords + the unlinked-writer fallback client-side and drift from the SQL `feed_items_derive_post_id`). `version` stays the event id (vote/reply target — never conflated with `post_id`, §2.3). **`usePostThread`** fetches the host thread once then keeps an accumulating Post pool keyed by `post_id` (`useReducer`; module cache 60s/200): `reroot(id)` is pure client-side when the node's subtree is loaded (§8), fetches `/thread/:id` + merges only for an unloaded subtree; `loadMore()` keysets the focal's descendants; `backToRoot()` returns to the host; a `reqSeq` guard drops stale re-root settles. **`deriveThreadView(pool, focalId)`** is the one pure walk (7 vitest cases): cycle-guarded `inReplyTo` walk to root (root-first ancestors) + transitive subtree flattened by `(publishedAt, id)` ascending — matching the server's chronological flatten + keyset order so appended pages merge in place. **`PostThread`** paints ancestors above / focal / replies below (all `PostCard`), with **re-root in place no residue** (§4.3 `reroot-focal`), **scroll-centre focal** on expand + re-root, **gutter overflow arrows** (`IntersectionObserver` on top/bottom sentinels), **`↑ Full conversation`**, focal click **collapses the card**, **`Show N more replies`** (§8 lazy, N=5). **WorkspaceView** flag-on render branches: collapsed → `PostCard level="feed"`; expanded note/external → `PostThread`. **Articles open the reader pane (Phase R), so they have no inline thread** (§3.1) — an article root of a native comment thread renders as a `thread-parent` preview whose click is `reader-pane` (inert until Phase R), never a re-root target. Native reply opens the compose overlay via `onReply`. **Documented scope cuts (consistent with Phase 2):** external inline reply + external all.haus reactions deferred; `isOwnContent` left undefined (no viewer pubkey at this render site — parity with the feed branch); boost attribution (`repostEdges`) threaded but unrendered until threads accumulate boosts; fresh-on-expand counter refresh + counter-click modals stay Phase-3-greenfield TODOs on the leaf components. **Validated:** `tsc --noEmit` clean (web + gateway); 32 web unit tests green (7 new `thread.test.ts` + 8 adapter + 17 level-spec); `npm run lint` clean on the new files; `scripts/check-hairlines.sh` clean on all new/touched paths; live dev DB derive-parity (all 1844 `feed_items` non-null 64-hex `post_id`; all 949 notes `post_id == feed_items_derive_post_id('nostr', nostr_event_id)`, so the `item.id` fallback never fires). Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 3; CLAUDE.md updated. **Remaining manual step:** live in-browser thread interaction (flag-on workspace: `localStorage.allhaus.devflags.postcard='1'`). Remaining phases: R (addressable reader pane), 4 (hover modal + profile), 5 (cutover + delete legacy).

Previously: 2026-05-31. UNIVERSAL-POST-ADR **Phase 1 `/feed` slice** shipped (`gateway/src/routes/post-feed.ts`; `/thread` half still pending) — the first read surface over the unified Post model, the consumer of the Phase 0a–0c identity/edge plumbing. `GET /feed/:feedId` (`feedId` ∈ `following`/`explore`, the legacy `reach` dial) returns the §9 contract `{ items: Post[], attribution, nextCursor }` and **coexists with the legacy `GET /feed` (`timeline.ts`)** until the Phase 5 cutover. The §5 hotness score is computed **live at query time, not materialised** — decay depends on `now()` and on `repost_edges` that change continuously, so a cron-materialised score would be stale; this supersedes the HN-gravity `feed-scores-refresh` score *for this endpoint only* (legacy `/feed` still reads `feed_items.score`). Score = `recencySeed + saturate(Σ trustWeight·timeDecay(boost age))`: recencySeed = `exp(-ln2·age/halflife)` per THING, boost mass summed from `repost_edges` joined on `post_id`, **saturation applied to the accumulated mass** (a documented deviation from §5's per-term `Σ saturate(...)` notation — per-term sat is linear in the pile, contradicting the stated "tenth boost lifts far less than the second"). Dedup-to-one via `DISTINCT ON (post_id)`. Score knobs in `platform_config` with defaults (`feed_recency_halflife_hours`=12, `feed_boost_halflife_hours`=6, `feed_boost_ceiling`=3, `feed_boost_half_sat`=3); `boost_ceiling > 1` operationalises the §5 "boosts CAN outrank fresh direct content" ceiling decision. Items map to the §2.2 `Post` shape (unified author/origin/body; `inReplyTo`/`quotes` resolved to parent `post_id`s via the same `feed_items_derive_post_id()` the identity trigger uses, so edges are `/thread`-resolvable); attribution is the per-Post `repost_edges` set, newest booster first, fetched only for the page. **Two deliberate, documented scope boundaries (not silent caps):** (1) boosts **re-float in-network candidates** but don't **inject** out-of-network THINGs — injection needs the follow↔external-identity bridge + trust weighting (deferred §9/§11) and would break the candidate-parity Accept; (2) the legacy explore feed's interleaved `new_user` cards aren't Posts, so this `Post[]` endpoint omits them. Validated against the live dev DB (reader w/ 30 follows): `tsc` clean; **candidate parity exact** (265 distinct `post_id`s == legacy following membership — no items added/dropped); dedup holds (265 rows = 265 distinct `post_id`s, 0 null); a rolled-back synthetic test (oldest post, 148 days, recencySeed≈0 + 10 fresh boosts) floated it to **rank 1 of 265** at score 2.31 (saturation correct: 10 boosts → 2.31 not 10, 2 boosts → 1.2), above all unboosted content — confirming re-float + ceiling + saturation. **Not yet exercised:** a live HTTP round-trip (gateway not running; data fetch needs an auth cookie) — the TS mapper + attribution query are `tsc`-checked over confirmed columns but unrun against a real request. Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 1; CLAUDE.md updated (Key docs entry + feed-query note). Remaining: Phase 1 `/thread` (the shape-unification of `useConversation` + the external thread endpoint into one `Post[]`+`repostEdges` walk), 2 (one `PostCard` + matrix), R (addressable reader pane), 3 (thread engine), 4 (hover modal + profile), 5 (cutover).

Previously: 2026-05-31. UNIVERSAL-POST-ADR Phase 0c shipped (migration `100_repost_edges.sql` + feed-ingest TS) — repost/boost ingestion, **ingestion-only** (feed assembly that consumes the edges is Phase 1; Phase 0 stays UI-neutral). Greenfield per the ADR: every adapter wrote `is_repost=false` and dropped repost-shaped input at the source (nostr `REQ` omitted kind 6/16; Jetstream filtered to `app.bsky.feed.post`; activitypub skipped `Announce`; getAuthorFeed skipped `reason`). A bare repost has no body, so it is an **edge** (booster handle + boosted THING), not a Post. New table `repost_edges`: `target_post_id` is `feed_items_derive_post_id(protocol, stableHandle)` — the *same* migration-098 function `feed_items.post_id` uses — so edges group by THING and join `feed_items` on `post_id`; **not** an FK (the THING may be un-ingested, and `post_id` isn't unique on `feed_items`). Idempotency = two partial-unique indexes: `(protocol, origin_uri)` where the protocol exposes the boost's own id, else `(protocol, target_post_id, actor_handle)`. `actor_external_author_id` lazily FKs `external_authors` (no minting — a pure booster has no author row yet); `trust_weight numeric DEFAULT 1` (§9); `boosted_at` is the boost time (§5 recency), never the original's publish time. One shared writer `feed-ingest/src/lib/repost-edge.ts::recordRepostEdge` derives the target via the SQL fn + `ON CONFLICT DO NOTHING`; a boost never writes an `external_items`/`feed_items` THING. Detection (pure fns): **atproto** `detectAtprotoRepostFromCommit` (Jetstream `app.bsky.feed.repost`, added to `WANTED_COLLECTIONS` + listener collection branch) and `detectAtprotoRepostFromReason` (getAuthorFeed `reasonRepost`, backfill task); **activitypub** `detectActivityPubRepost` (`Announce`, hooked into `fetchOutbox` before the `Create`-only filter); **nostr** `detectNostrRepost` (kind 6/16 added to the relay `REQ`, target from `e`/`a` tag). rss/email unchanged. Validated: 17 detection unit tests (`repost-detect.test.ts`) + full feed-ingest suite (142 tests) green, `tsc --noEmit` clean; a rolled-back synthetic SQL test against live dev confirmed derive-parity (3/3 real external THINGs: `post_id == derive(source_protocol, source_item_uri)`), edge→THING join, both idempotency paths, and cross-source dedup (2 boosters of 1 THING → 2 edges, 1 `target_post_id`). `schema.sql` mirrored (table + PK + 4 indexes + FK) and verified to load clean into a throwaway DB. **Known limitation (documented in `repost-edge.ts`):** edge→THING join is exact for external content; native targets (post_id minted under `'nostr'`, not the source protocol) and nostr targets (THING keyed on relay-encoded nevent/naddr vs the edge's raw event id / `kind:pubkey:dtag`) won't always join their THING — edges still dedup + carry attribution; THING-binding for those is best-effort until identity is normalised in a later phase. Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 0c; CLAUDE.md feed_items note updated. Remaining: Phase 1 (unified `/feed` + `/thread`, consumes these edges), 2 (one `PostCard` + matrix), R (addressable reader pane), 3 (thread engine), 4 (hover modal + profile), 5 (cutover).

Previously: 2026-05-31. UNIVERSAL-POST-ADR Phase 0b shipped (migration `099_external_author_identity.sql`) — the **author** half of the unified Post model. Mints a stable external-author identity record (`author.id`) for every tier-A/B external author (one carrying a stable origin handle), the load-bearing key for the later §4.4 / VI.3 constructed external-author profile (aggregates an author's Posts across sources by `author.id`). New table `external_authors` keyed on `UNIQUE(protocol, stable_handle)` (the dedup key) with a nullable `account_id` lazy-claim slot; new `feed_items.external_author_id` FK column — native `author_id` → `accounts` is **left untouched** (separate id-space, used by timeline block/mute filters). Like 0a, done by **extending the one `feed_items_post_identity` trigger** rather than per-adapter: it already joins `external_items` for version/biddability, so the author handle + metadata are at hand, and one definition covers every dual-write site plus the backfill. The author block is **mint-once-guarded** (`external_author_id IS NULL AND biddability_tier IN ('A','B')`) so tier-C/D rows and hot `score`/author-refresh UPDATEs never re-run the `external_items` join. Stable handle per protocol: nostr_external → `interaction_data->>'pubkey'` (author_uri is null there), atproto → `author_uri` (DID), activitypub → `author_uri` (actor URI); rss/email get no record (tier C/D → plain-text byline). Upsert COALESCE-merges `display_name`/`handle`/`handle_uri`/`avatar` so a later NULL can't clobber a known value. Validated against the live dev DB: 1844 rows backfilled, all invariants pass (tier-A/B external → non-null id, tier-C/D + native → null, no duplicate handle); a rolled-back synthetic test exercised all four protocol branches, cross-source dedup (one pubkey via two sources → one id), and mint-once idempotency. `schema.sql` mirrored (table + column + 2 constraints + 2 indexes + 2 FKs + replaced trigger fn) and verified to load clean into a throwaway DB. Recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 0b; CLAUDE.md feed_items note updated. No app code changed. The account **claim flow** (filling `account_id`, future hook: `linked_accounts (protocol, external_id)`) and all read-side/UI consumption (bylines → `/author/:id`, hover modal, profile — Phases 2/4, which flip the CLAUDE.md byline-routing rule) are out of scope here. Remaining: Phase 0c (repost detection + RepostEdge), 1 (unified `/feed` + `/thread`), 2 (one `PostCard` + matrix), R (addressable reader pane), 3 (thread engine), 4 (hover modal + profile), 5 (cutover).

Previously: 2026-05-31. UNIVERSAL-POST-ADR Phase 0a shipped (migration `098_feed_items_post_identity.sql`) — the first step of the new unified Post model. Mints a deterministic per-THING `post_id`, an edit-detecting `version`, and a persisted `biddability_tier` on `feed_items`. Implemented as a single `BEFORE INSERT/UPDATE` trigger (`feed_items_post_identity`) **rather than per-adapter** as the ADR bullet envisioned: one authority covers all ~10 scattered dual-write sites (gateway article/note routes + every feed-ingest adapter/ingest helper + reconcile crons) **and** the backfill with no TS↔SQL duplication; later phases only read the columns, so it's forward-compatible. `post_id` = `sha256(protocol␟stableHandle)` via `feed_items_derive_post_id` (article→naddr coord `30023:pubkey:dtag`, note→event id, external→`source_item_uri`); native `version` = `nostr_event_id`, external `version` = canonical content hash via `feed_items_content_version`; `version`/`biddability_tier` recompute is gated on content/identity-column changes so hot `score`-refresh and author-refresh updates skip the external_items join + hash. Validated against the live dev DB: 1844 rows backfilled, 0 nulls, 0 duplicate `post_id`; full insert / edit-supplant (same id, new version) / tier-matrix (A native·nostr·atproto, B activitypub, C rss·email w/ author, D otherwise) assertion passes. `schema.sql` mirrored (3 columns + CHECK inline, 3 functions, index + trigger) and verified to load clean into a throwaway DB. The trigger-vs-per-adapter deviation is recorded in `docs/adr/UNIVERSAL-POST-ADR.md` §10 Phase 0a; CLAUDE.md updated (Key docs entry + feed_items note). No app code changed. Remaining: Phase 0b (external-author identity records, tier A/B), 0c (repost detection + RepostEdge), 1 (unified `/feed` + `/thread`), 2 (one `PostCard` + matrix), R (addressable reader pane), 3 (thread engine), 4 (hover modal + profile), 5 (cutover).

Previously: 2026-05-30. Browser-verified the workspace card-behaviour work (commits `6a529c3`/`0c12091`/`a22c750`) in a real headless Chromium against the running stack with a seeded Bluesky fixture: confirmed body-click neighbourhood expansion (parent tile above via `/parent`, live reply playscript below via `/thread`), byline → internal `/source/[id]` (never origin), source-attribution line as the one route out (`window.open` origin), and `QuotedPostTile` nested mini-card + `MediaBlock` link preview. Two issues found and fixed: (1) **web `/api` proxy baked wrong** — `next.config.js` `rewrites()` is evaluated at `next build`, but the web Dockerfile had no `GATEWAY_URL` build arg, so it baked the `localhost:3000` fallback (= the web container itself → `ECONNRESET` on every API call, blank `/workspace`); added `ARG GATEWAY_URL`/`ENV` before `npm run build` in `web/Dockerfile` and `GATEWAY_URL: http://gateway:3000` to the compose web `build.args`. Verified end-to-end through the real proxy on a one-off container from the rebuilt image (login/`/workspace/feeds` now 200, card renders). (2) **`/parent` slower/flakier than `/thread`** — `safeFetch` builds a fresh SSRF-pinned agent per call (no keep-alive), and `/parent` did the parent fetch plus a sequential grandparent fetch on the critical path, both at the 5s cap; raised the essential cap `NEIGHBOURHOOD_FETCH_TIMEOUT_MS` to 8s (cold DNS+TLS headroom) and moved the optional grandparent-tag fetch to a tight new `GRANDPARENT_FETCH_TIMEOUT_MS` (2.5s) so it can't extend/jeopardise the parent response (it's also populated independently by `external_parent_prefetch`). Docs updated: `CLAUDE.md`, `CARD-BEHAVIOUR-ADR.md` (timeout caps), `DEPLOYMENT.md` (`GATEWAY_URL` build-arg gotcha). `tsc --noEmit` clean for `gateway`.
Previously: 2026-05-30. Card behaviour Phase 4 complete — closed gaps #2, #3, #4, #5 (#1 was done 2026-05-29), so all five verified-against-spec divergences are resolved; only the explicitly-deferred §VI.3 constructed external author profile remains (its own ADR). #3 external byline routing: built the minimal internal source surface — gateway `GET /api/v1/sources/:id` (`gateway/src/routes/sources.ts`) returns source metadata + a cursor-paginated page of that source's external items, reusing timeline's now-exported `FEED_SELECT`/`FEED_JOINS`/`feedItemToResponse`/`parseCursor` (and `timeline.ts` now returns `externalSourceId`); new `/source/[id]` page (`SourceSurface` client component); `ExternalCard` byline `next/link`s to `/source/{externalSourceId}` instead of the origin platform (origin link stays on the §VI.4 source-attribution line, the one route out), degrading to a non-link span when no source id. See the Phase 4 detail in the "verified-against-spec gaps" block below for #2/#4/#5. `tsc --noEmit` clean for `gateway` + `web`; 75 gateway tests green; not yet browser-verified.
Previously: 2026-05-30. Card behaviour Phase 4 #2 + #4 + #5 (spec-conformance gaps). #2 thread hydration: kept the `/external-items/:id/thread` + `/parent` endpoint pair but closed the three real misses — per-route rate limit `{ max: 30, timeWindow: "1 minute" }`, a 5s outbound `timeout` (`NEIGHBOURHOOD_FETCH_TIMEOUT_MS`) on every Bluesky/Mastodon parent/grandparent/thread source fetch, and a server-signalled `partial` flag now carried on `ThreadResponse` + `ParentContextResponse` (set when a source fetch fails/times out for a reply that expects content); `useNeighbourhood` prefers the server flag over inferring `partial` from a rejected promise. #4 NoteCard headline: body-expand region now carries an INTERIM comment mirroring `ExternalCard`'s headline marker (notes have no permalink region; body click expands until one exists). #5 empty-state reply affordance: `NeighbourhoodEmptyState` takes an optional `onReply` and renders `NO CONVERSATION YET — BE THE FIRST TO REPLY` as a composer-opening button when wired (`ExternalCard` passes `handleReply`). 75 gateway tests green; `tsc --noEmit` clean for `gateway` + `web`; not yet browser-verified. Gap #3 (external byline → internal source surface) remains, blocked on a scope decision — it needs a new source-surface route that doesn't exist and overlaps the deferred §VI.3 constructed external author profile.
Previously: 2026-05-29. Card behaviour Phase 4 #1 (neighbourhood expansion for native cards) + 2026-05-29 verification audit write-up. The audit verified shipped Phases 1–3 against CARD-BEHAVIOUR-ADR section-by-section and found five spec-conformance gaps (now tracked in the "verified-against-spec gaps" block below + ADR §X Phase 4). Closed gap #1: `NoteCard` is now kind-aware (`kind`/`dbId`/`replyToEventId` on `NoteEvent`; delete via `/replies/:dbId` for kind-1111 comments, vote/quote use `note.kind`) and hydrates its parent above the anchor on expand via a cached `useNativeParent` hook → `GET /content/resolve`, with `ExternalCard`-style scroll-pinning so the anchor never jumps. New `NativeParentCard` in `NeighbourhoodCard.tsx` renders the resolved note/article in the dimmed-bar inset grammar. Profile Replies (`SocialTab`) now render through `NoteCard` (mapping `DbReply → NoteEvent`, `showReplyThread={false}`), retiring the bespoke `ReplyCard`. `ArticleCard` unchanged by design (always `is_reply=false`). No backend change needed. `tsc --noEmit` clean for `web`; not yet browser-verified. Gaps #2–#5 remain.
Previously: 2026-05-27. Card behaviour audit fix-up — closed 5 dead-ends from Phases 1–3. (1) Migration 097 applied (had been written but never run; also applied pending 094–096); `is_reply` column now live, backfill populated, all dual-write paths functional. (2) Provenance line now shows `↳ REPLYING TO @handle` when parent author is known (correlated subqueries in both `timeline.ts` and `feeds.ts` FEED_SELECTs look up parent author from `external_items` for external replies, from `notes`→`accounts` for native note replies; `replyToAuthor` field piped through API → FeedView → NoteCard/ExternalCard; falls back to `A POST` when parent author unknown). (3) ExternalCard ActionSheet wired — was imported but never rendered; now shows `⋯` button on touch viewports with SHARE action. (4) Source Follow in AuthorModal wired — `author-card.ts` now returns `protocol`/`sourceUri` from `external_sources` in `followTarget`; FollowButton calls `feedsApi.subscribe()` instead of silently reverting. (5) ArticleCard bookmark in ActionSheet wired — replaced no-op stub with optimistic `bookmarks.add/remove` toggle. `schema.sql` regenerated. `tsc --noEmit` clean for `gateway` and `web`.
Previously: 2026-05-26. Card behaviour Phase 3 shipped (author affordances). New `GET /api/v1/author-card` endpoint resolves tier-aware author/source metadata for native users (DB account + follower/following/article counts + follow state) and external items (protocol dispatch: Bluesky via AppView `getProfile`, ActivityPub via actor doc + Mastodon REST fallback, Nostr via denormalised item data, RSS/email via source table). 5-min in-memory cache. `AuthorModal` component: portal-rendered hover modal with 300ms hover-intent delay, positioned relative to byline anchor, tier-degraded rendering (A/B: avatar+name+handle+bio+counts+follow button, C: source name+description, D: limited info). `useAuthorHover` hook manages hover state with `(hover: hover)` media query check (desktop-only). `useAuthorCard` hook with module-level cache + inflight dedup. `ActionSheet` component for touch devices: `⋯` trigger + portal popover with outside-click dismissal, used by ArticleCard (Quote/Bookmark/Share), NoteCard (Quote), and ExternalCard (Share). Desktop secondary actions use `[@media(hover:hover)]` inline/hidden split. `authorId` now piped from `timeline.ts` `feedItemToResponse` to frontend card components. `atproto-resolve.ts` and `activitypub-resolve.ts` extended with follower/following/post count fields.
Previously: 2026-05-26. Card behaviour Phase 2 shipped (neighbourhood expansion). Body-click on feed cards now hydrates the conversational neighbourhood inline: parent above, replies below, toggle on second click. New `useNeighbourhood` hook dispatches parallel parent + thread fetches to existing `/external-items/:id/parent` and `/external-items/:id/thread` endpoints (module-level cache, 10-per-page reply pagination, upward parent-chain walking via `loadParent`). New `NeighbourhoodCard` inset renderer (dimmed grey left bar, mono-caps byline, engagement icons) with `NeighbourhoodSkeleton`, `NeighbourhoodFailureStub` (quiet per-instance domain label), and `NeighbourhoodEmptyState` variants. `ExternalCard` is the primary consumer — renders parent chain above the anchor (scroll-stabilised via `useLayoutEffect` + `requestAnimationFrame` delta adjustment), replies below with `SHOW N MORE REPLIES` pagination and `↳ SHOW PARENT` upward walking. Tier-driven states: skeleton during fetch (A/B), quiet `COULDN'T REACH {INSTANCE}` stub on Mastodon failure (B), `NO CONVERSATION YET` for RSS/email (C/D). `ArticleCard` body-click expansion shows compact `ReplySection` inline. `NoteCard` body-click lifts the 3-reply preview limit to show the full thread. Also fixed: `FeedView.tsx` response mapping now passes `isReply`, `biddabilityTier`, `sourceReplyUri`, `sourceQuoteUri`, and engagement counts through to card components (Phase 1's provenance line was wired but never received data). `tsc --noEmit` clean for `web`.
Previously: 2026-05-25. Card behaviour Phase 1 shipped (CARD-BEHAVIOUR-ADR + build plan). Migration 097 adds `is_reply` boolean to `feed_items` with backfill from `external_items.source_reply_uri` and `notes.reply_to_event_id`. All 14 dual-write INSERT sites updated. Biddability tier (`A`/`B`/`C`/`D`) computed at response time in both `timeline.ts` and `feeds.ts`. Click region map: ArticleCard headline navigates via `<Link>`, body-expand wired for Phase 2; ExternalCard headline links to source URL (new tab), body-expand wired; NoteCard body-expand wired. `↳ REPLYING TO A POST` provenance line on reply cards (tier A/B only). Source attribution (`VIA {PROTOCOL} · {handle}`) made a clickable link; "View original" footer link removed. Feed-ingest Slice 9 shipped as part of this work. `tsc --noEmit` clean for `gateway`, `feed-ingest`, and `web`.
Previously: 2026-05-25. Feed-ingest attack plan extended with slices 7–9. Updated "What exists today" with email protocol + cross-cutting capabilities table. Slice 7: AP inbox (push delivery — real-time AP with edit/delete propagation, Follow lifecycle, coexistence with outbox polling; posture-gated). Slice 8: cross-source identity linking (dedup via `external_identity_links` table, query-time content fingerprint matching, automated bridge/cross-link/domain detection, subscription UI). Slice 9: CARD-BEHAVIOUR-ADR feed support (`is_reply` boolean on `feed_items` + biddability tier on API). Recommended sequence updated. CARD-BEHAVIOUR-ADR added to CLAUDE.md key docs + feature-debt.md strategic initiatives. Constructed external author profiles tracked (deferred from CARD-BEHAVIOUR-ADR §VI.3).
Previously: 2026-05-25. Feed ingest Slice 3 — email newsletter ingestion. Push-based ingest via Postmark inbound webhook (`POST /inbound-mail/:secret`). Migration 096 adds `ingest_address` on `external_sources` and `canonical_url` on `external_items`. Email adapter (`feed-ingest/src/adapters/email.ts`) handles newsletter-specific HTML sanitisation: strips tracking pixels (1×1 images, known tracker domains), collapses table-based layouts, strips MSO conditional comments, extracts canonical URL from "view in browser" links, then runs through `sanitizeContent()`. Dual-write helper (`feed-ingest/src/lib/email-ingest.ts`) with two-layer dedup: canonical URL cross-source match (catches newsletter-to-RSS overlap) + title+date fuzzy match (±1 hour). Subscribe flow generates `<sourceId>@ingest.all.haus` address with copy-to-clipboard UI. Email sources excluded from poll dispatch (push-only). `VIA EMAIL` badge on cards, reader pane on click. Engagement actions suppressed (tier4). `tsc --noEmit` clean for `gateway`, `feed-ingest`, and `web`.
Previously: 2026-05-25. Workspace slice 34 — numeral roundel → bottom-left numeral. Feed numeral originally moved from italic serif badge at VesselBar right end to a 26px black roundel mounted on the top-left corner. Later (2026-05-26) the roundel was removed: the numeral now renders as plain `label-ui` text at the bottom-left corner of the chassis at 50% opacity. Hover reveals descriptive name as a sharp-cornered `label-ui` chip above the numeral (no `borderRadius`; native `title` tooltip removed). Click-drag moves the vessel; double-click opens FeedComposer. `numeral`/`descriptiveName` props removed from VesselBar. `tsc --noEmit` clean.
Previously: 2026-05-25. Workspace slice 33 — feed numeral system + cleanup. Feeds now use a numeral system (next vacant positive integer by `createdAt` order) instead of the name-label nameplate; italic serif numeral at the right end of VesselBar, optional descriptive name on hover / in ForallMenu when hidden. Feed creation no longer requires a name (gateway schema defaults to empty string). Minimize removed (overlapped with hide, wasn't working properly) — `minimized` field, `setVesselMinimized`, `▁`/`□` button, content guard all deleted. Save system removed entirely (slice 20 retired) — `feed_saves` routes, `savedIds` tracking, `savedView` toggle, Save/Saved button on card strip all deleted. Caught-up tile refined: new copy, both buttons + scroll-up dismiss (scroll + wheel listeners), reappears only on next no-new-content pull-to-refresh. `tsc --noEmit` clean for `gateway` and `web`.
Previously: 2026-05-25. Workspace Full View post-ship fixes: (1) `PullToRefresh` now accepts explicit `scrollRef` prop — `Vessel` passes its content-div ref directly, fixing pull-to-refresh on default-height vessels where `findScrollParent` fell through to the wrong element (Bug 1 in `WORKSPACE-FULL-VIEW-DIAGNOSIS.md`); (2) `ParentContextTile` lifted out of the `expanded` ternary in `ExternalVesselCard` so reply items show parent context without click-to-expand (Bug 3). Bug 2 (missing engagement counts) is operational — no code typo exists; the `onLike`/`onReply` confusion reported in the diagnosis was incorrect (line 677 already reads `onReply`). Remaining: confirm `feed-ingest` worker running for engagement count population; decide on always-visible engagement row (design call); consider `source_reply_uri` backfill for pre-`4a8baac` items.
Previously: 2026-05-25. Completed: Workspace Full View Phase 6B (reply grouping) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §3.3/§15. Pure `groupReplies()` post-processing in `sourceFilteredItems()` — groups external items sharing the same `source_reply_uri` (2+ siblings) into `reply_group` envelopes positioned at the first occurrence. No SQL or cursor changes. Frontend: `WorkspaceFeedApiReplyGroup` + `ReplyGroupItem` types, `ReplyGroupCard` component renders `ParentContextTile` once per group + chronological `ExternalPlayscriptEntry` list (5 initial, paginated). Grey-300 left bar. All Workspace Full View phases (1–6B) now shipped.
Previously: 2026-05-25. Completed: Workspace Full View Phase 6A (bug fixes + data gaps) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §15. Six fixes: (1) context-only items filtered from `timeline.ts` and `feeds.ts` WHERE clauses so prefetched parents don't leak as standalone cards; (2) engagement count columns (`ei.like_count/reply_count/repost_count`) added to `timeline.ts` FEED_SELECT + `feedItemToResponse` so platform timeline renders counts; (3) `extractGrandparentTag` stub replaced — grandparent fetch moved before INSERT in both `fetchBlueskyParent` and `fetchMastodonParent`, persisted in `interaction_data.grandparent` JSONB, ON CONFLICT also updates `interaction_data`; same pattern added to `external-parent-prefetch.ts` (new `fetchBlueskyGrandparentTag`/`fetchMastodonGrandparentTag` helpers); (4) Mastodon prefetch INSERT now includes `source_reply_uri` and `content_text` columns + `in_reply_to_id` in status type; (5) `WorkspaceFeedApiExternal` type extended with `contentWarning` and `poll` fields; (6) `useLiveEngagement` cache TTL aligned to 30s (was 60s vs backend 30s), catch block resets to snapshot counts and clears `fetched.current` so retry is possible on next expand. `tsc --noEmit` clean for `gateway`, `feed-ingest`, and `web`.
Previously: 2026-05-24. Completed: Workspace Full View Phase 5 (final phase) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md`. Six sub-phases: 5A — content warnings (migration 093 adds `content_warning TEXT` to `external_items`; AP adapter extracts `sensitive` + `summary`; `ContentWarning` toggle component wraps cards); 5B — poll display + voting (AP adapter extracts `oneOf`/`anyOf` polls into `interaction_data`; `PollDisplay` component; `POST /external-items/:id/poll-vote` endpoint; outbound `voteMastodonPoll` dispatcher); 5C — reader pane (`GET /extract?url=` endpoint via Readability + jsdom; `useReader` Zustand store; `ReaderPane` overlay for RSS articles); 5D — inline video embeds (YouTube/Vimeo detection in `MediaBlock`, oEmbed iframe rendering, `prefers-reduced-motion` fallback); 5E — pull-to-refresh + empty states (`PullToRefresh` touch component, `EmptyFeedTile` with no-sources/no-items variants); 5F — context-only GC cron (`external-context-gc` task deletes unreferenced `is_context_only` items older than 30 days). All workspace full view phases (1–5) now shipped.
Previously: 2026-05-24. Completed: Workspace Full View Phase 4C (inline reply + dual-write) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §5.1. New `POST /api/v1/external-items/:id/reply` endpoint validates linked-account ownership + protocol match, rejects RSS (422). Signs kind 1 Nostr event via key-custody — NIP-10 `e`/`p` reply tags for nostr_external, plain kind 1 for atproto/activitypub (cross-post handles threading). Creates note with `external_parent_id` + feed_items dual-write in one transaction, enqueues relay publish, then best-effort enqueues outbound cross-post (`enqueueCrossPost` for atproto/activitypub, `enqueueNostrOutbound` for nostr_external). No new dispatcher work — `reply` handler already exists in `outbound-cross-post.ts`. New `InlineReplyBox` component (`web/src/components/workspace/InlineReplyBox.tsx`): inline text field below cards and playscript thread entries, platform badge (`REPLYING VIA BLUESKY` etc.), linked-account gating (hidden for RSS, greyed/prompt when no account), auto-grow textarea, Cmd+Enter submit, optimistic reply count increment. `EngagementRow` reply button made interactive (speech bubble, same pattern as like/repost — hidden for RSS, greyed when no linked account). `ExternalPlayscriptEntry` Reply tag wired to open InlineReplyBox per-entry; `ExternalPlayscriptThread` manages `replyingToId` state with linked account passthrough from ExternalVesselCard. `NoteVesselCard` renders `ParentContextTile` when note has `externalParentId`. Feed queries (`timeline.ts`, `feeds.ts`) pipe `n.external_parent_id` through to frontend types (`NoteEvent`, `WorkspaceFeedApiNote`). No new migration needed (`external_parent_id` column added in migration 092).
Previously: 2026-05-24. Completed: Workspace Full View Phase 4B (cross-platform repost/boost) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §5.3. New `POST /api/v1/external-items/:id/repost` endpoint validates linked-account ownership + protocol match, rejects `nostr_external` and `rss` (per §5.5 matrix). `enqueueRepost` helper in `gateway/src/lib/outbound-enqueue.ts` uses synthetic dedup key `repost:{itemId}`, same transactional pattern as `enqueueLike`. Feed-ingest dispatcher extended with `repost` case: `repostBlueskyRecord` (`app.bsky.feed.repost` createRecord via DPoP-signed XRPC) in `atproto-outbound.ts`, `reblogMastodonStatus` (resolve remote status + `POST /statuses/:id/reblog`) in `activitypub-outbound.ts`. Frontend: `externalItems.repost()` API method, interactive repost-arrows button on `EngagementRow` — hidden for nostr_external/rss per spec, greyed/disabled with tooltip when no matching linked account, optimistic +1 with crimson accent on success, error rollback. No new migration needed (`'repost'` already in `action_type` constraint from migration 092). `tsc --noEmit` clean for `gateway`, `feed-ingest`, and `web`. Phase 4C (inline reply + dual-write) + Phase 5 remain.
Previously: 2026-05-24. Completed: Workspace Full View Phase 3 (thread expansion) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §4. New `GET /api/v1/external-items/:id/thread` endpoint returns `{ ancestors, descendants }` as flat `ExternalThreadEntry[]` arrays. Bluesky via `getPostThread` (depth 50, parentHeight 100; recursive parent chain walked for ancestors, replies tree BFS-flattened + chronologically sorted for descendants; blocked/notFound posts skipped). Mastodon via `GET /statuses/:id/context` (maps directly). Nostr/RSS return empty arrays (deferred). 60s in-memory TTL cache. Frontend: new `useExternalThread` hook (module-level session cache, fetch-on-expand). New `ExternalPlayscriptThread` + `ExternalPlayscriptEntry` components render playscript format — speaker line (mono-caps, external author link), dialogue (14.5px Jost, HTML or text), non-adjacent parent `→` arrows, 10-entry pagination with "Show N more replies". `ExternalVesselCard` wired: synthetic `ReplyTarget` enables Thread/Hide thread toggle in `CardActions`; `ExternalCardThread` wrapper handles loading skeleton, error, and empty states; thread button suppressed for RSS and nostr_external. `tsc --noEmit` clean for `gateway` and `web`. Phases 4–5 remain (cross-platform interactions, reader pane).
Previously: 2026-05-24. Completed: Workspace Full View Phase 2 (live engagement + parent context) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md` §2–3. Migration 091 adds `is_context_only` to `external_items` (marks parent posts fetched purely as reply context, excluded from feed_items). New `gateway/src/routes/external-items.ts` with two endpoints: `GET /api/v1/external-items/:id/engagement` (live fetch from Bluesky `getPosts` / Mastodon `GET /statuses/:id`, 30s in-memory TTL cache, side-effect updates snapshot columns) and `GET /api/v1/external-items/:id/parent` (checks DB for parent by `source_reply_uri`, fetches from source on miss, stores as `is_context_only = TRUE`, returns parent + optional grandparent tag). New `feed-ingest/src/tasks/external-parent-prefetch.ts` eagerly fetches parent posts at ingest time — enqueued by Jetstream listener, atproto backfill, and AP poll tasks when `source_reply_uri` is non-null. Frontend: `sourceReplyUri`/`sourceQuoteUri` added to `WorkspaceFeedApiExternal` and `ExternalFeedItem` types + WorkspaceView mapper. New `useLiveEngagement` hook fetches live counts on card expand (60s client-side cache, falls back to snapshot). New `ParentContextTile` component renders parent post above reply content when expanded (byline + content + mini engagement row + grandparent tag). `ExternalVesselCard` integrates both: hook drives `EngagementRow` with live values, parent tile appears above expanded reply content. Reply grouping (§3.3) deferred. `tsc --noEmit` clean for all services; 103 feed-ingest tests pass. Phases 3–5 remain (threads, cross-platform interactions, reader pane).
Previously: 2026-05-24. Completed: Workspace Full View Phase 1 (content fidelity + engagement counts) per `docs/adr/WORKSPACE-FULL-VIEW-SPEC.md`. Migration 090 adds `like_count`, `reply_count`, `repost_count` to `external_items`. Atproto backfill captures counts from `getAuthorFeed` API response at ingest time. New `external_engagement_refresh` cron (every 30 min) batch-fetches fresh counts from Bluesky (`getPosts`, 25/call) and Mastodon (`GET /statuses/:id`, parallelised per instance) for items published in the last 7 days. Gateway `FEED_COLS` + `rowToItem` extended to select and return counts. Frontend types (`WorkspaceFeedApiExternal`, `ExternalFeedItem`) + WorkspaceView mapper carry counts end-to-end. `ExternalVesselCard` expanded view now renders `contentHtml` via `dangerouslySetInnerHTML` with `[&_p]:mb-2` paragraph spacing (fixes RSS paragraph break loss where `contentText` stripped all HTML). New `EngagementRow` component renders heart/speech-bubble/repost icons with counts in mono-caps 11px; hidden when all zero (RSS); repost icon hidden for Nostr per spec §5.5. Media renders in expanded view. `tsc --noEmit` clean for `gateway`, `feed-ingest`, and `web`. Phases 2–5 remain (live engagement, parent context, threads, cross-platform interactions, reader pane).
Previously: 2026-05-24. Completed: hardening pass (9 items). (1) `sourceRowToResponse` now maps DB `sampling_mode` to API vocabulary (`random`/`top`), matching author-volume endpoint; FeedComposer client-side re-mapping removed; `WorkspaceFeedSource.samplingMode` typed as `"random" | "top"`. (2) Cursor format mismatch between placeholder (3-part) and source-filtered (2-part) paths documented — stale cursor causes graceful first-page restart, no code change needed. (3) `any` types removed from feed API layer: `WorkspaceFeedApiItem` discriminated union with proper literal types for `pipStatus`, `sizeTier`, `media[].type`; `mapApiItem` and response interfaces typed; `matchItemToSource` casts to `ArticleEvent | NoteEvent` instead of `any`. (4) Relay URL validation — `relayUrls` entries require `.url()` format + `ws://`/`wss://` protocol. (5) Tag name DB constraint — migration 089 adds `CHECK (tag_name IS NULL OR char_length(tag_name) BETWEEN 1 AND 64)` on `feed_sources`. (6) Avatar URL validation — `avatarUrl` requires `.url()` format + `http://`/`https://` protocol. (7) Feed sources query index — migration 089 adds partial index `feed_sources_feed_active_idx ON feed_sources (feed_id, sampling_mode) WHERE muted_at IS NULL` for the `sourceFilteredItems` feed_mode CTE. (8–9) Feed items score index already existed (migration 087); float cursor precision non-issue. `tsc --noEmit` clean for `gateway` and `web`.
Previously: 2026-05-14. Completed: workspace slices 27–32 (default volume, minimize/hide, forall menu, card expand, feed merge, source move). Slice 27 — default source volume 100%: migration 082 changes `feed_sources.weight` column default from 1.0 to 4.0 so new sources default to step 5; existing weights unaffected. Slice 28 — minimize/hide vessels: `VesselLayout` extends with `minimized?` and `hidden?` fields; VesselBar gains minimize (`▁`/`□`) and hide (`×`) buttons; content area wraps in `{!minimized && (...)}`, resize handle suppressed when minimized, drag still works; WorkspaceView filters hidden vessels from render. Slice 29 — forall menu updates: `ForallAction` narrowed to `'new-feed' | 'new-note' | 'new-article'`; fork and reset removed; hidden feeds appear below an `<hr>` divider with 24px indent and muted colour, click restores. Slice 30 — card inline expand: click expands cards in place (full content + "Read full article →" / "Open original →" links) instead of navigating away; `expandedCards: Set<string>` in WorkspaceView; independent from thread expansion; compact density skips. Slice 31 — feed merge via drag: `POST /workspace/feeds/:id/merge` moves non-duplicate sources (NOT EXISTS subquery for partial unique indexes), copies saves via ON CONFLICT DO NOTHING, deletes source feed; frontend detects center-in-bounds on drag end, opens MergeFeedConfirm dialog. Slice 32 — move source via card drag: `POST /workspace/feeds/:id/sources/:sourceId/move` with 409 on duplicate; items API adds `authorId`/`externalSourceId` for source matching; sources API adds `accountId`/`externalSourceId` FK refs; VesselCard is HTML5-draggable with `application/x-vessel-card` MIME type; Vessel chassis registers drop zone with outline highlight; WorkspaceView caches sources per vessel, matches items to sources, calls move API on drop. `tsc --noEmit` clean for `web` and `gateway`; not yet exercised in a browser.
Last worked: 2026-05-16. Completed: FIX-SLICES Slice 2 — payment settlement three-phase refactor. `payment-service/src/services/settlement.ts` rewritten from single-transaction (Stripe-call-inside-txn with random UUID idempotency key — D76 + D77) into three-phase reserve→Stripe→complete mirroring payout.ts. Phase 1 (`reserveSettlement`): txn inserts `tab_settlements` as `'pending'`, checks for existing pending (D79), validates `>= 30p` Stripe minimum (D78), commits. Phase 2+3 (`completeSettlement`): Stripe `paymentIntents.create` with stable `settlement-${settlementId}` idempotency key outside any txn, then single UPDATE flips to `'completed'`. `resumePendingSettlements()` runs at startup. `accrual.ts::recordGatePass` moves `publishReceiptAsync` to after `withTransaction` returns (D82). `index.ts` adds `requireEnv('STRIPE_WEBHOOK_SECRET')`, `requireEnv('INTERNAL_SERVICE_TOKEN')`, `requireEnv('PLATFORM_SERVICE_PRIVKEY')` (D80). Migration 085 adds `status` column + partial index. 41 tests green.
Previously: 2026-05-12. Completed: frame-wide drag surface + workspace bounds clamping. Two changes to `Vessel.tsx`. (1) Drag surface widened from name-label-only to the entire frame: `onPointerDown={startDrag}` moved to the outer `motion.div`; `startDrag` checks `target.closest('button, a, input, textarea, select, [role="button"]')` and bails on interactive elements; the content area (cards) calls `e.stopPropagation()` on `onPointerDown` so card interactions don't trigger drag. Draggable surfaces: name label, chassis walls (8px borders), VesselBar background (gaps between buttons/input). Non-draggable: buttons, links, inputs, card content. `cursor: grab` on the outer div, `cursor: default` on the content area. (2) Workspace bounds enforcement for both drag and resize. New `clampPos(x, y)` helper floor-snaps the maximum position to the grid (`Math.floor((floorDim - vesselDim) / GRID) * GRID`) so the vessel's full rectangle stays within the workspace — not just the top-left corner. Uses a `vesselRef` on the `motion.div` for dimension measurement. For resize, `handleResizePointerDown` computes max width/height from the vessel's position relative to the floor (accounting for overhead above the chassis) and stores them in `resizeStateRef`; `handleResizePointerMove` clamps to these maxes before snapping. Drag also snaps per-frame (from the previous snap-to-grid commit) and now clamps via `clampPos` on every frame, so the vessel jumps in discrete 20px steps and can never exit the workspace during the gesture. ADR slice 5a/5b descriptions, "Drag vs click" paragraph, and snap-to-grid section updated. `tsc --noEmit` clean for `web`; not yet exercised in a browser.
Previously: 2026-05-11. Completed: workspace snap-to-grid. All vessel coordinates (position and size) are now quantized to a 20px grid. New `web/src/lib/workspace/grid.ts` exports a single `GRID = 20` constant and `snap()` function. Four insertion points: (1) collision output (`collision.ts`) snaps pushed positions so cascades produce grid-aligned results; (2) drag commit (`Vessel.tsx` `onDragEnd`) snaps the release position — the existing spring animation carries the vessel the last few pixels to its grid point; (3) resize (`Vessel.tsx` `handleResizePointerMove`) snaps during the gesture so width/height step in 20px increments with immediate visual feedback; (4) store safety net (`workspace.ts` `setVesselPosition` / `setVesselSize` / `batchUpdatePositions`) snaps all writes so no unsnapped value enters localStorage. Default grid padding adjusted 32→40px (2 grid cells). Grid unit chosen because it divides evenly into default vessel width (300 = 15 cells), min width (220 = 11 cells), gutter (40 = 2 cells), row height (600 = 30 cells). "Snap-to-grid" struck from the collision slice's "Skipped intentionally" list in the ADR. `tsc --noEmit` clean for `web`; not yet exercised in a browser.
Previously: 2026-05-02. Completed: workspace-experiment slices 21 + 22 + 23 per `docs/adr/WORKSPACE-EXPERIMENT-ADR.md`. Slice 21 — notifications anchor. The recipient-side gap on the chrome-less workspace floor closes: a 40px white disc with line-icon bell glyph and crimson unread badge sits at `right: 96, bottom: 32` immediately to the left of ∀ (cluster grammar = ∀ + bell, both workspace-scope controls), opening a 380×min(560, 100vh-120) popover that reuses the existing `notifications` + `unread-counts` + `markRead` + `readAll` routes — no schema or backend work. `NotificationsAnchor.tsx` polls `/unread-counts` every 30s regardless of panel state (one cheap COUNT scan; ADR-level user-memory record: real-time push is mobile-app territory, web stays on polling); panel-open additionally fetches `/notifications`. Rows: 6px crimson unread dot · actor name (Jost 13px, semibold when unread) + label string from `TYPE_LABELS` · for `new_reply` only an italic Literata excerpt (line-clamped 2) · time-ago mono-caps 10px; unread rows tint `#FAFAF7`. Click commits optimistic mark-read + navigates via re-used `getDestUrl` (articles → `/article/<dTag>`, profile-shaped → `/<username>`, dashboard-shaped → `/dashboard*`); MARK ALL READ commits across all visible rows + zeros badge with refetch-on-failure. New-message notifications excluded server-side so the bell doesn't double up with the legacy DM badge. Architectural call: vessel-shaped option ruled out (notifications aren't `feed_items` and would force decisions about persistence + re-render cadence); ∀-menu adjunct miscategorises notifications as object creation. Skipped intentionally: notification grouping, inline reply from popover, filter chips by type, focus trap, arrow-key nav (Tab works), real-time push, unread-by-type breakdown, snooze/mute by author, pip-coloured rows, animated open/close, per-row delete, badge-increment animation, badge for non-authenticated users. Slice 22 — search anchor. Discovery gap closes alongside notifications. New `SearchAnchor.tsx` mounts a 40px white disc with magnifier glyph at `right: 152, bottom: 32` (= bell `right: 96` + 40 width + 16 gap), completing the bottom-right cluster: ∀ · bell · search left-to-right. Click opens a 380×min(480, 100vh-120) popover with an autofocused Jost 14px text input on `#FAFAF7` and three result sections (Writers / Articles / Publications) under 11px mono-caps section headers. New `web/src/lib/api/search.ts` typed client wraps `/search?type=articles|writers|publications`, accepting `(q, limit, signal)` so an AbortController can cancel in-flight requests on every keystroke (slow trigram round-trip on `q="ali"` can't beat a fast one on `q="alic"` and clobber it). 200ms debounce; min 2 chars matches the gateway's `query.length < 2` 400-error guard. Per-section limits 5/8/5 (articles weighted as the dominant corpus). Click navigation: writer → `/${username}`, article → `/article/${dTag}`, publication → `/pub/${slug}` — all existing routes, no new pages. No full-search footer link to the carry-over `/search` page (the workspace experiment is about retiring carry-over surfaces; entrenching `/search` as a "see more" destination pushes against §1 of the ADR). Architectural call: search-as-vessel ruled out (results aren't `feed_items` in the ranking sense; would replicate the chassis-abstraction strain that killed the notifications-vessel option); ∀-menu adjunct miscategorises search as object creation. Cluster width consumes ~232px of the bottom-right edge — at a 1024px viewport this is well under a quarter of the floor and has roughly one more affordance slot before the cluster needs different geometry. Backend untouched (`gateway/src/routes/search.ts` already does pg*trgm trigram on titles + ILIKE on usernames/display-names + trigram on publication names + ILIKE on taglines). Skipped intentionally: arrow-key result navigation, recent-searches history, scoped search (within a vessel), filter chips by type, search-as-vessel, inline pagination, full-search footer, publication tagline / writer bio in result rows, match-substring highlighting, search-by-tag entry, per-query telemetry, prefetch-on-hover. Slice 23 — cards with media (note + external). Visual gap closes for two of three card types; articles defer until cover-image editor path exists. New `MediaBlock` component in `VesselCard.tsx` renders the first image (preferred) or first video item from a media array in a fixed 16:9 cover container with `objectFit: 'cover'`, lazy-loaded `<img>` with `referrerPolicy="no-referrer"` (matches the deprecated card's privacy posture — some external image hosts log Referer; the workspace shouldn't leak which native vessel is rendering content). Background colour pulls from `ctx.palette.interior` so the placeholder during load matches brightness state; per the user's call this turn, brightness states render media at full fidelity (dimming a JPEG either needs a CSS filter that breaks photo intent or a server variant that doesn't exist). Video items render the same image surface (using `thumbnail` if the source provided one, otherwise an empty palette-coloured surface) overlaid with a 44px white play-glyph disc; click stops propagation and opens source URL in a new tab. `+N` corner pill (mono-caps, white-on-90%-black) when `media.length > 1`; non-interactive in v1. Compact density early-returns `null` (matches the action-strip suppression rule; a hero image would defeat the row's airless semantic); standard + full both render. External cards consume `external.media[]` already plumbed through `external_items.media JSONB` → `FEED_SELECT.fi.media` → `rowToItem` external branch. Notes extract image URLs client-side: new `extractNoteMedia` in `web/src/lib/media.ts` reuses the existing `extractUrls` + `isImageUrl` (matches `.jpg|.jpeg|.png|.gif|.webp` + Blossom `/<sha256>` shape) returning `Array<{type:'image', url}>`; the displayed text is `stripMediaUrls(content).displayText` (strips matched URLs + nostr event references) when extraction returns anything, original content unchanged when not. Audio + link items not extracted; embeddable-URL providers (YouTube/Vimeo/Spotify/Twitter) recognised by `isEmbeddableUrl` but rendering needs oEmbed plumbing the slice doesn't ship; bare video file URLs in notes are rare in practice so v1 is images-only on the note path. Saved view inherits automatically (slice 20 reused `VesselCard`). Architectural call: article cover images deferred — `web/src/lib/publish.ts::buildNip23Event` doesn't produce an `image` tag, the editor (`/write` + Composer article mode) has no cover-image picker, `gateway/src/routes/articles/publish.ts` has no `image` field in its request schema; that's a multi-surface change tracked as slice 23b. Skipped intentionally: article covers (deferred), oEmbed link cards, inline `<video>` players, GIF autoplay control, image lightbox, alt-text-on-hover, blurhash placeholders, multi-image carousel, per-vessel "no images" toggle, image upload from Composer, markdown-style `![alt](url)` inline images (note compose is plaintext), per-density hero size variation. Migration map (`/workspace` row) rolled forward — `NotificationsAnchor` + `SearchAnchor` added to the component list, slices 13–23 appended to the slice-list footer, slice 21–23 narrative paragraph appended to the row body. ADR build log + status header rolled forward (slices 21, 22, 23; "Search entry point" + "Cards with media (lead images, video embeds)" struck from Deferred; "Article cover images" added to Deferred). `tsc --noEmit` clean for `web`; backend unchanged in all three slices; not yet exercised in a browser.
Previously: 2026-05-02 earlier. Completed: workspace-experiment slice 20 — per-feed save persistence + Save action on the card strip + saved view — per `docs/adr/WORKSPACE-EXPERIMENT-ADR.md`. The Deferred list's leading item retires; the slice-11 reserved Save slot on the card action strip is now real. Architectural call is per-feed: saves belong to the vessel that minted them, not a global cross-feed list — the vessel is the per-feed attentional surface and a "save to which feed?" picker would defeat one-tap commit. Migration 080 lands `feed_saves(feed_id, feed_item_id, created_at)` with cascade on both FKs (deleted items get filtered server-side, deleted feeds wipe their saved set), `UNIQUE(feed_id, feed_item_id)` for idempotent re-save, compound `(feed_id, created_at DESC, id DESC)` index for the listing cursor + `ids` lookup. No `saved_by` denorm — feeds are owner-private and the route enforces it. Four new endpoints in `gateway/src/routes/feeds.ts`: `GET /workspace/feeds/:id/saves` (cursor `${epoch_ms}:${feed_save_id}`, `FEED_SELECT`/`FEED_JOINS` reused so item shape is byte-identical to `/items` plus a `savedAt` epoch, soft-deleted feed_items filtered); `GET /workspace/feeds/:id/saves/ids` (light Set for first-paint label correctness); `POST /workspace/feeds/:id/saves { feedItemId }` (idempotent, pre-checks `feed_items.id IS NOT NULL AND deleted_at IS NULL` so a deleted target returns 404 rather than ghost-ing via ON CONFLICT); `DELETE /workspace/feeds/:id/saves/:feedItemId` (204, no-op on missing). `rowToItem` now exposes `feedItemId: row.fi_id` on all three card types so the unified id flows to the client; `web/src/lib/ndk.ts` extends `ArticleEvent`/`NoteEvent`/`ExternalFeedItem` with optional `feedItemId?` + `savedAt?`. Web client gains `workspaceFeedsApi.listSaves / listSavedIds / saveItem / unsaveItem`. `VesselCard.CardActions` adds a `Save` / `Saved` button (crimson when committed) suppressed in compact density and on items lacking `feedItemId`; external cards include it (saves key on `feed_items.id`, not Nostr event id, so externals are first-class save targets unlike vote/reply). `Vessel.tsx` accepts `savedView` + `onToggleSavedView` and renders a fourth chassis control (`★`/`☆`) alongside brightness · density · orientation; the name label appends ` · SAVED` when the toggle is active. `WorkspaceView` extends `VesselState` with `view: 'live' | 'saved'` + `savedIds: Set<string>`; bootstrap fires `listSavedIds` per feed in the background to populate the Set so labels render correctly from first paint; `loadVesselItems(feed, view?)` branches on view via a `vesselViewRef` Map (avoiding stale closure on the `useCallback`); `handleToggleSave` mutates the Set optimistically + drops the item from the saved view on unsave + reverts on failure; saved-view empty state reads *NO SAVED ITEMS YET — TAP SAVE ON A CARD TO KEEP IT HERE*. View-mode is intentionally session-ephemeral (no localStorage) — saving is a sticky channel, *being in saved view* is a brief detour. The legacy global `bookmarks` table is untouched and retires with the deprecated reading-mode chassis on merge to master. Skipped intentionally: cross-feed save view, save-to-different-feed picker, pagination beyond first 50, save-count badge on chassis, share-toast on commit, keyboard shortcut for SAVED toggle, drag-to-reorder saves, compact-density saves (compact suppresses the strip entirely), legacy-table cross-pollination, volume×save interplay (saved is intentional retention; mute is "less in my live feed" — they don't override), per-save annotation. ADR build log + status header rolled forward (slice 20 added; "Save persistence" struck from Deferred). `tsc --noEmit` clean for `gateway` and `web`; migration 080 not yet applied (postgres down on this laptop); not yet exercised in a browser.
Previously: 2026-05-01. Completed: workspace-experiment slices 6 + 7 + 8 per `docs/adr/WORKSPACE-EXPERIMENT-ADR.md`. Slice 6 — ∀ → *Reset workspace layout* wired. New `web/src/components/workspace/ResetLayoutConfirm.tsx` matches `NewFeedPrompt`'s scrim/panel grammar (40% scrim, 420px panel, 144px top inset) with a crimson `Reset layout` confirm button; body copy adapts to vessel count. `WorkspaceView` opens it on `ForallAction === 'reset'` and `handleResetLayout` calls `useWorkspace.reset()` *and immediately re-seeds default grid slots for current vessels in their existing order via `defaultGridSlot(i, viewportWidth)`* so the floor doesn't visibly collapse to `(0, 0)` for one paint while the bootstrap effect (keyed on `user`, not `positions`) fails to re-run. Slice 7 — vessel rename + delete UI inside `FeedComposer`. Header gains an inline `Rename` flow: click → input pre-filled and auto-selected for fast retype; Enter saves, Esc cancels; 1–80 char trim guard with `trim-equal-to-current = no-op close`. Footer gains a `Delete feed` mono-caps button (grey → crimson on hover) that swaps in-place to a two-step confirm row (`Delete this feed? Sources are removed; subscriptions are kept.` + Cancel + crimson Delete) — lighter than `ResetLayoutConfirm`'s modal because the action is feed-scoped. Last-feed guard via `deleteBlocked={vessels.length <= 1}`; in that case the footer renders `Can't delete your only feed — create another first.` rather than the delete button. `onRenamed(feed)` patches `vessels[].feed` so the vessel name label updates without a refetch; `onDeleted(feedId)` drops the vessel *and* calls `useWorkspace.removeVessel(feedId)` (its first wired caller — the store method existed since slice 5a). The composer's hint copy names the deliberate behaviour from slice 4: deleting a feed cascades to `feed_sources` rows but leaves `external_subscriptions` rows intact. Slice 8 — ∀ → *Fork feed by URL* wired. New `web/src/components/workspace/ForkFeedPrompt.tsx`. Single resolver-debounced input (300ms + Phase B polling, context `subscribe`) plus a `#tag` literal fallback; match candidates render as a button list. `handleFork(opt)` runs `workspaceFeedsApi.create(derivedName)` then `workspaceFeedsApi.addSource(feedId, opt.add)` in sequence; derived name = display name → @username → URI → feed title, clamped to 80 chars. Roll-forward on partial failure: if `create` succeeds but `addSource` fails, the partial feed is *kept* and handed back via `onForked(feed)`; the modal surfaces a `Feed created but source add failed: …` hint so the user can finish wiring it via the feed composer. `handleForked(feed)` mirrors `handleCreateFeed` — appends vessel, writes default-grid slot, fires `loadVesselItems`. All four ∀ menu items (new-note / new-feed / fork / reset) now live. ADR build log + status header rolled forward (slices 6, 7, 8). `tsc --noEmit` clean for `web`; not yet exercised in a browser.
Previously: 2026-05-01 earlier. Completed: workspace-experiment slices 5b + 5c per `docs/adr/WORKSPACE-EXPERIMENT-ADR.md`. Slice 5b — vessel resize via bottom-right corner handle. `useWorkspace.VesselLayout` extends from `{x,y}` to `{x,y,w?,h?}`; new `setVesselSize` merges into the existing per-feed record under the same `workspace:layout:<userId>` key (slice-5a values forward-compatible since w/h are optional). `Vessel.tsx` adds a 16×16 hit area at `right:-8, bottom:-8` (offsetting the 8px wall) carrying a small ◢ glyph at low opacity; resize is plain `onPointerDown` + `setPointerCapture` + `onPointerMove` (Framer Motion's `drag` API is for translation, not bounded resize) with `liveSize` mirroring the in-flight value and committing on pointerup. Min 220×200 per spec ("below which content becomes illegible"); max 2000×2000 defensively (the floor's `overflow:hidden` clips visually so spec's "no maximum" is honoured by the floor). When `size.h` is set the chassis takes a fixed height and the body becomes `overflow-y:auto` so cards scroll inside. Resize handle stops propagation so it doesn't conflict with the name-label translation drag. Slice 5c — vessel brightness + density + orientation. New `web/src/components/workspace/tokens.ts` consolidates the wireframe's three colour palettes (primary / medium / dim) into a `PALETTES: Record<Brightness, VesselPalette>` lookup including desaturated crimson `#C4545A` and `pipOpacity:0.7` at dim, plus three small `next*`cycle helpers and per-axis defaults (medium / standard / vertical).`VesselLayout`extends with optional`brightness | density | orientation`; three new setters (`setVesselBrightness`/`setVesselDensity`/`setVesselOrientation`) merge into the same record; slice-5a/5b values still forward-compatible. `Vessel.tsx`resolves a single`palette` from brightness; wall arrangement branches on orientation (`vertical`→ ⊔ left+right+bottom;`horizontal`→ ⊏ top+left+bottom, opening on right); inner flex direction switches`column ↔ row`; height-set vessels scroll on the active axis (`overflow-y`vs`overflow-x`). Three small mono-glyph cycle controls (`○|◐|●`brightness,`c|s|f`density,`||─`orientation) sit pinned to the chassis bottom-right edge just left of the resize handle; each cycles forward on click with`title=`carrying the full label. Per ADR §5 these are the desktop alternatives to the touch gestures (two-finger vertical drag for brightness, two-finger rotation, gestural density toggle); cycle buttons are honest about discreteness — when continuous brightness lands the storage shape evolves at that point.`VesselCard.tsx`accepts`density`+`brightness`props, resolves a`CardContext`, replaces hardcoded medium-bright tokens with palette lookups (so a dim vessel recolours its cards including pip opacity 0.7). Compact density = inline 9px pip + single-line title (with crimson `£` glyph for paywalls, no full price); standard = slice-1 grammar; full = adds source-attribution row (`VIA <PROTOCOL> · <IDENTIFIER>`mono caps 10px).`WorkspaceView`plumbs the new fields and propagates density + brightness to each card. Skipped: continuous brightness (touch deferred per ADR §5; storage stays discrete until then), real touch gestures, brightness-as-focus coupling (own design pass per WORKSPACE-DESIGN-SPEC.md), name-label repositioning to the opening side in horizontal mode (label still above the vessel root; spec calls for it to follow the opening), per-density default sizes, keyboard equivalents for the three controls (deferred per ADR §6 a11y floor), no-overlap collision (still later), thumbnails / lead images at full density (the spec calls for them;`feed_items`doesn't carry them in a way the slice can render — TODO). ADR build log + status header rolled forward (slices 5b + 5c).`tsc --noEmit`clean for`web`; not yet exercised in a browser.
Previously: 2026-05-01 earlier. Completed: workspace-experiment slice 5a — vessel drag-to-position + localStorage layout. Framer Motion (^11) enters the codebase. New `web/src/stores/workspace.ts` (`useWorkspace`Zustand store) holds`positions: Record<feedId, {x,y}>`, `hydrate(userId)`, `setVesselPosition`, `removeVessel`, `reset`— backed by localStorage`workspace:layout:<userId>`with a 200ms-debounced write; quota-exceeded / private-browsing failures swallowed silently (in-memory layout is authoritative for the session). New`web/src/lib/workspace/motion.ts`for shared Framer Motion config (drag spring + reduced-motion variant +`prefersReducedMotion()`helper) — primarily for the resize / rotate / ∀→H→⊔ slices that follow; 5a's drag uses`dragMomentum=false`so no spring runs.`Vessel.tsx`is now a`motion.div` (`position: absolute`, `x`/`y`motion values mirrored from props via`useEffect`); drag is gated through `dragControls`+`dragListener=false`so only the name-label`onPointerDown`initiates a drag (cards inside stay fully clickable);`dragMomentum=false`, `dragElastic=0`, `dragConstraints={floorRef}`so vessels can't be dragged off the floor;`dragMovedRef`flag suppresses the post-drag click on the name label so dragging the label doesn't accidentally open`FeedComposer`. `WorkspaceView`drops`flex flex-wrap`; floor is `position: relative; height: 100vh; overflow: hidden`; bootstrap blocks on `useWorkspace.hydrated`so default-slot writes never overwrite a stored layout; for any feed without a stored position a default grid slot is computed (340px col = 300 vessel + 40 gutter, 32px outer padding, wraps at viewport width) and written back. New-feed creation assigns the next slot before the vessel mounts. Loading / error hints centre on the floor via absolute positioning. Skipped: vessel resize / brightness / density / rotation, no-overlap collision, scrollable canvas beyond viewport, server-side persistence, keyboard-driven drag (deferred per ADR §6 a11y floor), mobile touch geometry (still desktop-only per ADR §5), default-grid recompute on viewport resize, garbage-collect orphaned`positions` on remote feed deletion (`removeVessel`exists but isn't wired since vessel deletion UI doesn't exist). The *Reset workspace layout* ∀ item still stubbed but`useWorkspace.reset()`exists ready to wire. ADR build log + migration map (workspace`/workspace`row + new`stores/workspace.ts`row) rolled forward.`tsc --noEmit`clean for`web`, `npm test`75/75 green,`next build`succeeds (workspace bundle 51.8 kB → 142 kB first load); not yet exercised in a browser.
Previously: 2026-05-01 earlier. Completed: workspace-experiment slice 4 — feed composer + items query honours sources. New routes`GET/POST /api/v1/feeds/:id/sources`and`DELETE /api/v1/feeds/:id/sources/:sourceId`author rows in`feed_sources`; the POST body is a discriminated union accepting native UUIDs (account / publication), a tag name (auto-inserted into `tags`), or external — either an existing `externalSourceId`or a`(protocol, sourceUri[, displayName, …])`pair that upserts`external_sources`and ensures an`external_subscriptions`row in one txn (so feed-ingest workers pick up the source) plus enqueues an immediate fetch for`rss`/`nostr_external`/`activitypub`. The slice 3 placeholder branch now coexists with a real `sourceFilteredItems`query — non-empty source sets fan out across four OR-ed`EXISTS`clauses (account →`fi.author_id`, publication → `a.publication_id`, external → `fi.source_id`, tag → `EXISTS`join through`article_tags + tags`); cursor narrows from `(score, published_at, id)`to`(published_at, id)`until weight + sampling_mode arrive. Web client extended with`workspaceFeeds.listSources / addSource / removeSource`and an`AddWorkspaceFeedSourceInput`discriminated union mirroring the route shape. New`web/src/components/workspace/FeedComposer.tsx`opens from a click on the vessel name label (Vessel gained an`onNameClick`prop) — scrim/panel grammar matching`NewFeedPrompt`/`Composer`, lists current sources with × remove, offers an "add a source" input that resolver-debounces 300ms (context `subscribe`, Phase B polling) and renders match candidates plus a `#tag`literal fallback.`WorkspaceView`re-fetches the affected vessel after a source change. Skipped: per-source weights / mute toggle (columns reserved, no UI), feed rename / delete UI (routes exist), drag-to-reorder, *Fork feed by URL* and *Reset workspace layout* ∀ stubs, bulk-import from existing follows, per-vessel cap on`feed_sources`adds. ADR build log + migration map rolled forward.`tsc --noEmit`clean for`gateway`and`web`; migration 077 still not applied (postgres down on this laptop); not yet exercised in a browser.
Previously: 2026-05-01 earlier. Completed: workspace-experiment slices 2 → 3 per `docs/adr/WORKSPACE-EXPERIMENT-ADR.md`. Slice 2 — persistent bottom-right ∀ menu (`web/src/components/workspace/ForallMenu.tsx`) with the four-item set per `WORKSPACE-DESIGN-SPEC.md`§"Workspace scope" (*New feed*, *New note*, *Fork feed by URL*, *Reset workspace layout*), arrow/Home/End keyboard, Escape/outside-click to dismiss, focus return. Slice 2.5 — fresh`Composer` (`web/src/components/workspace/Composer.tsx`) wired to *New note*; centred panel over a 40% scrim, `Publishing publicly`banner, body-only public publish via the existing`publishNote(content, user.pubkey)` rail. Slice 2.6 — To-field built as a chip row backed by the universal resolver (`POST /api/v1/resolve`, context `dm`, 300ms debounce + Phase B polling) plus four-protocol toggle pills (`ALL.HAUS · NOSTR · BLUESKY · ACTIVITYPUB`); broadcast-token autocomplete (*Everyone on Nostr / Bluesky / fediverse / all.haus*) lives in the same dropdown. Slice 2.7 — person-chip publish branches into the existing DM pipeline (`messages.createConversation`then`messages.send`), end-to-end NIP-44 via key-custody unchanged; banner suppressed, button label flips `Publish`→`Send`, `skippedRecipientIds`(DM pricing) surfaced as partial-success. Slice 2.8 — top-level cross-protocol broadcast wired through the existing Phase 5 outbound pipeline.`outbound_posts.action_type`already had`'original'`reserved (CHECK constraint in migration 057,`linked_account_id`and`source_item_id`already nullable post-058) so no migration;`POST /notes`swaps`crossPost`(singular) for`crossPosts: array`with a Zod refinement enforcing`actionType === 'original' ⇔ sourceItemId omitted`; `enqueueCrossPost`widens to optional`sourceItemId`+`'original'`; `feed-ingest/src/tasks/outbound-cross-post.ts`accepts`'original'`for both atproto + activitypub branches (existing adapters already produced top-level posts cleanly when no reply/quote refs were set). On the frontend`Composer`now calls`linkedAccounts.list()`on open, gates the atproto/activitypub toggles by connection state (disabled-grey + *Connect ⟨X⟩ in Settings → Linked accounts to broadcast there* on disconnected), and on broadcast publish anchors the Nostr publish *and* fans out one`crossPosts`entry per connected non-Nostr protocol the user has toggled on. Hint copy switches to`Publishing to Nostr · BLUESKY · ACTIVITYPUB — N/1000`whenever any cross-post target is queued. Cardinality-determines-publication is now real end-to-end for native + linked targets. Slice 3 — feeds object goes server-side. Migration 077 lands`feeds(id, owner_id, name, …)`(1–80 char name guard +`updated_at`trigger) and`feed_sources(feed_id, source_type, account_id|publication_id|external_source_id|tag_name, weight, sampling_mode, muted_at)`(target-matches-type CHECK + per-type partial unique indexes;`weight`/`sampling_mode`reserved for the ranking story). New`gateway/src/routes/feeds.ts`exposes owner-private`GET/POST/PATCH/DELETE /api/v1/feeds`(Zod-validated; UUID guard) and`GET /api/v1/feeds/:id/items`returning`{feed, items, nextCursor, placeholder}`— empty source set falls back to the caller's explore branch inline (small duplication of`timeline.ts`'s explore query, retires when source-set semantics arrive); non-empty falls through to `[]`with a TODO. New`web/src/lib/api/feeds.ts`exports`workspaceFeeds`(renamed off`feeds`to avoid collision with the existing external-feeds export consumed by`/subscriptions`+`SubscribeInput`). `WorkspaceView.tsx`now bootstraps the feed list, seeds "Founder's feed" on first authenticated load if none exist, renders one`Vessel`per feed in`flex-wrap`with parallel`items()`fetches, and refreshes every vessel after a publish. New`NewFeedPrompt`modal hooks ∀ → *New feed* — Enter submits, Esc / scrim cancels, body copy honest about the placeholder ("sources arrive in a later slice"). Source-set authoring (the actual point of having a feed object), rename/delete UI on vessels, and per-vessel pagination beyond the first 20 items remain deferred; *Fork feed by URL* and *Reset workspace layout* ∀ items remain`console.log`stubs. ADR build log + migration map both rolled forward.`tsc --noEmit`clean for`gateway`and`web`; migration not yet applied (postgres is down on this laptop); not yet exercised in a browser.
Previously: 2026-04-23. Completed: relay outbox §60 Phase 4 per `docs/adr/RELAY-OUTBOX-PHASE-4-ADR.md`. Publication publish path now enqueues in-txn — `publishToPublication`and`approveAndPublishArticle`in`gateway/src/services/publication-publisher.ts`sign outside the txn (IO to key-custody) then fold the INSERT/UPDATE articles + upsert feed_items +`enqueueRelayPublish(entity_type='article', entity_id=articleId)`into a single`withTransaction`; relay blips become invisible worker retries instead of 5xx. Eager-commit (Shape A) per the ADR — `POST /publications/:id/articles`and`POST /publications/:id/articles/:articleId/publish`now mean "signed and durably queued", parallel to the`POST /sign-and-publish`and scheduler contracts from earlier phases.`publishToRelay`and`publishToRelayUrl`deleted from`gateway/src/lib/nostr-publisher.ts`(the`ws`import too — only`finalizeEvent`/`getPublicKey`remain, used by`signSubscriptionEvent`). Grep across `gateway/src`, `payment-service/src`, `feed-ingest/src`, `shared/src`returns zero matches outside a descriptive comment in`shared/src/lib/relay-outbox.ts`. Gateway build clean, 24 gateway tests + 28 shared tests green; feed-ingest + payment-service also build clean. §60 programme complete end-to-end.
Previously: 2026-04-23 earlier. Completed: Relay outbox (§60) Phase 6 — observability + relay-failure integration tests per `docs/adr/RELAY-OUTBOX-ADR.md`. `feed-ingest/src/tasks/relay-publish.test.ts`(10 cases) covers the worker's full state machine against a scripted pg client + mocked`publishNostrToRelays`: `computeBackoff`(exported) holds the`2^attempts min, capped 1h, ±10% jitter`contract; happy-path writes`status='sent'`; relay rejection writes `status='failed'`, increments `attempts`, persists `last_error`, and schedules a versioned `relay_publish*<id>\_r<n>`retry via`helpers.addJob`; `attempts = max_attempts - 1`on failure flips to`status='abandoned'`with no retry; already-sent rows, SELECT-missed rows, and advisory-lock contention are no-ops that defer to the minute-cadence redrive; missing-relay-URL path fails cleanly. The final case is the acceptance-criterion deletion-path retry in miniature: two consecutive invocations on the same outbox id, relay throws once then resolves, row walks`pending → failed → sent`— the exact shape a real relay blip produces for the kind-5 tombstones enqueued from`articles/manage.ts`, `notes.ts`, `publications/cms.ts`, and `auth.ts`account-deletion. "Backfill" turned out to be a null task: migration 076 shipped atomically with Phases 2+3 so there is no pre-outbox drift to sweep; ongoing observability is the daily`relay_outbox_reconcile`metrics (abandoned / high-retry / sent-24h). Feed-ingest build clean, 52 tests green (11 + 31 + 10 new). Programme one complete; Phase 4 publish-path rewrite is the only remaining piece and is deferred to its own ADR.
Previously: 2026-04-23 earlier. Completed: Relay outbox (§60) Phase 5 — scheduler §1 contortion retired per`docs/adr/RELAY-OUTBOX-ADR.md`. `gateway/src/workers/scheduler.ts::publishPersonalDraft`no longer calls`publishToRelay`. Free drafts: INSERT articles + INSERT feed_items + `enqueueRelayPublish(v1)`commit atomically in one txn (the worker owns publish). Paywalled drafts: txn 1 establishes the vault-ownership anchor with v1.id (unchanged); the post-commit`await publishToRelay(v2)`that was the original §1 hazard source is gone — txn 2 now UPDATEs articles + feed_items to v2.id *and\* enqueues v2 together, so on crash between commit and worker pickup the outbox redrive catches the row and the DB can never say "published" while the relay hasn't seen the event. Retry semantics: outer catch retains the draft; re-sign v1'+v2' re-enters the articles ON CONFLICT upsert,`vaultService.publishArticle`reuses the existing content key by`article_id`, and the fresh v2' enqueues to a new outbox row (replaceable-event 30023 collapse handles the duplicate on the relay). `publishToRelay`import dropped from the scheduler; it remains live in`services/publication-publisher.ts`which is Phase 4 scope. Gateway build clean, 24 gateway tests + 28 shared tests pass.
Previously: 2026-04-22. Completed: Relay outbox (§60) Phases 2 + 3 call-site migration per`docs/adr/RELAY-OUTBOX-ADR.md`. All 13 non-publish-path sites now sign locally and hand the signed event to `enqueueRelayPublish`inside the caller's transaction; the`relay_publish`worker owns retry. Phase 2 (5 fire-and-forget / swallowed-error sites):`publishSubscriptionEvent`split into sign-only`signSubscriptionEvent`; `subscriptions/writer.ts`create/reactivate/cancel sign + enqueue inside`withTransaction`(cancel picked up a txn it didn't previously have),`nostr_event_id`now written atomically with the signed event id;`workers/subscription-expiry.ts`renew folds sign + enqueue + period-roll into one txn;`services/messages.ts::publishConversationPulse`enqueues (entity_type`conversation_pulse`), outer caller `.catch`unchanged. Phase 3 (8 awaited sites):`payment-service/lib/nostr.ts::publishReceiptEvent`→ sign-only`signReceiptEvent`(the parallel ws publisher deleted — payment-service now shares the gateway's outbox infrastructure per risk #3);`accrual.ts::publishReceiptAsync`signs then`withTransaction(UPDATE read_events + enqueueRelayPublish)`; `auth.ts`account-deletion loop enqueues per-article kind-5 tombstones inside the existing delete txn;`articles/manage.ts`, `notes.ts`, `publications/cms.ts`deletion flows fold the kind-5 sign + enqueue into the DB delete txn so a crash can't leave the DB marked deleted while the relay still serves the event;`drives.ts` `publishDriveEvent`/`publishDriveDeletion`sign + enqueue in short txns;`signing.ts POST /sign-and-publish`enqueues — **API semantic change** documented (200 = "signed and durably queued" rather than "on relay"). Build clean across all 8 workspaces, 165 tests pass (28 shared + 24 gateway + 41 payment + 11 key-service + 19 key-custody + 42 feed-ingest), knip silent, lint 0 errors (warnings 265→257). Phase 5 (§1 scheduler ordering retirement) and Phase 6 (backfill + integration tests simulating relay failure) still outstanding; Phase 4 publish-path rewrite remains deferred to its own ADR.
Previously: 2026-04-22 earlier. Completed: Relay outbox (§60) Phase 1 infra per`docs/adr/RELAY-OUTBOX-ADR.md`. Migration 076 (`relay_outbox`table + hot-query partial index on`(pending, failed)`by`next_attempt_at`, unique index on `signed_event->>'id'`for dedup,`(entity_type, entity_id)`reconciliation index).`shared/src/lib/relay-outbox.ts::enqueueRelayPublish(client, input)`— inserts the row and schedules the graphile-worker job inside the caller's txn; returns`{id, existed}`. `feed-ingest/src/tasks/relay-publish.ts`worker claims rows via`FOR UPDATE SKIP LOCKED`, acquires a `pg_try_advisory_xact_lock`keyed on`(entity_type, entity_id)`so concurrent workers on the same subscription/article don't interleave, reuses existing`publishNostrToRelays`adapter (which already carries the §71 one-accepts rule + partial-success warn log), owns retry semantics with exponential backoff`min(2^attempts minutes, 1h)`+ ±10% jitter, versioned`jobKey`per attempt.`relay_outbox_redrive`(minute cron, batch 100, distinct`job_key`per tick) provides a heartbeat independent of the enqueue path for rows whose original`add_job`was lost.`relay_outbox_reconcile`(daily 04:30 UTC) emits abandoned / failed-high-retry / sent-last-24h counts.
Previously: 2026-04-20. Completed: FIX-PROGRAMME Day 3 P1 mechanical. Ten low-risk cleanups: §38+§39 canonical`slugify`/`generateDTag`extracted to`shared/src/lib/slug.ts`(gateway scheduler/publication-publisher/articles switched over; web keeps a mirror with drift test); §40`expireAndRenewSubscriptions`and`expireOverdueDrives`moved to`gateway/src/workers/` (`logSubscriptionCharge`stays in`routes/subscriptions.ts`for the in-process endpoints); §41 advisory-lock IDs centralised in`shared/src/lib/advisory-locks.ts`(JETSTREAM consolidated alongside SUBSCRIPTIONS/DRIVES/SCHEDULER, 100003 gap documented); §42`requireEnv`/`requireEnvMinLength`adopted by key-service, key-custody, payment-service entrypoints; §43 five`(req as any).session?.sub`→`req.session!.sub!`in`traffology.ts`; §44 `db/client.ts`re-export shims deleted from key-service, key-custody, payment-service — imports now go directly to the symlinked shared path; §56 docker-compose header refreshed to list all 13 actual services; §20 duplicate`rsa2`join dropped in`loadConversationMessages`(read`nostr_pubkey`from`rsa`); §6 `listInbox`mute+block hygiene — mute filter moved inside`array_agg FILTER (WHERE muter_id IS NULL)` so a single muted member no longer blanks the whole conversation, HAVING guards 1:1 DMs where the counterparty is muted, block-exists subquery mirrors the send path's "hide if any non-me member has blocked me" invariant. Gateway (52) + web (75) + shared (28) = 155 tests green.
Previously: 2026-04-19. Completed: FIX-PROGRAMME Day 2 P0 Stripe orphans. §3 writer payout and §4 publication payout both refactored from single-transaction (Stripe-call-inside-txn → any later throw rolls back while transfers stay live) into three-phase reserve→Stripe→complete with stable idempotency keys (`payout-${payoutId}` and `pub-split-${payoutId}-${accountId}`). New `resumePendingWriterPayouts`and`resumePendingPublicationPayouts`run at the top of each cycle so a mid-flight crash is retried idempotently on the next run. §4 subsumes §33 (dead "mark completed" block gone) and as a side-effect finally gives KYC-waiting splits a retry path — previously they sat pending forever. No migrations; schema already supported nullable`stripe_transfer_id`+`'pending'`status on both`writer_payouts`and`publication_payout_splits`.
Previously: 2026-04-19 earlier. Completed: FIX-PROGRAMME Day 1 P0 — §1 (scheduler vault ordering), §2 (Stripe webhook dedup via nullable `processed_at`, migration 071), §7 (`recordSubscriptionRead`wrapped in`withTransaction`), §8 (await the expiry-warning insert), §9 (dedicated `expiry_warning_sent`event_type, migration 072), §10 (subscription charge reads`platformFeeBps`from config, not hardcoded 0.08).
Previously: 2026-04-18. Completed: Redesign audit-and-fix pass against`docs/adr/ALLHAUS-REDESIGN-SPEC.md`. Seven undocumented deviations closed: `ExternalCard`title changed from italic Literata to roman, and content branched so RSS-like items (with title) render a Literata 14.5px grey-600 summary while Bluesky/Mastodon-like items (no title) render a Jost 15px black body matching`NoteCard`— preserves italic-Literata as the native-article signal per §4;`ArticleCard`standard-tier excerpt bumped from 15px → 15.5px to match spec §4a; six grey-100 zone-rule borders in`ComposeOverlay`and two in`ArticleComposePanel`raised to grey-200 per spec §3 ("divided into three zones by internal 4px grey-200 rules");`PlayscriptReply`action row switched from`opacity: 0 / pointer-events: none` to conditional mount (`showActions && ...`) so the entry's vertical rhythm collapses when actions are hidden — matches the spec's reveal semantics rather than reserving layout space; migration 070 harmonises `articles_derive_size_tier()`with the 068 backfill (NULL`word_count`→`standard`, not `brief`); spec §2 line 117 corrected from "crimson ∀ mark at 14px" to "white" to match §1 and the shipped `Nav.tsx`.
Previously: 2026-04-18. Completed: Redesign Step 6 (end-of-feed / zero / filtered-empty / error states) per `docs/adr/ALLHAUS-REDESIGN-SPEC.md`§2 + §6.`FeedView.tsx`gains four local state components:`EndOfFeed`(mono-caps`END OF FEED`, 48px crimson under-rule, `SUBSCRIBE TO MORE →`link — scrolls to top and focuses the subscribe input, which now carries`id="feed-subscribe-input"`); `ZeroState`(Literata italic 32px`"Nothing here yet — which is fine."`, Jost 15px grey-600 body with underlined `above`link, mono-caps`TRY: A BLUESKY HANDLE · AN RSS URL · AN NPUB · A PUBLICATION NAME`ribbon);`FilteredEmptyState`(mono-caps`NO ITEMS MATCH THIS FILTER`+`CLEAR FILTER`— fired when`reach === 'following'`and the feed returns zero items, reverts to`explore`); `FeedErrorState` (`"Couldn't load the feed."`+`RETRY`at 33vh; the secondary`"The gateway may be down. This isn't a sync issue on your end."`line appears after ≥3 failures in 60s, tracked via`failureTimestampsRef`). All copy matches the spec verbatim. No filter-bar dependency — the filtered state already functions against the latent `reach`state.
Previously: 2026-04-18. Completed: Redesign Step 5 (compose overlay article mode) per`docs/adr/ALLHAUS-REDESIGN-SPEC.md`§3 + §6. New`ArticleComposePanel.tsx`mounts inside`ComposeOverlay`when`mode === 'article'`; owns its own Tiptap instance with the same extensions as the full editor (StarterKit, Markdown, Image, ImageUpload, EmbedNode, PaywallGateNode, Placeholder, CharacterCount). Top zone: title (Literata 22px italic) + `PUBLISH AS: …`selector from`publications.myMemberships()`. Inline toolbar. Body styled via new `.article-compose-body`CSS class (Literata 17px / 1.8). Paywall gate toggle with price input on insert. Controls zone: autosave timestamp,`OPEN IN FULL EDITOR ↗`, `SCHEDULE` (inline datetime picker), crimson Publish (`btn-accent`). Autosaves to the drafts table via the existing `createAutoSaver`(3s debounce); "Open in full editor" flushes the draft and navigates to`/write?draft=<id>[&pub=<slug>]`so the full editor hydrates with the same state.`stores/compose.ts`gains`'article'`mode,`openArticle({ draftId?, publicationSlug? })`, and `setMode(mode)`for mid-compose escalation. The previous broken`/write/new`escape link is now a`Write an article →`mode switch. Desktop overlay widens from 640→760px. V1 defers dek/tags/email-toggle/comments-toggle/show-on-writer-profile/FLOWING-CUSTOM to the full editor.
Previously: 2026-04-18. Completed: Redesign Step 4 (playscript thread treatment) per`docs/adr/ALLHAUS-REDESIGN-SPEC.md`§4 "Thread rendering".`ReplyItem.tsx`deleted and replaced with`PlayscriptReply.tsx`+`PlayscriptThread.tsx`. `ReplySection.tsx`flattens the nested reply tree into a chronological`PlayscriptEntry[]`, setting `replyingTo`only when the parent isn't the immediately-previous entry (drives the`→ NAME:`arrow for non-adjacent parents). Gateway`GET /replies/:targetEventId`now joins`trust_layer1`for author`pipStatus`and emits`parentCommentId`on each node. Card shape: 32px left step-in on the thread container, 32px vertical rhythm between entries, no borders or hairlines. Each entry: mono-caps 11px speaker line (pip · bold Jost name · colon, or`YOU:`with no pip for self), Jost 14.5px/1.55 dialogue line, interactive vote controls pinned top-right, action row (time · REPLY · DELETE · REPORT) reveals on hover/focus with a`#fafaf7`tint. Pagination: first 10 entries +`SHOW N MORE REPLIES`. Nesting depth erased — a reply to a reply to a reply is three flat entries.
Previously: 2026-04-18 earlier. Completed: Redesign Step 3¾ (reading-history resumption) per `docs/adr/ALLHAUS-REDESIGN-SPEC.md` §4 + §6. Migration 069 (`reading_positions`table keyed`(user_id, article_id)`+`always_open_articles_at_top`preference column on accounts). Gateway routes:`PUT /reading-positions/:nostrEventId`(upsert),`GET /reading-positions/:nostrEventId`, `GET/PUT /me/reading-preferences`. Client hook `useReadingPosition`debounces snapshots on scroll (~500ms) and flushes on`pagehide`/`visibilitychange`via`fetch keepalive`; restore skips the 10% grace zone, the "scrolled to foot" tail, anchored URLs, and the always-open-at-top preference. Bookmark button retained — §6 note: no window-of-no-mechanism risk while both coexist. Settings page gains a "Reading preferences" section.
Previously: 2026-04-18. Completed: Redesign Step 3½ (article tiers — lead/standard/brief) per `docs/adr/ALLHAUS-REDESIGN-SPEC.md` §4a. Migration 068 (`size_tier`column + BEFORE INSERT trigger that derives from word_count while preserving editorial overrides). Backfill: <1000 → brief, 1000–3000 → standard, ≥3000 → lead. Gateway feed route emits`sizeTier`. `ArticleCard`branches headline (30/22/20px) and skips excerpt+tags for briefs; new`twoUp`prop shrinks byline/action to 10.5px and drops Quote/Bookmark/Share.`FeedView.layoutBlocks()`pairs adjacent briefs two-up (40px gutter) with a 72px zone-break before each contiguous run.
Previously: 2026-04-17. Completed: Redesign Steps 1–3 (the swap, compose overlay, card chassis refactor) per`docs/adr/ALLHAUS-REDESIGN-SPEC.md`.
Previously: 2026-04-17. Completed: Trust graph Build Phase 4 (epoch aggregation and decay) — migration 067 (trust_epochs table, vouch decay tracking columns), pure library functions for attestor weighting (Phase A formula: age × payment × readership × activity) and aggregation (freshness decay, graduated small-scale protection, normalised scoring), trust_epoch_aggregate cron task (quarterly full epochs + Mon/Thu mop-ups, small-subject rule, threshold gate, dry-run mode, monitoring logs), 42 unit tests, gateway trust endpoint updated to prefer epoch scores over live counts, frontend dimension bars updated to use real scores. See `docs/adr/ALLHAUS-OMNIBUS.md`§II.8 + Build Phase 4.
Previously: 2026-04-17. Completed: Trust graph Build Phase 2 (vouching CRUD) — migration 066 (vouches + trust_profiles tables), POST /vouches (upsert with dimension/value/visibility), DELETE /vouches/:id (soft-delete withdrawal), GET /trust/:userId extended with Layer 2 dimension scores + public endorsements + Layer 4 relational data + viewer's existing vouches, GET /my/vouches for own vouch list. Frontend: TrustProfile component (dimension bars, endorsements, "your network says"), VouchModal (dimension checkboxes, visibility radio, aggregate disclaimer), vouch button on writer profiles, VouchList withdrawal UI on /network?tab=vouches. See`docs/adr/ALLHAUS-OMNIBUS.md`.
Previously: 2026-04-17. Completed: Trust graph Build Phase 1 (Layer 1 enrichment) — migration 065, trust_layer1_refresh cron, GET /trust/:userId, pip rendering on all feed cards. See `docs/adr/ALLHAUS-OMNIBUS.md`.
Previously: 2026-04-15. Completed: Gateway decomposition — service-layer extraction for `routes/messages.ts`. 693-line route file split into `routes/messages.ts`(202 lines, thin dispatchers) +`services/messages.ts`(563 lines, business logic).`ServiceResult<T>`discriminated union for HTTP error mapping without throws. All 13 DM endpoints covered (conversations, messages, likes, read-state, decrypt batch, pricing). Per`docs/adr/GATEWAY-DECOMPOSITION.md`, this hedges the eventual messaging-service extraction — the cutover becomes a mechanical import→HTTP-client swap rather than a combined factor+extract. `replies.ts`deliberately left in the gateway (article/note threading, not DMs). Build + 52 tests green.
Previously: 2026-04-14. Universal Feed audit triage pass 2 — all remaining items from`docs/audits/universal-feed-audit.md`landed. Security: S1 (DNS-rebinding TOCTOU closed — undici Agent with`connect.lookup` hook pins the validated IP through the socket layer), S4 (`GET /resolve/:requestId`now binds results to the initiating session). Correctness: K1 (Nostr kind 5 deletions match on raw event id via`interaction_data->>'id'`, not recomputed nevent), K2 (RSS sorts by publishedAt DESC before `maxItems`slice so recent content wins), K3 (Bluesky truncation appends`/{username}`all.haus link within the 300-grapheme budget), K4 (feed poll filters atproto rows when Jetstream is healthy), K5 (resolver regexes accept`+` addressing and multi-label TLDs), K6 (`outbound_token_refresh`skips rows with no session). Design: D3 (migration 061 —`resolver_async_results` table + 5m prune cron replaces per-replica Map), D4 (protocol-specific OAuth state cookies), D5 (`requireAuth`on Bluesky callback), D6 (versioned ciphertext with multi-key decryption for rollover). Minors: Nostr`last_error`truncated to 1000 chars, resolver ILIKE special chars escaped,`enqueueCrossPost`/`enqueueNostrOutbound`INSERT + add_job wrapped in`withTransaction`.
Previous: 2026-04-14 earlier — Universal Feed audit triage pass 1 — C1 (explore-feed score ordering preserved, new-user cards interleaved), C2 (Jetstream leader-elected via `pg_try_advisory_lock`), C3 (RSS `fetch_interval_seconds`resets on recovery), C4 (Mastodon quote appends source URL), S2/S3/S5/S6 (Nostr WS SSRF, future-timestamp rejection, subscribe input validation), partial S1 (IP range list extended), D1 (migration 060`DbStateStore`), D2 (`clientPromise` cache clears on rejection).
Previously: 2026-04-14 earlier — Universal Feed Phase 4 — Mastodon ingestion (ActivityPub outbox polling + WebFinger resolution).
Previously: 2026-04-14 earlier — Universal Feed Phase 3 (Bluesky via Jetstream). 2026-04-13 (v5.35.0) — Universal Feed Phase 1 (RSS) + Phase 2 (feed_items unified timeline, external Nostr).

---

## How this is organised

1. **Bugs & fixes** — things that are broken or dangerous right now
2. **Incomplete features** — half-built work from executed specs
3. **New features** — unbuilt features from executed specs, ready to build
4. **Strategic initiatives** — large-scope work with its own spec document still in the project root
5. **Missing table-stakes UI** — features any user would expect but that don't exist yet

---

## 1. Bugs & Fixes

### DONE — verified fixed in codebase audit 2026-04-06

All high-priority bugs have been resolved:

- ~~DM sender visibility~~ — WHERE clause includes `OR dm.sender_id = $2`
- ~~requireAdmin missing return~~ — `return reply.status(403)...` present
- ~~Auth middleware ignores account status~~ — queries `accounts.status`, rejects non-active
- ~~Rate limiting~~ — `@fastify/rate-limit ^8.1.0` installed with per-route config
- ~~Security headers~~ — HSTS, X-Frame-Options, CSP, Referrer-Policy all in nginx.conf
- ~~Non-root Docker containers~~ — all Dockerfiles have `addgroup/adduser` + `USER app`
- ~~Remove internal service port bindings~~ — only postgres, strfry, gateway, web, nginx expose ports
- ~~renderMarkdownSync XSS~~ — function deleted; only rehype-sanitize pipeline remains
- ~~LIKE metacharacters unescaped~~ — `escapeLike()` escapes `%`, `_`, `\`
- ~~Config cache never invalidated~~ — 5-minute TTL + `invalidateConfig()` method
- ~~Notification type mismatch~~ — **resolved in this session:** phantom types `dm_payment_required` and `new_user` removed from frontend union (backend never creates them). Fallback renderer covers future types. Notification centre redesigned as permanent log (v5.11.0).
- ~~Drive update truthiness bug~~ — uses `!== undefined`; Zod `.min(1)` rejects zero anyway
- ~~Auth hydration race~~ — every protected page has `if (loading || !user) return <skeleton>` guard
- ~~Article price upper bound~~ — `.max(999999)` on pricePence validation
- ~~Missing NODE_ENV=production~~ — all Dockerfiles have `ENV NODE_ENV=production`
- ~~Missing .dockerignore~~ — root `.dockerignore` exists
- ~~Docker health checks~~ — all 9 services have healthcheck blocks
- ~~Missing ON DELETE clauses~~ — fixed by migrations 018 + 021
- ~~Session storage not cleared on logout~~ — clears all `unlocked:*` keys
- ~~Dependency version conflicts~~ — pg `^8.20.0` and dotenv `^17.3.1` aligned everywhere

### Still outstanding

**From the two-day commit review (2026-06-10 → 06-11 work, reviewed 2026-06-12).** Four-area parallel review of the network-concierge, mobile-workspace, palette/theme, and composer/glasshouse changes. The bulk was clean (migrations round-trip, both OAuth CSRF fixes correct, registry ↔ globals.css in sync, no escapes, no Phase-4 concierge stub that can run). Real findings — **all substantiated against the codebase and fixed 2026-06-12 (`8410b57`)** except the pre-existing ceremony item; validated web+gateway `tsc` clean, root lint 0 errors on touched files, hairline tripwire clean, 117 gateway tests green. Frontend fixes not yet browser-verified (needs `docker compose build web && up -d web`):

- **Assisted-atproto tokens never refresh — uncommitted fix must land.** `feed-ingest/src/tasks/outbound-token-refresh.ts:41,77` — the committed code filters `provenance = 'linked'`, which excludes `assisted` presences (Phase 2/3 Bluesky/Mastodon), so their OAuth tokens expire and dispatch starts failing. The working-tree edit changes it to `provenance <> 'concierge'` (refresh both custody-by-OAuth tiers, skip key-custody concierge) — correct per the "branch on custody, not provenance" invariant. **Fixed + committed 2026-06-12.** (This was the one active bug; the rest below are lower-severity.)
- ~~**Mobile resume resets to feed 1 when the active feed is hidden**~~ (likely-bug — confirmed real). `MobileWorkspace.tsx:108-119` + `WorkspaceView.tsx:519` — `MobileWorkspace` only receives `visibleSorted` feeds, so a hidden active feed makes `indexOf(activeId)` → `-1`, `activeIndex` falls back to 0, and the `writeResume` effect then overwrites the stored resume id with feed-1's. The previously-active feed is forgotten even after un-hide, contradicting the spec's "resume keys off `id` to survive hide" goal. **Fixed 2026-06-12 (`8410b57`):** the write effect now persists only when the displayed feed is the chosen one (`activeId === null || activeId === activeFeedId`) — display still falls back to feed 1 while the resume feed is hidden, but the stored id survives until an explicit swipe, so un-hide restores the spot. (Known residue, accepted: a *deleted* feed's id lingers in storage until the next swipe — harmless, `readResume` falls back to feed 1.)
- ~~**Palette devtool can read "Custom" forever after a registry slug rename**~~ (likely-bug, operator-only surface — confirmed real). `web/src/stores/paletteDevtool.ts:68-78` — `hydrate()` loads the persisted blob verbatim with no sanitisation. A stale slug lingers in `overrides`: `applyPaletteOverrides` ignores it, but it inflates the "(N changed)" count and breaks `matchesPreset`'s length check, so a standard config reads as Custom permanently. **Fixed 2026-06-12 (`8410b57`):** `hydrate` filters persisted overrides to live `PALETTE_REGISTRY` slugs and re-persists if anything was dropped. Deliberately a registry filter, **not** `sanitizeThemeOverrides` — the devtool is the operator surface and legitimately holds `THEME_LOCKED_SLUGS`, which that helper would strip.
- ~~**Cosmetic (design-token drift, not functional):**~~ `NetworkReachPanel.tsx` (lines ~315/364/371/386/393) on the fixed-light Glasshouse pane uses `bg-grey-100`/`placeholder-grey-300` entry fields (should be solid `bg-white`) and `text-grey-400` secondary text (should be `grey-600`+ for contrast on `#DCDAD3`) — one such instance already fixed in the working tree at line 284, the rest remain. `PostMedia.tsx:163` caption scrim left as raw `rgba(0,0,0,0.72)` while the adjacent colour was migrated to `var()`. Scheme-id list order differs between gateway `FEED_SCHEME_IDS` and frontend `FeedScheme` (harmless for the `z.enum`/Set, but CLAUDE.md says they should mirror). **All fixed 2026-06-12 (`8410b57`):** entry fields `bg-white` (placeholders stay `grey-300` inside white fields per the pane rule), labels/secondary `grey-600`; scrim → `rgb(var(--ah-true-black-rgb) / 0.72)`; gateway list reordered to mirror `tokens.ts` (`sand` before `slate`).
- **Pre-existing, not from this window:** first-login ceremony fully commented out behind a live condition (`WorkspaceView.tsx:940-948`, `// TODO: re-enable / refine entrance animation`).

### Moved to Infrastructure backlog

- ```23 instances of `any` across the frontend~~ — moved to infrastructure backlog (not a bug, incremental cleanup)

  ```

  ```

- ~~No CI/CD~~ — moved to infrastructure backlog
- ~~TypeScript target mismatch~~ — moved to infrastructure backlog (cosmetic, no runtime impact)
- ~~Accessibility gaps~~ — **resolved:** vote buttons already had aria-labels; paywall indicator uses price text (not colour-only); dropdown keyboard nav (Escape-to-close, aria-expanded, role="menu") added to AvatarDropdown and NotificationBell.
- ~~Reduce JWT session lifetime~~ — **fixed:** 30-day JWT with 7-day refresh threshold (~23% of lifetime). Active users stay logged in; `sessions_invalidated_at` provides server-side revocation.

---

## 2. Incomplete Features

### DONE — verified complete in codebase audit 2026-04-06

- ~~Reader subscription management~~ — `SubscriptionsSection.tsx` with cancel controls, fully wired into account page
- ~~Reader tab overview~~ — `BalanceHeader.tsx` shows free allowance remaining, fully wired
- ~~Export modal polish~~ — uses `Set<ExportType>` (not single boolean), writer guard on backend, per-type error messages
- ~~Subscription price in settings~~ — by design: dashboard is the writer control room, `/settings` is reader-focused

### Still outstanding — backend exists, no UI

These endpoints are fully wired but have no way to trigger them from the frontend. Audited 2026-04-13.

~~Delete / archive publication~~ — **done (v5.31.0):** Danger zone in PublicationSettingsTab with archive (confirm dialog), transfer ownership (modal with EiC member selector), and delete (type-to-confirm modal matching publication name). Owner-only visibility.

~~Transfer publication ownership~~ — **done (v5.31.0):** Modal with radio-button selector for eligible Editor-in-Chief members. Redirects to personal dashboard after transfer.

~~Reading history page~~ — **done (v5.33.0):** ReadingHistory component on /account page. Paginated list showing article title (linked), writer avatar/name, date read, and Free/Paid label. Uses existing `GET /my/reading-history` endpoint.

~~Subscriber list for writers~~ — **done (v5.30.0):** SubscribersTab component with summary stats (active count, est. MRR, new this month) and full subscriber table. Conditional tab in personal dashboard (writer-only). Uses existing `GET /subscribers` endpoint.

~~Edit publication member role~~ — **done (v5.31.0):** Inline "Change role" action in MembersTab. Clicking replaces role cell with select dropdown + Save/Cancel. Uses existing PATCH endpoint.

~~Accept / decline commission~~ — **done (v5.33.0):** New `GET /my/commissions` endpoint. CommissionsTab in dashboard (writer-only) with pending/accepted/completed sections. Accept button and Decline with confirm pattern.

~~Pin drive to profile~~ — **already existed:** DriveCard had Pin/Unpin toggle calling `POST /drives/:id/pin`. Verified working.

~~Edit existing drive~~ — **done (v5.33.0):** Inline edit mode on DriveCard — Edit button swaps card to form with title, description, target amount fields. Uses existing `PUT /drives/:id` endpoint.

**Admin direct suspend** — `POST /admin/suspend/:accountId` suspends an account outside the report flow. Admin reports page has resolve/reject, but no standalone suspend action.

~~Unpublish personal article~~ — **done (v5.29.0):** `POST /articles/:id/unpublish` endpoint + Unpublish button in personal Articles tab with confirm dialog and inline "Moved to drafts" message.

### Previously outstanding — now done

~~Subscription offers system~~ — **done (v5.13.0):** migration 037 creates `subscription_offers` table with `code`/`grant` modes. `POST /subscriptions/:writerId` accepts optional `offerCode`, validates and applies discount. `offer_id` and `offer_periods_remaining` tracked on subscriptions; renewal job decrements and reverts to standard price when offer period elapses. Dashboard Offers tab with create/list/revoke. Public redeem page at `/subscribe/:code`.

~~Gift link frontend~~ — **done:** dashboard GiftLinksPanel (create/list/revoke per article in Articles tab) + "Gift link" option in ShareButton dropdown.

~~DM pricing / anti-spam settings~~ — **done:** GET/PUT `/settings/dm-pricing` + per-user override endpoints. Moved from dashboard settings tab to `/social` page (v5.14.0 settings rationalisation). **Note (2026-04-19):** pricing config persists, but the send-side 402 enforcement branch was pulled in FIX-PROGRAMME §12 because no endpoint existed to take payment and unblock the send. DMs are effectively free until a charge-and-unblock endpoint is built — the stored prices will start enforcing again the day that ships.

~~Commission social features~~ — **done:** Commission button in DM thread header opens CommissionForm modal. Migration 036 adds `parent_conversation_id` to `pledge_drives`. Backend and API client pass conversation context through.

---

## 3. New Features (unbuilt, from executed specs)

All items below are entirely unbuilt — no migrations, routes, or components found.

### ~~Bookmarks / save for later~~

**Done (v5.29.0):** Migration 047 (bookmarks table), gateway routes (POST/DELETE by Nostr event ID, GET list, GET batch IDs), BookmarkButton component with optimistic update, /bookmarks page, feed integration (batch bookmark ID loading, isBookmarked prop on ArticleCard), avatar dropdown link.

### ~~Hashtags / topics / tags~~

**Done (v5.29.0):** Migration 048 (tags + article_tags tables), gateway tag routes (autocomplete search, browse by tag, get/set article tags), TagInput component in editor (pill-style with autocomplete dropdown, 5 tag max), tag display on ArticleCard (linked pills below excerpt), /tag/[tag] browse page, tags saved through both personal and publication publish flows, tags loaded when editing existing articles, feed endpoint includes tags via correlated subquery.

### ~~Writer analytics~~

**Done (v5.28.0 — Traffology Phase 1):** Complete analytics system with page tracking script, ingest service, hourly/daily/weekly aggregation, source resolution, observation generation, feed UI, piece detail with provenance bars, and overview with baseline stats. See `TRAFFOLOGY-BUILD-STATUS.md` and `docs/adr/TRAFFOLOGY-MASTER-ADR-2.md`.

### Reposts / reshares

Requires: migration (reposts table), gateway routes, Nostr kind 6 event publishing, RepostButton component, feed integration with "Reposted by" labels. Needs feed algorithm to be meaningful.
_(Source: FEATURES.md feature 8)_

### Email-on-publish

Requires: migration (email*on_new_article boolean on accounts), send logic in article publish flow, email template, settings toggle.
*(Source: FEATURES.md feature 9)\_

### Subscription improvements (Phase 2)

Phase 1 is done (auto-renewal, annual pricing, subscribe at paywall, spend-threshold nudge, comp subscriptions). Remaining from Phase 2:

- **Free trials** — writer-configurable 7/30-day trial period
- **Gift subscriptions** — "buy a subscription for someone"
- **Welcome email** — configurable email on subscribe
- **Subscriber import/export** — CSV for migrating to/from Substack
- **Subscriber analytics** — growth, churn, MRR trend
- **Custom subscribe landing page** — `/username/subscribe`
  _(Source: docs/audits/SUBSCRIPTIONS-GAP-ANALYSIS.md)_

### Per-feed colour schemes + global preset themes (palette productisation)

**SHIPPED 2026-06-11, both tiers v1** — see the sixth-pass work-log entry in §1 for the full build record. The design constraints all landed as specified: per-feed schemes are curated **surface-only** sets (`web/src/lib/palette/registry.ts` scheme slugs) expanded deterministically by `deriveVesselPalette` (`tokens.ts`) — luminance-based family selection between the two tuned text ramps, crimson→crimson-soft flip on dark cards, trust pips untouched; storage is server-side feed character (`feeds.appearance` JSONB, migration 112); global theming is curated preset var-sets only (`web/src/lib/palette/themes.ts` + Settings `ThemeSection`) through the devtool override mechanism, with the ∀ chrome / Glasshouse / trust pips locked out (`THEME_LOCKED_SLUGS`).

**Update 2026-06-14 — user-facing palette/theme editing retired; schemes refreshed.** Two ADRs landed (`docs/adr/GLASSHOUSE-AND-PALETTE-ADR.md`, `docs/adr/FEED-SCHEME-REFRESH-ADR.md`):
- **Palette controls retired** (GLASSHOUSE-AND-PALETTE §III.5) — the ForallMenu "Palette" row and the Settings `ThemeSection` preset picker are gone (`ThemeSection` parked, not deleted). The registry + var indirection + override mechanism stay; the `PalettePanel` devtool is now **operator-only** (reach via `?palette` / `Ctrl+Alt+P`); boot-time hydration moved to a headless `PaletteHydrator` (the load-bearing component). Existing user overrides are **purged once on upgrade** (sentinel `ah:palette-purged-v1`) to reclaim a single canonical identity. Per-feed schemes (separate key) survive.
- **Per-feed scheme is now a click-through `AppearanceControl`** (§III.4), matching density/orientation/text-size — the swatch row is gone.
- **Glasshouse pane → pale parchment** (`#F5F4F0`) lifted over a desaturating **`.gh-scrim`** that does the separating (§III.1–2).
- **Four schemes refreshed** (FEED-SCHEME-REFRESH-ADR): `blush`/`sage`/`sand`/`slate` → `mata`/`cobalto`/`vela`/`caju` (Brazilian-modernist family); retired ids migrate on read via `SCHEME_ALIASES` (no DB backfill). Cycle is now six schemes — the ceiling for a click-through; a seventh should prompt a picker.

Remaining (deliberate residue, not blockers):

- **Free colour wheel per feed** — the picker swaps, the derivation stays. Needs arbitrary-surface storage (`appearance` carries hexes, not a scheme id) + the mid-luminance contrast floor the curated swatches currently avoid by construction.
- **Per-scheme accent token** — a fourth per-scheme slot distinct from system crimson (relieving crimson of double-duty, fixing the Caju red-on-red adjacency, and letting a hot splash drop into any scheme). Deferred to its own ADR (FEED-SCHEME-REFRESH §V).
- **Preset themes** — retired from user-facing surfaces (above); the curated var-sets in `themes.ts` are parked alongside `ThemeSection` for possible operator/future use.
- **Other appearance axes** — **density and `hidden` moved server-side 2026-06-11** with the MOBILE-LAYOUT-ADR build (density as a second `feeds.appearance` JSONB key, hide as a real `feeds.hidden` column + `sort_rank`, migration 113; both follow the scheme's server-plus-local-mirror precedence). Orientation and text size remain per-device localStorage by design: orientation is a canvas property with no mobile meaning (MOBILE-LAYOUT-ADR §VI), text size is unaddressed.

_(Source: design conversation 2026-06-11, following the canonical colour registry ship; shipped same day.)_

---

## 4. Strategic Initiatives

### DONE

**Feed algorithm Phase 1** — fully implemented. Migration 035 (`feed_scores` table), background scoring worker (`feed-ingest/src/tasks/feed-scores-refresh.ts`, Graphile cron `*/5 * * * *`), `GET /feed` with `reach` parameter (following/explore), UI reach selector in `FeedView.tsx`. Spec archived: `planning-archive/FEED-ALGORITHM.md`.

**Resilience & performance** — complete. Article/profile pages are Server Components, fonts self-hosted, NDK removed from client bundle, shared Avatar component, print stylesheet, error boundaries. Spec archived: `planning-archive/RESILIENCE.md`.

**Settings rationalisation** — done (v5.14.0), restructured again (v5.34.0). Six pages: Profile (public identity), Settings (email, payment, notifications, export, danger zone), Ledger (balance, earnings, subscriptions, pledges), Library (bookmarks, reading history), Network (following, followers, blocked, muted, feed dial, DM fees), Dashboard (articles+drafts, subscribers, proposals, pricing). Old URLs (/account, /bookmarks, /following, /social, /followers, /history, /reading-history) redirect to new locations. Spec archived: `planning-archive/SETTINGS-RATIONALISATION.md`.

**Publications Phases 1–3 + Phase 5** — done (v5.18.0–v5.20.0). Schema, CMS pipeline, reader surface, subscriptions/follows, RSS, search, feed integration, revenue (rate card, payroll, earnings).

**Universal Feed Phases 1–5** — done (v5.35.0–v5.36.0 + 2026-04-14). RSS, external Nostr, Bluesky (Jetstream), Mastodon (AP outbox) ingestion. Unified `feed_items` timeline. Outbound cross-posting (Mastodon + Bluesky + external Nostr). AT Protocol OAuth. Linked accounts UI. See `docs/adr/UNIVERSAL-FEED-ADR.md`.

**Relay outbox programme (§60)** — done (2026-04-22 to 2026-04-23). All 6 phases shipped: infra (migration 076), fire-and-forget sites, awaited sites, publish-path (publication articles), scheduler contortion retirement, observability + integration tests. `publishToRelay`/`publishToRelayUrl` deleted from gateway. See `docs/adr/RELAY-OUTBOX-ADR.md` + `docs/adr/RELAY-OUTBOX-PHASE-4-ADR.md`.

**Redesign Steps 1–6** — done (2026-04-17 to 2026-04-18). The swap (nav reduction, compose button, subscribe input), compose overlay (note/reply/article modes), card chassis (unified left-bar + byline + action row), article tiers (lead/standard/brief + two-up), reading-history resumption, playscript threads, end-of-feed/zero/error states. See `docs/adr/ALLHAUS-REDESIGN-SPEC.md`.

### Still outstanding

**Retire the legacy `/feed` model — make `/workspace` the entirety of all.haus (readiness assessment 2026-06-12).**

> **Fleshed out into an actionable plan: `docs/adr/FEED-RETIREMENT-PLAN.md` (2026-06-12)** — slices 0–7 with files, endpoints, acceptance criteria, deletion manifest. Re-verification corrected the assessment on four points: (1) there are **two** live workspace dependencies on legacy cards, not one — the profile overlay (`NativeProfilePanel` → `WriterActivity` → `WorkTab`/`SocialTab`) draws legacy `ArticleCard`/`NoteCard` too; (2) **post-login still lands on `/feed`** (`auth/page.tsx`, `auth/verify/page.tsx`) — the legacy page is the app's front door; (3) the global-reach product question already has its backend — `GET /feed/:feedId` serves `following`/`explore` as Post[] with zero frontend callers; (4) `timeline.ts` exports have **four** importers (`post-feed`, `post-thread`, `sources`, `author`). The external author profile (`AuthorProfileView`) is already ported to `PostCardInteractive` over `/author/:id/posts`, which also handles native authors — Slice 2 mostly needs a kind filter. The Slice 0 decision (reach as composable source / seeded default feeds / drop) is the operator's; recommendation in the plan is seeded feeds now, composable source later. The assessment below stands as the original capture.

The "direction of travel" (CLAUDE.md; `docs/adr/REDESIGN-SCOPE.md`): everything that isn't the workspace or an overlay is being retired. UNIVERSAL-POST-ADR §10 Phase 5 deliberately **scoped the cutover to the workspace** and left the standalone surfaces for "a later `/feed`+`/source`-scoped pass" that was never scheduled — so this is named-but-untracked work, captured here.

**Verdict: the expensive part is done.** The unified **Post model** (`web/src/components/post/` + the `GET /thread/:postId` projector) is at full card-rendering parity — articles, notes, all four external tiers, quote embeds, polls, media/video/HLS, content warnings, threads-with-re-root, reader pane all work and are proven in production on the workspace. Retirement is **not** blocked on architecture; it's a well-defined porting pass + a few missing overlays + one product decision. Three buckets:

- **Bucket A — port 5 legacy surfaces onto the Post model.** Each still renders the old `components/feed/` cards over legacy endpoints: (1) `/feed` page (`FeedView` → legacy `GET /feed?reach=`, `timeline.ts`); (2) `/source/[id]` (`SourceSurface` → `ExternalCard` → `GET /sources/:id`); (3) `/tag/[tag]` (`TagBrowser` — hand-rolled rows, not even cards); (4) `/search` (bespoke result rows); (5) native profile tabs (`WorkTab`/`SocialTab` → `ArticleCard`/`NoteCard`). **⚠️ Highest priority: `SourceSurface` is rendered _inside_ the workspace today via `SurfaceOverlay`** — so a source opened from the consolidated world is already drawing un-migrated legacy cards hitting a legacy endpoint. It's the one live workspace dependency on the retiring path; port it first.
- **Bucket B — build overlays for management pages that have none.** Route consolidation is further along than the feed (verbs — dashboard/messages/notifications/ledger/settings — and surfaces — profile/reader/source/tag/pub — are shimmed or overlay-backed, and **no active escapes to the black topbar remain**). But four pages have **no overlay equivalent at all**, and several legacy shims dead-end on them: `/network` (follow graph/followers/vouches/block/mute — `SettingsPanel` only has `NetworkReachPanel`), `/library` (bookmarks+history — and `/bookmarks`·`/history`·`/reading-history` all redirect _here_, onto a topbar page), `/subscriptions` (external feed management), `/profile` (name/bio/avatar editor). (`/traffology`, `/admin` are arguably legit ops-only standalones; `/`, `/about`, `/auth`, `/invite`, `/subscribe` correctly stay standalone — the workspace is login-gated.)
- **Bucket C — two backend untanglings before any deletion.** (1) `timeline.ts` is load-bearing despite being "legacy" — its exported `FEED_SELECT`/`FEED_JOINS`/`feedItemToResponse`/`parseCursor` are imported by the Post-model `post-feed.ts` + `post-thread.ts`; **extract those SQL fragments into a shared module before deleting the legacy `GET /feed` handler** or the new endpoints break. (2) The workspace doesn't even use the `GET /feed/:feedId` projector yet — it flows through a **third** query path (`/workspace/feeds/:id/items` in `feeds.ts`, own duplicated `FEED_SELECT`/`rowToItem`, ranks by `effective_score` not the §5 hotness projector). Decide whether to converge on the projector as part of this. Also retire `GET /conversation/:eventId` (`replies.ts`, no live frontend caller) and the deferred `useNeighbourhood`/`NeighbourhoodCard` legacy thread mechanism (UNIVERSAL-POST-ADR §10 Phase 5 names both).

**The one genuine product question (not engineering):** legacy `/feed` is a single **global following/explore reach timeline**; the workspace is **per-feed composed vessels**. Before deleting `/feed`, decide whether "following"/"explore" become default workspace feeds or whether global-reach is dropped. Everything else in Bucket A is mechanical.

Once A+B+C land, delete `web/src/components/feed/{FeedView,ExternalCard,ArticleCard,NoteCard,QuoteCard,NeighbourhoodCard,SubscribeInput,ActionSheet}.tsx` (note `AuthorModal.tsx` is **shared** — used by workspace `PostByline`/`FeedComposer`, not legacy-only — don't delete it), `useNeighbourhood`, and the orphaned `GET /conversation/:eventId`. Suggested order: SourceSurface-in-overlay (the live dependency) → global-reach product decision → remaining surface ports → backend untangle + deletions. Relates to / partly supersedes the card-behaviour entry below.

**Card behaviour unification — `docs/adr/CARD-BEHAVIOUR-ADR.md`**

Unified card interaction model across all four card types (ArticleCard, NoteCard, ExternalCard, QuoteCard). **Phases 1–3 shipped (2026-05-25 → 2026-05-27).** Phase 1: migration 097 (`is_reply` on `feed_items`), biddability tier on API, click region map, `↳ REPLYING TO @handle` provenance line, source attribution as single route out ("View original" removed); feed-ingest Slice 9 folded in. Phase 2: conversational neighbourhood expansion (`useNeighbourhood` + `NeighbourhoodCard`, tier-driven skeleton/failure/empty states, `↳ SHOW PARENT` / `SHOW N MORE REPLIES` walking). Phase 3: `AuthorModal` hover card + `GET /api/v1/author-card` metadata endpoint + `ActionSheet` touch `⋯` sheet + keyboard `group-focus-within` parity. **Note:** constructed external author profile pages (§VI.3) deferred to their own ADR — tracked below.

**Card behaviour — verified-against-spec gaps (audit 2026-05-29).** A section-by-section verification of the shipped code against CARD-BEHAVIOUR-ADR found five divergences. **All five now resolved (#1 on 2026-05-29; #2–#5 on 2026-05-30).** Phase 4 complete; only the explicitly-deferred §VI.3 constructed external author profile remains (its own ADR).

1. ~~**Neighbourhood expansion is ExternalCard-only (§V.1, §V.3) — substantive.**~~ **DONE (2026-05-29).** `NoteCard` is now kind-aware and renders its parent above the anchor on expand (via `useNativeParent` → `GET /content/resolve`) with the same anchor scroll-pinning as `ExternalCard`; new `NativeParentCard` inset renderer shares the dimmed-bar grammar. The profile Replies surface (`SocialTab`, the one place native replies render as standalone cards — both feeds exclude reply-notes) now flows through `NoteCard`, retiring the bespoke `ReplyCard`. `ArticleCard` left unchanged by design — articles are always `is_reply = false`, so there is no parent-above and the downward `ReplySection` never moves the anchor. Known limit: a reply nested under another comment points its parent-above at the thread root (`targetEventId`), since `/content/resolve` doesn't resolve comments. `tsc --noEmit` clean for `web`; not yet browser-verified.
2. ~~**Thread hydration endpoint shape (§V.4, §VII.3).**~~ **DONE (2026-05-30).** Kept the `/external-items/:id/thread` + `/parent` pair (ADR says name/shape negotiable) and closed the three concrete misses: per-route rate limit `{ max: 30, timeWindow: "1 minute" }` on both routes; 5s `timeout` (new `NEIGHBOURHOOD_FETCH_TIMEOUT_MS`) on every outbound source fetch reachable from them (Bluesky + Mastodon parent/grandparent/thread helpers); `partial` now a server-signalled field on `ThreadResponse` + `ParentContextResponse` (true on source fetch failure/timeout for a reply that expects content). `useNeighbourhood` prefers the server flag over inferring from a rejected promise. 75 gateway tests green; `tsc --noEmit` clean for `gateway` + `web`.
3. ~~**External byline routing (§VI.2).**~~ **DONE (2026-05-30).** Built the minimal internal source surface and routed the byline to it. Gateway `GET /api/v1/sources/:id` (`gateway/src/routes/sources.ts`) returns source metadata + a cursor-paginated page of that source's external items, reusing timeline's now-exported `FEED_SELECT`/`FEED_JOINS`/`feedItemToResponse`/`parseCursor`; `timeline.ts` now also returns `externalSourceId`. New `/source/[id]` page (`SourceSurface` client component: source header + `ExternalCard` list + SHOW MORE). `ExternalCard` byline now `next/link`s to `/source/{externalSourceId}` (origin link stays on the §VI.4 source-attribution line); degrades to a non-link span when no source id. `tsc --noEmit` clean for `gateway` + `web`; 75 gateway tests green; not yet browser-verified. Constructed external author profile (§VI.3) still deferred to its own ADR (tracked below).
4. ~~**NoteCard headline navigation (§IV).**~~ **DONE (2026-05-30).** The body-expand region in `NoteCard.tsx` now carries an INTERIM comment (mirroring `ExternalCard`'s headline marker): notes have no permalink region, so the body click expands until a note permalink page exists.
5. ~~**Empty-state reply affordance (§V.5) — minor.**~~ **DONE (2026-05-30).** `NeighbourhoodEmptyState` takes an optional `onReply` handler; with it the `NO CONVERSATION YET — BE THE FIRST TO REPLY` line renders as a `hover:text-black` button that opens the composer, falling back to the inert label otherwise. `ExternalCard` passes its `handleReply`.

Everything else in §IV–§VIII (region map for Article/External, biddability tier, `is_reply` dual-write + backfill, provenance line + tier degradation, author modal A→D, source attribution, touch `⋯` sheet, keyboard parity, §VI.3 backlog item) verified present and faithful.

**Constructed external author profile pages** — unified cross-platform post history for external authors. Deferred from CARD-BEHAVIOUR-ADR §VI.3. Needs its own ADR. Prerequisite: cross-source identity linking (feed-ingest Slice 8).

**Feed-ingest slices 7–9 — `FEED-INGEST-ATTACK-PLAN.md`**

Three new slices added to the feed-ingest build plan. Slice 7: ActivityPub inbox (push delivery — real-time AP, edit/delete propagation; posture-gated). Slice 8: cross-source identity linking (dedup when following same person on multiple protocols; no infrastructure gate). Slice 9: CARD-BEHAVIOUR-ADR feed support (`is_reply` column + biddability tier on API; 1–2 days, smallest slice, unblocks card behaviour frontend).

**Codebase audit — `docs/audits/AUDIT-REPORT.md`**

34-item audit from 7 April 2026. Most critical/high items fixed. Still outstanding:

- #6: `publications.ts` PATCH uses raw JS keys as SQL column names (fragile, no mapping)
- #12: `sendError` helper in `gateway/src/lib/errors.ts` is dead code (never imported)
- #14: Stale doc references in CLAUDE.md to FEATURES.md and DESIGN-BRIEF.md (moved to archive, refs not updated)
- Design issues #19 (inconsistent error shapes), #20 (pervasive `as any`), #21 (`requirePublicationPermission()` with no args), #22–23 (background workers in gateway process), #24 (no soft-delete for notes), #25–27 (naming inconsistencies)

**Code quality hardening — `docs/adr/CODE-QUALITY.md`**

Reference catalogue of tooling tiers. Nothing built yet. Priority items:

- Tier 1a: CI pipeline (GitHub Actions with tsc + vitest)
- Tier 1b: Backend ESLint (promise-safety rules)
- All other tiers deferred until second contributor or post-launch

**Traffology Phases 2–4 — `docs/adr/TRAFFOLOGY-MASTER-ADR-2.md`**

Phase 1 complete (build status archived: `planning-archive/TRAFFOLOGY-BUILD-STATUS.md`). Remaining phases:

- Phase 2: Nostr monitor service (relay polling for reposts/reactions/quotes)
- Phase 3: Outbound URL search (Bluesky, Reddit, HN, Mastodon APIs) + pattern observations
- Phase 4: Publication editor view

**Feed-ingest + hydration scaling — `docs/audits/FEED-INGEST-HYDRATION-AUDIT.md` + `…-PLAN.md`**

Deep audit of the ingest workers + feed read path. Every finding is **scaling** severity (harmless at 20–30 writers; bites as `repost_edges` / external-item count / followee history grow). **Tranche A** (read-path scoping: composite-index fix, candidate-scoped + recency-bound boost CTE, LATERAL attribution limit, `platform_config` cache), **Tranche B** (batching + cadence: batched RSS dual-write, adaptive RSS interval, batched/skip-unchanged engagement writes, age-tiered engagement cadence, debounced Jetstream cursor flush, debounce-batched atproto parent prefetch), **C4** (`feed_items.reply_to_author` denormalisation, migration 105), and the **C2 safe one-liner** (decoupled per-tick poll enqueue cap → `feed_ingest_max_enqueue_per_tick`, default 100, lifts the ~50-source throughput ceiling) all shipped (2026-06-04). Cross-cutting: dual-write invariant confirmed. **Two defects from this work were caught and fixed 2026-06-04 (commit 30750f4, "feeds not loading" follow-up audit):** (1) **C1#14 REVERTED** — the `following` native 30-day bound broke parity with legacy `followingFeed` (which bounds only its external arm) and collapsed the feed 266 → 1 items; removed, native stays unbounded. (2) **C2 cap was inert** — `feed_ingest_max_enqueue_per_tick` was never seeded so the fallback resolved to `max_concurrent='10'`; **migration 106** now seeds it to `'100'`. **Tranche C — deferred until metrics demand** (build the design before you need it; the trigger is feed p95 starting to track total `repost_edges` size / followee history):

- **C2 (#1) — per-host politeness relocation.** The safe one-liner lifted the throughput ceiling but per-host politeness is still an enqueue-layer throttle (`maxPerHost`, ≤2/host/tick). To enqueue *all* due sources without a herd, move per-host bounding into the fetch task (per-host advisory lock or per-host job queue), *then* drop the per-tick cap entirely.
- **C3 (#6) — Jetstream firehose sharding.** Above `WILDCARD_DID_THRESHOLD` (150 subs) the listener subscribes to the whole Bluesky firehose and `JSON.parse`s every event client-side (single-process CPU cliff scaling with Bluesky, not subscriptions). Shard into K filtered WebSockets each carrying ≤150 server-side-filtered `wantedDids`. Only matters past ~200 subscriptions.
- **C1 heavy (#12) — pagination work reduction.** Live `score_live` (function of `now()` + live `repost_edges`) can't be indexed, so the `scored → deduped` CTE materialises + sorts the whole candidate set every page (page 10 redoes page 1). Largest single item. Real fix: two-phase read (cheap candidate-id pull, then hydrate only the page) or a materialised per-reader candidate pool refreshed by a worker; also bound max page depth.
- **#15 — verify the plan.** `EXPLAIN ANALYZE` the `scored` CTE against a *seeded large dataset* to confirm the Tranche A scoping collapsed the correlated-subquery line items and the planner hashes the follow sets once. Needs a seeded large dataset to be meaningful (dev at 20–30 writers won't surface it).

**Frontend audit — `docs/audits/all-haus-frontend-audit.md`**

12-item ranked audit. 6/12 resolved. Outstanding items:

- ~~#1: Open Graph / social sharing metadata~~ — **done (v5.32.0):** OG + Twitter Card tags on all public pages
- ~~#2: Email / newsletter delivery~~ — **done:** `sendPublishNotifications()` wired to publish route + "Email subscribers" checkbox in editor
- #3: Landing page (minimal — no social proof, no screenshots, no tab model explanation)
- ~~#4: Writer analytics~~ — **done:** Traffology Phase 1 (feed UI, piece detail, overview, dashboard Analytics tab)
- #5: Publication homepage templates (wireframe-quality, no visual customisation)
- #6: Writer onboarding flow (no post-signup wizard)
- ~~#7: CSP header blocking external images~~ — **done:** `img-src` widened to include `https:` for external feed media
- #8: Import tooling (no Substack/Ghost/WordPress import)
- #9: Frontend test coverage (6 test files now exist — vault, publish, voting, format, markdown, media — was zero)
- ~~#10: Dashboard architecture~~ — **done (v5.34.0):** tab contents extracted into 10+ separate components
- #11: Dark mode
- #12: Design system housekeeping (CSS aliases cleaned; `platform-pub` naming persists in docker-compose/package.json)

**Owner dashboard — `docs/adr/OWNER-DASHBOARD-SPEC.md`**

Entirely unbuilt. Admin area has only the reports page. Spec covers:

- Overview (money pipeline visibility + trigger buttons)
- Users (account metrics, KYC-incomplete writers, conversion funnel)
- Content (publishing activity, system health)
- Config (platform_config editor)
- Regulatory (UK tax thresholds, VAT approach warning, custodial exposure)

**Subscriptions Phase 2 — `docs/audits/SUBSCRIPTIONS-GAP-ANALYSIS.md`**

Phase 1 complete (auto-renewal, annual pricing, subscribe at paywall, comp subscriptions, offers system). Phase 2 outstanding:

- Free trials (writer-configurable 7/30-day)
- Gift subscriptions ("buy for someone")
- Welcome email on subscribe
- Subscriber import/export (CSV)
- Subscriber analytics (growth, churn, MRR trend)
- Custom subscribe landing page (`/username/subscribe`)

**Publications Phase 4 — `docs/adr/PUBLICATIONS-SPEC.md`**

Theming and custom domains, deferred:

- Wildcard subdomain routing + custom domain DNS verification + TLS
- Theme settings UI + custom CSS editor
- Per-publication favicon

**Bucket categorisation system — `docs/adr/platform-bucket-system-design.md`**

A generic system for user-defined, non-overlapping categories with behavioural rules. Conceptual — no implementation plan yet.

**Currency strategy — `docs/adr/platform-pub-currency-strategy.md`**

Multi-currency support. Option 2 (launch with GBP, display-only conversion) recommended. Entirely unbuilt.

**Trust Graph — `docs/adr/ALLHAUS-OMNIBUS.md`**

Eight build phases specified in the omnibus. Spec covers trust graph (Layer 1 automatic signals, Layer 2 attestations, Layer 3 graph analysis, Layer 4 relational presentation) and Phase A/B anonymity strategy. Full spec in `docs/adr/ALLHAUS-OMNIBUS.md` (Books I, II, IV). Book III (workspace specification) was removed — the panel-based workspace was retired in favour of the single-surface model in `docs/adr/REDESIGN-SCOPE.md`.

Build Phase 1 (Layer 1 enrichment) — **DONE (2026-04-17):**

- Migration 065 (`trust_layer1` table — per-user precomputed signals + pip_status)
- `feed-ingest/src/tasks/trust-layer1-refresh.ts` — daily cron (01:00 UTC) computing signals from accounts, articles, read_events, stripe status
- `GET /api/v1/trust/:userId` — gateway endpoint returning Layer 1 signals
- Trust pip (5px circle) rendering on all feed cards (ArticleCard, NoteCard, ExternalCard)
- `PipStatus` type (`known`/`partial`/`unknown`) added to feed item types
- `.trust-pip` CSS class in globals.css
- Thresholds: known = >1yr + >50 paying readers + Stripe KYC; partial = >90d + any readers or articles; unknown = everything else
- NIP-05 verification defaults false (no NIP-05 table yet — wire up when that ships)

Build Phase 2 (vouching CRUD) — **DONE (2026-04-17):**

- Migration 066 (`vouches` + `trust_profiles` tables). Vouches: attestor/subject/dimension unique, CHECK constraints (no self-vouch, contests must be aggregate)
- `POST /api/v1/vouches` — upsert vouch (one per attestor/subject/dimension)
- `DELETE /api/v1/vouches/:id` — withdraw (soft-delete via withdrawn_at, attestor-only)
- `GET /api/v1/my/vouches` — list of authenticated user's active vouches
- `GET /api/v1/trust/:userId` extended with Layer 2 dimension scores, public endorsements, Layer 4 relational data (viewer's followed/subscribed intersected with public endorsements), viewer's existing vouches
- TrustProfile component (dimension bars, endorsements, Layer 4 "your network says")
- VouchModal (dimension checkboxes, visibility radio, aggregate disclaimer)
- Vouch button on writer profiles, VouchList on /network?tab=vouches with withdrawal

Build Phase 3 (reader-side product work) — **superseded by `docs/adr/REDESIGN-SCOPE.md` + `docs/adr/ALLHAUS-REDESIGN-SPEC.md`:**

- The four-panel workspace originally specified here was retired in favour of a single-surface product model
- Frontend work is now `docs/adr/REDESIGN-SCOPE.md` Phase A — ten incremental changes to the existing codebase
- `docs/adr/ALLHAUS-REDESIGN-SPEC.md` specifies the four core surfaces: topbar, feed, compose overlay, card family
- Redesign Steps 1–3 shipped (2026-04-17): the swap, compose overlay (note mode), card chassis refactor
- Redesign Step 3½ shipped (2026-04-18): article tiers (lead/standard/brief) with two-up brief pairing and 72px zone-break before contiguous brief runs
- Redesign Steps 3¾–5 shipped (2026-04-18): reading-history resumption, playscript thread rendering, compose overlay article mode
- Redesign Step 6 shipped (2026-04-18): end-of-feed, zero-state, filtered-empty, and error states
- Remaining from the spec: focus-preference nav filtering, mobile compose gestures, filter bar, dark mode

Build Phase 4 (epoch aggregation and decay) — **DONE (2026-04-17):**

- Migration 067 (`trust_epochs` table for audit trail, `last_reaffirmed_at` + `epochs_since_reaffirm` columns on vouches)
- `feed-ingest/src/lib/trust-weighting.ts` — Phase A attestor weight formula: `age × payment × readership × activity` (caps: 365d, 50 readers, 10 articles, verified payment)
- `feed-ingest/src/lib/trust-aggregation.ts` — freshness decay (6-epoch table), graduated small-scale protection (paused 1–3, quarter 4–6, half 7–9, full 10+), normalised [0,1] scoring
- `feed-ingest/src/tasks/trust-epoch-aggregate.ts` — quarterly full epochs (increment decay, score all subjects) + Mon/Thu mop-ups (small-subject rule: <10 always; threshold gate: ≥5 changes since last run). Dry-run mode (`TRUST_DRY_RUN=1`). Monitoring: profiles recomputed, largest delta, anomaly count
- 42 unit tests (trust-weighting + trust-aggregation)
- Gateway: `GET /trust/:userId` prefers epoch scores from `trust_profiles`, falls back to live vouch counts only pre-first-epoch
- Frontend: `TrustProfile` dimension bars use real epoch scores when available, count-based proxy as fallback
- Vouch upsert resets decay counters (`epochs_since_reaffirm = 0`, `last_reaffirmed_at = now()`) on reaffirmation

Build Phase 5 (graph analysis hardening) — outstanding:

- Sybil detection, diversity weighting, cluster discounting (Layer 3)
- Weak at small scale — meaningful once graph densifies past ~500 attestors

Build Phase 6 (anonymity Phase B) — future, gated on scale:

- Standalone attestation-service (port 3005, separate Docker network, separate DB user)
- Blind-signature registration (RSA, RFC 9474), NIP-44 encrypted attestation submission
- Client-side anonymous Nostr key generation + IndexedDB storage + BIP-39 seed phrase
- Credibility brackets, periodic re-keying, week-long prompt batching with jitter
- private_graph PostgreSQL schema

Build Phase 7 (mobile responsive) — **superseded by `docs/adr/REDESIGN-SCOPE.md`:**

- Single-column responsive view on narrow viewports, not a panel-swipe workspace
- Compose surface on mobile needs its own design pass (see `docs/adr/REDESIGN-SCOPE.md` Q7)

Build Phase 8 (external content rendering) — future, gated on legal review:

- Server-side readability extraction (Mozilla Readability / Mercury Parser)
- Per-user cache with short TTL, exclusion list, robots.txt respect
- DMCA response process needed before shipping
- Link-out fallback card for external content works without readability extraction

Known gaps (identified during earlier build phases, some now resolved by `docs/adr/REDESIGN-SCOPE.md`):

- ~~Transition path from current layout to workspace~~ — dissolved (no workspace to transition to)
- Layer 4 "valued set" includes "pin as sources" (localStorage) and "quote-post approvingly" (undefined signal) — hand-waved
- Layer 1 signals for external authors (follower count, account age) not stored in external_sources/external_items — feed-ingest adapters would need to persist these
- ~~Current nav features not accounted for in spec's three-tab topbar~~ — dissolved (avatar dropdown keeps existing nav)
- ~~Dual rendering for article page (standalone + panel-embedded)~~ — dissolved (articles stay on their own route)

**UI prototype — `provenance-ikb.jsx`**

Design prototype for Traffology piece view with IKB op-art bars. Kept as reference; the production implementation is in `web/src/app/traffology/`.

---

## 5. Missing Table-Stakes UI

Features any user would reasonably expect given the platform's existing capabilities. Neither backend nor frontend exists for these. Audited 2026-04-13.

### Account lifecycle

~~Account deletion / deactivation~~ — **done (v5.30.0):** Migration 049, `POST /auth/deactivate` (reversible) + `POST /auth/delete-account` (cancels subs, soft-deletes articles with kind-5 events, hard-deletes account). DangerZone component on /account page with confirm dialog (deactivate) and type-to-confirm modal (delete).

~~Change email address~~ — **done (v5.30.0):** `POST /auth/change-email` stores pending email + sends verification link, `POST /auth/verify-email-change` swaps email on confirmation. EmailChange component on /account page with inline editing pattern.

~~Change username~~ — **done (v5.30.0):** `POST /auth/change-username` with format validation, availability check, 30-day cooldown, 90-day redirect from old username. `GET /auth/check-username/:username` for debounced availability. UsernameChange component on /profile page replaces read-only display.

### Publication management

~~Publication logo / avatar upload~~ — **done (v5.31.0):** Image upload in PublicationSettingsTab above name field. Reuses profile avatar upload pattern with uploadImage + pubApi.update for logo_blossom_url. Upload/remove controls.

~~Publication layout template picker~~ — **done (v5.31.0):** Migration 050 adds `homepage_layout` column. 3-card picker (blog/magazine/minimal) with wireframe illustrations in PublicationSettingsTab. Saves immediately on click. Gateway updated to include homepage_layout in all publication queries.

~~Publication delete safeguards~~ — **done (v5.31.0):** Danger zone in PublicationSettingsTab with archive (confirm), transfer ownership (EiC modal), and delete (type-to-confirm). Covered by the archive/delete feature above.

~~Leave publication~~ — **done (v5.31.0):** `POST /publications/:id/leave` endpoint (self-remove, non-owner only, notifies managers). "Leave this publication" text link in MembersTab for non-owner members. Confirm dialog, redirects to personal dashboard.

### Reader & subscriber experience

~~Cancel subscription button~~ — **already existed:** SubscriptionsSection had Cancel button with `handleCancel` calling `DELETE /subscriptions/:writerId`. Verified working.

~~Notification preferences~~ — **done (v5.29.0):** Migration 046 (notification_preferences table), GET/PUT endpoints for 7 categories, NotificationPreferences component on /social page with On/Off toggles using FeedDial pattern, saves immediately on click.

~~Publication follow button on pub pages~~ — **done (v5.29.0):** PubFollowButton component with Follow/Following/Unfollow states (hover to reveal Unfollow), auth redirect for logged-out users, wired into publication homepage masthead.

### Writer tools

**Subscriber / follower dashboard metrics** — writers see earnings but have no view of subscriber growth, churn, or follower trends over time. `GET /subscribers` returns the raw list but there's no dashboard visualisation.

~~Note deletion from profile~~ — **already existed:** NoteCard has Delete button with confirm pattern, SocialTab passes `onDeleted` callback. Verified working.

### Social & safety

**Session management** — `POST /auth/logout` invalidates all sessions. There's no way to see active sessions or revoke a specific one (e.g. left logged in on a shared machine).

**Conversation management** — no way to leave, archive, mute, or delete a message conversation.

**Report feedback to reporter** — users can submit reports and admins can resolve them, but the reporter is never notified of the outcome.

### Discovery & distribution

~~RSS discovery links~~ — **done (v5.30.0):** `generateMetadata` with `<link rel="alternate" type="application/rss+xml">` on writer profile and publication homepage. Visible "RSS" text links in writer stats line and pub masthead.

---

## Suggested attack order

### Completed (v5.12.0 session, 2026-04-06)

- ~~Gift link frontend polish~~ — dashboard GiftLinksPanel + ShareButton integration
- ~~Commission social features~~ — commission from DM threads, migration 036
- ~~DM pricing configuration~~ — API endpoints + dashboard settings UI
- ~~JWT lifetime reduction~~ — 2-hour lifetime with 1-hour refresh

### Completed (v5.13.0 session, 2026-04-06)

- ~~Subscription offers system~~ — migration 037, backend routes, dashboard Offers tab, redeem page, offer-aware renewal
- ~~Editor bug fixes~~ — stale closure in auto-save, price auto-suggestion overwrite, grey-card styling refresh

### Completed (v5.14.0 session, 2026-04-06)

- ~~Settings rationalisation~~ — Profile absorbs payment/Stripe/export, Account gains free reads toggle, new Social page (feed dial, blocks/mutes, DM fees), dashboard tab settings→pricing, `/settings` and `/history` replaced with redirects, new block/mute CRUD APIs, nav updated

### Completed (v5.16.0 session, 2026-04-06)

- ~~Inline subscription management on Following/Followers tabs~~ — Following tab (own profile): unfollow button + subscribe/unsubscribe/resubscribe per writer, confirmation modal with period-end date. Followers tab (own profile): "Subscriber" badge. Backend enriched following response with `subscriptionPricePence`/`hasPaywalledArticle`, followers response with `subscriptionStatus` (owner-only).
- ~~Editor hairline cleanup~~ — title + standfirst wrapped in single grey card, toolbar changed to white, inter-field gaps removed
- ~~Missing API client methods~~ — `social.block()` and `social.mute()` POST wrappers added to match backend endpoints

### Completed (v5.18.0–v5.19.0 sessions, 2026-04-06)

- ~~Publications Phases 1–3~~ — schema, core model, key-custody signerType, member management, CMS pipeline, server-side publishing, editor integration, dashboard context switcher, invite page, reader surface (homepage/about/masthead/subscribe/archive/article pages), publication subscriptions/follows, RSS, search, feed integration, article page publication awareness, writer profile filtering.

### Completed (v5.20.0 session, 2026-04-06)

- ~~Publications Phase 5 (revenue)~~ — rate card routes, payroll routes (standing + per-article), publication payout worker, earnings routes, RateCardTab + PayrollTab + PublicationEarningsTab dashboard components, `can_manage_finances` gating throughout

### Completed (v5.29.0 session, 2026-04-13)

- ~~Unpublish personal article~~ — `POST /articles/:id/unpublish` endpoint + dashboard Unpublish button
- ~~Publication follow button~~ — PubFollowButton component on publication homepage masthead
- ~~Notification preferences~~ — migration 046, GET/PUT endpoints, NotificationPreferences on /social page
- ~~Bookmarks~~ — migration 047, full gateway routes, BookmarkButton, /bookmarks page, feed integration
- ~~Tags/topics~~ — migration 048, full gateway routes, TagInput in editor, TagDisplay on cards, /tag/[tag] page

### Completed (v5.30.0 session, 2026-04-13)

- ~~Subscriber list~~ — SubscribersTab with summary stats + table, conditional writer-only dashboard tab
- ~~Account deletion / deactivation~~ — migration 049, deactivate + delete routes, DangerZone component with type-to-confirm modal
- ~~Change email~~ — change-email + verify-email-change routes, EmailChange component on /account
- ~~Change username~~ — change-username + check-username routes, UsernameChange component on /profile with debounced availability + 30-day cooldown
- ~~RSS discovery links~~ — generateMetadata with RSS alternate link + visible RSS links on writer profile and pub homepage

### Completed (v5.31.0 session, 2026-04-13)

- ~~Delete / archive publication~~ — danger zone in pub settings: archive (confirm), transfer ownership (EiC modal), delete (type-to-confirm)
- ~~Transfer publication ownership~~ — modal with eligible EiC member selector
- ~~Edit member role~~ — inline dropdown in MembersTab with Save/Cancel
- ~~Publication logo upload~~ — image upload in pub settings (reuses profile avatar pattern)
- ~~Layout template picker~~ — migration 050, 3-card grid in pub settings (blog/magazine/minimal), saves immediately
- ~~Leave publication~~ — POST /publications/:id/leave endpoint + text link in MembersTab for non-owner members

### Completed (v5.32.0 session, 2026-04-13)

- ~~OG metadata~~ — Open Graph + Twitter Card tags on all public pages: root layout (fallback defaults + metadataBase), homepage, about (split to server component), writer profiles (avatar image, bio description), publication homepage/about/masthead, tag browse (split to server component). Article pages already had OG tags. Fixed publication page snake_case field name for logo URL.

### Completed (v5.33.0 session, 2026-04-13)

- ~~Reading history~~ — ReadingHistory component on /account page with paginated article list (title, writer, date, Free/Paid label)
- ~~Accept/decline commission~~ — `GET /my/commissions` endpoint, CommissionsTab in dashboard (writer-only), Accept + Decline with confirm
- ~~Edit existing drive~~ — inline edit mode on DriveCard (title, description, target amount), uses existing PUT endpoint
- ~~Cancel subscription button~~ — verified already existed in SubscriptionsSection
- ~~Note deletion from profile~~ — verified already existed in NoteCard
- ~~Pin drive to profile~~ — verified already existed in DriveCard

### Completed (v5.34.0 session, 2026-04-13)

- **Page restructure** — `/account` → `/ledger` (financial ledger), `/settings` (email, payment, notifications, export, danger zone), `/library` (bookmarks + reading history tabs), `/network` (following, followers, blocked, muted + feed dial + DM fees). `/profile` slimmed to public identity only. Old URLs redirect to new locations.
- **Dashboard consolidation** — 7 tabs → 4 (Articles, Subscribers, Proposals, Pricing). Drafts merged into Articles tab as unified content list. Commissions + Pledge drives + Offers consolidated into single Proposals tab with filter bar. Backwards-compatible tab aliases for deep-linked URLs.
- **Article scheduling** — migration 051 (`scheduled_at` column on `article_drafts`). Gateway schedule/unschedule endpoints. Background scheduler worker (60s poll, advisory lock, `FOR UPDATE SKIP LOCKED`). Full pipeline: publication articles via `publishToPublication()`, personal articles via key-custody signing + vault encryption for paywalled. Dashboard schedule/reschedule/unschedule actions with datetime picker. Editor "Schedule" button alongside Publish.
- **Editor layout cleanup** — tags and publication selector moved from above editor to below content area.

### Completed (v5.35.0 session, 2026-04-13)

- **Universal Feed Phase 1** — RSS ingestion + universal resolver + external items in feed. Migration 052 (external_sources, external_subscriptions, external_items). New `feed-ingest` Graphile Worker service (poll, RSS fetch, prune, metadata refresh). Universal resolver (`gateway/src/lib/resolver.ts`) with URL/RSS discovery, npub/nprofile, NIP-05, platform username, free-text search. Feed route extended to three-stream merge (articles + notes + external items with daily cap). ExternalCard component with provenance badges. SubscribeInput omnivorous input. `/subscriptions` management page. Publication invite migrated from email-only to resolver-backed. SSRF-hardened HTTP client in shared/. See `docs/adr/UNIVERSAL-FEED-ADR.md` for full spec (Phases 2–5 outstanding).

### Completed (session, 2026-04-15)

- **Gateway decomposition — messages service layer** — `routes/messages.ts` refactored from 693 lines into `routes/messages.ts` (202 lines, thin dispatchers) + `services/messages.ts` (563 lines, business logic). `ServiceResult<T>` discriminated union returns `{ ok, status, error }` so route handlers map errors to HTTP statuses without throws. All 13 DM endpoints covered; `ServiceResult`/`loadConversationMessages`/`sendMessage`/`decryptBatch` types exported for future reuse. Per `docs/adr/GATEWAY-DECOMPOSITION.md`, this is step 2 (service-layer refactor) ahead of step 3 (cross-process extraction) — de-risks the eventual messaging-service cutover. Also extracted feed scorer to feed-ingest Graphile cron (commit `d82833e`).

### Completed (v5.36.0 session, 2026-04-14)

- **Universal Feed Phase 2** — `feed_items` unified timeline table + external Nostr ingestion. Migrations 053–054 (feed_items table + backfill from articles/notes/external_items). Feed route rewritten from three-stream merge to single-table query with LEFT JOINs. Transactional dual-write paths in all article/note/external item creation flows. Feed scorer updated to write `feed_items.score` directly. Article soft-delete and unpublish set `feed_items.deleted_at`. New `feed_ingest_nostr` task for external Nostr relay ingestion (kinds 1, 30023, kind 5 deletions). Poll task routes `nostr_external` sources with relay-hostname rate limiting. `publishToExternalRelays()` helper for outbound Nostr replies. Daily `feed_items_reconcile` (05:00 UTC) and `feed_items_author_refresh` (04:00 UTC) cron jobs. POST /notes accepts optional `signedEvent` for outbound relay publishing. See `docs/adr/UNIVERSAL-FEED-ADR.md` (Phases 3–5 outstanding).

### Completed (2026-04-17)

- **Trust graph Build Phase 1 (Layer 1 enrichment)** — migration 065 (`trust_layer1` table), `trust-layer1-refresh` daily cron in feed-ingest, `GET /api/v1/trust/:userId` gateway endpoint, trust pip (5px circle, three-state: known/partial/unknown) on ArticleCard, NoteCard, ExternalCard. `PipStatus` type in `ndk.ts`, `.trust-pip` CSS class. `pipStatus` flows through feed API → FeedView mapping → card components. NIP-05 verification stubbed (no table yet). See `docs/adr/ALLHAUS-OMNIBUS.md` Build Phase 1.
- **Trust graph Build Phase 2 (vouching CRUD)** — migration 066 (`vouches` + `trust_profiles` tables with CHECK constraints). Gateway: `POST /vouches` (upsert per attestor/subject/dimension), `DELETE /vouches/:id` (soft-delete withdrawal), `GET /my/vouches` (own vouch list), `GET /trust/:userId` extended with Layer 2 dimension scores, public endorsements with attestor profiles, Layer 4 relational data (viewer's network intersection), viewer's existing vouches. Frontend: `TrustProfile` component (dimension bars, endorsements, "your network says"), `VouchModal` (dimension checkboxes, visibility radio, aggregate disclaimer), vouch button on writer profile action bar, `VouchList` on `/network?tab=vouches` with withdrawal. See `docs/adr/ALLHAUS-OMNIBUS.md` Build Phase 2.
- **Trust graph Build Phase 4 (epoch aggregation and decay)** — migration 067 (`trust_epochs` + vouch decay columns). Pure library functions: attestor weighting (Phase A formula), aggregation (freshness, decay, scoring). Cron task: quarterly full epochs + Mon/Thu mop-ups with small-subject rule and threshold gate. Dry-run mode. 42 unit tests. Gateway prefers epoch scores; frontend dimension bars use real scores. See `docs/adr/ALLHAUS-OMNIBUS.md` §II.8 + Build Phase 4.

### Completed (2026-04-17, redesign steps 1–3)

- **Redesign Step 1 (the swap)** — Nav reduced from `Feed | Write | Dashboard | Network` to `Feed | Dashboard`. `COMPOSE ⌘K` button added to topbar (text on desktop, ∀ mark on mobile). Global `⌘K`/`Ctrl+K` hotkey opens compose overlay. Canvas mode ∀ mark changed from white to crimson. Sticky NoteComposer in FeedView replaced with sticky SubscribeInput (transparent bg, slab-rule border, shorter placeholder). See `docs/adr/ALLHAUS-REDESIGN-SPEC.md` §1–2.
- **Redesign Step 2 (compose overlay, note mode)** — New `ComposeOverlay` component mounted globally in layout. Three-zone slab: top zone (mode label or reply preview), editing zone (auto-grow textarea, Jost 16px), controls zone (image upload, cross-post pills per linked account, character counter, Post button). Reply mode with pinned quote preview. Double-escape dismiss with confirmation. Mobile full-screen sheet variant. Zustand store (`stores/compose.ts`) coordinates overlay state. QuoteSelector and WriterActivity updated to use overlay instead of inline NoteComposer modals. See `docs/adr/ALLHAUS-REDESIGN-SPEC.md` §3.
- **Redesign Step 3 (card chassis refactor)** — All three feed card types unified around shared visual grammar: 4px left bar (black for native, crimson for paid, grey-300 for external), mono-caps 11px byline row (TrustPip · Author · Timestamp), mono-caps action row. NoteCard: avatar removed, byline changed from sans 14px to mono-caps 11px, left bar added, Reply routes through overlay. ExternalCard: avatar removed, byline unified, provenance badge moved into byline row (grey-400, was crimson), inline ExternalReplyComposer removed, Reply routes through overlay. ArticleCard: bar narrowed 6px→4px, padding 28px→24px, Reply button added. Feed vertical rhythm unified to 40px gap via `space-y-[40px]`. See `docs/adr/ALLHAUS-REDESIGN-SPEC.md` §4.

### Later: strategic work (priority order, updated 2026-05-25)

1. **CI/CD + backend linting** — zero infrastructure, Tier 1a priority. See `docs/adr/CODE-QUALITY.md`
2. **Email-on-publish** — trivial build, high writer-retention value. See `docs/adr/EMAIL-ON-PUBLISH-SPEC.md`
3. **Owner dashboard** — entirely unbuilt, need operator visibility before real money. See `docs/adr/OWNER-DASHBOARD-SPEC.md`
4. ~~**Card behaviour unification**~~ — **Phases 1–4 complete (2026-05-30).** Region map, `is_reply`, biddability, provenance, source attribution, neighbourhood expansion (native + external), author affordances, and all five spec-conformance gaps. Only §VI.3 constructed external author profile remains (deferred to its own ADR). See `docs/adr/CARD-BEHAVIOUR-ADR.md`
5. **Cross-source identity linking** — dedup multi-protocol follows. See `FEED-INGEST-ATTACK-PLAN.md` Slice 8
6. **Subscription Phase 2** — free trials, gift subs, welcome email, import/export, analytics, custom landing page. See `docs/audits/SUBSCRIPTIONS-GAP-ANALYSIS.md`
7. **Workspace experiment decision** — merge to master or retire? 34 slices on branch, never browser-tested
8. **ActivityPub inbox** — real-time AP, edit/delete propagation. See `FEED-INGEST-ATTACK-PLAN.md` Slice 7. Posture-gated
9. **Reposts** — needs feed algorithm to be meaningful
10. **Currency strategy** — see `docs/adr/platform-pub-currency-strategy.md`
11. **Publications Phase 4** (theming/custom domains) — see `docs/adr/PUBLICATIONS-SPEC.md` §10 Phase 4
12. **Bucket system** — see `docs/adr/platform-bucket-system-design.md`
13. **Audit report remaining items** — see `docs/audits/AUDIT-REPORT.md`
14. ~~OG metadata~~ — **done (v5.32.0)**

### Completed (v5.28.0 session, 2026-04-12)

- ~~Writer analytics~~ — Traffology Phase 1: page tracking, ingest service, aggregation pipeline, source resolution, observation engine, feed UI, piece detail, overview
- ~~Stripe idempotency keys~~ — all mutating Stripe calls (paymentIntents.create, transfers.create) now include idempotencyKey
- ~~Webhook event deduplication~~ — stripe_webhook_events table prevents reprocessing
- ~~oEmbed fetch timeout~~ — 5-second AbortSignal.timeout added
- ~~Template HTML escaping~~ — all Traffology template values escaped
- ~~Key service pubkey validation~~ — 64-char hex format check
- ~~subscription_events FK fix~~ — ON DELETE CASCADE added via migration 041
- ~~schema.sql sync~~ — all ON DELETE clauses from migrations 018/021/041 applied

### Infrastructure (fit in as time allows)

- CI/CD pipeline
- Standardise gateway error response shapes — 222 error responses across 24 route files use 4 different shapes (`{ error: string }`, `{ error: { code, message } }`, `{ error: ZodFlattenedError }`, `{ error: string, message: string }`). `gateway/src/lib/errors.ts` has an unused `sendError` helper ready to adopt. Mechanical refactor, no runtime bugs.
- TypeScript strictness (eliminate remaining ~23 `any` instances)
- Accessibility pass
- TypeScript target alignment
- **Session invalidation on logout** — `sessions_invalidated_at` provides server-side revocation (all-devices logout), but there's no per-session revocation (e.g. "log out of that device"). The `requireAuth` hook enforces `sessions_invalidated_at` on every request.
- **CSP nonce middleware** — `nginx.conf` CSP uses `'unsafe-inline'` for `script-src`, which undermines XSS protection. Removing it requires Next.js middleware to generate per-request nonces and inject them into both the CSP header and inline `<script>` tags. Needs careful testing to avoid breaking hydration. Flagged in v5.28.0 audit.
