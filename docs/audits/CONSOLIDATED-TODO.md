# platform-pub — Consolidated To-Do (the canonical work queue)

**This file is the single forward-looking work queue.** `docs/audits/FIX-PROGRAMME.md` is the work log (what shipped, with validation records); this file is what's next. When work ships: log it in FIX-PROGRAMME, delete or amend the item here. `feature-debt.md` — which previously carried both roles — was retired to `planning-archive/feature-debt.md` on 2026-07-07 (its work-log entries duplicated FIX-PROGRAMME; its open items are absorbed below; everything else was DONE-graveyard).

Compiled 2026-07-06 from every non-archived doc at `22419d0`. **Re-verified and updated 2026-07-07 against `2ccfa35`** — the same-day Wave-2 (F9/F5/F4/F10/F14 + Wave 5, migrations 142–144) and Wave-3 (migrations 146–147) economy remediation, migration 145, and the external-author-history ship (migration 148) landed in between and resolved most of the original §1. Every remaining claim below was re-verified in source (file:line where it matters), not just against the docs. **Updated 2026-07-09 with §0** — findings from the four-agent audit of the July 7–8 commits (`3b2d51d`…`515a13b`) — **and §0b**, the resolver & source-input audit (`RESOLVER-SOURCE-INPUT-AUDIT-2026-07-09.md`). **Updated 2026-07-12 with §0c**, the same-day audit of the follow-graph-import ship. **Updated 2026-07-16 with §0e** — the seven-agent full-repo deep audit (`docs/audits/DEEP-AUDIT-2026-07-16.md`): 1 CRITICAL + 14 HIGH + 25 MEDIUM + 18 LOW, none previously tracked, CRITICAL/HIGH re-verified in source. **Updated 2026-07-19 with §0f** — the five-agent commit audit of the Jul 16–19 window (the §0e fix batch itself, the 07-17 verification sweeps, the thread-hydration slices, Explain captions/copy, feed colour/density): 1 HIGH + 5 MEDIUM + assorted LOW, every finding re-verified in source by its finder. **Reconciled 2026-07-25 against every non-archived ADR/audit** (the §0m fix batch and the two post-window landing-copy commits `c714b95`/`3b080d5` are folded in): the CLOSED-BETA-ADR ship (Phases 1–3, built locally 2026-07-24, **not yet deployed**) drove the updates to §3.2 (landing rebuilt readers-first), the new §2.6 (waitlist privacy counsel), §3.7 (cohort-admission tooling), and the §11 closed-beta deploy + browser-verify item. No other non-archived doc had drifted from the queue. **Updated 2026-07-26 with the logged-out register sweep** (`docs/adr/LOGGED-OUT-REGISTER-ADR.md`, commits `34eb21b`…`335c363`): closed §0m.2, rewrote §3.2's stale landing description, added §0m.7–10 (the `/pub/*` register decision + its two real bugs, the three missing legal routes, `/about`'s fit, `/write`'s skeletons) and the §11 both-modes browser-verify. **Updated 2026-07-30 with §0n** — the four-reviewer commit review of the Jul 27–30 window: the act-now findings, the flag-flip blockers and the hygiene batch all fixed same-day (FIX-PROGRAMME 2026-07-30); §0n holds the remaining small tail. **Updated 2026-08-07 with the onboarding ship**: §3.3 closed (the first-session welcome, migration 176) and with it the last §3 item that had no partial work; §3.6's Phase-3 sheet absorbed and deleted; §0l item 1 amended (its repair no longer silently switches off a first-session surface — the coupling is severed); a new §11 pending-deploy entry covering the six-service batch; and a **NEXT SESSION STARTS HERE** block at the foot of the attack order. **Updated 2026-08-03 with §0o** — the four-auditor commit audit of the Jul 29–Aug 01 window (funds-segregation core, settlement + sandbox sequence, landing): 2 HIGH + 4 MEDIUM + a LOW/NOTE tail, none previously tracked; both HIGHs re-verified in source, one confirmed live in dev (the idempotency-wedged settlement row).

Organised by domain, priority-ordered within each. Source doc cited per item. §10 lists what's deliberately parked so it doesn't leak back into the working list.

---

## 0. 2026-07-09 commit audit — July 7–8 ships

Four-agent adversarial review of commits `3b2d51d`…`515a13b` (external-author-history Phases 1–4, paywall hardening, draft dedup + migration 149, Blossom cutover, Stripe webhook route). The paywall commit (`339b43e`) and the external-author-history cluster verified **clean** — money paths, validator lockstep, Step-1b ordering, promotion/shadow-source/job-key/SSRF/relay-free invariants all hold. **Items 1–7 (3 HIGH, 2 MEDIUM, 2 LOW) all FIXED 2026-07-09 — see the FIX-PROGRAMME 2026-07-09 entry** (nginx `/media/` locked to hash-shaped GET/HEAD; `scheduled_at IS NULL` on the draft guess; drive fulfilment moved into the index txn + onto the final index call only; scheduler fulfilment failures now block the draft delete; cleanup-script `published_at IS NOT NULL` predicate; paywalled publish email deferred to step 5 via `emailAsNew`; Blossom rollback docs corrected). The nginx fix needs the prod force-recreate (see §11). Remaining:

1. **Cosmetic tail (batch with §7):** atproto/AP promotion overwrites a hydrated `feed_items.author_name` with the null fallback pre-enrichment (`feed-ingest/src/lib/atproto-ingest.ts:129-141`; byline unaffected — reads `external_items` first — and enrichment self-heals); unguarded `post.record?.reply?.parent.uri` aborts one hydration pass on a malformed AppView entry (`gateway/src/lib/author-timeline-hydration.ts:274`); `stampGuard` evicts oldest-inserted, not expired-first (`author-timeline-hydration.ts:84-90`); `autoSaver.cancel()` clears only the debounce timer — the editor stays typeable during the multi-second publish, so a mid-publish keystroke can re-arm an autosave that lands after the draft delete and re-mints an orphan (narrow window, same bug class d34f9c0 fixes); the approve path never disposes of the kept in-review draft (draft+article shadow persists for approved submissions — decide scope or fix); stale "source_item_uri bakes in relay_urls" comment in `feed-ingest/src/lib/nostr-ingest.ts` (~378, false since migration 101); `PaywallGate` copy conservatively wrong iff `FREE_ALLOWANCE_FLOOR_PENCE` ≠ 0 (default correct); PATCH `/articles/:id` (`gateway/src/routes/articles/manage.ts:103`) still returns a raw `flatten()` as `error` (part of the §7.17/§8.9 error-shape batch).

## 0b. 2026-07-09 resolver & source-input audit

Three-agent audit of the universal resolver, the §V.5.8 discovery-fallback suggestion feature, the identity-input surfaces, and the `addSource` → feed-ingest path. **Full findings + fix shapes: `docs/audits/RESOLVER-SOURCE-INPUT-AUDIT-2026-07-09.md`** (also records what was verified sound — SSRF, fail-soft isolation, classification, async plumbing — so it needn't be re-audited). Headline: discovery branches 1–3 (catalog/Bluesky/NIP-50) shipped end-to-end with all guardrails intact; branch 4 (web-search bridge) unbuilt; catalog is 11 entries. Open items, in the doc's attack order:

1. ~~**F2 (HIGH):** atproto backfill outer catch is a silent no-op~~ — **DONE 2026-07-10** (nostr-model error accounting + re-throw in the backfill's outer catch, page-0 HTTP failure routed through it; `externalFetchMaxAttempts` gives atproto 5 subscribe-time attempts since graphile retry is its only retry path; FIX-PROGRAMME 2026-07-10).
2. ~~**F3 (MEDIUM):** match `confidence` never rendered~~ — **DONE 2026-07-10** (discovery-expansion Phase 3: Matches/Suggestions split in all three `useResolverInput` surfaces via `partitionMatchOptions`; FIX-PROGRAMME 2026-07-10).
3. ~~**F4 (MEDIUM):** omnivorous-input rule violations on DM new-conversation / DM fee override / publication invite~~ — **DONE 2026-07-10** (all three onto `useResolverInput` with picked-match actions, inline errors, and the MembersTab stale race closed via the hook's `genRef`; `MatchOption` gains the additive `account` identity for person-picking surfaces; FIX-PROGRAMME 2026-07-10).
4. ~~**F1 (HIGH, larger):** `addSource` validates syntax only~~ — **DONE 2026-07-10** (fix shape (b): `source-liveness.ts::verifySourceLiveness` normalises acct/handle/npub to canonical form and probes liveness per protocol pre-transaction, behind the `SOURCE_LIVENESS_ENFORCED` brake; error space split 400 `invalid_source_uri` / 422 `source_unreachable`; probe metadata backfills display fields; FeedComposer/VesselBar render the verdict; FIX-PROGRAMME 2026-07-10).
5. **F5 (batch):** resolver spec drift — no native-URL short-circuit, no njump re-entry, no generic AP actor probe on arbitrary URLs, regex-over-HTML `extractFeedLink` missing JSON Feed autodiscovery. (F5.4 per-context priority ordering DONE 2026-07-10 — discovery-expansion Phase 3 `mergeMatches` §6.2.)
6. **F6 (product call):** deactivated sources go silent with no user signal or re-verification path. (F7 discovery-reach is **DONE 2026-07-10** — see next item.)
7. ~~**F8:** resolver orchestration/network-path tests are zero~~ — **DONE 2026-07-10** (discovery-expansion Phase 0: `gateway/tests/resolver-orchestration.test.ts`, 15 tests + `nostr-search.ts` extraction + `safeChain` structural chain isolation; FIX-PROGRAMME 2026-07-10). The §8.5 `resolver.ts` decomposition now has its safety net.
8. **Post-ship review residue (2026-07-10 five-agent review of the shipped discovery expansion; the six bugs it found are FIXED — FIX-PROGRAMME 2026-07-10 "post-ship review" — these are the declined/deferred remainder):**
   - **Min-length-3 parity:** Bluesky + Nostr discovery branches have no query-length floor (known-world + AP have 3); a 1-char input classifies `free_text` and fans out to Bluesky and a search relay on submit. Same "noise" rationale, unevenly applied — add the floor at the two branch heads.
   - **DM/invite silent blank on external identifiers:** npub/nprofile/hex Phase A pushes an `exact` external match even under `skipExternal`; the client filters to `.account` rows but computes the "No one found" hint off the *unfiltered* list, so a non-member npub renders nothing forever. Compute the hint off the filtered list (or filter server-side, which would also make §8.5's "invite/dm never see external candidates" literally true on the wire).
   - **Branch-4 build trigger is uninstrumented:** the web-search bridge is deferred behind "measured zero-result rate", but nothing counts discovery submits returning zero external candidates — the trigger can never fire. One counter/log line in `resolveAsync`'s discovery block.
   - **Matches/Suggestions split is visual-only (a11y):** no `role="group"`/`aria-labelledby` association on the section headers in FeedComposer/VesselBar/IdentityLinkControl — a screen reader can't distinguish a verified Match from a speculative Suggestion, the exact information §6.4 exists to convey. (Adjacent pre-existing: no arrow-key navigation; VesselBar rows keyboard-unreachable via the 150ms blur-close.)
   - **atproto author-deletion signal** (§8.3 residual): Jetstream doesn't subscribe account events and `getProfile` error strings are brittle — decide the signal (Jetstream `account` events vs backfill error classification) and stamp `external_authors.deleted_at` for atproto too.
   - **Profile surfaces × `deleted_at`** (product call): `/author/:id/profile` and the author timeline still resolve tombstoned authors (historical bylines depend on it). Decide whether deletion should also gate those surfaces or only discovery.
   - **Untested claimed throttles:** `ap-account-search`'s per-instance concurrency-1 and memo TTL/LRU eviction have no tests (fake timers would pin both cheaply) — a regression to unbounded fan-out against a Mastodon instance passes the current suite.
   - Small: known-world winner discards a losing twin's richer metadata (field-level fold in `mergeMatches`); catalog gen could prefer https-upgraded feed URLs (66 plain-http entries) and map curly→straight apostrophes in `normalizeAlias`; generated-file CC0 banner overclaims (descriptions can be probed publisher copy); `discoveryInstances()` doesn't strip an accidental scheme from env values; VesselBar `SectionHeader` hand-rolls `text-[10px]` (won't scale with the type-size control); §6.4's "partition once in `resolveMatches`" sentence is stale (it's `partitionMatchOptions`).
9. **Discovery expansion (design accepted 2026-07-10; ALL BUILT PHASES 0–4 SHIPPED 2026-07-10): `docs/adr/RESOLVER-DISCOVERY-ADR.md`.** Phase 1 (known-world pg_trgm index over `external_authors`/`external_sources`, migration 150, `searchKnownWorld` in Phase A), Phase 2 (`activitypub_discovery` — unauthenticated Mastodon `/api/v2/search` behind the FASP-ready `ApAccountSearchProvider` interface, `MASTODON_DISCOVERY_INSTANCES`; plus the §5.2 addSource acct→webfinger amendment, a down-payment on F1), Phase 3 (bridge-aware `mergeMatches` — helpers relocated to `shared/lib/bridge-identity.ts`, NIP-48 tags retained, §6.2 ordering, Matches/Suggestions rendering; **discharged F3 and F5.4 above**), and Phase 4 (generated catalog — `scripts/gen-discovery-catalog.ts`, Wikidata-sourced + probed-live, `discovery-catalog.generated.ts` 500 entries merged under the curated head with query-side diacritic folding; **discharged F7**) shipped — see FIX-PROGRAMME 2026-07-10. Deferred by design: branch 4 web-search bridge behind `DISCOVERY_WEBSEARCH_ENABLED` (build trigger: measured zero-result rate on the grown catalog), FASP provider (§5.1 adoption bar); Feedly ruled out (API ToS).

## 0c. 2026-07-12 commit audit — follow-graph import ship

Four-agent adversarial review of the day's eleven commits (`212ed39`…`e32ecb7`: follow-import ADR + Phases 0–3, graphile schema excision, healthchecks). The invariants all verified clean (one-way inbound, pure-offer sheet, exclusion symmetry, feed-derived subscriptions, SSRF, migration/schema hygiene, hairlines, promise safety); the two healthcheck/schema commits verified empirically (containers healthy, drift guard exit 0). The four fixable findings (AP/atproto pager ceilings, OPML depth guard, `adoptFeed` impure updater + arrivals-drain race, Sync-now Apply double-fire) were **FIXED same-day — see FIX-PROGRAMME 2026-07-12 tenth entry**. Remaining low-severity residue:

1. **Exclusion asymmetry on move-in:** moving a source *into* an import-bound feed doesn't revoke a standing `feed_import_exclusions` row the way `addSource` re-add does (`gateway/src/routes/feeds/sources.ts` move handler) — membership is the user's evident intent, but the moved-in source stays outside sync's remit. Mirror `clearImportExclusion` on the move-in leg.
2. **Parse-dropped entries aren't counted as `unresolved`:** an entry the pagers silently drop at parse time (AP Account missing `id`, atproto follow missing `did`) is neither in `identities` nor in `unresolved`, so a complete read missing it could propose a false sync removal (needs a spec-violating server + user-confirmed preview). Count parse drops into `unresolved` (AP) / a truncation-equivalent flag (atproto).
3. **No per-account cap on `POST /follow-imports`:** `/sync` has an in-flight guard per feed but the initial-import route has none — repeated POSTs stack runs and mint unbounded feeds (ADR §9.1/§9.4 acknowledges the open feeds-per-user question). An in-flight guard per (account, protocol) is the cheap first step.
4. **Zod 400 shape:** `follow-imports.ts` uses the feeds-module legacy `{error:"Invalid body", details}` rather than the shared `zodValidationError` envelope — batch with the standing §7.17/§8.9 error-shape item.
5. **Frontend polish:** FeedComposer passes a fresh inline `onApplied` arrow so FeedSyncSection's 2s poll interval is torn down/recreated every parent render (starves under typing — memoise the callback); `file.text()` rejection in the OPML picker is silent (`FollowImportSection.tsx` — move it inside the try); the Bring-your-world sheet can supersede an overlay the user opened during the capabilities fetch (re-check presence just before mount); client copy hardcodes the server's `OPML_MAX_FEEDS = 10`; `text-red-600` in FollowImportStatus/FollowImportSection bypasses the colour registry (`danger-red` exists — matches ~15-file pre-existing debt, batch with that sweep).
6. **Informational, no action decided:** `/nginx-health` is world-reachable on :80 (discloses only "ok"); nginx healthcheck verified statically but not exercised in the dev stack (no nginx container in dev).

## 0d. 2026-07-15 commit audit — Jul 12–15 window (payments rework, Explain, follow-import residue)

Five-agent adversarial review of the window's 41 commits across the follow-import, payments-core, payments-idempotency/reconcile, Explain, and misc streams. No high-severity findings; test suites, drift guard, hairline and ledger-adjacency tripwires all green at HEAD. The four worst-and-cheapest findings were **FIXED same-day — see FIX-PROGRAMME 2026-07-15 first entry** (row-stable `pub-split-<payoutId>-<splitId>` idempotency key + collision test; card-shell `onKeyDown` anchor guard; Explain floor-click dismiss; first-run click inertness). Remaining residue, roughly by priority:

1. ~~**One-time prod check for in-flight pledge drives (conditional MEDIUM)**~~ — **DISCHARGED 2026-07-21, condition did not fire.** Measured on prod: **3 drives `open`, 0 `funded`**, and all three carry **zero active pledges, zero `current_total_pence`, zero pledgers, no `draft_id`, no deadline** — manual test rows from Apr/Jul. No money is committed, so the "charges pledgers who can't withdraw" hazard has no subject. Note the guard is *self-limiting* in this direction, which is why this can be closed rather than re-checked: the `onRequest` hook 403s the whole `/drives` plugin including pledge *creation*, so a parked drive cannot acquire pledges, and fulfilment against zero pledgers charges nobody. The asymmetry itself (withdrawal blocked, `matchDriveForPublish`/`fulfillDrive` live) is unchanged and stays correct for the publish invariant — it simply has no live exposure. **If PLEDGES_ENABLED is ever flipped back on, re-open this**: drives created after the flip can take pledges, and these three deadline-less rows would then be armed again. Consider cancelling them as part of any revival.
2. ~~**Explain: card-kind hover/pin anchors to the representative instance, not the hovered one**~~ — **MOOT** (the same-day post-live rework `30c790f`, which landed after this write-up, made hover a cursor bubble with no anchor/leader and deleted click-pin; `elementFor` now serves only the first-run pinned channel, whose representative card anchor is by design, D5). The dormant `pin` seam's comment in `stores/explain.ts` records the instance-key requirement if it's ever revived. Recorded FIX-PROGRAMME 2026-07-15.
3. ~~**Explain: the freeze is pointer-only and there is no SR surface**~~ — **DONE 2026-07-15** (focus-policing keyboard freeze + vessel `aria-hidden` — real `inert` would blind the `elementsFromPoint` hit-test; ⌘K gate; first-run bubble `role="dialog"` + `aria-live` copy + focus-per-beat; cursor bubble `role="status"`; FIX-PROGRAMME 2026-07-15).
4. ~~**Explain smaller residue**~~ — **DONE 2026-07-15**, all seven pieces (ResizeObserver observes the scroll content wrapper; clipped-target leader clauses implemented via `visibleTargetRect`; `localStorage` guarded in `FirstRunController`; About-pane arrow-key guard; bubbles on `text-ui-sm`; `EXPLAIN_KINDS` deleted; the About-button stale hover was already fixed in `3151a85`; FIX-PROGRAMME 2026-07-15). Browser-verify with the next web rebuild (§11 consolidated smoke session).
5. **Payments hygiene:** `payout.ts` `getWriterEarnings` comment still says "(held|released|paid)" vs the corrected query (mig 156); the `reverseSettlement`↔`computeChargebackReversal` skip-filter pairing has no assert; adjacency Guard 2 wouldn't catch an absolute `SET balance_pence = $1`; `vat` trigger type is in no balance view (checklist item for the MoR branch); a terminally-failed publication split has no release/re-pay path (parks the parent `pending` forever with reads already `writer_paid`); the payout-halt gate has zero test coverage despite a test comment claiming otherwise; conservation tests re-implement two SQL predicates in their query routers (and fall through to `{rows:[],rowCount:1}` on unmatched SQL).
6. **Reconcile:** no overlap guard on reconciliation runs (worker vs manual route vs second instance) — benign today (read-only single statements, idempotent halt) but diverges from the repo's advisory-lock habit.
7. **Follow-import residue beyond §0c:** poller host-grouping diverges for atproto DIDs (SQL partitions each DID alone, JS buckets all under one `maxPerHost=2` key — Jetstream-down backfill backlog starves to 2 jobs/tick); Nostr sync can act on a stale kind-3 from a lagging relay with no staleness signal (mitigated by preview; note in ADR); `readActivityPubGraph` can run minutes inside the POST handler pre-persist (needs a budget or async run).
8. **Misc frontend:** `linkifyText` bracket-keeping trims the balancing `)` off `…Foo_(bar).`-style URLs; linkify also runs in truncated collapsed mode contra the 82274a8 log entry (decide intent); the shell's `role="button"` now wraps nested anchors (ARIA nesting); parked-drive notifications deep-link to a tab whose subject is hidden; 82274a8 cites a nonexistent "CONSOLIDATED-TODO §12" in its message + log entry (sections run 0–11; no queue item existed — amend the FIX-PROGRAMME citation).

## 0e. 2026-07-16 full-repo deep audit

Seven-agent adversarial audit of the whole repo at HEAD (`c000beb`), one agent per subsystem (money/payments, gateway core, feed-ingest, feeds/follow/dedup, key services, web frontend, schema/shared/infra); all listed findings filtered against this file + FIX-PROGRAMME (nothing here is already-tracked), and every CRITICAL/HIGH re-verified in source by the orchestrator — four empirically against the running dev stack. **Full write-up with file:line, failure scenarios, proving excerpts, the verified-clean list, and a suggested fix order: `docs/audits/DEEP-AUDIT-2026-07-16.md`.** Attack order below is that doc's. **Shipped 2026-07-16: C1 + H3 (`a157834`), then H1 (key-service gate) + H10 (loopback port bindings), then H9 (comp-grant ON CONFLICT arbiter), then H2 (convert money-pump exploit closed — route flag-gated off; full economic rework still an owner decision, item 4), then the H7/H8 schedule-path pair, H4 (moderation completeness), H5 (deactivation reactivates on login), H6 (feed-delete subscription teardown), M1 (reconcile false-halt), and finally H11–H14 (Jetstream dead zone + socket leak, doppelgänger authors, mobile back-guard). **All 14 HIGHs + the CRITICAL are shipped. 21 of 25 MEDIUMs are shipped too (M1–M7, M9–M15, M18–M22, M24, M25).** The four remaining MEDIUMs are deliberately deferred as design-sensitive (rushing them risks hiding content / double-charge / a bad DDL): **M8** (event-id squatting — needs signed-event↔writer ownership verification, an architectural change, not just graceful 23505 handling); **M16** (RSS guid global dedup — namespacing by source is a unique-constraint migration + one-time re-ingest, and trades ingest-time cross-post dedup for Slice-8 read-time dedup — an owner call); **M17** (email fuzzy dedup dropping distinct newsletters globally — the correct fix is per-subscriber read-time suppression, not a global ingest drop); **M23** (strfry open-write — needs a write-policy plugin with a DB-synced custodial-pubkey allowlist, an operational change). The 18 LOWs remain (see the LOW section + the audit doc).

**Ordering constraint: land HIGH-3 (kind-5 email leak) before CRITICAL-1's redrive** — fixing the relay pin will flush the queued deletion events, so the malformed/PII event build must be corrected first or in the same change. **✅ Both shipped together 2026-07-16 (`a157834`); see items 1 & 3.**

**CRITICAL**

1. **✅ SHIPPED 2026-07-16 (`a157834`) — but C1 was only HALF the bug; the second half shipped 2026-07-17.** ~~Relay outbox worker can't reach the platform's own relay — every native Nostr publish silently abandons.~~ `pinnedWebSocketOptions` now takes an `allowHosts` exact-match exemption (pin still enforced); the outbound publisher passes only the operator-configured `PLATFORM_RELAY_WS_URL` host. Ships with `scripts/redrive-relay-outbox.sql` (purges the email-poisoned deletion rows before redriving the rest). **Prod-verified 2026-07-16 (post-deploy):** 22 abandoned rows, 0 email-poisoned; the pin fix confirmed (events now reach strfry). The 22 stale rows were recovered via `strfry import`.
   **⚠️ The 2026-07-16 sign-off was wrong** — its "new publishing works normally" was never tested and was false. The pin fix only got the worker *to* strfry; strfry then rejected **100%** of what it was handed, because `relay/strfry.conf` carried `rejectEventsOlderThanSeconds = 0` — a **reject window** ("reject anything whose `created_at` is older than N seconds"), not the "no limit" its comment assumed, i.e. reject every event not dated in the future. Latent since the 2026-03-20 baseline, masked by the pin, then misread as staleness (`strfry import` "worked" precisely because it bypasses that check). **Fixed 2026-07-17** (`rejectEventsOlderThanSeconds = 315360000`), proven in dev by a config-only before/after: `created_at=now` → `failed`/"too early"/absent, then `sent`/stored; a stale row now redrives without `strfry import`. **Deployed + prod-verified same day** — a real UI publish landed (`sent`, attempts=1, strfry count 89→90); blast radius was **zero** (nothing had been enqueued since the 2026-07-16 deploy — prod was idle, so no content was lost and no redrive was needed). **C1 is now closed on end-to-end evidence, not transport evidence.** → FIX-PROGRAMME 2026-07-17; §11.

**HIGH**

2. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~key-service vault/key routes ungated + `readerId==writerId` skips payment~~ (`key-service/src/routes/keys.ts`) — a plugin-scope `x-internal-secret` preHandler (constant-time compare, fail-closed) now gates **every** key-service route, so the identity headers are only honoured from the gateway; the two ungated gateway call sites (`proxyToService`, `fetchContentKey`) now send the secret. Verified empirically: the exploit (no secret) → 401; the authorised path (with secret) passes the gate. The `readerId==writerId` self-issue path in `vault.ts` is now unreachable from outside the gateway.
3. **✅ SHIPPED 2026-07-16 (`a157834`, FIX-PROGRAMME).** ~~Account deletion publishes the user's email in signed public kind-5 events~~ (`gateway/src/routes/auth.ts`) — the `a` coord now uses `accounts.nostr_pubkey`, matching the publications path. Landed with C1 per the ordering constraint.
4. **⚠️ EXPLOIT CLOSED (route disabled) 2026-07-16; economic rework still owed.** `POST /subscriptions/:writerId/convert` was an unmetered money pump (`gateway/src/routes/articles/subscription-convert.ts`) — no card gate; spend SUM had no `state`/`publication_id` filter (credited back never-charged provisional reads); phantom charge leg (bare `subscription_events` insert, no tab debit/no `subscription_earning`); repeatable via cancel→re-convert. **Confirmed no web UI calls it** (PaywallGate wires `/nudge/shown` but never the convert action), so it's gated off behind `SUBSCRIPTION_CONVERT_ENABLED` (default off → 503 `conversion_unavailable`) with zero product impact. **Owner decision to revive:** fix all four legs (card gate + filtered spend SUM + real `logSubscriptionCharge` leg + non-repeatable) **and** wire the PaywallGate convert button, then flip the flag. The four defects are documented in a block comment at the route. Until then the conversion nudge is a dead-end offer — consider hiding it in PaywallGate.

    **The third leg has a name now, and a reason (2026-08-06, from the payment-perimeter rev. 4 scoping — §1.16).** The phantom charge leg is not merely under-accounted: combined with the unfiltered spend SUM it mints a **Reader-funded negative tab** — a reader who settles mid-month and then converts ends below zero holding a redeemable claim on the platform, the instrument `PAYMENT-PERIMETER-ADR` §4 rule 4 bans. So the "real `logSubscriptionCharge` leg" is not a bookkeeping tidy-up: **debiting the first month's `subPrice` so the conversion nets to zero is the only shape that leaves the reader's offer intact** while ending the claim — capping the credit at the outstanding balance, or converting only unsettled reads, both close the hole by quietly shrinking what the reader was offered. Revival therefore has to price the offer honestly: this is what makes leg 3 a **product** call and not a cleanup. It is also why the flag must stay off — flipping it would put the `negative_reader_tab` alert into ordinary business.
5. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Comp-subscription grant can never execute~~ (`gateway/src/routes/subscriptions/subscribers.ts`) — the `ON CONFLICT (reader_id, writer_id)` now carries `WHERE writer_id IS NOT NULL`, matching the partial index. Verified in dev: the statement now plans (arbiter → `idx_subscriptions_reader_writer`) where it previously raised 42P10. (This is the §1.10 "functional gift mechanism.")
6. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~`web:3010` + `strfry:4848` bound 0.0.0.0; Docker iptables bypass UFW on prod~~ (`docker-compose.yml`) — both ports now `127.0.0.1:`-bound (matching postgres/gateway); nginx reaches them by service name, dev browser via `localhost:3010`/`localhost:4848` unchanged. Verified: both containers now publish on `127.0.0.1` only, web still 200s locally. **Prod needs `docker compose up -d web strfry`** to re-create with the new bindings.
7. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Scheduling a top-of-article paywall publishes the whole paid body free~~ (`web/src/hooks/useArticleEditorInit.ts`) — `handleSchedule` now reassembles the gate marker whenever `data.isPaywalled` (validation guarantees `paywallContent` non-empty), instead of gating on the falsy-when-gate-at-top `data.freeContent`. The scheduler re-splits `content_raw` on the marker as before.
8. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Scheduling a "Publishing as <publication>" article publishes it to the personal profile~~ (`useArticleEditorInit.ts`) — `handleSchedule`'s `saveDraft` now passes `publicationId` (plumbed end-to-end: drafts schema → `article_drafts.publication_id` → scheduler branches on it).
9. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Admin remove-content/suspend leaves content in every feed and on the relay~~ (`gateway/src/routes/moderation.ts`) — new `removeArticle`/`removeNote`/`removeContentByEventId`/`removeAllContentForAccount` helpers soft-delete `feed_items` for articles and enqueue a kind-5 tombstone (article + note), signed with the author's custodial key, mirroring the self-delete paths. Wired into `remove_content`, report-driven `suspend_account`, and `POST /admin/suspend/:accountId`.
10. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~"Reversible" deactivation is a permanent lockout~~ (`auth.ts`, `google-auth.ts`, `magic-links.ts`) — `requestMagicLink` now admits `deactivated` accounts (`status IN ('active','deactivated')`, suspended still blocked); `/auth/verify` and the Google exchange flip `deactivated`→`active` + invalidate the auth cache on successful login. Logging back in is now the promised reactivation path.
11. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~`DELETE /feeds/:id` orphans feed-derived `external_subscriptions`~~ (`gateway/src/routes/feeds/crud.ts`) — the handler now enumerates the feed's `external_source` rows and tears each down through `removeSource` (last-feed check + `feed_sub:<owner>` lock + kind-3 update, `recordExclusion:false`) before deleting the feed. Added a `loadFeed` ownership gate too.
12. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Jetstream dead zone at ~40–149 active atproto sources~~ (`feed-ingest/src/jetstream/listener.ts`) — the filtered upgrade URL exceeded the pin's default 2048-char cap before wildcard mode engages at 150. Now passes `maxLength: 16384` (covers 149 DIDs, at the documented server ceiling; never binds in wildcard mode) so the filtered range connects.
13. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~External parent/thread prefetch mints doppelgänger `external_authors` from origin-shaped `author_uri`~~ — all four feed-ingest sites (`external-parent-prefetch.ts`) and the gateway twins (`external-hydration.ts`, `external-items/{quote,parent,thread}.ts`) now write the canonical `author_uri`: the atproto DID (was `bsky.app/profile/<did>`) and the AP actor URI `account.uri` (was the human web `account.url`), matching the real ingest so the identity trigger derives one `stable_handle`.
14. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Jetstream reconnect leaks live sockets on every DID refresh~~ (`listener.ts`) — the `close` handler now returns early when `this.ws !== ws` (the socket has been replaced), so a stale socket's async close can't null the live one or spawn a redundant reconnect.
15. **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Mobile: opening reader/profile from any guarded sheet self-closes it~~ (`web/src/lib/backGuard.ts`) — the unregister cleanup now fires its balancing `history.back()` only when `history.state.ahBackGuard === id` (our sentinel is still the top). A URL-synced overlay opened from the sheet pushes its own URL on top first, so the old blind `back()` popped the successor and its popstate closed the just-opened pane.

**MEDIUM** (25 — full detail in the audit doc; headline set)

- **✅ SHIPPED 2026-07-16 (FIX-PROGRAMME).** ~~Reconcile `ledger_orphans` false-positive halts all payouts~~ on any real `transfer.reversed` — the A6 orphan check (both `reconcile-ledger.ts` and `scripts/reconcile-ledger.sql`) resolved `writer_payout_reversal`/`tribute_payout_reversal` against `tab_settlements`, but those refs are `writer_payouts`/`tribute_payouts` (and F5 reuses `writer_payout_reversal` with `ref_table='publication_payout_splits'`), so one real reversal would flag as orphan → `haltPayouts()` forever. Now `ref_table`-aware per reversal type; validated against dev DB (0 rows).
- **✅ SHIPPED 2026-07-16.** ~~Pledge fulfilment inserts `read_events` with NULL `tab_id`~~ (`drives.ts`) — the fulfilment now stamps `read_events.tab_id` from `applyLedgerDelta`'s returned `tabId` (the tab is upserted there, so the INSERT can't know the id up front), so `confirmSettlement`'s `WHERE tab_id = $2` advances the read to `platform_settled` and the writer accrual/payout fires. (Still latent behind parked pledges, but no longer a money bug on revival.)
- **⚠️ PARTLY SHIPPED 2026-07-16.** ~~Chargeback during a reserved-but-pending payout pays out clawed-back money with no reversal~~ — the INDIVIDUAL writer case is fixed: `ReversalRead.claimedByPendingPayout` (platform_settled + writer_payout_id set, non-pub) now reverses the author's paid slice as-if-paid, since a writer payout is atomic and the resume sweep transfers the locked amount in full. **Residual: the publication-split analogue is deliberately NOT closed** — the split load stays `status IN ('initiated','completed')` (paid-only) because a pending pub payout's splits can individually fail KYC, so reversing not-yet-paid splits would risk over-debiting a recipient whose split never pays. Narrower blast radius; revisit with the pub-payout resume redesign.
- **✅ SHIPPED 2026-07-16.** ~~`computePublicationSplits` bps overrides unclamped to remaining pool~~ (`payout.ts`) — the revenue_bps override payout is now `Math.min(floor(articleNet·bps/10000), remainingPool)`, matching the flat-fee clamp, so combined flat+bps can't overdraw the pool; the shares load gained `ORDER BY share_type, article_id, id` (flat fees first, stable) so which fee wins when short is deterministic.
- **✅ SHIPPED 2026-07-16.** ~~`GET /publications/:id/members` has no auth, leaks `revenue_share_bps` + permission matrix~~ — now `requireAuth` + an active-member check (403 for non-members); the public projection stays the masthead route.
- **✅ SHIPPED 2026-07-16.** ~~Publication unpublish leaves the article in all feeds~~ (`publications/cms.ts`) — now deletes `feed_items` in the same transaction, like personal unpublish.
- **✅ SHIPPED 2026-07-16.** ~~`GET /articles/by-event/:id` serves withdrawn articles~~ — the editor-load query now gates `published_at IS NOT NULL OR writer_id = caller`, so the author still loads their withdrawn draft but non-authors can't.
- **⏸ DEFERRED (design-sensitive).** **Event-id squatting on `POST /articles`/`POST /notes`** — client `nostrEventId` trusted, relay publish precedes indexing, attacker registers the victim's id first. Proper fix = verify the event id was custodially signed for THIS writer (a signed-event↔writer record the index route checks), not just graceful 23505 handling.
- **✅ SHIPPED 2026-07-16; verified by drive 2026-07-17 — with a RESIDUAL (below).** ~~`can_manage_members` alone can mint an `editor_in_chief`~~ — a shared `escalationBeyond` guard on both invite and PATCH blocks granting any permission the (non-owner) grantor doesn't hold. **Verified 2026-07-17** against a real fixture (manager holding `can_manage_members` but NOT finances/settings): invite EiC → 403, invite contributor/editor → 201, PATCH `canManageFinances` → 403, PATCH `canPublish` → 200, owner exempt → 200. Blocks escalation without over-blocking.
- **NEW (found 2026-07-17 by the 0b evidence audit) — M9's PATCH arm does not guard `role`, so the invite arm's role check is walked around.** `escalationBeyond` on PATCH is passed only the explicit `can_*` fields, but `role` **is** in the route's fieldMap and **is** written (`members.ts` PATCH). So a members-manager blocked from `invite(role=editor_in_chief)` simply does `invite(contributor)` → `PATCH {"role":"editor_in_chief"}` → **200**. **Not a privilege escalation** — every `can_*` stays FALSE and `requirePublicationPermission` gates on `can_*` only, so M9's core claim holds and the route comment's "role is a label here" is *almost* right. But `role` is not cosmetic in two places: (1) the **public** masthead (`GET /publications/:slug/masthead`, `optionalAuth`) renders `pm.role`, so a manager can forge the publication's public leadership without the owner; (2) transfer-ownership eligibility is gated **solely** on `role='editor_in_chief'` (`members.ts:407`) — driven end-to-end, the owner then transferred ownership to the manager-promoted member (**200, ownership moved**). Owner action is still required ⇒ confused-deputy / social-engineering shape, not a unilateral takeover ⇒ **not HIGH**, but real. **Fix shape:** guard `ROLE_DEFAULTS[data.role]` in the PATCH arm exactly as invite does (the *asymmetry between the two arms is the bug*); optionally also stop keying transfer-ownership eligibility on a manager-writable column. → FIX-PROGRAMME 2026-07-17.
- **✅ SHIPPED 2026-07-16.** ~~Magic-link single-use is non-atomic SELECT-then-UPDATE~~ (`shared/src/auth/magic-links.ts`) — now one atomic `UPDATE … WHERE used_at IS NULL … RETURNING`, so only one racer claims the token.
- **✅ SHIPPED 2026-07-16.** ~~Dedup can hide both copies~~ (`dedup-sql.ts`) — the `candidates` CTE now mirrors the host's context-only/reply visibility predicates, so a row that would be filtered can't be the dedup winner and suppress its visible twin. Test harness `matched` gained `allow_replies`; all 13 dedup integration tests pass against the dev DB.
- **✅ SHIPPED 2026-07-16.** ~~Feed merge 500s when both feeds carry the same `reach` source~~ (`crud.ts`) — the merge duplicate guard gained the `reach`/`reach_kind` arm, so a shared reach source is skipped instead of violating `feed_sources_reach_uniq`.
- **✅ SHIPPED 2026-07-16.** ~~Cursor truncation to whole seconds~~ — all four time-based cursors now carry sub-second precision: `feed_saves` emits `(EXTRACT(EPOCH …) * 1000)::bigint` (ms), and the explore-placeholder + two author-log cursors carry the fractional-second epoch (`EXTRACT(EPOCH …)` without `::bigint`) fed to `to_timestamp()` (which accepts fractional), matching the full-precision ORDER BY. The whole-second `published_at_epoch` stays for display.
- **✅ SHIPPED 2026-07-16.** ~~RSS: one unparseable `pubDate` deactivates the whole feed~~ (`adapters/rss.ts`) — both the RSS and JSON-feed date parses now guard `isNaN(getTime())` (the dead try/catch is gone), matching the AP/atproto adapters, so an Invalid Date falls back to now() instead of poisoning the batch.
- **✅ SHIPPED 2026-07-16.** ~~`external-items-prune` broken 2 ways + permanent wedge~~ — replaced the dead `WHERE FALSE` bookmark guard with a real `notes.external_parent_id` guard (a reply's external parent is preserved); added a `citation_edges` guard so a cited item never triggers the no-ON-DELETE RESTRICT wedge; dropped `deleted_at IS NULL` so tombstoned items are pruned too (not retained forever). The same citation_edges guard was added to `external-context-gc` (which also dropped `deleted_at IS NULL`) and to `external-sources-gc` Phase B (a source whose items are cited is spared, since the source-delete cascade would otherwise hit the same wedge).
- **⏸ DEFERRED (design-sensitive).** **RSS guid dedup is global** → cross-feed suppression, incl. hostile guid-replay (`feed-ingest-rss.ts:179`, `schema.sql:3544`). Namespacing the guid by source is a unique-constraint migration + a one-time re-ingest, and it trades the current ingest-time cross-post dedup for Slice-8 read-time dedup — an owner call, not a mechanical fix.
- **⏸ DEFERRED (design-sensitive).** **Email fuzzy dedup drops distinct newsletters globally** (`email-ingest.ts:41-55`) — the title±1h "shared subscriber" scope makes a GLOBAL non-insert decision, so subscriber B of source Y loses Y's issue because subscriber A also gets a same-titled issue from X. Correct fix is per-subscriber read-time suppression (Slice-8 shape), not a global ingest drop.
- **✅ SHIPPED 2026-07-16.** ~~Nostr poll interval never resets on success~~ (`feed-ingest-nostr.ts` + the backfill completer) — the success UPDATE now sets `fetch_interval_seconds = 300` (the backoff base), so a recovered source returns to the base cadence instead of staying on its last backed-off interval.
- **✅ SHIPPED 2026-07-16 (publish-now path).** ~~"Allow replies" is dead at publish~~ — `publishArticle`/`articlesApi.index` now send `commentsEnabled`, the index route's schema + INSERT/UPDATE write `articles.comments_enabled` (column already existed, default true), and the editor edit-load reads the real value from `/articles/by-event` (was hardcoded `true`). **Scheduled path:** now closed by migration 157 (M20) — `article_drafts.comments_enabled` carries the toggle and the scheduler sets it on the article.
- **✅ SHIPPED 2026-07-16 (migration 157).** ~~Dek/standfirst silently dropped by the whole draft pipeline~~ — migration 157 adds `article_drafts.dek` + `comments_enabled` (both nullable, for the upsert COALESCE pattern); the drafts route's schema + all four upsert paths + GET now carry them, and the scheduler emits the NIP-23 `summary` tag from the dek and sets `articles.summary` + `comments_enabled`. This also closes the M19 scheduled-path residual (a scheduled article now honours the reply toggle). schema.sql regenerated canonically (drift guard green, dev migrated to 157).
- **✅ SHIPPED 2026-07-16.** ~~Editor close discards work~~ — the autosave fingerprint now covers every persisted field (was title|content only), a metadata-change effect autosaves title/dek/price/cover/comments edits (the TipTap `onUpdate` fired on body edits only), and unmount now flushes a fire-and-forget save when dirty (gated on real content + `isDirty`) instead of just cancelling. A `disposedRef` (set after publish/schedule) suppresses the flush so it never recreates a disposed draft, and explicit Save calls `markSaved` so the flush doesn't re-write identical content — both respecting the one-draft-per-article invariant (every save targets `currentDraftId`/`dTag`, never a re-guess).
- **✅ SHIPPED 2026-07-16.** ~~Escape closes both the Lightbox and the Glasshouse under it~~ — the Lightbox's `document` keydown listener (which fires before Glasshouse's `window` listener in the bubble phase) now `stopPropagation()`s on Escape, so the topmost modal absorbs it and the pane below stays open (no stray `history.back()`).
- **strfry is open-write** (`relay/strfry.conf` has no write policy) — spam/disk-fill toward the 8GB LMDB cap + foreign-event injection at `wss://all.haus/relay`; needs a pubkey-allowlist plugin independent of the port fix.
- **✅ SHIPPED 2026-07-16.** ~~migrate.ts has no advisory lock~~ — the runner now takes a session `pg_advisory_lock` before applying, released in `finally`, so a second concurrent runner waits instead of double-applying (critical for the no-transaction ALTER TYPE / CONCURRENTLY path).
- **✅ SHIPPED 2026-07-16.** ~~safeFetch forwards `Authorization`+body across cross-origin redirects~~ (`http-client.ts`) — a redirect hop that changes scheme/host/port now strips `authorization`/`cookie`/`proxy-authorization`, and a 301/302/303 downgrades POST→GET and drops the body (307/308 preserve). All 44 http-client tests pass.

**LOW** (18) — approve-path missing `deleted_at` guard; scheduler drafts-delete on in-review submit; non-unique `created_at` cursors (notifications/DMs); `useFollows.hydrate()` clobbers optimistic follow; schedule-picker UTC/local `min` mix; unvalidated UUID → 500 on `GET /trust/:userId`; only-feed delete race; `/thread/:postId` unscoped email items; identity-link targets uncanonicalised (phantom sources, no-op link); offer redemption burns count on 409 + TOCTOU; reactivation double-charges the unexpired period; `withTransaction` failed-ROLLBACK masks error + poisons the pooled client; non-constant-time internal-secret compares; atproto malformed-reply TypeError aborts the whole backfill; non-transactional kind-5 tombstoning + cursor advance; unbounded relay-event buffer + unverified engagement counts; ledger-adjacency guard bypass shapes; NAT64 range absent from the v6 blocklist. Full detail in the audit doc.

## 0f. 2026-07-19 commit audit — Jul 16–19 window

Five-agent adversarial review of the window's ~40 commits: the §0e fix batch (`a157834`…`da541da`), the 2026-07-17 verification/smoke sweeps (`20c0a91`…`9fcd64b`), the thread-hydration slices (`413fd85`/`8e34973`/`89a3aa4`), the Explain caption + copy work, and the feed colour/density feature (`feb0b26`). Every finding below was re-verified in source by its finder (several empirically — SQL against the dev Postgres, test runs, mutation checks); the verified-clean list is at the end. **Pattern worth naming:** three separate fixes in this window shipped with their own *analysis* wrong (item 2's residual note, item 6's "fixed" claim for saves, item 5's settled semantics) — the same evidence-tier failure the 0b re-check was built to catch, now appearing in the fix commits themselves. **Independently re-verified 2026-07-19 (five fresh read-only agents over this section alone):** items 1–17 and 19 CONFIRMED in source at `c993579`; item 18 amended (two of its nits withdrawn); line refs corrected in items 3 and 12; one new sibling defect surfaced and appended to item 3.

**All 19 items FIXED 2026-07-19** — one sweep, logged in full in `FIX-PROGRAMME.md` (2026-07-19 "§0f fix batch" entry, item-by-item with the validation record). Summary of dispositions: 1 (unpublish gate) → rowCount-gated 404 before the DELETE; 2 (writer rollback) → state-filtered; 3 + sibling (A6 ref_table split in both checks incl. the restored chargeback-`writer_payout_reversal` branch; tribute rollback voids charged-back-read accruals via the `read_events.state` join — the chargeback-time terminalisation the item suggested was rejected because it skews the completion carve sum) → the pre-flag-flip gate for §2.1 is cleared; 4 (editor seed + disposal race), 5 (hydration success-bit), 6 (saves → shared fractional cursor), 7 (publication `comments_enabled` threaded end-to-end), 8–10 (docs), 11 (dedup predicate fixtures + positive controls), 12 (batched prune + de-flaked test), 13–19 (Jetstream socket close, two-phase moderation sign, lightbox/AuthorModal Esc yield, role-label + invite-accept grant guard, safeFetch POST-only 301/302 rewrite + header strip, SchemeMenu a11y + lockfile gitignore, export `/api/v1` prefix — verified empirically against the running stack).

**Residuals (the only §0f items still open, both pre-existing LOWs recorded under item 5):** the re-root path (`usePostThread.ts` ~302) ignores `res.hydrating` entirely — D2's poll contract covers only one of the two fetch paths; and the nostr k-of-n soft-deadline timer (`nostr-relay.ts`) starts before DNS-pin/socket-open, so a slow resolver eats the 2.5s budget and a *successful* sparse result gets throttle-pinned 60s. Also retained by design: `external-sources-gc`'s citation guard spares a whole source if one item is cited (documented, defensible, permanent exemption — item 18).

**Verified clean** (so it needn't be re-audited): the security batch (H1 gate — all five call sites send the secret, fail-closed constant-time preHandler; H10 loopback binds), the strfry reject-window analysis + 10-year value + redrive-runbook restart-vs-recreate reasoning (per-file caveat in item 8 aside), C1's `allowHosts` exemption (exact-host, operator-config-only, single call site), the comp-grant arbiter (matches the partial index, sole INSERT on it), the convert flag gate (default-off, first statement, no other caller), M4's pool clamp + deterministic ORDER BY, M24's advisory lock (same-connection, per-database, drift-guard-safe), the magic-link atomic claim (single UPDATE … RETURNING under READ COMMITTED), migration 157 + canonical schema.sql regen with seed, feed-delete teardown (reuses the real `removeSource` + lock, not a parallel implementation), H14's top-of-stack gate (out-of-order closes degrade gracefully), da541da's M2 tab_id stamp (`applyLedgerDelta` pairing respected), the D1/D2 hydration core (sync in-flight registry + `finally` cleanup; D3 exhaustive-mode scoping keeps newest-wins for all replaceable-by-author callers; D4 can't leak an unhandled rejection), M13's second pass for tags/sources/author×2/explore (every `to_timestamp` cursor consumer checked; saves item 6 is the sole residual), M15's guard set (matches the complete FK inventory on `external_items`), M12/M14/M18/H11/H12 (each re-verified against schema defaults and adapter parity), the Explain caption slices C1–C4 + scrim rework (ADR-conformant, hairline-clean, `tsc` clean, hit-test scope sound), c000beb (menu invariants hold on both branches), and `b621f96`'s test commit (transport-only mocking + the 8-row scramble genuinely defeat the false-pass classes it names). Hairline tripwire + `tsc --noEmit` clean over all touched web files; no ledger-adjacency, one-post-per-card, feed-derived-subscription, relay-free-identity, or promotion-invariant regressions found anywhere in the window.

## 0g. 2026-07-19 resonance pre-ship review — side-finding

Single-agent close-read of the SOCIAL-PROOF-RESONANCE drop (migration 158 + `engagement-baseline-refresh` task + ADR) against source before placement. The two in-scope findings (`articles.writer_id` vs the task's nonexistent `author_id`; the stale "paid up-vote" premise — voting free since F9) were **FIXED pre-ship 2026-07-19** (see FIX-PROGRAMME entry).

1. ~~**HIGH — account deletion is broken for everyone**~~ — **FIXED 2026-07-19** (FIX-PROGRAMME §0g.1 entry). Driving the endpoint peeled TWO further latent breaks behind the reported one — `DELETE FROM feed_saves WHERE user_id` (no such column; feed_saves is feed-scoped) and `SET status = 'deleted'` (enum value never existed; migration 049 only added `'deactivated'`). Fixed as: notes → hard-DELETE + kind-5 tombstone (the `notes.ts:351` pattern); feed_saves rescoped via the owner's feeds; **migration 159** adds terminal `'deleted'` (not a reuse of reversible `'deactivated'` — Google/magic-link login would resurrect it). Verified empirically pre/post: 500-and-rollback before, 200 + terminal deleted state + 403 on re-auth after. Deploy residue: migration 159 must run on prod before the gateway deploy.

## 0h. 2026-07-21 commit review — Jul 19–21 window

Five-agent adversarial review of the window's 27 commits (workspace floor + collision/merge, resonance steps 1–5 + battery, config-defaults fix + §7 cleanup + feed colour picker, §0f fix batch + account deletion, thread-hydration slices). The two HIGH findings — the collision resolver's reachable livelock silently persisting identical-coordinate stacks, and the FIX-PROGRAMME claim that the `visited` guard removal was "defensive" (it is load-bearing; the log was wrong) — were **FIXED 2026-07-21** (FIX-PROGRAMME "follow-up" entry: verify-and-repair pass, corpus widened to resize scale, both counterexamples landed as mutation-killing fixtures, hydrate heal for persisted piles, log corrected). Still open, by cluster:

1. **Money-path test debt (highest stakes in the window).** cdd5823's state-filtered rollbacks + tribute void shipped with zero new coverage, and the conformance-suite mocks now MIS-MODEL the fixes: ~~the tribute harness ignores `AND state='released'` and swallows the new void UPDATE in its default no-op~~ — **tribute half DONE 2026-07-21 §0i batch**; ~~the writer harness re-implements rollback ignoring `AND state='platform_settled'`~~ — **DONE 2026-07-21 code-tier batch `8e4b5f4`** (state-filtered mock + pointer-null handler + mid-flight charged_back fixture via one-shot `mutateOn`, mutation-verified). Still open: no DB-backed test for the repaired delete-account route (three type-invisible breaks, verified only empirically).
2. ~~**Payments one-liners**~~ — **ALL DONE 2026-07-21 code-tier batch `8e4b5f4`**: magic-link verify refuses `deleted` (active/deactivated only, mirrors Google OAuth); charged_back reads' dangling `writer_payout_id` nulled (state untouched, symmetric with the tribute void); A6 gains the unknown-ref_table catch-all in both TS and SQL twins; the M3 cross-reference was already in `df4265f`'s comment block.
3. **Thread hydration:** ~~k-of-n counts EMPTY EOSEs toward k~~ — **DONE 2026-07-21 code-tier batch `8423cd8`** (delivering relays only, + empty-EOSE test, mutation-verified); ~~in-flight-registry doc one-liner~~ + ~~the 45s client poll budget~~ — **DONE 2026-07-21 §0i batches**. Still open: the exhaustive profile phase pins cold hydration at ≥6s on a hung relay, mostly neutralising D5 (candidate Slice 3b: k-of-n on the thread's kind-0 REQ, newest-wins preserved).
4. **Workspace floor (remaining review findings, feel-pass tier):** ~~mid-gesture origin shifts via live pushes~~ and ~~the armed-merge target buried by a wave push~~ — both **MOOT** post-mover-yields (`26ddf55`: nothing moves mid-drag, so no push can shift the origin or bury the armed target); ~~un-hide, `adoptFeed`, and bootstrap default slots produce resting states with no resolution pass~~ — **DONE 2026-07-21 §0i batch** (all three settle through `findRestingPosition`); the last item — dedupe the two per-frame DOM sweeps (pointer test + `readFloorRects`) — is **MOOT 2026-07-22** (columnar Slice 3: drop resolution reads derived geometry, so the floor makes no per-frame DOM sweep at all). **Cluster closed.**
5. ~~**Frontend**~~ — **ALL DONE 2026-07-21 code-tier batch `41c6ad1`**: SchemeDot ring keys off the global mode (bone-on-dark); `.focus-ring` on trigger + swatches; aria-label colourway-id exposure recorded as deliberate (GLASSHOUSE-AND-PALETTE-ADR §III.4).
6. **Resonance:** ~~the per-post E formula is duplicated…~~ — **FORMULA HALF DONE 2026-07-21** (`externalEExpr`/`nativeEExpr`/`nativeEngagementJoins` in `resonance.ts`, in the existing `bandExpr`/`PCTL_EXPR` idiom; both modules call them, `feed-ingest/tests/resonance-e-parity.test.ts` guards it, four mutations verified, and the refactor was driven end-to-end against the dev DB — 613 baselines, 4 ambient rows — rather than only typechecked). **The population half is now an explicit DECISION, not drift:** the scorer bands `is_context_only` rows the baseline excluded from the distribution (reachable — `external-engagement-refresh` selects on `deleted_at IS NULL` with no context filter). Deliberately NOT closed, because it is a product question rather than a bug: context-only rows are hidden from feed queries but ARE visible in threads and on hydrated profile timelines, so excluding them from scoring leaves visible posts with no band, while including them scores against a distribution they were not part of. The glyph is dark, so nothing is user-visible either way — and changing scoring population unmeasured while dark is exactly what the dial discipline warns against. The current divergence is **pinned by a test** so it cannot shift silently: align it deliberately (updating the ADR) or record the asymmetry as intended. Owner: SOCIAL-PROOF-RESONANCE-ADR. **Still settle this before native reposts/zaps touch the formula.** Also: the flag-off external-feed ordering is effectively by uuid (`fi.score` native-only — pre-existing, documented in the ADR's step-5 note; the fix is the ranking flag, but the bug needs an owner if the flag stalls); the ADR could state outright that the dead clauses' unreachability proof expires under a band-gate retune.
7. **Config residue:** ~~the in-code `num(key, fallback)` fallbacks (×3 modules)…~~ — **DONE 2026-07-21.** Parity chosen over deletion (lower risk — the fallbacks stay but can no longer drift). `shared/db/config-defaults-parse.ts` reads the file; `feed-ingest/tests/config-fallback-parity.test.ts` + `gateway/tests/config-fallback-parity.test.ts` drive the REAL loaders with an empty config so they exercise the shipping fallback path, and each also pins that a seeded value WINS over its fallback (a fallback shadowing a present row would pass a naive parity check while silently removing all operator control). All 13 agreed — a guard, not a repair. The third site was deleted outright: `engagement-baseline-refresh` re-declared the five E weights with independently written fallbacks and now reads the scorer's loader. Three mutations verified. Incidental fix: `shared`'s build now copies `src/db/*.sql` into `dist` (tsc does not), closing a latent `migrate.ts`-from-dist ENOENT. ~~CLAUDE.md's config-rule rationale misdescribes history~~ and ~~`jetstream_healthy` unjustified in the defaults file~~ — **both DONE 2026-07-21 docs pass** (rationale corrected — the gates were re-tuned pre-landing and went into config in the same commit; state-vs-payouts_halted comment added above the seed).
8. **Pattern-tier:** ~~the 8×-hand-rolled feed cursor codec~~ — **DONE 2026-07-21**: `gateway/src/lib/cursor.ts` holds `parseCursorEpoch` (the M13 rule, previously written out 3×), `encodeTsIdCursor` (hand-rolled 5×) and `feedCursorEpoch`. The two WIRE FORMATS stay distinct on purpose — collapsing the untagged `ts:id` and tagged `scored:`/`explore:` families would invalidate every cursor a client holds mid-pagination, for no correctness gain; what matters is that both parse their epoch through one function. Also closes the previously undefended SQL half of M13: a `::bigint` cast truncates the epoch inside Postgres before any decoder runs, so `feedCursorEpoch` states the no-cast rule and a test pins it. Mutation-verified — reverting to `parseInt` fails **12** tests, vs 7 pre-consolidation, because one primitive now backs every decoder. **Still open:** the TS/SQL reconcile-check duplication (`reconcile-ledger.ts` / `scripts/reconcile-ledger.sql`) remains a two-file drift risk.

**Verified clean** (needn't be re-audited): the config-defaults mechanism on all four properties (every-run application incl. no-ops, no-clobber `ON CONFLICT`, `payouts_halted` exclusion, Checks 4a/4b/4c genuinely enforcing their claims — 4b derives ground truth by applying the defaults file itself); the §7 cleanup (real deletions, zero dangling refs, no dormant-marked component touched, CLAUDE.md in lockstep); density superset + normalise-on-read at all three seams; both resonance brakes at genuine choke points with ungated write paths and absence-vs-zero held end-to-end; the resonance battery's two dead clauses correctly RETAINED (unreachability is dial-contingent); the D1+D2 hydration fix (structural, cache-honest by construction, SSRF pinning preserved, no leaked sockets); D5's budget double-bound; the §0f sampled fixes (unpublish rowCount gate, two-phase moderation sign, invite re-validation at accept, safeFetch redirect semantics) all at the right layer; cdd5823's void fully Dial-A-conformant (terminal `'voided'` in the migration-156 CHECK, idempotent, ordered before release, no ledger/tab touch); the workspace two-coordinate-space model (one conversion seam under grep, extent derived never stored).

## 0i. 2026-07-21 second-pass audit — full Jul 19–21 window at HEAD

Five-agent adversarial review of all 22 window commits at `26ddf55` (thread-hydration slices; §0f/§0g fix batches; resonance steps 1–5 + battery + config-defaults; the workspace floor/collision arc INCLUDING the mover-yields rewrite §0h predates; brand/lens + §7 cleanup + colour picker). The worst findings — the floor-lurch scroll clamp, three overlap-minting system placement paths, the blind hydrate heal, gesture-slack multi-pointer/resize-seed defects, unreachable merge-error UI, the hydration silent-success half of §0f-5, and the tribute-void earned-ledger imbalance — were **FIXED same-day** (FIX-PROGRAMME 2026-07-21 "§0i" entry; §0h amended above where discharged). Still open, by cluster:

1. ~~**Engagement cron budget starvation (MED — resonance flip-gate caveat).**~~ — **DONE 2026-07-21 code-tier batch `49f9aa5`**: the daily sweep pages via the persisted keyset cursor `engagement_daily_sweep_cursor` (runtime state, payouts_halted class), rotating the whole <7d window across successive daily runs; short page resets, stale cursor re-selects same-run; warns corrected; five-test battery mutation-verified. Long-tail items now refresh every ceil(window/budget) days — the flip-gate coverage caveat is discharged. (Nostr leg documented as needing its own cursor before its flag lights at scale.)
2. ~~**D6 scored-mode cursor decays with `now()` (LOW/MED, dark).**~~ — **DONE 2026-07-21 code-tier batch `90e26ef`**: age pinned to a cursor-carried fractional-epoch `asOf` (optional 4th scored-cursor part; pre-deploy 3-part and flag-off cursors unchanged); time-shifted pagination + codec tests.
3. ~~**Publication DELETE route lingers feed cards (MED).**~~ — **DONE 2026-07-21 code-tier batch `a1d4248`**: feed_items DELETE inside the route's transaction, mirroring unpublish with the same rowCount guard.
4. **Account deletion leaves the external world live (MED, product-call flavoured).** `auth.ts` deletion removes follows/bookmarks/feed_saves but leaves: `feeds`/`feed_sources`/`external_subscriptions` (feed-ingest keeps polling for a deleted account forever; no `retractFollowList`, kind-0/kind-3 stay on the public mesh), `article_drafts` (plaintext content after "permanent" deletion), key-custody (custodial nsec retained, no destruction/export-cutoff decision), and the username (never freed). Also: the per-item `signEvent` loop runs INSIDE `withTransaction` with per-item swallow — the exact pattern §0f-14 two-phased in moderation.ts, and key-custody downtime deletes content with no tombstones. ~~And the magic-link verify route refuses only `suspended`~~ — **magic-link bullet DONE 2026-07-21 code-tier batch `8e4b5f4`** (refuses everything but active/deactivated).
5. ~~**Difference-lens under non-Glasshouse overlays (MED, brand).**~~ — **DONE 2026-07-21 code-tier batch `41c6ad1`**: self-declaring lens-suppressor store (`stores/lensSuppress.ts`) held by NewFeedPrompt + LightboxOverlay while open; folded into `lensMode`; the FORALL ADR's painted-state list is now exhaustive and names the store as the extension point for future bespoke overlays. **Historical only — the store, `lensMode` and the whole painted-state list were deleted with the lens on 2026-07-22 (Slice 4); a new bespoke overlay needs nothing from this.**
6. ~~**`reply_to_author` re-NULL misses soft-deleted external parents (MED).**~~ — **DONE 2026-07-21 code-tier batch `a1d4248`**: pass 6 keys on `ei_p.deleted_at IS NOT NULL` (origin deletion clears), row absence (prune/GC) RETAINS the last-known name — the decided prune-aged semantics; pass 4 skips origin-deleted parents; pass 5 stays absence-based.
7. ~~**FeedComposer/SchemeMenu.**~~ — **DONE 2026-07-21 code-tier batch `41c6ad1`** (Escape stopPropagation; §0h.5 ring/focus/aria items all shipped).
8. ~~**Hydration/relay LOWs.**~~ — **DONE 2026-07-21 code-tier batch `8423cd8`**: sync-ctor-throw guard; first-event id verification (junk neither resolves nor stores; ids-only misuse keeps any-event behaviour); duplicate EOSEs count once; `fetchFocal` polls on `hydrating: true` (seq-guarded). Four scripted-relay tests.
9. **Resonance/config LOWs:** ~~`protocol_engagement_ambient` rows never pruned~~ — **DONE 2026-07-21 code-tier batch `009801f`** (pairs absent from tmp_e deleted same-txn; battery test); ~~ADR "Absence" battery line~~ and ~~stale `feed-rank.ts` comment~~ — **both DONE 2026-07-21 docs pass**; ~~`PostResonance.networkLabel` Nostr-gloss fallthrough~~ — **DONE 2026-07-21 `41c6ad1`** (label by protocol alone).
10. **Cleanup/brand LOWs:** ~~Bluesky rename heal is one-shot~~ — **DONE 2026-07-21 code-tier batch `009801f`** (self-heal widened to missing-OR-failed-resolve via the shared `ATPROTO_ENRICH_FAILED_ERROR` marker; existing error_count backoff applies); still open: poll skip-logging can't see hosts starved by the TICK cap (hosts after the break are never counted — log the remainder); ~~§7 item 18 unrecorded~~ — **DONE 2026-07-21 docs pass**; brand-export splay: ~~docs mis-describe the supersession as rim-only~~ — **docs corrected 2026-07-21**; ~~the GEOMETRY DECISION itself stays open~~ — **DONE 2026-07-22**: neither branch. The narrow stance is the **disc form's** stance, in-app included — the `ForallMenu` trigger and `favicon.svg` (+ rasters) were ported onto §III.1's rim-kiss construction, so trigger/favicon/exports are one geometry again; the bare `ForAllMark` keeps ~20.5° because it has no rim. One-construction rule amended in CLAUDE.md to name the two stances and why the difference is derived (ADR §III.1 resolution note; §IV.4 superseded); `migrate.ts`'s CONCURRENTLY single-statement check is a naive `;` split (false-refuses a `;` inside a string literal — latent).
11. ~~**Workspace test debt (from the §0i fix batch itself)**~~ — **MOOT 2026-07-22** (columnar Slices 1–3). Every subject is deleted: `canvas.ts` and `collision.ts` (+ its corpus) are gone, and with them the origin-shift/scroll-compensation arithmetic the floor-lurch HIGH lived in — there is no origin. `layout.ts` carries 47 property tests over the model that replaced them, and `reconcileFeeds` now has the store-level test `reconcileLayouts` never got (`stores/workspace.test.ts`, mutation-checked).

**Verified clean** (needn't be re-audited): dark-ship discipline on both resonance brakes (mapper choke point, no `NEXT_PUBLIC_` twin, ungated write paths, DEPLOYMENT.md + compose rows); the c9856f7 dead-clause analysis (independently re-derived); 1d6b756's defaults mechanism end-to-end + no dial read-but-undefaulted anywhere; migration 158/159 schema.sql conformance (drift guard 6/6 green); §0f items 1/3/6/14/16/19 re-verified true in source incl. the A6 split walked through both reconcile copies; the §7 cleanup (zero dangling refs); density superset + normalise-on-read; the ∀ one-construction invariant in-app + blend-on-outermost-fixed + both isolation seams + dark-mode disc swap; feb0b26's scheme/density plumbing at all three seams; D1/D2/D3/D4 hydration wiring against the ADR (all four out-of-scope relay callers stay exhaustive); `findRestingPosition`'s geometry proof-checked (directional snap, escape-valve reachability, flush-touch semantics); hairline tripwire + root lint + `tsc` clean across the window.

## 0j. 2026-07-22 process retrospective — the Jul 21 self-audit loop

A **process** review, not a new-bug audit — prompted by the owner's read that "things are going off the rails." Conclusion: the *code* is healthy; the *cadence* is showing a self-reinforcing audit pattern worth braking now. No code action items below — the actionable output is the four working-discipline points at the end.

**The code is sound (this is the reassuring half).** Backend typechecks clean (gateway/payment-service/shared/feed-ingest/key-service all exit 0); web's only `tsc` error is the standing `globals.css` side-effect-import noise (`next build` is the real gate). The §0h/§0i fix batch is **not** make-work: of the 15 commits, **7 fix live, user-reachable bugs** (magic-link admitting `deleted` accounts on the login path `8e4b5f4`; publication DELETE leaving cards in every feed until daily reconcile `a1d4248`; live Bluesky/Mastodon engagement counts frozen for ~90% of items `49f9aa5`; thread hydration caching bare results on transient 429/5xx + a WebSocket ctor-throw that could hang forever `485493c`/`8423cd8`; workspace floor lurching ~a viewport per drop `062ca28`; SchemeMenu Escape killing the whole composer `41c6ad1`), 2 fix real bugs on dark features (tribute-carve imbalance `df4265f`, D6 keyset dup `90e26ef` — lower stakes today), the rest guards/refactors/docs. The collision arc (`4c933eb`→`1df5215`→`26ddf55`→`062ca28`) **converged** with meaningful tests (a 3,000-trial randomised property corpus that bites under mutation) — a clean endpoint, no dead code.

**But the pattern is the concern, and it is a process one:**
1. **Self-referential audit recursion with no exit condition.** §0i is explicitly a review of the *own prior* Jul 19–21 window; §0h reviewed the window before it. Each fix batch becomes the next audit's subject, so the loop cannot terminate on its own. The findings are getting granular (§0i.1 through §0i.11 with sub-bullets) and increasingly dark-feature-only.
2. **Documentation/ceremony overhead is outgrowing the code.** ~520 lines of FIX-PROGRAMME in the day; individual commit messages of 600 lines; itemised §0i.N grading. The work-log is growing faster than the product.
3. **Same-day design discard.** The collision push-wave resolver was *built* (`4c933eb`) and then *hardened over a second commit* with a livelock-repair shelf + a 600-line message (`1df5215`), then **deleted the same day** (`26ddf55`) for a simpler model that made the hardening moot. Good endpoint, but two commits of effort were spent polishing a mechanism before validating it was the right one.
4. **Displacement.** Every commit in the window is backend/infra polish or dark-feature correctness. Nothing net-new that a user can touch. Meanwhile §3 (launch-blocking product gaps — owner dashboard, landing page, writer onboarding) is untouched.

**Forward discipline (the actual queue items here):**
1. **Dark-feature findings go to the queue, not to a same-day fix commit.** Anything behind an off-by-default flag (tributes, resonance ranking/glyph) is deferred, not fixed in the audit cycle — it is the lowest-ROI churn and the easiest to hold.
2. **Cap the audit recursion at one pass per work window.** Review once, triage findings into this queue, then move on — rather than fix→re-audit→fix in a tight same-day loop. An Nth-pass audit of one's own audit fixes has sharply diminishing returns.
3. **Validate a mechanism before hardening it** (the collision lesson): prototype/prove the approach is the right one before adding livelock guards, repair shelves, or 600-line rationale.
4. **Rebalance toward §3.** The signal to watch for is not broken code — it is exactly this profile: lots of motion, real-but-shrinking bugs, a growing guard lattice, and nothing new shippable. Next window's default should be product-surface work, not another audit pass.

## 0k. 2026-07-22 diagnosis-verification sweep — same-class follow-ons

Seven-agent verification of the §0j bug catalog: every §0h/§0i fix diagnosis re-derived from pre-fix source (`git show <sha>^`) and checked against HEAD. **8 of 9 diagnoses CONFIRMED** (mechanism, fix, and claimed impact all accurate — incl. the a1d4248 24h window, the 49f9aa5 ~90% arithmetic, and the 062ca28 clamp-before-layout-effect mechanism independently re-derived); one PARTIALLY CORRECT (correction below). The sweep's real output is the same-class siblings the fixes did not cover — queued here per §0j discipline #1/#2, **not** fixed same-day:

**All four items FIXED 2026-07-22** — one owner-called batch, logged item-by-item in FIX-PROGRAMME (2026-07-22 "§0k fix batch" entry with the validation record). Dispositions:

1. ~~**Account deletion leaves feed cards live for up to 24h (MED, live).**~~ — **DONE 2026-07-22**: `feed_items.deleted_at` soft-stamp over the account's articles inside the deletion transaction (idiom matches the `manage.ts` soft-delete pairing; notes' cards already cascade). Driven end-to-end on the rebuilt dev stack (fixture account → delete-account → card stamped same-txn). The wider §0i.4 account-deletion cluster (external world teardown, drafts, key custody, username) remains open — this closed only the feed-card sibling.
2. ~~**Author-timeline hydration silent-fails on transient errors (MED, live).**~~ — **DONE 2026-07-22**: the three `safeFetch` sites throw on 429/5xx (definitive 4xx stays a clean settle) and the catch clears the TTL guard — the `485493c`/D1 treatment ported verbatim. 3 mutation-verified tests in `author-timeline-guard.test.ts`.
3. ~~**Five popovers still double-close on Escape (LOW, live UI).**~~ — **DONE 2026-07-22**: shared `web/src/hooks/useEscapeShield.ts` (stopPropagation-claim + `yieldTo` for a modal above) applied to all five sites; the SchemeMenu original ported onto it so the pattern has one home. Browser-tier confirmation rides the §11 smoke list.
4. ~~**Minor pair.**~~ — **DONE 2026-07-22**: (a) deleted-account refusal now reads "Account deleted"; (b) `parseCursorEpoch` range-clamped to `[0, 1e11]` — out-of-range degrades to page-1 restart instead of a `to_timestamp()` 500 (mutation-verified test).

**Correction to the §0j catalog (record, no code change):** the `8423cd8` ctor-throw consequence is misdescribed as "could hang forever". The defect and fix are real, but thread hydration's k-of-n mode has an unconditional soft-deadline timer and could **not** hang — only exhaustive/first-event callers could — and the likelier pre-fix observable was a **gateway process crash** (the throw rejects a `void`'d IIFE; no `unhandledRejection` handler is registered and Node 20's default is fatal). Also, §0j's "magic-link **admitting** deleted accounts" slightly overstates `8e4b5f4`: the session was middleware-dead (`requireAuth` re-checks `status='active'` per request), so the impact was a zombie cookie + identity-fields leak in a ≤15-min window, never API access — the commit message itself had this right.

**Verified clean in the sweep** (needn't be re-audited): `8e4b5f4` sibling coverage (Google OAuth already allowlisted, dev-login active-only, no separate refresh path); `49f9aa5`'s residuals all self-acknowledged in-commit (rotation lag ceil(19,944/2000)=10 days vs the 7-day window — operator-mitigable via the `feed_ingest_engagement_max_items` dial; strict-`<` tie skip; Nostr leg deferred); `062ca28`'s pointer-id keying + Ctrl+arrow mid-drag guard (the one residual — `floorScrollRef` is an async hint, staleness bounded to a few px — is acceptable); `41c6ad1`'s `stopPropagation`-not-`stopImmediatePropagation` choice (different-node shield, correct); `df4265f` order-safe in both chargeback↔void orderings, `'reversed'`-status filter correct, M3 residual honestly framed, A10a/A10b exclusions real; `90e26ef` wire-compatible (3-part cursors decode, flag-off untouched), the ~37%/h figure defensible as g/(age+2); dark framing accurate for both dark fixes.

---

## 0l. 2026-07-22 prod incident — starter template merged away

The feed flagged `is_starter_template` was dragged onto another feed on live and
deleted by the merge (merge deletes its SOURCE feed). Diagnosis, the code guard
that now prevents a recurrence, and the recovery levers: **FIX-PROGRAMME
2026-07-22 "prod incident" entry**. Runnable diagnostic + repair queries (with
placeholders): `docs/audits/starter-feed-repair-2026-07-22.sql`. Open:

1. ~~**PROD REPAIR**~~ — **APPLIED on prod 2026-08-10 (operator, DB-only, no deploy).** New template feed `a934368a-369c-4f60-b05d-a2e83d3fb13c`, owned by `billyisland`, `hidden = true`, 72 sources copied verbatim from the working feed `c1a4965a-…` in one transaction (feed + `feed_sources` + a no-op `external_subscriptions` upsert), verified as the ONLY flagged feed before commit. Seeding is live again; it had been dead since 2026-07-22, with `SELECT count(*) FROM feeds WHERE is_starter_template` returning **0** and every signup falling through to the client's empty `DEFAULT_FEED_NAME` mint. Deliberate choices: a **separate** feed rather than flagging the working one (so editing your own feed never silently changes what new members receive — it is a snapshot, and re-copying is a manual act), `hidden` so two identically-named feeds don't sit side by side in the operator workspace (seeding ignores `hidden`, and `cloneFeedForOwner` doesn't copy it, so members get a normal visible feed), and the template keeps the name "Billy Island's demonstration feed" — **note that name is cloned verbatim and is what every new member sees on first login** (operator's call, 2026-08-10; changing it is one UPDATE).

   **Two of this item's own premises did not survive contact with the data — do not re-derive them from the prose below.** (a) *"The merge target is over-stuffed and blows the 10s `statement_timeout`"* — **false as of 2026-08-10**: the largest operator feed held 71 sources, no foreign `created_at` cluster, and the §1d timing shape ran in **2,796 ms** against 90k `feed_items`. There was nothing to trim, and step (a) was skipped entirely. (b) *"rebuild from a pre-incident clone"* was available but **not** the route taken — `evanlake`'s 56-source copy is a genuine surviving clone (single `created_at` cluster on its owner's signup date, `cloned_from_feed_id` nulled by migration 114's FK exactly as this item predicts), but it is a 2026-07-07 snapshot, 16 sources behind the working set. Kept as evidence, not used.

   **Watch the timing number as the corpus grows.** 2.8s is a pessimistic reading (an unbounded `COUNT` where the real read is cursor-paged), but a 72-source feed against 90k items is the thing every new member now inherits, and both sides of that join grow. Re-run the §1d shape before the beta opens.

   **SUPERSEDED IN PART, 2026-08-11 — the separate-snapshot choice is reversed for the pre-launch month.** Operator ruling: while the beta holds no genuine members (only the operator's friends on test accounts), building the welcome mat is itself ongoing work, and a snapshot means every improvement reaches new members only when someone remembers to re-copy 72 rows by hand — with the two feeds drifting silently in between. So the flag moves onto the operator's **working, visible** feed and the hidden snapshot is deleted: one feed, edited live, cloned as-is by whoever signs up next. Recipe (survey → move the flag in one transaction → dispose of the snapshot **through the UI**, because a raw `DELETE FROM feeds` strands the derived subscriptions the H6 teardown exists to drop): `docs/audits/starter-template-live-mode-2026-08-11.sql`, whose Part 4 is the revert. **What the reversal costs, stated so it is a choice and not a discovery:** there is no staging left — a half-finished edit is what the next signup clones, and a removed source leaves every future clone at once. **What it buys beyond the live edit:** a flagged feed cannot be deleted or merged away, so the operator's daily-driver feed becomes the one feed on the site that cannot be destroyed by accident. **Revert when the beta opens** — that is the day the snapshot rationale above becomes correct again, and Part 4 restores it. Note also that the clone is taken on first WORKSPACE LOAD, not at admit time, so a member admitted today who first signs in next week gets next week's feed.

   *(2026-08-07: this repair no longer has a hidden second effect. Until then
   the destroyed template was the only reason the first-session import offer
   still appeared — it fired on the zero-feeds branch — so landing (b) would
   have silently switched that surface off. The welcome sheet now gates on
   `accounts.onboarded_at`, which is independent of feed state, so (b) is once
   again only about the template. See §3.3.)*
1b. ~~**`cloneFeedForOwner` never writes `external_subscriptions`**~~ — **FIXED 2026-08-11** (FIX-PROGRAMME 2026-08-11). The clone now upserts the derived subscription for the NEW owner and revives the source (`is_active`/`orphaned_at`) exactly as `addSource` does, with two deliberate departures recorded in the code: no `feed_sub` advisory lock (seeding runs only for a zero-feed owner under its own per-owner lock, so there is no teardown to race, and a second lock would add a deadlock surface for nothing) and no fetch job (the source already polls for the template owner; one per clone would stampede the worker on every signup). Pinned by the DB-backed `gateway/tests/feed-clone-subscriptions.test.ts` — including the failure mode end to end: the operator drops their own subscription and the member's clone must still hold the source open under the GC's own `NOT EXISTS` predicate. Original finding, kept because the reasoning is the reason the test looks like it does: It inserts `feeds` + `feed_sources` and stops (`gateway/src/routes/feeds/crud.ts:102`), bypassing `addSource` — the exact thing this item's own repair prose flags as REQUIRED for hand-written rows, except here it is the **code path, on every signup**. Now that seeding is live again (item 1), every new member gets a feed whose external sources carry no subscription row for them. Two consequences. **The latent one is data loss:** `external-sources-gc`'s orphan test is global (`NOT EXISTS` any subscriber), so these sources survive only while the *template owner* keeps them in one of their own feeds; the day the operator removes one from their last feed, `removeSource` drops the only subscription, the GC orphans it, and past the 90-day cull window hard-deletes it — and `feed_sources.external_source_id` is `ON DELETE CASCADE`, so it vanishes from every member feed that cloned it, taking its `external_items`/`feed_items` with it. A member loses content because the operator tidied up. **The immediate one:** a cloned member is not in the external follow graph for those sources (the same row is the graph per the feed-derived-subscriptions invariant), so kind-3 publishing and author-card "Following" state both under-report them. Fix is the upsert the repair playbook already spells out, inside `cloneFeedForOwner`'s transaction, keyed on the NEW owner. **Test it against a real DB** — the invariant is a cross-table one a mocked `pool.query` cannot evaluate.
2. ~~**`DELETE /feeds/:id` has the same hole**~~ — **FIXED 2026-08-10** (FIX-PROGRAMME
   2026-08-10). Refuses with the merge guard's own 409 `starter_template_source`.
   Two details worth keeping: the check sits **before** the `removeSource`
   teardown loop, since a refusal raised after it leaves the template alive but
   stripped of the subscriptions its clones depend on; and the `DELETE` carries
   `AND is_starter_template = FALSE` so a hand-flag landing between the check and
   the write fails closed rather than deleting, reporting 409 instead of a
   misleading 404. Pinned by `gateway/tests/feed-delete-starter-guard.test.ts`,
   whose flagged-feed case is an **ordering** assertion — mutate the early guard
   away and the status code stays 409 (the backstop catches it) while
   `removeSource` has already run, which is the failure the test exists to catch.
3. **Nothing surfaces which feed is the template (product call).** The flag is
   set and read only by hand (`UPDATE feeds SET is_starter_template = …`); no UI
   marks a template vessel, so it looks exactly like an ordinary feed right up
   to the point of destroying it. The guard now catches the merge path, but a
   badge on the vessel bar — or an operator row in the admin dashboard — is what
   would have prevented the gesture. Decide scope.

   **The 2026-08-11 live-mode ruling changes what this item is for.** It was
   about the destroying gesture, which both guards now refuse. Under live mode
   the template is the operator's daily-driver feed, so the unmarked state is no
   longer about deletion at all: **every ordinary edit now ships to the next
   signup, and nothing on the surface says so while you are making it.** A
   badge would now be telling the operator what an edit *means*, not what a drag
   would destroy — a different and better argument for the same small feature.
   Scope it as one marker in `FeedComposer` (it already renders the flag's 409
   copy on refusal, so the string exists), not an admin-dashboard row.
4. **The 10s timeout is a feed-size cliff, not a merge bug (MED, latent).** Any
   feed accumulating enough sources hits it — `sourceFilteredItems`'s `matched`
   CTE joins `feed_items × feed_sources` under a polymorphic `OR` with a
   correlated `EXISTS` per pair for `tag` sources, and a `reach:explore` row
   widens candidates platform-wide. A feed built up over months by ordinary
   use reaches the same state with no merge involved, and the only symptom is
   `COULDN'T LOAD FEED`. Wants either a source-count ceiling with a real error,
   or the `matched` rewrite (per-type UNION branches instead of one OR chain)
   — measure first. Related: §8's performance cluster.

## 0m. 2026-07-24 commit audit — Jul 22–24 window (columnar floor, owner dashboard, closed beta, brand)

Four-agent adversarial review of the window's 25 commits (`48913a2`…`d599d76`) plus the mechanical gates (drift guard, ledger adjacency, hairlines, root lint, `next build`, all test suites). The load-bearing invariants verified clean: no closed-beta signup bypass on any path, all `/admin` routes guarded with ledger-view money numbers, waitlist enumeration-safety matches WAITLIST-PRIVACY-NOTE, disc geometry numerically exact across all instances, schema/migration 162 hygiene green. **The 1 HIGH + 7 MEDIUM + the cheap LOWs were FIXED same-day — see the FIX-PROGRAMME 2026-07-24 audit-fix entry** (resize no-op/abort guard + regimented stamp protection; parked-vessel listener gating; horizontal pull-vs-floor-pan disarm; waitlist envelope + email cap; `trustProxy: 1`; one-home admin identity; saves-cursor clamp; `prefer-const`; `/waitlist` register; overview holding-warning dial; unhide-revert guard; `\` mid-resize guard; Google deleted-account label; starter-guard `FOR UPDATE`; config-PATCH transaction + rowCount; signup-gate test; stale comments; the literal-NUL memo-key bytes that made `WorkspaceView.tsx` read as binary). Deferred residue:

1. **Invite "Log in to accept" is a dead-end for logged-out invitees (LOW, now the only affordance):** `web/src/app/invite/[token]/page.tsx` links `/auth?mode=login&redirect=/invite/<token>` but `/auth` ignores `redirect` and magic-link verify always lands `/reader` — the invitee must rediscover the invite email. Fix shape: the tribute-claim sessionStorage stash, replayed after verify. (ADR-acknowledged pre-beta; Phase 3 made it the sole path.) **Unchanged by the 2026-07-26 register sweep** — that page was rewritten onto `PublicShell`/`PublicVessel`, but only its chassis moved; the round trip is still broken.

**New (2026-07-26 register sweep — `LOGGED-OUT-REGISTER-ADR.md` §IX "still open"):**

7. **`/pub/[slug]/*` — the register decision, plus two real bugs (MEDIUM).** That subtree has its own `layout.tsx` mounting `PublicationNav`/`PublicationFooter`; it is a publication's branded space, not an all.haus surface, and wrapping its children in our ⊔ would put three sets of chrome on one screen. Three ways to go and it is a product call: leave it a separate register (then that chrome needs a design pass of its own and our row should probably be suppressed on `/pub/*`), fold it in, or split the difference — publication chrome on the reading surfaces, ours on the transactional ones, on the grounds that taking someone's money is all.haus's business and should look like it. **Two bugs are real whichever way it goes:** `/pub/[slug]/subscribe` renders the monthly and annual prices and then stops — **there is no CTA and no handler in the file** — and `/pub/[slug]/masthead` sets its `h1` in `font-sans text-3xl`, the same face error `/auth/verify` and `/tribute/claim` had. (Also there: `text-grey-500`, another undefined grey that resolves to nothing.) → ADR §VI.
8. **`/privacy`, `/terms` and `/community-guidelines` do not exist as routes (MEDIUM, and a beta gate).** The orphaned `Footer` that linked to all three is deleted, which removes the dead links but not the requirement — the documents exist (the regulatory push produced the ToS and moderation policies) and publishing them is a logged-out-surface obligation, not a design task. Relates to §2.6 (waitlist privacy counsel), which is the same class of gate. → ADR §IV tranche 4.
9. **`/about` is fitted, so it scrolls inside its vessel (LOW, design call).** Fitted vessels make length expensive: everything past the first screenful sits behind a scroll inside a frame. The rewritten About is four cards and will scroll on a laptop. Worth deciding whether About should be cut to fit one screen, or whether it should be the one `PublicShell` page that scrolls. → ADR §V.
10. **`/write`'s three `animate-pulse rounded` skeletons (LOW).** The last home of the pattern `IndeterminateSlab` replaces, plus an off-palette colour. A logged-in surface, so deliberately out of the sweep's scope — a workspace pass of its own, batched with §7. → ADR §VII.
11. **`PublicNavRow` breaks below ~420px (MEDIUM — register-wide, every public route).** Measured 2026-07-26 at 390×844, light, logged out — the first time the row has been in a browser at phone width, and the sweep shipped without one (ADR §IX still-open 1). The row lays out at its desktop measure and the labels are too long for the width, giving three faults with one cause: the CTA is **65px tall inside a 56px row** ("Join the waiting list" wraps to two lines, so it overflows `NAV_ROW_H` top and bottom); "Log in" also wraps to two lines; and the CTA's right edge and the lockup's left edge are **both at x=243**, touching with zero gap. `LayoutShell` mounts the row on every non-workspace route, so this is not a landing-page bug. Fix shape is a design call between three: shorten the labels at mobile width, move the secondary "Log in" into the lockup's menu, or let the row grow taller on mobile — the last changes `--ah-row-band` and therefore every public page's clearance, so it is the invasive one. Deliberately not attempted with the mobile-vessel change, whose brief was the vessel. → ADR §IX still-open 6; FIX-PROGRAMME 2026-07-26 (landing shots + mobile chassis).
2. ~~**1.5px input borders on `/auth` + `/waitlist` (LOW, register-wide design call)**~~ — **DONE 2026-07-26.** Decided register-wide rather than patched per page, which is what the item asked for: **a field is a card with a 4px slab under it, and focus is the slab going crimson** (`web/src/components/public/Field.tsx`). Nothing is boxed, so the "enclosure ≥ 2px" floor doesn't arise; `1.5px solid grey-200` was the last border under 4px anywhere in the app and both pages were rewritten onto the primitive along with every other logged-out surface. → FIX-PROGRAMME 2026-07-26; `LOGGED-OUT-REGISTER-ADR.md` §III.
3. **Admin config changes have no durable audit trail (LOW):** `PATCH /admin/dashboard/config` logs adminId + old/new via pino only — the trail lives as long as process-log retention, on a surface that edits `platform_fee_bps`. Fix shape: a small append-only `config_audit` table written in the same transaction.
4. **Admin trigger proxies misreport long cycles (LOW):** the settlement/payout trigger proxies abort at 60s and render "Payment service unreachable" while the cycle keeps running server-side; double-run is defended (row claiming) but an operator may re-click a succeeded run. Fix shape: fire-and-poll instead of hold-open.
5. **PullToRefresh polish (pre-existing debt, batch with the palette sweep):** the `label-ui text-grey-400` indicator is hard-coded on a themed (possibly dark) vessel interior instead of `palette.cardMeta`; the resize grip (`role="button"`) has no `tabIndex`/`:focus-visible` so resize is pointer-only.
6. **`trustProxy: 1` prod verification (deploy note):** shipped so per-IP rate limits key per visitor behind nginx (one trusted hop; XFF appended entry). On the next prod deploy, verify gateway logs show real client IPs and that the waitlist 429 no longer trips globally. Dev direct-connection behaviour is unchanged.

## 0n. 2026-07-30 commit review — Jul 27–30 window (waitlist/admin, landing register, funds segregation, docs)

Four-reviewer pass over the window's 34 commits (~14.5k insertions, 78 files), findings re-verified before filing; the repo's own gates were run independently at HEAD (tripwires, ESLint, both money suites with the DB-backed tests genuinely running). **Everything act-now or flip-blocking is already FIXED** — the three act-now findings 2026-07-30 morning (the resolving `bergqvist.bsky.social` handle; the digest's clock-shaped cold-start window; the four clock-rotted red tests), then the flip blockers the same day (`3ef3e5cf` zero-net payout wedge, `d8df2fbe`/migration 166 `allocated_draws` comment sign, and the migration-164 sizing recipe dispositioned MOOT — 164 applied cleanly on prod 07-29 10:12 UTC so its gate has already passed, and fresh installs boot from `schema.sql` with 164 pre-seeded), then the hygiene batch (`files.zip`, the raw `rgb()`, the segregation ADR header, the 162/163 timestamps, §XI.5 + Export CSV re-queued at §3.7). See FIX-PROGRAMME 2026-07-30. What remains is the small tail, none of it urgent:

1. **Webhook parent-table-mismatch early-returns are silent (LOW, payments):** nine re-keyed handler sites return early on a `ref_table`/parent mismatch with no `logger.warn` — a mis-keyed event vanishes without trace, the exact failure mode the window's own commits argue against. One log line per site.
2. **`PUBLICATION_PAYOUT_COMPLETE_SQL` cannot complete a historical `'initiated'` parent (LOW):** the rule completes from `pending`-free state; a parent already `'initiated'` under the pre-fix predicate stays put. One read-only prod count (`SELECT count(*) FROM publication_payouts WHERE status = 'initiated'`) settles whether any exist; bundle with the Stripe session (attack order 1).
3. **Probe-harness fidelity (LOW, matters only when §5 step 0 runs):** `segregation-probes.ts`'s live-key refusal checks `sk_live_` but not `rk_live_` restricted keys, and its charges use `automatic_payment_methods` where prod pins `payment_method_types: ['card']` — a fidelity gap in a harness whose whole point is fidelity. Fix before running the probes, not before anything else.
4. **A crashed admit wedges the waitlist row forever (MEDIUM, admin):** `admin-dashboard.ts` releases the admit claim only on a *caught* exception; a process death mid-admit leaves `admitted_at` set with `admitted_account_id` NULL, and every later click 409s with advice ("reload") that can never help. Needs a stale-claim reclaim (age-based) or an operator release affordance. At one-operator scale the workaround is one UPDATE, which is why this is not act-now.
5. **Landing self-conformance (LOW, batch with the next landing pass):** the reader-floor demo breaks three geometry rules the same window canonised (gutter ≠ wall; interior pad thinner than its own wall; ⊔-with-bottom-wall — `globals.css` §1d reader-floor block); the Halcyon spine is enumerated three ways of which two are wrong (it appears six times, not five); `ReaderDemo.tsx`'s gate rules are `2px` in px inside an everything-in-em file (house shape would be `0.33em`). Also: `DemoCard`/`FloorCard` duplication, a dead `CheckboxField`, and `DemoBar`'s hardcoded `-1.1em` margins coupled invisibly to §1d's CSS padding.
6. **Citation rot inside the window (LOW, docs):** `DEPLOYMENT.md:794` is cited six times across trackers/ADRs and now sits at :830 (pushed down partly by the citing commit itself); the segregation ADR §1's `settlement.ts` line anchors all drifted when parts 1–3 rewrote the file. When touching those docs, prefer quoted grep-able phrases over line numbers.

## 0o. 2026-08-03 commit audit — Jul 29–Aug 01 window (funds-segregation core, settlement + sandbox sequence, landing)

**Status 2026-08-04: items 1–9c are ALL CLOSED** (1 + 4 on 08-03 with the idempotency recovery; 2, 3, 5, 6, 8 later that day; 7, 9 and 9c on 08-04 — FIX-PROGRAMME has each). Item 9 grew a migration on the way: its premise was inverted, and closing it properly meant making the `free_allowance_pence` dial govern anything at all (migration 169); 9c then put the parity test under all nine of those dials' fallbacks. Open remainder: **10** alone (NOTEs, no action decided).

Four-auditor adversarial review of the window's 55 commits (~7.6k insertions: the three-part segregation build with migrations 165–168, the §5 sequence harness and the P0 fixes it flushed out, the landing demo recasts, the tracker churn). Independently re-run at HEAD: drift guard 7/7 green, both money tripwires green, ESLint 0 errors, 72/72 payment tests passing with the DB-backed suites genuinely running. **Verified clean, needn't be re-audited:** ledger-per-child discipline (every `postLedger` inside its child's flip txn, gated on that flip's `rowCount`; no post hangs on `completeParentIfSettled`), refs staying on the PARENT row in all three specs, the carve read-before-advance order, the one-home "no split PENDING" rule, terminal/ambiguous splits + row-stable keys in both execute loops (a re-pay mints a fresh row = fresh key), the F4 deletion (zero remaining callers; both `rollback*` helpers retain live callers), migration-168 sweep completeness (no residual publication-claim read via `writer_payout_id` beyond the stale comment in item 8), the split repay sweep's safety argument, the fb974656 guard's lock ordering (no TOCTOU — guard and `confirmSettlement` serialise on the tab `FOR UPDATE`), the 4dbecb45 card-prompt chain end-to-end (flag set only on terminal decline, cleared atomically on re-attach, wire types now match the route), 51b0d4f8's shim param forwarding + `OVERLAY_PARAM_KEYS`, the waitlist cold-start fix (epoch open, microsecond-safe, clock-proof test), the harness scripts' live-key refusals, the landing namespace/register/spine rules at HEAD (only `all.haus`-scoped identifiers resolve), the "written and unrun" steps 4–9 (all discharged green 2026-08-01), and tracker discipline across all 55 commits — including an independent re-verification of §11's "prod is fully current with `3491f345`" from the marker evidence (sound; nothing needs deploying). Findings, ranked:

1. ~~HIGH — settlement resume rebuilds the Stripe create payload per attempt, so any payload drift wedges the row AND its tab permanently under the same idempotency key.~~ **FIXED 2026-08-03** (FIX-PROGRAMME, same day): the fix shape's specific handler is built — `isIdempotencyConflict` + `recoverIdempotencyConflict` look the PI up by `metadata.settlement_id` over `paymentIntents.list` (never `.search` — index lag would fake absence) and adopt / terminal-fail / fresh-key-fail / refuse-on-truncation; the 8h stuck-pending age alert ships with it. Mutation-verified conformance battery (+8 tests, 305/305). There were THREE wedged dev specimens, not one — **all three resolved same-day with a fresh sandbox key, via the NORMAL path**: Stripe GCs idempotency keys ~24h after first use, so the aged-out keys let the retried creates through as new requests (the audit's "2½ days refutes the 24h self-heal" was confounded by the sandbox API key expiring mid-window — auth errors, not conflicts, blocked most of it). Corrected wedge model: a drift event freezes the tab until Stripe drops the key (≥24h, at 3 retries/day) — not forever, but a silent day-plus freeze per event; the recovery cuts that to the next resume pass and the age alert (which fired live, twice, on its first day) makes anything lingering loud. The generic bounded retry stays open at attack order 0c. *(Original finding, kept as the audit record:)* `resumePendingSettlements` re-enters `completeSettlement`, which re-resolves the default payment method via a live `customers.retrieve` per attempt (`payment-service/src/services/settlement.ts:377-379`, fb974656) and recomputes the brand-dependent `allocatedFundsParam` (`:437`, 4fe99058); nothing persists the first attempt's payload, and the row-stable key `settlement-<id>` only dedupes a byte-identical request. Three drift sources, each ⇒ `StripeIdempotencyError` on every retry forever: a deploy changing the create shape (both named commits did), the reader swapping default cards between an ambiguous attempt and its retry, a brand-eligibility flip. The error is (correctly) classified ambiguous (`charge-errors.ts:24-29` names it as a retry case), so the row stays `pending` with no PI id — and the fb974656 in-flight guard's pending arm then **blocks all future settlements on that tab**, `card_action_required_at` never set, reads stuck `accrued`, writer unpaid, silently and forever. No attempt cap, no age alert, no admin surface; the only remedy is manual SQL. Live specimen: dev settlement `e14ab9c7…` (created post-fb974656/pre-4fe99058), erroring on every payment-service start for 2½ days — which also refutes "Stripe's 24h key expiry self-heals it". This is the exact frozen-tab chain 4fe99058 fixed for the 500 case, reintroduced through the idempotency channel; prod exposure today is narrow (no pre-fb974656 settlement ever left a long-lived pending row) but the card-swap path is live now and every future payload edit re-arms the deploy-transition case. **Fix shape:** `StripeIdempotencyError` is uniquely *resolvable* ambiguity — the error proves a first request was recorded, and the PI carries `metadata.settlement_id`. Handle it specifically (search PIs by settlement id; found → adopt + complete; not found → the create made nothing, so rotating the key suffix or failing + flagging the card is safe), or persist the resolved payment method + brand on the row at first attempt and replay verbatim. Either way add a stuck-pending age alert. Strengthens attack-order 0c, which see.
2. ~~HIGH — `syncAllocations` re-sync double-counts every draw, decaying budgets and permanently tripping the divergence alert.~~ **FIXED 2026-08-03** (FIX-PROGRAMME, same day): the audit's second fix shape, tightened — the staleness arm now skips any settlement with an `allocated_draws` row of ANY kind (`SYNC_ALLOCATION_CANDIDATES_SQL`; once drawn, the budget is identity-maintained by the draws and never re-stamped), the stamp adds back refund-kind draws snapshotted BEFORE the Stripe retrieve (so a refund preceding first sync isn't double-counted either — the finding's refund arm — with the ordering and kind-filter chosen so every race errs to under-draw), and the §3.6 divergence candidates now rank drawn charges FIRST by draw recency (their `allocation_synced_at` is frozen, so the old sync-recency order would have rotated exactly the charges the check exists for out of its window). +9 tests (5 DB-backed running the production SQL, 4 mocked pinning the TS composition), four mutants each killed; 314/314; tripwires green; ADR §3.3a carries the correction block. *(Original finding, kept as the audit record:)* `settlement.ts:1499` (80dcd714) re-stamps `tab_settlements.allocated_pence` with Stripe's **current remaining** balance once `allocation_synced_at` ages past the freshness window — with no exclusion or rebase for existing `allocated_draws`, while the budget everywhere else is `allocated_pence − Σ draws` (`payout-children.ts:131`, `allocation-reconcile.ts:114`). Sync 5000 → draw 2000 (Stripe remaining 3000) → 24h re-sync stamps 3000 → budget reads 1000: the draw subtracted twice. Money-safe (under-draw by construction) but coverage-destroying — drawn charges look exhausted and units route to the unsegregated residual for a reason nobody will trace here — and `checkAllocationDivergence` (`allocation-reconcile.ts:143`, 83667c08) computes `our = allocated_pence − draws` vs Stripe's remaining, so every drawn-then-resynced charge diverges by exactly Σ draws forever (tolerance 1p): the alert channel goes permanently red, burying the real signals (unrecorded refund, missed reversal) the module's header says it exists for. Same double-count hits `refund` draws once the charge re-syncs post-refund. The §5 sequence never crossed a 24h re-sync of a drawn charge, which is why steps 2/3/10/11 ran green.
3. ~~MEDIUM — the child-path parent-`reversed` flip can strand a pending sibling child forever (all three cycles).~~ **FIXED 2026-08-03** (FIX-PROGRAMME, same day): the first fix shape, given one home — `REVERSAL_OUTSTANDING_SQL` (exported, parameterised on `parent_table`, replacing the three inline copies) counts `'pending'` children as standing, so the parent stays `pending` and the resume sweep still owns the stranded sibling; `'failed'` stays excluded in both directions (nothing moved and, units released, nothing will). The transfer-reversal mock's tally route now parses the status list out of the SQL rather than restating it (the §0h.1 mis-modelling trap, pre-empted). +3 tests (1 DB-backed on the production statement, 2 mocked on the handler), mutation-verified — reverting the list turns both layers red; 317/317. *(Original finding, kept as the audit record:)* the reversal paths tallied `outstanding` over children `status IN ('completed','reversed')` only: child A completes; crash before child B executes; A is fully reversed (manual, 2am) before the next cycle → `outstanding` = 0 (B's pending net uncounted) → parent flipped `pending → 'reversed'` → the resume sweeps scan `status = 'pending'` parents only → B and its claimed units frozen permanently, and the parent's amount restatement never runs. Splits are immune to the stranding (one child by construction) but shared the missing guard.
4. ~~MEDIUM — the in-flight guard's "bounded, never frozen" comment is false for its pending arm.~~ **RESOLVED 2026-08-03 with item 1**: the comment now states the true bounding (reconcile bounds the completed arm; the pending arm is bounded by resume *succeeding*, with the conflict case recovered specifically), and the 8h stuck-pending alert makes any persistently-ambiguous freeze loud instead of silent — proven live the same day, when the expired dev sandbox key (`api_key_expired`, ambiguous, correctly retried) tripped it on all three specimens. The freeze itself, for a *non-conflict* persistent error, remains until the error clears or 0c's bounded retry is decided — that residue lives at attack order 0c, not here.
5. ~~MEDIUM (docs) — the segregation ADR's header is frozen again, in exactly the way its own warning names.~~ **FIXED 2026-08-03** (with §0o.2's docs pass): the "Build status" header now records §5 steps 0–11+13 green (step 0 closed 07-30, dials on recorded placeholders until payouts flow, step 12 on the Dial-A rework) and names the two live flip gates (Mastercard escalation per attack order 0b + live enablement); §10's intro line rewritten the same way; the header's self-warning parenthesis now records that it has frozen twice.
6. ~~MEDIUM (docs) — `DEPLOYMENT.md`'s `STRIPE_ALLOCATED_FUNDS` row still asserts the premise the 08-01 run falsified.~~ **FIXED 2026-08-03**: the flip instruction now states the falsified premise out loud (asking for allocation on an ineligible brand 500s → ambiguous → wedged tab), credits the `ALLOCATION_ELIGIBLE_CARD_BRANDS` pre-flight as the thing that makes "handled" true, gates the flip on the Mastercard escalation, and carries the two attack-order-0b operator warnings (re-measure-and-restore brands on Stripe's fix; never baseline `allocated_residual_alert_bps` during the workaround period).
7. ~~LOW batch — payments~~ **ALL THREE FIXED 2026-08-04** (FIX-PROGRAMME): (a) the compensating draw now returns principal + the fee prorated via `prorateCarveReversal` (the §5-validated cumulative-floor shape), so a fully-reversed child makes its charge whole; a flagless manual reversal now surfaces as a positive §3.6 delta instead of a silent permanent under-count (runbook bullet rewritten). (b) the resume sweep's no-customer branch takes the no-default-card disposition: `failed` + `failure_reason` + `card_action_required_at`, one transaction. (c) `confirmSettlement` adopts a crash-orphaned row via the webhook's `metadata.settlement_id` — IS NULL-guarded, rowCount-checked under the tab lock, wrong-owner-proof. +4 tests (1 DB-backed, 3 battery), three mutants each killed, 321/321; the exercise also caught the transfer-reversal mock handing out live objects (the copies-not-live-objects corollary) — fixed both sides.
8. ~~LOW batch — docs/trackers~~ **ALL FIVE FIXED 2026-08-03** (with the §0o docs pass): (a) ADR §5 gains the 2026-08-01 result banner (steps 4–9 seven-of-seven with the tallies, the step-9 flip-blocker story, and the "§5 is now green through 11 + 13" status) above the 07-31 banner; (b) §2's brand bullet's close now records the Mastercard question as asked-and-answered (Stripe's bug, escalated; the open remainder is only network-vs-brand eligibility) and carries the don't-baseline-during-workaround warning; (c) both discharged §11 instructions struck with their answering evidence cited (payment image at `3491f345` ⊇ `fb974656` by the 08-01 marker grep; web image built 07-31 ⊇ `8129b3de`); (d) the 0c deployment note now records both halves deployed and keeps the flip-precondition as a property to preserve; (e) `payout.ts` claim docblock corrected to `publication_payout_id` (migration 168).
9. ~~**LOW batch — web**~~ — **BOTH FIXED 2026-08-04** (FIX-PROGRAMME), and (a) was **backwards as filed**. The finding read "retune the dial and the gauge denominator misreports"; in fact retuning `free_allowance_pence` **changed nothing at all** — both account INSERTs hardcoded the grant, the column carried `DEFAULT 500`, and `config.freeAllowancePence` had zero consumers anywhere. The dial was inert, the second time that has happened to these dials (the first is in `loadConfig`'s docblock). The filed fix — route sends the config value — would have made the gauge display a number no operator could move. Shipped instead: **migration 169** stores `accounts.free_allowance_granted_pence` (what a reader was *gifted*, stamped from the dial at signup, never restated; backfill exact at 500 since both INSERTs and the column default were 500 for the table's whole life), the two grant sites now read the dial into both columns via one placeholder, and the two display sites read the reader's own grant — `/my/tab`'s total and the account statement's `500 AS amount_pence` starting-credit literal, found in the same file while mutating. Storing the grant is what makes the dial safe to retune: showing the live dial would tell a reader gifted £5 they had been gifted £7.50, restating the one fact the free-allowance invariant says is never revisited. +6 gateway tests, four mutants each killed; drift guard 7/7. (b) the `globals.css` "No hairline hrs" comment carries its `hairline-ok` marker + reason.
9c. ~~**LOW — `loadConfig`'s money-dial fallbacks are not parity-tested.**~~ — **FIXED 2026-08-04** (FIX-PROGRAMME). `shared/tests/config-fallback-parity.test.ts` is the third of the parity trio, same `diffAgainstDefaults` shape as the feed-rank and resonance twins, driving the **real** loader against an empty table (pool stubbed at the one SELECT, so it stays a no-DB suite test). All nine dials agree with the file today. Four tests — the parity diff, a seeded row beating its fallback, the `isNaN` arm, and a **completeness pin** that greps `int(map, '…')` out of `client.ts` so a tenth dial cannot ship unchecked — and **five mutants each killed**, including a drifted value in the SQL file itself, which is what proves the test reads the real file rather than a fixture. Docblock amended in the same commit: "six dials" was stale at nine, and "keep the two in step" now names the test that holds them there.
10. **NOTEs, no action decided:** the writer/tribute packer's funding pool is narrower than ADR §3.3b's "fall back to any charge with room" — it locks only the claimed units' *preferred* settlement ids, so a unit whose own charge lacks room falls to the residual even when unrelated charges hold budget (deliberate-looking lock-set bound; document the narrowing in the ADR or widen at the §3.3d measurement); Connect onboarding's `refresh_url` lands on settings without regenerating an account link (51b0d4f8's own "breadcrumbs" note — queued tail, strictly better than the 404 it replaced); the shared demo article's excerpt diverges from the reader pane's opening after sentence one (`OmnivoreDemo.tsx:116` vs `ReaderDemo.tsx:216` — the pairing comment says rewrite both); `page.tsx:86-87`'s SR description promises "a photograph of a city skyline" the abstract `DemoPhoto` can't depict; §0n.5's "six times, not five" Halcyon count is itself stale after fc8ca95e — re-count when that batch is picked up.

## 0p. 2026-08-05 commit audit — Aug 02–04 window (§0o fix batches, free-allowance dial, small-money batch)

Audit of `88ed1e5a..1ce113e3` (13 commits). Baseline re-measured at HEAD before
anything was read: payment **324/324** with the DB-backed suites genuinely
running, shared 117, gateway 446, drift guard 7/7, both money tripwires green.
**Three MEDIUMs found, all FIXED 2026-08-05 (migration 172) — see the
FIX-PROGRAMME 2026-08-05 entry** (adopted settlements left `pending` and
re-driven into a false card flag; the free-allowance literal surviving in the
statement's *second* query; the grant notification being unlabelled, unlinkable
and dedup-collapsed).

**Verified clean, needn't be re-audited:** `recoverIdempotencyConflict` end to
end (the create stamps both `customer` and `metadata.settlement_id`, so the
list-match is sound; `list` never `.search`; window bounded by the row's own
`created_at − 1h`; `MAX_PAGES` exhaustion leaves the absence unproven;
truncation changes nothing; the dead-PI arm mirrors the terminal disposition; a
failed immediate confirm defers to webhook/reconcile). §0o.2's first-sync arm —
`lockFundingSources` refuses `allocated_pence IS NULL`, which is exactly what
makes "pre-first-sync draws are refund-kind by construction" true, so the
unconditional NULL arm is safe; refund-only add-back and snapshot-before-retrieve
both err to under-draw as claimed. §0o.7a — `prorateCarveReversal(fee, net, 0,
net) === fee`, so a fully-reversed child returns principal + fee and makes its
charge whole, and the cumulative floor telescopes so staged partials cannot
drift; the draw is correctly negative and accumulates (vs the refund draw's
`GREATEST`, right for Stripe's cumulative `amount_refunded`). §0o.3 —
`REVERSAL_OUTSTANDING_SQL` covers three of the four statuses in the table's
CHECK, `failed` excluded deliberately and correctly. `subscription-period.ts` —
month-index arithmetic before the clamp, one `setUTCFullYear(y,m,d)` so no
intermediate 31-Feb normalises away, `daysInUtcMonth` via day-0-of-next-month,
anchor clamped 1..31, annual leap case does not walk; all four write paths plus
the renewal worker and `seed.ts` supply the anchor; migration 170's
`current_period_start` backfill matches its stated reasoning. The redeem page's
401 arm (login link with redirect back, grant-specific copy).

**Open remainder:**

1. ~~**`idx_notifications_dedup` lost migration 019's effect to `schema.sql`,
   and nothing can see it**~~ — **DONE 2026-08-05, both halves** (migration 173
   + drift-guard Check 3b; full record in FIX-PROGRAMME). The partial clause is
   restored, 019's *other* change (`COALESCE(actor_id, …)`) deliberately is not
   — it would collapse `pledge_fulfilled` to one notification ever across every
   drive — and Check 3b now compares index **definitions** by executing the
   migration's own CREATE under a probe name and diffing `pg_get_indexdef()`.
   Both mutation-verified. **Migration 173 is pending prod deploy** (§11).

   Two things worth carrying forward. **(a) The guard's new check cannot see the
   bug that motivated it, and that is correct** — 3b compares the LAST surviving
   CREATE, and migration 172 rebuilt the index from the live definition, making
   the live form the chain's own answer. It would have been red for the whole
   019..172 window; it guards the next one. **(b) Definition drift stays
   indexes-only on purpose.** An index is created whole and never ALTERed, so
   its migration CREATE is still its whole definition today. A table's is not —
   `CREATE TABLE` accumulates later ADD COLUMN / ALTER TYPE / ADD CONSTRAINT —
   so the same comparison would false-positive on every table the chain has ever
   touched. Column-level drift therefore remains the guard's residual gap,
   backed only by the pg_dump-and-re-append discipline.
2. **NOTE — migration 170 is not backward-compatible, and §11's deploy
   instruction reads as though it were.** `period_anchor_day` is `NOT NULL` with
   no default (deliberate and well argued: a default would let a new write path
   silently inherit "the 1st"). But old gateway code INSERTs into
   `subscriptions` without the column, so between `migrate` and the new image
   going live, subscribe / reactivate / comp **500s**. §11's entry carries
   "Ordering matters here and is safe in one direction only… Migrate first, then
   build", inherited verbatim from migration 169's entry where it was true
   *because* that column had a DEFAULT. Migrate-first is still the right order
   for 170 — it is simply not a no-impact window. Near-zero exposure on a closed
   beta with no subscription volume, and it has already passed; the point is to
   stop the rule being generalised. **A NOT NULL column with no default makes
   the deploy window breaking in BOTH directions, and the entry should say which
   kind it is.**
3. **NOTE — `subscription-convert.ts:140` computes its period end in local
   time. SHARPENED 2026-08-08 (§0q review): the consequence is a mischarge,
   not a drift.** `new Date(now.getFullYear(), now.getMonth() + 1, 1)` is the
   local constructor. East of UTC, "first of next month" is 23:00Z (or
   earlier) on the LAST day of the current month, stored against
   `period_anchor_day = 1` — and `advanceSubscriptionPeriod` then renews
   31 Aug 23:00Z → 1 Sep 23:00Z: **a one-day period charged at a full
   month's price**, correct only from the period after that. (This entry's
   earlier "self-corrects having run ~1 day long" understated it — the
   arithmetic was traced end-to-end in the §0q pass.) Still latent: no `TZ`
   in `docker-compose.yml` so containers run UTC today, and the route is
   flag-gated off (`SUBSCRIPTION_CONVERT_ENABLED`, §0e item 4). **Fix when
   reviving the route:** don't patch the constructor in place — add a
   `startOfNextUtcMonth()` export to `shared/src/lib/subscription-period.ts`
   (the declared one home for period arithmetic) and call it; the comment
   `a94947ca` added defends the anchor *semantics* while the expression under
   it sits outside the home, which is how the constructor went unnoticed.

---

## 0q. 2026-08-08 commit audit — Aug 04–08 window (perimeter W2/W4, parity probe, publications surface, publisher extraction)

Review of `a2355cd4..a574b7e8` (36 commits; the Aug 02–04 tail overlaps §0p and
was not re-litigated — §0p's dispositions stand). Method: the window read in
five clusters, every HIGH re-verified by hand at the cited lines, the
publications defects reproduced against the live dev stack (curl + live
payloads, not code-reading alone), and the adjacent suites run: payment 27 unit
+ 15 DB-backed green with migration 175 applied, gateway article-publisher 8/8,
hairline tripwire clean across the pub surface. The window's structural work is
sound — the invariants each commit claims were checked and hold (clean list at
the end). Six defects, one on a money path, plus a consolidation batch.

1. ~~**HIGH (money) — the per-account payout halt is bypassed by the two
   resume sweeps.**~~ — **DONE 2026-08-08, same day, both halves (full record
   in FIX-PROGRAMME).** The shipped shape deliberately diverged from the fix
   proposed here: not a loop-level `isAccountHalted` continue but the
   exclusion **in the sweeps' SQL** (exported `WRITER_PAYOUT_RESUME_SQL` /
   `TRIBUTE_PAYOUT_RESUME_SQL`), for the W4 test's own reason — a loop check
   is only testable against a mock answering from a fixture, while the SQL is
   run by the DB-backed suite with a paired control. The batched tail went
   further than proposed: the split loop's halt flag now rides its accounts
   SELECT, which orphaned `isAccountHalted` entirely — deleted, so
   `haltedAccountExclusionSql` is the predicate's ONE home at all six
   transfer-choosing sites. +3 tests, mutation-proved both halves. Deploy:
   payment-service image only.

2. **MEDIUM — the overlay masthead renders "· undefined" after every member's
   role.** `PublicationPanel.tsx:355` reads `m.contributor_type`, which the
   masthead endpoint (`publications/public.ts:122`) never selects, so
   `undefined !== "staff"` appends the literal text; `:336` keys the list on
   `m.account_id`, also never selected, so every React key is undefined. The
   standalone page fixed both lines in this same commit (`masthead/page.tsx:80`)
   while the overlay copy kept the broken forms — the register drift b06787f4
   set out to end, in the file it touched. **Fix:** minimal is the
   standalone's guard + `key={m.username}`; the right one is item 8a (share
   the masthead template, as home already is).

3. **MEDIUM — `/@username` member links 404 everywhere the overlay intercept
   doesn't run.** `masthead/page.tsx:73` and `PublicationPanel.tsx:348` (and
   pre-existing `ReadingHistory.tsx:62`) build `href={'/@' + username}`, but
   the profile route is `app/[username]` with no `@`-stripping — verified
   live: `/@prospect` 404s, `/prospect` 200s. Left-click works only because
   `ProfileLink`'s overlay path strips the `@`; new-tab, copy-link and SEO
   crawl — the commit's own stated purpose for the standalone routes — get
   the 404. **Fix:** drop the `@` from the hrefs; nothing else changes.

4. **MEDIUM (latent — gates §3.5 slice 3) — the `feed_items` upsert drops a
   corrected `published_at`.** `article-publisher.ts:187` takes
   `published_at = EXCLUDED.published_at` on the `articles` conflict arm; the
   `feed_items` arm (`:234–240`) omits the column. Invisible today (the
   scheduler passes now() to both), but ARCHIVE-IMPORT-ADR §VII *designs*
   re-runs to converge through this upsert via deterministic d-tags — a
   re-run correcting a date would split the article/profile surfaces from
   every follower's feed, silently. The "writes the same instant to both
   columns" test pins only the INSERT arm. **Fix:** one line in the conflict
   list + extend that test to the conflict arm; land it before slice 3 makes
   the path live.

5. **MEDIUM — the admin overview renders the parity `unverified` state as
   fine.** `admin/overview/page.tsx:92` banners on `!parity.ok` only, while
   the API ships `parity.unverified` under a doc comment reading "never to be
   rendered as fine" (`api/admin-dashboard.ts:74`). The sticky case: a peer
   rolled back to a pre-`/auth-check` image 404s forever (`no_endpoint`,
   never definitive), so a simultaneously drifted secret is silent everywhere
   but one boot log line — on the surface built to show it. **Fix:** a
   second, quieter banner for `unverified.length > 0` with its own copy
   ("never confirmed" is a third state, per the invariant).

6. **LOW (test) — the in-transaction outbox claim is asserted by name only.**
   `article-publisher.test.ts:251` checks the enqueue's payload but never its
   client argument — the only evidence the enqueue rode the transaction. Move
   `enqueueRelayPublish` after `withTransaction` and the suite stays green
   while the repo's most-cited invariant breaks. **Fix:** assert
   `enqueueMock.mock.calls[0][0]` is the client the mocked `withTransaction`
   handed its callback.

7. **Cross-ref:** the local-time month boundary in `subscription-convert.ts`
   re-surfaced with a sharper consequence than §0p.3 had recorded — amended
   there (the first renewal charges a full month for a one-day period; the
   fix belongs in `subscription-period.ts` as the one home).

8. **Consolidation batch (simplification/muddiness — one sitting):**
   - **(a) Finish what b06787f4 started: the registers still fork past the
     homepage.** The overlay archive is a flat relative-dated `ArticleList`
     against the standalone's year-grouped absolute dates (contradicting
     `article-shared.tsx`'s own "dates are part of the record" comment); the
     overlay header hand-duplicates the block `PublicationMasthead` owns; a
     workspace member has **no path to the subscription terms at all**;
     `showNav` is never passed false; `theme_config` is fetched, typed and
     consumed nowhere. One pass: share archive/masthead the way home is
     shared, add the Subscribe affordance to the overlay, delete the dead
     prop and the dead field.
   - **(b) `PlatformConfig` is declared twice** —
     `payment-service/src/types/index.ts:12` duplicates
     `shared/src/types/config.ts:8`, and both a0867c04 and 3e1f7dc2 had to
     edit both. The parity suite pins values, not types, so the local copy
     rots silently. Delete it; import shared's.
   - **(c) `proxyToService` still has the no-footprint defect 3dc8949a fixed
     in `callPaymentService`** (`articles/shared.ts:44`): an upstream non-2xx
     reaches the browser with nothing in the gateway log. Same ~6 lines,
     moved into the shared helper.
   - **(d) On the public pub pages an outage renders as emptiness:**
     `getArticles`/`getMasthead` return `{articles: []}` on any non-ok
     response, so a gateway 500 paints "NO ARTICLES PUBLISHED YET", cached
     60s (`archive/page.tsx:33`, `masthead/page.tsx:22`, `page.tsx:41`).
     These were `notFound()` before the rework. Distinguish error from empty.
   - **(e) `contributor_type !== 'staff'`** compares against a value the
     enum does not contain (`permanent`/`one_off`, schema.sql:95) — dead
     until someone selects the column, wrong the day they do. Fix the
     sentinel when (a) wires the field.
   - **(f) Parity transition-log wording overstates what is known** when
     boot seeded `null` (`internal-parity.ts:272`): a first-ever match logs
     "RESTORED", a first-ever mismatch logs "APPEARED AT RUNTIME —
     redeployed with a different secret", sending the operator to
     reconstruct a redeploy that never happened. Branch on `before === null`.
   - **(g) `Welcome.tsx:98`** splices the `world` step in when capabilities
     resolve, so a fast clicker sees the sheet jump backwards. Unreachable
     while `FOLLOW_IMPORT_ENABLED` is dark; lock the step list at open.
   - **(h) `MediaStoreError` conflates bad bytes (a 400) with Blossom
     failure (a 500)** and the route answers 500 for both (`media.ts:90–96`)
     — the extraction minted the typed error that would let the route
     distinguish, then didn't. Also `ALLOWED_IMAGE_TYPES` sits in the bytes
     module while CLAUDE.md files the declared-MIME check under the route;
     move the constant when touched.
   - **(i) `members.ts:273`** returns a raw `flatten()` as `error` — the
     banned envelope ("[object Object]" client-side); pre-existing but
     touched territory. One line via `zodValidationError`.
   - **(j) ARCHIVE-IMPORT-ADR §II** names the extracted function
     `publishArticle`; §XII and CLAUDE.md say `publishPersonalArticle` — a
     later reader greps the wrong name. One word.
   - **(k) Owner flag, no build:** the §1.6 unconditional `article_unlocks`
     flip also makes permanent the unlocks of *charged-back* reads (the
     reader got their money back). Probably right under the gift philosophy;
     nobody has said so explicitly.
   - **(l) Pre-existing, minor:** `/pub/:slug/:articleSlug` fetches by d-tag
     alone, so a wrong-slug URL renders the article and echoes the wrong
     slug into its OG url.

**Verified clean, needn't be re-audited:** a574b7e8's extraction is
behaviour-preserving line-by-line against the pre-commit scheduler (SQL
identical up to `now()` → params; options `!== false` so an omitted option
cannot flip a scheduled publish; articles + feed_items + enqueue in one
transaction, vault seal outside, v2 swing + enqueue in the second, v2
`created_at = v1 + 1` with `baseTags` carrying the backdated tag; drive
fulfilment awaited un-swallowed before the draft DELETE); media extraction
keeps hash-verify-before-INSERT and byte-identical route status codes, and
`storeImage` has exactly one caller. The parity state machine is genuinely
sticky both ways with transition-only logging (`reprobeParity` returns before
touching state on `unreachable`/`no_endpoint`), the classifier is 401∧403 with
the peers verified to actually disagree, all three `/auth-check` routes sit
behind the peers' own pre-existing guards returning only `{ok:true}`, the boot
probe is bounded and post-`listen()`, and the /health 503 cannot cascade
(compose deps are plain-list; `unless-stopped` ignores health). W4's
attribution runs genuinely uncapped off one shared predicate constant with
default-deny fall-through to the global halt, first-writer-wins preserves
`created_at` for the 24h escalation, and the two halts stay two controls in
route, banner and DEPLOYMENT.md. W2's tri-state predicate is
`allocation_synced_at IS NOT NULL AND allocated_pence = 0`, never-synced rows
are a third figure joining neither side, empty denominators return null and
are rendered in words, and the endpoint deliberately avoids
`runAllocationReconcile`. `negative_reader_tab` is alert-tier under its own
marker with `truncated`, in `reconcile-ledger.ts` not `allocation-reconcile.ts`,
and its DB test's parity-blindness control is real. a0867c04's publication
threshold dial is bound, seeded, parity-tested with the two dials at
*different* values; `revenueShareBps` is fully gone and `PATCH /payroll`
remains the sole share writer under the advisory lock and Σ clamp. The anchor
arithmetic recovers 31 Jan → 28 Feb → 31 Mar including leap years; migration
170's backfill reads `current_period_start`; migrations 169–174 insert nothing
into `platform_config`; the notification chain ends bound-and-indexed with
`actor_id` bare and `drive_funded` can no longer abort a pledge; grant offers
are sound end to end; the settlement adopt rides `confirmSettlement`'s
transaction between the tab lock and the debit. Migration 176 is nullable with
no default (safe both deploy directions) and all three welcome endings stamp.
The publications surface is hairline-green with no raw hex and no undefined
greys, the `ArticleLink` seam respects the escape ban on every call site, and
deleting `pub/[slug]/layout.tsx` leaves no orphan references. No new
`balance_pence` or ledger writers anywhere in the window; migration 175 is
additive DDL.

---

## 1. Money & payments (highest stakes)

Waves 1–3 of the 2026-07-05 logic-economy audit all shipped (migrations 139–147): F9 paid voting removed (free, one-vote cap), F4 payout-completion-off-create + `transfer.reversed` (incl. partial reversals), F5 full publication-aware chargeback (per-split prorated reversals), F10 fixed share-of-revenue splits with cumulative clamp, F14 allowance split persisted, Wave-5 hardening (periodic `resumePendingSettlements`, calendar renewal in the expiry worker, pairing comment), plus the Wave-3 subscription collection gate. The F3 tribute chargeback residual also closed (claimed-but-unpaid accruals now reversed, `chargeback.ts:38-53`). What remains:

1. ~~**F4 residual — live-Stripe premise check.**~~ — **DONE 2026-07-30. Premise confirmed, branches deleted.** `events.list` over the segregation sandbox returned **29 `transfer.created` and 24 `transfer.reversed`, every one with `destination` set** — i.e. exactly the platform→connected shape in question — against **0 `transfer.paid` and 0 `transfer.failed`**. The created count is the denominator and the reversed count the positive control; two empty lists alone would have been indistinguishable from an event log returning nothing, so the probe counts four types and reports UNKNOWN rather than passing by default when the controls are empty (`segregation-probes.ts --f4`, read-only, re-runnable). **386 lines removed**: both switch cases plus the six service methods they were the sole callers of (`confirmPayout`/`confirmTributePayout`/`confirmPublicationSplit`/`handleFailedPayout`/`handleFailedTributePayout`/`handleFailedPublicationSplit`). A stray delivery now falls to the default arm and is logged and ignored. The two `rollback*PayoutRows` helpers survive — each still has its terminal-create-rejection caller — and the ledger-adjacency floor is unchanged at 19, because the deleted handlers posted no ledger entries: the entry rides the create-response completion path, which is the F4 design working as stated. → logic-economy Wave 3; FIX-PROGRAMME 2026-07-06 Wave-2 + 2026-07-30 entries.
2. ~~**Publication-split transfer re-pay.**~~ — **DONE 2026-07-30** (migration 167). `repayFailedPublicationSplits()` runs at the head of `runPublicationPayoutCycle`, before the resume sweep, and mints a **fresh split row** for each terminally-rejected split — the new id yields a new idempotency key, which is the whole reason an in-place retry was impossible (the key is row-stable on purpose, so a retry against the same row dedupes back onto the rejected transfer). Two new columns carry it: `attempt` bounds the retry at 3 so a permanently-unpayable destination stops costing a Stripe call and a row every cycle, and `replaced_by_split_id` makes supersession an explicit pointer rather than a match on `(payout, account, share_type)` — which would be ambiguous, since one account can legally hold two splits in one payout. The sweep reopens a completed parent (or the resume sweep, scanning `pending` parents only, would never process the row) and `PUBLICATION_PAYOUT_COMPLETE_SQL` re-closes it, so it is a loop back into the normal lifecycle rather than a special case beside it. Under segregation the replacement is re-packed: the predecessor's children were failed by `failChild`, which DELETES their `allocated_draws` row, so the budget is already back and the re-pack draws it afresh rather than double-drawing. **14 DB-backed tests, mutation-verified** (`publication-split-repay-integration.test.ts`; three load-bearing predicates each mutated and each caught) — including the safety argument the design rests on, that a replacement is counted exactly once by the distribution view, the settlement read and the F5 chargeback selection. Five SQL constants exported so the test runs production's own statements. **Not covered, deliberately:** the sweep's control flow (attempt cap, un-onboarded skip, re-read-under-lock) drives the module-level `pool` and is unreachable from a rolled-back transaction, exactly like `executePendingChildren`.
3. ~~**F1 residual — publication-subscription distribution.**~~ — **DONE 2026-07-10** (migration 152: publication sub earnings claimed via `subscription_events.publication_payout_id` and distributed through the publication pool, sub net joining post-fee; reserve converted to claim-first; F5 proration denominator gains `sub_net_pence`; earnings display folds both legs. FIX-PROGRAMME 2026-07-10; runtime verify in §11).
4. **~~Cardholder re-auth prompt~~ — DONE 2026-07-30. Stripe S2 live test still owed.** `CardActionRequired` (`web/src/components/account/`) renders on the ledger (above the balance — a frozen tab is why the numbers below stopped moving) and at the top of the settings payment section (where "Card connected" would otherwise reassure a reader while settlement has stopped). The flag rides the **session payload** beside `hasPaymentMethod`, not a per-surface `/my/tab` fetch: one source, available anywhere `useAuth` is, and it self-dismisses everywhere at once because `connectPaymentMethod` already clears `card_action_required_at` in the same UPDATE that sets `stripe_customer_id`. **A live bug was found and fixed alongside it:** three of `TabOverview`'s four declared fields had never existed on the wire (the route sends `tabBalancePence`, the type said `balancePence`; the API client is a raw pass-through), so the ledger's net balance — `earnings − tabBalance` — had `tabBalance` pinned at 0 and a reader who owed money saw a balance as though they owed none. **Still owed:** the live browser test of SetupIntent card-attach incl. 3DS (`4000002500003155`), which needs real keys. → `STRIPE-INTEGRATION-AUDIT-2026-06-25.md`.
5. ~~**Calendar-arithmetic residuals.**~~ — **DONE 2026-08-04** (migration 170). Both halves. The fixed 30/365-day adds are gone from all four create paths, and the month-end walk is closed by making the anchor a **stored fact** (`subscriptions.period_anchor_day`) rather than something inferred from a period end that has already been clamped — once 31 Jan has become 28 Feb the row no longer says the subscription hangs off the 31st, which is exactly why re-deriving it from the previous end walked. One home for the arithmetic: `shared/src/lib/subscription-period.ts`. Backfill is deliberately `current_period_start`, freezing existing drift rather than pushing a live subscriber's renewal later than the date they were told. 13 mutation-verified tests, each with the pre-fix expression as a paired control. → FIX-PROGRAMME 2026-08-04.
6. ~~**`convertProvisionalReads` zero-rows early-return**~~ — **DONE 2026-08-04.** The return sat between the read claim and the `article_unlocks` flip, which are separate claims over sets that are not the same set (a converted/reversed/charged-back read leaves a provisional unlock with nothing left to convert). Flip is now unconditional; 3 tests, mutation-verified by restoring the return. → FIX-PROGRAMME 2026-08-04.
7. **Subscription catch-up multi-charge** after worker downtime — one charge per missed period per hourly tick rather than advancing to the next future boundary once. Narrowed by Wave 3 (card-less renewals now expire), so it only affects card-ful readers; still a carried product call. → `SUBSCRIPTIONS-GAP-ANALYSIS.md`.
8. **Connect-KYC follow-ups:** (a) DB integration test for `reconcileConnectKyc`'s six-`EXISTS` candidate SQL (needs a DB-fixture idiom the repo lacks); (b) `pg_advisory_lock` around the sweep if payment-service ever runs >1 replica; (c) **reverse flip** — `stripe_connect_kyc_complete` is never set back to FALSE when Stripe later disables an account (low risk: `transfers.create` fails loudly at payout time). → FIX-PROGRAMME 2026-06-24 entry.
9. **Ledger residue:** publication-distribution reads still reconciliation-only (the one un-cut-over read model); set-based ledger INSERTs still deferred (conflicts with the adjacency guard's per-file `recordLedger()` count); ~~`scripts/reconcile-ledger.sql` dev run~~ — **DONE 2026-07-16** (clean: every divergence check zero rows; dev holds no live money so aggregate parity is trivially satisfied — the meaningful run is prod post-deploy, carried in §11). → `WRITER-SIDE-LEDGER-CUTOVER.md`, code-economy §0.
10. ~~**Gift-subscription grant mode is modeled but unredeemable.**~~ — **DECIDED AND WIRED 2026-08-04** (migration 171). **Wiring was cheaper than deleting**: the redemption *gate* already existed (`writer.ts` has checked `offer.recipient_id !== readerId` all along) and only the *addressing* was missing, so the decision was not really "build vs delete" but "finish the last fifteen lines vs throw away the other two hundred". Both modes now carry a code — the code is an ADDRESS, not the secret — and the `mode = 'code'` filter moved out of the redeem lookup's `WHERE` clause into a recipient check with two arms: 401 with no session (so the recipient following the link from their notification gets a way in, rather than the dead end that would make the feature useless to precisely the person it is for) and a byte-identical 404 for any other account (so a forwarded link reveals nothing). Migration 171 mints codes for historical grants; the recipient notification needed no DDL (`notifications.type` is free text). Comp stays the 100%-free path; grant adds the discounted-offer-to-one-person case it never covered. 5 tests, mutation-verified against all three arms. → FIX-PROGRAMME 2026-08-04.
11. **Publication paywall vault pipeline** (hard-blocked 2026-07-07). The publication publish/approve pipeline never had a vault step — it silently discarded the paywall body (`fullContent` used only for word count) and would have charged readers for content that was never stored, so paywalled publication submissions are now refused (`PublicationPaywallUnsupportedError`, 400 `publication_paywall_unsupported`; the editor pre-blocks the combination; the scheduler un-schedules such drafts back to plain drafts). To unblock: give `publishToPublication` + `approveAndPublishArticle` the vault call the personal scheduler pipeline already has (`scheduler.ts::createVault` shape: index → vault against the anchor event id → payload-tagged v2 is the only relay event), decide who owns the vault ownership check for publication-signed events (key-service verifies `articles.writer_id` via `x-writer-id`; publication articles are signed by the *publication* key, so the check needs a publication-member path), then delete the blocks in `publication-publisher.ts`/`cms.ts` + the editor guard in `web/src/lib/publish-validation.ts` together. Alternatively decide publications stay free-only and record that in the publications ADR. → FIX-PROGRAMME 2026-07-07 paywall entry; CLAUDE.md "Paywall deliverability before charge".
12. **Payments ADR Part 1 — shipped in full; the ADR is archived (`planning-archive/PAYMENTS-FIXES-AND-DILEMMAS.md`, 2026-07-14).** Every decision-independent fix landed: the settlement lock-order deadlock fix (§A — `confirmSettlement` now locks `reading_tabs FOR UPDATE` first, matching its siblings); §1.8 `applyLedgerDelta` across all 9 tab-write sites + `check-ledger-adjacency.sh` Guard-2; §1.3(1) dispute-stake round-trip tests; §1.4 chargeback-attribution policy paragraph (`docs/HOW-MONEY-MOVES.md`); §1.5 tax-schema migration 155 (empty `vat_pence`/`vat_rate_bps`/`tax_point` + `vat` trigger type); §1.1 step 1 conformance battery (all four flows) → step 2 primitive extraction (`executeStripeIdempotent`; `statusGuardedTransition` assessed and not extracted); the Dial-A tribute rework (migration 156); and §1.2 **in full** — the scheduled reconciliation job (alert + halt payouts on mismatch) and the settlement/read-attribution conservation property tests (`settlement-attribution-conservation.test.ts`). §1.7 decided permanently: **no** durable-execution engine. **Everything that remains is gated, not buildable now** — split two ways: the legal fork (§2 item 5 — §1.6 merchant-posture module + the §2.1 Stripe posture fork, both waiting on counsel §2.3), and mothballed-feature test debt (§10 — §1.3(2) pledge and §1.3(4) traffology tests ride with their parked features). **Dropped from §1.3 per code check:** paid-DM charge path (unbuilt) and gift links (free `author_grant`, no money). Full detail + verified file:line targets live in the archived ADR; shipped-work records in FIX-PROGRAMME.
13. **Stripe funds segregation (allocated funds) — CODE COMPLETE 2026-07-29, dark, flag-gated. The remaining blocker is a MEASUREMENT, not a build.** Three same-day parts: the foundations + **writer** cycle (migration 165, `allocation-packer.ts`, `payout-children.ts`, the allocation-sync sweep, the re-keyed webhook handlers); the **publication** cycle (`packPublicationSplits`, `completePublicationSplitChildren`, the `charge.refunded` hooks on BOTH arms, the `finalisePublicationPayout` zombie fixed into the one home `PUBLICATION_PAYOUT_COMPLETE_SQL` on the rule "no split PENDING"); and the **tribute** cycle + **§3.6 reconcile sweep** + **§3.3d residual metric** + **§3.3f dust script** (`packTributePayout`, the per-child carve at accrual GROSS read *before* the advance, `prorateCarveReversal`, the alert-never-halt `allocation-reconcile.ts` sibling of `reconcile-ledger`, `scripts/sweep-allocated-dust.ts`). 28 DB-backed mutation-verified assembly tests (26 shipped with the build; +2 zero-net 2026-07-30). **All three payout cycles now fund through the packer**, and the review-found zero-net wedge is closed (`3ef3e5cf`: a fully-gifted or fee-floored-to-0p unit now lands in `packUnits`' `zeroNet` bucket as a childless claim instead of opening an uninsertable `net_pence: 0` child that would abort the same writer's reserve every cycle; migration 166 corrected the `allocated_draws` refund-sign comment the same day). `docs/adr/FUNDS-SEGREGATION-INTEGRATION.md` **§10 is the as-built record — read it before the five deltas below**, which are kept for the shape they describe but no longer name outstanding work. Corrections it carries: §5 step 0 was NOT run first as this item instructs, so `allocated_residual_alert_bps` and `payout_max_slices` shipped as PLACEHOLDERS and still need their measurements before any flip; and the last delta below miscounts the tripwire guard (it is Guard **1**, not 2 — and its `payout.ts` floor was stale at 4 against 9 real call sites, now 19).

    **What is left, in full:** (a) **§5 step 0 — the SANDBOX PROBES ARE RUN AND GREEN (2026-07-30); what remains is the two production baseline queries**, now the only flip blocker (attack order 1b). All five probes (1/4/6/7/7b) passed against the segregation sandbox, and the findings are in ADR §5's results block — read it rather than this summary. Headlines: `readAllocatedBalance()`'s shape confirmed against the real `transit_balance` object and both retrieval paths agree; `isTerminalTransferError`'s terminal arm confirmed against a real rejection; **§3.5's re-keying confirmed against the actual `transfer.reversed` payload** (child transfer's own id, sibling untouched, `amount_reversed` cumulative — proven by reversing one child twice, since a single reversal cannot distinguish cumulative from per-reversal); the fee is **prorated** on a partial reversal, as `prorateCarveReversal` assumes; `reversed` stays FALSE on a partial, so never key on it; and allocation takes ~1–3s to materialise, which confirms `syncAllocations`' out-of-band design. One Stripe-side 500 seen and then not reproducible: `refund_application_fee=true` on an allocated reversal failed 4 times inside one ~40-minute window, then succeeded **15 consecutive times** — read as a transient incident, not a defect in the call shape. Not our code either way (we never create a reversal), but `payout-children.ts:617`'s manual-reversal instruction still wants a *retry it* note in the runbook. **The methodological point is the durable one:** the isolation matrix ran entirely inside the failing window, so it showed the failure was specific to the fee-refund path (non-flag cases passed) but could NOT show the flag caused it — the first ticket draft claimed "when and only when", which the evidence never supported. A follow-up timing hypothesis was tested (`--mode delay`, 0–30s) and refuted 12/12. Re-runnable repro: `scripts/segregation-reversal-isolate.ts`. **The production baseline was ALSO run (2026-07-30) and returned NOTHING TO MEASURE** — zero payouts in the window, zero writers over threshold, zero connected accounts, on a gated pre-launch platform (migration 165 applied 07-29 16:35, window entirely post-165, so the plumbing is right and the money simply isn't there). So **step 0 is closed**: both dials keep their placeholders, and that is now a recorded state blocked on payout volume rather than an outstanding task — re-run the SQL once payouts flow. **The important consequence is that this inverts the ordering:** ADR §6.3's transition (a cycle spanning the flip mixing allocated and unallocated charges, residual spiking as pre-flip earnings drain) **does not exist if the flip happens before the first real payout**, because there are no pre-flip earnings. Measuring cannot precede the money, so the sequence is flip-when-Stripe-enables-live, then baseline from the first 30 days. §7.5 is moot until a connected account exists. Read that SQL's **Query 0 first**: migration 165 added `subscription_events.tab_settlement_id` with no backfill by design, so on a DB where 165 has not run *every* earning classifies as credit-funded and Query A's exact row reports a ~100% residual. Query 0 reads `_migrations` and says which of the two classifiers to trust. **165 is ALREADY on prod** — applied 2026-07-29 16:35 UTC, with the code that does the stamping (part 1) live in the backend images built ~16:10 the same day — so the window is mid-migration rather than pre-migration: read the **heuristic** row until roughly **2026-08-28**, when a full 30 days sits behind the stamp and the exact row takes over. Nothing needs deploying for the residual baseline; it can be run today. **(Deployment state, 2026-07-30 — read §11's "Pending prod deploy" entry, not this parenthesis. It said segregation part 3 was the only gap and that it was harmless; a day's work has since accumulated behind it, including migration 167 and two real bug fixes, so the deploy is no longer optional-whenever. Establish prod's state from evidence — image dates — and never from this line.)** (b) a **pre-existing carve × slice-cap over-carve** found while building the tribute cycle (ADR §10.3): the carve is apportioned across all units *before* packing, and overflowed units are then un-claimed and re-carved next cycle, so the recipient is permanently short by the overlap. It is in the SHIPPED writer cycle, the tribute cycle reproduces its shape deliberately rather than diverging, and it needs both tributes live and an overflow to bite. The fix is not local (carve → nets → packing → overflow is a fixpoint), and the slice distribution §5 step 0 measures is what decides how reachable it is — so it belongs to that session; (c) a publication split stays ONE indivisible unit, so a member's share can exceed any single charge and route to the residual — safe, not segregated, and the §3.3d metric is the measurement that should decide whether a divisible unit is worth its rounding; (d) the Stripe-facing half of `executePendingChildren` (terminal/ambiguous, the row-stable key, the flip-gated emit) remains pinned only by the mock conformance batteries, because it drives the module-level `pool` and is unreachable from a rolled-back transaction.

    The original framing, kept for the shape it describes (all five deltas are now BUILT): **Stripe funds segregation (allocated funds) — spec'd at rev 3, unbuilt, flag-gated.** The Stripe beta locks pooled reader funds so they can only reach connected accounts. Sandbox is enabled; live enablement expected from Stripe ~w/c 2026-08-03. Spec: `docs/adr/FUNDS-SEGREGATION-INTEGRATION.md` (rev 3, 2026-07-29 — rev 1 and rev 2 are both withdrawn in part; read §1 and §3.3 fresh and do not work from either). Everything ships behind `STRIPE_ALLOCATED_FUNDS` (default off), so this is mergeable before the live flip. **This is the concrete form of §2.5's Branch A** — but note the dependency has inverted: the code is now spec'd and buildable, while *flipping the flag* still sits behind the same counsel answer. The five deltas that make it non-trivial:
    - **The payout unit changes shape.** Allocated funds transfer only with `source_transaction: <charge id>`, so one aggregate transfer per writer becomes N transfers, one per charge drawn on — a new `payout_transfers` child table shared by all three payout cycles, a pure `allocation-packer.ts`, and a `payout_transfer_id` stamp on `read_events`/`subscription_events`/`tribute_accruals` so a single failed child releases exactly its own units. The claim arithmetic in `reserveWriterPayout` is deliberately **untouched** (spec §3.3): attribution stays, only funding is new.
    - **The ledger moves to per-child posting** (inside the child's flip txn, gated on its `rowCount` — the `processPublicationSplits` shape), with refs staying on the **parent** row because `ledger_publication_distribution` hard-filters `ref_table = 'publication_payout_splits'`. Re-pointing refs at the child silently empties that view.
    - **Six webhook handlers need re-keying** off `payout_transfers.stripe_transfer_id`: `confirmPayout`/`reverseWriterPayout`/`handleFailedPayout` and their tribute + publication twins all resolve by `writer_payouts.stripe_transfer_id` today, which can hold one id of N.
    - **The 8% fee must become an explicit `application_fee_amount`** — with the full charge locked, an unclaimed fee never leaves allocated state at all. Bites hardest on the publication cycle, where `fee = 0` per split would strand roughly the whole pooled fee every cycle (spec §3.4 prorates it).
    - **One migration** (six `ADD COLUMN`s + `payout_transfers` + `allocated_draws`, no `ALTER TYPE`, no `platform_config` INSERTs), and **`scripts/check-ledger-adjacency.sh` will NOT flag any of this** — Guard 2 is a per-file `recordLedger` count and Guard 3's `PAYOUT_MARKER` doesn't know `payout_transfers`. Both need deliberate updating in the same commit.

    **Do §5 step 0 first — it is cheap, unblocked, and produces two numbers the design consumes.** Five sandbox probes with no §3.3 code written (incl. 7b: reverse one child of a multi-child set and see what the webhook can be keyed on — that is the third defect above, in fifteen minutes), plus two read-only production queries that set `allocated_residual_alert_bps` and `payout_max_slices`. Nothing else in this item should start before those. *(Written but not run: the code half is `scripts/segregation-probes.ts` + `scripts/segregation-baseline.sql`, 2026-07-30. This paragraph's instruction stands — the build went ahead of the measurement anyway, which is why the two dials shipped as placeholders.)*

    Of the two live defects surfaced by the rev 3 review, **one is closed and one is still open**. ~~`finalisePublicationPayout` zombifies its parent on any failed split~~ — **FIXED 2026-07-29** (part 2): the rule is now "no split PENDING", in one exported home used by all three call sites, with a DB-backed test carrying the pre-fix predicate as a paired control and the conformance mock re-written to parse the predicate out of the SQL rather than restate it. ~~Still open: the **`DEPLOYMENT.md` webhook-scope contradiction**~~ — **RESOLVED 2026-07-30, and the doc was the wrong one.** Measured, not reasoned: `GET /v1/events?type=transfer.reversed` returned three events on the **platform** account and **none** with a `Stripe-Account:` header for the connected account. A Transfer is a platform-account object; the connected account sees an incoming payment (`py_…`) and a refund of it (`pyr_…`), never a `transfer.reversed`. Our config is correct, the feared failure (reversals silently never delivered) does not exist, and `DEPLOYMENT.md`'s Stripe-webhook bullet is rewritten with the two-endpoint table production actually runs.

14. ~~**Dead dial: `publication_payout_threshold_pence`**~~ — **SHIPPED 2026-08-06** (FIX-PROGRAMME). Wired, not dropped: **dropping the seed was never available**, because migration 038 seeds the key and drift-guard Check 4c replays every migration seed onto the defaulted DB and fails if a row appears — removing it from `config-defaults.sql` fails CI by construction. So it is now read by `loadConfig` → `publicationPayoutThresholdPence` and bound by the publication cycle's eligibility query in place of the writer dial. Pinned by `payment-service/tests/payout-threshold-dials.test.ts`, which sets the two dials to **different** values — with both at their shared 2000 default a test passes against the bug exactly as it passes against the fix, which is how this survived every green suite since 038.
15. ~~**`revenue_share_bps` writable without the finance permission**~~ — **SHIPPED 2026-08-06** (FIX-PROGRAMME). Taken by the second option, which deletes code rather than adding a guard: `revenueShareBps` is gone from `UpdateMemberSchema` and the fieldMap, and with it the duplicated `pub_shares` lock + Σ ≤ 10000 block. The finance-gated `PATCH /publications/:id/payroll` is now the **sole** writer of the column — nothing in the web ever called the members route with it (`updatePayroll` is the only client). Pinned by `gateway/tests/publication-member-finance-mandate.test.ts`. This makes W5's `set_by` mandate evidence sound by construction rather than by a check: there is only one writer left, and it holds the mandate.

16. **Spend-conversion's Reader-funded negative tab — real, but NOT a live blocker.** *(Written 2026-08-06 while building W1 as an urgent owner decision gating W0; corrected the same day on re-reading the route — the correction is the useful part, so both stand.)*

    **The mechanism is exactly as diagnosed.** `gateway/src/routes/articles/subscription-convert.ts` credits the reader's month-to-date spend back to the tab (`min(spend, subPrice)`) with **no offsetting debit** — the first month is logged to `subscription_events` only, unlike the renewal path's `logSubscriptionCharge` — and its spend query sums `read_events.amount_pence` **regardless of state**, so it counts reads the reader has already *settled*. A reader who settles mid-month then converts goes below zero holding a credit they funded: the instrument `PAYMENT-PERIMETER-ADR` §4 rule 4 bans.

    **What was wrong: the route does not ship.** It has been **503-gated behind `SUBSCRIPTION_CONVERT_ENABLED` since 2026-07-16** (§0b item 4), default off, with `DEPLOYMENT.md` carrying an explicit **"Do NOT flip to `1`"**. So (i) the `negative_reader_tab` alert **cannot fire on ordinary business** from this path — nothing reaches it; (ii) W0 describes what ships, and a dark route behind a written do-not-flip is not shipping behaviour; (iii) it is not gating anything, and does not belong at the head of the attack order (0e struck).

    **And the decision was already taken.** §0b item 4 has required, as a precondition of ever flipping the flag, "a real `logSubscriptionCharge` leg (no tab debit / no `subscription_earning`)" — which *is* option (c). Options (a) cap-the-credit and (b) unsettled-reads-only were foreclosed a year ago; re-opening them re-litigates a settled call. The one thing rev. 4's analysis genuinely adds is the *reason* (c) is right, which is now recorded where the revival decision lives: it is the only option that leaves the reader's offer intact.

    **What remained was small, and is now done: the prod negative-tab query ran 2026-08-07 and returned 0 rows.** No pre-2026-07-16 history left a real credit, so W1's detection will not open its life with an incident nobody can explain. The flag-off state stays a *property to preserve*, not work to do. Original framing kept below. Fold the negative-tab framing into §0b item 4's revival list (done), and run the negative-tab query against prod once — pre-2026-07-16 history could have left a real credit on a real account, which the new alert will surface as an incident on its first run with no live cause to explain it. That is a check, not a decision. → `docs/adr/PAYMENT-PERIMETER-ADR.md` §3.W1; `docs/runbooks/reader-tab-credit.md`; §0b item 4.

    **The generalisable lesson** (why this is left in rather than deleted): a defect found by *reading a route* is not yet a defect in production. The route's own header comment carried the flag, the four legs and the do-not-flip; the scoping pass read the body and not the guard. Before an audit finding is written up as blocking, establish that the code path is reachable in the deployment it is claimed to block.
17. **W1's admin refund action — a full new Stripe money path, not a button** (parallel lane; does NOT gate W0, whose detection + runbook shipped 2026-08-06). Needed only for a **residual** credit: the commonest cause (double settlement) is now known to resolve through the existing webhook — a **full** refund fires `charge.refunded` → `reverseSettlement`, restoring the debt — and a **partial** refund is not a resolution, since the handler logs `manual_review_required` and skips the unwind, leaving the reader the credit *and* the money. Build it with the whole money-moving-create discipline (`CLAUDE.md` Stripe invariant): three-phase reserve → create → confirm, row-stable idempotency key, terminal/ambiguous split via `charge-errors.ts`, resume-sweep coverage, a new `credit_refund` trigger type posted via `applyLedgerDelta`, a `ledger_orphans` branch, and adjacency registration (convention — Guard 1 is per-file floors and Guard 3 keys on payout-table INSERTs, so an unregistered pure-ledger site stays green; register it anyway). **The trap: `credit_refund` must join the `ledger_reader_balance` view in the SAME migration as the action.** Parity compares the view itself, so omitting it halts ALL payouts on the action's first use, platform-wide, every run. Plumbing home exists (gateway admin proxy → payment-service internal `x-internal-token`), and the `charge.refunded` webhook already posts the `allocated_draws` refund draw, so segregation accounting comes free. → `docs/adr/PAYMENT-PERIMETER-ADR.md` §3.W1.
18. **Payment-perimeter W2/W3/W5/W8 — the parallel lane, none gating counsel.** ~~W4~~ **SHIPPED 2026-08-07** (migration 175, FIX-PROGRAMME): the payout halt now has a per-account granularity for the one class that can be honestly attributed (`ledger_orphans`' payout-side trigger types), the global flag keeps everything it cannot attribute and says `unattributable` in its reason, attribution runs uncapped, and a `payout_halt_escalation_hours` bound escalates a halt nobody clears. **The ADR's open publication fork was resolved by checking rather than choosing:** a halted split stays `pending`, which is the shipped KYC-incomplete branch's own shape — other splits still pay, reads still finalise, the resume sweep pays it when the halt clears, and no pending-parent guard blocks the next pool. Both spec options were worse (`failed` lies in the books and burns the re-pay `attempt` cap; a new status needs an ALTER TYPE and a revival path the resume sweep already provides). Two exported eligibility constants (`WRITER_PAYOUT_ELIGIBILITY_SQL`, `TRIBUTE_PAYOUT_ELIGIBILITY_SQL`) came out of it so the DB test runs the cycles' own SQL. ~~W2~~ **SHIPPED 2026-08-07** (FIX-PROGRAMME): funds segregation is a measured number on `/admin/overview` — charge-side coverage over `tab_settlements` (a new `allocation-coverage.ts`, internal `GET /allocation-coverage`, gateway proxy), the payout-side residual beside it and labelled as a *different* number, and the window split **three** ways rather than two: a never-synced row is neither covered nor uncovered but "awaiting a read", reported as its own figure so a coverage share over a partial sample cannot read as one over all of it. Empty denominator ⇒ null ⇒ words, never a fake 0%. No migration. The one thing it still cannot do is attribute an uncovered settlement to Mastercard — no card brand is stored — which is the add-time gate W2 records as blocked on Stripe. W3 per-account payout threshold/cadence — item 14 is **already done** (2026-08-06), so W3 inherits a live `publicationPayoutThresholdPence` to make per-account rather than a dead key to revive-or-drop, and `payout-threshold-dials.test.ts` is waiting to catch a rewire that crosses the two cycles' dials again; note the eligibility query and the pack floor must read the SAME per-account value or a writer is claimed and rolled back every cycle, forever, silently — **and W4 has now made the payout cycles halt-aware per account, so a per-account THRESHOLD lands in eligibility queries that already carry a per-account exclusion.** W5 split-version mandate evidence — item 15 is **already done** (2026-08-06), and W5 gets the stronger form of it for free: `revenue_share_bps` now has exactly ONE writer and that writer is finance-gated, so `set_by` is mandate evidence by construction rather than by a check W5 would have had to add. W8 is documentation-only. Gated on counsel and NOT to be started: W9 (agent copy) and W3's "pay me now" endpoint. → `docs/adr/PAYMENT-PERIMETER-ADR.md` "Execution order". **W1 detection + runbook and W6 shipped 2026-08-06** (FIX-PROGRAMME).

## 2. Decisions & gates (nothing else moves until these are called)

1. **`TRIBUTES_ENABLED` flip** — all five phases code-complete and dark. **Now gated on TWO things (2026-07-13):** (a) the compliance owner signing memo residual #1 (platform-wide Stripe-PI baseline), and (b) the **Dial-A code rework** — the ADR is amended to consent-gated, forward-only accrual (no held share for a non-consenting party), so the shipped accrue-from-creation code must be reworked before the flag flips (a net simplification: delete the `held`/`swept`/`returned` machinery; settlement freezes only for `live` tributes). Residual #2 and the characterisation point are now moot; the segregation fork (`planning-archive/PAYMENTS-FIXES-AND-DILEMMAS.md` §2.2) is closed. → `docs/adr/UPSTREAM-EDGES-BUILD-PLAN.md` › *Dial-A rework*, `UPSTREAM-EDGES-TRIBUTE-COMPLIANCE.md` › *Decision (2026-07-13)*.
2. **`/author` + `/source` SSR** — blocked on a privacy/product call: converting the endpoints to `optionalAuth` publicly exposes author post-logs / follower counts / source item lists to anonymous crawlers. Benefit marginal (logged-in visitors get the overlay redirect anyway). Verified still `requireAuth`. → `allhaus-performance-audit.md` Outstanding #1.
3. **Concierge Phase 4 (custodial atproto)** — gated on §8.2 ratification (rotation-key custody split, durability targets) + W0 infra. Deliberately parked; only worth it for the branded `username.all.haus` handle. → `NETWORK-CONCIERGE-ADR.md` §13.
4. **Trust Phase B (dual-graph anonymity)** — gated on >1k attestors. Parked. → `ALLHAUS-OMNIBUS.md`.
5. **Payments compliance baseline + the Stripe posture fork** — everything here is downstream of one lawyer conversation; none of it is code to write now (`planning-archive/PAYMENTS-FIXES-AND-DILEMMAS.md` §2.1/§2.3). **Critical-path gate — residual #1:** counsel confirming that reliance on Stripe (the regulated PI/EMI that *holds* the funds) keeps all.haus itself outside PSR/EMR authorisation for the reading-tab float. Platform-wide — tributes merely inherit it (item 1). Until it returns, nothing downstream moves:
   - **The Stripe fork (§2.1):** once counsel answers, pick **Branch A** (ring-fence the writer-side float into a segregated Stripe balance) or **Branch B** (Merchant of Record — the charging machinery survives; the new work is live VAT/invoice/seller-of-record). If the answer is "the baseline is fine as-is," Branch A becomes belt-and-braces and the fork may not be worth taking at all. **Branch A is no longer a sketch: it is Stripe's funds-segregation beta, spec'd at rev 3 and queued as §1.13.** Two things follow. The old "days of code" estimate is wrong — the payout unit changes shape (N transfers per payout, a new child table, a packer, six re-keyed webhook handlers), so budget accordingly. And the ordering has inverted: the code ships behind `STRIPE_ALLOCATED_FUNDS` (default off) and is **buildable now, independent of counsel**; only *flipping the flag* waits on this gate. So a Branch-A decision no longer starts a build from zero, and §1.13's §5-step-0 probes are worth running whichever way the fork goes.
   - **Merchant-posture module (§1.6):** a greenfield module owning receipt/invoice text, the refund-source rule, and seller-of-record strings (schema stubs already shipped empty as migration 155). Only becomes live work if Branch B is chosen; nothing to build until then.
6. **Waitlist privacy note — counsel sign-off.** Storing prospective-user emails (migration 162) is new personal-data processing; the lawful-basis / purpose / retention note is drafted (`docs/adr/WAITLIST-PRIVACY-NOTE.md`) with three points flagged for counsel: consent sufficiency, a retention backstop, and whether a privacy line must sit on the form itself (it currently carries none, faithful to §V). Smaller than and independent of the payments-compliance gate (item 5), but it should clear before the beta collects real emails at scale. → CLOSED-BETA-ADR §D5.
7. **The empty-vessel explore placeholder — keep, replace, or drop.** Deferred by the operator 2026-08-10, split out of §9.16 so that work isn't blocked on it. A feed with `sourceCount === 0` auto-serves the platform-wide ranked stream (`placeholderExploreItems`, `gateway/src/routes/feeds/items.ts:443` — 48h, `article|note`, not your own, `fi.score DESC`), flagged `placeholder:true`. It was ruled deliberate on 2026-07-19 as the getting-started state, alongside `reach:explore` as its opt-in twin. **§9.16 removes the twin and leaves this standing**, which inverts the pairing that ruling rested on: explore survives only as the unavoidable default for an empty feed, and is no longer available as a deliberate choice. That is a coherent position — "explore is scaffolding for a feed you haven't built yet, not a thing you compose with" — but it is the opposite of the 2026-07-19 reasoning, which held explore acceptable *because* it was opt-in. Call it explicitly rather than inheriting it. Three ways out: keep as-is (accept the inversion, and reword the ruling); replace it with a non-ranked empty state (`EmptyFeedTile` already exists and says NO SOURCES, so the placeholder is arguably redundant with it); or drop it and let an empty feed be empty. Nothing else depends on the answer except the finale copy (`web/src/lib/explain/copy.ts` `FIRST_RUN_COPY.finale`, "There is no algorithm here"), already flagged as that ruling's one residual.

## 3. Launch-blocking product gaps

1. ~~**Owner dashboard**~~ — **SHIPPED 2026-07-22** (all six panels: overview money pipeline off the ledger views, users + KYC-stuck list, content + system health, config editor with the platform_config guardrails, regulatory thresholds as config dials, trigger proxies; `gateway/src/routes/admin-dashboard.ts` + `web/src/app/admin/*`; FIX-PROGRAMME 2026-07-22 entry has the full validation record). **Residue**: (a) account search + suspend/unsuspend affordance on the Users tab (today suspension is only reachable via a report); (b) open-report badge on the Reports tab; (c) browser-tier look on next dev browse (§11); (d) prod deploy — standard shape, migrate seeds the 6 regulatory dials, no flag.
2. **Landing page** — reframed readers-first for the closed beta (`web/src/app/page.tsx`): the three-propositions manifesto (ownership / farming / paying writers) + a single "Join the waiting list" CTA. **Rebuilt 2026-07-25/26 from the member grammar, not the retired register**: bone floor, one abstracted ⊔ vessel (`LandingVessel` — the ⊔ stance, 8px wall/lattice, cards on a continuous ground, no numeral/roundel/bar), a showcase of three demos and the `'FOR ALL'` coda, with the CTAs docked in the sitewide `PublicNavRow`. **The showcase is live markup, not screengrabs** (2026-07-27): the three `.webp` viewport captures shipped 2026-07-26 were replaced by inert components (`components/landing/demos/`) reading the same palette tokens as the live site — a 2.9× downscale had put the app's 15px type at ~5px, so the `via …` provenance lines that ARE the argument dissolved and the captions carried the whole claim. Their content is invented and must stay so (real names retyped as markup on a selling page are words we put in someone's mouth); the protocol labels are the exception. **Revised again the same day** (7quater): the three-column workspace demo became `CanvasDemo` — four irregular feeds that scale off a container query and never reflow, since the arrangement is the argument — and the bare gate became `ReaderDemo`, the whole Glasshouse figure (blurred floor, scrim, lifted pane). The old description of it — "centred ∀ · serif head · mono copy" — is superseded twice over: the ∀ dingbat left the design language with `ForAllMark` (2026-07-25) and the page no longer carries its own chrome. This is the *closed-beta* landing, deliberately minimal — the fuller marketing landing the frontend audit envisioned (tab-model explainer, screenshots, social proof, example articles) is **deferred until the beta opens**, a post-beta concern rather than a current blocker. The shipped copy is still marked "wants the author's ear" (CLOSED-BETA §V), so a redline pass is the one open thread; rendered-appearance verify is in §11. → CLOSED-BETA-ADR §IV/§X; frontend audit #3.
3. ~~**Writer onboarding flow**~~ — **SHIPPED 2026-08-07 as a first-session WELCOME, reader-first with a writer branch** (migration 176; `web/src/components/workspace/Welcome.tsx`). Two owner decisions reshaped it away from the literal spec, and both are worth carrying: **(a)** "profile → first article → pricing" was drafted from a frontend audit written before the beta went readers-first, and marching someone admitted to READ through a pricing form states the wrong thing about the platform — so publishing is ONE offer on step 3 (collapsed by default, `Set my prices` / `Write something now →`), not two mandatory steps. **(b)** The six-beat first-run tour, dormant since EXPLAIN-ADR amendment 1 judged auto-entry "a malfunction, not a welcome", is now OFFERED on the last step and opened by `FirstRunLauncher` on an explicit yes — that verdict was about the ENTRY, not the tour, so the tour survives and the entry becomes a question. `FirstRunController` stays dormant and un-mounted.

    **It absorbed `BringYourWorld`** (deleted), which FOLLOW-GRAPH-IMPORT-ADR §7.4 always described as a stand-in "until the §3.3 wizard builds" — its import offer is step 2, still D7-pure (nothing imports without an act inside it) and still absent entirely when capabilities are dark, which is the current beta state.

    **The build found a live bug in the thing it replaced, and that is the durable part.** `BringYourWorld` fired on `mintedFounderFeed` — the bootstrap returning zero feeds — which stopped meaning "brand-new account" when starter-seeding moved into `listFeedsForOwner`, the very function `/workspace/bootstrap` calls. **A healthy new account never has zero feeds**, so that branch is the seeding-FAILURE path: the offer appears on prod today *only* because the §0l incident destroyed the starter template, and would have gone silent the moment §0l item 1's repair landed — a first-session surface deleting itself as a side effect of an unrelated fix, with nothing to say it had. The welcome instead gates on `accounts.onboarded_at IS NULL`, which is a fact about the MEMBER rather than about the state of their feeds. **Generalisable:** a first-session signal derived from "the system produced nothing" is a signal that inverts the day the system starts working.

    **Server-side, not `localStorage`, deliberately.** The two older seen-flags (`workspace:ceremony_seen:`, `workspace:bring_world_seen:`) are per-device, right for a one-off animation and wrong for onboarding — a member who has introduced themselves must not be asked again on their laptop because they first signed in on their phone. Stamped by `POST /auth/onboarded` on complete, dismiss, AND walking out into the editor, because all three are answers; first-write-wins on `WHERE onboarded_at IS NULL`, so the client fires it without awaiting and a lost call costs one repeat offer.

    **Migration 176 is nullable with no default, so its deploy window is safe in BOTH directions** (the statement §0p.2 asks every migration to make): old code never names the column, new code reads NULL as "not yet welcomed", which is correct for every row that exists when it runs. Every existing member therefore reads as un-welcomed and is offered the sheet once — deliberate, not a backfill oversight, since nobody on the platform has ever been asked for a display name.

    **Browser-driven 2026-08-07 at 1440×900 and 390×844, with controls** (the earlier "not driven in a browser" note here was written mid-rebuild and is superseded): four steps desktop / three on mobile; the tour opens to first-run **beat 1/6 on the floor**, proving the pane unmounts before it; ✕ stamps; and two negative controls — a stamped account gets **no sheet** on reload, and a second `POST /auth/onboarded` a second later leaves the timestamp byte-identical. **`FOLLOW_IMPORT_ENABLED` was flipped dark and back** to prove the last claim that had only been asserted: with capabilities empty the sheet is **3 steps with the counter reading `/3`** — "Bring your world" absent entirely, not a blank fourth step — and 4 steps with it live. So the prod prediction below is measured, not inferred.

    **The INTERACTIONS are now driven too, and the account was provisioned the real way — 2026-08-07, second pass.** This closes what the paragraph here used to hold open. A fresh address went through the production journey rather than a simulation of it (`POST /waitlist` → admin admit → `provisionAccount` → dev-login → `/reader`), landing `display_name = 'wrenfallow'` with `onboarded_at NULL` — so **`looksDerived` has finally rendered**, the sentence the step is built around, never once on screen before today. Every write was confirmed against the `accounts` row *at the moment it happened*: photo upload (real multipart → WebP → hash-verified `media_uploads`, blob 200 from dev Blossom, and correctly NOT yet in the row — only Save persists), profile Save (name + bio + avatar in one write), **Back** (absent on step 1 as designed, and the remount re-reads the SAVED values, proving the `fetchMe` round-trip), `Set my prices` → **Save prices** (500→300, 15→10, NULL→20 fixed), and **"Write something now →"** — the supersede path: welcome gone, editor open, exactly **one** `role="dialog"` left, `onboarded_at` stamped, no sheet on reload. Zero page errors. One dev artifact, not a defect: the saved avatar renders BROKEN locally because `PUBLIC_MEDIA_URL` points at prod while the blob is in dev's Blossom. Details + evidence: FIX-PROGRAMME 2026-08-07 second follow-up.

    Unrelated and untouched: the first-login ceremony remains commented out behind `// TODO: re-enable / refine entrance animation`, still its own call. → frontend audit #6; FIX-PROGRAMME 2026-08-07.
4. ~~**Publication homepage templates**~~ — **SHIPPED 2026-08-07.** No migration, no flag.

    **It was two surfaces and only one was the wireframe.** The in-workspace `PublicationPanel` was already house-standard; the standalone `/pub/[slug]/**` pages were not, and their defect was structural before it was aesthetic — `pub/[slug]/layout.tsx` mounted a `PublicationNav` + `PublicationFooter` on routes where `LayoutShell` **already** mounts `PublicNavRow`, so every publication page carried two navigations, and the layout never cleared `--ah-row-band` so the fixed row sat over the footer. Both deleted (owner call: fold onto the public register rather than keep a per-publication chassis). Five views now compose `PublicPage` + a shared `PublicationMasthead`; the article route matches `/article/:dTag` exactly.

    **"Magazine" didn't look like a magazine because it had no picture to lay out** — `articles.cover_image_url` has existed since migration 081 and the by-slug endpoint never selected it, while the publication's own cover/logo were fetched and dropped. One column on one SELECT was the entire backend half. *Generalisable: a template starved of its data reads as a design failure and gets rebuilt as one.*

    **One set of templates now serves both registers**, which is the thing that will rot if anyone forks it: the overlay previously ignored `homepage_layout` and rendered a fourth arrangement, so Magazine meant one thing publicly and another in the workspace. The seam is `ArticleLink` — `onOpen` ⇒ a `<button>` opening the reader in place (escape ban), absent ⇒ a real `<Link>` (SSR/new-tab/copy-link).

    **Five hairlines cleared**, tripwire green. Incidental: `text-grey-500` was an **undefined token** (500/700/800/900 emit no rule and silently inherit), and `PublicationPanel`'s `text-[28px]`/`text-[15.5px]` were px that don't scale with the global type control — both out with the dead `rich` variant.

    **Verified in bytes AND browser-driven.** Bytes: all three layouts distinct, Minimal emitting strictly fewer image URIs than the others (its "no covers" rule is in force, not merely commented), and a mutation control — null every cover — flipping the magazine lead to its centred no-image treatment (data-URIs 14 → 8, all eight titles intact). Browser (Playwright, cached under `~/.npm/_npx/*`): three layouts × five views at **1440×900 and 390×844, light and dark**, no page errors. The magazine reads as one; the masthead lap-up holds at both widths; dark inverts the ground and correctly not the cover.

**The `revalidate: 60` trap fired TWICE and is the thing to carry.** First it showed all three layouts byte-identical (one cached page — *a layout switch checked without busting the cache proves nothing*), then it made the first screenshot run photograph the stale null-cover page from the mutation control, *after* the fixture had been restored. Pixels lie about a cached page exactly as bytes do. **Restart the service, assert the expected state in the markup, and only then believe a render.** → frontend audit #5.
5. **Import tooling** — Substack CSV/ZIP → NIP-23 importer (Ghost/WordPress later). Launch-cohort switching cost. **Design pass done 2026-08-07: `docs/adr/ARCHIVE-IMPORT-ADR.md`** (proposed, nothing built). The decisions that shape the build: it is a *gateway* service reusing the ONE publish pipeline (slice 1 extracts `publishPersonalDraft` out of the scheduler — a second bulk publisher is how the vault/outbox invariants get silently dropped); dates split NIP-23's own way, `published_at` backdated and `created_at` left at now (both publish paths currently hardcode `now()` in **four** places, so an unmodified import dates a 2019 archive today and lands 200 articles atop every follower's feed); an import emails nobody and fulfils no drive (200 broadcast emails is the worst thing this feature could do, and it would happen on first real use); **nothing imported is ever paywalled** (owner ruling the same day — a paid post lands in `article_drafts` with its full body and the writer prices and gates it in the editor, which takes the vault path, the price/gate lockstep across three validators and the publication hard-block out of the feature altogether, and collapsed a whole slice); images are rehosted through `safeFetch` (the export contains none — they are all still on Substack's CDN) as a separate resumable phase; d-tags are deterministic (`<slug>-sub<post_id>`) so a re-run converges instead of duplicating; and the job model is `follow_imports` copied — parse → **preview** → confirm → sweep, with no one-click path. **Slices 1 and 2 SHIPPED 2026-08-08** — the two pure refactors, taken before slice 0 because neither depends on the archive format: `gateway/src/services/article-publisher.ts` (the scheduler is now a caller of the one publish pipeline, not its owner; `publishedAt`/`sendEmail`/`matchDrives` options all defaulting to prior behaviour) and `gateway/src/services/media-store.ts` (`storeImage(uploaderId, buffer)`, so `sharp` rather than a declared MIME type is what validates an image — the check the importer needs and the route never had to make). 8 tests, mutation-proved three ways; suite 469 → 477. **First build step is still not code: get one real export from the launch cohort** (§XI) — five load-bearing facts about the format, chiefly **whether the paid body is in the archive at all** (if it is truncated to the free preview, a draft is worth less than an honest skip and §V changes), are sourced from documentation rather than from an archive. **All four open questions were called 2026-08-07** and are recorded at §XIII, not left hanging: no paywalled import, no email ever (not even a one-off "I've brought my archive over" digest), no Substack URL preservation, bodyless rows skipped and counted. → frontend audit #8.
6. **Follow-graph import (reader-side twin of §3.5)** — bring a user's existing follows in from any network as a new feed: atproto (public AppView, DID only), Nostr (public kind-3 from a pasted npub), ActivityPub (linked token has `read:accounts`; needs poller fairness first — the 100/tick + 2/host/tick AP budget has no per-user fairness), OPML upload for RSS. Snapshot import first, exclusion-aware "Sync now" second, continuous sync deferred; one-way only (never write follows back). Design accepted 2026-07-12; scoped for implementation same date. **Phase 0 + the 1a/1b backend SHIPPED 2026-07-12** (migration 153; engine + routes dark behind `FOLLOW_IMPORT_ENABLED`; both §6.4 stampede fixes; atproto + Nostr graph readers — FIX-PROGRAMME 2026-07-12). **Phase 1a/1b web surfaces SHIPPED 2026-07-12** (post-link offer, NetworkReachPanel per-presence + paste path, FeedComposer affordance, progress UI, `followImportProtocols` capabilities gate; §10 invariants now in CLAUDE.md — FIX-PROGRAMME 2026-07-12 second entry). **Phase 1d OPML SHIPPED 2026-07-12** (jsdom parser/planner + `POST /follow-imports/opml`, one run per folder-feed under the 10-feed cap, no sync binding per §5.4, engine rss probe-on D6 exception, `followImportOpml` capability + `FollowImportSection` upload block — FIX-PROGRAMME 2026-07-12 third entry). **Phase 2 "Sync now" SHIPPED 2026-07-12** (migration 154: `kind`/`removals`/cursors + `preview` status; `POST /follow-imports/sync` diff-preview → confirm/cancel; engine removals-before-adds with `recordExclusion:false` + apply-time exclusion re-check; truncated reads suppress removals, surfaced; `addSource` revokes the exclusion on manual re-add; FeedComposer `FeedSyncSection` — FIX-PROGRAMME 2026-07-12 fourth entry). **Phase 1c ActivityPub SHIPPED 2026-07-12** (behind the §6.6 sub-brake `FOLLOW_IMPORT_ACTIVITYPUB_ENABLED`, ANDed with the master switch, default off pending the §6.4 fairness soak; the gating live scope check PASSED — `read:accounts` authorises the following endpoint per Mastodon source, no re-consent flow; Mastodon-API reader with linked-token + public legs, hidden-follows detection → `follows_hidden`, actor-URI canonicalisation with throttled WebFinger fallback + surfaced `unresolved` which also suppresses sync removals; Mastodon post-link offer — FIX-PROGRAMME 2026-07-12 fifth entry). **Dev-stack run-through PASSED 2026-07-12** (all four protocols live end-to-end, the Phase 2 preview/confirm/exclusion loop, the §6.4 rails at ~1,600 sources; one bug found + fixed — claimed-count-derived `truncated` falsely suppressed sync removals for aged accounts; flags now wired into `docker-compose.yml` — FIX-PROGRAMME 2026-07-12 sixth entry). **Phase 3 "Bring your world" SHIPPED 2026-07-12** (the `import` resolver context — external-first, native-squatter-proof like subscribe — and a first-session Glasshouse sheet riding the founder-feed-mint signal, since no signup wizard exists; capabilities-gated, once-per-user, D7-strict pure offer reusing `FollowImportSection`; folds into the §3.3 wizard when that builds — FIX-PROGRAMME 2026-07-12 eighth entry). **Phase 3's sheet was ABSORBED 2026-08-07** into the §3.3 welcome exactly as planned: `BringYourWorld` is deleted and its offer is step 2 of `Welcome.tsx`, D7-pure and still absent entirely when capabilities are dark. Note what that fixed as a side effect — the sheet had been firing on a signal (`mintedFounderFeed`) that only holds when starter-seeding FAILS, so it was reachable on prod solely because of the §0l incident; see §3.3. **Remaining**: flip `FOLLOW_IMPORT_ENABLED` in prod (deploy-time env; compose defaults stay `0`) — note this is also what makes the welcome's step 2 exist at all, so the two are worth flipping and eyeballing together; flip the AP sub-brake only after the §6.4 soak **and** one authed self-import against a real linked token (the sole §2 check not exercisable in dev — no linked AP presence); a browser-level look at the step on next dev browse. Phasing + per-source matrix: `docs/adr/FOLLOW-GRAPH-IMPORT-ADR.md`.

7. **Closed-beta waitlist — the manual admit flow is DONE; one email remains.** The three items the 2026-07-27 incident produced all shipped that day (CLOSED-BETA-ADR §XI.4/§XI.5/§XI.6, logged in FIX-PROGRAMME): the operator **digest** (hourly tick, one message a day, only when the list moved), the panel's **read half** (`GET /admin/dashboard/waitlist` + `/admin/waitlist`, a seventh dashboard tab), and the **admit action** (migration 163 `admitted_at`/`admitted_account_id`/`invited_at`; `POST /admin/dashboard/waitlist/admit` claims the row, find-or-creates the account through the extracted `gateway/src/lib/account-provision.ts::provisionAccount` — which bypasses `CLOSED_BETA` by design, an admission being exactly the human decision that constant reserves, and which `google-auth.ts` now imports so the two paths cannot drift — then claims and sends the invitation, releasing either claim on failure so no row is ever stuck; 22 tests, 10 mutations). **So converting a waitlister to a member is no longer manual, and this item is closed for that purpose.** Invite codes and self-serve cohorts remain explicitly out of scope of the ADR (§VII).

   **Still open, three things:**
   - **The joiner acknowledgement (D8.1)** — the email a prospect gets on *joining*, as opposed to on being admitted. Gated on **D9**: the privacy line must land on the form first, because a list that produced no contact has had nothing for consent to cover. Note **D6** — it goes out on *every* accepted submission or not at all, since sending only on a new insert moves the enumeration side channel out of the (already fixed) HTTP response and into the victim's inbox. Deliberately last: it tells people something the success page already told them.
   - **Export CSV (§XI.2's second action)** — re-queued 2026-07-30: it was spec'd ("for the cohort planning the list exists to serve") and then fell off the build order without being shipped, deferred, or declined — the 2026-07-30 review caught the silent drop. Genuinely small (one authed route streaming the same query the panel reads, minus the cap). Worth a decision more than a build: at 20–30 people the panel may simply be enough, in which case decline it here explicitly rather than letting it vanish again.
   - **Nothing on this panel has been seen in a browser** — but the ADMIT ROUTE has now been driven for real (2026-08-07, as the fixture step of the §3.3 interaction drive): `POST /waitlist` then `POST /admin/dashboard/waitlist/admit` answered `{"admitted":true,"accountCreated":true,"username":"wrenfallow","invited":true}`, the row took `admitted_at`/`invited_at`/`admitted_account_id`, `provisionAccount` minted the account, and that account then logged in and completed onboarding. So the claim → find-or-create → invite sequence works end to end on a real address, with dev's `EMAIL_PROVIDER=console` swallowing the message. **What remains unverified is the PANEL, not the route**: the per-row action, the confirm dialog and the three status renderings have still never been rendered — narrower than this bullet used to be, and still the oldest unseen thing on the surface. Fold into the §11 browse.

   Prod note (corrected 2026-07-30): the earlier claim here — "the three live waitlist rows will produce one digest on the next deploy" — was false: the deploy landed >24h after those rows, and the cold-start window was `now − interval`, so the first run reported nothing and would never have reported them. Fixed 2026-07-30 (cold start now opens at epoch — the fix that also un-rotted the four clock-dependent digest tests found red on master); whether the fix surfaces those rows on prod depends on whether a watermark exists there yet — see the correction in CLOSED-BETA-ADR §XI.4. → CLOSED-BETA-ADR §VII + §XI; relates to §3.3 onboarding.

## 4. Subscriptions Phase 2

From `SUBSCRIPTIONS-GAP-ANALYSIS.md`; Phase 1 complete. More valuable now that F1 made subscription money real and Wave 3 made it collectible.

1. Free trials (writer-configurable 7/30-day).
2. Welcome-on-subscribe email (cheap — plumbing exists).
3. Subscriber analytics trends (growth/churn/MRR — data already there; also the "subscriber/follower dashboard metrics" table-stakes item).
4. Subscriber CSV import/export.
5. Custom `/username/subscribe` landing page.

(Gift subscriptions: see §1.10 — grant mode exists but is unredeemable; that's now a decided-scope item, not a verify.)

## 5. Trust & safety / messaging table-stakes

Carried from the retired feature-debt §5 (not re-verified this pass; last audited 2026-06-20):

1. Report → reporter outcome feedback (small).
2. DM conversation management: leave / archive / mute / delete.
3. DM charge-and-unblock 402 enforcement — pricing config persists but is inert; DMs are effectively free until a charge-and-unblock endpoint ships.
4. Session-management UI — `sessions_invalidated_at` gives all-devices logout only; no per-session view/revoke.
5. Admin direct-suspend UI (`POST /admin/suspend/:accountId` exists, no frontend trigger).

## 6. Slice-8 dedup cluster (one design pass, not piecemeal)

Deliberately deferred to a single session because the three knobs compose. Dedup suppresses content — a false-positive merge silently hides a different person's posts.

1. `domain_match` auto-merges distinct people sharing a website/custom domain — require a second independent signal or down-weight `website`. (MEDIUM)
2. Migration-123 200-char fingerprint collision — two distinct long posts sharing a boilerplate head false-merge; raise the length floor or full-text hash. (MEDIUM)
3. `confidence` recorded but never consulted by suppression — a 0.6 auto link hides content as hard as a 1.0 user assertion; make it a real dial. (MEDIUM)
4. Deferred detectors/features: `cross_link` bio-parse detector (needs cached bio metadata first); quorum promotion (explicitly don't-build-v1); route-layer Vitest suite; `EXPLAIN ANALYZE` the dedup CTE against a seeded large feed — *partially covered 2026-07-25*: the dedup CTEs were EXPLAIN'd in-situ during the feed-perf rewrite, but dev carries zero identity links, so only the zero-link fast path ("near-zero cost" claim: confirmed) was exercised; the high-link-density case still needs a seeded measurement. → `SLICE-8-IDENTITY-LINKING-PLAN.md`.

## 7. Cleanup cluster (mostly closed)

**Batch-closed 2026-07-20** — fourteen of the eighteen items shipped in one pass, two dispositioned without code, two narrowed (FIX-PROGRAMME 2026-07-20 "§7 cleanup cluster" entry has the item-by-item record + validation). Closed: 1 (Composer dead chip/DM machinery), 2 (ParentContextTile + `PostCard.header` + `reply_group`), 3 (`expandedByFeed` leak), 4 (stale `reply_to_author` — passes 5/6, empirically verified), 5 (njump permalinks), 7 (per-host skip logging), 8 (lint suppressions), 9 (infinite-scroll race — ref latch), 12 (migrate guard — comment-stripping + multi-statement refusal, mutation-verified), 13 (Bluesky handle renames — Jetstream identity events, premise verified live), 15 (card chassis → rem), 16 (live hairline debt), 17-part-1 (publications PATCH column allowlist + exhaustiveness guard), 18 (BalanceHeader sign-convention copy — bullet restored to the FIX-PROGRAMME entry 2026-07-21, §0i.10).

**Dispositioned, no code:**
- **6 — closed as compliant.** The FeedComposer source row is CLAUDE.md's sanctioned `ref` fallback and routes account vs pub/source/tag separately; `<ProfileLink>` would mis-classify `/pub`, `/source`, `/tag` as native profiles. Do not "fix" this.
- **10 — rejected, not implementable.** `referrerPolicy` is not valid on `<video>` (HTML allows it on `a`/`area`/`iframe`/`img`/`link`/`script` only); it fails typecheck. Bounded already by nginx's document-level `Referrer-Policy: strict-origin-when-cross-origin`.

**Still open:**

1. **Card body px → rem (narrowed from item 15).** The card BODY is sized by the per-feed text-size control as an inline px number (`ctx.bodyPx` ← `TEXT_SIZE_PX`), so it doesn't scale with the global type-size control. Converting it would COMPOUND two user-facing size controls — a design decision (do they multiply? does per-feed become a relative offset?), not cleanup. Everything else in the chassis is now rem.
2. **PipPanel hairline debt (8 literal 1px).** Left deliberately: the component is dormant (trust parked, `PipTrigger` not mounted), so it renders nothing. Burn down if/when the pip panel is revived — restoring it must not reintroduce them.
3. **§7.11 residue (flag-only, unreachable today):** D11 `signAndEnqueue` signs outside the enqueue txn (flagged, no fix); D13 overlay history/scroll-lock edge cases; D14 feed-batching test coverage; cross-overlay supersede (reader→profile) can orphan a history entry so Back lands on a stale URL (pre-existing); migrations 122/123 carry non-idempotent bare statements (low risk under the run-once guard).
4. **§7.14 carried nits:** native note-replies hard-dropped regardless of per-source `exclude_replies` (pre-existing); profile self-delete dropped (documented scope cut); `TagBrowser` not on `PageShell`. (The `collision.test.ts` "pushes upward…" flake is **closed 2026-07-21** — that file predated the infinite floor and asserted a horizontal bound and `x >= 0`; superseded by the co-located suite, live coverage ported. See FIX-PROGRAMME 2026-07-21.)
5. **§7.17 remainder** (`AUDIT-REPORT.md` residue): stale CLAUDE.md doc refs (#14); inconsistent error shapes (#19 — see §8.8; the `sendError` helper was deleted in the Day-6 P2 pass, so this needs a fresh helper); pervasive `as any` (#20, ~23 in web); `requirePublicationPermission()` no-args (#21) and no note soft-delete (#24) — both still carried unverified; naming (#25–27; `platform-pub`/`platformpub` vs all.haus in compose/package/credentials). (#6, the publications PATCH interpolation, closed 2026-07-20.)

## 8. Performance, scaling & infrastructure

1. **Infra (operator, no repo change):** CDN in front (single biggest felt-speed lever); Brotli only after switching off stock `nginx:alpine`; Postgres tuning / own box; measure first (Lighthouse + WebPageTest, two regions, `/reader`). → `allhaus-performance-audit.md`. **Next prod deploy:** run a one-time `ANALYZE;` and check autovacuum is analyzing `feed_items` — dev's was found never-analyzed (empty planner stats) during the 2026-07-25 feed-perf work; verify prod isn't in the same state (`pg_stat_user_tables.last_autoanalyze`).
2. `output: 'standalone'` + matching Dockerfile change (verified still absent from `next.config.js`). → perf audit #7.
3. Short-TTL gateway cache for hot public reads (article-by-dTag, author profile) — after the CDN lands. → perf audit.
4. **Feed-ingest Tranche C** (deferred until metrics demand — trigger: feed p95 tracking total `repost_edges` size): per-host politeness relocation into the fetch task then drop the per-tick cap; Jetstream firehose sharding past ~200 subs; materialised per-reader candidate pool (the remaining escalation if the 2026-07-25 rewrite's headroom runs out). → `FEED-INGEST-HYDRATION-PLAN.md`. ~~Two-phase paginated read~~ + ~~`EXPLAIN ANALYZE` the scored CTE against a large dataset (#15)~~ — **DONE 2026-07-25** for the workspace items query (the surface that actually degraded): `EXPLAIN` found the OR-of-arms `matched` cross product (24.9M pair evaluations/page), per-query JIT (~2.5s), and per-candidate projection; fixed as per-arm UNION + rank-then-project + pool-level `jit=off`. 7,246ms → 78ms on a 717-source feed at 34.7k items. FIX-PROGRAMME 2026-07-25.
5. Split `payout.ts` (now **2,378** lines after the Wave-2/3 work, up from 1,983; four concerns) — the money-path changes above should land first so refactor and behaviour change don't tangle. Also on the decomposition list: `WorkspaceView.tsx` (**2,083** post-columnar-rewrite — the old do-not-decompose-independently caveat is discharged: §9.14 shipped 2026-07-22, so its floor half is now stable and it can be split on its own schedule), `FeedComposer.tsx` (1,426), `UpstreamEdges.tsx` (1,300), `resolver.ts` (1,496). → code-economy §3/§6.6.
6. Extend `knip` to gateway/web — the triage pass is the real work (it's a CI job; config-first turns CI red). → code-economy §6.6.
7. Gateway decomposition: scheduler extraction deferred; full service extraction (messages → messaging-service) hedged but not started. → `GATEWAY-DECOMPOSITION.md`.
8. `000_base.sql` genesis extraction (item 1b) + `platform_config` seed rows in `schema.sql` (fresh prod boots run every operator-tunable key on code defaults). Deliberately deferred; the drift guard's residual column-level blind spot closes with it. → `ARCHITECTURE-AUDIT-IMPLEMENTATION-PLAN-2026-06-15.md`; AUDIT-BACKLOG B1/D1.
9. Standardise gateway error response shapes (4 shapes across 24 files; needs a new helper — see §7.17). CSP nonce middleware (removes `'unsafe-inline'` from `script-src`; careful with hydration). Accessibility pass. TS target alignment.
10. Cross-post outbound worker lacks `FOR UPDATE SKIP LOCKED` + deliver-then-mark-sent → crash can redeliver (no atproto/nostr idempotency key). Pre-existing, carried by the retry-helper extraction.
11. CSP `connect-src`/`media-src` widened to blanket `https:` for hls.js — tighten to a video-CDN allowlist if the origin set is bounded.
12. **Deployed-revision stamp (deploy hygiene, cheap).** There is no way to ask prod what commit it is running. Establishing it on 2026-07-21 took four ad-hoc `grep`s of compiled `dist` output plus a `docker image inspect` comparison across two timezones (image times UTC, `git log` BST) — and two of the four markers were invalid on the first try: `docker compose ps`'s CREATED column is *container recreation*, not image build, and an identifier grep against `web` is meaningless because Next minifies client-bundle names (only string literals survive). Bake `GIT_SHA` in at build time (a compose `build.args` → `ENV`) and expose it on each service's health endpoint, so "is prod on master" is one request instead of a bespoke forensic exercise. Related: the same session found prod's **schema ahead of its code** (migrations 158–161 applied, feed-ingest still on a day-old image) — benign only because all four were additive; a revision stamp makes that state visible instead of inferred. → FIX-PROGRAMME 2026-07-21 (deploy).

## 9. Features (specified, unbuilt)

1. **Reposts** — native kind-6 publishing; inbound + outbound infra exists, "Reposted by" label threaded but unrendered, no RepostButton on the Post path (verified: no component, no publish route).
2. **Email-on-publish Phases 2–4** — Phase 1 (send-on-publish) live; digests/scheduling/analytics phases unbuilt (14 requirements). → `EMAIL-ON-PUBLISH-SPEC.md` via ADR-CONFORMANCE.
3. **Publications Phase 4** — theming, custom domains (wildcard subdomains, DNS verification + TLS, custom CSS, per-pub favicon). → `PUBLICATIONS-SPEC.md`.
4. **`pub_payout_completed` notification** — spec'd, never emitted. → ADR-CONFORMANCE.
5. **Currency strategy** — Option 2 (GBP + display-only conversion) recommended, unbuilt. → `planning-archive/platform-pub-currency-strategy.md` (still cited live).
6. **Bucket system** — conceptual only. → `platform-bucket-system-design.md`.
7. **Frontend test coverage** — 6 test files exist; the unlock flow, gate state machine, and publish flow still untested. → frontend audit #9.
8. **Deferred greenfield from UNIVERSAL-POST** — `POST /post/:postId/react` external scoresheet (external repost/save stay quiet placeholders). → UNIVERSAL-POST-ADR §9/§10.
9. **Follow via email sources** — `addSource` can't take email, so email newsletters have no Follow affordance; and adding a source via Follow doesn't force an immediate feed refresh.
10. **Tribute chains publication×tribute composition (D1 revisit)** and chain-privacy refinement — deferred within the upstream-edges work. → UPSTREAM-EDGES-BUILD-PLAN.
11. **Free colour wheel per feed** + per-scheme accent token (its own ADR). → FEED-SCHEME-REFRESH §V.
12. **Resonance — the A/B, and the two brakes** (foundation shipped 2026-07-19: migration 158 + daily `engagement_baseline_refresh`; **step 3 shipped 2026-07-20**: `feed-ingest/src/lib/resonance.ts` writes resonance/band/pctl from both refresh crons, band gates retuned 1/2/3 → 2.5/4/6 in migration 160 after the distributions came in 2–3× hot — see FIX-PROGRAMME + the ADR's *Step-3 measurement*; **step 4 shipped 2026-07-20**: D7 glyph + tooltip + `card.resonance` Explain caption + full `FEED_SELECT`/mapper/`Post`/spec-matrix plumbing, behind the default-OFF operator brake `RESONANCE_GLYPH_ENABLED` which gates the mapper so the band never leaves the gateway while dark; **step 5 shipped 2026-07-20**: D6 read-time proof blend spliced into the `scored` CTE of `sourceFilteredItems` from `gateway/src/lib/feed-rank.ts`, behind the default-OFF, independent brake `RESONANCE_RANKING_ENABLED`, plus migration 161's `feed_proof_floor` correction — D6-as-drafted said NULL-band items "take proof_term = 0 and rank on recency", which is impossible since 0/(age+2)^g is 0 at every age, collapsing every silent item to a uuid tiebreak).

    **Test battery built 2026-07-20** (the ADR specified one; steps 4–5 had tests, the scoring engine had none — the "component ≠ feature" shape from §11). 27 live-DB tests, both files driving the crons' own exported SQL: `resonance.test.ts` (20) + `engagement-baseline-refresh.test.ts` (7); feed-ingest 221 → 248. Mutation-verified 16/18, and the two survivors turned out to be **provable redundancies, not gaps**: the band-2 ambient veto is unreachable (n ≤ 20 ⇒ baseline ≥ 3·p50/23 ⇒ resonance ≥ 4 already implies E ≥ p50), so D4's veto does real work on band 1 only and only above ambient p50 ≥ 22; and `PCTL_EXPR`'s `p50_e <= 0` branch duplicates the general path. Both retained + documented. ADR battery corrected on one point (the "≥87% own-median" figure is exactly 86.96% at p50 = 0). → FIX-PROGRAMME 2026-07-20.

    **What remains:**
    - **Monotonic writes** is the one battery item still uncovered — it belongs to the engagement cron's count writer, not to the two modules the battery covers.
    - **The A/B itself.** The flag is the mechanism; the measurement is the work, and it needs prod volume. Note from the step-5 smoke: with the flag off, every item in a *scored* external feed scores `fi.score = 0`, so today that surface is ordered **by uuid** — the flag-off state is therefore not a meaningful control for "did resonance help?"; the honest comparison for external-heavy feeds is against **chronological**. (ADR *Step-5 note*.)
    - ~~**Turning the step-4 glyph brake on**~~ — **LIT 2026-08-10**, by inverting the gate rather than discharging it first. The gate was "re-measure band-3 incidence per protocol on prod before showing the glyph"; while the beta is closed the only reader is the operator, so *browsing real ingested posts IS the measurement*, and holding a display-only mark dark to protect an audience of one costs more than it buys. Safe to lead with because the band gates are `platform_config` dials — a wrong distribution is an `UPDATE`, not a deploy. `RESONANCE_GLYPH_ENABLED` default `0` → `1` in `docker-compose.yml`; douse via `RESONANCE_GLYPH_ENABLED=0` in the **root** `.env` (compose `environment:` beats `gateway/.env`). **The measurement it was gated on is now the follow-up, not the precondition** — run it on prod after a week of real browsing:

      ```sql
      SELECT coalesce(source_protocol, 'native') AS proto,
             count(*) FILTER (WHERE resonance_band IS NOT NULL) AS scored,
             round(100.0 * count(*) FILTER (WHERE resonance_band = 3)
                   / nullif(count(*) FILTER (WHERE resonance_band IS NOT NULL), 0), 2) AS band3_pct,
             round(100.0 * count(*) FILTER (WHERE resonance_band >= 1)
                   / nullif(count(*) FILTER (WHERE resonance_band IS NOT NULL), 0), 2) AS lit_pct
        FROM feed_items
       WHERE deleted_at IS NULL AND published_at > now() - interval '14 days'
       GROUP BY 1 ORDER BY 2 DESC;
      ```

      Target is band-3 ≈ 1% per protocol. **A single global gate cannot land 1% on both** — dev sits at atproto 3.2% vs activitypub 1.3% of scored rows, and prod will differ again — so if the spread holds, the honest fix is a per-protocol gate (new dials), not a compromise value that is wrong for both. Retune meanwhile with `UPDATE platform_config SET value = … WHERE key = 'resonance_band3_min'` (currently 6; bands 1/2 at 2.5/4). **Revisit the default the day the beta opens.**
    - **The free-up-vote weight 5** (`resonance_weight_native_up`) is still unrevisited — dev had 8 scored native items, no signal. Unchanged by the glyph going live: it only bites once native posts draw votes.
    - **A gap in scoring is PERMANENT, and nothing reports it** (found 2026-08-10, the moment the glyph was lit and showed nothing). The scorer runs only over the ids whose engagement counts MOVED in that pass (`batchUpdateCounts` → `refreshResonanceFor`). That is right in steady state and wrong after any interruption: a row whose counts have settled is never revisited, so it keeps a NULL band **forever**. Dev had run a feed-ingest image built 2026-07-19 23:05, one day older than the scorer that shipped 2026-07-20 — so 21 days and ~20k external items were ingested with not one band written, and the newest banded row in the database sat frozen at 2026-07-20 17:34. Every symptom was silent: the crons logged success, `protocol_engagement_ambient` refreshed daily (that task WAS in the image), counts kept updating, and the only visible consequence was a feature that renders nothing — which is also exactly what a correctly-dark brake looks like. Three things follow, none done:
      - **The hole does not self-heal**, so any fix needs a backfill. `scripts/backfill-resonance.sql` is the one-off (a transcription of the cron's own SQL with the driving set widened from "these ids" to "everything unscored in the window"); it is not a substitute for the scorer and must be re-read against `resonance.ts` before each use.
      - **Nothing measures scoring freshness.** `max(published_at) WHERE resonance_band IS NOT NULL` falling more than a day behind `now()` is a one-line check that would have caught this on 2026-07-21 instead of 2026-08-10. It belongs somewhere an operator actually looks — the admin overview beside the internal-parity banner is the obvious home.
      - **The general shape is the trap**: a derived column whose writer is keyed on "what changed" cannot recover what it missed while it was not running, and a display feature reading that column reports the gap as *absence*, which is indistinguishable from the feature being off. Any future cron of this shape wants a periodic sweep over NULLs, not only a delta pass.
    - **The platform axis needs headroom above p90 before it can have a second step.** `PCTL_EXPR` saturates at 1.0 for anything ≥2× p90, and the tail past it is fat (6.8% of scored atproto rows sit at exactly 1.0 against 10.3% at ≥0.9), so the origin-row mark is deliberately single-state — a two-step split would make the modest mark rarer than the loud one. The fix belongs in the **scorer** (a p99 landmark, or log-scaling the tail past p90), never a threshold in the client; until then do not add a second platform glyph. Note this axis needs **no** per-protocol gate, unlike the bands below: a percentile is per-protocol by construction (10.3% atproto / 11.1% activitypub off one number).
    - **Per-protocol band gates are now the live question, with a real measurement behind them.** With the backfill run, seven days of fresh data give **atproto 22.4% lit / 5.02% band 3** against **activitypub 10.1% / 1.25%** — a 4× spread on band 3 against a ~1% target, so one in twenty Bluesky posts currently draws `···`. This is the measurement the ADR asked for and the reason the flip was worth making: it could not have been taken while the glyph was dark and the scorer was dead. A global `resonance_band3_min` bump fixes Bluesky and breaks the Fediverse, so the honest fix is per-protocol gates (three new dials × protocol, plus a reader — the tuning-dials invariant's "a dial with no reader is not a dial" applies), not a compromise number.
    - Later: zap ingestion, native repost recording, "resonant only" filter (partial index already shipped). → `docs/adr/SOCIAL-PROOF-RESONANCE-ADR.md`.

13. ~~**`platform_config` defaults never land on a fresh DB**~~ — **FIXED 2026-07-20**, same day it was found (and it was a *re*-discovery: AUDIT-BACKLOG D1, 2026-06-07, diagnosed the same root cause and took a code-default workaround for one key). Defaults moved out of migrations into `shared/src/db/config-defaults.sql`, applied by `migrate.ts` on every run (`ON CONFLICT DO NOTHING`); **46 dials seeded on dev** — 31 migration-seeded, plus 15 that had no default row anywhere, including the six money dials (`platform_fee_bps`, `free_allowance_pence`, both settlement thresholds, `writer_payout_threshold_pence`, `monthly_fallback_days`) that commit `f8c73e6` silently dropped from `schema.sql` in a `--schema-only` regeneration. `jetstream_healthy`'s bare-UPDATE write became an upsert — the one place the gap caused live misbehaviour (the atproto polling fallback could never engage). CI-enforced by drift-guard Checks 4a/4b/4c. See FIX-PROGRAMME 2026-07-20.

    **Residuals:**
    - **Needs the operator:** whether **prod** was affected. It may have been migrated incrementally from before genesis, in which case its rows exist. The next prod `migrate.ts` prints `Config defaults: seeded N missing dial(s)` — record N and compare against dev's 46.
    - **`feed_ingest_max_errors` vs `feed_ingest_max_error_count`** — two keys, one meaning, different defaults: the **email** adapter alone reads `feed_ingest_max_errors` (50), every other adapter reads `feed_ingest_max_error_count` (10), so an email source tolerates 5× the consecutive failures before deactivating. Almost certainly an oversight. Both are now seeded at their current values so behaviour is unchanged; unifying them is a deliberate ingest-behaviour change, hence logged rather than done.
    - **Three dead keys** (`note_char_limit`, `comment_char_limit`, `media_max_size_bytes`) were in the original genesis seed and have no reader anywhere in the repo. Deliberately not resurrected; delete the concept properly if a char limit is ever wanted.

14. ~~**The columnar workspace floor — Slices 4–5**~~ — **CLOSED 2026-07-22: the ADR is fully shipped, Slices 0–5.** Kept here only for the record of what it replaced; nothing outstanding. (`docs/adr/WORKSPACE-COLUMN-LAYOUT-ADR.md`, accepted 2026-07-22.) It replaced the free-coordinate floor with a model where geometry is **derived** from a stored *order* (columns left→right × slots top→bottom, plus per-slot sizes): illegal states become unrepresentable, so the whole repair layer — mover-yields collision, the resize clamp, the bootstrap heal, the signed-origin extent — is **deleted**, not fixed. The observed failures were exactly those repairs firing (a declined merge re-solving and relocating the source; the heal shelving a vessel off-screen; the extent stretching to cover it). The expected diff is strongly net-negative.

    **Slices 0–5 ALL SHIPPED 2026-07-22** — see FIX-PROGRAMME. Slice 0 (virtualization) was sequenced first deliberately: it is the fix for the Firefox memory pressure, it lands against the *current* floor, and none of it is thrown away by the rewrite. Slices 2 + 3 landed as one commit (`7a17150`) as planned — the old `WorkspaceView` read `positions` and could not compile against the new store, so an adapter shim would have been wasted work — carrying the v1→v2 localStorage migration and every §IX deletion except the lens, at **net −341 lines**. Slice 4 then took the lens and its every satellite.

    - ~~**Slice 4 — nav row + lens removal + Explain re-anchor.**~~ **SHIPPED 2026-07-22.** `NavRow.tsx` (`NAV_ROW_H = 56`) at z-58, Glasshouse insetting above it via `usableH`, and the lens dead with every satellite. One decision against the literal spec: the **lockup stays intact** at the row's right end rather than splitting wordmark-left / ∀-right, because §V's disc-to-cap-height ratio exists precisely so the two read as kin and the wordmark is part of the trigger — what §VI was after (killing the wordmark's *separate fixed layer*) is taken either way. See the ADR §X as-built note.
    - ~~**Slice 5 — the regimented hotkey.**~~ **SHIPPED 2026-07-22.** Plain `\` toggles a derived parade-ground view over the feed list; the stored layout is never replaced, which makes restore trivial and crash-safe. Pure `WorkspaceView` wiring — the layout module and store pieces had shipped in Slices 1–2 unchanged. Five as-built deviations (numeral-ordered parade, memoised feed list, the clamp re-pointed at the derivation the floor is arranged by, feed-list changes deliberately NOT materialising, scroll-to-0 on enter) in the ADR §X.

    **Doc obligations rode each slice's own commit** (ADR §IX) and are now **fully discharged**: CLAUDE.md's floor section was rewritten as the columnar rule with Slice 3 (the old "infinite sideways" rules retired outright, not footnoted) and the WORKSPACE-DESIGN-SPEC mover-yields addendum marked SUPERSEDED; Slice 4 then took the lens paragraphs (∀ mark / FORALL-CUT-AND-LOCKUP-ADR §IV, now *superseded* rather than *superseded-in-waiting*) and amended the crisp-above-the-frost invariant with the nav row's z-58 band. → `docs/adr/WORKSPACE-COLUMN-LAYOUT-ADR.md` §X.

    **Post-ship audit batch, same day (`2654eaf`)** — a code audit of the shipped slices found and fixed two behavioural bugs (unbounded vertical drop bands could stack a column past the viewport into the unreachable band below the nav row → capacity gate; a no-op "never mind" drop under regimented mode materialised the parade over the custom layout → `dropIsNoop` guard) plus slot-rect hit-testing, the faithful hide revert (`locateSlot`/`restoreSlot`), and `SLOT_MIN_W` 220→224. Full record: ADR §X "Post-ship audit fixes" + FIX-PROGRAMME 2026-07-22. Nothing remains open from it.

15. **The nav-row muster — Phases 2–4** (`docs/adr/NAV-ROW-MUSTER-ADR.md`). **Phase 1 (stable numerals, §III) SHIPPED 2026-07-25** — feed numerals are now identity over the live set, so minimising a feed no longer renumbers its neighbours (FIX-PROGRAMME 2026-07-25). That was the prerequisite; the muster is unbuildable on the old visible-set numbering. Outstanding:
    - **Phase 2 — the muster** (§IV): a centred row of numbered roundels in the nav row (between its left edge and the ∀ lockup), one per live feed in numeral order, fixed 32px cells with size-encoded state (24px in-view / 18px panned-off / 18px minimised). Needs a **new viewport-intersection selector** — the existing `visibleIds` is the 3-viewport mount band, too wide; a roundel is "in view" only when its `geom.rects` rect intersects `[panOffset, panOffset + viewport.w]` (reuse the `VIRT_QUANT` hysteresis by reading the same quantised `panOffset`). Ring is 2px (not a hairline — `check-hairlines.sh`); fallback is fill+size alone. Click = "go to this feed" (§V), closing any open Glasshouse pane first (presence-registry `close()`), and the muster mounts inside today's empty `NavRow` (the lockup is a separate `ForallMenu anchor="row"` sibling, not a child).
    - **Phase 3 — polish** (§IV/§VII): overflow + centring rule, hover feed-name label (`descriptiveName` treatment), the Explain `navRow.muster` label, and the a11y pass (`aria-hidden` off the row, roundels as a labelled `<button>` group — **not** `role="tablist"`, several feeds are "in view" at once).
    - **Phase 4 — arrival flags** (§VI), a later ADR: user-authored criteria only (platform-authored marks excluded by rule), one crimson mark, no counts, clears on view.

16. **Retire the `reach` source kind — drop Explore, MATERIALISE Following** (decided by the operator 2026-08-10). `feed_sources.source_type = 'reach'` carries two things wearing one name, and they get opposite treatments. **`reach:explore` is deleted outright.** **`reach:following` is replaced, not deleted**: the Follow button starts writing a concrete `account` source into a feed, so "everyone I follow" stops being a rule evaluated at read time and becomes N named rows the user can see, weight, mute and remove. Both halves land together — `reach` leaves the schema in one migration, so shipping half of this leaves a column with one legal value.

    **Why materialise rather than just drop.** `gateway/src/routes/feeds/items.ts:307` is the **only** place in the repo where the `follows` table affects what anyone sees; every other reader (`follows.ts`, `writers.ts`, `author-resolve.ts`, `discovery-publish.ts` kind-3, `trust.ts`) is counts, lists or publishing. Delete that arm with nothing in its place and native Follow becomes a button with zero feed consequence — and it lands asymmetrically, because external follow already works the other way (`AuthorModal.tsx:504` adds an `external_source` row to the current feed, while `:491` native follow writes only a `follows` row). Two buttons labelled Follow, opposite meanings, split along exactly the native/external seam the site works to erase. Materialising converges them. **Note the ethos argument applies to explore, not following:** `reach:following` is a pure membership rule over the user's own explicit graph, chronological by default like any other source; the score ranking that sits uneasily with "no algorithm here" is explore's.

    **It is also the fix for feed portability.** A reach row is *deictic* — both arms resolve against `$1`, the caller — which is what makes a feed containing one mean something different for anyone else, and therefore what blocks a feed ever being a transmissible object (there is no share/export/clone path today beyond the operator-only `cloneFeedForOwner`; every `feeds` read is `WHERE owner_id = $1`). Materialise following and every source in every feed is a concrete named target, and a feed becomes a portable list of things rather than a mix of things and self-references. That does not build sharing, but it removes the reason sharing can't be built.

    **The pre-code prod checks are DONE (2026-08-10) and the answer is: nothing a member can see.** Prod carries exactly **3** reach rows across **2** feeds, both owned by the operator's own accounts (`billyisland` / `evanlake`), both plainly test feeds ("Substack RSS", "Test"): two unmuted `reach:explore`, and one `reach:following` whose owner follows **5** accounts. No member holds a reach row of either kind. Two consequences. **(1) The conversion step is struck** — see below. **(2) `items.ts:307` fires for nobody but the operator**, so native Follow is *already* consequence-free for every member on prod; dropping the arm regresses no member's feed, and the Follow-writes-a-source path no longer has to ship in the same commit as the migration (it should still follow closely, but the coupling is gone). The starter-template arm of the check came back `0` — **not** "the template carries no reach row" but *no feed on prod carries the flag at all*, confirming §0l item 1 is still unapplied; that is now a verified §0l finding, not a §9.16 blocker.

    **Beware the dev DB when re-checking any of this.** The same three queries against dev return `0` reach rows, and dev/prod agree to the row on `feed_sources` (1600), `accounts` (125) and migration state (176) — so a dev reading looks exactly like a clean prod gate and is not one. The discriminator is `feed_items`: dev **71,616** vs prod **90,069** at 2026-08-10. Run `hostname` (prod = `ubuntu-4gb-nbg1-1`) before believing any "prod" psql result; the `platformpub=#` prompt is identical on both machines.

    **The design questions — the two that blocked it are DECIDED (operator, 2026-08-10):**
    - **Which feed does a new follow write into? → the current feed; where there is none, the feed picker that already exists.** No new "default feed" concept is required, and none should be invented: the site has already answered this question for external follow and shipped the answer. `web/src/components/profile/ProfileFollowControl.tsx` splits on the server's `followTarget.type` — `user` gets a plain global toggle, `source` gets `SourceFollowPicker`, a **"Follow ▾" menu of the viewer's feeds, each row toggling membership, with "following" = in ≥1 feed**. That is exactly the affordance a materialised native follow needs, so native follow stops taking the `user` branch and takes the same `source` branch. Nothing in `feeds` needs a `is_default` column (it has `sort_rank`/`hidden`/`is_starter_template`/`cloned_from_feed_id` and nothing meaning "default"), and a *derived* default would have been wrong anyway — lowest `sort_rank` silently moves when the user reorders their workspace, so the same Follow would land in a different feed from one week to the next.
      **The hover card gets the picker too** (operator, 2026-08-10) — rather than losing its Follow button on the feed-less surfaces. `AuthorModal` takes `feedId` as optional and today hides external follow outright when it is absent (`:530`, `if (isSource && !externalFollowable) return null`); `PostByline` passes it through as optional, so the article page and `/read/:postId` are real feed-less surfaces where native follow currently works only because it is global. Surveyed 2026-08-10, **every mechanism this needs already exists** — it is composition, not invention:
      - **The panel already hosts a stateful gadget that writes to the server.** `SourceVolume` (5-step weight + RANDOM/TOP sampling, resolving a `feed_sources` row and committing via `patchSource`) lives inside `AuthorModal`; `web/CLAUDE.md` records it as where the parked pip panel's per-feed actions went. An interactive control in that panel is the shipped design, not new ground.
      - **The close behaviour is a hover BRIDGE, not a blur-close.** `useAuthorHover` runs a 300ms open debounce and a **220ms** close grace as independent timers, and `onModalMouseEnter` cancels the pending close, so the panel stays open indefinitely while the pointer is inside it — explicitly "long enough for the pointer to cross the gap … (and between regions of the modal)". (The 150ms blur-close is **VesselBar's**, a different component — don't cite it here.)
      - **`dismissOnMouseLeave` is the seam for the harder case** (`AuthorModal.tsx:28/62/145`): pass `false` while the picker is open and the panel becomes click-dismissed instead of hover-dismissed. Note it is currently **declared but never passed false** — this would be its first real use.
      - **`useEscapeShield`'s `yieldTo` already arbitrates this nesting.** AuthorModal → Lightbox is the shipped pair (both listen on `document`, where registration order wins, so yielding — not `stopPropagation` — is what works). AuthorModal → picker is the same shape: one Escape closes the picker, a second the panel.
      - **Both menus are built.** `SourceFollowPicker` does lazy first-open resolve, per-feed membership fan-out, optimistic toggles, outside-click dismissal, shield Escape, and a `newMode` creating a feed inline; `SchemeMenu` is the reference `role="menu"` (focus lands on the checked item, arrows rove, Home/End).

      **Positioning — render the picker INSIDE the panel's box, capped and scrolling; do not portal it as its own layer.** `AuthorModal` already `createPortal`s to the body at `position: fixed`, `width: 300`, `zIndex` 50 (56 over the FeedComposer Glasshouse). Its dismissal reads `modalRef.current?.contains(target)`, so anything rendered **within** that div inherits outside-pointerdown, the hover bridge and the click-swallow for free — while a separately-portaled menu would sit outside `modalRef` in the DOM and read as "outside", closing the whole panel on its own first click, *and* fire the panel's `onMouseLeave` on the way in. Both are fixable only by inventing the Lightbox-style store arbitration that the in-flow option gets for nothing. The list is a handful of the viewer's own feeds (`SourceFollowPicker`'s own note), so **give it `max-height` + `overflow-y-auto`** (precedent: `FeedComposer.tsx:413`) sized so the panel's total height stays within the ~320px budget `position` is computed against (`:81`, `below = rect.bottom + 320 < window.innerHeight`, evaluated once in an effect keyed on `anchorRef`). Constraining the height is what keeps that one-shot decision valid — prefer it to recomputing position on picker-open. Note the `above` case is already safe either way: it anchors `bottom: window.innerHeight - position.top`, so growth extends upward.

      **Residual: touch.** `web/CLAUDE.md` fixes the hover panel's reachability as hover-only with no touch path (mobile tunes volume in the FeedComposer instead), so the picker inherits **desktop only**. Native Follow from `/article` on mobile still needs an answer, and it cannot be this one. Not a blocker for the item; do not let it ship believing mobile is covered.
    - **What does unfollow do? → mirror the feed-derived model.** Unfollow removes the `account` source, and a source leaving the owner's *last* feed drops the follow — the same shape `removeSource` already runs for external sources, under the same owner-scoped advisory lock. This makes `follows` a **projection of `feed_sources`**, which is the point: it is what keeps native and external Follow from meaning two different things. Consequences to build deliberately, not discover: every write path to `follows` becomes `addSource`/`removeSource`'s business (nothing else may INSERT it), `markFollowListDirty` must fire on both arms so the published kind-3 list tracks membership, and the `follows`-reading surfaces that are counts/lists (`follows.ts`, `writers.ts`, `author-resolve.ts`, `trust.ts`) keep reading the same table and need no change.
    - ~~**Conversion of existing rows.**~~ **STRUCK 2026-08-10 on the prod count above.** The plan was to expand each live `reach:following` into one `account` row per followee so nobody's feed changed under them, held as a *snapshot* requiring the Follow-writes-a-source path in the same commit. With all 3 rows in the operator's own test feeds and no member holding one, **the migration just deletes them** — no conversion logic, no snapshot, no same-commit coupling. Re-add the 5 follows as `account` sources by hand afterwards if those test feeds are still wanted. Reinstate this bullet only if a re-count before the migration shows a member row (it should be re-run at that point — 3 rows is a number that can change).

    **Mechanical scope, surveyed 2026-08-10 (small, and well contained).** DB: one new migration (114 is applied and seeded — corrections go in a NEW migration) deleting extant reach rows, dropping `feed_sources_reach_uniq` and the `reach_kind` column, and rewriting `feed_sources_source_type_check` + `feed_sources_target_matches_type`; then regenerate `schema.sql`, re-append the seed, run the drift guard (Check 3 nets drops out chronologically, so the dropped index resolves clean; no `CONCURRENTLY` involvement). Gateway: two `UNION ALL` arms out of the `matched` CTE (`items.ts:298-331`); in `routes/feeds/sources.ts` the schema variant (`:86-92`), the `addSource` branch (`:290-296`), `insertSource`'s column, `sourceRowToResponse`'s case (`:215-230`), the `FeedSourceRow` union member, and `reach_kind` out of three SELECTs (`:553/:723/:1036`). Web: `ReachKind` + the union members in `lib/api/feeds.ts`, `handleAddReach` + the two chips in `FeedComposer.tsx` (`:308`, `:683`), the `feedComposer.reach` Explain caption (`explain/registry.ts` + `copy.ts`). **`settings.reach` is unrelated** — that is outbound network reach (crossposting), do not touch it. Docs: a superseding note on FEED-RETIREMENT-PLAN (which is marked COMPLETE — amend, don't rewrite), the D6 α passage in SOCIAL-PROOF-RESONANCE-ADR §297/§465, the `RESONANCE_RANKING_ENABLED` row in DEPLOYMENT.md, and **§0l.4** here (the feed-size cliff, which names the `reach:explore` row as one of the two things that widen the `matched` CTE's candidate set — dropping it narrows that cliff slightly but does not close it; the `tag` correlated `EXISTS` is the other half and survives).

    **Two consequences to accept knowingly.**
    - **`feed_alpha_explore` becomes a dial with no reader, permanently.** Migration 158 seeded it, so Check 4c means it can never leave `config-defaults.sql` — removing it fails CI by construction. The tuning-dials invariant says the fix for a dead dial is to give it a reader, never to drop the seed; here there is nothing left for it to mean. CI stays green either way, but it is a wart that cannot be cleaned up, so decide it deliberately rather than discover it in the migration.
    - **D6's α loses its discriminator.** `feedAlphaCte` (`gateway/src/lib/feed-rank.ts:92`) picks α by asking whether the feed carries a non-muted `reach:explore` row — that is the *only* signal separating an "explore surface" from a "following surface". It collapses to a constant, and the explore A/B behind `RESONANCE_RANKING_ENABLED` (§9.12) needs a new discriminator before it can run. The flag is dark, so nothing breaks today; the A/B acquires a prerequisite.

    **Explicitly NOT in scope: the empty-vessel placeholder** (§2.7), deferred by the operator the same day. Explore therefore survives this item as the empty-feed default — read §2.7 before assuming this item removes explore from the site.

## 10. Deliberately parked (not pickup work — listed to keep it out of the queue)

- **Trust graph** (behind `TRUST_SYSTEM_ENABLED`; Phases 5–6 gated on scale) and the dormant PipPanel/PipTrigger. Known spec gaps parked with it: Layer 4 "valued set" signals hand-waved; Layer 1 signals for external authors not persisted by adapters.
- **Traffology** (containers down, beacon off; Phases 2–4 with it). Its untested aggregation math (Payments ADR §1.3(4), archived) rides with it — write those tests only if traffology comes back.
- **Pledge drives + commissioning** (behind `PLEDGES_ENABLED`, parked 2026-07-13; not deleted, revivable). Its ledger/tab fulfilment tests (Payments ADR §1.3(2), archived) ride with it — write them only on resurrection. ~~Two defects found 2026-08-05 that must be fixed before it is unparked~~ — **FIXED 2026-08-05 (migration 174), ahead of resurrection rather than with it.** `drive_funded` and `commission_request` were the only two `INSERT INTO notifications` with no `ON CONFLICT` at all, so a collision raised 23505, and `drive_funded`'s sat inside the pledge `withTransaction` — the same pledger funding a second drive by the same creator **aborted the pledge itself**. The clause alone would only have traded a crash for a silent drop, because the dedup index carried no drive reference: `notifications.drive_id` has existed since migration 020 "for frontend routing", is selected by the gateway and declared by the web, and **no insert had ever written it**. So 174 binds the column on all three drive notifications, puts it in the index (two drives are two rows) and adds the clause. Four DB-backed cases, both halves mutation-verified — see FIX-PROGRAMME 2026-08-05.
- **Network-concierge Phase 4** (see §2.3).
- **Content spine / federation** (item 2 Plan C) — deferred to the self-host effort with the genesis migration.
- **Feed-ingest Slices 4–7**: Telegram (demand-gated), Farcaster + Matrix (infrastructure-gated), AP inbox (posture-gated).
- **Preset themes** (`ThemeSection` parked, operator PalettePanel retained).
- **Omnibus Phase 8** external-content rendering legal review (the `/extract` reader exists; the DMCA process item stands if that surface widens).

## 11. Verification debt (work is done, proof isn't)

> **STOP WRITING "PENDING PROD DEPLOY" ENTRIES. The developer pulls and deploys whatever was pushed, at the end of every session** (stated 2026-08-07, and it explains all five staleness incidents in this section as ONE wrong assumption rather than five lapses). The default state of anything pushed in a previous session is therefore **live**, not queued — an entry written at the end of a session is describing a deploy that will have happened before anyone reads it, which is precisely how one came to be stale thirty-four minutes before it was written.
>
> What to write instead, when a batch needs something beyond pull-migrate-build-up: a **DEPLOY REQUIREMENTS** note — the env var, the migration ordering, the secret pre-check, the flag flip — stated in the session's own response FIRST (that is what the developer actually reads), and mirrored here only so it survives the conversation. Requirements are durable; "pending" is a claim about the world that rots on contact with it.
>
> And the corollary that matters more: **a push is a ship.** Nothing goes to master expecting to be finished next session — by then it is in front of members. If something is genuinely not ready to be live, say so in the response and do not push it.

- ~~**PENDING PROD DEPLOY, opened 2026-08-07 (W2 + internal parity + the welcome)**~~ — **DISCHARGED 2026-08-07 18:15, and it had never been pending: the deploy ran at 17:12, THIRTY-FOUR MINUTES BEFORE this entry was written at 17:46.** Not stale by days, as the four occasions below were — stale before the ink dried, because it was composed from the repo's commit list rather than from the box. Measured, in this order, and the first three commands of the pre-check surfaced it:

  - `All migrations already applied`, `_migrations` = **176** against 176 files on disk;
  - all four code markers positive in the RUNNING containers — `onboarded_at` **3** in gateway's `dist/routes/auth.js`, `onboardedAt` **1** in `shared/dist/auth/accounts.js`, `PARITY_PEERS` **4** in `dist/lib/internal-parity.js`, `allocation-coverage.js` present in payment's dist;
  - every container created 55 minutes earlier, so `up -d` recreated nothing.

  **The flag claim below was wrong in the same way** and is corrected here rather than deleted, because the error is the useful part: "prod's compose default is `0`, so this is a deploy-time env addition" was read off `docker-compose.yml`. Prod's root `.env` had carried `FOLLOW_IMPORT_ENABLED=1` all along; appending it produced a duplicate line (removed, backed up, diffed to exactly one line). **`docker compose exec gateway printenv FOLLOW_IMPORT_ENABLED` returned `1`** — the running process, which is the only thing that decides whether the welcome is four steps or three. A line in `.env` is not a flag in a container; only `printenv` on the container is.

  **The internal-parity probe's first production run passed** — three boot lines at 17:12, `Internal parity: confirmed` for `payment-service`/`INTERNAL_SERVICE_TOKEN`, `key-custody`/`INTERNAL_SECRET` and `key-service`/`INTERNAL_SECRET`. The pre-deploy hash comparison (`printenv … | sha256sum` per peer, so no secret reaches the scrollback) matched on both secrets beforehand, so the fatal path was never armed. Gateway `(healthy)`, which independently means the 5-minute re-probe has found no drift since.

  **Two durable additions to this section's standing rule.** (1) *Same-day is no protection* — the interval that matters is between the measurement and the claim, not between the deploy and today. (2) **`docker compose up -d` without a preceding `build` is not a deploy**, and it reports success either way: this pass skipped the build loop, which happened not to matter because the images already carried the code, but had the batch been genuinely pending, every marker would have come back stale after an `up` that printed nine green ticks.

  What the batch was, for the record:

  - `f3716b3e` **W2** — segregation coverage on `/admin/overview` (`payment-service`, gateway proxy, web).
  - `3dc8949a` — the admin proxy footprint fix (gateway, web).
  - `0a1bc534` + `2cddbe91` **internal parity** — the boot probe and its re-probe (gateway, plus `key-service`/`key-custody`/`payment-service` route touches).
  - the **welcome** (§3.3) — **migration 176**, a **`shared/` change** (`shared/src/auth/accounts.ts`), gateway (`auth.ts`) and web.

  **A `shared/` change makes EVERY backend image stale** regardless of whether its own directory moved, so this is the full six-service shape, not the three-service one. Migrate by hand first (`npx tsx shared/src/db/migrate.ts` — migrations never auto-run), then rebuild looping with `|| break`; **never a bare `docker compose build`** on the 4GB box.

  **Ordering for 176 is safe in BOTH directions** and the entry says so deliberately, per §0p.2: the column is nullable with no default, so old code (which never names it) runs fine against a migrated DB, and new code reads NULL as "not yet welcomed" — correct for every pre-existing row. Migrate first anyway, because that is the order that also holds for everything else in the batch.

  **One deploy-order risk that is NEW and does not exist anywhere else in this file.** The internal-parity boot probe **exits the gateway** on a definitive 401/403 from any peer. That is the design (`CLAUDE.md` › shared-secret invariant) and the ambiguous cases are correctly non-fatal — but it means this is the first deploy where a pre-existing secret mismatch stops the gateway from starting rather than silently breaking paywalled unlocks. **Confirm before deploying, not after:** `INTERNAL_SERVICE_TOKEN` must match between gateway and payment, and `INTERNAL_SECRET` between gateway and both key services. If a peer is rebuilt in the same pass, deploy the gateway LAST so its probe runs against peers already carrying the new code.

  **Markers** (each a string the change INTRODUCES, so 0 → n is decisive):

  ```sql
  SELECT EXISTS (SELECT 1 FROM information_schema.columns
                  WHERE table_name = 'accounts' AND column_name = 'onboarded_at') AS col_176;
  ```

  and in the RUNNING containers, not the images (the 08-07 lesson — a rebuilt-but-never-recreated service reads current by date and stale by behaviour): `onboarded_at` in gateway's `dist/routes/auth.js`; `onboardedAt` in `shared/dist/auth/accounts.js`; `PARITY_PEERS` in gateway's `dist/lib/internal-parity.js`; `allocation-coverage` in payment's `dist`. For **web** use a CHANGED chunk hash, never an identifier grep.

  **Flag note — measured in DEV, then wrong about PROD (2026-08-07), and the correction is above.** The dev half stands: flipped dark and back, the sheet is **3 steps with the counter reading `/3`** and "Bring your world" absent rather than blank, 4 with it live. The prod half — "prod's compose default is `0`, so this is a deploy-time env addition" — was inferred from `docker-compose.yml`'s `${FOLLOW_IMPORT_ENABLED:-0}` and was false: prod's root `.env` already carried `=1`, and the running gateway `printenv`s `1`. **The compose default tells you what happens when the variable is ABSENT, which is not a statement about any particular box.** One real trap survives the correction and is worth keeping: the flag must live in the ROOT `.env` (compose interpolation), never `gateway/.env` — `environment:` overrides `env_file:`, so a value put in the service env-file would be silently replaced by the `:-0` default.

- ~~**PENDING PROD DEPLOY, opened 2026-08-05 (§0p fix batch)**~~ — **DISCHARGED
  2026-08-07: prod is current with `3e1f7dc2`, both halves measured.** Migrations
  172–175 applied and the code verified in the RUNNING containers by three
  markers this batch introduces (`payout_halt_stale`, `payouts_halted_accounts`,
  `payoutHaltEscalationHours`), all present.

  **The first attempt was schema-only, and the markers are what caught it** —
  the DB read `t | t` while all three code markers read **0**. Nothing broke,
  because the batch was deliberately safe in both orderings, so the new table
  and dial simply sat unread with no error anywhere. Two lessons worth keeping:
  **a migration marker is not a deploy check** (pair the schema half with a code
  half, since the code half is the one that can be silently absent), and **grep
  the running container rather than the image** — that proves the code is
  executing, which an image build date cannot, and it catches the
  rebuilt-but-never-recreated case a date comparison sees straight past.

  Also cleared the same day: the §1.16 negative-tab check ran against prod and
  returned **0 rows**, so no pre-2026-07-16 history left a real reader in credit
  for W1's detection to surface as a phantom incident. That was the perimeter's
  last outstanding residue.

  Historical detail of what the batch contained, kept for the record: master had
  gained
  **migration 172** (`notifications.offer_id` + the rebuilt
  `idx_notifications_dedup`), **migration 173** (that index made partial —
  §0p.1), **migration 174** (`drive_id` into that index + `idx_notifications_drive`),
  **gateway** code (`my-account.ts`, `notifications.ts`,
  `subscription-offers.ts`, **`drives.ts`**), **payment-service** code
  (`settlement.ts`) and **web** code (the notification type, label and
  destination). **No `shared/` change**, so this is NOT the everything-is-stale
  shape: `gateway`, `payment` and `web` only.

  **AMENDED 2026-08-07 — that last sentence no longer holds.** The batch has
  since gained the 08-06 money fixes (§1.14/§1.15) and W4 (§1.18), and **both
  touch `shared/`**: `publicationPayoutThresholdPence` and
  `payoutHaltEscalationHours` are new `PlatformConfig` fields read by
  `loadConfig`, plus `config-defaults.sql`. So this IS now the
  everything-is-stale shape — **every service that links `shared/` needs
  rebuilding**, not just the three. Also pending: **migration 175**
  (`payouts_halted_accounts`).

  Ordering for 175 is safe in both directions: old code never reads the table,
  and new code treats an empty table as "nobody halted", so migrate-then-build
  and build-then-migrate both behave. Functional marker for 175, in the spirit
  of 173/174's (a marker should be something the change INTRODUCES):

  ```sql
  SELECT to_regclass('public.payouts_halted_accounts') IS NOT NULL AS table_175,
         EXISTS (SELECT 1 FROM platform_config
                  WHERE key = 'payout_halt_escalation_hours') AS dial_w4;
  ```

  Both TRUE ⇒ migration 175 applied *and* `config-defaults.sql` re-applied (the
  dial is seeded by the defaults file on every migrate run, never by a
  migration — the tuning-dials invariant).

  **Ordering is safe in both directions here, unlike 170** (see §0p.2): 172's
  new column is nullable with no constraint on existing writes, so old code runs
  fine against a migrated DB and new code carries the column the migration
  supplies; **173 is DDL only and needs no code at all** — every notification
  insert is a bare `ON CONFLICT DO NOTHING`, which a partial unique index serves
  unchanged, so it takes effect the moment it is applied and no service rebuild
  is owed for it. Migrate first by hand anyway (`npx tsx
  shared/src/db/migrate.ts` — migrations never auto-run), then rebuild the three
  services with `|| break`; never a bare `docker compose build` on the 4GB box.

  **Markers** (each a string the change INTRODUCES): `offer_id` in gateway's
  `dist/routes/subscription-offers.js`; `status = 'completed'` adjacent to
  `stripe_payment_intent_id IS NULL` in payment's `dist/services/settlement.js`;
  `drive_id` in gateway's `dist/routes/drives.js` (the notification inserts —
  absent before 174); `174_notifications_dedup_drive.sql` as the last row of
  `_migrations`. For **web** use a CHANGED chunk hash, never an identifier grep.
  173 and 174 additionally have a *functional* marker, which is the better kind
  because it reads the effect rather than the bookkeeping — one query covers
  both:

  ```sql
  SELECT indexdef LIKE '%WHERE (read = false)%' AS partial_in_force_173,
         indexdef LIKE '%drive_id%'             AS drive_in_index_174
    FROM pg_indexes WHERE indexname = 'idx_notifications_dedup';
  ```

  **One data check worth running once after the deploy**, since the settlement
  fix corrects a state that could already exist on prod: any settlement left
  `pending` with a charge id against it was collected but mislabelled, and the
  resume sweep will now heal it rather than re-drive it — but a row already
  stamped `failed` by the old path needs an eye.

  ```sql
  SELECT id, status, failure_reason, created_at
    FROM tab_settlements
   WHERE stripe_charge_id IS NOT NULL
     AND status <> 'completed'
   ORDER BY created_at DESC;
  ```

  Expected empty on a platform with this little settlement volume; a `failed`
  row with a charge id is a collected settlement wearing the wrong label and
  should be corrected to `completed` by hand (the money and the tab are already
  right — only the row's status and any `card_action_required_at` it raised are
  wrong).

- **PENDING PROD DEPLOY, opened 2026-08-04 (small-money batch)** — *a claim to check, not prod's state; measure the box first, per the lesson two entries down.* Master has gained the small-money batch: **migrations 170 and 171**, a **`shared/` change** (`shared/src/lib/subscription-period.ts`, new), **gateway** code (the four subscription write paths, the renewal worker, `subscription-offers.ts`) and **web** code (the redeem page + the offer API types). A `shared/` change makes **every** backend image stale regardless of whether its own directory moved, so the deploy shape is the standard full one: **run `npx tsx shared/src/db/migrate.ts` by hand first** (migrations never auto-run), then rebuild each service in a loop with `|| break` — **never a bare `docker compose build`** on the 4GB box.

  **Markers** (each a string the change INTRODUCES, so 0 → n is decisive rather than suggestive): `period_anchor_day` in gateway's `dist/workers/subscription-expiry.js`; `advanceSubscriptionPeriod` in `shared/dist/lib/subscription-period.js`; `login_required` in gateway's `dist/routes/subscription-offers.js`; and `171_grant_offer_codes.sql` as the last row of `_migrations`. Nothing here is flag-gated, so the behaviour change is live the moment the images are.

- ~~**PENDING PROD DEPLOY, opened 2026-08-04**~~ — **DEPLOYED 2026-08-04, and this entry was STALE WHEN IT WAS READ. Fourth time.** Measured on the box before anything was run: `git log --oneline HEAD..origin/master` **empty** (checkout on `0e0a6f57`, both of the day's later commits included), `_migrations` ending at **169** applied **13:37:26 UTC**, and all six app images rebuilt within the preceding 17 minutes (feed-ingest/key-custody/keyservice/payment 17m, gateway 16m, web 14m — a serial loop, in order). So the migration and the full six-service rebuild the entry called for had **already happened**, and the step-0 evidence check is the only reason no one wasted twenty-five minutes rebuilding them again. Marker confirmation (`free_allowance_granted_pence` in gateway's `my-account.js`, `no_stripe_customer` in payment's `settlement.js`, `freeAllowanceTotalPence` in web's `.next`) is the outstanding decisive check; image age proves only that a build ran.

  **The lesson is not "amend the entry sooner" — it has been that three times and did not take.** The failure mode is reading this section at all before measuring. Prod's state has four cheap sources (`HEAD..origin/master`, `_migrations`, `docker compose images`, a marker grep) and this file is not one of them. **Read the queue for what work exists; read the box for whether it is deployed.**

  **Side-finding from the same `docker compose images` output, unrelated to this deploy — FOUND AND CLOSED the same day.** Prod was running `platform-pub-traffology-ingest-1` and `platform-pub-traffology-worker-1` on **7-week-old images**, while `docker-compose.yml` had had both services commented out since 2026-06-16 with a parking comment asserting they "are stopped". The parking was applied to the file and never to the box, so two containers serving a subsystem nobody is viewing held memory on the 4GB host for seven weeks — plausibly a contributor to the concurrent-build `ETIMEDOUT` that made the serial build loop necessary in the first place. Cleared within the hour (`docker stop` reported `No such container`, which covers stopped ones too, so they were gone in any state; ten live containers healthy, site 200). **Which command removed them is not established** — most likely a `docker compose up -d --remove-orphans`, since they are invisible to an ordinary `up -d` — and that ambiguity is itself the point: a container outside the compose config is outside every routine that maintains the stack, including the one that was supposed to have stopped it. **A parked service is only parked where it is actually stopped, and the compose comment asserting otherwise was true nowhere but in the file.** Worth a `docker ps -a` against the compose config after any future parking.

  *(Original entry, kept for the record:)* **PENDING PROD DEPLOY, opened 2026-08-04 (read as a claim to check, not as prod's state — this entry's own history demands it).** Prod was last verified current with `3491f345` (2026-08-01, entry below). Since then master has gained the §0o fix batches: `6ae0fb3c` + `f7fff2b3` + `6a66e23b` (08-03: the §0o.2 sync double-count, the §0o.3 pending-sibling flip, docs) and the §0o.7 batch (08-04: fee-inclusive reversal draws, the no-customer card flag, the webhook metadata fallback). **AMENDED 2026-08-04 (§0o.9): the blast surface grew from one service to everything, and this is precisely the staleness this entry's own header warns about.** It previously read "`payment-service` only; no migrations, no `shared/` change, no web/gateway code" — true when written, false within the day. The §0o.9 ship adds **migration 169** (`accounts.free_allowance_granted_pence`), a **`shared/` change** (`shared/src/auth/accounts.ts` — signup stamps the allowance from the dial), **gateway** code (`my-account.ts` + `account-provision.ts`) and **web** code (`LedgerPanel` + the `TabOverview` type). A `shared/` change makes **every** backend image stale — `gateway`, `payment`, `keyservice`, `key-custody`, `feed-ingest` — even where their own directories have not moved, and web needs a rebuild of its own. So the deploy is now: **run the migration by hand first** (`npx tsx shared/src/db/migrate.ts` — migrations never auto-run), then rebuild every service, looping with `|| break` and **never a bare `docker compose build`** on the 4GB box.

  **Ordering matters here and is safe in one direction only.** The new column is `NOT NULL DEFAULT 500`, so the migration is backward-compatible: old code keeps working against a migrated DB (it simply never writes the column, and the default supplies the truth for the accounts it creates). New code against an un-migrated DB is what breaks — `/my/tab`, the account statement and both signup paths all name the column. **Migrate first, then build.**

  **§0o.9c adds nothing to the deploy surface.** It is a new test file plus a comment correction in `shared/src/db/client.ts` — no behaviour, no schema, and `shared/` was already moved by §0o.9, so the "every backend image is stale" conclusion above is unchanged rather than newly re-earned.

  **No urgency on the payments half**: §0o.2/3/7a are inert while `STRIPE_ALLOCATED_FUNDS=0`; §0o.7b/7c improve live crash-recovery paths but fix silent-UX/robustness cases, not money loss. They MUST be live before the flag ever flips (same class as the brand pre-flight). §0o.9 is likewise not urgent — today the dial equals the old literal at 500, so the ship is behaviour-identical until someone retunes; its value is that retuning is now possible at all.

  Verify from evidence as always: `git log -1` on prod vs `docker image inspect -f '{{.Created}}'` per image, `SELECT filename FROM _migrations ORDER BY filename DESC LIMIT 3` (expect **169**), and two marker greps — `grep -c no_stripe_customer /app/payment-service/dist/services/settlement.js` and `grep -c free_allowance_granted_pence /app/gateway/dist/routes/my-account.js` — strings that cannot exist before 08-04.

- ~~**PENDING PROD DEPLOY, opened 2026-07-31 17:00**~~ — **DEPLOYED AND VERIFIED 2026-08-01. Prod is fully current with `3491f345`.** Measured state at deploy time: `_migrations` = **168** (schema already current), `gateway` and `web` images built 07-31 (both newer than their last relevant commits), `payment` stale by one commit. So the deploy was **one service**: rebuild `payment` for the ineligible-brand pre-flight. Verified 0 → 3 on `grep -c ALLOCATION_ELIGIBLE_CARD_BRANDS /app/payment-service/dist/lib/stripe-client.js`, container healthy — a marker that could not exist before 08-01, so the transition is decisive rather than suggestive.

  **THIS ENTRY WAS WRONG WHEN YOU READ IT, AND THAT IS THE POINT.** It claimed migration 168 and the publication-cycle fix were outstanding. **They had been live since 2026-07-31 16:53 UTC** — `0586c389` landed at 16:16 UTC, thirty-seven minutes before that image build. The entry was opened at 17:00 the same day and never amended when the deploy happened. This is the second time a pending-deploy note in this file has asserted a backlog that was already deployed. **Establish prod's state from evidence — `_migrations` count, `docker image inspect -f '{{.Created}}'`, and a marker grep — never from what this section last claimed.** The three checks take a minute and both times would have prevented a wasted deploy.

  **A second error, from the same session and the same shape:** the outstanding-services list was derived by comparing commits against a *remembered* image date (07-29) rather than a measured one (07-31), which produced "web is 7 commits behind" when all seven predated its build. Compare against `docker image inspect`, and mind that image times are UTC while `git log` prints BST.

  **What was genuinely gained:** the ineligible-brand pre-flight is now live, which is a **precondition of ever flipping `STRIPE_ALLOCATED_FUNDS`** (without it, every reader on a Mastercard wedges permanently and silently on their first settlement — attack-order 0b). It changes nothing today: `allocatedFundsParam` returns `{}` on the flag before it ever looks at the brand.

- **PROD, 2026-07-31 12:40 — and read the two lists apart, because the difference is the whole point of this entry.**

  **Verified by direct observation:** prod was on `8129b3de` immediately before the settlement deploy; migrations through **167** applied (07-31 07:41), so 166/167 and the whole 07-29→07-31 backlog were already live from that morning's deploy — the Connect-onboarding 404 fix, the zero-net payout wedge, segregation part 3, publication-split re-pay, the card-decline prompt, the ledger net-balance fix, the audit-F4 deletion, the landing invented-handle fix. After the settlement deploy: payment restarted clean with all four workers scheduled, `STRIPE_ALLOCATED_FUNDS` **0**, and `accounts.card_action_required_at` count **0** — so no reader was left falsely flagged by the no-payment-method bug.

  ~~**Assumed, NOT verified — check this first next session.** That the running `payment` image actually contains the two settlement P0 fixes (`fb974656`).~~ **DISCHARGED — answered by the 2026-08-01 entry above**: `payment` was rebuilt from `3491f345` (⊇ `fb974656`) and verified by a marker grep that could not exist before 08-01, so the fixes are decisively in the running image. The check's rationale — fresh uptime proves a bounce, not new code — stands and is preserved in the "a restart is not a deploy" paragraph below.

  **This entry has now been wrong three times, and the third is the instructive one.** It was rewritten on the morning of 07-31 to say the deploy was overdue and to list a six-service rebuild — while prod had in fact been deployed at ~07:41 that same morning, before the entry was written. Deriving the deploy from **evidence** collapsed it from six services and a migration to `docker compose build payment` alone: prod's `git log -1` was one commit behind, `shared/` had not moved, and no migration was outstanding. **So: never read this paragraph as prod's state. Read it as a claim to check.** The two commands that settle it in ten seconds:

  ```bash
  git log -1 --format='%h %ad %s'
  docker exec -i platform-pub-postgres-1 psql -U platformpub -d platformpub -c \
    "SELECT filename, applied_at FROM _migrations ORDER BY filename DESC LIMIT 3;"
  ```

  Then rebuild only what actually moved between that commit and `origin/master` — remembering that a `shared/` change makes **every** backend image stale (`gateway`, `payment`, `keyservice`, `key-custody`, `feed-ingest`) even when their own directories have not moved, and that a `NEXT_PUBLIC_*` change needs a `web` **rebuild**, not a restart, because it is a build arg. **Never a bare `docker compose build`** on the 4GB box — six concurrent `npm ci` runs time out; loop with `|| break`. Migrations never auto-run.

  **A restart is not a deploy.** `docker compose logs` showing a fresh uptime proves the container bounced, not that it holds new code — the same *component ≠ feature* trap as §11's 2026-07-17 batch. Verify the artefact: `docker inspect -f '{{.Created}}' <image>` against the commit time, or grep the running container for a string the new code introduced.

  **Still owed on prod, unrelated to the above:** the `trustProxy: 1` real-client-IP verification (§2 item 6), the one-time `ANALYZE;` + autovacuum check on `feed_items` (§8), and the `scripts/reconcile-ledger.sql` prod run (the meaningful money-parity check — dev holds no live money, so its clean run is trivially satisfied). ~~Also unconfirmed: whether the `copy(landing)` commit (`8129b3de`, 10:33 BST) made it into the web image built around the same time.~~ **DISCHARGED — the 2026-08-01 entry measured the web image as built 07-31, newer than its last relevant commit, i.e. `8129b3de` is in it.**

- ~~**Re-check the 2026-07-16 deep-audit batch against the "component ≠ feature" standard**~~ — **✅ DONE 2026-07-17 (the high-risk candidates; see the two follow-on items directly below for what it left).** Result: **the hypothesis was confirmed, and for three items it was worse than "unproven" — the recorded evidence was structurally incapable of supporting the claim.** Driven and **passed**: **M5** (anon 401 / non-member 403 / member 200), **M9** (both arms; + a new residual, §0e), **M7** (with a baseline — same non-author 200 published → 404 withdrawn), **M6** (feed_items 1 → 0), **M10** (8 concurrent verifies → exactly 1 session) and **M24** (two concurrent `migrate.ts` → one applies, one waits). **M10 and M24 both carry a negative control**, which is what makes them proof: the pre-M10 SELECT-then-UPDATE pattern **mints 8 sessions from one token** where the shipped atomic UPDATE mints 1; and with the lock stripped, a concurrent `migrate.ts` runner **crashes** (`duplicate key … _migrations_filename_key`) where the locked pair serialises cleanly. **M3/M4/M25 have ZERO coverage** (mutation-proven — revert the fix, suite stays green) → split out as its own item below. **Method note for the next reader:** the first M10 control used too narrow a window (5ms) and returned a **false negative** (INCONCLUSIVE — the buggy pattern also yielded 1 winner); widening to 300ms gave 8/8 vs 1/8. **An un-controlled pass can be the harness passing rather than the feature working — a verification needs verifying.** Not covered by this pass: **M13/M15** (see below). → FIX-PROGRAMME 2026-07-17.

- ~~**M3 / M4 / M25 ship with ZERO test coverage — write the tests**~~ — **✅ DONE 2026-07-17.** All three covered, **every test mutation-verified** (revert the fix → tests fail, where before all three could be deleted with the suite green). 248 → 272 tests. **M3**: 3 tests in `chargeback-reversal.test.ts` (full reversal, carve-reduced variant, and the publication read that must *not* author-reverse) → dropping the flag fails 2. **M4(a)**: 2 in `payout-math.test.ts`, pool starved so `Math.min` binds; asserts Σ splits ≤ pool, **not** `remainingPool` (the F10 floor hides an unclamped overdraw) → removing the clamp fails 1. **M4(b)**: new DB-backed `publication-share-order-integration.test.ts` on the `dedup-integration.test.ts` idiom (real PG, always-rollback — *proven* by before/after row counts — `skipIf(!DB_URL)` so CI stays green); the two order-dependent reads extracted to exported constants (`PUBLICATION_ARTICLE_SHARES_SQL`/`PUBLICATION_STANDING_MEMBERS_SQL`) so the test runs the cycle's own SQL, not a copy; real SQL → real `computePublicationSplits`, each ordering test paired with a deterministic control showing the unordered order pays a **different person**. **M25**: new `shared/tests/safe-fetch-redirect.test.ts`, 13 tests mocking only transport+DNS (never `safeFetch` itself — the existing `activitypub-follow-reader.test.ts:206` mocks the defence away) → neutering the block fails 8; the 5 survivors are the same-origin controls, which correctly assert the absence of stripping. **Method note that cost a false negative:** the first M4(b) draft used a 2-row fixture and **passed under mutation** — with the `ORDER BY` stripped the Hash Join's incidental output order happened to match. An unordered query's order is a *plan artefact*, not random, so a small fixture is a coin flip; widened to 8 scrambled rows to make it detect. **When a test passes the moment you write it, mutate before believing it.** → FIX-PROGRAMME 2026-07-17.

- **(superseded — kept for the measurement)** M3 / M4 / M25 ship with ZERO test coverage (found 2026-07-17 by the 0b audit; **mutation-proven**, i.e. each fix was reverted and the suite stayed fully green, so this was measured, not inferred). These three were recorded as verified by "payment-service suite green (164 passed)" / "All 44 http-client tests still pass"; **no test touches any of them.** (a) **M3** `claimedByPendingPayout` — *money*: the identifier appears only in src + docs, never in a test file; no test builds the triple state (`platform_settled` + `writer_payout_id` set + non-publication). Deleting `|| r.claimedByPendingPayout` → 164/164 pass. The **tribute analogue of the identical concept IS tested** (`chargeback-reversal.test.ts:92`), so this is a one-object-literal addition to an existing file. (b) **M4(a)** pool clamp — the sole combined flat+bps test never overdraws (pool 820 vs payout 80, so `Math.min` never binds); reverting the clamp → 164/164 pass. Also a pure-function test in `payout-math.test.ts`. (c) **M4(b)** short-pool `ORDER BY` — **structurally unreachable**: its enclosing `runPublicationPayoutCycle` is called by zero tests and `computePublicationSplits` takes an already-ordered array, so *no unit test can cover it*; needs a DB-backed test of the reserve path (doesn't exist — the bigger of the three). (d) **M25** credential stripping — **`safeFetch` is not imported** by `shared/tests/http-client.test.ts`; all 44 tests are IP/IPv6/WebSocket classification and predate the fix. Deleting the whole strip-and-downgrade block → 84/84 pass. Worse: `gateway/tests/activitypub-follow-reader.test.ts:206` asserts a user's Mastodon Bearer token is passed **into** a mocked `safeFetch` — the one test touching the exfiltration risk mocks the defence away. **These are the highest-value tests in the repo right now**: two of the three are money/credential paths whose fix is *currently* load-bearing only in source. → FIX-PROGRAMME 2026-07-17.

- ~~**Finish the 0b sweep: M13 + M15**~~ — **✅ DONE 2026-07-17, both driven before/after.** **M13 was STILL LIVE** — the 2026-07-16 fix made the SQL emit a fractional epoch but the *decoders* still used `parseInt`, truncating it straight back to whole seconds, so the round trip stayed lossy and the fix was inert. Driven on the running stack (`GET /tags/:name/posts`, 5 rows in one second, limit 2): **before = page 1 got 2, page 2 EMPTY, 3 rows unreachable; after = 2/2/1, all 5.** Also caught two cursors the original fix never touched (`tags.ts`/`sources.ts` still encoded whole-second `published_at_epoch`). Fixed via a shared `Number`-based `parseCursorEpoch`; guarded by `gateway/tests/feed-cursor.test.ts` (15 tests, mutation → 7 fail). **M15 was correct but unproven** — all three claims now reproduced against a live DB: the `23503` **permanent wedge** (cited item fails the whole batch forever), both reference guards, and the `deleted_at` privacy inversion; guarded by `feed-ingest/.../external-items-prune-integration.test.ts` (4 tests, SQL extracted to `EXTERNAL_ITEMS_PRUNE_SQL` so the test can't drift, mutation → 2 fail). M13 being a live content-loss bug a day after "fixed" is the strongest vindication of the 0b thesis. → FIX-PROGRAMME 2026-07-17.

- **(superseded — kept for the record)** Finish the 0b sweep: M13 + M15 (the two remaining behavioural-claim-on-typecheck items) (2026-07-17). Not reached by the first 0b pass; both now done above.

- **(superseded — kept for the reasoning)** Re-check the 2026-07-16 deep-audit batch against the "component ≠ feature" standard (added 2026-07-17, prompted by the C1 miss). **The shape:** C1 was signed off "confirmed fixed empirically" on evidence that only proved *transport* reached the relay; publishing stayed 100% dead for another day because nobody drove a publish to a **stored event**. The evidence proved the component changed, and was read as proving the feature worked. **Why this is systemic, not a one-off:** the deep-audit remediation shipped 1 CRITICAL + 14 HIGHs + 21 MEDIUMs in a single rapid batch (23 FIX-PROGRAMME entries on 2026-07-16), and **14 of those entries rest on "typecheck clean / `next build` clean / suite green" alone — compile-time evidence for behavioural claims. Only ~6 mention a rebuilt image, a restarted stack, or any empirical check.** (Not a criticism of the batch's velocity — it's simply that the proof tier never caught up, exactly as C1 shows it can fail to.) **Method:** for each, ask "what did the recorded evidence actually exercise?" and drive the *feature* end-to-end where it diverges — the 2026-07-17 smoke session shows this is cheap (a 6-way concurrent race proved the draft advisory lock; two events differing only in `created_at` proved the strfry window). **Highest-risk candidates first**, by blast radius: **M3/M4** (publication-split clamp + chargeback reversal of pending-payout reads — money, verified by suite-green only; does the suite actually cover the reversal?); **M24** (serialising `migrate.ts` — a *concurrency* fix verified by typecheck; a race fix deserves a race, cf. the draft-lock check); **M5/M6/M7/M9/M10** (auth/integrity: members leak, unpublish, escalation, magic-link race — security-shaped, so a compile-clean-but-wrong fix is a live hole); **M15/M13** (external-items prune "unwedge" + sub-second keyset cursors — both behavioural claims about queries). Already discharged by the 2026-07-17 session: H1 (gate re-proved 401/401/pass), H2, C1, and H10 (loopback bindings observed on the running containers). **Don't re-audit the code — audit the evidence**, then drive only what the evidence doesn't reach. → FIX-PROGRAMME 2026-07-17; `DEEP-AUDIT-2026-07-16.md` banner.

- ~~**PROD IS PUBLISHING NOTHING — deploy the strfry reject-window fix**~~ — **✅ DEPLOYED + PROD-VERIFIED 2026-07-17.** `git reset --hard origin/master` + `docker compose up -d --force-recreate strfry` (the `--force-recreate` is load-bearing: `strfry.conf` is a **single-file bind mount**, `./relay/strfry.conf:/etc/strfry.conf:ro`, so git replacing the file mints a new inode the running container isn't bound to — the same trap as the nginx.conf/Blossom cutover; a plain `up -d`/`restart` can silently keep the old config). Confirmed live in-container (`grep rejectEventsOlder` → `315360000`), then **proved end-to-end with a real UI publish**: new `relay_outbox` row `sent`, `attempts=1`, no error, and strfry's event count rose 89 → 90. **Blast radius was ZERO** — the pre-fix diagnosis found 22 rows, all already `sent` (yesterday's imported batch), and **nothing enqueued since the 2026-07-16 deploy at all**, so no content was lost: prod was idle (pre-launch; discovery publishing dark by default). The bug was a loaded gun, not a fired one. **No redrive was needed** (nothing `failed`/`abandoned`). Cosmetic residue: those 22 imported rows are `sent` but retain a stale `last_error` ("All relays rejected or timed out") and `attempts=5` from before the import recovery marked them `sent` directly — a `sent` row carrying an error string is import scar tissue, not a live failure. → FIX-PROGRAMME 2026-07-17.

- **§0 commit-audit fixes runtime verification + prod deploy residue** (shipped 2026-07-09; **(b) VERIFIED 2026-07-17** — scheduled draft excluded from the guess target, fresh save → 201 new row, scheduled draft untouched; the rest still need `docker compose build web gateway && up -d` + on prod `docker compose up -d --force-recreate nginx`, the bind-mount inode gotcha): (a) `PUT https://all.haus/media/upload` → 404/405, existing `/media/<sha>.webp` still 200 `image/webp`, `DELETE /media/<sha>.webp` → 403 — **prod-only (no nginx in the dev stack)**; ~~(b) schedule article A, start new article B~~ **DONE**; (c) publish a drive-linked paywalled draft → drive matches only after v2 lands, one subscriber email, sent after the vault sealed — **blocked: `PLEDGES_ENABLED=0` (parked), needs the flag flipped to exercise**; (d) publish a drive-linked free article normally → same blocker. `nginx -t` passed in a throwaway container 2026-07-09; gateway tsc/tests + web `next build` green. → FIX-PROGRAMME 2026-07-09 entry.
- **Publication-subscription distribution runtime verification** (shipped 2026-07-10, migration 152, NOT runtime-verified — needs `docker compose build gateway payment-service web && up -d`): (a) subscribe to a publication with a card on file → tab debited, `subscription_charge` + `subscription_earning` (publication-targeted) rows; (b) settle the tab → the earning gains `settled_at`; (c) run the publication payout cycle → the earning is claimed under a `publication_payouts` row whose `sub_net_pence` matches, standing members' splits grow by their share of the sub net; (d) publication earnings tab shows the "From subscriptions" card and the payout row's subscription line; (e) a subscription-only publication (zero paid reads) above threshold pays out. → FIX-PROGRAMME 2026-07-10 entry.
- ~~**F4 premise check against live Stripe**~~ — **DONE 2026-07-30** off `events.list`; branches deleted. → §1.1.
- **Live Stripe browser test: S2 card attach incl. 3DS** (`4000002500003155`). → §1.4. Needs the sandbox **publishable** key and a `docker compose build web` — `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is a build arg, so a restart will not pick it up, and dev's web is a production build with no hot reload. **Now more interesting than when it was written**: the 2026-07-31 sandbox run proved the settlement charge works when the customer has a default payment method, but every card in that run was attached by API (`pm_card_visa`). The *browser* SetupIntent path is what real readers use and is still undriven end-to-end, and it is the path that sets `invoice_settings.default_payment_method` — the field the settlement fix now depends on.

- ~~**P0 — `runPublicationPayoutCycle` cannot claim a read, and never could.**~~ — **FIXED 2026-07-31, migration 168** (option (a): `read_events.publication_payout_id`, FK → `publication_payouts`, `ON DELETE SET NULL` mirroring the writer twin, partial index). No backfill was possible or needed — the FK made it impossible for any row to carry a publication payout id, so every existing row correctly starts NULL. **Six sites moved**: the eligibility CTE, the claim, the article-earnings base, finalise, the re-pay sweep's settlement recovery (whose *subscription* arm already keyed on the right column), and the chargeback reversal's publication arm in `settlement.ts` — whose comment had reasoned the column was "overloaded across payout kinds"; it never was, and the value it read was always null. **Two precision fixes that only bite once the claim works:** the KYC-reconcile candidate query's "writer read earnings" arm now excludes publication reads (else a *paid* publication read matches forever), and the admin dashboard's two held-reads figures now require BOTH columns null. **The test that would have caught it** is `payment-service/tests/publication-claim-integration.test.ts`, which drives the **exported** statements against a real DB with real FKs; its paired control runs the pre-fix statement and asserts it still raises 23503, without which the file would pass against a schema where the FK had been quietly dropped and would pin nothing (mutation-verified: revert the constant, 4 of 5 go red). **Verified end to end: §5 step 11 is 18/18**, including a true §3.3e over-transfer. → FIX-PROGRAMME 2026-07-31.

- **(Superseded, kept because the diagnosis is the durable part.) P0 — `runPublicationPayoutCycle` cannot claim a read, and never could (found 2026-07-31 by §5 step 11).** `reservePublicationPayout` claims the pool's reads with `UPDATE read_events SET writer_payout_id = $1` where `$1` is a **`publication_payouts`** id, while that column carries `fk_read_events_writer_payout → writer_payouts(id)`. Every such UPDATE raises `23503` and rolls back the reserve. The FK is in `schema.sql` **genesis**; no migration ever touched it. It fails **silently** — the cycle catches per-publication errors and logs, so the only symptom is that publications are never paid. **Scope:** any pool with ≥1 settled unclaimed read dies; a **subscription-only** pool survives (that claim uses `subscription_events.publication_payout_id`, a column that exists — and that asymmetry is the tell: the subs side got a dedicated column, the reads side was left reusing the writer one). **Why nothing caught it:** no test calls `runPublicationPayoutCycle` (the one DB-backed publication test says so in its own comment and exercises the ordering SQL + the pure `computePublicationSplits` instead), and prod is pre-launch with zero publication payouts. **The fix is a fork, and it is not local:** (a) add `read_events.publication_payout_id` mirroring `subscription_events` and move the pool's claim/release/recompute onto it — correct, consistent, and touches chargeback attribution and the F5 reversal selection; or (b) drop the FK and let the column be polymorphic — one migration, but it abandons referential integrity and keeps an actively misleading column name. **(a) is the recommendation.** Whichever is chosen, the DB-backed test that would have caught this — drive `runPublicationPayoutCycle` end to end against a real database — is the deliverable that matters, and §5 step 11 is already written and waiting to prove it. → FIX-PROGRAMME 2026-07-31.

- **§5 sequence steps 2–12** (`docs/adr/FUNDS-SEGREGATION-INTEGRATION.md` §5, harness `scripts/segregation-sequence.ts`). Steps 1 and 13 were green 2026-07-31; **steps 2, 3 and 10 are now GREEN too, driven against the segregation sandbox on 2026-07-31** — 41/41, 15-plus-one-honest-UNKNOWN, and 15/15 respectively, all three in a single `--step 2,3,10` invocation. Read the FIX-PROGRAMME entry for what each proved; the headline is that **no defect in the code under test was found by any of them**, while six harness bugs and one fortnight-old silent payout halt were. **Step 11 is GREEN too (18/18)** once migration 168 landed — it is what found the P0 above, and then proved the fix: per-split children drawing from the preference set, `Σ(child fee) ≤ platform_fee_pence` with the shortfall bounded at a penny per split, the ledger at the SPLIT grain, and a deliberately failed split completing its parent rather than zombifying it (a real §3.3e over-transfer: an allocated child inflated 1104 → 6500, rejected `StripeInvalidRequestError`, its split failed, the sibling completed, the parent completed). **Step 12 remains blocked** on the Dial-A tribute rework.

    **ALL OF 4–9 ARE NOW GREEN (2026-08-01)** — 4 at 19/19, 5+6 at 25/25, 7+7b at 36/36, 8 at 15/15, 9 at 11/11 after the ineligible-brand fix it forced. Read the FIX-PROGRAMME entry for the flip blocker step 9 found (an ineligible brand 500s and wedges the reader's tab permanently), the measured brand set that overturned both prior lists in the repo, and the four harness defects the run exposed — three of which shared one shape, a check that could not fail. **The one thing still owed is the Stripe question at attack-order 0b.** The paragraph below is the as-written record from the day they were built, kept for the design reasoning.

    **Steps 4, 5, 6, 7, 7b and 9 were WRITTEN AND UNRUN (2026-07-31), along with 8.** They compile, they lint clean and they have never touched Stripe — the sandbox key was cycled after the 07-31 session, and a step that has not run is a hypothesis. All seven are driven by WEBHOOKS rather than by a cycle call, which is the shape difference from 2/3/10/11: `stripe listen` is not optional for any of them, and every assertion polls for the container handler's effect rather than reading once. New shared machinery in the harness: `pollUntil` (one home for the waiting), `drawableRemaining` (production's own `lockFundingSources` under a rolled-back read-only transaction, so the drawability claims cannot drift from a copy of its SQL), `paymentLogsSince` (the `manual_review_required` marker is a log line with no table behind it, so it greps the container and reports UNKNOWN rather than a silent pass when docker is unreachable), and `runCycleForNewPayout` — the guard against the worst outcome available here, a step asserting against a payout an EARLIER step completed and reporting green having exercised nothing.

    **Writing them produced two findings without running anything, both recorded in ADR §5 beside the steps they belong to.** (a) **§5.6's `manual_review_required` claim does not match the code**: `webhook.ts` emits the marker from the PARTIAL arm of `charge.refunded` only, and step 6 is a full refund — so either the ADR means a partial refund post-transfer (step 5 already covers that) or a full refund of a paid-out charge deserves its own marker. The step states the claim as written and expects to fail it; settle the question before patching either side. (b) **§5.8's "drive a `subscription-convert` credit-back" is unbuildable as written**: that route is 503-gated *and* its charge leg is a phantom with no `subscription_earning`, so it mints nothing to pay out. Step 8 drives the live credit-funded branch instead (`logSubscriptionCharge` with a pre-paid credit, post-charge balance ≤ 0 ⇒ `settled_at` stamped at charge time, `tab_settlement_id` NULL) — production's own two functions in the convert route's own order, which is what the residual actually carries in production.

    **Three assertions are deliberately loose and say so in their own headers**, because guessing tightly would have been guessing: step 6's platform-balance figure (a fact about Stripe, and this harness measures those rather than deriving them — the direction is asserted and every component recorded, so the first green run can tighten it with evidence), step 6's marker claim above, and step 9's card token (two ineligible brands are tried, since a sandbox that declines JCB outright says nothing about allocation).

  **One harness trap worth carrying forward, because it produced a failure to fail.** Step 11's fabrication originally inflated `children2[0]` blindly. An **allocated** child is bounded by its funding charge, so inflating past that is a true over-transfer and is rejected; a **residual** child has no charge behind it and is bounded only by the platform balance, so the same inflation just makes a larger ordinary transfer, which SUCCEEDS. On the first run both children packed residual (arm 1 had drawn the pool's charges down) and Stripe happily paid £50 while the step reported that the child had not failed. It now prefers an allocated child, falls back to exceeding the platform balance only when none was packed, and records which mode it used — the two prove different things about §3.3e even though both prove §3.3c.

  **NEXT SESSION STARTS HERE: steps 4–9.** The per-step implementation shape — what each needs as a fixture, which webhook drives it, and the specific trap in each — is written up in `scripts/segregation-sequence.ts`'s header, beside the code that will implement them; read that rather than working from ADR §5's one-line claims. The shape of the work in one paragraph: **all six are refund / reversal / edge-funding steps, so unlike 2/3/10/11 they are driven mostly by WEBHOOKS** — `stripe listen` is mandatory for every one, and assertions must poll for the handler's effect rather than reading once. **4** (full refund pre-transfer) and **5** (partial — the harder twin, whose distinctive claim is that the reads stay *payable* and the next cycle packs around the shortfall rather than wedging, so it needs a second cycle run) and **6** (refund post-transfer) exercise the `charge.refunded` draw upsert, whose GREATEST-not-assignment is load-bearing because webhook delivery is unordered. **7/7b** are the reversal pair; 7b needs step 2's multi-child payout as its fixture and pins §3.5's re-keying against our code, the raw payload having already been confirmed by probe 7b. **8** (credit-funded earning) is the only step whose fixture is not a read — drive a subscription-convert credit-back — and it is **entirely residual**, so it is the step that will fail outright on a thin platform balance. **9** (ineligible brand) swaps `pm_card_visa` for JCB/Diners and must assert `allocation_synced_at IS NOT NULL AND allocated_pence = 0`, because "0" and "not yet stamped" otherwise look identical. **12 stays blocked** on the Dial-A tribute rework.

  **The harness's five paid-for traps are in its header too, and are worth reading BEFORE debugging any failure**, because every one of them presented as a broken payout in a working system: the accrual space is `readers × articles` and a repeat pair mints nothing; one `syncAllocations` is not enough; `bigint` comes back as a string; allocated and residual children have *opposite* transfer contracts (and inflating a residual over-transfers nothing, so a fabrication aimed at one reports a failure to fail); and a DB-backed assertion on a global aggregate is not isolated by a rolled-back transaction, since rollback isolates writes and not reads.

  **Sandbox prerequisites, all now satisfied and worth recording because two were not obvious.** `STRIPE_ALLOCATED_FUNDS=1` must be set **in the shell that runs the script**, not merely the container (the steps import `payoutService` and run the cycle in-process; each refuses up front rather than reporting a wall of zeroes). The **platform balance must be positive** — residual children draw on it, not on segregated funds, so an overdrawn sandbox fails them `balance_insufficient`; top up with a `pm_card_bypassPending` PaymentIntent. And a **second onboarded connected account** is needed for step 11's failed-split arm; the Stripe **dashboard cannot create one** under the controller-properties model, so `scripts/create-sandbox-connect-account.ts` exists (test-mode magic values — dob 1901-01-01, `address.line1: address_full_match`, id_number 000000000, GB sort 108800 / account 00012345 — are what make it verify instantly instead of sitting in `currently_due` forever).

  **(Superseded, kept for the shape of the claims.) Steps 2, 3, 10 and 11 WRITTEN and UNRUN (2026-07-31, earlier the same day)** — what each covers: **2** the multi-child writer payout (one child per drawn charge, `source_transaction`/`application_fee_amount` against Stripe's own copy of the transfer, fees landing in the platform balance, one ledger entry per child reffed at the parent, the restated parent amount, reconcile-ledger gaining no violation); **3** forced over-transfer (the child failed, its draw deleted, exactly its units released and the sibling's untouched, the parent completing on the restated smaller amount); **10** crash-resume (the crash state is a real one — `reserveWriterPayout` committed, nothing sent — then resume, then resume AGAIN as the double-delivery control, plus the `xfer-<childId>` idempotency key asked of Stripe directly); **11** the publication cycle (pool spanning ≥2 charges, per-split children drawn from the preference set, the §3.4 fee-dust bound, ledger at the SPLIT grain, and a deliberately failed split completing its parent rather than zombifying it). **Step 12 is blocked** on the Dial-A tribute rework — driving it today tests a model already scheduled for replacement, so skip it and say so rather than booking a pass against the old one.

  **Two prerequisites the harness cannot supply itself, both of which fail LOUDLY rather than silently.** (a) **`STRIPE_ALLOCATED_FUNDS=1` must be set in the shell that runs the script**, not merely in the container: steps 2/3/10/11 import `payoutService` and run the cycle in their own process, so with the flag off `completeWriterPayout` takes the aggregate path, mints no children, and every child-level assertion reports 0 against an expected 2 — a step that looks comprehensively broken when only the shell is wrong. Each step refuses up front instead. (b) **Step 11's second arm needs a SECOND onboarded sandbox connected account** (`--destination acct_A,acct_B`). With one, the publication has one split, and a single failed split correctly fails its parent outright — so "a failed split completes its parent" has no sibling to be true of. Arms 1 and 2 still run in full; arm 3 reports UNKNOWN naming the prerequisite. **Run step 11 last or alone** — it re-homes the connect ids onto the publication's members and `accounts.stripe_connect_id` is UNIQUE, so a writer-cycle step after it finds its writer un-onboarded. `main` orders them; a caller passing `--step` out of order owns that.

  **What steps 3, 10 and 11 fabricate, and what they deliberately do not.** All three need the state between Txn 1 and the Stripe call — a committed pack with nothing yet transferred — which no public entry point exposes, because in production only a crash produces it. They call production's own `reserveWriterPayout` / `reservePublicationPayout` rather than hand-writing a payout row, so the fabrication is confined to one column of one already-committed child (`net_pence` inflated past its funding charge, with the `allocated_draws` row left stale ON PURPOSE so **Stripe** is the thing that rejects rather than our own budget noticing first — §3.3e's claim is about Stripe's enforcement). **Step 10 does NOT reconstruct the mid-child-sequence crash, and that is the append-only ledger working rather than a gap:** a faithful reconstruction would have to DELETE the completed child's ledger entry (in a real crash it was never posted — it rides the same transaction as the flip), and the DB refuses that; reconstructing it *without* the delete would make the resumed flip post a second entry and the step would report a double-credit no crash can produce. So it pins the mechanism instead — `executePendingChildren` selects `status = 'pending'` only, and the row-stable key dedupes. A future step 10b with a fault-injection seam could reach the real interleaving; hand-editing cannot.

  **Re-wiring the sandbox is four things** (nothing persists between sessions — the harness rebuilds its own fixture, and the 07-31 wiring was torn down deliberately when the key was cycled): the sandbox secret key into `payment-service/.env` **and** `gateway/.env`; `STRIPE_ALLOCATED_FUNDS=1` in the root `.env`; a `docker-compose.override.yml` publishing payment on `127.0.0.1:3001` (it is compose-internal by default, so the CLI cannot reach it); and `stripe listen --api-key <key> --forward-to localhost:3001/webhooks/stripe`, whose printed `whsec_` goes into `payment-service/.env`. Then `docker compose up -d gateway payment`. **Confirm it is the segregation sandbox before anything else** — sandbox and classic-test keys share the `sk_test_` prefix, and the read-only preview-header `curl` in §5 settles it without creating anything.

  **Three traps already paid for, all of which produce a convincing false negative:** (a) `allocated_funds.balance` is not returned by a bare `charges.retrieve` — it must be **expanded**, as `syncAllocations` does; without the expand a fully-allocated charge reads as no allocation at all. (b) A reader who has already settled may carry a **negative** tab, and pre-paid credit makes every subsequent read land `platform_settled` on the spot, so no threshold is crossed and no settlement is created — the harness mints fresh readers per run for exactly this reason. (c) Allocation materialises ~1–3s **after** the PI returns, so every read-back polls. In all three cases the symptom is "the feature looks off"; suspect the read before the feature.
- **Paywall publish/unlock runtime verification + prod cleanup** (shipped 2026-07-07 `339b43e`; **(a) + (d) VERIFIED 2026-07-17**, (b)/(c) still open): ~~(a) publish a short paywalled article without touching the price field → blocked in the editor, not a 400~~ — **DONE** (the rule + both call sites: `validatePaywalledPublish` runs pre-sign on the publish AND schedule paths, early-returns into `publishError`; gateway 400s price0/gate0 in the shared envelope; key-service `PublishVaultSchema` agrees — all three validators in lockstep); (b) publish a priced paywalled article → only the payload-tagged v2 on the relay, unlock works — **now unblocked by the strfry fix; the relay leg itself is proven (a kind-30023 lands and is stored), but the paywalled v1/v2 + vault + unlock round-trip is not yet driven**; (c) unlock with a card-less account inside/over the £5 float → allowance copy honest, over-float shows add-card CTA, retry after a failure still shows an error message — **browser + needs a real vaulted article**; ~~(d) gate-pass against a vaultless paywalled row → 409, no charge~~ — **DONE** (409 `article_misconfigured`, honest copy, reads/ledger/unlocks all 0 after). **Prod cleanup after deploy**: sweep `SELECT a.id, a.title FROM articles a LEFT JOIN vault_keys vk ON vk.article_id = a.id WHERE a.access_mode='paywalled' AND vk.id IS NULL AND a.deleted_at IS NULL`, unpublish hits, check `read_events` for charges against them (reverse if any), have the test writer re-publish from their draft. → FIX-PROGRAMME 2026-07-07 paywall entry.
- **Duplicate-draft-after-publish runtime verification + prod cleanup** (shipped 2026-07-08, migration 149; **(a) + (b) VERIFIED 2026-07-17**, (c)/(d) open): ~~(a) autosave racing an explicit Save → exactly one row~~ — **DONE, and stronger than the manual check**: 6 *concurrent* first-saves → one 201 + five 200s, all six returning the same `draftId`, exactly 1 row (the `pg_advisory_xact_lock` holds under real concurrency); ~~(b) schedule it, let the scheduler publish → the draft is gone~~ — **DONE** (article published with event id, draft row deleted, kind-30023 stored in strfry); (c) publish a draft with the **button** → its draft row is deleted too — **still open: that delete is client-side/best-effort in `useArticleEditorInit.handlePublish`, so it needs the browser (the scheduler's server-side delete is what (b) proved)**; (d) reopen an older draft while a newer untagged one exists → autosave writes into the reopened row — **open (needs the editor threading `currentDraftId`; the server side is the `draftId` echo branch)**. **Prod cleanup after deploy** (pre-existing orphans the fix does not retroactively remove): run `scripts/cleanup-orphaned-drafts.sql` (in-review-submission predicate fixed 2026-07-09 — Tier 1 now requires `published_at IS NOT NULL`) — dry-run by default (reports untagged/unscheduled drafts shadowing a live same-title article, tiered by confidence), and deletes only the Tier 1 exact-content-match set when re-run with `-v apply=true`. Tier 2 (title-only) and drive-linked candidates are reported for manual review, never auto-deleted. → FIX-PROGRAMME 2026-07-08 entry.
- **External-author-history runtime verification** (shipped 2026-07-07, NOT runtime-verified — needs a user container restart): the ADR §7 manual verifies — unfollowed-author profile hydrates in ~3s; Nostr subscribe pulls week-deep history + write relays onto `relay_urls`; later subscribe of a hydrated author reactivates the shadow row and promotes its rows. → `EXTERNAL-AUTHOR-HISTORY-ADR.md`.
- Mobile workspace: full device verification (swipe/axis-lock/pip precedence, drag-to-rank, hide→renumber, density cross-device, resume-by-id). **Pip order VERIFIED 2026-07-17** (Playwright, 390×844: 6 pips, Feed 1 leftmost, ascending 1→6, aria-labels carrying feed names, matching the desktop numerals in the same session) — the `.reverse()` removal has not regressed. The gesture/drag items remain (they need real touch, not just a mobile viewport). → MOBILE-LAYOUT-ADR §X.
- ~~**Closed-beta ship — deploy + browser-verify**~~ — **✅ DEPLOYED + VERIFIED 2026-07-29 (owner-confirmed on the live site).** The beta is now actually closed on prod: migrations 162 + 163 are applied and gateway + web are rebuilt, so `CLOSED_BETA` is live and account creation is reserved to the admit route. The browser tier was swept on `all.haus` itself in **both modes at both form factors** — the readers-first landing, `/waitlist` join + success, `/auth` login-only, the swept CTAs — all accepted as-is, **no changes required**. Residue, small and unclaimed: the `trustProxy: 1` / real-client-IP check (§0m.6) is a log-tier check, not an eyeball one, so it is **not** discharged by this pass — confirm the waitlist rows carry real client IPs rather than the nginx address. The publication-invite exemption stays conservatively ruled (`/invite/[token]` logged-out → "Log in to accept"; §0m.1 tracks the dead-end UX). Original deploy shape, kept for the record — migration 162 (the `waitlist` table) run *before* the gateway deploy, then gateway + web rebuilt, then the `trustProxy: 1` / real-client-IP verify (§0m.6). The gate is a code constant (`CLOSED_BETA`), not an env flag, so there is nothing to flip — the deploy itself closes it. Browser-tier (no browser tooling in the build session): the readers-first landing + manifesto, the `/waitlist` join form + success + `?from=beta` edge line, `/auth` login-only with the `?mode=signup`→`/waitlist` redirect, the Google unknown-email→`/waitlist` hop, and every swept "Sign up"→"Join the waiting list" CTA (About, PaywallGate, and the `PublicNavRow` CTA that replaced Nav's when the topbar was deleted 2026-07-26). Note the publication-invite token-scoped signup exemption was **ruled conservatively** (recruit only existing members during the beta), so `/invite/[token]` logged-out → "Log in to accept"; reopening that as a real exemption is a deferred design decision (§0m.1 tracks the dead-end UX). → CLOSED-BETA-ADR §VIII–§X.
- ~~**The logged-out register — browser-verify in BOTH modes**~~ — **✅ DONE 2026-07-29 (owner-confirmed on the live site, both modes × both form factors).** The largest unverified surface the register sweep left is now discharged: `/`, `/auth`, `/waitlist`, `/about` and the landing demos were looked at on prod in light and dark, on desktop and on a phone, **and the answer was that they read correctly as built — no changes required**. That resolves the two §VIII judgement calls in favour of the shipped arithmetic (islanded bone at 4px does not read hot on the ink card, so `cardStandfirst` is NOT needed; the row's band sits correctly against the fitted floor at six points' separation), the dark-mode mouth on a fitted vessel, the phone chassis (no ⊔ survives at ≤767px, so no inline border is beating the media query), the vertical fit on desktop (no `flex: 1 1 0` survived the chain), the waitlist panel's table, and all three landing demos — including the claim this list called the smallest surface it had ever been made at, that `CanvasDemo`'s four seasonal walls still read as four distinct feeds at an 8.5px base. **Residue, deliberately unclaimed:** sub-item (d) is only *partly* covered — `/admin/waitlist` was rendered, but `/write` (whose sticky bar lost its `top-[53px]` offset) and `/traffology/*` were not, so the row's band clearance over those two tool surfaces is still untested visually. Kept below as the record of what the checks were. In the original priority order: **(a) the two §VIII judgement calls** — bone at 4px under a field (does `controlLine`'s islanded bone read hot on an ink-900 card? the fallback is `cardStandfirst`, but it lightens light mode too, so look at both), and **the row's band against the new floor** (in dark the row is `--ah-bone` 20 19 17 sitting under a fitted page's `interior` 26 26 24 — six points apart, which is either chrome distinguishing itself or wants to track the floor; in light all three are identical). **(b)** the mouth on a fitted vessel — content clipping at a wall-less line where interior and floor share a colour was judged "open, not broken" on the light-mode reasoning; confirm it in dark. **(c)** `/`, `/auth`, `/waitlist`, `/about`, a 404, and a shared article link while logged out, each in both modes — **and now at both form factors, because the chassis changed under all of them** (2026-07-27, ADR §IX 7quinquies): on a phone every one of these should show NO ⊔ at all (the walls are gone, not thinned), and on desktop the vessel should be exactly as tall as its content and centred vertically — `/auth` and a 404 are the short pages that show it, `/about` the long one that must still shrink to the viewport and scroll *inside* its walls with the bottom wall on screen. A stretched short vessel means a `flex: 1 1 0` survived somewhere in the chain; a walled phone means an inline border is beating the media query. **(d)** the row over a logged-in tool surface (`/admin/*`, `/traffology/*`, `/write`) — the band clearance is untested visually, and `/write`'s sticky bar lost its `top-[53px]` offset. **(e)** `.btn-accent` in dark anywhere it appears (the fix is sitewide, not just the row). **(f2) the waitlist panel** (2026-07-27) — `/admin/waitlist` compiles and its route is proven against a real database, but the page has never been rendered: check the table at a narrow width (it is the dashboard's only unbounded-width column, an email address), and the "Never" last-digest tile in dark. **(f) the three landing demos** (2026-07-27, ADR §IX 7ter then 7quater — live markup replacing the screengrabs, so what used to be a fixed image is now rendered and can be wrong; 7quater then replaced two of the three and widened this list): in DARK, whether `CanvasDemo`'s four seasonal walls still read as four distinct feeds at a base that now goes as low as 8.5px — the dark variants are a near-shared neutral by design and identity rides the walls, and this is by some way the smallest surface that claim has ever been made at; whether `ReaderDemo`'s floor survives its own `blur(2.6px) saturate(0.55)`, i.e. whether four wall colours still say "your feeds" rather than "grey", and whether the pane's elevation shadow reads as lift against a dark scrim; **container-query support**, the one genuinely new platform feature on the page (fine in current browsers; fallback is a fixed 11px base); and at 390px, that the canvas keeps its arrangement while shedding card bodies and tags (it must NOT tidy into a stack), the reader pane keeps a visible margin, and omnivore is still the largest of the three. → FIX-PROGRAMME 2026-07-26 + 2026-07-27; `LOGGED-OUT-REGISTER-ADR.md` §VIII + §IX.
- A standing pile of "NOT browser-verified" items across FIX-PROGRAMME entries — worth one consolidated smoke session rather than per-item. **Session part 1 ran 2026-07-17 (11 verifies; found the strfry CRITICAL above) — see the FIX-PROGRAMME 2026-07-17 entry for exactly what passed.** Remaining browser-tier: Explain (pane-mode, first-run, cursor bubble), PaywallGate copy, the client-side publish→draft-delete, reopen-older-draft, mobile gestures. **Playwright is available and works against the dev stack** (`~/.npm/_npx/*/node_modules/playwright`, cached Chromium; auth via `POST /api/v1/auth/dev-login` then transplant the `pp_session` cookie onto the web origin — the pattern is in the session's scratchpad scripts). Two standing traps when driving dev: seeded accounts have **no email** (dev-login needs one — set one on a seeded writer), and dev's `STRIPE_SECRET_KEY` is the placeholder `sk_test_...`.
- `scripts/reconcile-ledger.sql` — **dev run DONE 2026-07-16, clean** (see §1.9); repeat on prod after the next deploy for the meaningful money-parity check.
- `/thread` external N+1 walk: needs real thread fixtures or a parity harness before the recursive-CTE rewrite is safe — the rewrite itself stays documented debt.
- ~~**∀ lens eyeball pass (FORALL-CUT-AND-LOCKUP-ADR §VII).**~~ — **VOID 2026-07-22, WORKSPACE-COLUMN-LAYOUT-ADR Slice 4**: the lens it was to verify (seam through disc + wordmark, the lens→paint swap, the difference-blended focus ring) no longer exists — the disc is docked in an opaque nav row and always paints. **What survives of it as a live browser-tier check** is the nav row itself, which is equally visual and equally unverified: with the row rendered in **light and dark**, confirm the row reads against the bone floor on its 4px slab alone, that the lockup sits right at the row's right end (disc 40 / wordmark 24 — §V says tune ±2, and the 40 is what GRID-centres it in `NAV_ROW_H = 56`), that the menu and Search open upward with clean clearance over the slab, that a Glasshouse pane never slides under the row, and that a floor-mode Explain bubble anchored on the disc still reads (it sits below the row by design — see the Slice 4 as-built note). → FIX-PROGRAMME 2026-07-22 (Slice 4) entry.
- **Regimented mode eyeball pass (WORKSPACE-COLUMN-LAYOUT-ADR §V, Slice 5)** — shipped green on every static gate but never driven in a browser. With ≥3 feeds: `\` opens the parade with Feed 1 leftmost and the numerals ascending; `\` again returns the custom layout **unchanged** (the mode is a view, so this is the load-bearing check); a drag or resize *inside* the parade leaves the mode and keeps that one edit; **picking a vessel up and dropping it straight back stays IN the mode and leaves the custom layout untouched** (the `dropIsNoop` guard from the 2654eaf audit batch — the silent-loss bug it closed is exactly this gesture, so it is the second load-bearing check); hiding a feed while regimented re-derives without touching the stored layout; `\` is inert in a text field, under a Glasshouse pane, and mid-drag; enough feeds to force the `SLOT_MIN_W` floor still scrolls rather than rendering slivers. From the same audit batch, two floor-mode checks: a band drop onto a column already at capacity arms a **merge** (never an insertion that would push feeds below the nav row), and hovering the empty ground beside a slot narrower than its column arms nothing. → FIX-PROGRAMME 2026-07-22 (Slice 5 + audit batch) entries.

---

## Suggested attack order

0. ~~§0 commit-audit HIGHs + MEDIUMs~~ — **done 2026-07-09** (all seven items; only the cosmetic tail remains, batched with §7). Deploy residue: prod nginx force-recreate + the §11 runtime verifies.
0b. ~~Re-check the 2026-07-16 batch against "component ≠ feature"~~ — **done 2026-07-17** for the high-risk candidates: M5/M6/M7/M9/M10 + M24 all **driven and passed** (M10 + M24 with negative controls), one new residual found (M9's PATCH arm doesn't guard `role` — §0e). **The batch's fixes are real; its *evidence* was not.** Two follow-ons split out into §11: **write the missing M3/M4/M25 tests** (mutation-proved zero coverage — do these next, they're money + credential paths) and **finish the sweep with M13/M15**.
0c. ~~Write the M3/M4/M25 tests~~ — **done 2026-07-17** (§11). All three covered and **mutation-verified**; 248 → 272 tests. M4(b) got the repo's second DB-backed test (real PG, always-rollback, skipped without a DB URL — CI unaffected), which required extracting the reserve path's two order-dependent reads to exported SQL constants so the test runs the cycle's own SQL. One production change, behaviour-preserving.
0d. ~~Finish the 0b sweep: M13 + M15~~ — **done 2026-07-17** (§11). Both driven before/after. **M13 was still live** (the fix's decoder truncated the fractional epoch back to whole seconds — 3 of 5 same-second rows unreachable on `/tags/:name/posts`; re-fixed, +2 untouched cursors) and **M15's three claims reproduced** (23503 wedge, guards, privacy inversion). Three mutation-verified tests; two SQL constants extracted to stop test/prod drift. **The 0b sweep is now complete — the next unblocked item is the Stripe session (1), which needs real keys.**
0. ~~**RUN §5 sequence steps 4–9**~~ — **DONE 2026-08-01, seven of seven green.** Steps 1–11 and 13 are now all green; only 12 remains, blocked on the Dial-A rework. Full record in FIX-PROGRAMME; the headline is that step 9 could not run at all as written and **why** was a flip blocker: asking for allocation on an ineligible card brand 500s, which is classified (correctly) as ambiguous and wedges that reader's tab permanently — no charge, no card-action flag, reads stuck `accrued`, writer unpaid, silently and forever. Fixed by pre-flighting the brand; step 9 now passes 11/11 as the end-to-end proof.

0b. **AWAITING STRIPE ENGINEERING on the Mastercard 500 — raised 2026-08-01, still a flip gate.** Measured (`segregation-probes.ts --brands --repeat 5`, fifty samples, Visa control in-window): `mastercard` 500s on `allocated_funds[enabled]=true` in all three variants, while `diners` works. **Stripe support confirmed Mastercard IS in scope** — their docs list Visa/Mastercard/Discover/Amex/Swish — **and that these 500s are a bug in the preview implementation**, now escalated. So the framing has changed: this is a Stripe defect with a fix pending, not a permanent scope boundary, and **the residual consequence is temporary rather than structural**. Outstanding with them: (a) when the Mastercard fix lands; (b) whether eligibility is network-based (which would explain Diners working despite not being in the documented list, and make relying on it safe) or brand-based (in which case Diners could start 500ing when they tighten it, and our allow-list would wedge those readers); (c) our argument that an unsupported brand should return `400 invalid_request_error` rather than a 500 — the half most likely to actually get fixed, and the half that causes the damage, since the 500 is what makes it ambiguous and ambiguity is what wedges the tab.

    **Two things must happen when they reply.** **(1) RE-MEASURE AND RESTORE:** re-run `--brands --repeat 5` (against live once enabled) and put every 5/5 brand back into `ALLOCATION_ELIGIBLE_CARD_BRANDS`. Leaving Mastercard excluded after the fix is not safe-by-default — it silently routes roughly a third of UK card volume to the unsegregated residual forever, and nobody will connect that back to the constant. **(2) DO NOT BASELINE `allocated_residual_alert_bps` DURING THE WORKAROUND PERIOD** (§3.3d): with Mastercard excluded the residual reads roughly the Mastercard share of payments, not the small credit-funded floor the dial was designed around, so a baseline taken now would be measuring the workaround.

0c. **Bounded retry for a deterministic ambiguous failure — proposed, not built, owner call. Priority raised 2026-08-03: a SECOND deterministic-wedge mechanism is confirmed live (§0o.1).** The Mastercard bug demonstrated (not hypothesised) that Stripe can return a 500 that recurs identically forever. Our design correctly treats an ambiguous error as retryable, so such a settlement retries every reconcile cycle indefinitely while the tab stays blocked by the in-flight guard and `card_action_required_at` is never set. The brand allow-list stops the cases we know about; it cannot stop the next one, and it is now a list that can go stale in **both** directions (Mastercard restored, Diners possibly withdrawn). **And the next one has already arrived — and is now handled:** §0o.1's `StripeIdempotencyError` payload-drift wedge (three specimens measured in dev) got its specific handler 2026-08-03 (`recoverIdempotencyConflict` — that one was *resolvable* ambiguity, and the resume sweep gained an 8h stuck-pending age alert covering every mechanism, known or not). What remains proposed HERE is the generic bounded retry, the only cap on mechanisms not yet met. A bounded retry — after N consecutive ambiguous failures on the same settlement row, mark it failed, release its claims, flag the card — caps *any* deterministic ambiguous failure at "the reader is asked for a different card" rather than a permanent silent wedge. Complementary to the pre-flight, not an alternative. **Not built:** it changes failure semantics on a money path and wants a deliberate decision on N and on what the reader sees.

    **Deployment note:** ~~bundle the pre-flight with migration 168 (§11's pending-deploy entry)~~ — **both deployed**: 168 was live 07-31, the pre-flight 08-01 (verified by marker grep — §11). The standing fact worth keeping: the pre-flight is **inert while the flag is off** (`allocatedFundsParam` returns `{}` on the flag before it ever consults the brand), but it MUST be live before the flag is ever flipped — it is, so this is now a property to preserve, not work to do.

0e. ~~**Payment perimeter — decide the spend-conversion credit (§1.16), the only thing blocking the W0 letter.**~~ — **STRUCK 2026-08-06, the day it was written; its one residual check ran clean on prod 2026-08-07 (0 rows), so the perimeter's critical path is now discharged in full.** The route it turns on has been 503-gated dark since 2026-07-16 (`SUBSCRIPTION_CONVERT_ENABLED`, "Do NOT flip"), so it blocks nothing, the alert cannot fire from it, and the fix it demanded was already a precondition of revival in §0b item 4. Full correction at §1.16. **The perimeter's critical path is therefore discharged**: W1 detection + runbook and W6 shipped 2026-08-06, and everything else (§1.18) is parallel-lane. The one residue is a *check*, not a decision — run the negative-tab query against prod once, since pre-2026-07-16 history could have left a real credit for the new alert to surface.

1. **F4 live check + pub-split re-pay + card re-auth prompt** (§1.1/§1.2/§1.4 — one Stripe-correctness session with real keys; closes the last reader-facing money loop). **Bundle the §11 Stripe verifies into this same session** — they're blocked on the same thing (dev's key is the placeholder `sk_test_...`), so one key unblocks both. *(§1.13's two independent defects are both closed now — the `finalisePublicationPayout` zombie parent 2026-07-29, the webhook-scope contradiction 2026-07-30.)* **Largely discharged 2026-07-30/31**: F4 confirmed and deleted, pub-split re-pay shipped, the card prompt shipped. What the key actually bought was bigger than the list: driving the real service against a sandbox found **two P0s in the pre-existing settlement path** (no `payment_method` on the PI ⇒ no charge could ever succeed; and a duplicate-charge window between complete and confirm) — both fixed 2026-07-31, FIX-PROGRAMME. **Still owed from this item: the §1.4 S2 browser test** (SetupIntent card attach incl. 3DS `4000002500003155`), which needs the sandbox *publishable* key and a `docker compose build web` (it is a build arg).
1b. ~~**Funds-segregation §5 step 0**~~ (§1.13) — **CLOSED 2026-07-30, both halves.** The five sandbox probes ran green, and the production baseline ran and **returned nothing to measure**: zero payouts in the window, zero writers over threshold, zero connected accounts, on a gated pre-launch platform. So `allocated_residual_alert_bps` and `payout_max_slices` keep their placeholders — but that is now a **recorded state blocked on payout volume**, not an outstanding task, and it is written in beside them in `config-defaults.sql` so the next reader of that file learns it there. Re-run the SQL once payouts flow.

    **This inverted the ADR's own ordering, which is the part to carry forward.** §5 says measure then flip, and §6.3 warns that a cycle spanning the flip mixes allocated and unallocated charges and spikes the residual as pre-flip earnings drain. With zero payouts there *are* no pre-flip earnings, so flipping before the first real payout removes the transition entirely and segregates every pound from the first one. The measurement cannot precede the money. The sequence that applies: **flip when Stripe enables live, then baseline from the first 30 days.** §7.5's cross-border question is moot until a connected account exists.

    Findings from the probe half live in ADR §5's results block — read that rather than any summary. The two things worth knowing without opening it: `transfer.reversed` carries the **child's own transfer id** with a **cumulative** `amount_reversed` (so §3.5's re-keying is confirmed against a real payload), and `reversed` stays FALSE on a partial reversal, so nothing may key on that boolean. Also recorded there: a Stripe-side transient 500 on `refund_application_fee=true`, reported and closed by Stripe, whose durable lesson is methodological — an isolation matrix run entirely inside a failure window shows correlation with whatever it varies and cannot show cause.

    Commands, kept for re-running rather than as owed work:

    ```bash
    STRIPE_SECRET_KEY=<sandbox key> npx tsx scripts/segregation-probes.ts --probe 1,4,6,7,7b
    ssh <prod> 'docker exec -i <pg> psql -U platformpub -d platformpub' < scripts/segregation-baseline.sql
    STRIPE_SECRET_KEY=<live key> npx tsx scripts/segregation-probes.ts --countries
    ```

    What each needs from a human, since none of it can be done from a dev laptop: a Stripe **Sandbox with the allocated-funds beta enabled** and its secret key (not classic test mode — allocation does not exist there; dev's key is the placeholder `sk_test_...`), and prod DB access. Pass `--destination acct_…` if the sandbox has an onboarded connected account; the auto-create fallback may need onboarding before transfers succeed. The §7.5 country enumeration is about the *live* connected accounts, so it alone reaches for the live key — but only for a read-only `accounts.list`, and the SQL's Query C gives the same id list from the DB if you would rather resolve them in the dashboard. Nothing that moves money touches a live key; the probes refuse one outright.

    A fourth mode joined the file afterwards and is **not** part of step 0: `--f4` settles the audit-F4 premise (attack order 1 item 1) off the same sandbox's `events.list`. It is read-only and shares the technique that resolved the webhook-scope contradiction — ask the event log what Stripe emitted rather than reason about what it ought to.

    **When the SQL is re-run, read Query 0 before Query A.** Migration 165 stamps `subscription_events.tab_settlement_id` going forward with no backfill, so the exact credit-funded classifier only speaks for the window *since the stamp started* — on prod, **2026-07-29** (migration applied 16:35 UTC; the stamping code live from ~16:10). A window straddling that date must be read off the **heuristic** row; the exact row takes over once a full 30 days sits behind the stamp, i.e. from roughly **2026-08-28**. Query 0 reads `_migrations` and says which classifier the window supports; Query A prints both side by side.

    The probes deliberately drive Stripe raw rather than our code (§5 step 0: "with no §3.3 code at all") and record a stated expectation as a check with a verdict without ever halting the run — an unexpected shape is the finding, not an error. They refuse an `sk_live_` key for anything that moves money; `--countries` and `--f4` are read-only and are the two modes that accept one.
2. ~~Publication-subscription distribution~~ — **done 2026-07-10** (§1.3; runtime verify in §11).
3. ~~**Owner dashboard**~~ — **done 2026-07-22** (§3.1; small residue queued there).
3b. ~~**§5 sequence steps 2–12**~~ — **superseded by item 0 above.** Steps 2, 3, 10 and 11 went green on 2026-07-31 (and 11 found a P0 in the process); 4–9 are written and awaiting a sandbox key; 12 stays blocked on the Dial-A rework. The argument this item made stands and is worth keeping: the same key found **two P0s that had been on prod for months, in code the mocked suite pronounced green** — which is why the remaining steps are worth a session rather than a shrug. Its one live residue: **confirm prod is running the settlement fix** before the next session (§11's assumed-not-verified item — ten seconds, and everything else is moot if it is not actually live).

4. ~~**Small-money batch**~~ — **done 2026-08-04**, all four parts. §1.5 calendar arithmetic + the month-end anchor (migration 170), §1.6 the provisional-unlock flip, §1.10 grant-mode redemption (decided: wire, migration 171). §1.9's dev reconcile-ledger run had already closed 2026-07-16; the meaningful prod run stays in §11. **Deploy residue:** migrations 170 and 171 are pending prod — see §11's pending-deploy entry, and establish prod's state from image dates, never from a tracker line.
5. ~~**Cleanup cluster** (§7) as a palate-cleanser batch~~ — **done 2026-07-20** (14 of 18 items in one pass; 2 dispositioned without code, 2 narrowed to design calls — see §7). **Slice-8 dedup design session** (§6) still outstanding, separately.
6. **Landing page + onboarding + subscriptions Phase 2** (the launch-cohort conversion set). **Onboarding shipped 2026-08-07** (§3.3 — the first-session welcome); the rest of the set stands.
7. Everything in §8 by measurement, not vibes — CDN + measurement first.

---

## NEXT SESSION STARTS HERE (rewritten 2026-08-08, after §3.5 slices 1 + 2)

Read the box before the queue — this block says what work EXISTS, not what is deployed.

**Where the last session ended.** §3.5 slices 1 and 2 shipped: `services/article-publisher.ts` (the scheduler is now a *caller* of the one publish pipeline, with `publishedAt`/`sendEmail`/`matchDrives` options all defaulting to prior behaviour) and `services/media-store.ts` (`storeImage(uploaderId, buffer)`). Pure refactors, no import code, 8 new tests mutation-proved three ways, suite 469 → 477. Nothing is half-finished behind you. **A four-day commit review then landed 2026-08-08 (§0q):** the window's structure held; its one money fix (§0q.1, the resume-sweep halt bypass) **shipped the same day** — deploy is a payment-service rebuild alongside the gateway one above. Still open from it: the publications-overlay pair visible on dev today (§0q.2/3) and the smaller batch (§0q.4–8); all unblocked, none gated on a human, worth folding into whichever session comes next.

**Deploy requirements for the slices 1 + 2 commit:** **gateway only** — `docker compose build gateway && docker compose up -d gateway`. No migration, no env var, no flag, no web change. Both are behaviour-preserving extractions, so it is safe in either ordering. Prod was last MEASURED current with `dab1a95c` at 17:12 on 2026-08-07; establish its state from the box, never from this line.

**1. §3.5 is now GATED on a human, and the gate is the pickup: get a real Substack export** (ADR §XI, slice 0). Slice 3 is the adapter — unzip, CSV, HTML→markdown — and writing it against documentation rather than an archive is guessing at the only part of the feature that is hard to test afterwards. Five facts are unconfirmed, and one of them decides a shipped design decision: **whether the paid body is in the export at all, or truncated to the free preview.** If it is truncated, §V's "a paid post lands as a draft" is wrong and should become an honest skip. One cooperating writer from the launch cohort answers all five in an afternoon; the fallback is a throwaway Substack with three posts (one free, one paid, one unpublished), an hour's work. **Do not start slice 3 without one** — the two slices that could honestly be built blind have been built.

**2. If no archive is to hand, take §3.7 instead** rather than guessing at slice 3. Its **browser look at the waitlist panel** is the oldest unseen thing on the admin surface — the admit ROUTE is proven end to end (2026-08-07), so what is unrendered is only the UI: the per-row action, the confirm dialog, the three status renderings. Playwright is cached and the driving pattern is in FIX-PROGRAMME 2026-08-07 (init-script `ah:color-mode` for pre-paint mode; two framings, because `fullPage` paints `position: fixed` chrome across the middle of the shot). Alongside it, §3.7's **Export CSV decide-or-decline** — a decision more than a build, which has already vanished from a build order once.

**Also live, worth an eyeball rather than a task: the welcome is in front of every existing prod member**, since migration 176 reads NULL as un-welcomed and nobody has ever been asked for a display name. Log in and take the four steps as a member would. That is also the web image's only real verification (Next minifies, so an identifier grep proves nothing).

**Dev-state notes for whoever picks this up.**
- **A publication fixture now sits on the dev DB**: `evidence-audit` is dressed as *The Longshore Review* — cover, logo, tagline, about, 8 articles across 2025–26 (3 with covers, 2 paywalled), plus 3 invented writer accounts (`ilse_marchetti`, `oreto_kanda`, `wynn_abbotsley`) on the masthead. All imagery is SVG **data URIs**, so it needs no network and no Blossom blob. Layout is set to `magazine`. Delete with `DELETE FROM articles WHERE nostr_d_tag LIKE 'lr-%'` if it is in the way; otherwise it is the fastest way back into any publication surface.
- `wrenfallow@test.local` is a completed-onboarding account (`onboarded_at` stamped), so it can no longer serve a `looksDerived` drive; a fresh one needs a waitlist join + admit, two curl calls.
- Dev's `nginx` is in a restart loop on a missing letsencrypt cert (`/etc/letsencrypt/live/all.haus/fullchain.pem`) — harmless for the direct-port dev flow, not a new fault to go hunting.
- `PUBLIC_MEDIA_URL` points at prod in `docker-compose.yml`, so any avatar or upload SAVED in dev renders broken there — check the blob against `BLOSSOM_URL` before calling that a bug.

**After §3.5 the §3 queue is:** the landing copy redline (§3.2), and §3.7's last item (the joiner acknowledgement email, still gated on counsel D9).
