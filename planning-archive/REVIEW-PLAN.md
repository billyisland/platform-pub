# Codebase Review & Refactoring Plan

Produced 2026-05-15 against the `workspace-experiment` branch (merged to `master` 2026-05-29). Assessed against all 18 live ADRs, 8 audit documents, `feature-debt.md`, and the full codebase inventory (~3,700 .ts/.tsx files, 82 migrations, 8 workspaces, 11 Docker services).

Each session is scoped for roughly 2–4 hours of focused Claude Code work. Sessions are grouped into phases; within a phase, sessions can run in any order unless noted. Cross-session dependencies are called out explicitly.

**Session output format:** The deliverable from each session is an update to this document: the session's checklist items are marked done, and a **Diagnoses & Remedies** section is appended beneath the checklist recording every issue found and its proposed fix. Detailed metrics go to a companion file in `docs/audits/` when the volume warrants it; this document is the master index of what was found and what to do about it.

---

## Phase 0 — Orientation & Baseline

### Session 0.1: Static analysis baseline

**Goal:** Establish measurable starting points before any refactoring.

- [x] Run `tsc --noEmit` across all 9 workspaces; record error count per workspace — **0 errors across all**
- [x] Run `npx knip` at root; record unused exports, unused files, unused dependencies per workspace — **47 items (all web)**
- [x] Run `npm audit --audit-level=high` for backend + web; record counts — **8 backend (1 critical), 6 web (1 high)**
- [x] Run ESLint across all workspaces; record warning count (errors should be 0) — **285 warnings / 0 errors; web ESLint not configured**
- [x] Grep for `as any` across all `.ts`/`.tsx` files; categorise by service, record count — **22 total (16 gateway)**
- [x] Grep for `// TODO`, `// FIXME`, `// HACK` in source (excluding node_modules/dist) — **0**
- [x] Count test files per service; compute ratio of test files to source files — **23 test files / 375 source files (6.1%)**
- [x] Output: `docs/audits/BASELINE-2026-05.md` with all metrics

**ADR reference:** `CODE-QUALITY.md` (tools catalogue)

**Diagnoses & Remedies (0.1):**

| #   | Diagnosis                                                                                   | Severity | Remedy                                                                                                                                                                                 | Effort  |
| --- | ------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D1  | `sanitize-html` ≤2.17.3 — critical XSS via `xmp` raw-text passthrough (GHSA-rpr9-rxv7-x643) | Critical | `npm audit fix` — non-breaking patch available                                                                                                                                         | Minutes |
| D2  | `fast-uri` ≤3.1.1 — path traversal + host confusion; chains through fastify (5 high CVEs)   | High     | Upgrade to `fastify@5.8.5`. Breaking change — requires testing all route schemas                                                                                                       | Hours   |
| D3  | `ip-address` ≤10.1.0 — XSS in Address6 HTML methods; chains through `geoip-lite` (moderate) | Moderate | Upgrade to `geoip-lite@2.0.2`. Breaking change — verify traffology-ingest IP lookup API                                                                                                | Hours   |
| D4  | Next.js 14.x carries 13 advisories (DoS, cache poisoning, XSS, SSRF, request smuggling)     | High     | Upgrade to Next.js 16.x. Major version jump — significant migration effort                                                                                                             | Days    |
| D5  | Web frontend has no ESLint configuration — 207+ source files unlinted                       | Medium   | Create `eslint.config.mjs` with `@next/eslint-plugin-next` strict preset + shared TS rules                                                                                             | Hours   |
| D6  | 175 `no-unnecessary-type-assertion` warnings (auto-fixable)                                 | Low      | `npx eslint --fix` across backend workspaces                                                                                                                                           | Minutes |
| D7  | 83 `no-explicit-any` warnings across backend (243 of 285 total warnings in gateway)         | Medium   | Triage gateway routes — most are Fastify generic param/query types that should use typed route schemas                                                                                 | Hours   |
| D8  | 25 `no-unused-vars` warnings                                                                | Low      | Remove or prefix with `_`; review `shared/accounts.ts` (`destroySession`), `shared/http-client.ts` (`logger`), `shared/subscription-emails.ts` (`logger`) for intentional dead code    | Minutes |
| D9  | 22 `as any` casts — 16 in gateway, mostly untyped Fastify `req.params`/`req.query`          | Medium   | Add Fastify generic route schemas (`RouteGenericInterface`) to affected routes: `publication-auth.ts`, `publications/cms.ts`, `publications/public.ts`, `trust.ts`, `notifications.ts` | Hours   |
| D10 | 47 knip findings (8 unused files, 16 unused exports, 23 unused types) — all in web          | Low      | Audit each: remove genuinely dead code; for knip false positives (6 test files), configure knip's `vitest` plugin or entry patterns                                                    | Hours   |
| D11 | Test ratio 6.1% — gateway has 4 tests for 73 files; traffology has 0 tests for 15 files     | Medium   | Addressed in Sessions 7.1 / 7.2. Highest priority: payment settlement, auth flow, resolver classification                                                                              | Days    |
| D12 | `postcss` <8.5.10 XSS (moderate) — transitive via Next.js                                   | Low      | Resolved by D4 (Next.js upgrade)                                                                                                                                                       | —       |

**Full metrics:** `docs/audits/BASELINE-2026-05.md`

### Session 0.2: ADR conformance matrix

**Goal:** Produce a single table mapping every live ADR section to its implementation status.

- [x] For each of the 18 live ADRs, list every normative section (numbered requirements, specified behaviors, data model definitions) — **~400+ requirements across 18 ADRs**
- [x] For each section, grep/read the implementing code and mark: **conformant**, **partial**, **missing**, or **divergent** — **core platform strongly conformant; gaps in unbuilt specs**
- [x] Flag any code that contradicts its governing ADR — **7 divergences found (see below)**
- [x] Output: `docs/audits/ADR-CONFORMANCE-2026-05.md`

**Key ADRs to verify:**

- `ALLHAUS-OMNIBUS.md` — trust graph layers 1/2/4 vs gateway/trust.ts + feed-ingest trust libs
- `UNIVERSAL-FEED-ADR.md` — all 5 phases vs feed-ingest tasks + gateway routes
- `ALLHAUS-REDESIGN-SPEC.md` — all 6 steps vs web components
- `RELAY-OUTBOX-ADR.md` + Phase 4 — all 6 phases vs relay-outbox.ts + relay-publish.ts
- `PUBLICATIONS-SPEC.md` — data model + roles vs gateway routes + migrations
- `EMAIL-ON-PUBLISH-SPEC.md` — status (appears unbuilt)
- `TRAFFOLOGY-MASTER-ADR-2.md` — Phase 1 vs traffology-ingest + traffology-worker + web
- `WORKSPACE-EXPERIMENT-ADR.md` — slices 1–32 vs web/src/components/workspace/

**Diagnoses & Remedies (0.2):**

| #   | Diagnosis                                                                                           | Severity | Remedy                                                                                       | Effort  |
| --- | --------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------- | ------- |
| D13 | `BookmarkButton` still rendered on ArticleCard; redesign spec says replaced by reading-history      | Low      | Remove `BookmarkButton` from ArticleCard action row, or update spec to acknowledge retention | Minutes |
| D14 | `feed_items.content_html` column specified in Universal Feed ADR but not created; uses JOIN instead | Low      | Intentional — document divergence in ADR or add denormalised column for query perf           | Hours   |
| D15 | Jetstream DID threshold at 150 vs ADR's ~10K; daily cap omits 24h window filter                     | Low      | Both intentional. Document in ADR. CLAUDE.md already documents the daily cap decision        | Minutes |
| D16 | Mastodon OAuth scope `read:accounts` vs ADR's `read:statuses`; needed for `verify_credentials`      | Low      | Update ADR to match implementation                                                           | Minutes |
| D17 | Relay outbox CHECK constraint includes `drive_deletion` not listed in ADR entity_type enum          | Low      | Update ADR to include `drive_deletion`                                                       | Minutes |
| D18 | Publications ownership transfer lacks magic-link re-auth required by spec                           | Medium   | Implement re-auth step or downgrade spec requirement                                         | Hours   |
| D19 | Publication articles route uses `/by-slug/:slug/articles` vs spec's `/:slug/articles`               | Low      | Path conflict avoidance — update spec to match                                               | Minutes |
| D20 | `pub_payout_completed` notification type never emitted                                              | Low      | Wire notification into publication payout worker finalisation step                           | Hours   |
| D21 | Traffology ingest port 3005 vs ADR-specified 3004                                                   | Low      | Update ADR to match implementation (nginx already correct)                                   | Minutes |
| D22 | Traffology interpreter: only 5/29 observation types have generation logic                           | Medium   | Implement remaining interpreter detectors (prioritise subscriber + anomaly types)            | Days    |
| D23 | Traffology: zero test files for either service                                                      | Medium   | Write tests for aggregation + source resolution (pure functions, easy to test)               | Hours   |
| D24 | 2 missing Layer 1 trust signals: subscriber_count, continuous_activity                              | Low      | Add to `trust-layer1-refresh.ts` — data is queryable, just not yet computed                  | Hours   |
| D25 | Gateway decomposition Steps 2–3 stalled (service layer extractions, messaging-service)              | Medium   | Resume after workspace merge decision. Not blocking other work                               | Days    |
| D26 | Owner dashboard spec entirely unbuilt (10 requirements)                                             | Low      | Schedule as dedicated build phase. All data queryable from existing tables                   | Days    |
| D27 | Email-on-Publish Phases 2–4 unbuilt (follower emails, publication emails, stats)                    | Medium   | Phase 2 (follower expansion) is highest priority — adds value to existing followers          | Days    |
| D28 | Currency strategy Option 2 (display currency) unbuilt                                               | Low      | Five deliverables, small effort. Schedule when international users appear                    | Days    |
| D29 | UI-DESIGN-SPEC batches 3–6 (~20 features) outstanding                                               | Low      | Triage against feature-debt priorities                                                       | Days    |
| D30 | REDESIGN-SCOPE anti-workspace stance superseded by experiment; no formal retirement                 | Low      | Add "superseded by WORKSPACE-EXPERIMENT-ADR" note to REDESIGN-SCOPE                          | Minutes |
| D31 | ALLHAUS-ADR-UNIFIED dual-graph anonymity (§VII) entirely unbuilt                                    | Low      | Acknowledged future work. No action needed until trust Phase B                               | —       |
| D32 | Backfill migration 054 uses monolithic INSERT...SELECT vs ADR's batched requirement                 | Low      | Works at current scale. Refactor if table exceeds ~1M rows                                   | Hours   |
| D33 | `consistent-type-imports` ESLint rule in Code Quality ADR but not in actual config                  | Low      | Add rule to `eslint.config.mjs`                                                              | Minutes |

**Full matrix:** `docs/audits/ADR-CONFORMANCE-2026-05.md`

---

## Phase 1 — Shared Foundation

### Session 1.1: `shared/` library review

**Scope:** 21 .ts files, 3 test files. The foundation every service imports.

- [x] **Auth module** (`accounts.ts`, `keypairs.ts`, `magic-links.ts`, `session.ts`): verify session lifecycle matches the 2h JWT / 1h refresh spec from feature-debt §1; check for leaked secrets in error paths — **JWT is 30d/7d refresh (not 2h/1h — feature-debt spec is stale). Magic links: 32-byte token, SHA-256 hashed, 15-min expiry, single-use. No secrets in error paths. `keypairs.ts` is pubkey-only (privkey ops moved to key-custody). `accounts.ts` signup uses withTransaction correctly. No password/privkey leakage in error responses**
- [x] **DB layer** (`client.ts`, `migrate.ts`): verify pool configuration (max connections, idle timeout, statement timeout); check transaction helpers for proper rollback on error — **Pool: max=20, idle=30s, connect=5s, statement_timeout=10s. `withTransaction` does BEGIN/fn/COMMIT with ROLLBACK on any error and client.release() in finally. Fatal pool errors exit the process for orchestrator restart. Migration runner wraps each file in its own txn. All sound**
- [x] **Crypto** (`crypto.ts`): review AES-256-GCM implementation for `LINKED_ACCOUNT_KEY_HEX`; verify IV uniqueness, authenticated encryption, key rotation support per D6 (versioned ciphertext) — **Sound. 12-byte random IV per encrypt via `randomBytes(12)`. AES-256-GCM with 16-byte auth tag. Key versioning: writes with current version byte, reads try versioned then legacy. Version byte 1–8 range avoids ambiguity with random IV first bytes. `encryptJson`/`decryptJson` convenience wrappers. One minor: catch-all fallback in decrypt could mask auth failures (see D45)**
- [x] **HTTP client** (`http-client.ts`): verify SSRF protections — DNS-rebinding TOCTOU fix (undici Agent with pinned lookup), private-range blocklist completeness, `validateWebSocketUrl` coverage — **Comprehensive. Numeric IPv4 CIDR ranges cover all RFC 1918 + loopback + link-local + CGNAT + multicast + reserved. IPv6 parser handles :: compression, zone IDs, IPv4-embedded suffixes; checks ::, ::1, ::ffff:x.x.x.x, fe80::/10, fc00::/7, ff00::/8. DNS-rebinding pinned via undici Agent custom lookup hook. Manual redirect following re-validates each hop. `pinnedWebSocketOptions` returns pinned lookup for WS callers. 10s timeout, 5MB limit, 3 redirect max**
- [x] **Relay outbox** (`relay-outbox.ts`): verify `enqueueRelayPublish` contract — transactional insert + job scheduling, entity dedup — **Sound. INSERT with ON CONFLICT on signed_event->>'id' for dedup. Graphile-worker job scheduled inside same txn with job_key for dedup, max_attempts:=1 (relay-publish task owns retry). Conflict path returns existing row ID. All parameterised queries**
- [x] **AT Protocol OAuth** (`atproto-oauth.ts`): verify `NodeOAuthClient` factory — confidential client config, PKCE + DPoP + PAR, loopback dev mode, JWK validation — **Sound. Prod: private_key_jwt + ES256 + DPoP + PKCE, requires HTTPS base URL + ATPROTO_PRIVATE_JWK. Dev: loopback client_id with allowHttp, token_endpoint_auth_method=none. DbSessionStore encrypts sessions with AES-256-GCM via shared crypto. DbStateStore for PKCE state with 15-min TTL. Singleton with null-on-rejection cache so transient errors don't lock out**
- [x] **Email** (`email.ts`, `publish-emails.ts`, `subscription-emails.ts`, `publish-email-template.ts`): review template injection risks, unsubscribe token security — **Publish emails properly escape all user content via `escapeHtml()`. Subscription emails do NOT escape writerName/readerName (D36). Unsubscribe tokens use HMAC-SHA256 + `timingSafeEqual` verification — secure. Magic link email is hardcoded template (no injection surface). Postmark + Resend + console providers all structurally sound**
- [x] **Utilities** (`slug.ts`, `text.ts`, `env.ts`, `advisory-locks.ts`, `voting.ts`): check for consistency with gateway/web consumers — **`slugify`/`generateDTag` shared between gateway and scheduler. Web has a mirror at `web/src/lib/publish.ts` (can't import shared/ from Next.js) — drift caught by `web/tests/publish.test.ts`. `truncatePreview` is code-point-aware to match Postgres `LEFT()`. `requireEnv`/`requireEnvMinLength` used at service startup. Advisory lock IDs centralised. `voteCostPence` is isomorphic (no Node/browser deps)**
- [x] Run shared tests; verify all pass — **28/28 tests pass (3 test files, 1.04s)**
- [x] Identify missing test coverage for critical paths (crypto, auth, HTTP client) — **Crypto has 3 test files covering encrypt/decrypt round-trips. Auth (session, magic-links, accounts) has 0 dedicated test files — session verification, magic link lifecycle, and account signup are untested. HTTP client SSRF protections have 0 test files — private IP rejection, IPv6 parsing, redirect re-validation, DNS-rebinding pin are untested. These are the two highest-priority gaps**

**ADR reference:** `RELAY-OUTBOX-ADR.md`, `UNIVERSAL-FEED-ADR.md` §V (SSRF), `ALLHAUS-OMNIBUS.md` (crypto)

**Diagnoses & Remedies (1.1):**

| #   | Diagnosis                                                                                                                  | Severity | Remedy                                                                                                                          | Effort  |
| --- | -------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D45 | Crypto decrypt catch-all fallback (`shared/src/lib/crypto.ts:108`) swallows auth-tag failures alongside version collisions | Low      | Narrow catch to specific error messages or log a warning when falling back, so genuine corruption isn't silently masked         | Minutes |
| D46 | Session spec mismatch: feature-debt §1 says "2h JWT / 1h refresh" but implementation is 30d/7d                             | Low      | Update feature-debt to match implementation (30d/7d is the correct production choice)                                           | Minutes |
| D47 | Zero test coverage for auth module (session, magic-links, accounts) — the login/signup path is untested                    | Medium   | Write vitest tests for: session create/verify/refresh/invalidate, magic link generate/verify/expire/single-use, signup txn      | Hours   |
| D48 | Zero test coverage for HTTP client SSRF protections — private IP blocklist, IPv6 parsing, DNS pin untested                 | Medium   | Write vitest tests for: isPrivateIpv4/v6 edge cases, parseIpv6 non-canonical forms, safeFetch redirect re-validation, pin agent | Hours   |
| D49 | `updateProfile` in `accounts.ts` uses dynamic SET clause construction (`setParts.join(', ')`) — safe but fragile           | Low      | Current code only appends hardcoded column names; no user input reaches column names. Document the invariant                    | Minutes |

### Session 1.2: Database schema & migrations review

**Scope:** 82 migration files + `schema.sql`

- [x] Verify `schema.sql` matches the cumulative result of all 82 migrations — **26 migrations behind (057–082 missing); 15 tables, ~15 columns, ~20 indexes absent from schema.sql**
- [x] Review index coverage: every foreign key should have an index; every query in gateway routes should have a supporting index — **4 missing FK indexes found (subscriptions.publication_id, publication_article_shares, publication_payout_splits.article_id); 1 explore-feed tiebreaker index incomplete**
- [x] Check for missing `ON DELETE CASCADE` / `ON DELETE SET NULL` where referential integrity requires it — **6 FK constraints lack ON DELETE action (articles/drafts/subscriptions on publication_id, vouches attestor/subject, trust_profiles/trust_layer1 user_id); inconsistent with similar tables that do CASCADE**
- [x] Review CHECK constraints for completeness (e.g., `feed_sources` type-target matching, `outbound_posts` action type) — **feed_sources CHECK is comprehensive; 2 raw UUID columns lack FK constraints (feed_scores.publication_id, subscription_nudge_log.publication_id)**
- [x] Verify partial indexes are correctly scoped (relay_outbox hot-query, feed_items scoring, etc.) — **relay_outbox_ready_idx correctly covers the worker poll; feed_items score index missing id tiebreaker for cursor pagination**
- [x] Check for tables that should have `updated_at` triggers but don't — **8 tables have updated_at column without trigger (subscriptions, linked_accounts, external_sources, activitypub_instance_health, notification_preferences, vote_tallies, platform_config, atproto_oauth_sessions)**
- [x] Review migration 076–082 (relay outbox through workspace) for correctness — **All sound. 077 (workspace feeds) has comprehensive CHECK constraints and per-type partial unique indexes. One note: feed_sources_touch_parent trigger takes write lock on parent feed row on every source change**
- [x] Assess vacuum/bloat risk for high-write tables (`feed_items`, `external_items`, `relay_outbox`, `reading_positions`) — **feed_items score UPDATE every 5min creates WAL + dead tuples; traffology.sessions beacon updates hit 4 indexes; relay_outbox has no completed-row cleanup; reading_positions UPSERT-heavy. All need autovacuum tuning**
- [x] Output: list of missing indexes, schema inconsistencies, migration ordering issues

**ADR reference:** `RELAY-OUTBOX-ADR.md` (migration 076), `UNIVERSAL-FEED-ADR.md` (migrations 052–063), `ALLHAUS-OMNIBUS.md` (migrations 065–067), `WORKSPACE-EXPERIMENT-ADR.md` (migrations 077–082)

**Diagnoses & Remedies (1.2):**

| #   | Diagnosis                                                                                                                                                                                                                                    | Severity | Remedy                                                                                                                                                         | Effort  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D53 | `schema.sql` is 26 migrations behind (covers 001–056, actual is 001–082). 15 tables, ~15 columns, ~20 indexes missing. Fresh deploy from schema.sql produces incomplete database                                                             | High     | Regenerate via `pg_dump --schema-only` from migrated DB; update `_migrations` pre-seed to 001–082. Add CI check: dump migrated schema, diff against schema.sql | Hours   |
| D54 | Missing index on `subscriptions.publication_id`. Unique index on `(reader_id, publication_id)` doesn't help publication DELETE FK check — requires seq scan                                                                                  | Medium   | `CREATE INDEX idx_subscriptions_publication ON subscriptions(publication_id) WHERE publication_id IS NOT NULL`                                                 | Minutes |
| D55 | Missing indexes on `publication_article_shares` — `publication_id` and `article_id` FKs have no indexes                                                                                                                                      | Low      | Add individual indexes on both columns                                                                                                                         | Minutes |
| D56 | Missing index on `publication_payout_splits.article_id` FK                                                                                                                                                                                   | Low      | `CREATE INDEX idx_pub_payout_splits_article ON publication_payout_splits(article_id) WHERE article_id IS NOT NULL`                                             | Minutes |
| D57 | Three FK constraints on `publication_id` lack ON DELETE action: `articles`, `article_drafts`, `subscriptions`. Inconsistent — `publication_members/invites/follows` all CASCADE                                                              | Medium   | Add `ON DELETE SET NULL` for articles/drafts (nullable column) and `ON DELETE CASCADE` for subscriptions. Document the decision                                | Hours   |
| D58 | `vouches.attestor_id` and `vouches.subject_id` FKs lack ON DELETE action. Account deletion would fail. Inconsistent — `trust_polls` uses CASCADE                                                                                             | Medium   | Add `ON DELETE CASCADE` to both FKs                                                                                                                            | Minutes |
| D59 | `trust_profiles.user_id` and `trust_layer1.user_id` FKs lack ON DELETE action. Daily cron seeds trust_layer1 for every account — orphaned rows are a real risk                                                                               | Medium   | Add `ON DELETE CASCADE` to both                                                                                                                                | Minutes |
| D60 | `subscription_nudge_log.publication_id` has no FK constraint and no index — raw UUID, no referential integrity                                                                                                                               | Low      | Add FK `REFERENCES publications(id) ON DELETE SET NULL`                                                                                                        | Minutes |
| D61 | `feed_scores.publication_id` has no FK constraint — raw UUID with index but no referential integrity                                                                                                                                         | Low      | Add FK `REFERENCES publications(id) ON DELETE SET NULL`                                                                                                        | Minutes |
| D62 | No composite index for subscription nudge query `SUM(amount_pence) FROM read_events WHERE reader_id=$1 AND writer_id=$2 AND read_at >= month` — scans per-reader row set                                                                     | Low      | `CREATE INDEX idx_read_events_reader_writer_date ON read_events(reader_id, writer_id, read_at DESC)`. Add when analytics show slowness                         | Minutes |
| D63 | Explore feed orders by `(score, published_at, id)` but `idx_feed_items_score` only covers `(score DESC, published_at DESC)` — missing `id` tiebreaker column                                                                                 | Low      | Extend index to include `id DESC`; drop old one                                                                                                                | Minutes |
| D64 | Workspace `sourceFilteredItems` query uses four OR-ed EXISTS clauses in a CTE JOIN — cannot use a single index efficiently                                                                                                                   | Medium   | Acceptable at current scale. Monitor query times; consider denormalised source-match table when feeds grow                                                     | Days    |
| D65 | 8 tables have `updated_at` column without automatic trigger (`subscriptions`, `linked_accounts`, `external_sources`, `activitypub_instance_health`, `notification_preferences`, `vote_tallies`, `platform_config`, `atproto_oauth_sessions`) | Low      | Create `set_updated_at()` triggers in a single migration. Function already exists                                                                              | Minutes |
| D66 | `read_events` composite index `idx_read_events_reader_article` (migration 022) is missing from schema.sql — payment hot-path index                                                                                                           | Medium   | Covered by D53 regeneration, but worth highlighting as payment-critical                                                                                        | —       |
| D67 | `relay_outbox` has no periodic cleanup of completed (`sent`) rows — accumulates forever                                                                                                                                                      | Medium   | Add `relay_outbox_prune` cron task to delete `sent` rows older than 30 days                                                                                    | Hours   |
| D68 | `articles_derive_size_tier` trigger defined in migration 068 then superseded by migration 070 (maps NULL word_count to `standard` not `brief`). No bug but intermediate inconsistency                                                        | Low      | Document in migration 070's comment that it supersedes 068's trigger                                                                                           | Minutes |
| D69 | `trust_layer1` has no index on `computed_at` — stale-detection queries would seq-scan                                                                                                                                                        | Low      | Add index when table exceeds ~10K rows or stale-detection query is added                                                                                       | Minutes |
| D70 | `feed_items` receives full-table UPDATE from `feed_scores_refresh` every 5 min on 48h window — significant WAL and dead tuples                                                                                                               | Medium   | Set `autovacuum_vacuum_scale_factor = 0.01` on feed_items. Consider moving scores to a separate `feed_item_scores` table to avoid wide-row dead tuples         | Hours   |
| D71 | `reading_positions` has no explicit autovacuum tuning despite UPSERT-heavy workload from scroll saves                                                                                                                                        | Low      | Set `autovacuum_vacuum_scale_factor = 0.02` on this table                                                                                                      | Minutes |
| D72 | `pledges.drive_id` FK has no ON DELETE action. Likely intentional (drives use status lifecycle, not hard delete) but undocumented                                                                                                            | Low      | Add comment documenting the decision, or add `ON DELETE CASCADE` if hard delete should work                                                                    | Minutes |
| D73 | `outbound_posts.nostr_event_id` has no standalone index — only included in composite dedup unique index                                                                                                                                      | Low      | Add index only if operational debugging queries by `nostr_event_id` become needed                                                                              | Minutes |
| D74 | `traffology.sessions` has high UPDATE frequency from beacon heartbeats (4 indexes generate dead entries on each UPDATE)                                                                                                                      | Medium   | Set `autovacuum_vacuum_scale_factor = 0.01`; consider `FILLFACTOR = 70` for HOT update efficiency on fixed-length columns                                      | Minutes |
| D75 | `resolver_async_results` has short-lived rows (60s TTL) with prune cron — high INSERT+DELETE churn. Small table but benefits from aggressive autovacuum                                                                                      | Low      | Set aggressive autovacuum parameters; monitor dead tuple count                                                                                                 | Minutes |

---

## Phase 2 — Backend Services (Gateway)

The gateway is the largest backend service (256 .ts files, 54 routes). Split into four sessions by domain.

### Session 2.1: Auth, accounts & middleware

**Scope:** `middleware/auth.ts`, `middleware/publication-auth.ts`, `routes/auth.ts`, `routes/google-auth.ts`, `routes/my-account.ts`, `workers/`

- [x] Review `requireAuth` / `optionalAuth` hooks — session validation, account status check, error responses — **`requireAuth` is well-structured: JWT verify → account status → `sessions_invalidated_at` enforcement → silent refresh past 7d half-life. `optionalAuth` skips status and invalidation checks — suspended/invalidated users treated as authenticated on 29 routes (D170). Incoming identity headers (`x-reader-id`/`x-writer-id`) not stripped from unauthenticated requests (D171)**
- [x] Review `publication-auth.ts` — role hierarchy enforcement, member lookup — **Boolean-flag permission model is correct. Member lookup parameterised, ties to authenticated `req.session.sub`. No IDOR. Minor: `as any` casts for params and permission checking (D172)**
- [x] Review auth routes — magic link flow, Google OAuth, session refresh, logout — **Magic link flow sound: 32-byte token, SHA-256 hash stored, single-use, 15-min expiry, 5/min rate limit on `/auth/login`. Session refresh correct (30d JWT, 7d refresh window). Logout sets `sessions_invalidated_at` for all-devices semantic. Missing rate limits on `/auth/verify` (D173) and `/auth/verify-email-change` (D174). Email change token stored plaintext with no expiry (D175). Google OAuth bypasses account status check (D178)**
- [x] Review account management — deletion (kind-5 tombstones via relay outbox), status transitions — **Account deletion is fundamentally broken for any user with content: hard-deletes account row but multiple FKs use ON DELETE RESTRICT (D180). Kind-5 tombstones published for articles but not notes (D181). Dead variable in my-account.ts (D182). `feeBps` interpolated into SQL (D183)**
- [x] Review workers: `drive-expiry.ts`, `scheduler.ts`, `subscription-expiry.ts` — error handling, transaction boundaries, retry semantics — **Drive-expiry clean and correct with `withTransaction`. Scheduler `FOR UPDATE SKIP LOCKED` outside transaction provides zero lock protection (D184, confirms D108). Subscription-expiry auto-renewal correct with offer-period handling; renewal can drive balance negative (D186); single failure permanently expires subscription (D187)**
- [x] Verify scheduler's paywalled-draft fix (§1 contortion retirement per relay outbox Phase 5) — **Two-transaction shape correctly implemented: txn 1 inserts article + feed_items + relay outbox (free) or article + feed_items (paywalled); txn 2 swings event ID and enqueues v2 atomically for paywalled. Draft retained on txn 2 failure for retry**
- [x] Check for timing attacks in auth comparison operations — **Google OAuth state uses `timingSafeEqual` (correct). Unsubscribe token uses `timingSafeEqual` (correct). Email change token compared via SQL equality — not directly exploitable but inconsistent. Internal service token comparisons use `!==` (D39, low risk on internal network)**
- [x] Cross-cutting: missing `x-internal-token` header on card-connected call to payment service — silently fails for all card connections (D188)

**ADR reference:** `RELAY-OUTBOX-ADR.md` Phase 5 (scheduler), `ALLHAUS-OMNIBUS.md` (trust-aware auth is future)

**Diagnoses & Remedies (2.1):**

| #    | Diagnosis                                                                                                                                                                                                                                                        | Severity | Remedy                                                                                                                                                   | Effort  |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D170 | ✅ **`optionalAuth` skips account-status and `sessions_invalidated_at` checks.** Suspended/invalidated users treated as authenticated on 29 routes. Stale sessions silently refreshed instead of destroyed                                                       | Medium   | Extract status/invalidation check into shared helper; call from `optionalAuth` when `session?.sub` is truthy. On failure, clear session + destroy cookie | Hours   |
| D171 | ✅ **Incoming `x-reader-id`/`x-writer-id`/`x-reader-pubkey` headers not stripped.** On unauthenticated `optionalAuth` routes, client-supplied identity headers pass through to downstream services                                                               | Medium   | Add `onRequest` hook that deletes these headers from all incoming requests before route handlers                                                         | Minutes |
| D172 | ✅ `as any` casts in `publication-auth.ts` for params and permission checking. `(member as any)[perm]` could match prototype properties                                                                                                                          | Low      | Type the permission parameter as a union of known boolean column names                                                                                   | Minutes |
| D173 | ✅ No rate limiting on `/auth/verify` (magic link verification). Token entropy (256 bits) makes brute-force infeasible but defense-in-depth. Confirms D37                                                                                                        | Medium   | Add `{ config: { rateLimit: { max: 10, timeWindow: '1 minute' } } }`                                                                                     | Minutes |
| D174 | ✅ No rate limiting on `/auth/verify-email-change`. Confirms D38                                                                                                                                                                                                 | Medium   | Add rate limiting                                                                                                                                        | Minutes |
| D175 | ✅ **Email change verification token stored as plaintext with no expiry.** Unlike magic links (SHA-256 hashed, 15-min TTL), email change tokens are raw base64url compared via SQL equality and valid indefinitely                                               | Medium   | Hash before storage (same as magic links), add `email_verification_requested_at` with 24h TTL, rate-limit endpoint                                       | Hours   |
| D176 | ✅ Email change returns 409 "Email already in use" — email enumeration vector (requires authentication)                                                                                                                                                          | Low      | Use generic error message or always return success with email sent                                                                                       | Minutes |
| D177 | Google ID token JWT payload parsed without RSA signature verification. Server-to-server HTTPS mitigates. Confirms D43                                                                                                                                            | Low      | Use `google-auth-library` `verifyIdToken` for full verification                                                                                          | Hours   |
| D178 | ✅ **Google OAuth login bypasses account status check.** `SELECT id FROM accounts WHERE email = $1` has no `AND status = 'active'` filter. Suspended accounts get session cookie. Inconsistent with magic link path                                              | Medium   | Add `AND status = 'active'` to account lookup or check status after lookup                                                                               | Minutes |
| D179 | HMAC state nonce not stored server-side — replay within 10-min window theoretically possible. Google's single-use auth code makes this moot                                                                                                                      | Low      | Document as accepted risk                                                                                                                                | —       |
| D180 | ✅ **Account deletion hard-deletes account row but multiple FK constraints use ON DELETE RESTRICT** (`articles.writer_id`, `notes.author_id`, `read_events`, `vote_charges`, `unlock_records`, etc.). Will throw FK violation for any user with content          | High     | Switch to soft-delete (`status = 'deleted'`), or change FK actions and explicitly clean referencing rows before deletion                                 | Hours   |
| D181 | ✅ Account deletion publishes kind-5 tombstones for articles but not for notes. Notes on the relay persist after account deletion                                                                                                                                | Low      | Add kind-5 events for notes in the deletion loop                                                                                                         | Minutes |
| D182 | ✅ Dead variable `sinceClause` in `my-account.ts:86` — constructed but never used                                                                                                                                                                                | Low      | Remove                                                                                                                                                   | Minutes |
| D183 | ✅ `feeBps` interpolated into SQL via template literal in `my-account.ts:147,268`. Safe (DB-sourced integer) but violates parameterised-query discipline. Confirms D42                                                                                           | Low      | Parameterise with `$n`                                                                                                                                   | Minutes |
| D184 | ✅ **Scheduler `FOR UPDATE SKIP LOCKED` outside transaction.** `pool.query` auto-commits, so lock is acquired and released immediately. Zero protection against double-publish. Advisory lock in `gateway/index.ts` is the actual guard. Confirms D108           | High     | Wrap SELECT + processing in `withTransaction`, or use claim-based pattern with `UPDATE ... SET claimed_by = $1 ... RETURNING *`                          | Hours   |
| D185 | ✅ `(v1 as any).created_at` cast in `scheduler.ts:273` — fragile if key-custody response shape changes                                                                                                                                                           | Low      | Type the signed event response properly                                                                                                                  | Minutes |
| D186 | ✅ **Subscription renewal charges `free_allowance_remaining_pence` with no floor check.** Can drive balance deeply negative. No CHECK constraint on schema                                                                                                       | Medium   | Add `CHECK (free_allowance_remaining_pence >= 0)` or check balance before charging                                                                       | Minutes |
| D187 | Single renewal failure permanently expires subscription with no grace period or retry counter                                                                                                                                                                    | Low      | Add `renewal_attempts` counter; only expire after N failures                                                                                             | Hours   |
| D188 | ✅ **Missing `x-internal-token` header on card-connected call to payment service.** `auth.ts:374-379` — payment service requires `x-internal-token` and returns 403 without it. All card-connection notifications silently fail; provisional reads never convert | Medium   | Add `'X-Internal-Token': process.env.INTERNAL_SERVICE_TOKEN` to fetch headers                                                                            | Minutes |

### Session 2.2: Content routes — articles, notes, drafts, media

**Scope:** `routes/articles/` (7 files), `routes/notes.ts`, `routes/drafts.ts`, `routes/media.ts`, `routes/rss.ts`, `routes/tags.ts`, `services/publication-publisher.ts`

- [x] **Article publish pipeline**: verify `routes/articles/publish.ts` → `publication-publisher.ts` flow; check relay outbox Phase 4 integration (eager-commit shape); verify feed_items dual-write — **Publication publish correctly implements eager-commit (sign → single txn does article + feed_items + enqueueRelayPublish → 201). Personal article indexing intentionally expects client-side relay publish (no enqueue), but scheduler correctly uses eager-commit for scheduled personal articles (D93). Minor pricing edge case for short articles (D107)**
- [x] **Gate-pass orchestration** (`services/article-access/`): review `gate-pass.ts` → `access-check.ts` → `unlock-records.ts` flow; verify price validation, Stripe interaction, unlock idempotency — **Well-structured with clear typed results. Two significant issues: no deleted/unpublished filter on article lookup (D91), and key proxy does not inject reader identity from session (D92)**
- [x] **Article earnings**: verify `routes/articles/earnings.ts` calculation correctness — **Sound**
- [x] **Notes**: verify publish flow, cross-post fan-out (`crossPosts` array shape per feature-debt), external Nostr reply via `enqueueNostrOutbound` — **Cross-post fan-out and external Nostr reply via `enqueueNostrOutbound` correctly implemented. Note deletion signs event before confirming ownership (D109)**
- [x] **Drafts**: verify autosave, scheduled publish, draft→article transition — **Autosave and draft→article transition sound. Scheduler uses `FOR UPDATE SKIP LOCKED` outside a transaction — doesn't prevent double-publish (D108). Draft ID not validated as UUID (D103). Single-draft-per-writer assumption for concurrent new articles (D104)**
- [x] **Media**: verify upload validation (file type, size limits), oEmbed proxy (SSRF risk), Blossom integration — **12MB body limit. D34 (oEmbed raw fetch) confirmed — practical risk low due to provider allowlist (D101). No rate limit on upload (D102)**
- [x] **RSS feed generation**: verify XML output, pagination, caching — **CDATA injection: `]]>` in article content terminates CDATA section, allows XML injection (D100)**
- [x] Check Nostr event signing — `finalizeEvent` / `getPublicKey` usage, key-custody client calls — **All signing correctly uses key-custody-client for custodial user keys. `nostr-publisher.ts` uses `finalizeEvent` only for platform service key (appropriate — not a custodial key). Publication routes correctly pass `'publication'` as signer type**

**ADR reference:** `RELAY-OUTBOX-PHASE-4-ADR.md`, `PUBLICATIONS-SPEC.md`, `EMAIL-ON-PUBLISH-SPEC.md` (unbuilt — note gap)

**Diagnoses & Remedies (2.2):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                                                                   | Severity | Remedy                                                                                                                 | Effort  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------- | ------- |
| D91  | **Gate-pass fetches deleted articles.** `performGatePass` queries `WHERE nostr_event_id = $1` with no `deleted_at IS NULL` or `published_at IS NOT NULL` filter. Reader could be charged for a deleted/unpublished article                                                                                                                                                  | High     | Add `AND deleted_at IS NULL AND published_at IS NOT NULL` to the article lookup                                        | Minutes |
| D92  | **Key proxy does not inject reader identity from session.** `POST /articles/:nostrEventId/key` proxies to key service but does not set `x-reader-id` from `req.session.sub`. Client could set `x-reader-id` to another reader's ID. Key service verifies payment via `article_unlocks` mitigating access theft, but audit trail and encrypted-key targeting are compromised | High     | Inject `req.headers['x-reader-id'] = req.session!.sub!` before `proxyToService`, same pattern as vault routes          | Minutes |
| D93  | **Personal article indexing does not enqueue to relay outbox.** `POST /articles` creates article + feed_items but no `enqueueRelayPublish`. By design (client publishes to relay first), but if client-side relay publish fails, event is lost. Scheduler correctly uses eager-commit for scheduled personal articles                                                       | Medium   | Document design choice explicitly, or migrate to server-side eager-commit (scheduler already demonstrates the pattern) | Hours   |
| D94  | **Publication unpublish does not remove feed_items.** `POST /publications/:id/articles/:articleId/unpublish` sets `published_at = NULL` but does NOT delete feed_items row. Personal unpublish correctly does `DELETE FROM feed_items`. Unpublished publication articles remain visible in feeds                                                                            | High     | Add `DELETE FROM feed_items WHERE article_id = $1` after the article update; wrap both in a transaction                | Minutes |
| D95  | CMS PATCH dual-writes are non-transactional: article UPDATE + feed_items title UPDATE + feed_items media UPDATE run as 3 separate `pool.query` calls. Crash between them leaves inconsistent data                                                                                                                                                                           | Low      | Wrap in `withTransaction`                                                                                              | Minutes |
| D96  | CMS PATCH does not verify article exists — `UPDATE ... WHERE id = $X AND publication_id = $Y` with no `RETURNING` clause or rowcount check. Silently returns `{ ok: true }` for non-existent articles                                                                                                                                                                       | Low      | Add `RETURNING id` and check `rowCount === 0` to return 404                                                            | Minutes |
| D97  | `/content/resolve` returns deleted articles — article lookup has no `deleted_at IS NULL` filter. Leaks metadata (title, summary, author) for deleted articles                                                                                                                                                                                                               | Medium   | Add `AND deleted_at IS NULL AND published_at IS NOT NULL` to the article query                                         | Minutes |
| D98  | **Tab balance can go negative.** `POST /subscriptions/:writerId/convert` does `UPDATE reading_tabs SET balance_pence = balance_pence - $1` with no floor check. No `CHECK (balance_pence >= 0)` on the table                                                                                                                                                                | Medium   | Add `CHECK (balance_pence >= 0)` via migration, or add `WHERE balance_pence >= $1` to UPDATE and check rowcount        | Minutes |
| D99  | **Subscription-convert charge calculation may be wrong.** If `spendPence > subPrice`, charge is 0 but full `spendPence` is already deducted from tab. Reader who spent more than subscription price gets full spend refunded but charged 0 — net gain                                                                                                                       | Medium   | Clamp the credit to `Math.min(spendPence, subPrice)` so reader is never credited more than the subscription costs      | Minutes |
| D100 | **CDATA injection in RSS `content:encoded`.** Article content placed inside `<![CDATA[...]]>` without escaping `]]>`. Content containing `]]>` terminates the CDATA section and allows XML injection                                                                                                                                                                        | Medium   | Replace `]]>` in content with `]]]]><![CDATA[>` (standard CDATA escaping) before interpolating                         | Minutes |
| D101 | oEmbed proxy uses raw `fetch()` — confirming D34. Practical risk is LOW because oEmbed endpoint URLs are hardcoded (YouTube, Vimeo, Twitter, Spotify); user only controls the `url=` query parameter                                                                                                                                                                        | Low      | Migrate to shared SSRF-hardened HTTP client for consistency                                                            | Minutes |
| D102 | No rate limit on media upload (`POST /media/upload`). Authenticated user could exhaust disk space or CPU (Sharp processing) by uploading rapidly                                                                                                                                                                                                                            | Medium   | Add rate limit: `config: { rateLimit: { max: 20, timeWindow: '1 minute' } }` or per-user daily cap                     | Minutes |
| D103 | Draft ID parameter not validated as UUID — PostgreSQL rejects non-UUID cast as 500 instead of 400. Other routes (manage.ts, tags.ts) validate with `UUID_RE`                                                                                                                                                                                                                | Low      | Add UUID validation on `:id` parameter for all draft routes                                                            | Minutes |
| D104 | Single-draft-per-writer assumption for new drafts: `POST /drafts` without dTag finds most recent dTag-less draft and updates it. Two concurrent new articles in separate tabs would overwrite each other                                                                                                                                                                    | Low      | Use draft ID cookie/parameter from client to disambiguate concurrent new drafts                                        | Hours   |
| D105 | `GET /articles/deleted` has no cap on pubkeys array size — request with thousands of pubkeys causes slow `ANY($1)` scan                                                                                                                                                                                                                                                     | Low      | Cap array at 200; return 400 if exceeded                                                                               | Minutes |
| D106 | CMS list route interpolates `limit`/`offset` via template literal. Values from `parseInt()` are always numbers or NaN; NaN causes Postgres syntax error (500). Not SQLi but a robustness issue. Confirms D41                                                                                                                                                                | Low      | Default NaN to 50/0: `const limit = Math.min(parseInt(...) \|\| 50, 100)`. Or pass as parameterised values             | Minutes |
| D107 | `resolveDefaultPrice` for per-1000-word pricing: `Math.floor(wordCount / 1000) * price`. A 500-word article gets price 0, making it free even though writer intended it paywalled                                                                                                                                                                                           | Low      | Use `Math.max(1, Math.ceil(wordCount / 1000)) * price` or a minimum price floor                                        | Minutes |
| D108 | **Scheduler uses `FOR UPDATE SKIP LOCKED` outside a transaction.** `pool.query` (auto-commit) releases lock immediately after SELECT — no protection against double-publish between select and `DELETE FROM article_drafts`                                                                                                                                                 | Medium   | Wrap fetch + per-draft publish + delete in `withTransaction`, or use advisory locking                                  | Hours   |
| D109 | Note deletion signs event (HTTP call to key-custody) before confirming ownership. If note doesn't belong to user, signing call is wasted and a deletion event is signed for a note the user doesn't own (never enqueued)                                                                                                                                                    | Low      | Move ownership check before signing call                                                                               | Minutes |
| D110 | Publication soft-delete calls `signEvent` (HTTP to key-custody) inside `withTransaction`. If HTTP call hangs, transaction holds row locks indefinitely. Publication-publisher correctly moves signing outside the transaction                                                                                                                                               | Medium   | Move `signEvent` before the `withTransaction` block, same pattern as personal article deletion                         | Minutes |

### Session 2.3: Social routes — follows, subscriptions, trust, votes, replies

**Scope:** `routes/follows.ts`, `routes/subscriptions/` (7 files), `routes/trust.ts`, `routes/votes.ts`, `routes/replies.ts`, `routes/social.ts`, `routes/bookmarks.ts`, `routes/notifications.ts`, `routes/history.ts`, `routes/reading-positions.ts`

- [x] **Subscriptions**: verify create/cancel/renew lifecycle; check for auto-renewal gap (feature-debt item #1 — single most critical missing feature); review all 7 subscription route files for consistency — **Auto-renewal fully implemented in subscription-expiry worker. Writer subscription path is well-structured with transactions and financial operations. Publication subscription path has significant gaps (D111). Comp path has minor issues (D112, D113)**
- [x] **Trust routes**: verify Layer 1 + Layer 2 epoch scores + Layer 4 relational data assembly; verify vouch CRUD (upsert, withdrawal, dimension/visibility rules); check pre-first-epoch fallback path — **All layers correctly assembled. Vouch CRUD with dimension validation, visibility rules, self-vouch prevention all correct. Layer 4 relational query has wrong column name (D114 — breaks the feature entirely)**
- [x] **Votes**: verify vote recording, count aggregation, user-vote lookup; check for race conditions — **Solid. Advisory locks serialise concurrent votes. Tally upsert is atomic. Self-vote prevention. Free allowance + tab billing correct**
- [x] **Replies**: verify threaded reply tree construction; check `parentCommentId` integrity; verify trust pip enrichment via `trust_layer1` join — **Correct. Nested tree returned from API, frontend flattens to playscript. Block/mute filtering, soft-delete, trust pip enrichment all present. Minor: `pipStatus` type missing `'contested'` state (D115)**
- [x] **Reading positions**: verify upsert, retrieval, preference management; check for data leakage across users — **Clean. All queries filter by authenticated user ID. Zod validation on scroll ratio [0,1]. No cross-user leakage**
- [x] **Notifications**: verify type routing, mark-read, unread-count; confirm `dm_payment_required` and `new_user` phantom types are fully excised — **Well-structured. `new_message` excluded from general notifications. No phantom types found. Cursor pagination correct**
- [x] **Follows/social**: verify follow/unfollow, follower/following lists, social graph queries — **Follow/unfollow idempotent via ON CONFLICT. Self-follow prevention. Blocks/mutes have wrong avatar column name (D116 — breaks both endpoints)**

**ADR reference:** `ALLHAUS-OMNIBUS.md` (trust layers), `ALLHAUS-REDESIGN-SPEC.md` §4 (playscript threads), `SUBSCRIPTIONS-GAP-ANALYSIS.md`

**Diagnoses & Remedies (2.3):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                  | Severity | Remedy                                                                                                                                           | Effort  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| D111 | ✅ **Publication subscription has no transaction, no financial operations, no relay outbox.** `publication.ts` uses `pool.query` without `withTransaction`. Does not deduct `free_allowance_remaining_pence`, log subscription charge, or sign Nostr attestation. TOCTOU race on duplicate check yields 500 instead of 409 | Medium   | Wrap in `withTransaction`, add financial operations + relay outbox. Mirror writer subscription path structure. Use `ON CONFLICT` for idempotency | Hours   |
| D112 | ✅ Comp subscription grant path uses `pool.query` without transaction. Check-then-insert race mitigated by unique index but produces 500 on concurrent requests                                                                                                                                                            | Low      | Wrap in `withTransaction` or use `INSERT ... ON CONFLICT`                                                                                        | Minutes |
| D113 | ✅ Comp notification sent to wrong recipient: `recipient_id = writerId, actor_id = readerId` with type `new_subscriber`. Writer already knows they granted the comp; the reader should receive the notification                                                                                                            | Low      | Swap recipient and actor IDs                                                                                                                     | Minutes |
| D114 | ✅ **Layer 4 relational query uses `followed_id` (non-existent column) — should be `followee_id`.** `trust.ts:151` — breaks the "N writers you follow endorse this person" feature entirely with a runtime SQL error                                                                                                       | High     | Change `followed_id` to `followee_id`                                                                                                            | Minutes |
| D115 | ✅ `pipStatus` TypeScript type in `replies.ts:247,283` missing `'contested'` state from migration 079. Runtime value passes through but type annotation misleads frontend consumers                                                                                                                                        | Low      | Add `'contested'` to the union type                                                                                                              | Minutes |
| D116 | ✅ **Blocks and mutes list queries use `a.avatar` — column doesn't exist (should be `a.avatar_blossom_url`).** `social.ts:34,112` — breaks `GET /my/blocks` and `GET /my/mutes` with SQL errors                                                                                                                            | Medium   | Change `a.avatar` to `a.avatar_blossom_url` in both queries                                                                                      | Minutes |

### Session 2.4: External feeds, resolver, linked accounts, timeline

**Scope:** `routes/external-feeds.ts`, `routes/timeline.ts`, `routes/resolve.ts`, `routes/linked-accounts.ts`, `routes/signing.ts`, `routes/search.ts`, `lib/resolver.ts`, `lib/activitypub-resolve.ts`, `lib/atproto-resolve.ts`, `lib/outbound-enqueue.ts`, `lib/nostr-publisher.ts`

- [x] **Timeline**: verify single-table `feed_items` scan with LEFT JOINs; check cursor correctness (`published_at, id` compound); verify per-source cap windowing; verify explore vs following branching — **Single-table scan correct. Compound cursor correct for following; explore uses 3-column `(score, published_at, id)`. Per-source cap via `ROW_NUMBER() PARTITION BY source_id` in CTE. Block/mute exclusions present on both branches. Missing `fi.title` in FEED_SELECT (D150 — articles return undefined title)**
- [x] **Resolver**: verify pattern classification (URL, handle, email, npub, DID, dotted_host); verify Phase B async polling with `resolver_async_results` persistence; verify context filtering (`subscribe`, `invite`, `dm`, `general`); verify `bsky.social`/`bsky.team` restriction on `bluesky_handle`; verify well-known RSS path probing + memoisation — **Thorough implementation. Classification is deterministic across all input types. Phase B async polling with initiator-bound rows, 60s TTL, 100-row cap. Context filtering correctly skips external chains for `invite`/`dm`. Bluesky handle restriction correct. Well-known RSS probing parallel with 5-min TTL, 1000-entry FIFO cache (D153). Nostr relay hints have no cap (D155)**
- [x] **External feeds CRUD**: verify subscribe/list/remove/mute/refresh; verify protocol dispatch (RSS, Nostr, atproto, activitypub); verify SSRF protections on user-supplied URLs — **Subscribe/list/remove/mute/refresh correct. Protocol dispatch covers all four. Advisory lock for race-safe subscription cap. Refresh rate-limited (6/min). Subscribe endpoint lacks rate limiting (D156). RSS URLs not SSRF-checked at subscribe time (D157, defense-in-depth). `dailyCap` PATCH has no range validation (D158)**
- [x] **Linked accounts**: verify Mastodon OAuth flow; verify AT Protocol OAuth flow (authorize URL → callback → session storage); verify encryption of OAuth credentials — **Mastodon flow well-implemented: signed state cookie, SSRF-safe `safeFetch`, encrypted credential storage. AT Protocol flow uses `@atproto/oauth-client-node` with DB-backed stores. Bluesky callback uses cookie userId instead of session (D159 — cross-user link risk)**
- [x] **Search**: verify pg_trgm usage, query sanitisation (ILIKE special chars), result ranking — **`similarity()` for fuzzy matching. ILIKE special chars properly escaped via `escapeLike`. Parameterised queries. Rate limited 30/min. Missing trigram index on `content_free` (D161). Offset has no upper bound (D163)**
- [x] **Signing**: verify `POST /sign-and-publish` semantic change ("signed and durably queued" per relay outbox Phase 3) — **Correct. Signs via key-custody, enqueues to `relay_outbox` via `enqueueRelayPublish` inside `withTransaction`. `publishToRelay`/`publishToRelayUrl` deleted. Clean**

**ADR reference:** `UNIVERSAL-FEED-ADR.md` (all phases), `RELAY-OUTBOX-ADR.md` Phase 3

**Diagnoses & Remedies (2.4):**

| #    | Diagnosis                                                                                                                                                                                                                                                               | Severity | Remedy                                                                                              | Effort  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------- | ------- |
| D150 | ✅ **Missing `fi.title` in FEED_SELECT.** `timeline.ts:131-161` — neither `fi.title` nor `a.title` is selected; `feedItemToResponse` reads `row.title` (undefined). Every article in the feed response has `title: undefined`                                           | High     | Add `fi.title,` to the FEED_SELECT (or `COALESCE(a.title, fi.title) AS title`)                      | Minutes |
| D151 | Explore feed 3-column cursor `(score, published_at, id)` lacks matching 3-column index. Existing `idx_feed_items_score` covers only `(score DESC, published_at DESC)`                                                                                                   | Low      | Extend index to include `id DESC` when explore performance degrades                                 | Minutes |
| D152 | ✅ `capped_external` CTE materialises all external `feed_items` for user's subscriptions regardless of cursor position — no date floor filter                                                                                                                           | Low      | Add `published_at > now() - INTERVAL '30 days'` inside the CTE to bound the window                  | Minutes |
| D153 | ✅ Well-known RSS cache eviction is FIFO not LRU — popular origins evicted early while rare origins persist                                                                                                                                                             | Low      | Use `lru-cache` package or update entry position on cache hit                                       | Minutes |
| D155 | ✅ **No cap on relay hints in nprofile input.** `resolver.ts:784-903` — `fetchNostrProfile` opens a WebSocket to each relay in the hints array. Malicious nprofile with many hints spawns unbounded concurrent connections                                              | Medium   | Cap relay hints to 5 before passing to `fetchNostrProfile`                                          | Minutes |
| D156 | ✅ **Subscribe endpoint lacks rate limiting.** `external-feeds.ts:102` — per-user subscription cap limits total but rapid create/delete cycles trigger fetch jobs against arbitrary external endpoints                                                                  | Medium   | Add `{ config: { rateLimit: { max: 10, timeWindow: '1 minute' } } }` to subscribe route             | Minutes |
| D158 | ✅ `dailyCap` in PATCH has no type or range validation — accepts negative, zero, or very large integers                                                                                                                                                                 | Medium   | Validate as positive integer between 1 and 1000                                                     | Minutes |
| D159 | ✅ **Bluesky callback uses `statePayload.userId` (from cookie) instead of `req.session!.sub!`.** `linked-accounts.ts:429` — if user A starts OAuth, logs out, and user B completes callback, B links the Bluesky account to A. Mastodon callback correctly uses session | Medium   | Use `req.session!.sub!` (like Mastodon path) or verify `statePayload.userId === req.session!.sub!`  | Minutes |
| D161 | ✅ No trigram index on `articles.content_free` — `ILIKE` fallback in search causes sequential scan                                                                                                                                                                      | Medium   | Add `CREATE INDEX idx_articles_content_free_trgm ON articles USING gin (content_free gin_trgm_ops)` | Minutes |
| D163 | ✅ `offset` parameter has no upper bound — very large offset forces expensive DB scan                                                                                                                                                                                   | Low      | Cap at 1000 or switch to cursor-based pagination                                                    | Minutes |

---

## Phase 3 — Backend Services (Other)

### Session 3.1: Payment service

**Scope:** 10 .ts files, 3 test files. Handles real money.

- [x] **Accrual** (`services/accrual.ts`): verify tab accumulation logic, PaymentIntent lifecycle, `publishReceiptAsync` sign-then-enqueue shape (relay outbox Phase 3) — **`recordGatePass` runs inside a single txn with FOR UPDATE on reader row. Tab balance only updates for accrued (card-present) reads. Receipt publishing is fire-and-forget via relay outbox. One race: `publishReceiptAsync` starts its own txn before parent commits (D82)**
- [x] **Settlement** (`services/settlement.ts`): verify settlement trigger, amount calculation, Stripe charge execution — **Two triggers (threshold at 800p, monthly fallback at 200p after 30d inactive). FIX #13 correctly leaves tab balance intact until webhook confirms. HOWEVER: Stripe call is inside DB transaction with random idempotency key (D76+D77) — same bug class that fix-programme already corrected for payouts**
- [x] **Payout** (`services/payout.ts`, `workers/payout.ts`): verify three-phase reserve→Stripe→complete (fix-programme §3/§4); verify `resumePendingWriterPayouts` / `resumePendingPublicationPayouts` idempotency; verify stable idempotency keys — **Sound. Three-phase pattern correctly implemented. Stable idempotency keys (`payout-${payoutId}`, `pub-split-${payoutId}-${accountId}`). Resume functions called at start of each cycle. Integer arithmetic throughout, `Math.floor` everywhere**
- [x] **Webhooks** (`routes/webhook.ts`): verify Stripe signature validation; verify dedup via `processed_at` (migration 071); verify event type routing — **Stripe signature verification present. Dedup via `stripe_webhook_events` with nullable `processed_at` handles crash-between-receipt-and-completion. All 5 relevant event types routed. No test coverage (D86)**
- [x] **Nostr** (`lib/nostr.ts`): verify `signReceiptEvent` (renamed from `publishReceiptEvent` per relay outbox); verify it no longer has its own WS publisher — **Correctly renamed. Returns `SignedNostrEvent` for caller to enqueue via `enqueueRelayPublish`. No WS publisher in payment service**
- [x] Run all 3 payment tests; verify they cover the critical paths — **41/41 tests pass (987ms). Tests cover `classifyRead` pure function (13), `shouldTriggerSettlement` logic (12), `computePublicationSplits` (16). Pure business logic well-tested**
- [x] Identify missing test coverage: webhook handler, accrual edge cases, multi-currency scenarios — **No test coverage for: webhook handler (5 event branches), `recordGatePass` integration flow, concurrent settlement race, multi-currency. Webhook handler is highest-priority gap (D86)**

**ADR reference:** `platform-pub-currency-strategy.md`, `RELAY-OUTBOX-ADR.md` Phase 3

**Diagnoses & Remedies (3.1):**

| #   | Diagnosis                                                                                                                                                                                                                                                                                                                                 | Severity | Remedy                                                                                                                                                                                                              | Effort  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D76 | **Stripe call inside DB transaction (settlement)**: `executeSettlement` calls `stripe.paymentIntents.create` inside `withTransaction`. If Stripe succeeds but COMMIT fails, PaymentIntent is orphaned — reader charged with no audit trail. Payout code correctly avoids this with three-phase pattern, but settlement was not refactored | High     | Refactor to three-phase pattern: (1) Txn 1: lock tab, insert `tab_settlements` as 'pending'; (2) Stripe call outside txn; (3) Txn 2: update with `stripe_payment_intent_id`. Add resume function for crash recovery | Hours   |
| D77 | **Settlement idempotency key is `randomUUID()`**: retry after crash generates fresh key → duplicate Stripe charge. Payout code correctly uses stable keys (`payout-${payoutId}`). Compounds D76                                                                                                                                           | High     | Use `settlement-${settlementId}` (requires D76 fix so settlement row exists before Stripe call)                                                                                                                     | Minutes |
| D78 | Zero-amount settlement possible: concurrent settlement could drain balance to 0, causing Stripe `amount: 0` call (rejected for GBP <30p). Error is caught but creates noise                                                                                                                                                               | Medium   | Add `if (actualAmount < 30) return null` after the re-check (matches Stripe's minimum charge for GBP)                                                                                                               | Minutes |
| D79 | Double settlement race window: `checkAndSettle` reads balance outside lock, two concurrent calls both pass unlocked check, both enter `initiateSettlement` and create separate PaymentIntents                                                                                                                                             | Medium   | Check `tab_settlements` for existing row with same `tab_id` and no `stripe_charge_id` (settlement in progress). Or add `active_settlement_id` to `reading_tabs`                                                     | Hours   |
| D80 | Missing startup validation for `STRIPE_WEBHOOK_SECRET`, `INTERNAL_SERVICE_TOKEN`, `PLATFORM_SERVICE_PRIVKEY` — only `STRIPE_SECRET_KEY` and `DATABASE_URL` are validated                                                                                                                                                                  | Medium   | Add `requireEnv()` calls for all three in `index.ts`                                                                                                                                                                | Minutes |
| D81 | Internal service token uses `!==` not `timingSafeEqual` (payment.ts lines 44, 79, 149, 167). Same issue as D39                                                                                                                                                                                                                            | Low      | Replace with `timingSafeEqual` + length guard. Create shared `verifyInternalToken()` helper                                                                                                                         | Minutes |
| D82 | `publishReceiptAsync` races with parent transaction: called inside `withTransaction` callback but not awaited (fire-and-forget). Starts its own txn for relay_outbox INSERT — relay row may reference `read_events.id` not yet visible                                                                                                    | Low      | Move `publishReceiptAsync` call after `withTransaction` returns (after parent commits). `readEvent.id` is available in the return value                                                                             | Minutes |
| D83 | No cleanup for `stripe_webhook_events` table: migration 041 says "keep 90 days" but no cron/task actually deletes old rows                                                                                                                                                                                                                | Low      | Add daily cron: `DELETE FROM stripe_webhook_events WHERE processed_at < now() - INTERVAL '90 days'`                                                                                                                 | Minutes |
| D84 | `computePublicationSplits` can drive `remainingPool` negative if sum of article-revenue share payouts exceeds pool (e.g., two 60% shares on same article)                                                                                                                                                                                 | Low      | Clamp: `payout = Math.min(payout, Math.max(0, remainingPool))` before deducting. Or enforce business rule that per-article revenue_bps cannot exceed 10000 total                                                    | Minutes |
| D85 | Missing `payment_method` in off-session PaymentIntent: Stripe may fail with `payment_intent_requires_action` if customer has multiple cards and no default set                                                                                                                                                                            | Low      | Fetch default payment method before creating PI, or set default when card is first connected in `/card-connected` route                                                                                             | Hours   |
| D86 | No test coverage for webhook handler (`handleStripeEvent` and 5 event type branches) — the critical entry point for all financial state transitions                                                                                                                                                                                       | Medium   | Write vitest tests mocking Stripe signature verification. Cover: `payment_intent.succeeded`, `payment_intent.payment_failed`, `transfer.paid/failed`, `account.updated`, dedup, unknown event                       | Hours   |
| D87 | No test coverage for `recordGatePass` integration flow — transaction, tab balance update, concurrent reads, receipt async failure                                                                                                                                                                                                         | Medium   | Write integration tests covering: accrued read updates tab balance, provisional read debits allowance, concurrent gate passes don't lose updates                                                                    | Hours   |
| D88 | All amounts hardcoded to `'gbp'`/`'GBP'`. No validation that `amountPence` is within Stripe's allowed range (minimum 30p for GBP PaymentIntents)                                                                                                                                                                                          | Low      | Add `if (amountPence < 30) throw` in `executeSettlement`. Document single-currency assumption                                                                                                                       | Minutes |
| D89 | `vote_charges.amount_pence` is BIGINT, all other monetary columns are INT. TypeScript uses `parseInt` on SUM results. Works but schema inconsistency suggests oversight                                                                                                                                                                   | Low      | Align all monetary columns to same type (INT sufficient at current scale; BIGINT for SUM aggregates is safer convention)                                                                                            | Minutes |
| D90 | Settlement test `TestableSettlementService` hardcodes `30 * 24 * 60 * 60 * 1000` instead of using `config.monthlyFallbackDays` — would not catch bug where config value is ignored                                                                                                                                                        | Low      | Update to use `config.monthlyFallbackDays`; add test case with non-default value (e.g., 14 days)                                                                                                                    | Minutes |

### Session 3.2: Key service + Key custody

**Scope:** key-service (9 files, 1 test), key-custody (4 files, 2 tests). Security-critical services.

- [x] **Key custody** (`routes/keypairs.ts`, `lib/crypto.ts`): verify custodial key generation, storage, retrieval; verify NIP-44 signing operations; verify access control (which services can request signing) — **Sound overall. `generateKeypair` uses nostr-tools `generateSecretKey` (32 random bytes). Private keys encrypted at rest with AES-256-GCM using ACCOUNT_KEY_HEX (random 12-byte IV per encrypt). `signEvent` decrypts privkey, signs with `finalizeEvent`, then zeroes the privkey buffer (`fill(0)`) — good hygiene. `unwrapKey` uses NIP-44 conversation key derivation with platform service pubkey as counterparty. `nip44Encrypt`/`nip44Decrypt`/`nip44EncryptBatch` accept arbitrary counterparty pubkeys for DM E2E. Batch variant decrypts privkey once for N recipients. All 6 endpoints gated by `requireInternalSecret` preHandler. Zod validation on all request bodies. `signerType` enum controls table lookup (`accounts` vs `publications`) — enum is validated by Zod so the `FROM ${table}` interpolation is safe. One issue: INTERNAL_SECRET comparison uses `!==` not `timingSafeEqual` (D39)**
- [x] **Key service** (`routes/keys.ts`, `services/vault.ts`, `services/verification.ts`, `lib/crypto.ts`, `lib/nip44.ts`, `lib/kms.ts`): verify vault encryption/decryption; verify NIP-44 encrypted key issuance; verify key verification — **Sound. Vault publish: generates 32-byte random content key via `randomBytes(32)`, envelope-encrypts with KMS master key (AES-256-GCM), encrypts article body with XChaCha20-Poly1305 (24-byte random nonce). On re-publish: reuses existing content key (readers keep access). Key issuance (`issueKey`): resolves article by stable article_id (not event ID), verifies access (own content → always, permanent unlock → article_unlocks, paid → read_events with valid state), decrypts content key from KMS envelope, wraps with NIP-44 to reader pubkey, logs issuance (first vs re-issuance). Writer export: wraps all vault keys to writer's own pubkey for data portability. Rate limiting: 10 req/min per reader on key issuance endpoint. Payment verification checks 4 valid states (provisional, accrued, platform_settled, writer_paid). Reader pubkey validated with `/^[0-9a-f]{64}$/` regex. `x-internal-secret` guards export endpoint (same `!==` timing issue as D39). All SQL parameterised**
- [x] **Crypto review**: verify key derivation, IV handling, authenticated encryption, constant-time comparisons — **Three independent crypto implementations, all sound: (1) `shared/src/lib/crypto.ts` — AES-256-GCM for OAuth credentials, random IV, versioned ciphertext for key rotation. (2) `key-custody/src/lib/crypto.ts` — AES-256-GCM for Nostr private keys at rest, random IV, auth tag verified. (3) `key-service/src/lib/crypto.ts` — XChaCha20-Poly1305 for article body encryption (24-byte random nonce eliminates nonce-reuse risk), legacy AES-256-GCM for backward compat. (4) `key-service/src/lib/kms.ts` — AES-256-GCM envelope encryption for content keys. All use `randomBytes` for IV/nonce generation. Auth tags set and verified. No hardcoded IVs. Constant-time: Node crypto internals handle auth tag comparison; explicit `timingSafeEqual` used in unsubscribe token verification and Google OAuth state. Internal service secret comparisons use `!==` (D39)**
- [x] **Inter-service communication**: verify how gateway calls key-custody (`lib/key-custody-client.ts`); check for credential exposure in logs or errors — **Gateway→key-custody via internal HTTP with `X-Internal-Secret` header. Key-custody URL from `KEY_CUSTODY_URL` env var. Error responses are opaque ("key-custody /path failed: status") with upstream error object attached but not logged to client. Secret transmitted per-request in header (not in URL or body). No privkey material in logs — key-custody logs signerId/signerType/eventKind but never key bytes. Gateway client functions (`generateKeypair`, `signEvent`, `unwrapKey`, `nip44EncryptBatch`, `nip44Decrypt`) are thin wrappers that pass UUIDs and event templates, never raw keys**
- [x] Run all tests (1 key-service + 2 key-custody); assess coverage adequacy — **All pass: key-service 11/11, key-custody 19/19 (crypto round-trips + Zod schema validation). Coverage is adequate for crypto primitives but does NOT cover: vault publish/re-publish flow, key issuance with payment verification, rate limiting behavior, writer export, NIP-44 wrap/unwrap end-to-end with real keys**
- [x] Check for hardcoded keys, seeds, or secrets — **None found. All keys loaded from env vars: `ACCOUNT_KEY_HEX`, `KMS_MASTER_KEY_HEX`, `PLATFORM_SERVICE_PRIVKEY`, `INTERNAL_SECRET`. Startup validation via `requireEnv`/`requireEnvMinLength`. `.env` files are gitignored**

**Diagnoses & Remedies (3.2):**

| #   | Diagnosis                                                                                                                                                                                                                                                                                                         | Severity | Remedy                                                                                                                               | Effort  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| D39 | _(repeated from 8.1)_ Internal secret comparison uses `!==` in both key-custody and key-service                                                                                                                                                                                                                   | Low      | Replace with `timingSafeEqual` + length guard. Low risk since services are on internal Docker network                                | Minutes |
| D50 | `key-service/src/types/index.ts` has `@deprecated` JSDoc on `nostrVaultEvent` field but the field is still constructed in `vault.ts:107-117`                                                                                                                                                                      | Low      | Remove the deprecated vault event construction from `publishArticle` once all callers confirm they use ciphertext+algorithm directly | Hours   |
| D51 | No integration test for the vault publish → key issuance → NIP-44 unwrap end-to-end flow                                                                                                                                                                                                                          | Medium   | Write a vitest integration test covering: publish paywalled article → simulate payment → issue key → unwrap → verify decryption      | Hours   |
| D52 | `ACCOUNT_KEY_HEX` validation at startup uses `requireEnvMinLength('ACCOUNT_KEY_HEX', 32)` — checks string length (32 chars = 16 bytes), not the required 64 hex chars (32 bytes). The crypto code separately validates `key.length !== 32` on the decoded buffer, so this is defense-in-depth not a vulnerability | Low      | Change to `requireEnvMinLength('ACCOUNT_KEY_HEX', 64)` for clearer startup validation                                                | Minutes |

### Session 3.3: Feed ingest service

**Scope:** 40 .ts files, 4 test files. Background worker service — no HTTP, Graphile Worker tasks only.

- [x] **RSS adapter** (`adapters/rss.ts`, `tasks/feed-ingest-rss.ts`): verify fetch, parse, normalise, dual-write pipeline; verify SSRF protection via shared HTTP client; verify `fetch_interval_seconds` reset on recovery (C3) — **Pipeline sound: safeFetch with conditional GET → rss-parser.parseString → per-item withTransaction dual-write (ON CONFLICT DO NOTHING). SSRF via shared hardened client. Recovery resets to 300s on success. Exponential backoff (300×2^n, cap 6) on error, deactivation after 10 failures. Per-host (2) and global (10) concurrency caps. Sanitisation via strict allowlist at ingest time. One dead code path: try/catch around new Date() never fires (D200)**
- [x] **Nostr adapter** (`tasks/feed-ingest-nostr.ts`): verify WebSocket handling, kind 1/30023/5 filtering, NIP-19 encoding, relay connection lifecycle; verify SSRF check for user-supplied relay URLs (S2) — **Temp WebSocket per relay, 10s timeout, partial events on timeout (resilient). Schnorr sig verification + pubkey match + future timestamp rejection on every event. SSRF via pinnedWebSocketOptions. NIP-19 naddr/nevent encoding correct. Kind 5 deletion handles both 'e' tag and 'a' tag forms with pubkey match guard. Relay URLs capped at 10 at subscribe time. Profile metadata ratchet prevents stale overwrites. One issue: kind 5 deletion pairs not wrapped in transaction (D201)**
- [x] **Bluesky adapter** (`adapters/atproto.ts`, `jetstream/listener.ts`, `tasks/feed-ingest-atproto-backfill.ts`, `lib/atproto-ingest.ts`): verify Jetstream WebSocket (leader election via `pg_try_advisory_lock`, DID set refresh, cursor resume); verify backfill pagination; verify facets→HTML conversion — **Leader election via session-scoped advisory lock correct. DID refresh via setTimeout (no stacking), wildcard optimisation at 150 DIDs. Cursor resume from oldest time_us with BigInt, GREATEST guard. SSRF on WS via pinnedWebSocketOptions. Backfill pages getAuthorFeed (5 pages × 100, 24h lookback). Facets→HTML via @atproto/api RichText + escapeHtml + sanitizeContent defense-in-depth. Dual-write transactional with ON CONFLICT DO NOTHING. No WS ping/pong watchdog (D202). Cursor UPDATE outside transaction (D203)**
- [x] **ActivityPub adapter** (`adapters/activitypub.ts`, `adapters/activitypub-outbound.ts`, `tasks/feed-ingest-activitypub.ts`, `lib/activitypub-ingest.ts`): verify outbox polling, visibility filtering (public only), cursor management; verify outbound posting (`/api/v1/statuses` + Idempotency-Key) — **Outbox polling correct: safeFetch actor doc → discover outbox URL → page newest-first with cursor dedup. Visibility: checks both activity and note for AS Public URI. Cursor anchored only to accepted activities. Outbound: /api/v1/statuses with Idempotency-Key, federated reply via /api/v2/search?resolve=true, graceful degradation to top-level post on failure. All external fetches via safeFetch. activitypub_instance_health counters correct. Boosts correctly skipped (type !== 'Create'). One note: only canonical Public URI matched, not as:Public or bare Public (D204)**
- [x] **Outbound cross-posting** (`tasks/outbound-cross-post.ts`, `adapters/atproto-outbound.ts`, `adapters/nostr-outbound.ts`): verify protocol dispatch, retry with versioned jobKey (§72), error handling — **Dispatch clean: activitypub/atproto/nostr*external with protocol-specific precondition guards. Retry with versioned jobKey (outbound_cross_post*{id}\_r{n}), exponential backoff (30s × 2^n), bounded by per-row max_retries (default 3). atproto: OAuthSession.fetchHandler with DPoP, Intl.Segmenter truncation, reply strong-refs + quote embeds correct. nostr: pre-signed events only, SSRF-hardened WS, all-or-nothing semantics with at-least-one-accepts rule. Credentials: Mastodon AES-256-GCM encrypted, atproto DB-backed session stores, Nostr keys never leave key-custody. Sound**
- [x] **Relay outbox worker** (`tasks/relay-publish.ts`): verify state machine (pending→sent, pending→failed→retry→sent/abandoned); verify advisory lock; verify backoff calculation; review the 10 test cases — **State machine complete with no missing transitions. Advisory lock via pg_try_advisory_xact_lock on entity_type:entity_id, graceful fallback to redrive cron. Backoff: 2^n minutes with 1h cap and ±10% jitter. 10 tests cover all branches: success, failure+retry, abandon, already-sent, row-vanished, lock contention, no-relay-URLs, deletion-path retry. Relay URL resolution: row-specific → env fallback. publishNostrToRelays uses one-accepts rule. Well-designed**
- [x] **Trust tasks** (`tasks/trust-layer1-refresh.ts`, `tasks/trust-epoch-aggregate.ts`): verify Layer 1 signal computation; verify epoch aggregation (quarterly full + Mon/Thu mop-up); verify attestor weighting formula — **L1 signals: account_age_days, paying_reader_count, article_count, payment_verified, encounter_count all computed; nip05_verified hardcoded FALSE (stub). subscriber_count and continuous_activity confirmed missing (D24). Epoch: quarterly full increments decay + scores all; Mon/Thu mop-up uses small-subject rule (<10) + threshold gate (>=5 changes). Attestor weight: age × payment × readership × activity — matches ADR §II.8. Freshness decay 0→1.0 to 6→0.0 with graduated small-scale protection. Dry-run correct (skips writes, logs top-20 diffs). Batch INSERT can exceed PG 65535 param limit at >3,276 subjects (D205). Double full-epoch risk on quarterly Mon/Thu (D206)**
- [x] **Trust libs** (`lib/trust-weighting.ts`, `lib/trust-aggregation.ts`, `lib/trust-pip.ts`): review the 42 unit tests; verify weighting formula matches ADR §II.8 — **Actually 61 tests (not 42 — count grew with Slices 17-18). Weighting: 11 tests cover caps, zeros, linear scaling, compound. Aggregation: 31 tests cover freshness table, decay rate bands, effective freshness, dimension scoring with weighted/stale/expired/zero-weight vouches. Pip: 19 tests cover all four states, encounter paths, contested override, boundary conditions. Gaps: no integration test for epoch aggregate SQL, no test for expired+small-scale interaction, no negative input tests for attestorWeight. All 61 pass**
- [x] **Cron tasks**: verify all recurring jobs (`feed_items_reconcile`, `feed_items_author_refresh`, `feed_scores_refresh`, `resolver_results_prune`, `atproto_oauth_states_prune`, `relay_outbox_redrive`, `relay_outbox_reconcile`, `outbound_token_refresh`, `activitypub_instance_health_prune`, `external_items_prune`, `external_sources_gc`, `source_metadata_refresh`) — **16 cron entries total, well-staggered 01:00–07:00 UTC daily. Lightweight crons (resolver prune, oauth prune, redrive, reconcile) are clean. Heavy crons (reconcile, author refresh, scores refresh) use unbounded queries with no batch limit but are acceptable at current scale. feed_scores_refresh (every 5m) has no overlap guard and uses NOT IN with nullable column (D207). external_items_prune has dead interaction guards — both bookmark and vote subqueries are non-functional (D208). source_metadata_refresh fetches all protocols but only processes RSS (D209). Trust epoch + external_items_prune collide at 02:00 on Mon/Thu (D210)**
- [x] **Sanitisation** (`lib/sanitize.ts`): verify HTML sanitisation rules for all protocols — **Strict allowlist: p/br/a/em/strong/code/pre/blockquote/ul/ol/li/img. Only href/rel on a, src/alt on img. Schemes: http/https only. All four protocols route through sanitizeContent. Nostr stores plaintext only (correct). Bluesky double-escapes (escapeHtml + sanitizeContent). No allowProtocolRelative:false — protocol-relative URLs pass through (D211). No input size limit (D212)**

**ADR reference:** `UNIVERSAL-FEED-ADR.md` (all phases), `ALLHAUS-OMNIBUS.md` (trust), `RELAY-OUTBOX-ADR.md` (worker)

**Diagnoses & Remedies (3.3):**

| #    | Diagnosis                                                                                                                                                                                                                                                                    | Severity | Remedy                                                                                                                                               | Effort  |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D200 | RSS adapter `rss.ts:106-111`: `try/catch` around `new Date()` is dead code — `Date` constructor never throws on invalid strings, returns `Invalid Date` instead. An item with unparseable date gets NaN timestamp that fails on Postgres INSERT                              | Low      | Replace try/catch with `if (isNaN(publishedAt.getTime())) publishedAt = new Date()` fallback                                                         | Minutes |
| D201 | Nostr adapter `feed-ingest-nostr.ts:328-384`: kind 5 deletion pairs (`external_items` UPDATE + `feed_items` UPDATE) are not wrapped in a transaction. Crash between the two leaves inconsistent state until daily reconcile cron. Upsert path correctly uses withTransaction | Medium   | Wrap each deletion pair in `withTransaction`                                                                                                         | Minutes |
| D202 | Jetstream listener `listener.ts`: no WebSocket ping/pong or read-timeout watchdog. A silently-dead TCP connection could go undetected until OS TCP timeout (minutes). DID refresh at 60s provides eventual detection but no proactive liveness check                         | Medium   | Add client-side ping interval (e.g., 30s) or a "last message received" watchdog timer that triggers reconnection after 2 min silence                 | Hours   |
| D203 | Jetstream listener `listener.ts:391,432`: cursor UPDATE runs outside the dual-write transaction. If transaction succeeds but cursor UPDATE fails, cursor lags behind causing re-ingestion on reconnect. Safe due to ON CONFLICT DO NOTHING idempotency                       | Low      | Move cursor UPDATE inside the withTransaction block, or accept as documented risk                                                                    | Minutes |
| D204 | ActivityPub visibility check `activitypub.ts:239-243`: only matches canonical AS Public URI `https://www.w3.org/ns/activitystreams#Public`. Some non-Mastodon AP servers (Pleroma, Akkoma, GoToSocial) use `as:Public` or bare `Public`                                      | Low      | Add alternate Public URI forms to `isPublic()` check for broader fediverse compatibility                                                             | Minutes |
| D205 | Trust epoch aggregate `trust-epoch-aggregate.ts:248-268`: batch INSERT builds `$1, $2, ..., $N` placeholders. For >3,276 subjects (65535 / 4 dimensions / 5 params), exceeds PostgreSQL's 65535 parameter limit                                                              | Medium   | Refactor to use UNNEST arrays like the L1 refresh does, or batch into chunks of ~3,000                                                               | Hours   |
| D206 | Trust epoch aggregate: when a quarterly date (Jan 1, Apr 1, Jul 1, Oct 1) falls on Monday or Thursday, both quarterly and mop-up crons fire at 02:00. Could double-increment `epochs_since_reaffirm` if Graphile Worker doesn't dedup by `?id=` suffix                       | Medium   | Add `isQuarterlyBoundary()` guard in the mop-up cron path to skip when quarterly cron handles it, or use a single cron entry with runtime date logic | Minutes |
| D207 | `feed_scores_refresh`: (a) `NOT IN` subquery with nullable `nostr_event_id` is fragile anti-pattern; (b) no index on `feed_items.nostr_event_id` for the scoring JOIN; (c) no overlap guard — if queries take >5m, next invocation runs concurrently                         | Medium   | (a) Use `NOT EXISTS` instead; (b) add index on `feed_items.nostr_event_id`; (c) add `jobKey` or `pg_try_advisory_lock` to prevent overlap            | Hours   |
| D208 | `external_items_prune`: both interaction guards are dead code. Bookmark subquery contains `WHERE FALSE` (placeholder). Vote subquery compares `target_nostr_event_id` to `ei.id::text` (UUID-to-Nostr-hex — never matches). Items with user interactions silently deleted    | Medium   | Fix vote guard to use correct join column, or remove guards with documented acceptance that external items are ephemeral                             | Minutes |
| D209 | `source_metadata_refresh`: fetches all active sources regardless of protocol but only processes `protocol === 'rss'`. The 200-row LIMIT is wasted on non-RSS sources that get no-opped                                                                                       | Low      | Add `AND protocol = 'rss'` to the source query                                                                                                       | Minutes |
| D210 | Schedule conflict: `trust_epoch_aggregate` mop-up and `external_items_prune` both fire at `0 2 * * *` on Mon/Thu. If both are heavy, they compete for DB resources                                                                                                           | Low      | Stagger `external_items_prune` to 02:30 or 01:30                                                                                                     | Minutes |
| D211 | `sanitize.ts`: no `allowProtocolRelative: false` in config. Protocol-relative URLs (`//evil.com/tracker.gif`) in `<img src>` pass through as tracking pixels                                                                                                                 | Low      | Add `allowProtocolRelative: false` to sanitize-html options                                                                                          | Minutes |
| D212 | `sanitize.ts`: no input size limit. Multi-megabyte RSS entry bodies parsed in full. Mitigated by safeFetch 5MB limit but sanitiser itself has no guard                                                                                                                       | Low      | Add `if (html.length > 1_000_000) html = html.slice(0, 1_000_000)` before sanitisation                                                               | Minutes |
| D213 | `applyDecay` imported but never called in `trust-epoch-aggregate.ts:7` — SQL does the increment directly. Dead import                                                                                                                                                        | Low      | Remove unused import                                                                                                                                 | Minutes |
| D214 | Nostr outbound `nostr-outbound.ts:83`: WebSocket `error` handler clears timeout but does not call `ws.close()`. Socket may linger until GC on transient errors                                                                                                               | Low      | Add `ws.close()` in the error handler                                                                                                                | Minutes |
| D215 | `feed_items_reconcile` + `feed_items_author_refresh`: unbounded queries with no batch limit. Full-table joins could hold locks and bloat WAL at scale                                                                                                                        | Low      | Add batch limits (e.g., LIMIT 10000 in a loop) when tables exceed ~100K rows                                                                         | Hours   |
| D216 | No startup env var validation for task-specific variables (`LINKED_ACCOUNT_KEY_HEX`, `ATPROTO_PRIVATE_JWK`, `PLATFORM_RELAY_WS_URL`). Misconfiguration only surfaces when the task runs                                                                                      | Low      | Add `requireEnv()` calls in index.ts or in individual task init paths for critical variables                                                         | Minutes |

### Session 3.4: Traffology services

**Scope:** `traffology-ingest/` + `traffology-worker/`. Analytics pipeline.

- [x] **Note:** These services appear in docker-compose but were not found in the codebase inventory — verify they exist and are buildable — **both exist, compile, Dockerfiles are correct; zero test files in either service**
- [x] **Ingest** (`traffology-ingest/`): verify beacon receiver, IP hashing with `IP_HASH_SALT` (now `requireEnv`'d per §64), geoip, UA parsing, rate limiting — **all sound; IP hashed with SHA-256 + salt; geoip via `geoip-lite` (in-memory); UA parsed to coarse `deviceType`/`browserFamily` only; rate limit 120/min on `/beacon`**
- [x] **Worker** (`traffology-worker/`): verify hourly aggregation (ranking UPDATE inside transaction per §67), source resolution, observation interpretation — **hourly aggregation is transactional but ranking query has a fatal `ROW_NUMBER() FILTER` syntax error (see D34); source resolution has SSRF (see D35); 6/32 observation types implemented (see D37)**
- [x] **Gateway routes** (`routes/traffology.ts`): verify the 5 `as any` casts were resolved (§43); verify data retrieval for writer analytics — **all 5 `as any` casts resolved; all routes use `requireAuth` with writer-scoped queries; one `Record<string, any[]>` weak type annotation remains (line 222)**
- [x] **Web components** (`components/traffology/`): verify dashboard rendering, observation templates, shared components — **dashboard renders cleanly; 18 observation templates with proper `escapeHtml()` on all user values; `ProvenanceBar` canvas component is clean; `TraffologyMeta` exposes no PII**
- [x] Check for PII leakage in analytics pipeline (IP hashing, user agent storage) — **no raw IP or UA string stored; raw `referrer_url` stored which may contain PII in query params (see D38); city-level geo stored (minor fingerprinting signal)**

**ADR reference:** `TRAFFOLOGY-MASTER-ADR-2.md` (Phase 1 complete; Phases 2–4 pending)

**Diagnoses & Remedies (3.4):**

| #   | Diagnosis                                                                                                                                                                                                                                                                                                                                                                                                          | Severity | Remedy                                                                                                                                                                                                               | Effort  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------- | --- | -------- | ------- |
| D34 | `ROW_NUMBER() OVER (...) FILTER (WHERE ...)` in `traffology-worker/src/tasks/aggregate-hourly.ts:193-198` — `FILTER` is only valid on aggregate functions in PostgreSQL, not window functions. This causes the entire hourly transaction to roll back, so `piece_stats`, `source_stats`, and `half_day_buckets` are never committed. All downstream tasks (interpret, observations) operate on stale or empty data | Critical | Move the filter into the CTE's FROM clause: `JOIN traffology.pieces p ON ... WHERE p.published_at >= date_trunc('year', CURRENT_DATE)` for the `rank_this_year` window, or use a CASE expression inside the ORDER BY | Minutes |
| D35 | Bare `fetch()` on attacker-controllable `referrer_url` in `traffology-worker/src/tasks/resolve-source.ts:209` — does not use the SSRF-hardened HTTP client from `shared/src/lib/http-client.ts`. An attacker who crafts a Referer header can force the worker to make HEAD requests to internal network addresses                                                                                                  | High     | Replace `fetch()` with the shared `httpClient` (which pins DNS lookup against private-range blocklists). The 3-second timeout and `redirect: 'manual'` limit blast radius but don't prevent internal reconnaissance  | Minutes |
| D36 | Race condition in `findOrCreateSource` (`resolve-source.ts:262-297`): SELECT-then-INSERT without unique constraint or `ON CONFLICT`. Concurrent `resolve_source` jobs for the same new source create duplicate rows in `traffology.sources`                                                                                                                                                                        | Medium   | Add a unique constraint on `(writer_id, source_type, domain)` and switch to `INSERT ... ON CONFLICT DO NOTHING RETURNING id` with a fallback SELECT                                                                  | Minutes |
| D37 | Only 6 of 32 ADR-specified observation types have generation logic in `traffology-worker/src/tasks/interpret.ts`. `ARRIVAL_CURRENT`/`ARRIVAL_NONE` are listed in the Phase 1 comment but have no implementation. The observation feed is sparse                                                                                                                                                                    | Medium   | Implement remaining types in priority order: arrival detectors (need real-time counters), subscriber events (data available from `public.subscriptions`), pattern detectors (need sufficient historical data)        | Days    |
| D38 | Raw `referrer_url` stored verbatim in `traffology.sessions` (`traffology-ingest/src/routes/beacon.ts:71`) — referrer URLs can contain PII in query parameters (emails, search terms, auth tokens)                                                                                                                                                                                                                  | Medium   | Strip query parameters before storage (store path only or domain only), or implement a data-retention policy with periodic purge of raw URLs after source resolution completes                                       | Hours   |
| D39 | `pieceId` route param interpolated into ingest service URL without validation (`gateway/src/routes/traffology.ts:41`). Mitigated in practice because the DB ownership check (line 32) will throw a Postgres type error on non-UUID input before the fetch, but produces 500 instead of 400                                                                                                                         | Low      | Add UUID format regex check on `pieceId` at route entry: `if (!/^[0-9a-f-]{36}$/.test(pieceId)) return reply.status(400)`                                                                                            | Minutes |
| D40 | `escapeHtml()` in `web/src/lib/traffology-templates.ts:228` omits single-quote escaping (`'` → `&#39;`). Not exploitable in current usage (output goes to `dangerouslySetInnerHTML`, not into attribute values) but fragile if templates ever place output inside single-quoted HTML attributes                                                                                                                    | Low      | Add `'` → `&#39;` to the escape function                                                                                                                                                                             | Minutes |
| D41 | Unbounded in-memory cache in `traffology-ingest/src/lib/piece-resolver.ts:14` — one entry per unique article that receives a beacon, never evicted. At current scale (thousands of articles) this is fine; at larger scale it would grow without bound                                                                                                                                                             | Low      | Replace with an LRU cache (e.g., `lru-cache` with max 10K entries) or add a TTL                                                                                                                                      | Minutes |
| D42 | `aggregate_weekly` (`traffology-worker/src/tasks/aggregate-weekly.ts:16`) uses `pool.query()` directly instead of `withTransaction()` — inconsistent with hourly/daily/interpret tasks. Single-statement atomicity is sufficient but breaks the pattern                                                                                                                                                            | Low      | Wrap in `withTransaction()` for consistency, especially if the task ever grows a second query                                                                                                                        | Minutes |
| D43 | `total_readers_this_month` column in `aggregate-daily.ts:91` actually sums `readers_today` rather than monthly readers — misleading column name                                                                                                                                                                                                                                                                    | Low      | Either rename the column to `readers_today_sum` or fix the query to aggregate from `traffology.sessions` with a monthly window                                                                                       | Minutes |
| D44 | Zero test files in both `traffology-ingest/` and `traffology-worker/` — `--passWithNoTests` masks the gap                                                                                                                                                                                                                                                                                                          | Medium   | Priority tests: (1) beacon validation + IP hashing in ingest, (2) source resolution classify logic in worker, (3) observation generation thresholds in interpret — all are pure-function or DB-testable              | Hours   |
| D45 | `limit` query param coercion in `gateway/src/routes/traffology.ts:81` — `parseInt` on non-numeric input returns `NaN`, propagated to `Math.min(NaN, 50)` = `NaN`, causing SQL `LIMIT NaN` error                                                                                                                                                                                                                    | Low      | Add `                                                                                                                                                                                                                |         | 20`fallback:`const limit = Math.min(parseInt(..., 10) |     | 20, 50)` | Minutes |
| D46 | `SELECT *` in overview route (`traffology.ts:199,239`) for `writer_baselines` and `topic_performance` — risks leaking future sensitive columns to the API response                                                                                                                                                                                                                                                 | Low      | Replace with explicit column lists                                                                                                                                                                                   | Minutes |
| D47 | 18 observation templates defined in `web/src/lib/traffology-templates.ts` but only 6 generated by the worker — 12 templates are dead code until their generators are implemented (D37)                                                                                                                                                                                                                             | Info     | No action needed — templates are ready for when generator logic ships                                                                                                                                                | —       |

---

## Phase 4 — Frontend (Web)

The web frontend is the largest surface (~3,400 files). Split into five sessions by functional domain.

### Session 4.1: Design system & shared components

**Scope:** `web/src/components/ui/`, `web/tailwind.config.js`, global CSS, `web/src/components/layout/`

- [x] **Design tokens**: verify all tokens from CLAUDE.md are defined in tailwind.config.js (`text-ui-xs`, `text-ui-sm`, `text-mono-xs`, `.label-ui`, `.btn`, `.btn-accent`, `.btn-ghost`, `.btn-soft`, `.btn-text`, `.btn-text-muted`, `.btn-text-danger`, `.toggle-chip`, `.slab-rule-4`) — **All 13 tokens defined. Fonts (Jost, Literata, IBM Plex Mono) all configured. Max-widths (article 640, feed 780, editor-frame 780, content 960) all correct. Extra undocumented `article-frame: 960px` and `text-mono-sm: 15px` tokens exist. Minor: IBM Plex Mono only loads latin regular 400 (no bold/italic), Jost has no italic @font-face**
- [x] **Token usage audit**: grep for hand-rolled equivalents that should use tokens (e.g., `font-mono text-[12px] uppercase`, `text-[13px] font-sans`, `text-[14px] font-sans`) — **243 violations across 40+ files: 88 hand-rolled `.label-ui`, 73 hand-rolled `text-ui-xs`, 57 hand-rolled `text-ui-sm`, 25 hand-rolled `text-mono-xs`. Workspace components are the largest concentration (~half of all violations). See D300–D303**
- [x] **PageShell**: verify all top-level pages use `<PageShell>` — grep for hand-rolled `mx-auto max-w-* px-4` wrappers — **8 pages use PageShell (dashboard, settings, profile, library, network, ledger, admin/reports, traffology layout). 18 pages use hand-rolled wrappers (admin, messages, notifications, search, subscriptions, write, auth, invite, subscribe, about, [username], pub/\* sub-pages). Some are intentional (feed, workspace, article reader, editor). See D304**
- [x] **Three-voice compliance**: verify serif (Literata) is never used for platform UI elements; verify sans (Jost) is never used for infrastructure labels; verify mono (IBM Plex Mono) is used for all labels/metadata — **~20 serif-for-UI violations (page titles: notifications, search, pub archive/masthead/about; error pages ×4; auth pages ×5; modal titles ×2; danger-zone headings ×3; data values: BalanceHeader £amount, SubscribersTab counts, PaywallGate price). 5 sans-for-labels violations (traffology pages, UsernameChange). 3 mono-for-prose violations (PaywallGate status sentences, ProfileDriveCard, GiftLinkModal close button). See D305–D308**
- [x] **Button audit**: grep for inline button styles that should use `.btn` classes — **1 violation: QuoteSelector.tsx:45 hand-rolls `bg-black text-white px-3 py-1.5`. 6 hand-rolled toggle-chip groups in dashboard/\*, traffology, ComposeOverlay. Button system is well-adopted overall**
- [x] **Form labels**: verify all use `.label-ui text-grey-400` — **~15 violations. Wrong typeface: UsernameChange (text-ui-xs uppercase tracking-wider), DriveCard/DriveCreateForm (text-ui-xs font-medium text-grey-600 ×6), CommissionForm (text-[13px] font-sans text-grey-600 ×2), MembersTab (text-ui-xs text-grey-400), messages/page. Wrong class: DangerZone + PublicationSettingsTab (text-sm text-black ×2). Undefined class: auth/page uses `label-muted` (not in globals.css). Incomplete: GiftLinksPanel + GiftLinkModal (font-mono but missing uppercase/tracking). See D309**
- [x] **Layout components**: review `Nav.tsx` (topbar), `PageShell.tsx`, any layout wrappers — **PageShell is clean: `font-sans text-2xl font-medium` h1, three width variants, `py-12` + `mb-8` gap. Nav has two modes: platform (full bar, logo+wordmark, nav links, search, compose ⌘K, avatar dropdown, mobile hamburger sheet) and canvas (minimal bar, logo only, avatar only). Topbar is 60px fixed black. Unread badges on avatar, mobile hamburger. `useLayoutModeContext()` controls mode**
- [x] Cross-reference with `docs/audits/ADMIN-PAGE-AUDIT.md` findings — **All 13 issues marked Done. `.btn-soft` defined, page titles fixed to sans, `.label-ui` sweep applied to admin pages, PageShell introduced, editor settings consolidated, `.btn-text` variants defined, `.toggle-chip` defined, traffology typography aligned, analytics promoted to tab. Remaining drift is in non-admin pages (notifications, search, messages, write, auth, pub sub-pages) and workspace components — the admin audit fixed its scope but the rest of the codebase wasn't touched**

**ADR reference:** `ALLHAUS-REDESIGN-SPEC.md` §1–§6, CLAUDE.md design system rules

**Diagnoses & Remedies (4.1):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                                                       | Severity | Remedy                                                                                                                                                                                                 | Effort  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| D300 | **88 hand-rolled `.label-ui` across 22 files.** `font-mono text-[11px] uppercase tracking-[0.06em]` repeated everywhere instead of the single-class token. Heaviest: workspace components (~40), feed cards (~12), compose (~6), layout/Nav+Footer (~10), replies (~4). VesselBar uses `tracking-[0.04em]` (intentionally tighter, won't map 1:1)               | Medium   | Mechanical find-and-replace. VesselBar's 0.04em tracking needs a variant token or stays inline. Workspace components are the priority since they're new code                                           | Hours   |
| D301 | **73 hand-rolled `text-ui-xs` (`text-[13px]`) across 24 files.** Heaviest: Composer (9), ExportModal (8), RateCardTab (8), dashboard/page (5), PipPanel (4). Some `font-serif text-[13px]` instances are literary content previews (not violations, but still an arbitrary size)                                                                                | Medium   | Replace `text-[13px]` with `text-ui-xs` in all non-serif contexts. Serif content previews need a dedicated literary-preview token or stay as-is                                                        | Hours   |
| D302 | **57 hand-rolled `text-ui-sm` (`text-[14px]`) across 21 files.** Heaviest: dashboard/page (6), RateCardTab (6), MessageThread (4), CommissionForm (3), Nav (3). Same serif caveat as D301                                                                                                                                                                       | Medium   | Same approach as D301                                                                                                                                                                                  | Hours   |
| D303 | **25 hand-rolled `text-mono-xs` (`font-mono text-[11px]` without uppercase) across 12 files.** These are tabular/data values that should use the `text-mono-xs` token instead of inline sizing                                                                                                                                                                  | Low      | Replace with `text-mono-xs` token                                                                                                                                                                      | Hours   |
| D304 | **18 pages use hand-rolled page shells** instead of `<PageShell>`. Admin/settings pages were migrated (admin audit batch 3) but remaining pages were not: `admin/page`, `messages/*`, `notifications`, `search`, `subscriptions`, `write`, `auth/*`, `invite/*`, `subscribe/*`, `about`, `[username]`, `pub/*/about\|archive\|masthead\|subscribe`              | Medium   | Migrate remaining pages to `<PageShell>`. Some (auth, invite, subscribe) use non-standard widths (max-w-sm) — either add a `narrow` width variant or keep those as intentional exceptions              | Hours   |
| D305 | **~20 serif-for-UI violations.** Page titles: notifications, search, pub/archive, pub/masthead, pub/about. Error pages: error.tsx ×4. Auth: auth/page ×2, auth/verify ×3. Modals: ExportModal, GiftLinkModal. Danger zone: DangerZone, PublicationSettingsTab ×2. Data values: BalanceHeader (£ amount), SubscribersTab (counts ×3), PaywallGate (price)        | Medium   | Page titles + error headings + auth headings + modal titles → `font-sans text-2xl font-medium` (match PageHeader). Data values (BalanceHeader, SubscribersTab, PaywallGate) → `font-mono tabular-nums` | Hours   |
| D306 | **5 sans-for-labels violations.** `uppercase tracking-` without `font-mono`: UsernameChange:84, traffology/FeedItem:9, traffology/piece page:103,145,192. Traffology uses non-standard tracking values (0.08em, 0.10em, 0.12em) alongside non-standard sizes (10px, 8px)                                                                                        | Low      | UsernameChange → `.label-ui text-grey-400`. Traffology labels → `.label-ui` (accept standard 0.06em tracking) or define a compact `label-ui-xs` variant for the 10px/8px uses                          | Minutes |
| D307 | **3 mono-for-prose violations.** PaywallGate:148,153 renders full sentences ("You've spent £x on writer this month…") in `font-mono text-[12px]`. ProfileDriveCard:129 renders "Pledged — added to your tab." in mono. GiftLinkModal:64 renders "Close" button in mono                                                                                          | Low      | PaywallGate + ProfileDriveCard → `text-ui-xs text-grey-400`. GiftLinkModal "Close" → `.btn-text-muted`                                                                                                 | Minutes |
| D308 | **Writer display name on profile page in serif.** `[username]/page.tsx:71` renders `displayName` in `font-serif text-3xl`. Display names are platform metadata, not literary content. SubscribersTab:60,64,70 renders subscriber counts in serif (data values, should be mono)                                                                                  | Low      | Display name → `font-sans text-3xl font-medium`. Subscriber counts → `font-mono text-2xl tabular-nums`                                                                                                 | Minutes |
| D309 | **~15 form label violations across 10 files.** Three failure modes: (a) wrong typeface — Jost instead of Plex Mono (UsernameChange, DriveCard ×3, DriveCreateForm ×3, CommissionForm ×2, MembersTab, messages/page); (b) wrong class — `text-sm text-black` (DangerZone, PublicationSettingsTab); (c) undefined class — `label-muted` in auth/page (not in CSS) | Low      | All → `.label-ui text-grey-400`. Auth page's `label-muted` references need replacing since the class doesn't exist                                                                                     | Hours   |
| D310 | **QuoteSelector.tsx:45 hand-rolls button styles** (`bg-black text-white px-3 py-1.5 text-[12px] font-sans shadow-lg`). Should use `.btn btn-sm` or a dedicated floating-action variant                                                                                                                                                                          | Low      | Replace with `.btn btn-sm` or `.btn-accent btn-sm` depending on desired style                                                                                                                          | Minutes |
| D311 | **6 hand-rolled toggle-chip groups.** Dashboard price mode (auto/fixed), traffology layout tabs, ComposeOverlay mode toggle, AnalyticsTab period toggle, PublicationSettingsTab toggle, RateCardTab pricing toggle — all use conditional `bg-black text-white` / `bg-grey-100 text-grey-600` instead of `.toggle-chip` + `.toggle-chip-active/inactive`         | Low      | Replace with `.toggle-chip .label-ui` + active/inactive variants (pattern already used in ReadingPreferences, NotificationPreferences)                                                                 | Hours   |
| D312 | **IBM Plex Mono only loads latin regular 400.** No bold weight, no italic, no latin-ext character coverage. If bold mono is ever used (e.g., `font-bold` on a `.label-ui` element), the browser synthesises faux-bold                                                                                                                                           | Low      | Add `@font-face` for 500/700 weights if bold mono is used. Check whether any `.label-ui font-bold` or `font-mono font-bold` exists in the codebase                                                     | Minutes |
| D313 | **Jost has no italic @font-face.** Only normal style loaded. If `font-sans italic` is ever used (e.g., placeholder text), the browser synthesises faux-italic                                                                                                                                                                                                   | Low      | Add italic `@font-face` for Jost if italic sans is needed                                                                                                                                              | Minutes |
| D314 | **Undocumented tokens.** `article-frame: 960px` max-width and `text-mono-sm: 15px` fontSize exist in tailwind.config.js but aren't in CLAUDE.md. If these are intentional, document them; if accidental, remove                                                                                                                                                 | Low      | Audit usage; add to CLAUDE.md if in active use, or remove if dead                                                                                                                                      | Minutes |
| D315 | **`.btn-ghost` and `.btn-soft` are near-identical.** Both use `bg-grey-100` backgrounds; differ only in padding and font-size. The semantic distinction (ghost = full-size muted, soft = compact secondary) is implicit and undocumented                                                                                                                        | Low      | Document the distinction in CLAUDE.md or collapse into one class with a size modifier                                                                                                                  | Minutes |

### Session 4.2: Feed surface & card family

**Scope:** `web/src/components/feed/`, card components, `FeedView.tsx`

- [x] **Card chassis**: verify `ArticleCard`, `NoteCard`, `ExternalCard` all follow the unified grammar — left bar (4px, correct colours), byline row (mono-caps 11px), action row (mono-caps 11px). **⚠ F4.2-A**: `NoteCard.tsx:145` middle-dot separator missing `tracking-[0.06em]` (Low)
- [x] **Article tiers**: verify `sizeTier` branching (lead/standard/brief headline sizes, brief suppression of excerpt+tags, two-up layout for adjacent briefs)
- [x] **ExternalCard**: verify provenance badge rendering (VIA RSS / VIA NOSTR / VIA BLUESKY / VIA MASTODON); verify sanitised HTML rendering; verify `at://` → `bsky.app` URL rewriting
- [x] **Feed states**: verify end-of-feed, zero state, filtered-empty, error state components (Step 6)
- [x] **Feed rhythm**: verify 40px gap between items, no horizontal rules
- [x] **No avatars**: verify no avatar images in card bodies. **⚠ F4.2-B**: `QuoteCard.tsx:117-123` renders 18×18px avatar in quoted-note body — violates no-avatars rule (Medium)
- [x] **TrustPip**: verify rendering on all card types
- [x] **NoteComposer**: verify it's truly deprecated and not imported anywhere (file deleted in `fc9f816`, zero live imports)
- [x] **Reading history resumption**: verify `useReadingPosition` hook, scroll debounce, `pagehide`/`visibilitychange` flush. Excellent implementation; no test coverage (minor)

**ADR reference:** `ALLHAUS-REDESIGN-SPEC.md` §2, §4, §4a, §6

### Session 4.3: Compose overlay & editor

**Scope:** `web/src/components/compose/`, `web/src/components/editor/`, `web/src/stores/compose.ts`, `web/src/components/workspace/Composer.tsx`, `web/src/app/write/`

- [x] **ComposeOverlay**: verify three modes (note, reply, article); verify Zustand store (`open`, `openArticle`, `setMode`); verify scrim + topbar interaction; verify mobile full-screen behavior — **Three-mode architecture correct. Zustand store clean (`open`, `openArticle`, `setMode`, `close`). Scrim `bg-black/40` at z-40, topbar z-50 — topbar stays interactive. Mobile: full-screen bottom sheet with 90vh + drag handle. Desktop widths correct (640/760px). NoteComposer fully deprecated, no imports remain. Cross-post IDs collected in state but never passed to `publishNote` (D316). Escape key stale closure (D317). Note→article escalation discards typed content (D318)**
- [x] **ArticleComposePanel**: verify TipTap instance, title input (Literata 22px italic), `PUBLISH AS` selector, paywall gate + price, autosave via `createAutoSaver`, `OPEN IN FULL EDITOR ↗` navigation, schedule, publish — **TipTap setup matches full editor (StarterKit, Markdown, Image, PaywallGateNode, EmbedNode). Title uses `font-serif italic text-[22px]` (Literata 22px italic, correct). Publish button uses `btn-accent` (correct) but with inline size overrides (D325). `OPEN IN FULL EDITOR` correctly flushes draft and navigates to `/write?draft=<id>&pub=<slug>`. `suggestPrice` auto-pricing by word count. autosaver timer leaks on unmount (D320). `sendEmail: true` hardcoded — every overlay publish sends email blast (D322). gatePositionPct hardcoded to 50 (D323). autosave/flush omit publicationId (D324)**
- [x] **Workspace Composer**: verify cross-protocol broadcast (Nostr + linked accounts fan-out); verify DM branch (NIP-44 via key-custody); verify To-field resolver integration — **Cross-post correctly constructs `CrossPostTarget[]` from linked accounts and passes through `publishNote`. DM path delegates NIP-44 encryption to gateway/key-custody (plaintext in, encrypted on server). To-field hand-rolls resolver debounce/poll instead of using `useResolverInput` — lacks stale-closure guard (D326). `useResolverInput` hardcodes `context: 'subscribe'` — no way to pass `'dm'` (D327). Two-call note publish (sign+relay then index) means cross-posts lost if index call fails (D332)**
- [x] **Editor**: verify TipTap extensions (StarterKit, Markdown, Image, PaywallGateNode, EmbedNode); verify paywall gate node positioning and content splitting; verify markdown serialisation — **Extensions architecturally sound. CRITICAL: scheduled articles lose paywall split — `handleSchedule` saves stripped content, scheduler's `splitContent` never finds the marker (D333). Gate marker (`<!-- paywall-gate -->`) doesn't survive markdown round-trip because `html: false` entity-escapes it (D334). EmbedNode has no markdown serialisation — URLs lost on publish (D335). NaN propagated from empty price input (D337). `insertPaywallGate` uses stale state after deleteRange (D338)**
- [x] **Full editor** (`/write`): verify draft hydration from compose overlay handoff (`/write?draft=<id>&pub=<slug>`) — **Handoff mechanically sound: `flushDraft` awaits save, navigates with draft ID + pub slug. Full editor reads params, loads draft, resolves pub slug via memberships. TipTap extension sets identical across both surfaces. Full editor autosave doesn't know incoming draftId — can orphan the overlay's draft row (D339). Tags not stored in drafts table — lost on round-trip (D340). Edit mode loads only `contentFree` — editing a paywalled article discards all paid content (D336)**

**ADR reference:** `ALLHAUS-REDESIGN-SPEC.md` §3

**Diagnoses & Remedies (4.3):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                         | Severity | Remedy                                                                                                                                                                                                                 | Effort  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D316 | **Cross-post IDs collected but never sent.** `crossPostIds` state maintained and toggled via `CrossPostPill` UI, but `handlePost` calls `publishNote(content, pubkey, replyTarget)` without passing `crossPostIds` as the 4th argument. Selected cross-posts silently discarded                                                   | High     | Pass `crossPostIds` (mapped to `CrossPostTarget[]`) as the 4th argument to `publishNote`                                                                                                                               | Minutes |
| D317 | **Escape key handler stale closure over media attachments.** `useEffect` dependency array omits `media.attachments`. If only images are attached (no text), pressing Escape skips the confirmation prompt and closes immediately, discarding attachments                                                                          | Medium   | Add `media.attachments.length` to the effect dependency array                                                                                                                                                          | Minutes |
| D318 | **Note-to-article escalation discards typed content.** Clicking "Write an article" calls `setMode('article')` which swaps to `ArticleComposePanel`. Note textarea's local `content` state is not transferred to the TipTap editor. Typed text silently lost                                                                       | Medium   | On mode switch, pass current `content` to ArticleComposePanel (via store or prop) and seed the TipTap editor body                                                                                                      | Hours   |
| D319 | **Article mode close button has no unsaved-changes guard.** Escape is disabled for article mode by design (correct), but the close button calls `close()` directly, bypassing the `handleDismiss` confirmation. User could lose a long article draft by clicking X                                                                | Medium   | Wire the article mode close button through `handleDismiss` guard or call `flushDraft` before closing                                                                                                                   | Hours   |
| D320 | **autoSaver timer leaks on unmount.** `createAutoSaver` returns a closure over a `setTimeout` handle. Pending timer can still fire after panel unmount, calling `saveDraft` and setting state on unmounted component. No cleanup effect cancels the timer                                                                         | Medium   | Return a `cancel()` method from `createAutoSaver`; call it in a `useEffect` cleanup                                                                                                                                    | Hours   |
| D321 | **Close button uses viewport-relative absolute positioning.** `right-[calc(50%-308px)]` (note) / `right-[calc(50%-368px)]` (article) assumes centered overlay at exact width. On viewports narrower than 640/760px (medium-width tablets 768–960px in desktop breakpoint), button is off-screen or misaligned                     | Low      | Position close button relative to the overlay container (`absolute top-1 right-2` inside the overlay div)                                                                                                              | Minutes |
| D322 | **`sendEmail: true` hardcoded in compose overlay article publish.** `buildPublishData` always sets `sendEmail: true`. Every article published from the overlay triggers an email blast to followers with no opt-out                                                                                                               | Medium   | Default to `false` in the overlay surface (full editor is the proper channel for email-on-publish), or add a visible toggle                                                                                            | Hours   |
| D323 | **autosave and flushDraft hardcode `gatePositionPct: 50`.** Both always write 50% regardless of actual gate position. Draft row has stale gate position. Not harmful if scheduler recomputes, but misleading data                                                                                                                 | Low      | Compute `gatePositionPct` from editor content in autosave/flush paths                                                                                                                                                  | Minutes |
| D324 | **autosave and flushDraft omit `publicationId`.** `selectedPubId` only included in publish path, not autosave or flush. Draft row won't carry publication association. URL param `pub=slug` partially compensates but requires membership re-fetch                                                                                | Low      | Pass `selectedPubId` to `saveDraft` in autosave and `flushDraft` calls                                                                                                                                                 | Minutes |
| D325 | **Publish button mixes `btn-accent` with inline overrides.** `py-1.5 px-5 text-[12px] font-sans font-semibold` override `btn-accent` token sizing. Violates design system rule: "Do not hand-roll button styles with inline classes"                                                                                              | Low      | Use `btn-accent` alone or with a defined size variant                                                                                                                                                                  | Minutes |
| D326 | **Workspace Composer duplicates resolver logic.** Hand-rolls debounce, poll loop, and state management for To-field (lines 353–396) instead of using shared `useResolverInput` hook. Inline version lacks `genRef` stale-closure guard — fast type-then-clear can apply results from an abandoned query                           | Medium   | Refactor Composer.tsx To-field to use `useResolverInput` with a configurable `context` parameter                                                                                                                       | Hours   |
| D327 | **`useResolverInput` hardcodes `context: 'subscribe'`.** Always passes `'subscribe'` to `resolver.resolve()`. Workspace Composer needs `'dm'` context (skips external Phase B chains). Neither caller can override                                                                                                                | Medium   | Add `context` option to `useResolverInput({ context })` with default `'subscribe'`                                                                                                                                     | Minutes |
| D328 | **allhaus and nostr toggles appear independent but aren't.** Protocol selector lets users toggle allhaus OFF and nostr ON (or vice versa), but both ride the same custodial key. UI suggests independence that doesn't exist in the write path                                                                                    | Low      | Lock allhaus + nostr as a single native toggle, or document which aspect each controls                                                                                                                                 | Hours   |
| D329 | **Stale debounce timer in Composer.tsx on close.** `pollCountRef` never reset on re-open. If Composer closes mid-poll and reopens, retained value shortens the poll budget for the next session                                                                                                                                   | Low      | Reset `pollCountRef.current = 0` in the open effect, or adopt shared hook (D326)                                                                                                                                       | Minutes |
| D330 | **DM path does not distinguish NIP-44 encryption failure.** If key-custody is unreachable, gateway returns 500 which becomes generic "Failed to publish." error. No hint whether encryption failed vs. DB error                                                                                                                   | Low      | Return `error: 'encryption_failed'` from gateway and surface a specific message                                                                                                                                        | Hours   |
| D331 | **Price input allows negative values.** `min={0.01}` on the number input, but `onChange` handler does `Math.round(n * 100)` with no floor. Negative value from manual entry sets negative `pricePence`                                                                                                                            | Low      | Clamp to `Math.max(0, ...)` in the onChange handler                                                                                                                                                                    | Minutes |
| D332 | **Two-call note publish pattern loses cross-posts on index failure.** Note path calls `signAndPublish` (relay outbox) then separately POSTs to `/notes` for DB indexing with `crossPosts` in body. If index call fails, event is on relay but cross-posts never fire                                                              | Medium   | Make `/notes` the single entry point — accept content + tags, sign + index + enqueue cross-posts atomically                                                                                                            | Days    |
| D333 | ✅ **Scheduled articles lose paywall split.** `handleSchedule` saves `data.content` to draft, but `PublishData.content` is `fullContent.replace(PAYWALL_GATE_MARKER, '')`. Scheduler's `splitContent` never finds the marker, so paywalled scheduled articles publish fully free                                                  | Critical | Pass raw markdown (with gate marker) to the draft when scheduling — either add `rawContent` field to `PublishData` or re-derive from `freeContent + marker + paywallContent` before save                               | Hours   |
| D334 | **Gate marker doesn't survive markdown round-trip.** `Markdown` extension configured with `html: false`, so `markdown-it` entity-escapes `<!-- paywall-gate -->`. PaywallGateNode's `parseHTML` (matching `div[data-paywall-gate]`) never fires on reload. Gate silently disappears from draft on re-open                         | High     | Implement a `parse.setup` hook on PaywallGateNode that registers a `markdown-it` rule to convert the HTML comment into `div[data-paywall-gate]`. Safer than `html: true` which allows arbitrary HTML in pasted content | Hours   |
| D335 | **EmbedNode has no markdown serialisation.** Missing `addStorage` with `markdown.serialize` spec. With `html: false`, fallback serialises as `[embed]` — embed URL lost on publish and in drafts                                                                                                                                  | High     | Add `addStorage()` to EmbedNode that serialises as bare URL on its own line, and corresponding parse hook to convert embeddable URLs back to `div[data-embed]`                                                         | Hours   |
| D336 | **Edit mode loads only free content for paywalled articles.** `write/page.tsx` line 110 sets `content: meta.contentFree` when editing. All paid content silently discarded                                                                                                                                                        | High     | Fetch both `contentFree` and `contentPaywall` from API; concatenate with gate marker. Requires API to return `contentPaywall` to the article's own author                                                              | Hours   |
| D337 | **NaN propagated from empty price input.** `parseFloat('')` returns `NaN`, `Math.round(NaN)` is `NaN`. Clearing the price input propagates NaN to publish payload and database                                                                                                                                                    | Medium   | Guard with `Number.isFinite(val) ? Math.round(val * 100) : 0`                                                                                                                                                          | Minutes |
| D338 | **`insertPaywallGate` uses stale state after `deleteRange`.** Command calls `deleteRange()` then `insertContent()` in sequence against the same initial state. If existing gate is before cursor, cursor position used by `insertContent` may be off by one node                                                                  | Medium   | Use `chain()` (single transaction) instead of two separate `commands.*` calls                                                                                                                                          | Hours   |
| D339 | **Full editor autosave orphans compose overlay draft.** `ArticleEditor` receives content via `initialContent` but its autosaver never receives the incoming `draftId`. Always runs the "find latest null-dTag draft" heuristic, which can match wrong row or create a new one. `currentDraftId` state never seeded from URL param | Medium   | Thread incoming `draftId` from URL param through to `ArticleEditor` so autosave updates the same row by ID                                                                                                             | Hours   |
| D340 | **Tags not round-tripped through drafts.** `article_drafts` table has no `tags` column. Tags entered in a previous full-editor session are lost when draft is reloaded. `initialTags` only populated from the edit-article path, not the draft path                                                                               | Medium   | Add `tags text[]` column to `article_drafts` and round-trip through the draft API                                                                                                                                      | Hours   |
| D341 | **`suggestPrice` display is circular.** "Suggested" label shows `pricePence / 100` (the current price), not the `suggestPrice(wordCount)` value. For articles under 700 words, shows "Suggested: 0.00" even when a price is manually set                                                                                          | Low      | Show actual `suggestPrice(wordCount)` value in the hint, not `pricePence`                                                                                                                                              | Minutes |

### Session 4.4: Reply threads, trust, social features

**Scope:** `web/src/components/replies/`, `web/src/components/trust/`, `web/src/components/social/`, `web/src/components/messages/`

- [x] **Playscript threads** (`PlayscriptReply.tsx`, `PlayscriptThread.tsx`, `ReplySection.tsx`): verify flat chronological rendering, no nesting; verify 32px step-in, 32px vertical rhythm; verify speaker line format (pip · bold name · colon, or `YOU:`); verify non-adjacent parent arrow; verify vote controls pinned top-right; verify hover action row; verify pagination (first 10 + SHOW N MORE) — **Core architecture correct: flat chronological, single `ml-8` step-in, `space-y-[32px]` rhythm, `<ol>/<li>` semantics, no avatars, no nested indentation, no left borders. Pagination correct (first 10 + SHOW N MORE). Non-adjacent parent arrow correct (DFS → sort → compare prev.id). VoteControls pinned `absolute right-0 top-[18px]`. Action row hover-reveal correct with `#fafaf7` tint. Action row tracking `0.02em` diverges from spec's `0.06em` (D400). `useEffect` dep array missing `user` and `onReplyCountLoaded` (D402). Delete count not rolled back on failure (D404). Timestamp only in DOM when hovered (D405)**
- [x] **Trust profile** (`TrustProfile` component): verify dimension bars from epoch scores, endorsements, Layer 4 relational data ("N writers you follow endorse this person") — **All three layers correctly assembled. Dimension bars render epoch scores with fill widths. Layer 4 `networkSays` and `attributedEndorsements` rendered. `viewerVouches` fetched but unused in TrustProfile (only consumed in WriterActivity for modal pre-population). `encounter.affirmCount` silently dropped. Layer 4 negative case ("No one in your network…") shown in compact mode, adding noise to every writer profile (D411). Error state returns null silently (D412). Double fetch: WriterActivity and TrustProfile both independently GET `/trust/:userId` (D413). `pipStatus` type missing `'contested'` (D406)**
- [x] **Vouch modal** (`VouchModal`): verify dimension selector, visibility radio, aggregate disclaimer — **Dimension checkboxes, visibility radio, and aggregate disclaimer all correct. Sequential multi-dimension submit can partially succeed (D408). Error text uses wrong crimson hex `#c41230` instead of design token `#B5242A` (D409). No `role="dialog"`, no `aria-modal`, no focus trap, no ESC handler (D410). No contest support — only `'affirm'` value hardcoded (D414)**
- [x] **DMs** (`web/src/components/messages/`): verify NIP-44 encrypted messaging flow; verify conversation list, message display, sender visibility — **NIP-44 encryption architecture correct (key-custody batch encrypt → per-recipient ciphertexts → single-txn multi-row INSERT; server-side decrypt via `/dm/decrypt-batch`). CRITICAL: conversation list preview completely broken — frontend expects `lastMessage: { content, senderUsername, createdAt }` but gateway returns only `lastMessageAt: string` (D415). `createConversation` has no dedup — repeated new-message to same user creates duplicate conversations (D416). Partial DM delivery (skipped recipients without pubkey) silent on client (D417). New message input accepts only username — violates omnivorous input principle (D418). `alert()` used for errors (D419)**
- [x] **Social features**: verify follow/unfollow UI, follower/following lists, vouch button on writer profiles, `VouchList` on `/network?tab=vouches` — **Follow/unfollow idempotent server-side (`ON CONFLICT DO NOTHING`). Follow status check loads entire follow list for a boolean (D420). VouchList flat (not grouped by subject as comment claims) (D421). No optimistic update on follow toggle (D422). Tab navigation across app lacks ARIA roles everywhere (D423). Writer display name on profile uses serif (D424). FollowingTab unsubscribe modal has no focus trap, no ESC, no ARIA dialog role (D425). `bg-red-600` override on `.btn` class instead of `btn-accent` (D426)**

**ADR reference:** `ALLHAUS-REDESIGN-SPEC.md` §4 (threads), `ALLHAUS-OMNIBUS.md` (trust/vouches)

**Diagnoses & Remedies (4.4):**

| #    | Diagnosis                                                                                                                                                                                                                                                                      | Severity  | Remedy                                                                                                                                                 | Effort  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------------------------------------------------------------------------ | ------- |
| D400 | **Action row tracking `0.02em` instead of spec's `0.06em`.** `PlayscriptReply.tsx:134` uses `tracking-[0.02em]`; spec says action row is mono-caps 11px same as speaker line. Also hand-rolls `.label-ui` instead of using the token                                           | Low       | Replace with `label-ui text-grey-400`                                                                                                                  | Minutes |
| D401 | **Speaker line and "Show more" button hand-roll `.label-ui`.** `PlayscriptReply.tsx:87`, `PlayscriptThread.tsx:70`, `ReplySection.tsx:158` — all repeat `font-mono text-[11px] uppercase tracking-[0.06em]` instead of the single-class token                                  | Low       | Replace with `label-ui` + additional classes                                                                                                           | Minutes |
| D402 | ✅ **`useEffect` deps missing `user` and `onReplyCountLoaded`.** `ReplySection.tsx:95` — if user logs in after mount, vote tallies branch won't re-run; stale `onReplyCountLoaded` closure                                                                                     | Medium    | Add `user` and `onReplyCountLoaded` to deps (wrap callback in `useCallback` at call sites)                                                             | Minutes |
| D403 | **`setTimeout` in `handleDelete` leaks on unmount.** `PlayscriptReply.tsx:50` — 3s timer not cleared; can call `setConfirmDelete` on unmounted component                                                                                                                       | Low       | Store ID in `useRef`; clear in `useEffect` cleanup                                                                                                     | Minutes |
| D404 | **`handleDelete` decrements count without rollback on failure.** `ReplySection.tsx:108-114` — on API error, count stays decremented and entry stays marked deleted. No rollback                                                                                                | Medium    | Roll back `totalCount` and `replies` state on catch                                                                                                    | Minutes |
| D405 | **`<time>` only in DOM when hovered.** `PlayscriptReply.tsx:132` — conditionally mounted action row means timestamp invisible to screen readers and search engines                                                                                                             | Low       | Always render `<time>` in the DOM; only show/hide action buttons                                                                                       | Minutes |
| D406 | ✅ **`pipStatus` type missing `'contested'`.** `web/src/lib/api/trust.ts:15` — type is `'known'                                                                                                                                                                                | 'partial' | 'unknown'`but migration 079 +`trust-pip.ts`emit`'contested'`. Confirms D115                                                                            | Medium  | Add `'contested'` to the union                                           | Minutes |
| D407 | **TrustProfile dimension bars have no ARIA.** `TrustProfile.tsx:102-106` — plain `<div>` fill bars with no `role="progressbar"`, no `aria-valuenow/min/max`                                                                                                                    | Medium    | Add `role="progressbar"` with `aria-valuenow={score}` `aria-valuemin={0}` `aria-valuemax={1}`                                                          | Minutes |
| D408 | **VouchModal partial-failure on multi-dimension submit.** `VouchModal.tsx:60-66` — sequential `for...of await`; first dimension persists, second fails, generic error shown. No indication which succeeded                                                                     | Medium    | Use `Promise.allSettled` or show per-dimension success/failure; or batch into a single API call                                                        | Hours   |
| D409 | **VouchModal error uses wrong crimson hex.** `VouchModal.tsx:185` — `text-[#c41230]` instead of `text-crimson` (`#B5242A`). Also `text-[11px] font-sans` is not a token                                                                                                        | Low       | Change to `text-ui-xs text-crimson`                                                                                                                    | Minutes |
| D410 | ✅ **VouchModal has no `role="dialog"`, no `aria-modal`, no focus trap, no ESC key handler.** `VouchModal.tsx:79,117` — modals are `fixed inset-0` divs without dialog semantics. Tab escapes to background                                                                    | High      | Add `role="dialog"` + `aria-modal="true"` + `aria-labelledby` pointing to h3 ID. Add ESC handler. Add focus trap (FocusTrap component or manual)       | Hours   |
| D411 | **Layer 4 negative case shown in compact mode.** `TrustProfile.tsx:117-143` — "No one in your network has publicly endorsed this person" shown on every writer profile. Noise for compact placement                                                                            | Low       | Suppress Layer 4 section in `compact` mode when `networkSays` is the negative-case string                                                              | Minutes |
| D412 | **TrustProfile error state silently returns null.** `TrustProfile.tsx:69` — `if (error                                                                                                                                                                                         |           | !data) return null`. No error message or retry affordance; leaves blank gap in layout                                                                  | Medium  | Show a minimal error state with retry button, or pass error up to parent | Minutes |
| D413 | **Double fetch for trust data on writer profiles.** `WriterActivity.tsx:98-103` fetches `trustApi.getProfile(userId)` for vouch modal pre-population; `TrustProfile` at line 266 independently fetches the same endpoint. Two identical requests per profile page load         | Medium    | Lift trust data fetch to `WriterActivity` and pass as prop to `TrustProfile`; or add `initialData` prop to `TrustProfile`                              | Hours   |
| D414 | **No contest support in VouchModal.** `VouchModal.tsx:64` hardcodes `value: 'affirm'`. API accepts `'contest'` and spec defines contests as aggregate-only. No UI affordance to contest a dimension                                                                            | Low       | Intentional deferral — document in spec. Or add a "Contest" toggle that auto-forces aggregate visibility                                               | Hours   |
| D415 | ✅ **Conversation list preview completely broken.** Frontend `Conversation.lastMessage` expects `{ content, senderUsername, createdAt }` but gateway `listInbox` returns only `lastMessageAt: string`. Preview text and timestamp at `ConversationList.tsx:68-76` never render | High      | Either extend `listInbox` SQL to return last message snippet (needs server-side decrypt), or change frontend to use `lastMessageAt` for timestamp-only | Hours   |
| D416 | ✅ **`createConversation` has no deduplication for 1:1 conversations.** `gateway/src/services/messages.ts:26-58` — repeated new-message to same user creates duplicate conversation rows. No unique constraint on sorted member pair                                           | Medium    | Add lookup for existing 1:1 conversation before INSERT; or add unique constraint on sorted `(member_a, member_b)` and use `ON CONFLICT`                | Hours   |
| D417 | **Partial DM delivery is silent.** `skippedRecipientIds` returned by gateway but client only `console.warn`s at `MessageThread.tsx:238`. User not told their message didn't reach some recipients                                                                              | Medium    | Surface an inline warning when `skippedRecipientIds.length > 0`                                                                                        | Minutes |
| D418 | **New message input violates omnivorous input principle.** `messages/page.tsx:115-119` — `placeholder="Username"`, uses `GET /search?type=writers`. Should use the universal resolver with `context: 'dm'`                                                                     | Medium    | Replace with `useResolverInput({ context: 'dm' })` or `POST /api/v1/resolve`                                                                           | Hours   |
| D419 | **`alert()` used for error messages.** `messages/page.tsx:61,68` — blocking OS dialog with no styling                                                                                                                                                                          | Low       | Replace with inline error state                                                                                                                        | Minutes |
| D420 | **Follow status check loads full follow list for a boolean.** `WriterActivity.tsx:81-87` — `GET /follows` returns all followed writers; `.some()` checks one ID. O(n) payload for O(1) answer                                                                                  | Medium    | Add `GET /follows/check/:writerId` endpoint returning `{ following: boolean }`                                                                         | Hours   |
| D421 | **VouchList is flat, not grouped by subject.** `VouchList.tsx:75` — `vouches.map()` renders one row per vouch. Three vouches for the same person = three separate rows with repeated avatar/name. Comment at line 12 says "grouped by subject"                                 | Medium    | Group by `subject.id`; render one row per person with dimension chips and a single withdraw affordance per chip                                        | Hours   |
| D422 | **No optimistic update on follow toggle.** `WriterActivity.tsx:105-116` — waits for server response before updating UI. Button shows `'...'` during loading                                                                                                                    | Low       | Apply optimistic update pattern (toggle immediately, revert on error)                                                                                  | Minutes |
| D423 | **Tab navigation lacks ARIA roles everywhere.** `network/page.tsx:106-120`, `WriterActivity.tsx:271-281` — plain `<button>` elements without `role="tablist"`, `role="tab"`, `aria-selected`, `role="tabpanel"`. No keyboard left/right arrow navigation                       | Medium    | Add proper ARIA tab roles and keyboard arrow navigation across all tab implementations                                                                 | Hours   |
| D424 | **Writer display name on profile uses serif.** `[username]/page.tsx:71` — `font-serif text-3xl`. CLAUDE.md says "Never use serif for … user display names." Borderline (editorial writer persona) — document as exception or change to `font-sans`                             | Low       | Either change to `font-sans text-3xl font-medium` or add documented exception for writer profile hero                                                  | Minutes |
| D425 | **FollowingTab unsubscribe modal has no focus trap, no ESC, no ARIA dialog role.** `FollowingTab.tsx:186-225` — `bg-white rounded-lg shadow-xl` with no `role="dialog"` or keyboard handling. Also uses `rounded-lg` (no rounded corners per redesign spec)                    | Medium    | Add `role="dialog"` + `aria-modal` + ESC handler + focus trap; remove `rounded-lg` and `shadow-xl`                                                     | Hours   |
| D426 | **FollowingTab button uses `bg-red-600` instead of system crimson.** `FollowingTab.tsx:218` — `btn bg-red-600 hover:bg-red-700 text-white` overrides `.btn` with raw Tailwind. Should use `btn-accent`                                                                         | Low       | Replace with `btn-accent`                                                                                                                              | Minutes |
| D427 | **TrustPip `<span>` missing `role="img"`.** `TrustPip.tsx:36` — has `aria-label` but bare `<span>` means screen readers may skip it. Needs `role="img"` for label to be announced                                                                                              | Medium    | Add `role="img"` to the TrustPip span                                                                                                                  | Minutes |
| D428 | **Unread indicator has no screen-reader text.** `ConversationList.tsx:62` — visual-only 2×2 crimson dot. Invisible to assistive technology                                                                                                                                     | Low       | Add `<span className="sr-only">Unread</span>` alongside                                                                                                | Minutes |
| D429 | **VouchList silent failure on withdrawal.** `VouchList.tsx:39-41` — catch block re-enables button but shows no error; no user feedback on failure                                                                                                                              | Medium    | Add error state and show inline error message                                                                                                          | Minutes |
| D430 | **`ReplySection` shows no error UI on API failure.** `ReplySection.tsx:87-88` — `console.error` only; blank section with no retry affordance                                                                                                                                   | Medium    | Add `error` state and render retry button                                                                                                              | Minutes |
| D431 | **DmFeeSettings £0 override silently deletes.** `DmFeeSettings.tsx:103` — user expects "Free" override to appear in list; server deletes the row when `pricePence === 0`                                                                                                       | Low       | Either store zero-price explicitly, or show validation hint when £0 is entered                                                                         | Minutes |
| D432 | **Design token violations across messages components.** `ConversationList.tsx:30,33,64,69,75` and `MessageThread.tsx:310,319,343,354,357,381` — mix of `text-[12px]`, `text-[13px]`, `text-[14px]`, `tracking-[0.04em]` instead of `.label-ui`, `text-ui-xs`, `text-ui-sm`     | Low       | Mechanical replacement with design system tokens                                                                                                       | Hours   |
| D433 | **`previewLimit` slices from tail.** `ReplySection.tsx:125` — `entries.slice(-previewLimit)` shows the N most recent, not the first N. Intent appears correct (preview = most recent activity) but undocumented and bypasses PlayscriptThread's own `INITIAL_VISIBLE` logic    | Low       | Add comment documenting intent; or accept as-is since tail-preview is more useful for card previews                                                    | Minutes |

### Session 4.5: Pages, routing & state management

**Scope:** `web/src/app/` (31 route directories), `web/src/stores/`, `web/src/hooks/`, `web/src/lib/`

- [x] **Route inventory**: verify all 31 app directories have working pages; check for dead routes that should have been removed — **42 directories total (39 with pages, 6 grouping-only, 1 error-boundary-only). No dead routes. All leaf pages render**
- [x] **API client** (`lib/api/` — 21 modules, split from the former 1,685-line `api.ts`): verify each module's fetch calls, error handling, type safety; check for missing error boundaries — **Excellent centralised `request<T>()` wrapper; 6 `any[]` escapes, 1 `request<any>`, 2 wrong `Blob` type annotations (see D34–D36)**
- [x] **Zustand stores** (`stores/compose.ts` + any others): verify store hygiene — no leaked subscriptions, proper cleanup — **4 stores (auth, compose, unread, workspace). All clean: proper cleanup, no leaked subscriptions. workspace.ts has robust localStorage persistence with debounce, quota handling, SSR safety**
- [x] **Hooks**: audit custom hooks for proper cleanup (abort controllers, event listeners, intervals) — **6 hooks audited. useReadingPosition has an unused AbortController (D37). useResolverInput's gen-based stale cancellation is sound; no formal useEffect cleanup but benign under React 18. Others clean**
- [x] **Settings pages**: verify consistent use of `PageShell`, `label-ui`, form patterns — **settings, profile, dashboard, ledger, admin/reports, library all use PageShell. search and subscriptions hand-roll wrappers (D38). traffology uses custom grid layout (acceptable for analytics). Form labels consistently use `label-ui text-grey-400`**
- [x] **Admin pages**: cross-reference with `docs/audits/ADMIN-PAGE-AUDIT.md` findings — **All 13 findings from ADMIN-PAGE-AUDIT.md resolved. admin/page.tsx correctly redirects to admin/reports**
- [x] **Linked accounts** (`/settings`): verify `LinkedAccountsPanel` for both Mastodon and Bluesky connections — **Both protocols fully implemented: OAuth flows, error handling, loading states, disconnect confirmation, default-on toggle. Type-safe**
- [x] **Subscriptions page** (`/subscriptions`): verify external feed subscription management; verify BETA label on Mastodon sources — **Full CRUD with health status. Mastodon BETA label present in all 3 locations (subscriptions list, SubscribeInput, ExternalCard) with identical `.label-ui text-amber-600` styling and tooltip**

**ADR reference:** `WORKSPACE-MIGRATION-MAP.md` (route verdicts), `UI-DESIGN-SPEC.md` (batches 1–2), `ALLHAUS-REDESIGN-SPEC.md`

**Diagnoses & Remedies (4.5):**

| #   | Diagnosis                                                                                                                                         | Severity | Remedy                                                                                                                  | Effort  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------- | ------- |
| D34 | 6 `any[]` type escapes in API modules: `feed.ts` (items, comments), `articles.ts` (tag search), `publications.ts` (articles ×2, masthead members) | Medium   | Define typed interfaces for feed items, article listings, and masthead members; replace `any[]` with proper generics    | Hours   |
| D35 | `request<any>` in `trust.ts` line 92 (vouch creation)                                                                                             | Low      | Define a `VouchResponse` interface and use `request<VouchResponse>`                                                     | Minutes |
| D36 | `request<Blob>` on `exportReceipts` and `exportAccount` in `account.ts` — both endpoints return JSON                                              | Low      | Replace `Blob` with proper response interfaces (`ReceiptExport`, `AccountExport`). No runtime impact — JSON parses fine | Minutes |
| D37 | `useReadingPosition.ts` line 45: `AbortController` created but never passed to any fetch call                                                     | Low      | Remove the controller (lines 45, 116) or pass `controller.signal` to the two API calls at lines 54–55                   | Minutes |
| D38 | `/search` and `/subscriptions` pages hand-roll `mx-auto max-w-*` instead of using `PageShell`                                                     | Low      | Wrap both pages in `<PageShell>` (`width="article"` for search, `width="feed"` for subscriptions)                       | Minutes |

---

## Phase 5 — Workspace Experiment

The workspace experiment is on a separate branch with 32 slices of unmerged work. Review in isolation before any merge decision.

### Session 5.1: Workspace data layer & API

**Scope:** `gateway/src/routes/feeds.ts` (workspace feeds CRUD), migrations 077–082, `web/src/lib/api/feeds.ts`, `web/src/stores/workspace.ts`

- [x] **Feeds CRUD**: verify `GET/POST/PATCH/DELETE /api/v1/feeds`; verify owner-only access enforcement; verify name validation (1–80 chars) — **All routes use `requireAuth` + `req.session!.sub!` and scope queries with `owner_id = $ownerId`. `loadFeed` enforces ownership with 404-for-not-owned (no info leakage). Name validated 1–80 chars at both Zod and DB CHECK levels. No per-user feed count limit (D200)**
- [x] **Feed sources**: verify `GET/POST/DELETE /feeds/:id/sources`, discriminated union (native UUID, tag, external upsert); verify `external_subscriptions` row creation + immediate fetch enqueue — **Discriminated union handling correct. External source upsert creates `external_sources` + `external_subscriptions` inside `withTransaction`, then `insertSource` (feed_sources row) runs outside the transaction — atomicity gap (D201). Fetch job also enqueued outside txn. Partial unique indexes per source type catch duplicates at DB level, mapped to 409**
- [x] **Feed items query**: verify source-filtered items query with four OR-ed EXISTS clauses; verify cursor narrowing (`published_at, id`) — **`matched` CTE joins `feed_items` to `feed_sources` via four OR-ed conditions (account, publication, external_source, tag). Cursor uses `(effective_score, fi_id)` compound for DESC ordering — correct. CTE has no time-bound filter — scans entire history (D202). Block/mute exclusions present. `sampling_mode` mapping inconsistency between PUT author-volume and PATCH source endpoints (D203)**
- [x] **Feed saves**: verify `feed_saves` table (migration 080), CRUD endpoints, cascade behavior, idempotency — **`ON CONFLICT (feed_id, feed_item_id) DO NOTHING` for idempotent saves. `ON DELETE CASCADE` from both feeds and feed_items. Saves query correctly filters `fi.deleted_at IS NULL`. `GET /saves/ids` is unbounded (D204). Cursor pagination on saves listing correct**
- [x] **Feed merge**: verify `POST /workspace/feeds/:id/merge` — source dedup, save copy, feed deletion — **Properly wrapped in `withTransaction`. Source dedup via anti-join on type+FK. Save copy uses `ON CONFLICT DO NOTHING`. Source feed deletion cascades remaining orphans. Sound**
- [x] **Source move**: verify `POST /workspace/feeds/:id/sources/:sourceId/move` — duplicate detection (409) — **23505 error code correctly mapped to 409. Ownership verified on both source and target feeds. Not transactional — concurrent target deletion could yield 500 instead of 404 (D205, low risk)**
- [x] **Workspace store** (`stores/workspace.ts`): verify localStorage persistence, debounced write, quota-exceeded handling, `hydrate`/`reset`/`setVesselPosition`/`setVesselSize`/`removeVessel` — **200ms debounced writes, quota-exceeded catch, SSR guard. No schema validation on localStorage read (D206). `writeTimer` not cancelled on `hydrate` user switch — stale timer could write to wrong user's key (D207). `items: any[]` in API layer defeats TypeScript (D208)**
- [x] **Migrations 077–082**: verify schema correctness, index coverage, CHECK constraints, trigger definitions — **Solid. CHECK constraints on discriminated unions thorough. Partial unique indexes per source type correct. CASCADE behavior appropriate. Triggers for `updated_at` correct. `weight` column has no range CHECK (D209). Migration 083 (`CONCURRENTLY` inside runner's transaction) will fail at apply time (D210)**

**ADR reference:** `WORKSPACE-EXPERIMENT-ADR.md` slices 3–4, 20, 31–32

**Diagnoses & Remedies (5.1):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                  | Severity | Remedy                                                                                                                                                     | Effort  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D200 | **No per-user feed count limit.** `POST /feeds` creates feeds without checking count. A compromised client could create thousands                                                                                                                                                                                          | Low      | Add `SELECT COUNT(*) FROM feeds WHERE owner_id = $1` check before INSERT; cap at ~50                                                                       | Minutes |
| D201 | **External source `addSource` atomicity gap.** `withTransaction` commits `external_sources` + `external_subscriptions`, then `insertSource` (feed_sources row) and fetch job enqueue run outside the transaction. If `insertSource` fails, subscription is committed but feed_sources row missing                          | Medium   | Move `insertSource` and `add_job` inside `withTransaction`; pass `client` to `insertSource`                                                                | Hours   |
| D202 | **`matched` CTE in `sourceFilteredItems` has no time bound.** Scans all non-deleted `feed_items` joined to `feed_sources` and materialises the full result set before sorting/limiting. Will degrade as `feed_items` grows                                                                                                 | Medium   | Add `AND fi.published_at > now() - INTERVAL '30 days'` inside the CTE, or push `ORDER BY ... LIMIT` into the CTE via lateral join                          | Hours   |
| D203 | **`sampling_mode` mapping inconsistency.** PUT `/author-volume/:pubkey` (line 583) maps API `"random"` → DB `"random"`. PATCH `/sources/:sourceId` (line 783) maps API `"random"` → DB `"chronological"`. The items query treats these differently: `"random"` = `random() * weight`, `"chronological"` = `epoch * weight` | High     | Pick one mapping and apply it consistently. The PATCH route's mapping appears intentional; update the PUT route to also map `"random"` → `"chronological"` | Minutes |
| D204 | **`GET /feeds/:id/saves/ids` is unbounded.** Returns all `feed_item_id` values with no LIMIT. Large save sets produce oversized responses                                                                                                                                                                                  | Low      | Add `LIMIT 10000` or paginate                                                                                                                              | Minutes |
| D205 | **Source move is not transactional.** Ownership checks and UPDATE run as separate `pool.query` calls. Concurrent target feed deletion yields FK violation (500) instead of 404                                                                                                                                             | Low      | Wrap in `withTransaction` with `SELECT ... FOR SHARE` on both feeds                                                                                        | Minutes |
| D206 | **No schema validation on localStorage read.** `readFromStorage` parses JSON and checks `typeof === "object"` but does not validate individual `VesselLayout` entries. Corrupt data (e.g. `x: "foo"`) passes through, producing `NaN` after `snap()` and broken CSS transforms                                             | Medium   | Add per-entry validation in `readFromStorage` that drops entries failing `typeof x === 'number'` checks                                                    | Minutes |
| D207 | **`writeTimer` not cancelled on user switch.** `hydrate` does not clear a pending debounced write from the previous user. If `hydrate("userA")` triggers writes, then `hydrate("userB")` is called within 200ms, the pending timer writes userA's positions to userB's storage key                                         | Medium   | Add `if (writeTimer) { clearTimeout(writeTimer); writeTimer = null; }` at the top of `hydrate()`                                                           | Minutes |
| D208 | **`items: any[]` in `WorkspaceFeedItemsResponse` and `WorkspaceFeedSavesResponse`.** The actual item shape is well-defined by the gateway but the client type is `any[]`, defeating TypeScript's ability to catch property access errors in consumers like `mapApiItem`                                                    | Medium   | Define a `WorkspaceFeedItem` discriminated union type and use it instead of `any[]`                                                                        | Hours   |
| D209 | **`weight` column has no range CHECK.** `weight NUMERIC NOT NULL DEFAULT 4.0` allows negative or extreme values. The app validates step 0–5 but the DB has no guard                                                                                                                                                        | Low      | Add `CHECK (weight >= 0)` to `feed_sources`                                                                                                                | Minutes |
| D210 | **Migration 083 `CREATE INDEX CONCURRENTLY` inside transaction will fail.** The migration runner wraps every file in `BEGIN`/`COMMIT`. PostgreSQL rejects `CONCURRENTLY` inside a transaction block — this migration will error on apply                                                                                   | Critical | Remove `CONCURRENTLY` (acceptable at current scale), or extend runner to skip txn wrapper for `CONCURRENTLY` files, or apply manually                      | Minutes |

### Session 5.2: Workspace UI components

**Scope:** `web/src/components/workspace/` — all vessel, menu, and compositor components

- [x] **WorkspaceView**: verify vessel bootstrap, grid layout, parallel item fetching, refresh-after-publish, view-mode toggling (live/saved), drag-to-merge detection — **Bootstrap: list → create-if-empty → seed positions → fire-and-forget parallel fetches. Grid: fixed 340×600 slots computed once at bootstrap (no resize recalc, D218). Parallel fetching correct (void loop). Refresh-after-publish via `refreshAll` but stale `vessels` closure (D211). View-mode toggle correct (explicit `nextView` arg avoids ref race). Merge detection: centre-point-in-bbox via DOM read on drag end; stale closure in confirm handler (D212). Hard 12-item cap with no pagination (D213)**
- [x] **Vessel**: verify drag (Framer Motion, pointer capture on name label only); verify resize (bottom-right handle, min 220×200, max 2000×2000); verify brightness/density/orientation cycling; verify snap-to-grid (20px); verify workspace bounds clamping — **Drag handle is the entire outer div, not name label only (D215). Snap grid is 10px not 20px (verify with designer). Resize: bottom-right handle with pointer capture, min 220×200, max 2000×2000 correct. `clampPos` includes name label height in bounds calc (D219). Brightness/density/orientation cycling all correct. `dragMovedRef` set but never read (D220)**
- [x] **VesselCard**: verify three-density rendering (compact/standard/full); verify brightness palette application; verify media rendering (`MediaBlock` — 16:9 cover, lazy load, `no-referrer`); verify action strip (Reply, Quote, Vote, Save, Bookmark) — **Three-density dispatch correct via `CardContext`. Palette application correct and exhaustive. MediaBlock: 16:9, `objectFit: cover`, `loading="lazy"`, `referrerPolicy="no-referrer"` all present. Action strip: Reply/Quote/Vote/Save wired; Share missing for notes. ActivityPub media URL has no scheme guard (D222, high). `contentHtml` rendered as escaped text in expanded external cards (D223). CardShell not keyboard-accessible (D224). Video play button has no accessible label (D225)**
- [x] **VesselBar**: verify minimize/hide controls; verify source list display — **Minimize/hide controls correct via Zustand store. Source list not inline (deferred to FeedComposer modal via gear button — by design). `handleAdd` has no user-visible error state on API failure (D226)**
- [x] **FeedComposer**: verify rename flow, delete flow (last-feed guard), source management with resolver — **Rename: inline edit with PATCH, correct. Delete: client-side `deleteBlocked` guard but counts hidden vessels (D228); no server-side guard (D229). Source management: resolver integration via `useResolverInput` correct. `commitStep`/`commitSampling` swallow errors silently (D230). Close button doesn't block during in-flight rename (D231)**
- [x] **ForallMenu**: verify four actions (new-feed, new-note, fork, reset); verify hidden-feed restoration — **Only new-feed and new-note implemented. Fork and reset are missing (D227, high). Hidden-feed restoration works but restore buttons lack aria-labels (D232)**
- [x] **Composer** (workspace version): verify DM branch, cross-protocol broadcast, resolver integration — **DM branch functional: `isPrivate` gating, `createConversation` + `send`. Cross-protocol: ProtocolSelector with four toggles, `crossPostTargets` correctly gated on linked accounts. Resolver: `dm` context, poll-for-results. Poll loop not guarded by open state (D233). `pollCountRef` not reset per-request (D234). `pollForResults` setTimeout handles not cleaned up (D235). DM partial-send can create duplicate conversations (D236). `useEffect` deps include unstable `onClose` (D237). No mobile bottom-sheet (D238, product gap). `enabledProtocols` shows unlinked protocols before fetch resolves (D239)**
- [x] **NotificationsAnchor**: verify 30s polling, popover rendering, mark-read, navigation — **30s `setInterval` with cleanup on unmount. Popover: conditional render, outside-click + Esc handlers correct. Mark-read: optimistic UI, fire-and-forget API call, no rollback on failure (D240). `new_message` notification routes to `#` no-op (D241, medium). Search stale results on reopen (D242)**
- [x] **SearchAnchor**: verify debounced search, AbortController cancellation, three-section results — **200ms debounce correct. AbortController aborts previous request on each new search + on close + on unmount. Three-section results (Writers/Articles/Publications) conditional render with empty/error states. Clean implementation**
- [x] **Collision/grid utilities**: verify `web/src/lib/workspace/collision.ts`, `grid.ts`, `motion.ts` — **Collision: BFS ripple-push correct with visited set and 30-iter safety. Snap-after-clamp can push 1–9px past floor (D243). Grid: `GRID = 10`, `snap = Math.round`, minimal and correct. Motion: ceremony/responsive timing tokens match spec; `VESSEL_DRAG_TRANSITION` duplicated inline in Vessel.tsx (D244). `prefersReducedMotion` not reactive to OS changes (D245)**
- [x] **useResolverInput hook**: verify shared resolver logic (extracted in refactoring pass), debounce + Phase B polling, stale cancellation — **Gen-counter pattern correctly prevents stale results. Debounce clears on each keystroke. Phase B polling via `pollForResults` with configurable `maxPolls`. No unmount cleanup — inflight fetches can set state on dead component (D246). Context hardcoded to `"subscribe"` (D247)**

**ADR reference:** `WORKSPACE-EXPERIMENT-ADR.md` slices 1–32, `WORKSPACE-MIGRATION-MAP.md`

**Diagnoses & Remedies (5.2):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                               | Severity | Remedy                                                                                                                                               | Effort  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------- | ----- |
| D211 | **`refreshAll` closes over stale `vessels` array.** If vessels are added while the composer is open, post-publish refresh misses newly added vessels                                                                                                                                                                                    | Medium   | Read vessels via functional updater or a ref instead of the render-time closure                                                                      | Minutes |
| D212 | **`handleMergeConfirm` reads stale `vessels` closure.** Post-merge `loadVesselItems` call may target undefined vessel if state changed between render and await resolution                                                                                                                                                              | Medium   | Use functional updater form: `setVessels(prev => { const tgt = prev.find(...); if (tgt) void loadVesselItems(tgt.feed); return prev.filter(...); })` | Minutes |
| D213 | **Hard `.slice(0, 12)` item cap with no pagination or "load more".** Items beyond 12 are silently dropped from every vessel                                                                                                                                                                                                             | Medium   | Add a "load more" button or virtual scroll; expose the cap as a named constant                                                                       | Hours   |
| D214 | **Bootstrap `loadVesselItems` calls not cancelled on unmount.** Component unmount during bootstrap leaves fire-and-forget fetches that call `setVessels` on a dead component; StrictMode double-invoke can cross-contaminate mount instances                                                                                            | Medium   | Add an `AbortController` to the bootstrap effect; pass its signal through to `loadVesselItems`                                                       | Hours   |
| D215 | **Drag handle is the entire vessel, not the name label only.** `onPointerDown={startDrag}` is on the outer `<motion.div>`. Only interactive descendants are excluded via `target.closest(…)`. Spec says name label only                                                                                                                 | Medium   | Move `onPointerDown={startDrag}` to the name label div; set `cursor: grab` on label instead of outer div                                             | Minutes |
| D216 | **`deleteBlocked` counts hidden vessels.** A user with two feeds can hide one then delete the other, leaving zero visible vessels and an empty floor with no recovery path until next bootstrap                                                                                                                                         | Medium   | Change guard to `vessels.filter(v => !positions[v.feed.id]?.hidden).length <= 1`                                                                     | Minutes |
| D217 | **No server-side last-feed guard on `DELETE /workspace/feeds/:id`.** Client-side `deleteBlocked` is the only guard; direct API calls or two-tab race can zero out all feeds                                                                                                                                                             | Medium   | Add server-side count check: return 409 if delete would leave zero feeds for owner                                                                   | Minutes |
| D218 | **Default grid slots computed once at bootstrap; never recalculated on window resize.** Vessels may overlap or clip on viewport change                                                                                                                                                                                                  | Low      | Add a `resize` listener that recomputes slot positions (debounced); or document as intentional for fixed-workspace paradigm                          | Hours   |
| D219 | **`clampPos` includes name label height in `offsetHeight`.** Bottom-edge clamp allows vessel to extend ~18px past the floor boundary                                                                                                                                                                                                    | Low      | Read chassis element height instead of full vessel `offsetHeight`, or subtract name-label height                                                     | Minutes |
| D220 | **`dragMovedRef` is set on every drag move but never read.** Likely intended to suppress click-on-name after drag, but that logic was never wired                                                                                                                                                                                       | Low      | Wire into `onNameClick` guard, or remove to reduce confusion                                                                                         | Minutes |
| D221 | **`dragActiveRef` written in drag start/end but never read.** Dead code                                                                                                                                                                                                                                                                 | Low      | Remove                                                                                                                                               | Minutes |
| D222 | **ActivityPub media URL has no scheme guard.** `extractAttachments()` in `feed-ingest/src/adapters/activitypub.ts` accepts raw `url`/`href` from AP objects with no `http`/`https` check before storing in `external_items.media`. A malicious instance could inject `javascript:` or `data:` URIs loaded via `<img src>` in MediaBlock | High     | Add `if (!/^https?:\/\//i.test(url)) continue` before `media.push()` in all three ingest adapters (activitypub, RSS, nostr)                          | Minutes |
| D223 | **`contentHtml` rendered as escaped plain text in expanded `ExternalVesselCard`.** When `contentText` is null, raw HTML markup (`<p>`, `<a>`, etc.) is visible to the user as text. Not XSS but garbled output                                                                                                                          | Medium   | Strip HTML tags before display: use `stripHtml(contentHtml)` or render via `dangerouslySetInnerHTML` behind sanitizer consistent with `ExternalCard` | Minutes |
| D224 | **`CardShell` clickable div is not keyboard-accessible.** No `role="button"`, no `tabIndex`, no `onKeyDown`. WCAG 2.1 SC 2.1.1 violation                                                                                                                                                                                                | Medium   | Add `role="button" tabIndex={0} onKeyDown` with Enter/Space handling                                                                                 | Minutes |
| D225 | **Video play button in `MediaBlock` has no accessible label.** SVG is `aria-hidden` inside an `aria-hidden` overlay. Screen readers cannot announce the play affordance                                                                                                                                                                 | Medium   | Add `role="img" aria-label="Play video"` to the play button span                                                                                     | Minutes |
| D226 | **`handleAdd` in VesselBar has no user-visible error state.** API failure is `console.error`'d but the user sees no indication                                                                                                                                                                                                          | Medium   | Set a local `addError` flag; render a brief error row in the dropdown. Clear on next query change or successful add                                  | Minutes |
| D227 | **ForallMenu is missing "fork" and "reset" actions.** `ForallAction` only declares `new-feed                                                                                                                                                                                                                                            | new-note | new-article`. The ADR specifies four actions; fork-by-URL and reset-layout are unimplemented stubs                                                   | High    | Implement `ForkFeedPrompt` (per ADR §slice 32) and wire reset to `useWorkspace.reset()` behind a confirm modal | Hours |
| D228 | **`deleteBlocked` counts hidden vessels (same as D216).** Documented under FeedComposer consumer side                                                                                                                                                                                                                                   | Medium   | See D216                                                                                                                                             | —       |
| D229 | **No server-side last-feed guard (same as D217).** Documented under FeedComposer consumer side                                                                                                                                                                                                                                          | Medium   | See D217                                                                                                                                             | —       |
| D230 | **`commitStep` and `commitSampling` in FeedComposer `SourceRow` swallow errors silently.** `try/finally` with no `catch`; failed PATCH leaves UI and DB in disagreement within the session                                                                                                                                              | Low      | Add `catch` that surfaces error via parent `onError` prop or local state                                                                             | Minutes |
| D231 | **FeedComposer close button doesn't block during in-flight rename.** Pressing Close/Escape while `savingName` is true unmounts the component; `onRenamed` callback never fires if the PATCH succeeds after unmount                                                                                                                      | Low      | Disable Close button and suppress Escape while `savingName \|\| deleting` is true                                                                    | Minutes |
| D232 | **Hidden-feed restore buttons in ForallMenu lack `aria-label`.** Screen readers announce the name but not the "restore" action                                                                                                                                                                                                          | Low      | Add `aria-label={\`Restore hidden feed: ${hf.name}\`}` to each restore button                                                                        | Minutes |
| D233 | **Composer `pollForResults` not guarded by `open` state.** If the Composer closes while a poll is in flight, callbacks set state on unmounted/re-opened state                                                                                                                                                                           | Medium   | Add a `cancelledRef` checked in `pollForResults` callback; reset in `open` effect cleanup                                                            | Minutes |
| D234 | **Composer `pollCountRef` not reset when a new debounce fires.** Fast typing can cause the old poll cycle's count to bleed into the new one, exhausting the budget early                                                                                                                                                                | Medium   | Store poll count per-request via a generation ID; only honour callbacks matching the current generation                                              | Minutes |
| D235 | **Composer `pollForResults` recursive `setTimeout` handles not cleaned up.** Anonymous timers are never captured or cleared on unmount/close                                                                                                                                                                                            | Medium   | Capture `setTimeout` handle in a `pollTimerRef`; clear in the `open` effect cleanup                                                                  | Minutes |
| D236 | **DM partial-send can create duplicate conversations.** On `skippedRecipientIds`, error is shown but body is kept intact and `conversationId` is not stored. A second "Send" click calls `createConversation` again for the same members                                                                                                | Medium   | Store `conv.conversationId` and reuse on retry, or clear the body after successful send                                                              | Minutes |
| D237 | **Composer `useEffect` deps include unstable `onClose`.** If `onClose` is an inline arrow in the parent, the effect re-fires on every parent render, resetting all Composer state (body, chips, resolver) mid-session                                                                                                                   | Medium   | Wrap parent's `onClose` in `useCallback`, or reference via a ref inside the effect                                                                   | Minutes |
| D238 | **Composer has no mobile bottom-sheet layout.** CLAUDE.md spec requires "Mobile: full-screen bottom sheet" but the Composer is a fixed centred overlay with hardcoded `width: 640`. Collapses to `max-width: calc(100vw - 48px)` on narrow viewports but no sheet                                                                       | Medium   | Implement bottom-sheet layout for mobile breakpoints per redesign ADR                                                                                | Days    |
| D239 | **`enabledProtocols` shows unlinked protocols before linked-accounts fetch resolves.** Status bar displays "Publishing to Nostr · BLUESKY · ACTIVITYPUB" before the async check. No spurious cross-posts fire, but copy is misleading                                                                                                   | Low      | Initialise `enabledProtocols` to native-only (`allhaus`, `nostr`); add non-native once `linkedByProtocol` resolves                                   | Minutes |
| D240 | **Individual mark-read has no rollback on API failure.** Item stays visually read while server disagrees; self-heals on next panel open via `loadList()`                                                                                                                                                                                | Low      | On rejection, reverse the optimistic update and increment `unreadCount`                                                                              | Minutes |
| D241 | **`new_message` notification routes to `#` (no-op).** `destUrl()` has no `case 'new_message'` branch. Clicking the row does nothing and closes the panel                                                                                                                                                                                | Medium   | Add `case 'new_message': return n.conversationId ? \`/messages/${n.conversationId}\` : '/messages'`                                                  | Minutes |
| D242 | **Search panel does not clear stale results when re-opened.** Previous session's query and results persist in state                                                                                                                                                                                                                     | Low      | In `close()` or the toggle handler, reset `query`, `results`, and `status`                                                                           | Minutes |
| D243 | **Collision `resolveCollisions`: snap-after-clamp can push vessels 1–9px past floor edge.** `snap()` uses `Math.round` after `floorBounds` clamp; if the clamped value is not a multiple of `GRID`, rounding goes outward                                                                                                               | Low      | Swap order: clamp after snap, or use `Math.floor` inside the bounds guard so rounding always goes inward                                             | Minutes |
| D244 | **`VESSEL_DRAG_TRANSITION` token in `motion.ts` duplicated as inline spring constants in `Vessel.tsx`.** If the spring feel is tuned in `motion.ts`, the push animation in `Vessel.tsx` diverges silently                                                                                                                               | Low      | Import `VESSEL_DRAG_TRANSITION` in `Vessel.tsx` instead of duplicating the stiffness/damping/mass values                                             | Minutes |
| D245 | **`prefersReducedMotion()` called as a plain function per position change, not reactive to OS setting changes.** User switching reduced-motion preference mid-session won't see the change until remount                                                                                                                                | Low      | Replace with a `useMediaQuery('(prefers-reduced-motion: reduce)')` hook that listens for `change` events                                             | Minutes |
| D246 | **`useResolverInput` has no unmount cleanup.** Inflight fetches after component unmount hold a stale `gen` value and call `setResult`/`setResolving` on a dead component. React 18 suppresses warning but leaks async chain for full poll duration (~8s)                                                                                | Medium   | Add `useEffect` cleanup that increments `genRef.current` and clears `debounceRef`                                                                    | Minutes |
| D247 | **`useResolverInput` context hardcoded to `"subscribe"`.** Hook is exported as general-purpose but offers no way to override the resolver context. Future `invite`/`dm` callers must bypass the hook entirely                                                                                                                           | Medium   | Add a `context` option to the hook opts, defaulting to `"subscribe"`                                                                                 | Minutes |

### Session 5.3: Workspace merge readiness assessment

**Goal:** Determine whether the workspace experiment is ready to merge to master.

- [x] List all workspace components and verify each compiles (`tsc --noEmit`) — **All three services (gateway, web, shared) compile cleanly with zero errors. 27 workspace-specific files inventoried: 15 components, 1 store, 4 lib utilities, 1 API client, 2 hooks, 1 route page, 1 gateway route, 6 migrations (077–082)**
- [x] Verify no workspace code is accidentally imported by non-workspace routes — **No runtime leaks. Two structural hygiene issues: (1) `useResolverInput` hook in shared `hooks/` unconditionally imports from `lib/workspace/resolve.ts` — latent boundary violation, no current non-workspace consumer; (2) `lib/api.ts` barrel re-exports `api/feeds.ts`, leaking workspace types into the shared namespace (tree-shaken in prod, but no lint guard). See D250, D251**
- [x] Check for deprecated components that should be removed on merge (`NoteComposer`, etc. per migration map) — **`NoteComposer.tsx` already deleted (CLAUDE.md stale). `WORKSPACE-MIGRATION-MAP.md` lists ~20 components + 4 routes for retirement on merge; all are still present and imported by non-workspace routes. `ComposeOverlay` conditionally excluded on `/workspace` via `LayoutShell`. No TODO/FIXME comments referencing "merge" found in source**
- [x] Verify workspace route (`/workspace`) is properly gated (feature flag? auth? experiment?) — **Frontend: `web/src/app/workspace/page.tsx` is an open Next.js route, no auth guard, no feature flag, accessible by direct URL. `useLayoutMode` suppresses Nav/ComposeOverlay/Footer on `/workspace`. No navigation link points to it. Backend: `gateway/src/routes/feeds.ts` registered at `/api/v1/workspace/*` with `requireAuth` on every endpoint. No feature flag — all authenticated users can hit the API. See D252**
- [x] Assess migration safety: can 077–082 be applied to the master schema without conflict? — **Yes. 077, 078, 080 are purely additive (CREATE TABLE). 079 widens a CHECK constraint (superset, safe). 081 adds nullable columns to `articles` + `article_drafts` (safe). 082 changes a DEFAULT on a table created by 077 (no master code inserts). Untracked 083 + 084 are independent of the workspace experiment. See full assessment below**
- [x] Check for shared-code modifications that might break master-branch behavior — **18 gateway files modified. Critical findings: (1) `crossPost` → `crossPosts` breaking API change in notes.ts (D253); (2) 24h external-items time window removed from timeline.ts (D254); (3) resolver behavioral changes: `@username` handling, fuzzy fallback, incremental partial results (D255); (4) `cover_image_url` woven through 5 files, requires migration 081 (D256); (5) trust polls endpoints require migration 078 (D257); (6) 10+ files with uncommitted formatting changes (D258). Plus unstaged security improvements in auth.ts, search.ts, external-feeds.ts, subscription-expiry.ts (D259)**
- [x] List all browser-untested slices and their risk level — **See browser-testing matrix below**
- [x] Output: merge readiness report with blockers, risks, and recommended pre-merge testing plan — **See below**

**ADR reference:** `WORKSPACE-EXPERIMENT-ADR.md`, `WORKSPACE-MIGRATION-MAP.md`, `REDESIGN-SCOPE.md`

---

#### Merge Readiness Report

**Branch:** `workspace-experiment` (merged to `master` 2026-05-29 via fast-forward)
**Date:** 2026-05-15 (report); merged 2026-05-29
**Verdict (original):** NOT READY — 2 blockers, 5 high-risk items, significant uncommitted working-tree noise. Blockers and high-risk items were addressed in subsequent slices before merge

---

##### 1. Compilation & file inventory

All three workspaces (`gateway/`, `web/`, `shared/`) pass `tsc --noEmit` with zero errors.

| Category                                     | Count  | Files                                                                                                                                                                                              |
| -------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Components (`web/src/components/workspace/`) | 15     | WorkspaceView, Vessel, VesselCard, VesselBar, FeedComposer, ForallMenu, ForallCeremony, MergeFeedConfirm, NewFeedPrompt, Composer, NotificationsAnchor, SearchAnchor, PipPanel, PipTrigger, tokens |
| Stores                                       | 1      | `workspace.ts`                                                                                                                                                                                     |
| Lib utilities (`web/src/lib/workspace/`)     | 4      | collision, grid, motion, resolve                                                                                                                                                                   |
| API client                                   | 1      | `web/src/lib/api/feeds.ts`                                                                                                                                                                         |
| Hooks                                        | 2      | `useResolverInput.ts`, `useLayoutMode.ts`                                                                                                                                                          |
| App route                                    | 1      | `web/src/app/workspace/page.tsx`                                                                                                                                                                   |
| Gateway route                                | 1      | `gateway/src/routes/feeds.ts` (61.5 KB)                                                                                                                                                            |
| Migrations                                   | 6      | 077–082                                                                                                                                                                                            |
| **Total**                                    | **27** |                                                                                                                                                                                                    |

##### 2. Import isolation

No runtime cross-boundary leaks. Two structural issues:

| #    | Issue                                                                                                                                                                                                                                                     | Risk             |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| D250 | `hooks/useResolverInput.ts` unconditionally imports from `lib/workspace/resolve.ts`. All current callers are workspace components, but the hook's location implies general-purpose. A future non-workspace consumer would silently pull in workspace code | Low (latent)     |
| D251 | `lib/api.ts` barrel re-exports `api/feeds.ts` via `export * from './api/feeds'`. Workspace types leak into the shared namespace. Tree-shaken in prod but no lint guard prevents non-workspace files from importing `workspaceFeeds`                       | Low (structural) |

**Remedy:** Move `useResolverInput` into `components/workspace/hooks/` OR move `resolveMatches`/`MatchOption` out of `lib/workspace/`. Remove `export * from './api/feeds'` from the barrel; have workspace components import directly.

##### 3. Deprecated components

`NoteComposer.tsx` is already deleted. CLAUDE.md reference is stale.

`WORKSPACE-MIGRATION-MAP.md` lists ~20 components and 4 routes for retirement on merge. All are still present and actively imported by non-workspace code (the legacy feed, compose, trust, and social surfaces). These cannot be deleted until the workspace _replaces_ them, meaning the merge is a parallel-route merge, not a replacement merge. Both surfaces will coexist until the workspace is promoted to default and the old routes are removed in a follow-up.

##### 4. Route gating

| Layer                           | Gating                                                   | Assessment                                                                              |
| ------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| Frontend (`/workspace`)         | None — open Next.js page, no auth check, no feature flag | Any visitor can navigate to `/workspace` and see the (empty, broken-without-auth) shell |
| Backend (`/api/v1/workspace/*`) | `requireAuth` on every endpoint                          | Authenticated users only. No feature flag                                               |
| Navigation                      | No link from Nav or other surfaces                       | Discoverable only by direct URL                                                         |

| #    | Issue                                                                                                                                                                                                                               | Risk   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| D252 | No feature flag on `/workspace`. The page is publicly accessible. Unauthenticated visitors see an empty workspace shell (API calls fail silently). On merge to master, every production user could stumble onto it via URL guessing | Medium |

**Remedy:** Add `requireAuth` redirect on the Next.js page (or a `WORKSPACE_ENABLED` env flag) before merge.

##### 5. Migration safety

| Migration                                         | Type                                                           | Safe for master?                                                                                          |
| ------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 077 `workspace_feeds`                             | CREATE TABLE ×2 + indexes + triggers                           | **Yes** — purely additive                                                                                 |
| 078 `trust_polls`                                 | CREATE TABLE + index + trigger                                 | **Yes** — purely additive                                                                                 |
| 079 `pip_status_contested`                        | ALTER CHECK constraint (widens from 3→4 values)                | **Yes** — superset; master never writes `'contested'`                                                     |
| 080 `feed_saves`                                  | CREATE TABLE + index                                           | **Yes** — purely additive                                                                                 |
| 081 `article_cover_image`                         | ALTER TABLE (nullable column on `articles` + `article_drafts`) | **Yes** — nullable, no default                                                                            |
| 082 `feed_sources_default_volume`                 | ALTER DEFAULT on new table (from 077)                          | **Yes** — master never inserts into `feed_sources`                                                        |
| 083 `search_content_trgm_index` (untracked)       | CREATE INDEX                                                   | **Independent** of workspace. Uses `CONCURRENTLY` — will fail inside runner's transaction (D210 from 5.1) |
| 084 `email_verification_requested_at` (untracked) | ALTER TABLE (nullable column)                                  | **Independent** of workspace. Safe                                                                        |

All migrations can be applied to a master-branch database without breaking existing functionality. The only deployment constraint is that migration 081 **must** be applied before the gateway code that references `cover_image_url` runs, and migration 078 must be applied before the trust-polls endpoints are hit.

##### 6. Shared-code modifications

###### 6a. Committed behavioral changes (will land on merge)

| #    | File                                                                                                  | Change                                                                                                                                                                                                                                                                                                                                        | Risk                       |
| ---- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| D253 | `routes/notes.ts`                                                                                     | **BLOCKER.** `crossPost` (object) → `crossPosts` (array). Breaking API contract. Frontend sending `crossPost: {...}` will have it silently stripped by Zod. Cross-posting from the existing compose overlay will break                                                                                                                        | **Critical**               |
| D254 | `routes/timeline.ts`                                                                                  | Removed 24h time window from external-items CTE. All historical external items now eligible for the following feed. Changes user-visible content; may degrade query performance on large `feed_items` tables                                                                                                                                  | **High**                   |
| D255 | `lib/resolver.ts`                                                                                     | Three behavioral changes buried in a formatting pass: (a) `@username` now resolves as `platform_username` instead of `free_text`; (b) exact username miss falls through to fuzzy search; (c) `dotted_host` stores partial results incrementally. Plus `debug` → `warn` log escalation across resolver + atproto-resolve + activitypub-resolve | **Medium**                 |
| D256 | `scheduler.ts`, `articles/publish.ts`, `drafts.ts`, `publications/cms.ts`, `publication-publisher.ts` | `cover_image_url` woven through 5 files. Requires migration 081 or queries crash. All article publish paths affected                                                                                                                                                                                                                          | **High** (deploy ordering) |
| D257 | `routes/trust.ts`                                                                                     | New trust-polls CRUD endpoints + encounter-affirm count query. Requires migration 078 table. Unconditionally registered — will 500 if migration missing                                                                                                                                                                                       | **High** (deploy ordering) |
| D258 | 10+ route files                                                                                       | Uncommitted formatting changes (single→double quotes, semicolons). Not part of branch commits but in working tree. Will create a noisy merge diff if committed, or will be lost if not                                                                                                                                                        | **Low** (hygiene)          |

###### 6b. Unstaged security/correctness improvements (working tree only — not yet committed)

| #     | File                             | Change                                                                                                                      | Assessment                                                               |
| ----- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| D259a | `middleware/auth.ts`             | New `stripIdentityHeaders` (exported but not wired). `optionalAuth` hardened to check account status + session invalidation | **Valuable security fix** — should be committed and merged independently |
| D259b | `workers/subscription-expiry.ts` | `GREATEST(free_allowance_remaining_pence - $1, 0)` prevents negative balance                                                | **Bug fix** — should be merged independently                             |
| D259c | `routes/search.ts`               | `Math.min(offset, 1000)` caps deep pagination                                                                               | **Performance fix** — should be merged independently                     |
| D259d | `routes/external-feeds.ts`       | Rate limit on `/feeds/subscribe` (10/min)                                                                                   | **Security hardening** — should be merged independently                  |
| D259e | `routes/auth.ts`                 | Rate limits on `/auth/verify` and `/auth/verify-email-change` (10/min)                                                      | **Security hardening** — should be merged independently                  |

These are all valuable improvements that are **stuck in the workspace branch's working tree** instead of on master where they'd protect production today.

##### 7. Browser-untested slices

The ADR records 32 slices shipped between 2026-04-30 and 2026-05-14. The commit log shows slices 27–32 were shipped as a **single commit** (`19564a7`, 2026-05-14) — six features in one batch. Slices 25–26 each got their own commit within 48 hours. No ADR entry for any slice records a browser verification pass. The ADR explicitly notes "manual smoke is the verification floor" for several slices (e.g. slice 16).

| Slices    | Shipped                        | Browser-test risk | Reason                                                                                                                                                                      |
| --------- | ------------------------------ | ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–4       | 2026-04-30 → 05-01             | **Low**           | Core bootstrap; likely exercised during subsequent development                                                                                                              |
| 5a–5c     | 2026-05-01                     | **Low**           | Drag/resize/brightness — gestural, would fail visibly                                                                                                                       |
| 6–8       | 2026-05-01                     | **Medium**        | Reset, rename/delete, fork — three distinct modal flows                                                                                                                     |
| 9         | 2026-05-01                     | **Low**           | Ceremony animation — single visual path                                                                                                                                     |
| 10        | 2026-05-01                     | **Medium**        | Article compose mode — full compose→publish pipeline                                                                                                                        |
| 11–12     | 2026-05-01                     | **Medium**        | Card actions + pip panel — interactive surfaces                                                                                                                             |
| 13–15     | 2026-05-02                     | **Medium**        | Inline threads + volume + polls — three distinct features                                                                                                                   |
| 16        | 2026-05-02                     | **Medium**        | Weight/sampling query changes — data correctness, not visual                                                                                                                |
| 17–19     | 2026-05-02                     | **Low**           | Pip composition + encounter + framing — incremental on 12                                                                                                                   |
| 20        | 2026-05-02                     | **Medium**        | Feed saves — CRUD + view toggle                                                                                                                                             |
| 21–22     | 2026-05-02                     | **Medium**        | Notifications + search anchors — floating UI, polling, popover                                                                                                              |
| 23–23b    | 2026-05-02 → 05-07             | **Medium**        | Media rendering — images, video play, cover images                                                                                                                          |
| 24        | 2026-05-07                     | **Medium**        | Vessel bar inline input — resolver integration                                                                                                                              |
| 25        | 2026-05-10                     | **High**          | Collision detection — geometry/physics, edge cases                                                                                                                          |
| 26        | 2026-05-13                     | **Medium**        | Per-source volume controls                                                                                                                                                  |
| **27–32** | **2026-05-14 (single commit)** | **High**          | Default volume, minimize/hide, forall menu updates, card expand, feed merge (drag), source move (drag). **Six features in one commit, no individual verification possible** |

##### 8. Blockers

| #      | Blocker                                                                                                                                                                                                                                                                                                                  | Resolution                                                                                                                                         |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B1** | **D253: `crossPost` → `crossPosts` breaking API change.** The existing compose overlay's cross-post UI will silently fail after merge. The web frontend must be updated in the same deploy, or the old API shape must be preserved alongside the new one                                                                 | Either update all frontend `crossPost` callers to `crossPosts` before merge, or accept both shapes in the Zod schema with a transform              |
| **B2** | **D258/D259: Uncommitted working-tree changes mix formatting noise with security fixes.** 10+ files have unstaged changes. The security improvements (auth hardening, rate limits, negative-balance fix, pagination cap) are stuck on the branch. The formatting changes will create merge conflicts or be silently lost | Commit D259a–e as independent fixup commits (or cherry-pick to master). Either commit or revert the formatting changes — they cannot stay in limbo |

##### 9. High risks (not blocking but strongly recommended before merge)

| #      | Risk                                                                                                                                                                | Recommendation                                                                               |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| **R1** | D254: Removing the 24h external-items window changes the following feed for all users. Historical backfill items will flood the feed. No query performance analysis | Add a configurable time-bound (e.g. 30d) or test query EXPLAIN on a production-scale dataset |
| **R2** | D252: No feature flag on `/workspace`. Any user can navigate to it on production                                                                                    | Add auth redirect or env flag before merge                                                   |
| **R3** | Slices 27–32 shipped as a single commit with no individual browser verification. These include drag-to-merge and drag-to-move-source — complex interactive features | Browser smoke-test each of the six features before merge                                     |
| **R4** | D256/D257: `cover_image_url` and `trust_polls` code is unconditionally registered. Deploy must apply migrations 078 + 081 before restarting the gateway             | Document deploy ordering in merge PR; consider gating with column-existence checks           |
| **R5** | D222 (from 5.2): ActivityPub media URL has no scheme guard. `javascript:` or `data:` URIs from a malicious instance could be rendered via `<img src>`               | One-line fix: `if (!/^https?:\/\//i.test(url)) continue` in all three ingest adapters        |

##### 10. Recommended pre-merge testing plan

**Phase 1 — Fix blockers (hours)**

1. Resolve D253: decide on `crossPost`/`crossPosts` migration strategy (dual-accept transform recommended)
2. Triage working-tree changes: commit security fixes independently, commit or revert formatting

**Phase 2 — Browser smoke test (half day)**
Priority order (highest risk first):

1. **Slices 27–32**: minimize/hide, forall menu, card expand, feed merge via drag, source move via drag, default volume
2. **Slice 25**: collision detection — drag vessels into each other, verify push behavior at edges
3. **Slice 24**: vessel bar — type a URL, Nostr npub, @username; verify resolver results and source addition
4. **Slice 10**: article compose mode — write 400+ words in note, verify nudge, switch to article, publish
5. **Slices 21–22**: notifications + search anchors — verify popover positioning, polling, dismiss
6. **Slices 13–15**: inline thread expansion, volume bar, poll voting

**Phase 3 — Integration verification (hours)**

1. Start fresh: delete localStorage, log in, verify ceremony + default feed bootstrap
2. Cross-post a note with linked Bluesky + Mastodon accounts (exercises `crossPosts` array)
3. Publish an article with a cover image (exercises migration 081 + 5-file `cover_image_url` path)
4. Cast trust polls on a writer, verify pip colour updates after cron
5. Resize browser window with multiple vessels — verify no overlap/clipping

**Phase 4 — Deploy ordering (minutes)**

1. Apply migrations 077–082 to production database
2. Deploy gateway + web simultaneously (atomic, due to D253)
3. Verify `/workspace` is reachable and `/api/v1/workspace/feeds` returns 401 for unauthenticated requests

##### Diagnoses & Remedies (5.3)

| #    | Diagnosis                                                                                                                                                                                      | Severity | Remedy                                                                                               | Effort  |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------- | ------- |
| D250 | **`useResolverInput` imports from `lib/workspace/resolve.ts`.** Shared hook location creates latent boundary violation                                                                         | Low      | Move hook into `components/workspace/hooks/` or extract `resolveMatches` to a non-workspace location | Minutes |
| D251 | **`lib/api.ts` barrel re-exports workspace API module.** Namespace pollution, no lint guard                                                                                                    | Low      | Remove `export * from './api/feeds'`; import directly in workspace components                        | Minutes |
| D252 | **No feature flag on `/workspace` route.** Publicly navigable, no auth on the page itself                                                                                                      | Medium   | Add `requireAuth` redirect or `WORKSPACE_ENABLED` env check                                          | Minutes |
| D253 | **`crossPost` → `crossPosts` breaking API change in `notes.ts`.** Silently breaks existing cross-post callers                                                                                  | Critical | Accept both shapes in Zod schema with transform, or update all callers simultaneously                | Hours   |
| D254 | **24h external-items time window removed from `timeline.ts`.** Changes following feed content; potential query perf degradation                                                                | High     | Add configurable time bound; EXPLAIN ANALYZE on production-scale data                                | Hours   |
| D255 | **Resolver behavioral changes buried in formatting pass.** `@username` handling, fuzzy fallback, incremental partial results all changed with no commit isolation                              | Medium   | Extract into separate commits with clear descriptions; verify each change in isolation               | Hours   |
| D256 | **`cover_image_url` woven through 5 gateway files.** Requires migration 081 or all article publish paths crash                                                                                 | High     | Document deploy ordering; consider column-existence guard                                            | Minutes |
| D257 | **Trust-polls endpoints require migration 078.** Unconditionally registered; will 500 if table missing                                                                                         | High     | Document deploy ordering; consider try-catch or feature flag                                         | Minutes |
| D258 | **10+ files with uncommitted formatting changes.** Working-tree noise that will complicate merge                                                                                               | Low      | Commit as a separate formatting pass or revert                                                       | Minutes |
| D259 | **Security/correctness fixes stuck in working tree.** Auth hardening, rate limits, negative-balance prevention, pagination cap — all blocked from production by being on the experiment branch | High     | Cherry-pick to master or commit and merge independently                                              | Hours   |

---

## Phase 6 — Infrastructure & DevOps

### Session 6.1: Docker & deployment

**Scope:** `docker-compose.yml`, all Dockerfiles, `nginx.conf`, `DEPLOYMENT.md`

- [x] **Dockerfiles**: verify all use multi-stage builds with `omit=dev`; verify non-root users; verify health checks; verify no secrets baked in — **All 8 Dockerfiles use two-stage builds (build + production). All 6 backend services use `npm ci --omit=dev` in the production stage. Web copies full node_modules (no omit — acceptable, Next.js needs devDependencies at build time but the production stage copies the built output). All 7 custom images create non-root `app` user via `addgroup -S app && adduser -S app -G app` and switch with `USER app`. No secrets baked into any Dockerfile. No health checks in Dockerfiles themselves — all defined in docker-compose.yml instead (acceptable)**
- [x] **docker-compose.yml**: verify service dependencies are correct; verify volume mounts; verify environment variable injection; verify all 11 services + certbot — **13 services defined (postgres, strfry, gateway, payment, keyservice, key-custody, traffology-ingest, traffology-worker, feed-ingest, web, nginx, blossom, certbot). Dependencies: all backend services depend on postgres:service_healthy. Payment depends on strfry:service_healthy (correct — needs relay for receipt events). Gateway does NOT depend on strfry (acceptable — relay outbox decouples). Feed-ingest missing `PLATFORM_RELAY_WS_URL` env var (D400). Feed-ingest and gateway missing `ATPROTO_CLIENT_BASE_URL` and `ATPROTO_PRIVATE_JWK` (D401). Blossom has no port mapping — internal only (correct). Volume mounts correct. Healthchecks present on all HTTP services. Workers (traffology-worker, feed-ingest) correctly have no healthcheck**
- [x] **Nginx**: verify route mapping correctness (`/api/*` → gateway, `/.well-known/*` → gateway, `/ingest/*` → traffology, `/relay` → strfry, `/media/*` → volume, `/` → web); verify security headers (HSTS, CSP, X-Frame-Options); verify client body size limits; verify dynamic DNS resolution — **Route mapping correct: `/api/` → gateway:3000, `/.well-known/oauth-client-metadata.json` + `/.well-known/jwks.json` → gateway:3000, `/ingest/` → traffology-ingest:3005 (with trailing slash strip), `/relay` → strfry:7777 (WebSocket upgrade with 86400s timeouts), `/media/` → alias /app/media/ (static), `/` → web:3000. Docker DNS resolver 127.0.0.11 with 10s TTL. Security headers: HSTS 2yr + includeSubDomains + preload, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy camera/mic/geo denied. CSP present but incomplete (D402). `/media/` block has own `add_header` which drops parent security headers (D403). `/api/` body limit 12m, `/ingest/` 16k. No default body limit for web proxy (D404)**
- [x] **DEPLOYMENT.md**: verify instructions match current docker-compose and service configuration — **Architecture diagram matches docker-compose. Service table matches. Volume table matches. Env var table comprehensive. Fresh deployment steps correct. Migration table stops at 041 but actual migrations go to 084 (D405). "Known limitations" section references v4.8.0 and lists features that have since been built (RSS ingestion, traffology) (D406). schema.sql header says "through migration 038" but seed list goes to 056 — inconsistent (D407). DEPLOYMENT.md says "schema.sql includes changes through migration 045" — incorrect, seed covers 001–056 (D407)**
- [x] Check for environment variable completeness — are all required vars documented? — **DEPLOYMENT.md env var table covers all critical secrets and service URLs. Missing from table: `ATPROTO_CLIENT_BASE_URL` and `ATPROTO_PRIVATE_JWK` for feed-ingest (only documented for gateway). Missing `PLATFORM_RELAY_WS_URL` for feed-ingest (relay-publish task uses it). `JETSTREAM_URL` undocumented (feed-ingest, optional with default). `TRUST_DRY_RUN` undocumented (feed-ingest, optional). `NOSTR_PROFILE_RELAYS` undocumented (gateway, optional). No `.env.example` for traffology-ingest or feed-ingest (D408)**
- [x] Verify `schema.sql` is used correctly for postgres initialization — **Mounted as `/docker-entrypoint-initdb.d/01-schema.sql:ro` in docker-compose — correct, runs only on first boot when pgdata volume is empty. Schema covers migrations 001–038 structurally but seeds `_migrations` through 056. Migrations 057–084 (28 migrations) will need to be applied after first boot. This gap is documented in D53 (Session 1.2) as a high-severity finding. DEPLOYMENT.md's "no action needed for fresh databases" claim is incorrect — migrations 057–084 must still be run (D409)**

**Diagnoses & Remedies (6.1):**

| #    | Diagnosis                                                                                                                                                                                                                                                | Severity | Remedy                                                                                                                                                | Effort  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D400 | `feed-ingest` is missing `PLATFORM_RELAY_WS_URL` in docker-compose.yml `environment:` block. The `relay-publish` task falls back to `ws://localhost:4848` which won't resolve inside Docker — relay publishing will silently fail                        | Medium   | Add `PLATFORM_RELAY_WS_URL: ws://strfry:7777` to the feed-ingest service environment in docker-compose.yml                                            | Minutes |
| D401 | `feed-ingest` and `gateway` are missing `ATPROTO_CLIENT_BASE_URL` and `ATPROTO_PRIVATE_JWK` in docker-compose.yml. Without these, AT Protocol OAuth (Bluesky linked accounts) falls back to loopback dev mode in production                              | Medium   | Add both vars to gateway and feed-ingest environment blocks, sourced from `.env` or service-specific env files                                        | Minutes |
| D402 | CSP missing directives for Stripe.js (`script-src` needs `https://js.stripe.com`, `frame-src` needs `https://js.stripe.com` for 3DS), external oEmbed iframes, and `font-src 'self'`. `connect-src 'self' wss:` blocks Stripe API calls from frontend    | High     | Expand CSP: add `https://js.stripe.com` to script-src/frame-src, `https://api.stripe.com` to connect-src, `font-src 'self'`                           | Minutes |
| D403 | `/media/` location block uses its own `add_header` directives, which drops all parent server-level security headers (HSTS, X-Frame-Options, CSP, Referrer-Policy, Permissions-Policy) due to nginx inheritance rules                                     | Medium   | Either repeat all security headers in the `/media/` block, or move security headers into a shared snippet file and `include` it in both blocks        | Minutes |
| D404 | No `client_max_body_size` on the web proxy (`location /`). Nginx default is 1MB — Next.js SSR responses and large HTML pages could be truncated. Not critical but worth setting explicitly                                                               | Low      | Add `client_max_body_size 4m` to the web proxy location block                                                                                         | Minutes |
| D405 | DEPLOYMENT.md migration table stops at migration 041. Migrations 042–084 (43 migrations) are undocumented. Upgrade instructions only cover up to v5.30.0 but production is well past that                                                                | Medium   | Add migration entries for 042–084 and upgrade instructions for recent releases. Consider generating migration table from filenames                    | Hours   |
| D406 | DEPLOYMENT.md "Known limitations" section references v4.8.0 and lists features that have since shipped (RSS feed ingestion, external Nostr, Bluesky, Mastodon). Creates false impression of missing functionality                                        | Low      | Update limitations section to reflect current state. Remove shipped items, add actual current limitations                                             | Minutes |
| D407 | `schema.sql` internal inconsistency: file header says "migrations 001–038", body includes structures through ~056, `_migrations` seed covers 001–056. DEPLOYMENT.md says "through migration 045". Three different claims about the same file             | Medium   | Regenerate schema.sql from a fully-migrated database (per D53). Update header comment and DEPLOYMENT.md to match                                      | Hours   |
| D408 | No `.env.example` files for `traffology-ingest` or `feed-ingest`. These services require env vars (`IP_HASH_SALT`, `DATABASE_URL`, `LINKED_ACCOUNT_KEY_HEX`, etc.) but have no template for new deployments                                              | Low      | Create `.env.example` files for both services listing required and optional vars with placeholder values                                              | Minutes |
| D409 | DEPLOYMENT.md "fresh deployment" §4 says "no action needed" for fresh databases, but schema.sql only covers through ~056 while 84 migrations exist. Migrations 057–084 must be run manually after first boot — undocumented                              | Medium   | Update §4 to state that after first boot, the migration runner must be executed to apply migrations 057+. Add the command explicitly                  | Minutes |
| D410 | Web Dockerfile copies entire `node_modules` from build stage to production stage (`COPY --from=build /app/node_modules node_modules`). This includes all devDependencies (~hundreds of MB). Next.js `output: 'standalone'` would produce a minimal image | Low      | Consider adding `output: 'standalone'` to next.config.js and updating Dockerfile to copy only `.next/standalone` + `.next/static` + `public`          | Hours   |
| D411 | Web Dockerfile builds in isolation from the monorepo — uses `web/package.json` and `web/package-lock.json` directly, not the workspace root lockfile. This means web's lockfile can drift from the root lockfile                                         | Low      | Acceptable for now — web is intentionally standalone. Document the decision. Consider unifying under root lockfile if web gains shared/ dependencies  | —       |
| D412 | Blossom service has a healthcheck against port 3003 (`wget -qO- http://localhost:3003/`) but no port mapping in docker-compose — not reachable from host. Config file `blossom-config.yml` exists but service is documented as "not in upload path"      | Low      | Acceptable — blossom is kept for future federation. No action needed unless activated                                                                 | —       |
| D413 | No `proxy_set_header X-Forwarded-For` or `X-Real-IP` on `.well-known` locations. Not critical (metadata endpoints) but inconsistent with other proxy blocks                                                                                              | Low      | Add standard proxy headers for consistency                                                                                                            | Minutes |
| D414 | `certbot` service has no restart policy and no scheduled renewal command in docker-compose. Renewal depends on external cron (`harden-server.sh`). If cron breaks, certificates silently expire                                                          | Low      | Document the dependency on `harden-server.sh` cron more prominently. Consider a certbot sidecar with `--keep-until-expiring` and entrypoint cron loop | Hours   |

### Session 6.2: CI pipeline & code quality tooling

**Scope:** `.github/workflows/ci.yml`, root `package.json` scripts, knip config, ESLint config

- [x] **CI pipeline**: review current workflow (build all workspaces, ESLint, knip, npm test, web lint+vitest, npm audit); identify gaps — **4 jobs (backend build+lint+knip, test, web lint+vitest, audit). Well-structured. Audit job currently fails on master due to 12 unresolved vulns (D1/D2/D3/D4). No Docker build verification, no migration ordering check, no `tsc --noEmit` for web (relies on `next lint` which only catches Next.js rules + partial TS checking)**
- [x] **Missing CI coverage**: no integration tests, no database tests, no Docker build verification, no migration verification — **Confirmed. All tests are unit-only (no DB connection). Docker builds only verified on manual deploy. Migration ordering and idempotency untested. No smoke test for service startup. These are acceptable for a solo developer but become gaps at scale (D300)**
- [x] **Knip configuration**: verify dead-code detection is correctly configured for all workspaces; review the ~46-type baseline — **Knip covers root + shared only; gateway, payment-service, key-service, key-custody, feed-ingest, traffology-ingest, traffology-worker, and web are all uncovered (D301). Current output: 1 unused export (`PipStatus` type). Down from 47 at baseline. The `ignoreDependencies` list suppresses 10 packages that are runtime-only or imported dynamically — all legitimate**
- [x] **ESLint**: verify promise-safety rules are enforced; check for missing rules from `CODE-QUALITY.md` recommendations — **Promise-safety is comprehensive: `no-floating-promises`, `no-misused-promises`, `await-thenable` all at error level. Zero violations. 279 warnings: 176 `no-unnecessary-type-assertion` (auto-fixable), 77 `no-explicit-any`, 26 `no-unused-vars`. `consistent-type-imports` recommended in CODE-QUALITY.md ADR but not present in config (D302, confirms D33). Web frontend has no ESLint config — `next lint` prompts for setup interactively, meaning CI may hang or fail on fresh runs (D303). eslint-plugin-security (Tier 1c) not adopted — acceptable per ADR's "defer until needed" guidance**
- [x] **TypeScript**: verify `strict` mode across all tsconfigs; verify `target: ES2022` / `module: NodeNext` consistency — **All 8 backend services extend `tsconfig.base.json` (ES2022 / NodeNext / strict). Consistent. Three services (feed-ingest, traffology-ingest, traffology-worker) use `include: ['src/**/\*']`without`exclude: ['tests']`— test files compile into`dist/`(D304, confirmed:`trust-weighting.test.js`etc. present in`feed-ingest/dist/`). Web uses ES2020 / esnext / bundler (appropriate for Next.js, no issue). Node 20 in CI matches Dockerfiles\*\*
- [x] **Dependency management**: verify npm workspace configuration; check for version mismatches across workspaces — **Web is intentionally outside npm workspaces (separate lockfile, separate `npm ci`). 8 backend services in workspace. No version mismatches found across shared dependencies (all resolve to same version via hoisted lockfile). `@platform-pub/shared` is the only cross-workspace dependency — used by gateway, payment-service, key-service, key-custody, feed-ingest. `passWithNoTests` in traffology-ingest and traffology-worker prevents test job failure for services with zero tests — correct**
- [x] **Feature-debt item #2**: CI pipeline is listed as launch-blocking — draft a CI improvement plan — **feature-debt.md line 16 still reads "zero infrastructure despite being Tier 1a priority" — stale, CI exists since the CODE-QUALITY.md work. `CODE-QUALITY.md` line 14 also says "No CI pipeline (no `.github/workflows/`)" — stale. Both need updating (D305). CI improvement plan below**

**ADR reference:** `CODE-QUALITY.md`

**Diagnoses & Remedies (6.2):**

| #    | Diagnosis                                                                                                                                                                                                                                          | Severity | Remedy                                                                                                                                                                     | Effort  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D300 | **CI has no Docker build verification.** Dockerfile breakage (base image changes, copy path errors, multi-stage failures) is only caught on manual deploy to production                                                                            | Medium   | Add a `docker-build` CI job that runs `docker compose build` (or individual `docker build --target build` for each service). Cache layers with `actions/cache`             | Hours   |
| D301 | **Knip covers only root + shared.** The other 6 backend workspaces and web are uncovered — dead exports/files/deps in gateway (73 files) etc. go undetected                                                                                        | Low      | Add workspace entries for all 8 backend services + web to `knip.json`. Set `entry` to `src/index.ts` for services, `src/app/**/page.tsx` + `src/app/**/layout.tsx` for web | Minutes |
| D302 | **`consistent-type-imports` rule absent.** CODE-QUALITY.md recommends it; not in `eslint.config.mjs`. Confirms D33 from Session 0.2                                                                                                                | Low      | Add `'@typescript-eslint/consistent-type-imports': 'warn'` to ESLint config                                                                                                | Minutes |
| D303 | **Web has no ESLint config file.** `next lint` prompts interactively for setup on first run — CI may hang. The `web` CI job runs `npx next lint` which works only because `.next/` cache exists from a prior local run or because Next.js defaults | Medium   | Create `web/.eslintrc.json` with `{ "extends": "next/core-web-vitals" }` so `next lint` runs non-interactively                                                             | Minutes |
| D304 | **Test files compiled into `dist/` for 3 services.** feed-ingest, traffology-ingest, traffology-worker use `include: ['src/**/*']` without `exclude: ['tests']`. Test files ship in production Docker images                                       | Low      | Add `"exclude": ["node_modules", "dist", "src/**/*.test.ts"]` to all three tsconfigs (matching the other 5 services' pattern of excluding tests)                           | Minutes |
| D305 | **Stale "no CI" claims in feature-debt.md (line 16) and CODE-QUALITY.md (line 14).** CI pipeline has existed since the CODE-QUALITY.md implementation work; both docs still say it doesn't exist                                                   | Low      | Update feature-debt.md to mark CI as done; update CODE-QUALITY.md "Current state" section to reflect the actual pipeline                                                   | Minutes |
| D306 | **CI audit job fails on master.** 12 unresolved npm vulnerabilities (1 critical sanitize-html, 5 high fast-uri/fastify). The audit job (`npm audit --audit-level=high`) exits non-zero, so the CI pipeline is red on every push                    | High     | Fix D1 (sanitize-html patch) and D2 (fastify upgrade) first. Until then, audit gate is noise — either fix vulns or temporarily downgrade to `--audit-level=critical`       | Hours   |
| D307 | **No migration ordering verification.** Migrations are numbered SQL files; nothing in CI checks that numbering is sequential, that no gaps exist, or that filenames follow the convention                                                          | Low      | Add a shell step to CI: `ls migrations/*.sql \| sort -n \| awk` to verify sequential numbering. Low priority — single developer, no collision risk                         | Minutes |
| D308 | **279 ESLint warnings (0 errors).** 176 are auto-fixable `no-unnecessary-type-assertion`. These pass CI (warnings don't fail) but create noise that obscures real issues                                                                           | Low      | Run `npx eslint --fix` to clear the 176 auto-fixable warnings. Triage the 77 `no-explicit-any` and 26 `no-unused-vars` per Sessions 0.1 D6/D7/D8                           | Minutes |

**CI improvement plan (feature-debt item #2):**

The pipeline is functional and covers the highest-value gates (type safety, promise safety, tests, audit). Gaps are real but proportionate to team size. Prioritised improvements:

1. **Immediate (unblock green CI):** Fix D1 sanitize-html patch + D2 fastify upgrade so the audit job passes. Alternatively, gate on `--audit-level=critical` until the fastify upgrade is tested
2. **Quick wins (minutes each):** Create `web/.eslintrc.json` (D303), add `consistent-type-imports` (D302), exclude test files from 3 tsconfigs (D304), update stale docs (D305)
3. **Expand knip coverage (D301):** Add all workspaces to `knip.json` — likely surfaces additional dead code in gateway and web
4. **Docker build verification (D300):** Add a `docker-build` job to CI. Most valuable gate after the existing ones — catches Dockerfile regressions before they reach production
5. **Defer:** Migration verification (D307), integration/DB tests (Session 7.1 scope), coverage reporting (Tier 2b in CODE-QUALITY.md — add when test volume justifies it)

---

## Phase 7 — Test Coverage Expansion

### Session 7.1: Backend test gap analysis & critical path coverage

**Scope:** All backend test files (gateway: 4, payment: 3, feed-ingest: 4, shared: 3, key-service: 1, key-custody: 2)

- [x] Run all 17 backend test files; record pass/fail — **All 17 pass: shared 28/28, gateway 25/25, payment-service 41/41, key-service 11/11, key-custody 19/19, feed-ingest 142/142 (but see D325). 266 total tests across 6 services. Traffology has 0 test files**
- [x] Map critical paths to existing test coverage:
  - Auth flow (magic link → session → refresh → logout): **partially covered** — `session.test.ts` covers JWT create/verify/refresh-detect (6 tests), `auth-middleware.test.ts` covers requireAuth/optionalAuth hooks (9 tests). NOT covered: magic link generate/verify/expire/single-use (`magic-links.ts`), auth route handlers (login/verify/logout/email-change), Google OAuth flow
  - Article publish pipeline (create → sign → publish → feed_items): **minimally covered** — `publication-publisher.test.ts` covers only `generateDTag` slug generation (5 tests). NOT covered: `articles/publish.ts`, `articles/manage.ts`, `nostr-publisher.ts`, `key-custody-client.ts` signing flow, feed_items dual-write
  - Gate-pass (access check → unlock → payment): **partially covered** — `access.test.ts` covers `checkArticleAccess` decision logic (10 tests). NOT covered: `performGatePass` orchestrator, unlock-records persistence, inter-service HTTP call to payment-service, key-service content key fetch
  - Payment settlement (accrue → settle → payout): **well-covered for pure logic** — `accrual.test.ts` covers `classifyRead` (16 tests), `settlement.test.ts` covers `shouldTriggerSettlement` + fee calculation (13 tests), `payout-math.test.ts` covers `computePublicationSplits` (18 tests). NOT covered: actual settlement flow (Stripe PaymentIntent creation, DB transaction, idempotency), payout worker, webhook handler
  - Relay outbox state machine: **covered** — `relay-publish.test.ts` (10 tests covering success/failure/retry/abandon/lock-contention/already-sent/no-relays/deletion-retry)
  - Trust epoch aggregation: **excellently covered** — `trust-aggregation.test.ts` (42 tests), `trust-weighting.test.ts` (11 tests), `trust-pip.test.ts` (19 tests) = 72 tests across all pure scoring/weighting/pip functions
  - External feed ingest (RSS/Nostr/Bluesky/AP): **not covered** — 0 tests for any of the 5 feed-ingest task files, 0 tests for RSS/atproto/activitypub/nostr adapters, 0 tests for jetstream listener, 0 tests for sanitize.ts
  - Resolver pattern classification: **not covered** — `classifyInput` is a pure function (11 input types, regex-based) but module-private and untested. No tests for any resolver logic
- [x] Prioritise test additions by risk: payment paths > auth paths > resolver > feed ingest — **see priority ranking below**
- [x] Draft test skeletons for the top 5 coverage gaps — **see D326–D330**

**ADR reference:** n/a (test infrastructure)

**Diagnoses & Remedies (7.1):**

| #    | Diagnosis                                                                                                                                                                                                                                                                                                                            | Severity | Remedy                                                                                                                                                                                                                                                | Effort  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D325 | **Feed-ingest vitest runs tests from both `src/` and `dist/`.** `tsconfig.json` has `"include": ["src/**/*"]` but no vitest config excludes `dist/`. The 4 `.test.ts` files compile to `dist/*.test.js` which vitest also discovers → 8 "files" but only 4 unique test suites, inflating reported counts                             | Low      | Add `vitest.config.ts` with `include: ['src/**/*.test.ts']` to exclude `dist/`. Or add `dist/` to vitest's `exclude` list                                                                                                                             | Minutes |
| D326 | **Gap 1 (High): Resolver `classifyInput` is untested.** Pure regex-based function with 11 input types — ideal for unit testing. Handles npub, nprofile, hex pubkey, DID, bluesky handle, fediverse handle, ambiguous email-like, dotted host, URL, platform username, free text. Classification errors cause wrong resolution chains | High     | Export `classifyInput` from resolver.ts, write `resolver-classify.test.ts` with ~30 cases covering each type + edge cases (leading @, case sensitivity, minimum-length username, hex-64 boundary). Pure function — no DB mocks needed                 | Minutes |
| D327 | **Gap 2 (High): Magic link lifecycle untested.** `requestMagicLink` (token generation, SHA-256 hashing, DB insert) and `verifyMagicLink` (hash lookup, expiry check, single-use delete) have zero test coverage. Auth entry point for all non-Google users                                                                           | High     | Write `magic-links.test.ts` in shared/tests. Requires DB mock for pool.query. Key cases: generate returns token, hash is stored not plaintext, verify with valid/expired/already-used/invalid token, cleanup of expired links                         | Hours   |
| D328 | **Gap 3 (High): SSRF private-IP validation untested.** `isPrivateIpv4`, `isPrivateIpv6`, `parseIpv6` are module-private pure functions with complex logic (CIDR ranges, IPv6 compression, zone IDs, IPv4-mapped v6). A bug here defeats all SSRF protection                                                                          | High     | Export `isPrivateIpv4`, `isPrivateIpv6`, `parseIpv6` (or a combined `isPrivateIp`) for testing. Write `http-client.test.ts` with edge cases: loopback, link-local, CGNAT, multicast, IPv4-mapped IPv6, :: compression, zone IDs, public IPs that pass | Hours   |
| D329 | **Gap 4 (Medium): RSS adapter/sanitizer untested.** `feed-ingest/src/adapters/rss.ts` parses XML, extracts fields, normalises dates. `sanitize.ts` strips XSS from external HTML content. Both handle hostile input from the open internet                                                                                           | Medium   | Write `rss.test.ts` with sample RSS/Atom XML (valid, malformed, empty). Write `sanitize.test.ts` with XSS vectors (script tags, event handlers, data URIs, CSS injection). Both are pure-function testable                                            | Hours   |
| D330 | **Gap 5 (Medium): Feed-ingest adapter normalisation untested.** atproto adapter (facets→HTML, embeds→media, quote refs), activitypub adapter (outbox pagination, visibility check, content extraction), nostr adapter (event parsing, deletion handling) — all handle external protocol data with no tests                           | Medium   | Start with atproto adapter (most complex): test `normalisePost` with sample post records covering text-only, facets, images, external embeds, quote posts, reply threads. Extend to AP and Nostr                                                      | Hours   |
| D331 | **Test ratio by service.** Coverage is extremely uneven: feed-ingest has 72% of all tests (mostly trust libs), while gateway (73 source files) has only 25 tests across 4 files. Traffology (15 source files) has zero tests                                                                                                         | Medium   | Focus new tests on gateway pure functions (resolver, access check edge cases) and payment-service integration paths. Traffology tests can wait until the service matures past Phase 1                                                                 | Days    |

**Test coverage summary:**

| Service         | Test files | Tests | Source files | Ratio | Coverage quality                                                                 |
| --------------- | ---------- | ----- | ------------ | ----- | -------------------------------------------------------------------------------- |
| shared          | 3          | 28    | 21           | 14%   | Session/voting/email-format covered; auth/crypto/SSRF gaps                       |
| gateway         | 4          | 25    | 73           | 5%    | Auth middleware + access check + boot + slug; resolver/routes/services uncovered |
| payment-service | 3          | 41    | 10           | 30%   | Pure logic well-covered; Stripe integration + workers uncovered                  |
| key-service     | 1          | 11    | 9            | 11%   | Crypto round-trips covered; route handlers uncovered                             |
| key-custody     | 2          | 19    | 4            | 50%   | Best ratio; keypair gen + schema validation covered                              |
| feed-ingest     | 4 (×2)     | 72\*  | 36           | 11%   | Trust libs excellent; adapters/tasks/listener zero                               |
| traffology-\*   | 0          | 0     | 15           | 0%    | No tests at all                                                                  |

\* 142 reported by vitest due to D325 (dist/ double-run); actual unique tests = 72

**Priority ranking for new tests (risk × ease):**

1. **Resolver classification** (D326) — pure function, ~30 cases, minutes to write, high impact on omnivorous input correctness
2. **SSRF IP validation** (D328) — pure functions once exported, security-critical, hours
3. **Magic link lifecycle** (D327) — requires DB mock, auth entry point, hours
4. **RSS adapter + sanitiser** (D329) — pure functions, handles hostile internet input, hours
5. **Atproto/AP adapter normalisation** (D330) — pure functions, protocol fidelity, hours

### Session 7.2: Frontend test review

**Scope:** `web/tests/` (6 test files)

- [x] Run all 6 web test files; record pass/fail — **6/6 files pass, 75/75 tests pass (2.70s)**
- [x] Review what each test covers:
  - `format.test.ts` — date/number formatting — **20 tests. `formatDateRelative` (7 time-bucket cases with faked timers), `formatDateFromISO` (4 cases: today, yesterday, recent, older), `truncateText` (3 cases: short, word-boundary, exact), `stripMarkdown` (6 cases: headings, bold/italic, links, images, multi-newline, combined). Well-designed: frozen timestamps via `vi.useFakeTimers`, correct timer cleanup**
  - `markdown.test.ts` — markdown parsing — **17 tests. `renderMarkdownSync` (12 tests: headings, bold/italic, code, blockquotes, hr, safe links with rel, relative/hash links, XSS prevention via `javascript:` stripping, image fallback, Nostr npub/note/nevent→njump links, paragraph wrapping). `enhanceEmbedUrls` (5 tests: YouTube watch/short URLs→iframe, mixed-content paragraphs preserved, non-embeddable URLs untouched). Security-relevant XSS test is valuable**
  - `media.test.ts` — media URL extraction — **18 tests. `isEmbeddableUrl` (6 tests: YouTube, youtu.be, Vimeo, Twitter/X, Spotify, rejection), `isImageUrl` (5 tests: extensions, query params, Blossom hashes, rejection), `extractUrls` (3 tests: multi-URL, empty, http/https), `stripMediaUrls` (4 tests: image strip, embed strip, nostr strip, multiple). Good edge-case coverage**
  - `publish.test.ts` — Nostr event building — **5 tests. `generateDTag` slug generation: lowercase+hyphenated+timestamp suffix, special chars, collapsed spaces, long title truncation at 80 chars, parity with gateway's shared `generateDTag`. The gateway-parity test is the most valuable — catches web/shared slug drift**
  - `vault.test.ts` — encryption/decryption — **7 tests. `decryptVaultContentXChaCha` (4 tests: round-trip, unicode, wrong-key rejection, tampered-ciphertext rejection). `base64ToUint8Array` (3 tests: standard, URL-safe, unpadded). Crypto tests use real `@noble/ciphers` — no mocking, actual encrypt-then-decrypt round-trips. High-value tests for the paywall unlock path**
  - `voting.test.ts` — vote calculations — **8 tests. `voteCostPence` (5 tests: free first upvote, exponential upvote formula, downvote formula, downvote-n-equals-upvote-n+1 loop). `formatPence` (3 tests: zero→Free, sub-pound→Xp, pounds with/without pence). Isomorphic with shared/ — same formula, no parity test (unlike publish.test.ts)**
- [x] Identify critical frontend logic without tests:
  - API client error handling — **`web/src/lib/api/client.ts` (37 lines): thin `fetch` wrapper, throws `ApiError(status, body)` on non-2xx. No retry, no timeout. Testable without mocking DOM — just mock `fetch`. Low complexity, low risk. Not a test priority**
  - Zustand store behavior (compose, workspace) — **`compose.ts` (53 lines): trivial state machine — `open`/`close`/`setMode`/`openArticle` set predictable state. Not worth unit testing. `workspace.ts` (175 lines): localStorage persistence with debounced write, `readFromStorage` JSON parse with error handling, `scheduleWrite` with quota-exceeded catch, `hydrate` user-switching guard, `batchUpdatePositions` skip-if-unchanged optimisation. The workspace store is the only store with testable logic worth covering — specifically the hydrate→write→readback cycle and the batch no-op guard (D350)**
  - Resolver input debounce + polling — **`useResolverInput` (114 lines): debounce 300ms, generation counter for stale cancellation, poll with 1s delay up to 8 times. The generation-counter stale-cancellation pattern is subtle (3 `gen !== genRef.current` guards) and not testable without rendering a component or extracting the logic. However, the downstream `resolveMatches`/`matchToOptions`/`tagFallback` in `web/src/lib/workspace/resolve.ts` (86 lines) are pure functions and highly testable — they transform resolver API responses into UI options (D351)**
  - Reading position hook — **`useReadingPosition` (123 lines): scroll-save debounce 500ms, visibility-change flush, restore-from-API on mount, skip restore for hash links and near-top positions, beacon on pagehide. All browser-API-dependent (`window.scrollTo`, `document.visibilityState`, `requestAnimationFrame`). Would require jsdom/happy-dom environment — feasible but high-effort for low-risk logic (best-effort saves with catch-all swallows). Not a priority**
  - Feed layout (tier pairing, block construction) — **`layoutBlocks` in `FeedView.tsx` (25 lines): collects contiguous brief-tier articles into runs, pairs them two-up, emits remainder as singles, marks `leadsBriefRun` for 72px zone-break. Pure function, no DOM dependency. Currently embedded in the component file — extractable and testable with minimal effort. The pairing logic has an off-by-one risk (odd brief count leaves a solo) that's handled but untested (D352)**
  - Workspace collision resolution — **`resolveCollisions` in `collision.ts` (72 lines): BFS overlap resolution with floor-bounds clamping, 30-iteration safety cap. Pure function, no DOM dependency. Non-trivial algorithm with edge cases (chain cascades, boundary clamping, cycle prevention). Highest-value untested pure function in the frontend (D353)**
- [x] Assess component test feasibility (React Testing Library / Vitest) — **Current setup: Vitest 1.4 with `@` path alias, no test environment configured (defaults to Node). No `@testing-library/react`, no `jsdom`/`happy-dom` in devDependencies. Adding component tests would require: (1) `npm i -D @testing-library/react @testing-library/jest-dom happy-dom` (~3 packages), (2) add `environment: 'happy-dom'` to vitest config, (3) configure CSS module/Tailwind stubs. Feasibility: straightforward for isolated components (modals, stores via `renderHook`), harder for page-level components that depend on Next.js routing, NDK context, and API calls. Recommendation: don't add a DOM test environment now — the highest-value gaps are all pure functions extractable from components (layoutBlocks, resolveCollisions, resolveMatches). Test those in the existing Node environment first**

**Diagnoses & Remedies (7.2):**

| #    | Diagnosis                                                                                                                                                                                                                                 | Severity | Remedy                                                                                                                                                                                      | Effort  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D350 | **Workspace store untested.** `workspace.ts` has testable logic: localStorage round-trip, hydrate user-switching guard, batch no-op guard, quota-exceeded resilience. The store uses module-scoped `writeTimer` and `window.localStorage` | Low      | Export `readFromStorage`/`scheduleWrite` for direct testing, or test via `useWorkspace.getState()` with a localStorage mock. Low priority — logic is simple and failures are visible in UI  | Hours   |
| D351 | **Resolver match-mapping untested.** `resolveMatches`/`matchToOptions`/`tagFallback` in `web/src/lib/workspace/resolve.ts` are pure functions that transform resolver API responses into workspace UI options                             | Medium   | Write vitest tests for: native_account mapping, external_source mapping, rss_feed mapping, tag fallback for `#tag` input, dedup of tag fallback when already in results. ~15 cases, minutes | Minutes |
| D352 | **Feed `layoutBlocks` untested.** Pure function embedded in `FeedView.tsx` — pairs contiguous brief-tier articles, marks zone-breaks. Off-by-one on odd brief count is handled but unverified                                             | Low      | Extract `layoutBlocks` + `isBrief` to `web/src/lib/feed-layout.ts`, test: empty, all standard, all brief (even/odd count), mixed runs, single brief. ~10 cases                              | Minutes |
| D353 | **Workspace `resolveCollisions` untested.** BFS overlap resolution with floor-bounds clamping and 30-iteration safety cap. Non-trivial algorithm with chain-cascade and cycle-prevention edge cases                                       | Medium   | Write vitest tests for: no overlap (no-op), single overlap (push direction), chain cascade (A pushes B pushes C), boundary clamping, iteration cap, disjoint vessels. ~12 cases             | Hours   |
| D354 | **No `voteCostPence` parity test.** `voting.test.ts` tests the formula but unlike `publish.test.ts` doesn't verify parity with `shared/src/lib/voting.ts`. Both are isomorphic, but drift would cause UI/API cost mismatch                | Low      | Add parity assertion: import shared's `voteCostPence`, compare outputs for n=1..10 across up/down. Or verify the web copy is a re-export of shared (it's not — it's a reimplementation)     | Minutes |
| D355 | **`stripMarkdown` image handling is lossy.** Test documents that `![alt](url)` → `!alt` (the `!` leaks). Not a test gap — the test correctly documents the behavior — but the behavior itself is imperfect                                | Low      | Fix `stripMarkdown` to strip images before links: move the image regex before the link regex, or use a single-pass `!\[.*?\]\(.+?\)` replacement. Test already expects this                 | Minutes |

**Summary:**

- 6 files, 75 tests, all pass. Test quality is high: frozen timers, real crypto, XSS coverage, gateway-parity checks
- Coverage is focused on isomorphic utility functions (format, media, vault, voting, publish, markdown) — the right layer to test
- Highest-value untested code: `resolveCollisions` (D353, algorithmic complexity) and `resolveMatches` (D351, omnivorous input mapping). Both are pure functions testable in Node without DOM dependencies
- Component-level testing (React Testing Library) is feasible but not recommended yet — the pure-function gaps yield more value per effort
- No DOM test environment is configured; adding one is straightforward when needed but premature now

---

## Phase 8 — Cross-Cutting Concerns

### Session 8.1: Security review

**Scope:** Full codebase, focused on OWASP Top 10

- [x] **Injection**: SQL (parameterised queries throughout?), NoSQL, command injection, XSS (HTML sanitisation in feed-ingest, markdown rendering in web) — **SQL: mostly parameterised, 2 interpolation patterns to fix. XSS: feed sanitisation sound, 1 email template gap**
- [x] **Authentication**: session management, JWT lifecycle, magic link token entropy, Google OAuth state parameter — **JWT 30d/7d refresh correct, magic links 32-byte SHA-256 hashed, Google OAuth state HMAC+timingSafeEqual. 2 rate-limit gaps on verification endpoints**
- [x] **Sensitive data exposure**: check for secrets in logs (§79 DPoP/token leak fix), error messages, git history — **1 token logged in gift-links. Error responses are generic. .env files gitignored. No DPoP/OAuth token leaks**
- [x] **Access control**: verify all routes have appropriate auth middleware; check for IDOR vulnerabilities (user A accessing user B's data) — **All tested routes (drafts, reading-positions, notifications, linked-accounts) properly isolate by user. sessions_invalidated_at enforced. Public routes are intentionally open**
- [x] **SSRF**: verify all user-supplied URLs pass through the SSRF-hardened HTTP client; check for bypasses — **2 bypass paths: oEmbed proxy + traffology referrer resolver use raw fetch(). All feed-ingest/resolver/outbound paths use safeFetch/pinnedWebSocketOptions correctly**
- [x] **Cryptography**: verify all crypto operations use appropriate algorithms, key sizes, IV handling; check for timing attacks — **AES-256-GCM + XChaCha20-Poly1305 with random IVs throughout. Key versioning for rotation. 2 timing-vulnerable secret comparisons in internal services**
- [x] **Supply chain**: review high-severity npm audit findings; check for typosquatting in dependencies — **12 vulns: 1 critical (sanitize-html XSS), 5 high (fast-uri via fastify), 6 moderate. No typosquatting detected**
- [x] Cross-reference with `docs/audits/universal-feed-audit.md` (21 items resolved) and `docs/audits/FIX-PROGRAMME.md` (10 P0 items resolved) — **prior audit items confirmed resolved; new findings below are net-new**

**Diagnoses & Remedies (8.1):**

| #   | Diagnosis                                                                                                                                                               | Severity | Remedy                                                                                                                                                                                             | Effort  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D34 | **SSRF: oEmbed proxy** (`gateway/src/routes/media.ts:160`) uses raw `fetch()` — redirect from whitelisted endpoint can reach internal IPs                               | High     | Replace `fetch()` with `safeFetch()` from `shared/src/lib/http-client.ts`                                                                                                                          | Minutes |
| D35 | **SSRF: traffology referrer resolver** (`traffology-worker/src/tasks/resolve-source.ts:209`) uses raw `fetch()` on user-controlled referrer URL                         | High     | Replace `fetch()` with `safeFetch()` from shared HTTP client                                                                                                                                       | Minutes |
| D36 | **XSS in email: subscription emails** (`shared/src/lib/subscription-emails.ts`) interpolate `writerName`/`readerName` into HTML via `paragraph()` without escaping      | Medium   | Apply `escapeHtml()` (already exists in `publish-email-template.ts`) to all user-derived values before passing to `paragraph()`/`button()`                                                         | Minutes |
| D37 | **Rate limit: magic link verification** (`gateway/src/routes/auth.ts:114`) POST `/auth/verify` has no rate limiting — brute-force on token space                        | Medium   | Add `{ config: { rateLimit: { max: 10, timeWindow: '1 minute' } } }` to the route. Token entropy (32 bytes) makes brute-force impractical but defense-in-depth                                     | Minutes |
| D38 | **Rate limit: email change verification** (`gateway/src/routes/auth.ts:591`) POST `/auth/verify-email-change` has no rate limiting and no token expiry                  | Medium   | Add rate limiting + add `email_verification_token_expires_at` column with 1h TTL                                                                                                                   | Hours   |
| D39 | **Timing attack: internal secret** (`key-custody/src/routes/keypairs.ts:27`, `key-service/src/routes/keys.ts:190`) uses `!==` not `timingSafeEqual` for INTERNAL_SECRET | Low      | Replace `provided !== secret` with `timingSafeEqual(Buffer.from(provided), Buffer.from(secret))` + length guard. Risk is low (internal network only)                                               | Minutes |
| D40 | **Token logging** (`gateway/src/routes/gift-links.ts:61,185`) logs raw gift link tokens                                                                                 | Medium   | Remove `token` from logger.info payloads; log only `creatorId`, `articleId`, `maxRedemptions`                                                                                                      | Minutes |
| D41 | **SQL interpolation: LIMIT/OFFSET** (`gateway/src/routes/publications/cms.ts:84`) interpolates `parseInt`'d values into SQL instead of using `$n` params                | Low      | Change `LIMIT ${limit} OFFSET ${offset}` to `LIMIT $n OFFSET $m` with parameterised values. Currently safe due to `parseInt` but violates principle                                                | Minutes |
| D42 | **SQL interpolation: feeBps** (`gateway/src/routes/my-account.ts:147,268`, `publications/revenue.ts`) interpolates db-sourced integer into SQL                          | Low      | Parameterise with `$n`. Currently safe (value from `platform_config` not user input) but better practice                                                                                           | Minutes |
| D43 | **Google ID token: no signature verification** (`gateway/src/routes/google-auth.ts:210-233`) decodes JWT payload without verifying RS256 signature                      | Low      | Token is received server-to-server over HTTPS from Google's token endpoint — signature verification is defense-in-depth, not critical. Consider adding `google-auth-library` for full verification | Hours   |
| D44 | **sanitize-html critical XSS** (GHSA-rpr9-rxv7-x643) ≤2.17.3 — `xmp` raw-text passthrough                                                                               | Critical | `npm audit fix` — non-breaking patch available. **Same as D1, still unresolved**                                                                                                                   | Minutes |

### Session 8.2: Performance review

**Scope:** Database queries, API response times, frontend bundle

- [x] **N+1 queries**: grep for query-in-loop patterns across gateway routes — **13 genuine N+1 patterns found across gateway (7), payment-service (3), feed-ingest (3). Most are unbounded loops with INSERT/UPDATE per iteration. Highest-risk: drives.ts (4 queries × N pledges), payout.ts (SELECT+UPDATE per split), tags.ts (INSERT per tag)**
- [x] **Missing indexes**: cross-reference with Session 1.2 findings — **Session 1.2 findings confirmed. Migration 083 (content_free trigram) staged. D63 explore feed tiebreaker index still pending. D54/D55/D56 FK indexes still pending. search.ts content_free ILIKE falls back to seq scan without 083**
- [x] **Large queries**: review `timeline.ts` feed query (single-table scan + LEFT JOINs), `search.ts` (pg_trgm), `trust.ts` (multi-join) — **timeline.ts well-designed (single-table fan-out + 6 LEFT JOINs, parallel explore queries). Correlated tag subquery per article row is the main cost. search.ts has 3 correlated COUNT subqueries per result row. trust.ts GET fires 5–6 serial queries per profile view (classic waterfall). feeds.ts sourceFilteredItems has 4-way OR JOIN (D64 confirmed)**
- [x] **Connection pooling**: verify pg pool settings across all services sharing the database — **Single shared pool (max=20, idle 30s, statement_timeout 10s) imported by all services. 7 Docker containers each instantiate their own pool → 7×20 = 140 potential connections. PostgreSQL default max_connections=100. No DB_POOL_MAX override in docker-compose. Pool exhaustion risk under load**
- [x] **Frontend bundle**: check `next build` output for oversized chunks; verify code splitting for route-based lazy loading — **No bundle analyzer configured. No explicit code splitting beyond Next.js defaults. TipTap (7 packages) loaded on every page that imports the compose overlay (global layout mount). No dynamic import for editor. No build output to analyse (need `next build` run)**
- [x] **Cron job timing**: verify cron schedules don't overlap or stampede (feed_items_reconcile 05:00, feed_items_author_refresh 04:00, trust_layer1_refresh 01:00, etc.) — **22 scheduled tasks across 5 services. 02:00 UTC stampede: trust_epoch_aggregate (heavy, quarterly full 2–5min / Mon+Thu mop-up 30–60s) + external_items_prune + payout cycle all overlap. 01:00 UTC Monday: trust_layer1_refresh + aggregate_weekly overlap. feed_scores_refresh every 5min → full-table UPDATE on feed_items (D70)**
- [x] **Media pipeline**: verify image upload processing, Sharp usage, caching headers — **Sharp pipeline sound: EXIF rotation, 1200px resize, WebP q80, SHA-256 content-addressed dedup. Nginx serves /media/ with `expires 1y` + `public, immutable` (correct). No width/height or blur placeholder in upload response (client can't reserve layout space). No API response caching anywhere — no Cache-Control, ETag, or Last-Modified on feed/search/trust endpoints**

**Diagnoses & Remedies (8.2):**

| #    | Diagnosis                                                                                                                                                                                                                                     | Severity | Remedy                                                                                                                                                                                           | Effort  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| D300 | **Pool exhaustion: 7 containers × max 20 = 140 potential connections vs PostgreSQL default max_connections=100.** No `DB_POOL_MAX` override. Under load all containers could saturate the database                                            | High     | Either: (a) set `DB_POOL_MAX=10` for non-gateway services in docker-compose, or (b) add `command: postgres -c max_connections=200` to the postgres service. Option (a) is safer                  | Minutes |
| D301 | **Trust profile waterfall: `GET /trust/:userId` fires 5–6 sequential queries** (L1, L2 dimensions, live fallback, encounter count, endorsements, network endorsements + viewer vouches). ~6 round-trips per profile view                      | Medium   | Combine L1 + L2 + encounter into one query with LEFT JOINs. Combine endorsements + network endorsements with conditional aggregation. Reduces to 2–3 queries                                     | Hours   |
| D302 | **Correlated tag subquery per row in feed SELECT.** `COALESCE((SELECT array_agg(...) FROM article_tags ... WHERE at2.article_id = a.id), '{}')` executes per article row in both timeline.ts and feeds.ts                                     | Medium   | Replace with a LATERAL join or pre-aggregate tags in a CTE. At current scale (20–50 rows per page) the cost is tolerable but grows linearly with LIMIT                                           | Hours   |
| D303 | **Search correlated COUNTs: `searchWriters` and `searchPublications`** each embed `(SELECT COUNT(*) ...)` subqueries per result row (article_count, member_count). Scans articles/publication_members per row                                 | Low      | Replace with LEFT JOIN + GROUP BY, or materialise counts in a denormalised column updated by trigger. Low-severity because search is rate-limited (30/min) and LIMIT 50                          | Hours   |
| D304 | **N+1 in `tags.ts`: two loops** — first `INSERT INTO tags` per tag name (line 163–168), then `INSERT INTO article_tags` per tag (line 177–182). Both should be batch inserts with `unnest($1::text[])`                                        | Low      | Batch: `INSERT INTO tags (name) SELECT unnest($1::text[]) ON CONFLICT DO NOTHING`, then `INSERT INTO article_tags ... SELECT ... FROM unnest($1::uuid[], $2::uuid[])`. Bounded by tag count      | Minutes |
| D305 | **N+1 in `drives.ts`: 4 queries per pledge** in settlement loop (read_events INSERT, article_unlocks INSERT, reading_tabs UPDATE, pledges UPDATE) at lines 760–792. Already batched by outer loop but still N×4 per batch                     | Medium   | Use CTE-based batch: single INSERT ... SELECT for read_events and article_unlocks across the batch; single UPDATE ... FROM for reading_tabs and pledges. Cuts pledge-batch queries from N×4 to 4 | Hours   |
| D306 | **N+1 in `payout.ts`: SELECT + UPDATE per split** at line 878–913. Queries stripe_connect_id per split when all splits could be pre-fetched in one query                                                                                      | Medium   | Pre-fetch all account rows with `WHERE id = ANY($1::uuid[])` before the loop, index into a Map                                                                                                   | Minutes |
| D307 | **N+1 in `messages.ts`: INSERT per member** (line 83–89) and `replies.ts`: INSERT per mentioned user (line 205–217). Both are notification/membership inserts in loops                                                                        | Low      | Batch INSERTs with `unnest()`. Low-severity because conversation membership and mention sets are typically small (2–10)                                                                          | Minutes |
| D308 | **N+1 in feed-ingest `feed-ingest-nostr.ts`: 2 queries per deleted event** in nested loops (lines 328–384). UPDATE external_items + UPDATE feed_items per deletedId and per aTagAddr                                                          | Medium   | Use `WHERE source_item_uri = ANY($1::text[])` batch UPDATE for each table. Deletion events can arrive in bursts                                                                                  | Hours   |
| D309 | **02:00 UTC cron stampede.** `trust_epoch_aggregate` (heavy: 2–5min quarterly, 30–60s Mon/Thu), `external_items_prune`, and payment-service payout cycle all fire at 02:00 UTC. On quarterly epoch dates, total concurrent DB load is extreme | Medium   | Stagger: move payout to 02:30, move `external_items_prune` to 02:15. Quarterly epoch stays at 02:00 (needs quiet DB); the other two adapt around it                                              | Minutes |
| D310 | **01:00 UTC Monday overlap.** `trust_layer1_refresh` (15–45s) and traffology `aggregate_weekly` (30–90s) both fire at 01:00 UTC on Mondays. Different table sets but competes for I/O                                                         | Low      | Move `aggregate_weekly` to `0 1 * * 2` (Tuesdays) or `30 1 * * 1` (01:30 Monday). Minimal code change                                                                                            | Minutes |
| D311 | **No API response caching.** No Cache-Control, ETag, or Last-Modified on any authenticated endpoint. Public endpoints (RSS feeds, well-known JSON) have caching. Explore feed and trust profiles are candidates for short-TTL caching         | Medium   | Add `Cache-Control: private, max-age=60` to explore feed. Add `Cache-Control: private, max-age=300` to trust profiles. Consider nginx proxy_cache for public RSS                                 | Hours   |
| D312 | **No nginx proxy_cache layer.** All API requests pass through to the gateway on every hit. The RSS endpoints set `Cache-Control: public, max-age=600` but nginx doesn't cache them — the header is only for downstream CDNs/browsers          | Low      | Add `proxy_cache_path` directive to nginx.conf and cache RSS + well-known endpoints. Low priority until traffic warrants it                                                                      | Hours   |
| D313 | **TipTap editor in global layout.** ComposeOverlay is mounted in `app/layout.tsx` which imports TipTap (7 packages, ~200KB+ gzipped). Every page load pays the TipTap parse+eval cost even if the user never opens compose                    | Medium   | Wrap ComposeOverlay in `dynamic(() => import(...), { ssr: false })`. TipTap only loads when the overlay opens. Saves ~200KB on initial page load                                                 | Hours   |
| D314 | **No bundle analyzer.** No `@next/bundle-analyzer` configured. Can't measure chunk sizes, identify heavy imports, or verify code splitting without running `ANALYZE=true next build`                                                          | Low      | `npm i -D @next/bundle-analyzer`, wrap next.config.js with the analyzer when `ANALYZE=true`. One-time setup                                                                                      | Minutes |
| D315 | **Media upload returns no dimensions.** Sharp processes the image but the response only includes `url`, `sha256`, `mimeType`, `size`. No `width`/`height` for the client to reserve layout space (prevents CLS)                               | Low      | Add `sharp.metadata()` call after processing to get width/height; include in response and store in `media_uploads`                                                                               | Minutes |
| D316 | **feed_scores_refresh UPDATE storm.** Every 5 minutes, scores are bulk-written to `feed_items.score` for the 48h window. Each UPDATE generates WAL + dead tuples across the full wide row. Confirms D70                                       | Medium   | Move scores to a separate `feed_item_scores(feed_item_id, score)` table. The 5-min UPDATE touches a narrow table; the feed query adds one more JOIN but dead-tuple churn drops dramatically      | Hours   |

### Session 8.3: Dead code & dependency cleanup

**Scope:** Full codebase

- [x] Run knip with verbose output; review every finding — **Exceptionally clean: 1 unused exported type (`PipStatus` in `feed-ingest/src/lib/trust-pip.ts`). The web frontend defines its own `PipStatus` in `web/src/lib/ndk.ts` — the feed-ingest copy is only used internally via `composePipStatus` return type, so the export is genuinely unnecessary. 3 stale `ignoreBinaries` entries in knip.json (`next`, `vitest`, `tsx`). Zero unused files, zero unused dependencies via knip**
- [x] Verify deprecated `NoteComposer` is truly unused; remove if so — **Already deleted. File `web/src/components/feed/NoteComposer.tsx` does not exist. Zero imports or references across the entire codebase. CLAUDE.md documents it as "deprecated — kept in codebase but no longer imported anywhere" which is now stale — the file is gone, not just unused**
- [x] Check `VesselCard.tsx` deprecation marker — is this intentional or stale? — **Not deprecated. VesselCard is actively used by WorkspaceView.tsx (imported and rendered for every workspace feed item). The comment on line 73 ("kept local to avoid pulling in the deprecated card module") refers to avoiding imports from the _old_ card family, not to VesselCard itself being deprecated. No action needed**
- [x] Check `web/src/lib/api/feeds.ts` deprecation marker — **Not deprecated. The comment at line 157 mentions "the BookmarkButton retires with the deprecated chassis on merge" — it's documenting that the workspace's save surface replaces BookmarkButton when the experiment merges. The file itself is active workspace API client code. No action needed**
- [x] Review `key-service/src/types/index.ts` deprecation — what's deprecated and why? — **`nostrVaultEvent` field (kind 39701) is deprecated. The key-service still constructs and returns it in every vault encrypt response (`vault.ts:107–119`, `keys.ts:81`), but the gateway has zero references to this field — it's dead payload. The web client reads ciphertext directly from the NIP-23 event's payload tag (new format). Safe to remove the field from the response and stop constructing the vault event object (D317)**
- [x] Identify shared functions with divergent implementations (e.g., slug generation previously had 4 copies — verify consolidation to `shared/src/lib/slug.ts`) — **Three divergent function families found: (1) `slugify`: canonical in `shared/src/lib/slug.ts`, mirrored in `web/src/lib/publish.ts` (necessary — web can't import shared), standalone in `scripts/seed.ts` (simplified — no maxLen param, no diacritics, OK for seed data). Web mirror is tested for parity in `web/tests/publish.test.ts`. (2) `escapeHtml`: 3 copies — `shared/src/lib/publish-email-template.ts`, `web/src/lib/traffology-templates.ts`, `feed-ingest/src/adapters/atproto.ts`. All functionally identical (escape &<>") but atproto version uses single-pass regex. The shared copy is module-private (not exported), so each consumer reimplements. Should export from shared and import in feed-ingest; web copy is unavoidable (D318). (3) `truncateText` (web) vs `truncatePreview` (shared) — different functions for different purposes (word-boundary truncation vs code-point-aware Postgres-compatible). Not divergent, just confusingly named**
- [x] Check for unused npm dependencies across all workspaces — **Ran depcheck across all 9 workspaces. Results after triage: (1) `jose` in gateway — false positive, used transitively via `shared/src/auth/session.ts` (SignJWT, jwtVerify). Declared in both shared and gateway package.json; gateway's copy is redundant but harmless (D319). (2) `nostr-tools` in shared — false positive, imported by gateway, payment-service, feed-ingest, key-service, key-custody directly (not via shared). shared/package.json listing is the redundant one since shared doesn't import it (D320). (3) `pg` in gateway and traffology-ingest — false positive, used transitively via shared's db client. (4) `pino` in gateway, feed-ingest, traffology-worker — false positive, used transitively via shared's logger. (5) `@atproto/oauth-client-node` in gateway and feed-ingest — false positive, gateway uses it via shared; feed-ingest's listing may be redundant but it references the types. (6) `pino-pretty` in shared — false positive, used in `shared/src/lib/logger.ts`. Overall: 2 genuinely redundant dependency listings (D319, D320), no truly unused packages**
- [x] Check for unused database tables or columns (compare schema.sql against actual query usage) — **Scanned all 66 tables against .ts/.tsx source (excluding migrations). Two traffology tables have zero source references: `traffology.nostr_events` and `traffology.public_mentions` (created in migration 040). These are Phase 2/3 tables from the traffology spec — the ingest pipeline for Nostr social signals and web mentions was designed but never implemented. The tables are empty, unindexed beyond the initial migration, and have no INSERT/SELECT in any service code. They represent planned-but-unbuilt traffology features (D321). All other tables have active queries. No orphaned columns detected beyond the schema.sql drift already captured in D53**

**Diagnoses & Remedies (8.3):**

| #    | Diagnosis                                                                                                                                                                                               | Severity | Remedy                                                                                                                                                          | Effort  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D317 | **Dead payload: `nostrVaultEvent` (kind 39701) still constructed in key-service vault encrypt but no consumer reads it.** Gateway, web, and all other callers use ciphertext directly from NIP-23 tag   | Low      | Remove `nostrVaultEvent` from `VaultEncryptResult` type, stop constructing the object in `vault.ts`, remove from `keys.ts` response. No callers to update       | Minutes |
| D318 | **`escapeHtml` reimplemented 3× across shared, web, and feed-ingest.** All functionally identical but the shared copy is module-private (not exported)                                                  | Low      | Export `escapeHtml` from `shared/src/lib/text.ts` (or new `shared/src/lib/escape.ts`). Import in feed-ingest. Web copy stays (can't import shared from Next.js) | Minutes |
| D319 | **Redundant `jose` in gateway/package.json.** Gateway never imports jose directly — it's used via shared's session module which already declares jose                                                   | Low      | Remove `jose` from gateway/package.json. Verify `npm ls jose` still resolves for gateway at runtime                                                             | Minutes |
| D320 | **Redundant `nostr-tools` in shared/package.json.** Shared never imports nostr-tools directly — every consumer (gateway, payment-service, feed-ingest, key-service, key-custody) imports it itself      | Low      | Remove `nostr-tools` from shared/package.json. Verify no transitive breakage                                                                                    | Minutes |
| D321 | **Two empty, unreferenced traffology tables: `traffology.nostr_events` and `traffology.public_mentions`.** Created in migration 040 for planned Nostr social signal and web mention ingest, never built | Low      | Leave in place — dropping tables requires a migration and the tables have zero cost when empty. Document as "reserved for Phase 2/3" in schema docs             | Minutes |
| D322 | **Unused exported type: `PipStatus` in `feed-ingest/src/lib/trust-pip.ts:33`.** Only used internally via `composePipStatus` return type; web has its own `PipStatus` in `ndk.ts`                        | Low      | Remove the `export` keyword. The type is inferred from the function return                                                                                      | Minutes |
| D323 | **CLAUDE.md stale: says NoteComposer is "kept in codebase but no longer imported anywhere".** File is already deleted                                                                                   | Low      | Update CLAUDE.md to remove the NoteComposer deprecation note                                                                                                    | Minutes |
| D324 | **3 stale `ignoreBinaries` entries in knip.json** (`next`, `vitest`, `tsx`). Knip now auto-detects these                                                                                                | Low      | Remove the three entries from knip.json                                                                                                                         | Minutes |

---

## Phase 9 — Documentation & Spec Hygiene

### Session 9.1: ADR and spec reconciliation

**Goal:** Bring all documentation into alignment with the codebase.

- [x] Archive completed ADRs that are now fully implemented and stable (candidates: `RELAY-OUTBOX-ADR.md` all phases complete, `GATEWAY-DECOMPOSITION.md` if extraction is stable) — **3 archived: RELAY-OUTBOX-ADR.md, RELAY-OUTBOX-PHASE-4-ADR.md, ALLHAUS-ADR-UNIFIED.md (superseded). GATEWAY-DECOMPOSITION.md kept live (Steps 2–3 still outstanding)**
- [x] Update `ALLHAUS-REDESIGN-SPEC.md` to reflect Steps 1–6 shipped status with remaining items clearly marked — **Status header updated to 2026-05-15, 3 stale ⏳ TODO markers corrected to ✅ SHIPPED (article mode, article auto-save on dismiss, playscript threads), workspace experiment cross-reference added**
- [x] Update `feature-debt.md` to reflect any items resolved during this review — **Last-audited date → 2026-05-15, frontend audit 4/12 → 6/12, workspace slices 23 → 32, JWT lifetime spec corrected (30d/7d not 2h/1h), session invalidation description updated**
- [x] Verify `DEPLOYMENT.md` environment variable list is complete — **Fixed IP_HASH_SALT name mismatch (docs said TRAFFOLOGY_IP_HASH_SALT, code uses IP_HASH_SALT). Added 10 missing env vars (GATEWAY_URL, GATEWAY_INTERNAL_URL, POSTMARK_API_KEY, RESEND_API_KEY, EMAIL_FROM, STRIPE_WEBHOOK_SECRET, MEDIA_DIR, PUBLIC_MEDIA_URL, LINKED_ACCOUNT_KEY_VERSION, plus NEXT_PUBLIC_RELAY_URL confirmed stale)**
- [x] Check for stale cross-references between documents (e.g., docs referencing deleted files or renamed routes) — **Fixed: CLAUDE.md relay outbox paths → planning-archive/, ALLHAUS-OMNIBUS.md supersedes path → planning-archive/, REDESIGN-SCOPE.md workspace-superseded note added, WORKSPACE-MIGRATION-MAP.md missing-design-corpus note added. Identified but left as-is: historical log entries in feature-debt.md, source-file renames in audit docs (gateway/src/services/access.ts → article-access/, subscriptions.ts split), 6 un-committed design-corpus docs referenced from workspace ADRs**
- [x] Verify `WORKSPACE-MIGRATION-MAP.md` verdicts are current — **All verdicts consistent with codebase. Key checks: /write survives ✓, /feed coexists ✓, ComposeOverlay coexists ✓, NoteComposer removed (was deprecated) ✓. Slice-by-slice detail deferred to WORKSPACE-EXPERIMENT-ADR.md**
- [x] Reconcile `docs/audits/SUBSCRIPTIONS-GAP-ANALYSIS.md` with current subscription route implementation — **Phase 1 complete (all 5 MVP steps + 2 should-include items resolved). Phase 2 not started (free trials, gift subs purchaser→recipient, welcome email, import/export, growth analytics, /[username]/subscribe). Reconciliation status header added to the doc**
- [x] Review `docs/audits/all-haus-frontend-audit.md` — update the 4/12 resolved count — **Now 6/12 resolved. Newly marked: #2 email delivery (sendPublishNotifications), #4 writer analytics (Traffology Phase 1), #10 dashboard architecture (10+ extracted components). #9 (tests) and #12 (design system) partially addressed but not fully resolved**

**Diagnoses & Remedies (9.1):**

| #    | Diagnosis                                                                                                                                   | Severity | Remedy                                                                                             | Effort  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------- | ------- |
| D325 | `IP_HASH_SALT` env var name mismatch — DEPLOYMENT.md documented `TRAFFOLOGY_IP_HASH_SALT` but code uses `IP_HASH_SALT`                      | Medium   | Fixed: DEPLOYMENT.md corrected to match code                                                       | Minutes |
| D326 | 10 env vars used in code but missing from DEPLOYMENT.md (GATEWAY_URL, STRIPE_WEBHOOK_SECRET, POSTMARK_API_KEY, etc.)                        | Medium   | Fixed: added to DEPLOYMENT.md env var table                                                        | Minutes |
| D327 | `NEXT_PUBLIC_RELAY_URL` in `web/.env.example` but unused in code — vestigial from when NDK relay config was env-driven                      | Low      | Remove from `.env.example` (no runtime impact)                                                     | Minutes |
| D328 | 3 stale ⏳ TODO markers in ALLHAUS-REDESIGN-SPEC.md for features that shipped (article compose mode, article auto-save, playscript threads) | Low      | Fixed: corrected to ✅ SHIPPED                                                                     | Minutes |
| D329 | REDESIGN-SCOPE.md anti-workspace stance not annotated as partially superseded by workspace experiment                                       | Low      | Fixed: added note referencing WORKSPACE-EXPERIMENT-ADR.md                                          | Minutes |
| D330 | 6 design-corpus companion docs referenced from WORKSPACE-MIGRATION-MAP.md and WORKSPACE-EXPERIMENT-ADR.md were never committed to the repo  | Low      | Added explanatory note to WORKSPACE-MIGRATION-MAP.md. ADRs are self-contained                      | Minutes |
| D331 | `gateway/src/services/access.ts` referenced in 5+ audit/spec docs — refactored into `article-access/` directory                             | Low      | Historical references left as-is (audit snapshots). Living code paths verified correct             | —       |
| D332 | feature-debt.md JWT lifetime description contradicted implementation (said 2h, actual is 30d)                                               | Low      | Fixed: corrected to match implementation (30d JWT, 7d refresh, sessions_invalidated_at revocation) | Minutes |
| D333 | Subscriptions gap analysis had no reconciliation status despite Phase 1 being fully complete                                                | Low      | Fixed: added status header documenting Phase 1 complete, Phase 2 outstanding                       | Minutes |
| D334 | Frontend audit resolved count stale (4/12 vs actual 6/12) — items 2 (email), 4 (analytics), 10 (dashboard) not marked resolved              | Low      | Fixed: marked resolved with implementation notes                                                   | Minutes |

### Session 9.2: Schema documentation

**Goal:** Ensure the database schema is self-documenting.

- [x] Verify `schema.sql` is the authoritative schema dump (not stale) — **Regenerated via `pg_dump` from fully-migrated database (all 84 migrations applied). Old file covered 001–056 structurally with header claiming 001–038; new file is authoritative through 084. Validated by loading into a fresh test database with zero errors. 83 tables, 151 indexes, 157 FKs, 50 CHECK constraints, 8 triggers. Migration 083 `CONCURRENTLY` → non-concurrent (cannot run inside runner's transaction). D53 resolved**
- [x] Add SQL comments to tables/columns where the purpose isn't obvious from the name — **82 COMMENT ON TABLE statements added covering all tables (public + traffology). Comments document: business purpose, data flow relationships (dual-write targets, cron sources), security properties (encrypted at rest, hashed), and lifecycle (reserved for future phase). Traffology schema gets a COMMENT ON SCHEMA explaining its isolation purpose**
- [x] Document all CHECK constraints, partial indexes, and triggers in a schema reference — **Output: `docs/audits/SCHEMA-REFERENCE-2026-05.md`. Catalogues 50 CHECK constraints by domain, all partial indexes with supported query patterns, all 8 triggers with behavior, and notable FK patterns including the 37 FKs lacking ON DELETE action**
- [x] Verify all foreign key relationships are correctly documented — **157 FKs verified. 37 lack ON DELETE action (default RESTRICT). Key findings confirmed from D57–D59: articles/article_drafts/subscriptions on publication_id, vouches attestor_id/subject_id, trust_layer1/trust_profiles user_id. Cross-schema FKs (traffology → public.accounts) all use ON DELETE CASCADE. No orphaned or incorrect FK references found**
- [x] Cross-reference table documentation with ADR data model sections — **Checked ALLHAUS-OMNIBUS.md (trust), UNIVERSAL-FEED-ADR.md (external feeds), WORKSPACE-EXPERIMENT-ADR.md (workspace), RELAY-OUTBOX-ADR.md (outbox). All core tables conformant. Known divergences already captured in Session 0.2 (D14 content_html JOIN vs column, D15 Jetstream threshold, D17 drive_deletion entity type). No new divergences found**

**Diagnoses & Remedies (9.2):**

| #    | Diagnosis                                                                                                                                                                    | Severity | Remedy                                                                                                 | Effort  |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------ | ------- |
| D335 | `schema.sql` was 28 migrations behind (covered 001–056, actual 001–084). Header said "001–038". Fresh deploys from the old file would produce an incomplete database         | High     | Fixed: regenerated via `pg_dump` from fully-migrated database. Now covers all 84 migrations            | Done    |
| D336 | Migration 083 uses `CREATE INDEX CONCURRENTLY` inside runner's transaction — will fail on apply                                                                              | Medium   | Fixed: removed `CONCURRENTLY` with comment explaining the constraint. Production should apply manually | Done    |
| D337 | DEPLOYMENT.md §4 claimed schema.sql covers "through migration 045" — inaccurate                                                                                              | Low      | Fixed: updated to reference migration 084 with date                                                    | Done    |
| D338 | CLAUDE.md still referenced NoteComposer as "deprecated — kept in codebase" when the file was already deleted                                                                 | Low      | Fixed: updated to reflect deletion                                                                     | Done    |
| D339 | 37 FKs lack ON DELETE action (default RESTRICT). Most are intentional but articles/subscriptions on publication_id, vouches, trust tables are problematic (confirms D57–D59) | Medium   | Documented in schema reference. Remedies specified in Session 1.2 (D57–D59) — requires migration       | Hours   |
| D340 | 8 tables have `updated_at` without trigger (confirms D65). `set_updated_at()` function exists; just needs CREATE TRIGGER statements                                          | Low      | Single migration with 8 CREATE TRIGGER statements. Function already defined                            | Minutes |

**Full schema reference:** `docs/audits/SCHEMA-REFERENCE-2026-05.md`

---

## Priority & Sequencing Summary

| Priority   | Sessions                                         | Rationale                                 |
| ---------- | ------------------------------------------------ | ----------------------------------------- |
| **Urgent** | 0.1, 0.2, 8.1                                    | Baseline + security before any changes    |
| **High**   | 1.1, 1.2, 2.2, 2.3, 3.1, 3.2                     | Shared foundation, money paths, crypto    |
| **Medium** | 2.1, 2.4, 3.3, 4.1, 4.2, 4.3, 4.4, 6.2           | Core feature surfaces, CI gap             |
| **Normal** | 3.4, 4.5, 5.1, 5.2, 5.3, 6.1, 7.1, 7.2, 8.2, 8.3 | Completeness, workspace decision          |
| **Low**    | 9.1, 9.2                                         | Documentation — valuable but not blocking |

**Hard dependencies:**

- Session 0.1 before all others (establishes metrics)
- Session 0.2 before Phase 2–4 sessions (identifies ADR divergences to check)
- Session 1.2 before Session 8.2 (index findings feed performance review)
- Sessions 5.1 + 5.2 before Session 5.3 (merge readiness needs review findings)

**Recommended attack order for a single reviewer:**

1. 0.1 → 0.2 (orientation, 1 day)
2. 8.1 → 1.1 → 3.2 (security-critical paths, 1 day)
3. 1.2 → 3.1 → 2.2 (data layer + money + content, 1 day)
4. 2.3 → 2.4 → 2.1 (remaining gateway, 1 day)
5. 3.3 → 3.4 (workers + analytics, 1 day)
6. 4.1 → 4.2 → 4.3 → 4.4 → 4.5 (frontend, 2 days)
7. 5.1 → 5.2 → 5.3 (workspace, 1 day)
8. 6.1 → 6.2 → 7.1 → 7.2 (infra + tests, 1 day)
9. 8.2 → 8.3 → 9.1 → 9.2 (performance + cleanup + docs, 1 day)

**Total estimated effort:** 24 sessions across 9–10 working days.

---

## Verification Pass (2026-05-16)

30 of the highest-severity diagnoses were checked against the current codebase (including uncommitted working-tree changes). Roughly half had already been fixed during or after the review sessions via the unstaged working-tree changes documented in D259.

### Already resolved in working tree (do NOT attempt to fix — just commit)

These diagnoses describe real problems that **have already been fixed** in the uncommitted working tree. The fix plan should commit these, not re-implement them.

| Diagnosis | Claimed issue                                          | Actual state                                                              |
| --------- | ------------------------------------------------------ | ------------------------------------------------------------------------- |
| D94       | Publication unpublish doesn't remove feed_items        | Unpublish route has `DELETE FROM feed_items WHERE article_id = $1`        |
| D114      | Layer 4 query uses `followed_id`                       | Column is already `followee_id`                                           |
| D116      | Blocks/mutes use `a.avatar`                            | Both queries use `a.avatar_blossom_url`                                   |
| D150      | Missing `fi.title` in FEED_SELECT                      | `fi.title` is present on line 132 of timeline.ts                          |
| D170      | `optionalAuth` skips status/invalidation checks        | `optionalAuth` now performs identical checks, clears session on failure   |
| D171      | Identity headers not stripped                          | `stripIdentityHeaders` exported and registered as global `onRequest` hook |
| D175      | Email change token stored plaintext, no expiry         | Token is SHA-256 hashed; 24h TTL via `email_verification_requested_at`    |
| D178      | Google OAuth bypasses account status check             | Explicit `status !== 'active'` → 403 check present                        |
| D180      | Account deletion hard-deletes row                      | Uses `UPDATE accounts SET status = 'deleted'` (soft-delete)               |
| D184      | Scheduler `FOR UPDATE SKIP LOCKED` outside transaction | Wrapped in `withTransaction` with claim-and-update pattern                |
| D188      | Missing `x-internal-token` on card-connected call      | `X-Internal-Token` header is present                                      |
| D159      | Bluesky callback uses cookie userId blindly            | Cross-checks `statePayload.userId` against `req.session!.sub!`            |
| D210      | Migration 083 uses CONCURRENTLY                        | `CONCURRENTLY` already removed with explanatory comment                   |
| D253      | `crossPost` → `crossPosts` breaking API change         | Both gateway and frontend already use `crossPosts` (plural)               |

### Confirmed — still need fixing

| Diagnosis  | Severity | Summary                                                                        |
| ---------- | -------- | ------------------------------------------------------------------------------ |
| D76        | Critical | Settlement: Stripe call inside DB transaction                                  |
| D77        | Critical | Settlement: random UUID idempotency key                                        |
| D91        | High     | Gate-pass fetches deleted/unpublished articles                                 |
| D92        | High     | Key proxy doesn't inject `x-reader-id` from session                            |
| D34 (8.1)  | High     | oEmbed proxy uses raw `fetch()` (SSRF)                                         |
| D35 (3.4)  | High     | Traffology referrer resolver uses raw `fetch()` (SSRF)                         |
| D34 (3.4)  | Critical | `ROW_NUMBER() FILTER` syntax error — all traffology hourly aggregation broken  |
| D222       | High     | ActivityPub media URL accepts `javascript:`/`data:` schemes                    |
| D402       | High     | CSP missing Stripe.js directives                                               |
| D36 (8.1)  | Medium   | Subscription email XSS — unescaped display names                               |
| D100       | Medium   | CDATA injection in RSS `content:encoded`                                       |
| D203       | High     | `sampling_mode` maps `"random"` to two different DB values                     |
| D333       | Critical | Scheduled articles lose paywall split (gate marker stripped before draft save) |
| D334       | High     | Gate marker not reconstructed on draft load (missing parse rule)               |
| D336       | High     | Edit mode loads only `contentFree` — paid content discarded                    |
| D300 (8.2) | High     | Pool exhaustion: 7×20=140 connections vs PG default 100                        |

### Partially correct — fix needed but mechanism differs

| Diagnosis | Claimed                                      | Actual                                                                                                                            |
| --------- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| D316      | `publishNote` doesn't accept crossPosts      | `publishNote` does accept crossPosts; the break is `ComposeOverlay.handlePost` not passing them                                   |
| D334      | `html: false` entity-escapes the gate marker | Custom serialiser bypasses `html: false`; real issue is `parse: {}` — no parser rule to reconstruct gate node from stored comment |

---

## Fix Slices

Each slice is scoped for a single context window. Slices are ordered by priority (security → money → data correctness → UX → polish). Within each slice, items are grouped by file proximity to minimize context switching.

### Slice 1: Security — SSRF, XSS, injection (gateway, shared, feed-ingest, traffology-worker)

**Priority: URGENT. Estimated effort: 1 hour.**

All items are one-line or few-line fixes with a clear pattern.

| #   | Diagnosis  | File                                            | Fix                                                                                                                       |
| --- | ---------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 1   | D1/D44     | root                                            | `npm audit fix` — sanitize-html patch                                                                                     |
| 2   | D34 (8.1)  | `gateway/src/routes/media.ts`                   | Replace `fetch()` with `safeFetch()` in oEmbed proxy                                                                      |
| 3   | D35 (3.4)  | `traffology-worker/src/tasks/resolve-source.ts` | Replace `fetch()` with `safeFetch()` from shared HTTP client                                                              |
| 4   | D36 (8.1)  | `shared/src/lib/subscription-emails.ts`         | Apply `escapeHtml()` to `writerName`/`readerName` before HTML interpolation                                               |
| 5   | D100       | `gateway/src/routes/rss.ts`                     | Replace `]]>` in content with `]]]]><![CDATA[>` before CDATA interpolation                                                |
| 6   | D222       | `feed-ingest/src/adapters/activitypub.ts`       | Add `if (!/^https?:\/\//i.test(url)) continue` before media push. Also check `rss.ts` and `feed-ingest-nostr.ts` adapters |
| 7   | D211 (3.3) | `feed-ingest/src/lib/sanitize.ts`               | Add `allowProtocolRelative: false` to sanitize-html options                                                               |

**Verification:** Run existing tests (`npm test` in shared, gateway, feed-ingest). Manually test oEmbed for a YouTube URL. Grep for remaining raw `fetch()` calls in non-web services.

### Slice 2: Payment settlement three-phase refactor (payment-service)

**Priority: HIGH. Estimated effort: 2–3 hours.**

D76 and D77 are the most dangerous confirmed bugs — a Stripe charge can succeed with no audit trail if the DB commit fails. The payout code already demonstrates the correct pattern.

| #   | Diagnosis | File                                         | Fix                                                                                                                                                                                  |
| --- | --------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | D76       | `payment-service/src/services/settlement.ts` | Refactor to three-phase: (1) txn 1 locks tab, inserts `tab_settlements` as 'pending'; (2) Stripe call outside txn with stable key; (3) txn 2 updates with `stripe_payment_intent_id` |
| 2   | D77       | same                                         | Use `settlement-${settlementId}` as idempotency key (requires D76 so settlement row exists first)                                                                                    |
| 3   | D78       | same                                         | Add `if (actualAmount < 30) return null` after re-check (Stripe GBP minimum)                                                                                                         |
| 4   | D79       | same                                         | Check for existing pending settlement before creating new one                                                                                                                        |
| 5   | D82       | `payment-service/src/services/accrual.ts`    | Move `publishReceiptAsync` call after `withTransaction` returns                                                                                                                      |
| 6   | D80       | `payment-service/src/index.ts`               | Add `requireEnv()` for `STRIPE_WEBHOOK_SECRET`, `INTERNAL_SERVICE_TOKEN`, `PLATFORM_SERVICE_PRIVKEY`                                                                                 |

**Verification:** Run existing payment tests. Trace the settlement flow with `console.log` to verify txn boundaries. Check payout code for the reference pattern.

### Slice 3: Article access + gate-pass fixes (gateway)

**Priority: HIGH. Estimated effort: 1 hour.**

| #   | Diagnosis | File                                                     | Fix                                                                                                    |
| --- | --------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1   | D91       | `gateway/src/services/article-access/gate-pass.ts`       | Add `AND deleted_at IS NULL AND published_at IS NOT NULL` to article lookup                            |
| 2   | D92       | `gateway/src/routes/articles/gate-pass.ts`               | Inject `req.headers['x-reader-id'] = req.session!.sub!` before `proxyToService` on the key proxy route |
| 3   | D97       | `gateway/src/routes/articles/manage.ts` or resolve route | Add `AND deleted_at IS NULL AND published_at IS NOT NULL` to `/content/resolve` article query          |
| 4   | D98       | migration                                                | Add `CHECK (balance_pence >= 0)` on `reading_tabs`, or add `WHERE balance_pence >= $1` to the UPDATE   |
| 5   | D99       | `gateway/src/routes/subscriptions/`                      | Clamp credit to `Math.min(spendPence, subPrice)` in subscription-convert                               |

**Verification:** Check that gate-pass returns 404 for deleted articles. Verify tab balance can't go negative.

### Slice 4: Editor paywall round-trip (web)

**Priority: HIGH. Estimated effort: 3–4 hours.**

These four bugs interact — fixing them requires understanding the full paywall content lifecycle: compose → save draft → load draft → publish → edit.

| #   | Diagnosis | File                                                 | Fix                                                                                                                                                                                                                       |
| --- | --------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D333      | `web/src/app/write/page.tsx`                         | Pass raw content (with gate marker) to `saveDraft` when scheduling, not `data.content` (which has marker stripped). Either add a `rawContent` field to `PublishData` or re-derive `freeContent + marker + paywallContent` |
| 2   | D334      | `web/src/components/editor/PaywallGateNode.ts`       | Add a `parse.setup` hook that registers a `markdown-it` rule to convert `<!-- paywall-gate -->` back into a `paywallGate` node on load                                                                                    |
| 3   | D335      | `web/src/components/editor/EmbedNode.ts`             | Add `addStorage()` with `markdown.serialize` that writes embed as bare URL on its own line, and a parse hook to reconstruct                                                                                               |
| 4   | D336      | `web/src/app/write/page.tsx` + gateway               | Fetch both `contentFree` and `contentPaywall` from API for the article's own author; concatenate with gate marker. Requires gateway to return `contentPaywall` to the owning writer                                       |
| 5   | D337      | `web/src/components/compose/ArticleComposePanel.tsx` | Guard price input: `Number.isFinite(val) ? Math.round(val * 100) : 0`                                                                                                                                                     |
| 6   | D338      | `web/src/components/editor/PaywallGateNode.ts`       | Use `chain()` for single transaction instead of separate `deleteRange` + `insertContent`                                                                                                                                  |

**Verification:** Create a paywalled article in compose overlay → verify gate marker survives autosave → reload draft → verify gate is present → schedule → verify scheduler publishes with paywall split → edit existing paywalled article → verify paid content loads.

### Slice 5: Compose overlay + cross-posting (web)

**Priority: MEDIUM. Estimated effort: 2 hours.**

| #   | Diagnosis | File                                                 | Fix                                                                                                  |
| --- | --------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 1   | D316      | `web/src/components/compose/ComposeOverlay.tsx`      | Pass `crossPostIds` (mapped to `CrossPostTarget[]`) as 4th argument to `publishNote` in `handlePost` |
| 2   | D317      | same                                                 | Add `media.attachments.length` to Escape key `useEffect` dependency array                            |
| 3   | D318      | same                                                 | On `setMode('article')`, pass current note `content` to ArticleComposePanel and seed TipTap body     |
| 4   | D319      | same                                                 | Wire article mode close button through `handleDismiss` guard or call `flushDraft` before closing     |
| 5   | D320      | `web/src/components/compose/ArticleComposePanel.tsx` | Return `cancel()` from `createAutoSaver`; call in `useEffect` cleanup                                |
| 6   | D322      | same                                                 | Default `sendEmail` to `false` in overlay surface, or add visible toggle                             |
| 7   | D324      | same                                                 | Pass `selectedPubId` to `saveDraft` in autosave and `flushDraft` calls                               |

**Verification:** Cross-post a note with a linked account — verify it actually posts. Press Escape with only images attached — verify confirmation prompt. Switch note→article — verify text transfers. Close article mode — verify unsaved-changes guard.

### Slice 6: Database schema migration (migrations/)

**Priority: MEDIUM. Estimated effort: 1–2 hours.**

One migration file containing all schema-level fixes. No application code changes.

| #   | Diagnosis | Fix                                                                                                            |
| --- | --------- | -------------------------------------------------------------------------------------------------------------- |
| 1   | D54       | `CREATE INDEX idx_subscriptions_publication ON subscriptions(publication_id) WHERE publication_id IS NOT NULL` |
| 2   | D55       | Indexes on `publication_article_shares(publication_id)` and `(article_id)`                                     |
| 3   | D56       | Index on `publication_payout_splits(article_id)`                                                               |
| 4   | D57       | `ALTER TABLE articles/article_drafts` FK → `ON DELETE SET NULL`; `subscriptions` FK → `ON DELETE CASCADE`      |
| 5   | D58       | `ALTER TABLE vouches` both FKs → `ON DELETE CASCADE`                                                           |
| 6   | D59       | `ALTER TABLE trust_profiles/trust_layer1` FKs → `ON DELETE CASCADE`                                            |
| 7   | D65       | 8× `CREATE TRIGGER set_updated_at` for tables missing the trigger                                              |
| 8   | D67       | Add `relay_outbox_prune` cron task (delete `sent` rows older than 30 days)                                     |
| 9   | D63       | Extend `idx_feed_items_score` to include `id DESC`                                                             |
| 10  | D98       | `ALTER TABLE reading_tabs ADD CHECK (balance_pence >= 0)` (if not done in Slice 3)                             |

**Verification:** Apply migration to dev DB. Run `\d+ table_name` to verify indexes and constraints. Run full test suite to confirm no regressions.

### Slice 7: Infrastructure fixes (nginx, docker-compose)

**Priority: MEDIUM. Estimated effort: 30 minutes.**

| #   | Diagnosis  | File                                | Fix                                                                                                                               |
| --- | ---------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D402       | `nginx.conf`                        | Add `https://js.stripe.com` to `script-src` and `frame-src`; add `https://api.stripe.com` to `connect-src`; add `font-src 'self'` |
| 2   | D403       | `nginx.conf`                        | Repeat security headers in `/media/` block or use `include` snippet                                                               |
| 3   | D300 (8.2) | `docker-compose.yml`                | Add `DB_POOL_MAX: 10` for non-gateway services, or `max_connections=200` on postgres                                              |
| 4   | D400       | `docker-compose.yml`                | Add `PLATFORM_RELAY_WS_URL: ws://strfry:7777` to feed-ingest environment                                                          |
| 5   | D401       | `docker-compose.yml`                | Add `ATPROTO_CLIENT_BASE_URL` and `ATPROTO_PRIVATE_JWK` to gateway and feed-ingest                                                |
| 6   | D309 (8.2) | `docker-compose.yml` or cron config | Stagger 02:00 UTC crons: payout → 02:30, external_items_prune → 02:15                                                             |

**Verification:** `docker compose config` to validate YAML. `nginx -t` to validate nginx config. Test Stripe checkout in browser to confirm CSP doesn't block.

### Slice 8: Traffology critical fix + misc gateway fixes (traffology-worker, gateway)

**Priority: MEDIUM. Estimated effort: 2 hours.**

| #   | Diagnosis | File                                              | Fix                                                                                                                                            |
| --- | --------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | D34 (3.4) | `traffology-worker/src/tasks/aggregate-hourly.ts` | Replace `ROW_NUMBER() OVER (...) FILTER (WHERE ...)` with CTE filter or CASE expression. This is currently **breaking all hourly aggregation** |
| 2   | D36 (3.4) | `traffology-worker/src/tasks/resolve-source.ts`   | `ON CONFLICT` on `(writer_id, source_type, domain)` for `findOrCreateSource` race                                                              |
| 3   | D203      | `gateway/src/routes/feeds.ts`                     | Fix sampling_mode mapping: PUT author-volume should map `"random"` → `"chronological"` (matching PATCH)                                        |
| 4   | D109      | `gateway/src/routes/notes.ts`                     | Move ownership check before signing call in note deletion                                                                                      |
| 5   | D110      | `gateway/src/routes/publications/`                | Move `signEvent` before `withTransaction` in publication soft-delete                                                                           |
| 6   | D95       | `gateway/src/routes/publications/cms.ts`          | Wrap CMS PATCH dual-writes in `withTransaction`                                                                                                |
| 7   | D96       | same                                              | Add `RETURNING id` and rowcount check to CMS PATCH                                                                                             |

**Verification:** Check that traffology hourly aggregation actually commits. Test workspace sampling mode via both routes.

### Slice 9: Workspace data layer + UI fixes (gateway, web)

**Priority: MEDIUM. Estimated effort: 2 hours.**

| #   | Diagnosis  | File                                                                            | Fix                                                                                        |
| --- | ---------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 1   | D201 (5.1) | `gateway/src/routes/feeds.ts`                                                   | Move `insertSource` and `add_job` inside `withTransaction`                                 |
| 2   | D206       | `web/src/stores/workspace.ts`                                                   | Add per-entry validation in `readFromStorage` (drop entries where `typeof x !== 'number'`) |
| 3   | D207       | same                                                                            | Add `if (writeTimer) clearTimeout(writeTimer)` at top of `hydrate()`                       |
| 4   | D216/D217  | `web/src/components/workspace/FeedComposer.tsx` + `gateway/src/routes/feeds.ts` | Fix `deleteBlocked` to exclude hidden vessels; add server-side count check returning 409   |
| 5   | D211 (5.2) | `web/src/components/workspace/WorkspaceView.tsx`                                | Fix `refreshAll` stale closure — read vessels via ref                                      |
| 6   | D212 (5.2) | same                                                                            | Fix `handleMergeConfirm` stale closure — use functional updater                            |
| 7   | D246       | `web/src/hooks/useResolverInput.ts`                                             | Add `useEffect` cleanup that increments `genRef.current` and clears `debounceRef`          |
| 8   | D247       | same                                                                            | Add `context` option with default `'subscribe'`                                            |

**Verification:** Test workspace: add/delete feeds, drag-to-merge, resize browser, toggle view modes. Verify no stale data after rapid interactions.

### Slice 10: Test coverage — critical pure functions (shared, gateway, feed-ingest, web)

**Priority: MEDIUM. Estimated effort: 3–4 hours.**

All items are new test files for untested pure functions. No application code changes (except exporting module-private functions).

| #   | Diagnosis  | File to create                            | What to test                                                                                                                |
| --- | ---------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 1   | D326 (7.1) | `gateway/tests/resolver-classify.test.ts` | Export `classifyInput` from resolver.ts; ~30 cases covering all 11 input types                                              |
| 2   | D328 (7.1) | `shared/tests/http-client.test.ts`        | Export `isPrivateIpv4`/`isPrivateIpv6`/`parseIpv6`; test loopback, link-local, CGNAT, multicast, IPv4-mapped v6, public IPs |
| 3   | D329 (7.1) | `feed-ingest/src/lib/sanitize.test.ts`    | XSS vectors: script tags, event handlers, data URIs, protocol-relative URLs                                                 |
| 4   | D351 (7.2) | `web/tests/resolve.test.ts`               | `resolveMatches`/`matchToOptions`/`tagFallback` — native account, external source, RSS, tag dedup                           |
| 5   | D353 (7.2) | `web/tests/collision.test.ts`             | `resolveCollisions` — no overlap, single push, chain cascade, boundary clamp, iteration cap                                 |

**Verification:** `npm test` in each workspace. All new tests pass.

### Slice 11: Design system token sweep (web)

**Priority: LOW. Estimated effort: 3–4 hours.**

Mechanical find-and-replace across ~40 files. No logic changes. Can be split into sub-slices by token type if needed.

| #   | Diagnosis  | Scope                   | Fix                                                                                      |
| --- | ---------- | ----------------------- | ---------------------------------------------------------------------------------------- |
| 1   | D300 (4.1) | 22 files, ~88 instances | Replace hand-rolled `font-mono text-[11px] uppercase tracking-[0.06em]` with `.label-ui` |
| 2   | D301 (4.1) | 24 files, ~73 instances | Replace `text-[13px]` with `text-ui-xs` (non-serif contexts only)                        |
| 3   | D302 (4.1) | 21 files, ~57 instances | Replace `text-[14px]` with `text-ui-sm` (non-serif contexts only)                        |
| 4   | D303 (4.1) | 12 files, ~25 instances | Replace `font-mono text-[11px]` (without uppercase) with `text-mono-xs`                  |
| 5   | D305 (4.1) | ~20 locations           | Fix serif-for-UI violations → `font-sans`                                                |
| 6   | D309 (4.1) | ~15 locations           | Fix form labels → `.label-ui text-grey-400`                                              |
| 7   | D311 (4.1) | 6 components            | Replace hand-rolled toggle groups with `.toggle-chip`                                    |

**Verification:** Visual inspection of affected pages at localhost:3010. Verify no layout shifts or missing styles.

### Slice 12: Frontend accessibility fixes (web)

**Priority: LOW. Estimated effort: 2 hours.**

| #   | Diagnosis  | File                                               | Fix                                                                                        |
| --- | ---------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 1   | D410       | `web/src/components/trust/VouchModal.tsx`          | Add `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, ESC handler, focus trap       |
| 2   | D407       | `web/src/components/trust/TrustProfile.tsx`        | Add `role="progressbar"` with `aria-valuenow/min/max` to dimension bars                    |
| 3   | D423       | Multiple tab components                            | Add `role="tablist"`, `role="tab"`, `aria-selected`, `role="tabpanel"`, keyboard arrow nav |
| 4   | D425       | `web/src/components/social/FollowingTab.tsx`       | Add dialog semantics to unsubscribe modal                                                  |
| 5   | D427       | `web/src/components/trust/TrustPip.tsx`            | Add `role="img"` to span                                                                   |
| 6   | D224 (5.2) | `web/src/components/workspace/VesselCard.tsx`      | Add `role="button"`, `tabIndex={0}`, `onKeyDown` to CardShell                              |
| 7   | D225 (5.2) | `web/src/components/workspace/VesselCard.tsx`      | Add `aria-label="Play video"` to media play button                                         |
| 8   | D428       | `web/src/components/messages/ConversationList.tsx` | Add `<span className="sr-only">Unread</span>`                                              |

**Verification:** Tab through affected components. Test with screen reader if available.

### Slice 13: Cleanup, dead code, docs (various)

**Priority: LOW. Estimated effort: 1 hour.**

| #   | Diagnosis  | File                                             | Fix                                                                                  |
| --- | ---------- | ------------------------------------------------ | ------------------------------------------------------------------------------------ |
| 1   | D6 (0.1)   | all backend                                      | `npx eslint --fix` — clear 176 auto-fixable `no-unnecessary-type-assertion` warnings |
| 2   | D317 (8.3) | `key-service/src/services/vault.ts` + types      | Remove dead `nostrVaultEvent` construction and type                                  |
| 3   | D318 (8.3) | `shared/src/lib/text.ts` or new `escape.ts`      | Export `escapeHtml`; import in feed-ingest                                           |
| 4   | D319 (8.3) | `gateway/package.json`                           | Remove redundant `jose` dependency                                                   |
| 5   | D320 (8.3) | `shared/package.json`                            | Remove redundant `nostr-tools` dependency                                            |
| 6   | D322 (8.3) | `feed-ingest/src/lib/trust-pip.ts`               | Remove `export` from `PipStatus` type                                                |
| 7   | D323 (8.3) | `CLAUDE.md`                                      | Remove stale NoteComposer deprecation note                                           |
| 8   | D324 (8.3) | `knip.json`                                      | Remove 3 stale `ignoreBinaries` entries                                              |
| 9   | D213 (3.3) | `feed-ingest/src/tasks/trust-epoch-aggregate.ts` | Remove unused `applyDecay` import                                                    |
| 10  | D305 (6.2) | `feature-debt.md`, `CODE-QUALITY.md`             | Update stale "no CI" claims                                                          |

**Verification:** `npm test` across all workspaces. `tsc --noEmit`. `npx knip`.

### Slice 14: Commit the working-tree fixes

**Priority: URGENT (should be Slice 0 — do first). Estimated effort: 30 minutes.**

The working tree contains valuable security and correctness fixes that are blocking production. These should be committed as independent, well-described commits and either cherry-picked to master or merged via the workspace branch.

| #   | What                        | Files                                                                 | Commit message                                                                                         |
| --- | --------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1   | Auth hardening              | `gateway/src/middleware/auth.ts`                                      | `fix(auth): harden optionalAuth — check account status + session invalidation, strip identity headers` |
| 2   | Rate limits                 | `gateway/src/routes/auth.ts`                                          | `fix(auth): add rate limits to /auth/verify and /auth/verify-email-change`                             |
| 3   | Email change security       | `gateway/src/routes/auth.ts` + migration 084                          | `fix(auth): hash email change token, add 24h expiry`                                                   |
| 4   | Negative balance prevention | `gateway/src/workers/subscription-expiry.ts`                          | `fix(subscriptions): prevent negative free_allowance via GREATEST(..., 0)`                             |
| 5   | Pagination cap              | `gateway/src/routes/search.ts`                                        | `fix(search): cap offset at 1000 to prevent deep-scan DoS`                                             |
| 6   | Subscribe rate limit        | `gateway/src/routes/external-feeds.ts`                                | `fix(feeds): add rate limit to subscribe endpoint`                                                     |
| 7   | Account deletion            | `gateway/src/routes/auth.ts`                                          | `fix(auth): soft-delete accounts instead of hard-delete to avoid FK violations`                        |
| 8   | Google OAuth status check   | `gateway/src/routes/google-auth.ts`                                   | `fix(auth): check account status on Google OAuth login`                                                |
| 9   | Bluesky OAuth cross-check   | `gateway/src/routes/linked-accounts.ts`                               | `fix(oauth): verify session matches state cookie on Bluesky callback`                                  |
| 10  | Various column/query fixes  | `gateway/src/routes/social.ts`, `trust.ts`, `timeline.ts`, `notes.ts` | `fix(gateway): correct column names, add feed_items cleanup on unpublish`                              |

**Verification:** `tsc --noEmit`. `npm test` in gateway. Diff each file to confirm only the intended fix is included — separate formatting changes into their own commit.

### Recommended attack order

```
Slice 14 (commit working-tree fixes)     — 30 min  ← DO FIRST
Slice 1  (security: SSRF/XSS/injection)  — 1 hour
Slice 2  (payment settlement refactor)    — 3 hours
Slice 3  (article access / gate-pass)     — 1 hour
Slice 4  (editor paywall round-trip)      — 4 hours
Slice 7  (infrastructure / nginx / docker) — 30 min
Slice 8  (traffology + misc gateway)      — 2 hours
Slice 6  (database schema migration)      — 2 hours
Slice 5  (compose overlay)               — 2 hours
Slice 9  (workspace fixes)               — 2 hours
Slice 10 (test coverage)                 — 4 hours
Slice 13 (cleanup / dead code / docs)    — 1 hour
Slice 11 (design system token sweep)     — 4 hours
Slice 12 (accessibility)                 — 2 hours
```

**Total estimated effort:** ~28 hours across 14 slices.
